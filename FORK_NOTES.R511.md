# R511 — 宏观分析「现在」卡: 四维拆解改回四行上下叠

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|---|---|---|---|
| R511 | 用户:「四个维度用回之前四行的样子」。R510 把四条排成一横排、条子定宽 96px, 用户要回 R505 之前那张小卡的排法: 一维一行, 四行上下叠, 条子撑满。R505 把几张卡并成「现在」一张卡后, 四维这一栏是 1fr, 宽屏上会很宽, 所以整组加 max-w-xs 封顶 320px, 条长与原来四张并排小卡里的那一张相当, 不会再出现 R510 那种半屏长的条子。R510 修的轨道底色 --elevated、每条的 title、「过滤」按钮都保留。守卫: test_macro_regime_prune_r505 的 R510 那条改成钉四行上下叠 + 封顶宽度 + 轨道可见 | frontend/src/pages/Regime.tsx; backend/tests/test_macro_regime_prune_r505.py | 无 | 可以: git revert 本提交, 回到 R510 的一横排 |
