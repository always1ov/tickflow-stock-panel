# R389 — 策略 AST 安全名单的两个位置性漏洞:换名调用与再导出

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R389 | **承同一轮安全审查(run-1)第三批**。`AIStrategyGenerator._validate_safety` 的**名单本身没错, 错在只在很窄的语法位置上看**, 两个洞互相独立: **① 危险内建只在「直接调用」位置被拦** —— 检查是 `isinstance(node.func, ast.Name) and node.func.id in forbidden_calls`, 所以 `_ev = eval` 那一次 `eval` 出现的是 Load 而不是 `Call.func`, 随后 `_ev("…")` 的 func 名是 `_ev`, 不在名单。改成**凡是 Load 位置的裸名**命中名单就拒 —— 危险内建连「被取到」都不允许, 不只是「被调用」。**② `from X import a, b` 只校验了 X, 从不看 a/b** —— 而白名单里的 `app.backtest.matrix` 是个**真模块**, 它自己 `import os / shutil / json / hashlib / threading / uuid / time` 并 `from pathlib import Path`, 于是 `from app.backtest.matrix import os` 完全合规地把真正的 `os` 绑进策略命名空间; 随后 `os.system(...)` 是 `ast.Attribute` 调用, 上面那条 Call 检查**根本不看属性调用**。也就是说**白名单模块成了它本要排除的那些模块的再导出口**, 而且不需要任何 dunder 遍历 —— 名单里精心列的 `__globals__`/`__subclasses__` 那一串在这条路上一个都用不上。现在对每个被导入的名字问一句「它本身是不是一个未被放行的模块」, 用 `importlib.util.find_spec` **只查不导入**(不执行目标模块任何代码), 查得到且不在白名单就拒; 星号导入单独一条理由。**这两个洞正好落在仓库原有两条测试的外面** —— 那两条钉的是最朴素的 `import os` 和 `from os import path`。**顺带**: `engine.py` 在 `exec_module` 之前那次复校验, 原本把非 `ValueError` 异常**静默吞掉**然后照常 import; 下一步就是执行代码, 所以「复校验没跑成」和「复校验通过了」在日志里必须分得开 —— 改成 `logger.warning(..., exc_info=True)`, 行为(不阻断)不变。**新增 18 条守卫**, 其中**第一条钉的是不许误伤**(正常策略代码必须照过), 还有一条钉住 Store 位置不算取用(`type = 1` 仍合法), 免得新检查被放宽成「见名就拒」。**变异逐一确认会红**: 把 Name-Load 检查还原成只看调用 → 7 红; 把 ImportFrom 的 names 检查整段删掉 → 7 红。 | 改 `app/strategy/ai_generator.py`(Name-Load 检查 + `_is_unallowed_module_name`)、`app/strategy/engine.py`(吞异常改成告警);新增 `tests/test_strategy_validator_escapes.py`(18 条);**`app/strategy/builtin/` diff 全空 —— 一个因子、一个阈值、一个 META 都没碰**;冻结的 `opportunity_score.py`/`score_ledger.py`/`strategy/scoring.py` 未动;**判定、六态、口径、指标色一个没动**;后端 pytest 4273 passed / 10 skipped 退出码 0 | 中(收紧了策略代码的可写范围: 合法策略不受影响, 但若有历史策略用了 `from <白名单模块> import <某模块>` 会在保存/加载时被拒 —— 那正是要拒的) | 可 |

**仍未修、且这一轮修不了的**: `_validate_safety` 是 AST 名单, **不是沙箱**, 模块自己的 docstring 也这么写。真正的隔离要把策略放进受限子进程执行(原文记的「后续 P0」)。这次只是把两个已知的绕法堵上, 不改变「名单挡不住没想到的花样」这个事实。
