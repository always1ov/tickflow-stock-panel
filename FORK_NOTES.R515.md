# R515 — 模拟盘精简: 只展示最重要的(盯着只列快转多 2% 以内, 另撤五处重复或低频的读数)

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|---|---|---|---|
| R515 | 用户:「模拟盘的只需要展示最重要的, 像盯着这部分, 这么多没有精力看」→「我只是举了个例子, 你检查模拟盘页面有哪些不需要展示出来的」, 看过方案图后「确认」。原则: 交易只在收盘转折那一刻发生, 天天要看的只留「要不要动手、手上离清仓多远」。撤掉六处: ① 盯着只列离转多且离触发价 NEAR_BUY(= NEAR_EXIT, 2%)以内的几只(明天收盘站上就是买点), 其余只报一行个数「另有 N 只离转多还差 2% 以上、M 只离转空, 不列出」, 不给展开(用户规模下 99 行 → 约 6 行; 后端照旧全发, 登记进 docs/hidden-features.md「一、隐藏」并加 STILL_THERE 锚点); ② 持仓方块里的走势格(那一段只问离清仓线多远, 要动手卡片上的走势格保留); ③ 今日信号说明里「N 只没进候选池」「持仓 N 只」(不影响动手 / 与分栏数重复); ④ 成绩里「持仓」「最后一天」两格(前者与持仓栏重复, 日期挪进成绩卡说明「截至 …」), 六格变四格, 骨架同步; ⑤ 参数条旁「板块与门槛只改打分的标注」那句, 只留在「回溯」框提示里; ⑥ 页头副标题的刷新节奏, 挪进刷新按钮提示。保留: 板块筛选/体检/门槛(用户说过门槛很重要)、要动手卡片的名次与走势(用户点名要的)、流水、规则、默认收起的净值走势。守卫改钉法: test_flip_fusion(R344 盯着那一档、R362 四格、R365 节奏说明只剩按钮一处、R381 列数; R362 当下两格次序与 R384 标题报没进候选池两条退役留言)、test_flip_layout_r498(四格断点与骨架)、test_flip_tabs_r512(那句说明只在提示里)、test_flip_signals_r513(盯着只列快转多)、test_flip_today(R331 盯着报的数、R342 挑选不混打分) | frontend/src/pages/FlipPaper.tsx; docs/hidden-features.md; backend/tests/test_hidden_features_registry.py; backend/tests/test_flip_fusion.py; backend/tests/test_flip_layout_r498.py; backend/tests/test_flip_tabs_r512.py; backend/tests/test_flip_signals_r513.py; backend/tests/test_flip_today.py | 低: FlipPaper.tsx 是 fork 页面 | 可以: git revert 本提交 |
