# R503 — 宏观分析: 市场环境与情绪周期同页同时展示, 菜单改名

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|---|---|---|---|
| R503 | 用户:「市场环境和情绪周期两组内容不再搞同页切换, 都放在同一个页面一次性同时展示, 但仍共用时间范围。左边菜单市场环境改成宏观分析」。/regime 页撤掉「市场环境 / 情绪周期」那组页签及其 view 状态, 两组上下同时铺开: 市场环境在前(原来的默认页签), 情绪周期在后并与上一组隔开一段; 每组加一个 L2 组标题(新组件 GroupTitle, 比卡片标题 SectionTitle 高一级, 标题不许断、说明放不下整块换行, 沿用 R401 的规矩), 原来由页签承担的「看的是哪一组」改由组标题说清。页头那组时间范围(1年 / 2年 / 全部 / 自定义)与重算按钮不动, 仍同时管两组。三张图的 useEChart 依赖里去掉 view(页签撤了就没有 display:none → 可见的切换, resize 留作兜底)。菜单与页名改为「宏观分析」(侧栏、菜单设置默认表、AI 对话的页面上下文), 路由 /regime 不变所以存过的菜单排序与隐藏不受影响; 两处把它当页面名渲染的文案跟着改(连板梯队 AI 复盘的数据来源说明、后端「阶段历史不足」提示)。「市场环境」仍是第一组内容、数据页卡片、回测「市场环境过滤」的正名(指 regime 数据), 没有动; 名词表只收「市场环境页」这一个别名。数据、算法、接口一个字没动。守卫: 新增 test_macro_page_r503.py 5 条(没有页签且两组常驻、顺序与组标题、组标题层级与不断行、仍只有一组时间范围、页名与菜单同名); test_terminology 加一行; 全量验证通过 | frontend/src/pages/Regime.tsx; frontend/src/components/Layout.tsx; frontend/src/pages/settings/MenuSettings.tsx; frontend/src/custom/assistant/AssistantLauncher.tsx; frontend/src/components/LadderAiReview.tsx; backend/app/api/regime.py; backend/tests/test_terminology.py; backend/tests/test_macro_page_r503.py(新增) | 低(Regime.tsx 只是把两块 JSX 换了外壳与顺序, 块内一行未改; 上游改块内内容仍能直接合, 改页签那几行会冲突, 保留本侧即可) | 可以: git revert 本提交 |
