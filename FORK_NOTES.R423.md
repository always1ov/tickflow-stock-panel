# R423 — 量化MACD 副图去掉纵轴刻度数字

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R423 | 用户问副图纵轴那几个数是什么意思, 答明之后: 「有意义吗, 没意义就去掉, 对我帮助大不大, 我看了又用吗, 会看吗」。那几个数是 DIFF / DEA 的值(价格差, 单位元), **跟股价挂钩, 不同股票之间没法比**; 看共振用的是柱子在 0 上还是 0 下、金叉死叉、黄柱, 一个数都不用读; 0 轴也不必标 —— 每根柱都从 0 长出来, 柱根就是 0。所以副图纵轴的刻度数字与刻度线一并去掉, 删掉只为它服务的 `fmtSub`。纵轴本身保留(ECharts 需要它定比例), 左边距不变, 与主图对齐不受影响。 | 改 `frontend/src/components/stock-analysis/AnalysisKChart.tsx`;**后端、打分系统、`strategy/`、六态 diff 全空**;vitest 154 passed、vite build 0;亮 / 暗出图核对 | 低 | 可(恢复 axisLabel 与 `fmtSub`) |
