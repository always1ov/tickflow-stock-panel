# R416 — 量化MACD 副图: 亮色主题也逐字用通达信四色, 去掉多画的 0 轴线

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R416 | 用户: 「显示必须得和通达信显示的一样, 空柱子就要空柱子, 不能画蛇添足, 也不能删除」「颜色和柱子类型都得一样」。对照 R415 出图找出两处**不一样**: ① **亮色主题的绿与黄被改过**(R415 为白底对比度把 00FF00 压成 009600、FFFF00 压成 AA8C00)—— 那是自作主张, 撤回: `QUANT_MACD_COLORS` 从按主题两套改成**一份**, 画法函数 `quantMacdSeries` 不再收主题参数, 亮暗都是 FF0000 / CC0000 / 00FF00 / FFFF00(白底上黄柱淡, 是这组颜色放到白底上本来的样子); ② **多画了一条 0 轴横线** —— R415 把日期刻度挪到副图底下时打开了副图类目轴的轴线, ECharts 把它画在 0 处, 原文没有这条线, 关掉。柱子类型本来就对(DIFF 实心 / DEA 空心 / 黄柱实心, 宽度 2 同位叠放), 未动。测试改为钉「只有一份颜色表且逐字等于通达信」「所有系列都不响应悬停、只有柱与图标两种」。 | 改 `frontend/src/lib/theme.ts`、`lib/quantMacdSeries.ts`、`lib/quantMacdSeries.test.ts`、`components/stock-analysis/AnalysisKChart.tsx`;**后端、打分系统、`strategy/`、六态 diff 全空**;亮暗两主题出图逐像素取色: 副图只剩 FF0000 / CC0000 / 00FF00 / FFFF00 与抗锯齿过渡色, 0 轴线的灰色消失 | 低 | 可(恢复按主题两套颜色与轴线即回到 R415) |
