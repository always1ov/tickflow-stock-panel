# R534 — 设置: 通知合到一栏(推送通知 + 通知弹窗 + 语音播报)

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|---|---|---|---|
| R534 | 用户:「通知设置合到一栏」(R533 汇报里提过这一处没合)。设置新增「通知」栏, 排在「实时监控」后面: 左边「推送通知」(飞书 / 企业微信 / 钉钉 / 第三方 / 邮件 / 企微智能机器人, 原在「实时监控」), 右边「通知弹窗」「语音播报」(原在「系统」)。**三张卡原样整块搬**: 推送卡连同它自己的状态、保存、校验、测试发送抽成 `Monitoring.tsx` 导出的 `PushChannelsCard`(仍复用本文件的 Card / ToggleRow / TestSendButton / HighlightContext); 弹窗与语音连同本地偏好读写抽成 `System.tsx` 导出的 `AlertPopupSettings`; 新文件 `settings/Notifications.tsx` 只负责把两块摆成两列。唯一的行为差异: 弹窗开关原来挂着「策略页」那个开关的落库忙碌态 `disabled={saving}`, 与弹窗无关, 搬出来就没了。深链: 站内两处「前往设置配置」(规则编辑器、复盘定时推送)改指 `?tab=notifications&highlight=webhooks`; 老书签 `?tab=monitoring&highlight=webhooks` 由 Settings.tsx 自动转到「通知」栏, 定位与闪烁照旧; 复盘里「复用「设置 → 实时监控」的渠道配置」改成「设置 → 通知」。冒烟: 老链接转栏、飞书渠道展开、弹窗个数写入本地偏好都实测通过。守卫: test_settings_r533 追加三条 R534 | frontend/src/pages/Settings.tsx; frontend/src/pages/settings/Notifications.tsx(新); frontend/src/pages/settings/Monitoring.tsx; frontend/src/pages/settings/System.tsx; frontend/src/components/monitor/RuleEditor.tsx; frontend/src/pages/Review.tsx; backend/tests/test_settings_r533.py | 中: Monitoring.tsx / System.tsx 有上游底子, 这两张卡在上游仍在原处; 同步时以「卡片整块在导出组件里」重放, 卡内改动照搬 | 可以: git revert 本提交(R533 的分栏不受影响) |
