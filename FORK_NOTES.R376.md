# R376 — 同步上游 v0.3.0(作者强推重写历史)

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R376 | **同步上游 v0.3.0 —— 作者把历史整个重写并强推, 两边没有共同祖先**(用户: 「找出最优解合并更新」)。`git fetch` 报 `48ddc68b...0cd46f9 main -> upstream/main (forced update)`, 而 `git merge-base HEAD upstream/main` **退出码 1、输出为空**: 新的上游根提交是 `d93dc0cb Initial release v0.1.19`, 整条 862 个提交全是新对象。**直接 merge 会退化成「无基三方合并」** —— git 只能拿空树当基, 于是 fork 这 999 个提交碰过的每一个文件都会变成冲突, 几千行要人肉裁决, 而裁错一行不会有任何东西报错。**最优解不是硬合, 是把基找回来**: 先比 `git diff --stat 48ddc68b upstream/main` 确认**代码本身**只差 91 个文件 / 7739 行且几乎全是新增 —— 也就是说作者重写的是**历史**不是代码, 上次同步点 `48ddc68b` 的内容仍然是新上游的真祖先, 只是提交对象换了。于是造一个**合成提交**: 树 = `upstream/main` 的树, 父 = `48ddc68b`(`git commit-tree`), 再合并它。两条自证当场跑过: `tree(合成) == tree(upstream/main)`、`git merge-base HEAD 合成 == 48ddc68b`。这么一来 git 拿到了正确的基, **冲突从「几千行」降到 2 处**, 且两处都是纯 import 排序(`api/kline.py` 上游那块把 fork 已排好的 `is_valid_ext_ident` 又写了一遍; `api/watchlist.py` 上游新增 `datetime` 而 fork 这边 `Callable` 已按 ruff 迁到 `collections.abc`)—— 与之前每一次同步同一个形状。**这条经验必须留在这儿**: 上游以后再强推还得这么做, 不知道这一手的人会以为只能弃用 fork 重来。**上游这一轮带进来的**: AI 对话助手整个新模块(流式对话 + 18 个只读工具 + 自动附图, `app/custom/assistant/**` 与 `src/custom/assistant/**` 全是新文件)、自选页「加入日期/加入以来」与 7 个行情列、6 处盘中指标与盘后同口径的修正(MACD/RSI 暖机、BOLL 样本标准差、60 日极值按收盘、递推窗口按交易日)、ETF/指数的行情与回测资产路由修正、3 处 UTC 主机时区修正、parquet 原子写。**作者的东西一个字没改**(那 2 处 import 除外)。**fork 增强逐项点过名**: `_monthly`/`_window_bars`/`FlipMonth`、`LevelsDialog`、PWA 那 6 个文件、浅色蓝白 token、`_serve_root_file`、两个 R369/R370 守卫 —— 全在。**`backend/app/strategy/builtin/` 本次合并零触碰**; `strategy/monitor.py` 只落了上游那 42/7 行, fork 自己的改动原样保留。**合并后自己抓到两件事**: ① 守卫 `R319 市场时钟只有一处产地` 红了 —— 上游新增的 `lib/format.ts` 的 `cnDateFromUtc`/`cnDateTimeFromUtc` 带了 `Asia/Shanghai`, 但它们只把**传进来的**时间戳渲染成北京时间, **自己从不取「现在」**。立论说的是「谁都不许自己换时区判时段」, 纯格式化器本来就不在其内(这条区分早就写在守卫自己的注释里)。所以**不是放行而是收紧**: 判据从「文件里出现 Asia/Shanghai」改成「把**当前时刻**喂进时区格式化器」(`.format(new Date())` / `.formatToParts(new Date())`), 并加第二张**纯格式化器**名单同样双向自证 —— 新冒出来一个就得先过目, 不许靠「曾经允许过」混进来。② 上游新的 AI 助手悬浮球带一圈**写死成蓝色字面量的辉光阴影**, 撞上 R368 那条「不使用发光效果」, 且绕开了 `--accent` 令牌; 换成普通抬升阴影, 层次留着辉光去掉。(R368 那条发光守卫只扫 `shadow-[0_0_…]` 这种正中光晕, 带偏移的辉光漏网 —— **和当初漏掉 `bg-[radial-gradient(…)]` 是同一类洞**, 连同另外几处存量一起在 R377 补。)**4 处变异逐一确认会红。**本条原编号 R371, 推送时发现分支上已有他处提交的 R371~R375(且台账体例已改成「每条一个文件」), 于是让号到 R376 并跟着改用新体例。 | 合并 93 个文件 / +7792 / -163(上游 91 个 + fork 自己改的 2 个);改 `tests/test_today_health.py`(R319 判据收紧 + 纯格式化器名单)、`src/custom/assistant/ui/AssistantFloatingButton.tsx`(去辉光);**`backend/app/strategy/builtin/` diff 全空**;后端 pytest **4206 passed / 10 skipped 退出码 0**(R370 时 4052, 上游带进来 154 条), 前端 tsc 0 / eslint 0 error / build 通过 / vitest **25 条全过**(上游带进来 19 条) | 中(上游一次带进来一个完整新模块与 6 处指标口径修正 —— 但 fork 侧只有 2 处 import 冲突, 判定、打分、六态、模拟盘一个字节没动) | 否(整条上游历史进来了, 单独回退这一条等于放弃本轮同步) |
