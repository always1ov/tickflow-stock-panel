# R443 — 关键价位: 作者的指标回到作者原色, 自定的另选深色

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R443 | 用户: 「关键指标的颜色还是用作者原来的颜色, 然后自定的你再帮我选深色的」。R409 / R424 两轮重排作废: ① 作者的 10 组(压力支撑 #F97316、枢轴点 #8B5CF6、前高前低 #EAB308、布林带 #F97316、量化通道短 / 中 / 长 #06B6D4 / #22D3EE / #67E8F9、ATR波动通道 #EF4444、斐波那契一型 #F59E0B、整数关口 #71717A)逐字取自上游 `upstream/main` 的 `AnalysisKChart.tsx`, 两套主题同一个值(作者就是一个值); 作者单独配过色的两条曲线(布林中轨 #FB923C、ATR上轨 #F87171)也照回, 放在 `LEVEL_CURVE_COLOR`。作者原样就有的撞色(压力支撑 = 布林带)、偏浅(通道长期)、ATR 像阳线, 不再替他改。② 缺口位作者原色 #EC4899 是粉, 全站禁粉(AGENTS.md 第 15 条), 归到自定的那一类。③ 自定的五种线另选深色(亮色 / 暗色): 六态关键点 蓝 #1D4ED8 / #3B82F6、斐波那契二型 深青 #115E59 / #0F766E、二型上攻推算位 天蓝 #0369A1 / #0369A1、二型这组作废 暗褐黄 #854D0E / #A16207、缺口位 靛 #3730A3 / #4F46E5。作者的颜色占掉了橙黄金紫青红灰, K 线又占掉红绿, 剩下这五族; 深绿 / 橄榄绿试过, 离「跌」绿只有 ΔE 8 多, 会被读成下跌, 不用。量出来彼此 ≥ ΔE 10、离作者色 ≥ 9.8、离 K 线涨跌色 ≥ 10.4、对页底对比 亮 ≥ 5.5 / 暗 ≥ 3.1。后端 `dinapoli.py` 的三个角色色值跟着换(它们兼作前后端的角色键), 旧键收进 `FIB2_ROLE_RETIRED`, 缓存里的旧响应仍画对色。守卫: `test_level_palette.py` 撤掉 R409 / R424 那 6 条「全体两两分得开 / 一律不浅」, 新增 `test_R443_作者的指标就是作者原来的颜色`(逐字比对上游原色与两条曲线色, 并钉图上确实用了曲线色)与 `test_R443_自定的是深色_不粉_分得开`; 变异 9/9 杀死 | `frontend/src/lib/theme.ts`、`frontend/src/components/stock-analysis/AnalysisKChart.tsx`、`backend/app/indicators/dinapoli.py`、`backend/tests/test_level_palette.py` | 中(配色表是 fork 自己的, 但与上游 `AnalysisKChart.tsx` 的 LEVEL_GROUPS 同源; 上游改色时要同步 `AUTHOR` 那张表) | 回退本提交即回到 R424 的整张重排 |
