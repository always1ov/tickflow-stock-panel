# Fork 改动台账(重建版)

> 本 fork 策略(2026-08-14 重建):基于作者 `upstream/v0.2` 干净代码,**逐个**重加增强功能,
> 每加一个经用户实测验证后再加下一个。数据源铁律:**只用 TickFlow,不引入第三方数据源**。
> 完整历史台账见封存分支 `claude/docker-image-build-trigger-7ryn7i` 的 FORK_NOTES.md(49 条)。

## 存档版本(可回退基线)

| 版本 | 分支 | 提交 | 镜像 | 功能范围 |
|------|------|------|------|---------|
| v6 | `claude/archive-fork-v6-20260829` | `24ce014` | `:24ce014` | R1~R81 全部(AI 操盘手/今日总览/多 AI 档位/盘面参考分组/分钟策略与分钟回测/牛来/异动监控/外部网页/并发与存储加固/测试全绿 1883) |
| v1 | `claude/archive-fork-v1` | `6df359c` | `:6df359c` | R1~R4(整批移植基线) |
| v2 | `claude/archive-fork-v2` | `5e862ec` | `:5e862ec` | R1~R10 全部(六态/回测调参/出场线/生命线/今日总览:把握分筛选+可配门槛+AI 导读·优选合一+导出) |
| v5 | `claude/archive-fork-v5-20260820` | `cfbc752` | `:cfbc752` | R1~R35 全部(个股分析改版/板块跷跷板/六态翻转触发价 + PRD 收口/AI 自动挖掘/量化研究与策略回测 AI 代跑/多 key 错峰抖动与每轮启用数) |
| v4 | `claude/archive-fork-v4-20260819` | `fdad334` | `:fdad334` | R1~R24 全部(合并作者分组功能之前: 含情绪周期阶段修正/盘中实时预留/版面重排) |
| v3 | `claude/archive-fork-v3` | `8e7a329` | `:8e7a329` | R1~R22 全部(合并上游 v0.2 之前的最后状态: 大盘红绿灯/仓位建议/RS/建仓路径/实时链路/批量推送/AI 容错) |

> 回退方式:`docker compose` 镜像 tag 改为对应版本, 或从存档分支重建 `claude/upstream-clean`。

## 上游基线与升级约定(按 docs/secondary-development.md §8)

- **当前上游基线**: commit `4d27f31`(upstream/main, tag `v0.2.2`), 于 R92 并入; 此前 `afbf432`(R82)、`e346e25`(R78)、`196af2f`(R71)、tag `v0.2.1`=`17ca245`(R69)。
  每次同步上游后更新这一行 —— 守则要求二开分支记录确切 Tag/commit, 不能只写"基于 v0.x"。
- **升级预检**: 下次同步上游前先跑只读预演 `python3 scripts/upgrade_check.py <目标Tag>`,
  它会列出双方都改过的文件和可预见的文本冲突; 预演不动工作区, 但只看已提交内容。
- **高冲突热点复核清单**(守则 §5.2 点名的文件里, 本 fork 动过的 + 每次合并上游后必须复核的点):
  - `backend/app/main.py` — fork 接线: 3 个路由(today/workflows/paper_trading)、操盘手定时安装、
    工作流节拍器、数据持久化自检、操盘手账本 `StoreError`→503。复核: 路由都还挂着、scheduler 注入还在。
  - `backend/app/strategy/engine.py` — `watchlist_only` 注入(context.watchlist_symbols, 未注入退化为全市场)。
    对应守则里"按需扩展"的 `CandidateFilter` 接口 —— 出现第二个过滤类需求时应提取成正式接口。
  - `frontend/src/router.tsx` — 根路径→/today、/dashboard、fork 页面路由。
  - `frontend/src/components/Layout.tsx` — nav 数组的 fork 项、「盘面参考」分组(R57/R67)。
  - `frontend/src/lib/api.ts` — fork 端点全部加在此(守则要求的单一客户端, 增长是预期内的)。
  - `frontend/src/lib/queryKeys.ts` — fork 键集中在文件尾部 `[fork 增强]` 段(R70)。
  - `backend/app/backtest/engine.py` — R83 按作者分钟回测实现扩展了分钟数据入口；同步上游时复核分钟/日线回测路由与 fork 既有任务链均完整。

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R92 | **同步上游 v0.2.2(afbf432→4d27f31, 56 提交), 一处明确背离作者**: 大头是 feat/capability-routing 分支入正史(R81/R83 已按分支状态先行整合, 本次接上历史), 真正的新内容照单全收 —— TickFlow 取数列式直转提速(as_dataframe=False)+除权因子 UTC 偏移修复、分钟增量两段式重构(intraday.universe 单请求稳态 + batch 全天修复, 间隔 [3,120] 默认 6s, 上游整文件接管)、单块失败重试的全市场脉冲、能力路由矩阵 UI(数据源页上游正式功能, fork 旧 CapBadge 路由显示让位——R70 先例)、复盘接入 fuyao 龙虎榜/盘前风向标上下文、弹窗分时档位按分钟源深度收窄、监控中心"实时不可用"横幅(适配 fork 版式)、首用引导改版、图表角标关闭。**背离**: 上游 v0.2.2 砍掉免费档自选实时("free=无实时, 用 fuyao 补"), 本 fork 明确保留 —— watchlist 档、多 key 轮转拉取链(_fetch_watchlist_quotes/_quotes_to_records/get_realtime_watchlist_symbols)、监控自选档、放量编辑器"非全市场模式不触发"提示全数保住, realtime_mode 注明背离原因(fork 主干功能, 用户即此形态运行)。放量差值保 fork 实现(金额缺失 fail-closed + 跨轮距守卫, 为上游超集), 状态声明并入上游 _holiday_active 探针; 规则指纹保 fork"改配置重置冷却"。Keys 页被上游收编进数据源页, fork 多 key 清单(R58 TickflowKeys)嵌入新 TickFlowKeySection 顶部。AI 设置保多档位 Profiles、Review 保钉钉、Screener 保摘要→明细顺序与 columnsReady 门闩、监控卡片保 R60/R89 版式。测试: 矩阵/ETF 计数保 fork(+factor_rank_research), ETF 采上游新形状; 放量测试 fork 超集; watchlist_batch 测试保留(上游随功能删除); 上游新测试 test_realtime_mode 按 fork 语义改写并注明背离。后端全量 2051 passed / 0 failed; 前端 tsc/vite build 过、ESLint 0 error(顺手修 Review 页 _DtStockTable 下划线命名触发的 rules-of-hooks) | 29 个冲突文件(quote_service/preferences/monitor*/minute_refresh/kline_sync/market_recap/settings/worker + 前端 13 文件) + 大量自动合并 | 高(实时链/分钟链/监控链全动过, 已逐处核对 fork 增强完整) | 回退 merge commit(会同时退掉 v0.2.2 全部新功能) |
| R91 | **外部网页(利弗莫尔趋势)移出「盘面参考」分组**: R88 把它归进了分组, 用户要求回到一级菜单且可拖动。分组成员资格集中于 `BROWSE_GROUP.paths`(R67 设计), 从数组移除 `/external-page` 即完成 —— Layout 与菜单设置共用同一 `isBrowsePath` 判定, 该项自动回到顶层并参与 `nav_order` 排序。已存过顺序的用户: 它出现在原分组块之后的位置(存档顺序里它就在那), 可在设置里拖到任意位置 | `frontend/src/lib/navGroups.ts` | 无(单数组一行) | 把路径加回 `paths` |
| R90 | **AUTH_DISABLED 免登录开关(用户明确选择, 风险已告知)**: 部署在 Cloudflare Access 等外层门后面时, 面板自己的登录页成了第二道多余的门。新增环境变量 `AUTH_DISABLED=1`: 认证中间件对 `/api/` 完全放行(公网也放), `/api/auth/status` 一律报告已登录(手动打开 `/login` 会被前端弹回首页——前端零改动, 复用其"已登录即跳转"逻辑)。安全边界: 默认(未设置)时行为与从前逐字节一致, 有专项测试钉死(未设密码公网 403 / 已设密码无会话 401 / status 字段原样); 开启时启动日志打大字裸奔警告(明文 key/清库/仓位对全网开放)。回退路径: 删掉环境变量重启即恢复, auth.json 里的密码原样保留 | `config.py`、`main.py`(中间件+启动警告)、`api/auth.py`(status)、`tests/test_auth_disabled.py` | 无(默认关闭, 不碰上游认证逻辑本体) | 删开关三处 + 测试文件 |
| R81 | **同步上游 1 提交(回测子进程收尾修复)**: 上游 afbf432 与本 fork 另一会话的 937411b 是**同一个 bug 的两份修法**(终态结果已送达但子进程退出收尾超 10s 时, 不再把成功结果当失败丢弃): 子进程侧 close+join_thread 冲刷两边一致; 父进程侧 fork 版是超集(多一层 join 后兜底排空, 盖住"先看到 Empty 再发现进程已死"的另一个竞态, 带回归测试)。冲突解法: 注释措辞取上游对齐(减少未来 diff), 功能与测试保留 fork 版 | `backtest/worker.py`、`tests/backtest/test_worker_process.py` | — | 回退 merge commit |
| R89 | **参考 FluxDown 重构全局字体、字距与控件密度**: 以 `zerx-lab/FluxDown@3e63139` 的 `web/src/{design,index}.css` 为排版依据，建立中文优先字体回退链（MiSans→HarmonyOS/PingFang/微软雅黑 UI→系统字体）、数字/代码等宽栈、13px 主字号/12px 辅助字号、语义字号行高与克制字距；常用 `h-9` 控件统一为 32px、输入圆角统一 6px，页面标题栏固定最小 52px 并处理长副标题省略，侧栏顶级菜单收至 32px 行高，设置页区块标签从 0.2em 过度字距收至 FluxDown 同档的 10.5px/0.045em。未复制 FluxDown 的字体二进制或颜色主题，不新增远程字体请求；显式像素字号、K 线/分时/图表画布和业务表格列宽保持不动，实时、策略、监控及后端零改动。后端全量可执行范围 `1872 passed`；前端 TypeScript + Vite 生产构建通过，定向 ESLint 0 error | `frontend/{tailwind.config.ts,src/index.css}`、`components/{Layout,PageHeader}.tsx`、三个设置页区块标题 | 低（全局排版令牌会影响语义字号/常用控件高度；金融图表和数据逻辑不变） | 回退本提交恢复 R88 的字体与间距 |
| R88 | **可配置外部单页嵌入“盘面参考”**: 复用「设置 → 扩展页面」增加外部网页名称、HTTP(S) 地址和启用开关，默认启用用户指定的利弗莫尔趋势页；启用后动态加入「盘面参考」分组，沿用现有折叠、隐藏和组内排序。新增独立 iframe 页面，提供刷新、设置和新窗口兜底；浏览器直接连接目标站，后端不代理、不读取第三方内容，也不转发 TickFlow 数据/API Key/牛来登录凭据。iframe 使用 sandbox、严格 referrer 策略，后端拒绝非 HTTP(S) 及 URL 内嵌账号密码；目标站若以后设置 X-Frame-Options/CSP 禁止嵌入，仍可新窗口打开。旧 preferences 缺字段自动取默认值，实时、策略、监控和数据链路均未修改。后端全量可执行范围 `1872 passed`，新增专项 `7 passed`；前端 TypeScript + Vite 生产构建通过，定向 ESLint 0 error | `services/preferences.py`、`api/settings.py`、`lib/{api,navGroups}.ts`、`components/Layout.tsx`、`pages/{ExternalPage,settings/ExtPages,settings/MenuSettings}.tsx`、`router.tsx`、专项测试 | 低（独立展示页与偏好配置；不接触金融数据/实时服务） | 删除 `/external-page` 路由和菜单接线、外部网页设置及对应偏好字段 |
| R87 | **策略监控卡片显示真实策略名 + 全系统更名“牛来”**: 监控规则卡片旧逻辑先按 `scope=symbols` 渲染，导致限定标的的策略监控显示第一只股票代码/名称而非策略名。现对 `type=strategy` 提升显示优先级，复用完整策略池按 `strategy_id` 解析日线、分钟、自定义及叠加策略的当前名称；保存策略规则时后端同时将规则名规范为 `策略监控 · {策略名}`，保证后续卡片与通知口径一致，既有规则无需迁移即可正确显示。用户可见品牌名、浏览器标题、登录/引导、导出页脚、桌面安装显示名、后端应用标题及相关开发文档统一由 Tick Stock Panel/TickFlow 股票面板改为“牛来”；内部 TickFlow 数据源名、API、镜像、可执行文件名和旧数据迁移目录保持不变，避免破坏部署与兼容性。后端全量可执行范围 `1865 passed`，监控专项 `26 passed`；前端 TypeScript + Vite 生产构建通过，定向 ESLint 0 error | `frontend/src/pages/Monitor.tsx`、`backend/app/api/monitor_rules.py`、品牌展示/打包/文档文件、新增策略名称回归测试 | 低（监控执行条件与实时链路不变；只修显示优先级、保存名称和品牌文案） | 回退本提交恢复原名称和卡片标题逻辑 |
| R86 | **策略结果个股弹窗增加“关键价位”视图切换**: 策略页筛选结果原先只能打开通用日 K/分时详情，查看六态趋势、盘中口径和十一类关键价位必须退出后再去个股分析页。现将个股分析弹窗的既有关键价位主体提取为共享组件，策略详情顶栏增加第三个“关键价位”页签，可在同一股票、同一弹窗内往返日 K/分时/关键价位；复用原 `analysisKline`、`stockLevels`、`stockTrend` 查询键和 R74/R76 单票实时刷新顺序，不新增接口或第二套缓存。入口采用显式 prop，仅策略页启用，今日总览/监控/自选/板块等其他共用弹窗保持原样；切股仍重置为日 K，原 footer 插槽只在其既有日 K/分时契约内渲染。前端 TypeScript + Vite 生产构建通过，定向 ESLint 0 error | `frontend/src/components/StockPreviewDialog.tsx`、新增 `frontend/src/components/stock-analysis/StockLevelsPanel.tsx`、`frontend/src/pages/{Screener,StockAnalysis}.tsx` | 低（纯前端复用与 opt-in 接线，不改后端、数据、实时服务或查询契约） | 删除 `enableLevelsView` 接线与共享组件，将个股分析页主体移回原文件 |
| R85 | **补实时修复的重启/并发兼容闭环**: 复审 R84 时发现两个历史系统高概率边界：(1) 启动 30 秒自检已创建完整性修复并用 `QuoteService.paused()` 暂停行情时，实时开关 API 原先先判 paused、直接返回普通 409，前端拿不到正在运行的修复任务 ID；现改为先检查完整性并接续已有任务，仅没有可接续任务的普通数据同步才返回 409。(2) 容器更新/后端重启会让仅存内存的活跃任务 ID 变成 404；实时开关轮询现在静默识别真实 HTTP 404，重新按磁盘状态创建/接续修复，避免再次掉进 `job not found`。数据门禁、写盘暂停和最多两轮保护均保持不变。新增启动自检并发复现测试；后端全量可执行范围 `1864 passed`，前端 TypeScript + Vite 生产构建通过 | `api/settings.py`、`tests/test_data_integrity.py`、`lib/{api,useSharedMutations}.ts` | 低（只调整门禁判定顺序与任务失联恢复，不改修复算法/行情源/数据格式） | 回退本提交恢复 R84 行为 |
| R84 | **修实时行情“点开即触发修复、修完仍打不开”**: 历史完整性门禁检测到停机前盘中快照时，旧流程只创建/复用修复任务并返回 409，用户必须自行等待、再点一次；任务慢时表现为开关始终回弹。改为结构化返回修复任务 ID，所有实时开关入口统一跟踪任务，成功后重新扫描并自动完成原本的开启动作；失败显示任务错误，两轮后仍有坏快照则停止，禁止无限建任务。修复期间仍不调用 QuoteService、实时写盘仍保持暂停，未放宽任何数据安全门禁；监控设置在等待期间禁用重复点击。现场触发样例：2026-08-27 盘中快照、任务 `957af037fa`。专项 55 passed，后端可执行全量 1863 passed，前端 TypeScript + Vite 生产构建通过 | `api/settings.py`、`tests/test_data_integrity.py`、`lib/{api,useSharedMutations}.ts`、`pages/settings/Monitoring.tsx` | 低（只补门禁后的异步续接，不改行情拉取与修复内容） | 回退本提交恢复“修完手动再点” |
| R83 | **整合作者分钟策略完整功能链，同时保留全部 fork 增强**: 同步 upstream/main `afbf432` 后，按作者 `feat-minute-strategy@8e84304` 的最终状态整合分钟策略执行上下文、日线/分钟统一策略池与周期路由、受 Expert 能力及开关门控的盘中分钟增量刷新、分钟回测/回放、分钟图表实时补取、卡片数据更新闪烁，以及作者后续的 worker 终态竞态、Fuyao/custom provider 百分比口径修正。冲突按“作者功能语义优先、fork 增强不丢”处理：策略页保留 R80 的摘要→明细顺序、旧完整结果占位和原子缓存失效，分钟结果不写入日线摘要/明细缓存；Review 保留钉钉配置识别，AI 设置保留多档位 Profiles；矩阵/ETF 测试保留 fork 额外策略计数。补三项并发/失败安全：分钟分区 read-merge-write 复用 repository 写锁，缺配置状态 fail-closed，worker 收到终态后不再多等一轮；固定两项随日期漂移的完整性测试时钟。TickFlow 单一数据源、多 Key、单票实时隔离、轮询放量监控、今日总览/操盘手/挖掘等 fork 功能均未删除。后端全量可执行范围 `1863 passed`，分钟/相关专项 `109 passed`，worker 终态竞态专项 `2 passed`；前端生产构建由推送后的 Docker workflow 验证 | 后端分钟策略/刷新/回测/worker/数据源适配，前端策略池/回测/图表/设置/监控，相关文档与测试（共约 60 文件） | 高（策略引擎、回测、Screener、分钟写盘均为核心链路，已逐处适配） | 回退本提交（会整体移除作者分钟功能链，R1~R82 保留） |
| R82 | **同步作者回测 worker 收尾修复**: 整合 upstream/main `afbf432`。worker 发送终态消息后显式冲刷队列并立即退出，避免大数据量任务因解释器 teardown 超过 10 秒；父进程在终态消息已送达但子进程仍未退出时强制结束进程并保留真实结果/错误，同时记录 `worker_exit_forcibly` 指标。新增对应回归测试 | `backtest/worker.py`、`tests/backtest/test_worker_process.py` | 低(独立 worker 生命周期修复) | 回退本提交 |
| R81 | **整合作者次要分支的“轮询放量监控”并补安全边界**: 仅移植 `feat-volume-delta-alert` 这一项，不合并整条旧分支。全市场实时行情每轮记录股票累计成交量/成交额，与上一轮完整快照求差；跨交易日、09:30/13:00 时段首轮、非连续竞价、累计量回退均不触发，缺成交额时金额口径 fail-closed，不用 0 伪造增量。监控规则新增手数/金额阈值、价格/市值/当日成交额/ST 基础过滤、5 分钟默认冷却与批量合并通知；仅全市场模式可运行，免费自选轮询不会进入差值状态，编辑器明确提示当前模式是否可用。规则阈值或过滤器修改会重置旧冷却态，避免新配置被旧状态压住。保持 TickFlow 单一数据源，不改策略加载/缓存/SSE 链路 | `api/monitor_rules.py`、`services/quote_service.py`、`strategy/{monitor,monitor_rules}.py`、前端规则编辑/监控展示/API 类型/梯队类型收窄、专项测试 | 中(接入实时行情→监控→告警链，但仅新增临时列与独立规则类型) | 删除 `volume_delta` 类型、快照状态/注入函数及前端表单，移除专项测试 |
| R80 | **修同步时"一直 404 job not found / 502"刷屏**: 链条 —— 运行中的任务只存内存, 后端重启(R77 让用户重启过)后正在跑的任务 ID 消失; 数据页还捏着旧 ID 每秒轮询 `/jobs/{id}` → 每秒 404; 重启窗口内代理层 502。页面其实**有**自愈守卫, 但它用 `/404/` 正则匹配错误**文本** —— 而 `request()` 取后端 detail("job not found")当消息时把状态码丢了, 文本里没有"404", 守卫永远匹配不上; 更糟的是拿到 activeJobId 后历史列表停止刷新, 死循环没有任何出口。修三处: `request()` 把 HTTP 状态码挂上错误对象(err.status, 调用方从此不再靠正则猜文本); 自愈守卫改判 `err.status === 404`(保留文本回退), 清 ID 同时失效历史列表让它接回真实活跃任务; job 轮询对 404 不重试(别拿死 ID 反复撞) | `lib/api.ts`、`pages/Data.tsx` | 低(错误对象加字段, 现有 catch 不受影响) | 还原两文件 |
| R79 | **测试全绿: "13 个缺 SDK 失败"逐个查清, 全部消灭(1785 passed / 0 failed)**: 装上 `tickflow` SDK(就在 PyPI 上)后真跑那批被挡住的测试, 发现"缺 SDK"这个标签下积压着四类完全不同的问题 —— (1) `test_ai_provider` 2 条: 上游测试桩掉底层、没配 key, 撞上 fork 多档位兜底的"未配置即报错"(生产行为正确); 测试补一个空档位(回落全局配置)走到钳制逻辑; (2) `test_watchlist_enriched_join` 8 条: `_FakeRepo` 假件缺 fork 后来加的 `overlay_watchlist_live`/`get_watchlist_live` —— **一直被误归类为缺 SDK, 其实早就该修**; (3) `test_watchlist_batch` 3 条: 测试还在打桩旧接口 `get_paid_realtime_client`(fork 已改多 key 池), 且断言"一次全拉完"的旧语义(R30 轮转窗口后 6 只超容量应分两轮) —— 按轮转语义重写; (4) `test_extensions` 1 条: 容器 fastapi 0.141(新版 include_router 懒包装 `_IncludedRouter`, 不再摊平进 app.routes)与项目锁定 0.136.1 不符, 装回锁定版即过 —— **用户机器上 uv --frozen 用锁定版, 从未受影响**; `test_data_integrity` 1 条: R69 铺的 130 天密度让 enriched 分区数变多, 断言按意图放宽(守"坏分区被删后重算写回", 不守总数)。产品代码零改动 —— 全是测试假件失修与环境错配 | 4 个测试文件 | 无(纯测试) | — |
| R78 | **同步上游 3 提交**: 挖掘两处修复 —— enriched 数据并发更新时**重读快照**而非整轮失败(挖掘跑一半赶上数据刷新不再报废)、市场环境数据缺失时挖掘**启动即失败并前置提示**(不再跑到一半才炸)。零冲突自动合并; 上游另开了 `feat/volume-delta-alert` 分支(开发中, 未并 main, 不动) | 7 文件(挖掘运行时/worker/API + MiningWorkbench) | — | 回退 merge commit |
| R77 | **修 R74/R76 造成的监控/策略污染(用户实况: 异动监控和策略出的票几乎没用)**: 单票按需刷新此前"复用"了自选实时叠加层和 kline_daily —— 但那两层有**隐含契约**: "只含自选、只含当天、只由轮询喂"。三处被污染: (1) **监控引擎**在自选实时档直接拿叠加层当股票评估快照、且把日期强制标成 cn_today(quote_service ~L1301) —— 弹窗随手点过的票混着可能过期的价进去后, 异动监控和策略规则评估的就是一份垃圾快照; (2) 自选页 overlay_watchlist_live 无日期守卫, 盘后快照会被弹窗票的旧行盲目顶掉; (3) `_build_daily` 把日期**无条件标 cn_today**, 非交易时段点弹窗会把"幽灵交易日"分区写上磁盘。另发现 R74 慢的真正大头: 实时关着时 live_agg 为空 → 走全量回退, **为一只票把全市场 90 天 enriched 重算一遍**。修法: 单票刷新**彻底退出共享层** —— 结果只进独立的 `_single_live` 缓存(≤200 条), 日期从行情自带 quote_ts 推**真实交易日**(推不出宁可放弃, 不造日期), 不写盘、不算 enriched(顺带把重算开销归零); 消费者只有弹窗那一族: 日K注入兜底读它(带日期守卫)、六态 live 映射只补叠加层没有的票(不覆盖, 轮询喂的永远更新鲜)。监控/自选页/今日总览回到 R74 之前的干净输入。已污染现场: 叠加层是内存, **重启后端即清**; 盘中写脏的当日分区被盘后管道 batch 覆盖自愈, 幽灵分区极端情况可手动数据修正。测试重写 +2(共享层写入口全装警报器: 单票刷新碰到即失败 / 无 ts 拒绝造日期) | `services/quote_service.py`(`_pull_single` 重写 + `_single_live`/`get_single_live`)、`api/kline.py`(注入兜底)、`services/live_quotes.py`(可选并入)、`api/stock_analysis.py`(传 quote_service)、测试 | 低(共享层回到只由轮询喂; 弹窗功能不变) | 还原这批文件 |
| R76 | **弹窗提速: 实时拉取移出响应路径 + 空闲 key 单独服务**: R74 把"现拉行情"放在了日K响应路径上, 弹窗要白等一次网络才出图(用户反馈"点开加载变慢了")。两处改: (1) `refresh_single_background` —— 认领冷却位后把拉取扔进后台线程, 响应**立刻**回现有数据, 带 `live_refresh` 状态(started=后台在拉/fresh=已是新的/off=没 key); 前端看到 started 在 1.5s/4s 各补取一次把新蜡烛接进来, 第二次必落在冷却期内回 fresh, 不会打转。(2) 单票拉取**挑空闲 key**(用户点的方向): 后台轮询每轮占用池里一部分 key(R35 错峰), 单票再去挤同一个 key 撞上的就是限流等待 —— 轮询侧现在记下本轮占用的下标(`_busy_key_idx`), 单票优先挑没被占的, 全忙/轮询关着时随机摊开。顺手修一个被全量跑测出来的**既有 flake**(改动前也挂): mining_manager 三个 finisher 和 cancel() 都是"先翻终态状态、后落终态事件", 轮询方看到终态去读事件流会撞上"还是 running"的窗口 —— 统一改成**事件先落、状态后翻**, 15 轮连跑无失败。测试 +3(started→fresh 节奏/无 key 说 off/空闲 key 优先) | `services/quote_service.py`(拆 claim/pull + background + `_pick_single_client`)、`api/kline.py`(非阻塞 + live_refresh)、`services/mining_manager.py`(写序)、`lib/api.ts`、`pages/StockAnalysis.tsx`(补取节奏) | 低(refresh_live 不传时行为不变; mining 只动写序不动语义) | api 里改回 refresh_single 同步调用即回 R74 |
| R75 | **修"每天自动同步会被僵尸任务静默挡掉"**: 用户实况 —— 26 号发现有些数据停在 24 号, 手动点同步才更新到 25 号。链条: 卡死任务的自愈(reap_stale)只挂在 `/run` 和 `/jobs/{id}` 两个 **HTTP 端点**上(上游注释"任意轮询都能自愈"), 但轮询只有浏览器开着才发生; 定时同步 15:30 到点时没人开页面, 一个卡死的 running 任务让 `_run_tracked` 的单飞判定天天返回"已有活跃任务"→ **静默跳过, 数据停更**; 手动点同步恰好走先 reap 的那条路, 所以一点就好 —— 而"没人看页面时也能跑"恰恰是定时任务存在的意义。修法: 把前置 `reap_stale()` 放进 `JobStore.create()` 本身(单飞检查前永远先回收), 让**每个**入口(定时管道/开机完整性自检/分钟K按钮/手动同步)统一拿到自愈, 不用逐个记得补; reap 失败不阻断, 按现状单飞。正常推进中的任务(心跳新鲜)照旧去重, 不受影响。测试 +2, 卡死挡 create 那条在旧代码上真实失败。另: "容器 15:30 没开机"那类漏跑, 上游 R69 并入的开机完整性自检(检测缺分区→自动建修复任务)已覆盖, 两个洞现在都有兜底 | `services/pipeline_jobs.py`(create 前置 reap)、`jobs/daily_pipeline.py`(注释)、`tests/test_job_stall_and_cancel.py` | 低(reap 本就设计为可随时调用; 多调一次幂等) | 还原 create() 里那一段 |
| R74 | **个股分析弹窗"点开就是最新"(单票按需实时)**: 弹窗的 K 线只到最后一个同步日 —— 六态条能亮「盘中口径」是因为趋势接口读自选实时叠加层, 但后台实时开关平时是**关**的, 叠加层没人喂, 日K注入(`_maybe_inject_live_candle`)自然没料可捡。修法不是再开一条口径, 而是给 QuoteService 加 `refresh_single(symbol)`: 现拉**这一只**的行情, 走与自选实时**完全相同**的落盘链(建日K行 → 增量 enriched → overlay 进叠加层), 于是日K注入/六态盘中口径/决策台读到同一份数据。约束: 只走 TickFlow(fork 铁律); 每票 **15s 冷却**且失败也占位(反复开关弹窗、坏 key 连点都不烧配额); 无 key/失败一律静默 False, 图表退回收盘口径不报错; 网络请求在锁外、写盘段拿 `_fetch_lock` 与后台轮询串行(§6.2)。接线: `/api/kline/daily?refresh_live=1`(服务端先喂叠加层再返回), 弹窗两处查询带上此参 + `refetchOnMount: always`; 日K回来后补一次 trend/levels 失效 —— 它们是并行发的, 可能赶在叠加层落地之前。测试 +4(无 key 静默/走 overlay 链/冷却短路/失败占位) | `services/quote_service.py`(+`refresh_single`)、`api/kline.py`(+参数)、`lib/api.ts`、`pages/StockAnalysis.tsx` | 低(默认不带参时行为与之前完全一致; 带参失败也只是退回原行为) | 前端两处查询去掉 refreshLive 参数即回 |
| R72 | **按 CONTRIBUTING 全文自查 + 修掉六处并发写隐患**: 上次(R70)查的是二开守则(扩展结构), 这次把 CONTRIBUTING.md 的金融数据契约(§3)、插件化(§4)、并发(§6.2)逐条对到 fork 代码上。**查过没问题的**: 涨停/炸板/连板全取 enriched 预计算列不自己判(§3.2); change_pct 全链小数制口径一致, _fmt_pct ×100 只在展示层(§3.1); 交易窗口按数据行数(交易日)算, timedelta 只用于超取 fetch 范围(§3.3); 信号年龄用同 tzinfo 差值, 无时区混用; fork 服务零处直连 TickFlow SDK, 全走 repo 抽象(§4)。**修掉的**: R68 在操盘手账本上修过的"裸 write_text + 读-改-写无锁", 在 fork 另外六个自建 JSON 存储里原样存在(§6.2 明确要求锁/原子替换) —— 最危险是 ai_signals.json: 今日总览定时、个股分析页、多个操盘手 refresh 并发写同一文件, 撕裂后 load_all 回空、下一次保存把全部信号清空, 和丢操作员同一形状。按守则"出现真实重复再提取"(第 7 处重复)抽 services/json_store.py(lock_for 按文件互斥 + atomic_write_json 临时文件+fsync+os.replace), 六个店(stock_signal/workflow/mining_autopilot_store/seesaw_store/today_ai_store/today_prefs)全部接上; 操盘手账本不迁 —— 它另有 .bak 回退与 StoreError fail-closed 语义且有测试守着。测试 +7, 其中两条并发测试在旧代码上真实失败 | 新增 services/json_store.py、tests/test_json_store.py; 改上述六个 store | 低(键与文件格式不变, 只加锁与原子化) | 还原这批文件 |
| R71 | **同步上游 9 提交(v0.2.1 之后)**: fuyao 同花顺官方实时行情插件(默认 hidden)+插件 Key 界面配置、**数据源能力卡片标签式路由**(每个数据集单独选提供方 + TickFlow 档位要求徽标)、自定义源百分制涨跌幅自动归一小数制、主线维度映射 Series 报错修复、Codex CLI 状态检测实跑 --version、PR #193 的 AI 调用放开 max_tokens、监控新建规则默认通知事件改选股结果、macOS dev.sh 修复。唯一冲突 `DataSources.tsx`: 我们 R58 期自己做过"按数据集逐项路由"下拉, 上游这次把同一件事做成了正式功能且更完整(能力卡片+档位徽标+服务中标识)——按二开守则"不平行实现第二套"**整块让位给上游**, fork 无独有功能损失(明文 key 列表在独立的 TickflowKeys 页, 不受影响)。ai_provider 自动合并干净, 多档位兜底链完好(测试守着) | 22 文件(上游侧); 冲突仅 `DataSources.tsx` | — | 回退 merge commit |
| R70 | **按上游二开守则(docs/secondary-development.md)自查并整改**: 逐条对照后两类结果 —— **整改**: (1) fork 页面的 React Query 键此前散在 14 个文件里内联写(守则 §3.4 明令查询键集中于 `queryKeys.ts`), 其中 `'ai-profiles'`/`'workflows'` 各自重复出现在两个文件 —— 正是改一处漏一处、缓存悄悄失联的形状; 全部收进 QK 的 `[fork 增强]` 段(22 个键), 上游自己的内联键不动(改动最小原则)。(2) 台账补上守则 §8 要求的**上游基线锚点**(v0.2.1=17ca245)、**高冲突热点复核清单**(§5.2 的 7 个文件里我们动过哪些、每次合并后复核什么)和 `scripts/upgrade_check.py` 预检约定。**查过没问题的**: 无绕过 api.ts 的裸 fetch/URL 拼接; 无复制核心页面后长期分叉; 后端无继承大型编排服务(操盘手/工作流/今日总览全是独立模块+组合), AI 调用全走 `ai_provider` 单一链路; 工作流驱动复用挖掘/回测引擎公开入口而非平行实现; `strategy/engine.py` 的 watchlist_only 注入正对应守则"按需扩展"的 CandidateFilter —— 守则明说单用例不建接口, 按 L3 管理并已进复核清单; ruff 报的 RUF100 类噪音上游自身同样触发(main.py 29 条), 属 ruff 版本/配置基线, 不追 | `lib/queryKeys.ts` + 14 个 fork 页面/组件、`FORK_NOTES.md` | 低(键值字符串未变, 只是引用集中; 行为零变化) | 还原这批文件 |
| R69 | **同步上游 v0.2.1→main(38 提交)**: 上游把 v0.2 正式并进 main 并追加一批。拿到的新东西: **异动监控全链路**(偏离值计算/独立监控页 `/abnormal`/系统告警接入 + ST 过滤, 页面已进侧栏与菜单设置)、**AI 生成自定义信号条件**(上游试验性合并的 PR #193 + 12 个测试修复)、**自选分组多组并存**(M:N, `group_ids`, 搜索下拉可把已加标的继续加进其他组)、**同步卡死判定改进度停滞 + 协作式取消 + 执行槽所有权**(慢带宽冷启动不再被误杀; 与我们 R55 撞车, 上游版本更完整 —— 基础设施整体取上游, `kline.py` 三处旧 `JobCancelled` 调用点对齐新 API)、**停机缺口检测与自动修复**(盘中停机次日开实时留下的中午快照不再被"只刷今天"分支永久留存)、僵死 enriched 发布标记修复、ETF/指数矩阵链路不再要求股本字段、监控中心新建标的展示重构、数据源插件化文案与引导页去品牌化。冲突 14 个文件, 原则**基础设施取上游、fork 功能保留**: `daily_pipeline` 取上游完整性自愈的同时**保留 fork 的历史稀疏检测**(上游只扫最近 7 天, 年尺度大洞仍要稀疏检测兜底, 两个互补); `ai_provider` 保留多档位兜底链、叠加上游的输出上限/输入预算检查(预算检查放在进档位循环**前** —— 预算超了换哪档都超, 不该触发兜底); `watchlist` 的缓存失效与上游 `_REVISION` 并存; `watchlist_ai_group`(R26) 适配 M:N。上游自身修了三处: 时区脆弱测试(`date.today()` vs `cn_today()`, UTC 16-24 点必挂)、integrity 测试铺足历史密度避免与稀疏检测打架、`WatchlistGroups` 新代码踩 fork 的 eslint 规则 | 全仓 347 文件(上游侧); 冲突解在 `pipeline_jobs`/`api/pipeline`/`daily_pipeline`/`ai_provider`/`watchlist`/`main`/`kline` + 前端 7 页 | —(这就是同步本身) | 回退整个 merge commit `5709f2c` |
| R68 | **修操作员凭空消失(账本会被写坏, 写坏之后被当成空账本)**: 建了三个操作员, 点一下"改定时", 剩一个。链条有三环: (1) 前端 `<input type="time">` 打一个时间会**分几次** onChange(时段一次、分段一次), 每次发一个 PUT —— 顺带一提, 中间那几次还会把定时短暂地设到 `01:30` 这种莫名其妙的时间上; (2) 同步路由跑在 FastAPI 线程池里、定时任务跑在调度器线程里, 几个请求**同时** `Path.write_text` 同一个 JSON —— 而 `write_text` 是**先清空再写**, 撞上就留下一个半截的文件; (3) **最要命的一环**: `_read()` 把解析失败 `except` 掉、返回 `{"traders": []}` —— 于是紧接着的一次 `save()` 写回去的是一份"只剩当前这个人"的文件。前两环只是**短暂**的损坏, 第三环把它变成了**永久**的删除, 而且全程没有任何报错。三环各修各的: `_write` 改成**原子落盘**(同目录临时文件 → `flush`+`fsync` → `os.replace`, 换之前留一份 `.bak`), 所有"读—改—写"进同一把 `RLock`(名额检查和写入也必须在同一把锁里, 否则同时开的两个会双双通过 `MAX_TRADERS` 检查), `_read` 读不出来时**先退 `.bak`、再抛 `StoreError`**(→ 503)而**绝不返回空**; 另加 `mutate(id, fn)` 让改设置的路由在锁里就地改, 不再"先 get 出来捧着、隔一会儿再 save"(一次 AI 决策要跑好几分钟, 这中间人完全来得及去改设置, 旧副本一回写就把它盖掉了); `save()` 发现人已被删就**不写**(否则一次跑了几分钟的决策会把刚删掉的操作员连账一起复活)。前端把两个输入框改成停手 0.6 秒才发 —— 改成 14:30 本来就该是一次保存, 不是四次。测试 +8, 全部在旧代码上失败(其中并发那条复现出的正是 `pt.get()` 中途返回 `None`, 也就是"人不见了") | 改 `services/paper_trader.py`(`_read`/`_write`/`save`/`delete`/`create`/`reset` + `_LOCK`/`mutate`/`StoreError`)、`api/paper_trading.py`(设置/本金/定时三个路由走 `mutate`)、`main.py`(`StoreError` → 503)、`pages/PaperTrading.tsx`(`useDebouncedField`) | 低(只动这一个功能的存储层; 老账本文件格式没变, 直接能读) | 还原这四个文件; `.bak` 会留在 `user_data/` 里不影响老代码 |
| R67 | **「盘面参考」变成菜单里真正的一行**: R57 建这个分组时, 表头是"挂"在**这一组第一个可见成员**上的 —— 分组本身在「设置 → 菜单」里没有对应的一行。于是三个毛病: (1) 想挪它没得挪, 列表里只有四个成员没有分组; (2) 去拖成员也不一定挪得动它 —— 表头挂在谁身上取决于谁碰巧排最前, 拖了个排在后面的等于白拖; (3) 成员一旦被拖散到顺序两端, 表头和成员会被中间的普通菜单**切开**, 看起来就是"分组卡在中间不动"。现在分组有自己的 id(`group:browse`), 和普通菜单项一样进 `nav_order` 参与排序: **拖它就是拖整块**, 四个成员从顶层抽出来只作它的子项(仍可各自单独隐藏, 组内也能单独排序); 分组行自己也能隐藏 —— 一下藏掉整块。存盘时成员紧跟在分组行后面写进同一条扁平 `nav_order`, 顺序本身就连成一块, 不可能再被切开。**升级不动位置**: 老数据里没有 `group:browse`, 走"新条目插回默认位置"那条路, 默认位置特意放在「个股分析」之后 —— 正是老逻辑算出来的那个位置, 所以已经存过顺序的人看到的位置和以前一样, 只是从此拖得动。顺带把侧栏那段重复的菜单项 JSX 抽成 `PlainNavLink`, 顶层和组内共用一份(原先组内那份是把整段抄了一遍, 加个徽标要改两处) | 新增 `lib/navGroups.ts`(分组定义 + `splitBrowseGroup`/`composeNavOrder`, 两边共用一份定义); 改 `components/Layout.tsx`(分组进 `nav` 数组, 成员从顶层抽出, +`PlainNavLink`)、`pages/settings/MenuSettings.tsx`(分组行 + 组内嵌套拖拽上下文) | 低(纯前端菜单渲染与排序; `nav_order`/`nav_hidden` 后端不校验 id, 无需改接口) | 还原这三个文件 |
| R66 | **操作员能触发新数据(信号旧了让它现场重出)**: R65 补齐了信息面, 但漏了一件事 —— 系统里大部分东西(六态趋势/通道三档/关键价位/日 K 走势)每次都是**现算**的, 本来就不会旧; 只有 **AI 个股信号是缓存的**, 会摆着一个三周前的判断不动。而缓存信号最危险的一点是: **它读起来和今天刚出的一模一样** —— 不标年龄的话, 模型没有任何办法知道自己在拿旧判断当今天的依据。所以: (1) 细看里的 AI 信号一律**带上生成时间与年龄**, 超过 3 天标「已过期, 建议 refresh」; (2) 看盘那一轮除了 `focus` 还能给 `refresh` —— 列进去的会**现场真跑一次 AI 个股分析**再进细看(重出成功的自动进细看名单: 它开口要新信号, 就是打算据此判断)。上限 3 只/次: 每次都是真实开销, 无上限的话模型一句"全都刷一遍"就能把一次决策拖成几十次调用; refresh 同样受"本轮候选 + 自己持仓"的范围约束 —— 那一步会花钱, 更不该被一个范围外的代码触发。**关键的一处**: 重出时把操作员的 AI 档位**放掉**, 走系统默认那条链 —— AI 信号是全局缓存的**系统产物**(个股分析页上人看到的就是它), 用操作员自己的模型生成的话, 这份"系统数据"就变成了那个模型的意见, 而且会被**别的操作员**当成系统数据读到, 等于绕开隔离互相影响; 跑完要还回来, 不还的话紧接着的下单轮会用系统默认模型, 这一天的成绩就记到错误的模型名下了(两条都有测试守着)。测试 +8 | 改 `services/paper_trader_run.py`(+`_signal_line`/`refresh_signals`, `parse_focus` 返回二元组, `run_once` 在细看前先重出)、`pages/PaperTrading.tsx`、`lib/api.ts` | 中(一次决策可能多出最多 3 次 AI 调用; 全部有上限与范围约束, 失败不影响下单) | `run_once` 里去掉 refresh 那一段即回到 R65 |
| R65 | **操盘手的信息面纠偏: 禁的是外部信息, 不是"少给它看"**: R59 把"不能联网"错做成了"只能看一小段摘要"——模型每天只拿到候选清单的一行行摘要, 那考的就不是"这套系统的信息够不够用", 而是"一段摘要够不够用"。界限本来是: **新闻/公告/研报/行情网站一律不许用, 但这套系统自己算出来的东西一样不该藏**。改成**两轮**: 第 1 轮「看盘」给候选与账户, 让它自己挑最多 6 只想细看的; 第 2 轮把系统里关于那几只的东西一次给全 —— 六态趋势(状态/天数/翻转触发价/关键点/系统建议)、Keltner 三档与结论、`summarize_levels` 的十一类关键价位(与个股分析图上画的同一批)、近 20 日走势(带涨停/跌停/炸板标注 + 收盘在均线哪一侧 + 区间), 以及这只票**已有的 AI 个股分析报告**。刻意分两轮而不是一次给全: 所有候选的完整明细会长到把账户和规则都挤出上下文, 而且大半是它根本不打算买的票。**"看图"就是这段走势摘要** —— 模型读不了图片, 但"哪天涨停、收在均线哪一侧、这段区间多宽、缺口在哪"正是人看图时真正在读的东西。细看范围仍受限于**本轮候选 + 自己的持仓**: 放它细看一只不在候选里的票, 等于让它出了这本账的选股范围, 而两本账能对照的前提就是各自只在自己那个池子里选(凭记忆报的代码会被丢掉)。看盘轮失败不让这一天报废 —— 退回只看摘要下单。提示词也改了措辞: 只写禁令会让模型保守到不敢用系统给的东西, 所以明写"系统里的东西你都可以用, 想细看哪几只就说"。测试 +8 | 改 `services/paper_trader_run.py`(+`symbol_detail`/`_kline_story`/`_recent_report`/`_allowed_symbols`/`parse_focus`/`LOOK_PROMPT`, `run_once` 走两轮)、`pages/PaperTrading.tsx`(文案纠正 + 显示细看了几只)、`lib/api.ts` | 中(每次决策多一次 AI 调用; 失败有退路, 撮合与账本未动) | `run_once` 去掉第 1 轮即回到 R59 |
| R64 | **看板收进「盘面参考」, 并从根路径挪走**: 和连板梯队/概念分析/行业分析同一类 —— 打开看两眼有概念, 但不产出任何可执行的东西。除了收进折叠分组, 还把它从 `/` 挪到 `/dashboard`, 根路径改为跳今日总览: 一个展示型的页面不该是每次打开应用第一眼看到的那一页, 决策汇聚层才该在那个位置。`/overview` 的旧跳转跟着指向 `/dashboard`。**顺带修一处会静默失效的地方**: 设置→菜单 里内置页清单还按 `/` 登记看板, 路径改了之后隐藏/排序对它就失效了 —— 那种失效不报错, 只表现为"我明明勾了不显示, 它还在"; 同时补上原来漏登记的「今日总览」和「AI 操盘手」(它们此前同样隐藏不掉) | 改 `router.tsx`、`components/Layout.tsx`、`pages/settings/MenuSettings.tsx` | 低(路由与导航; 页面本身未动) | 把 index 路由改回 Dashboard |
| R63 | **操盘手: 持仓只数上限 + 两本账各自的本金**: (1) **同时最多持有几只**可设(默认 10, 上限 50)。这个上限存在的理由不是"防止乱买", 而是**让成绩可比** —— 一个能同时拿 40 只的账户跑出来的曲线, 本质上是在拿分散度换波动, 和只拿 5 只的账户不是同一回事; 两本账要对照, 这个数就得对齐, 所以它是**操作员级**设置而不是按账设。只挡**新开**的仓, 已有持仓加仓不受限 —— 挡加仓等于逼它去开一只新的, 正好和"控制分散度"反着来。上限会写进给模型的上下文: 不写的话它会开一堆买单、大半被拒, 那一天的决策就废了一半。(2) **本金移到每本账自己身上**, 两本可以设成不同的数(比如全市场那本给多一点看分散效果), 收益率各按各的本金算所以照样可比。改本金会**连带把那本账重置** —— 中途换本金而不重来的话, 收益率的分母变了但历史成交还在, 那条曲线就再也读不懂了; 反过来, 单纯的「重置」**不动本金** —— 我改过的设置不该被一次重置抹回默认值。老账本没有本金字段的就地补上(取操作员那份, 行为不变)。测试 +15 | 改 `services/paper_trader.py`(本金进账本 + `max_positions` + `clamp_max_positions`, `apply_order` 加上限)、`services/paper_trader_run.py`(上限写进上下文并传下去)、`api/paper_trading.py`(+2 端点)、`pages/PaperTrading.tsx`、`lib/api.ts` | 低(有迁移路径与测试守着) | 本金移回操作员级, 去掉 `max_positions` |
| R62 | **去掉 AI 服务商预设, 一律 OpenAI 兼容**: 用户明确"不需要这些玩意选择, 默认兼容 openai 就行, 我自己填链接密钥模型"。删掉两处: AI 档位表里的「套用常用配置…」下拉, 以及设置页那张「快速预设」卡片(DeepSeek / Kimi / OpenAI / 炸鸡中转站 / Codex CLI 的按钮组)。理由不只是"用户不喜欢": 那种清单只在你正好用清单里那几家时省事, 否则**先得从里面挑一个再把三个字段全改掉, 比直接填还多两步**; 而且它会过期(模型名换代、中转站换域名), **过期的预设比没有预设更误事** —— 照着填一遍发现模型不存在, 还以为是自己填错。现在只剩三个输入框: 接口地址 / 模型 / API Key, 占位符改成说明性的("服务商给的模型名, 原样填")而不是某个具体厂商的值。配置卡片标题也不再随匹配到的预设变化 —— 预设都不给选了还显示「DeepSeek 配置」, 会让人以为自己在某个选项里。**注意**: Codex CLI 那一路原来只能从预设按钮进入, 预设去掉后界面上没有入口了(已配置的仍然能跑, 后端逻辑一行没动); 需要的话再单独给它一个入口 | 改 `pages/settings/AiProfiles.tsx`(删 PRESETS 与下拉)、`pages/settings/AI.tsx`(删「快速预设」卡片与 `handlePreset`, 标题固定) | 低(纯删 UI; 后端 provider 字段默认就是 openai_compat, 判定逻辑未动) | 把两处预设列表加回去 |
| R61 | **操盘手: 两本账 / 定时 / 生命线实时例外**: (1) **每人两本独立的账** —— 「全市场」只能从全市场候选里选, 「我的自选」只能从我圈的票里选。这不是两个功能, 是这套系统最想问的那个问题的**对照组**: 我这份自选到底有没有价值。同一个模型、同一天、同一套判定口径, 两条净值曲线的差就是我选股这件事的价值 —— 所以两本账严格分开(各自一份初始资金、现金持仓流水互不相干), 共用任何一样这个对照就废了。全市场候选**复用系统已有的「结论」层**(按成交额粗筛 → 三档通道 → 只留偏买那几档), 刻意不另造一套排序: 两边口径不同的话, 比出来的是"两套规则谁强"而不是"我的自选有没有用"。「需要行动」只进自选那本 —— 那讲的是我自己持仓的风险, 漏给全市场那本等于让对照组看到了我的持仓。老数据就地迁移进**自选**那本(之前那版的上下文本来就是自选口径, 记到全市场会把成绩安到错误的对照组上)。(2) **定时** —— 长期观察靠人记得点按钮是不成立的, 漏几天就是净值曲线上几个说不清的缺口。默认**关闭**且默认时间在收盘后(收盘价出来了才有得算); 一次定时**先查生命线再让 AI 决策**, 顺序不能反: 清仓会腾出现金, 反过来模型是拿着一笔本该已卖掉的持仓在做判断。关掉/删掉操作员时任务会真的从调度器上摘下来 —— 只管添加的话"我明明关了它还在跑"会持续烧 AI 额度。(3) **生命线是全流程唯一的实时例外** —— 操盘手其余全部走收盘口径(拿盘中价成交等于给模型一个它复盘时看不到的价格)。跌破 20 日线是硬纪律不是策略, 所以这一路**完全不问 AI**: 让模型有机会"再看看"就等于把纪律变成建议。触发可以看实时价, 但生命线本身永远是收盘均线 —— 拿实时价掺进均线, 这条线自己就会跟着盘中抖; 不足 20 根就没有生命线, 不拿 12 根算个假的去强平。强平在流水里单独标「纪律强平」, 不和模型自己的决定混在一起。测试 +20 | 改 `services/paper_trader.py`(两本账 + 迁移 + 定时字段, 撮合改吃账本)、`services/paper_trader_run.py`(scope 化 + `market_candidates` + `check_lifelines`)、`api/paper_trading.py`(按账本的端点 + 定时 + 生命线)、`main.py`(启动装定时)、`pages/PaperTrading.tsx`、`lib/api.ts`; 新增 `services/paper_trader_schedule.py` | 低(R59 刚落地, 没有历史数据要担心; 迁移路径仍有测试守着) | 回到 R59 的单账本形态 |
| R60 | **全站统一版式**: 先量了一遍才动手 —— 内容宽度散着 6 个值(1500 / 1440 / 1280 / 1100 / 6xl / 7xl)外加好几页完全不限宽; 页面留白 5 种组合(`px-8 py-6` / `px-4 py-5` / `px-6 py-5` / `p-4 md:p-6` / `p-1.5`); 今日总览与市场环境还各自手搓了页头。后果不是"某一页丑", 而是**切页面时内容边界每次都在跳** —— 同一块卡片在今日总览宽 1500、在复盘宽 1280、在自选顶到边, 眼睛每次都要重新找基准线。这种不齐单看任何一页都不明显, 只有连着翻才难受, 所以最容易一直没人改。新增 `PageShell` 把"一页长什么样"收在一处, 只定三件事: **内容宽度**三档(`full` 表格页不限宽 —— 限宽只会逼出横向滚动条; `wide` 1440 看板页; `read` 1100 文字页, 一行太长眼睛回不到行首), **页面留白**一档(`px-3 pb-4 pt-3 lg:px-4`), **纵向节奏** 12px。限宽层与留白层刻意分开 —— 合成一层的话宽屏上内容会贴着限宽边缘, 左右留白反而消失, 那正是原来几页"顶到边"的原因。今日总览/市场环境/指数三页的手搓页头改走 PageShell; 其余页保留 PageHeader 只统一留白与宽度。**Dashboard 的渐变头部条刻意保留** —— 它带着日期选择器、行情时延、实时状态, 是看板自己的仪表而不是页标题, 硬塞进 PageHeader 会把这些挤成一行小字; 只把它的 `p-1.5`(全站最窄)并入标准留白。改后: 宽度只剩 1440/1100 两个值, 留白 16 处全部统一, 弹窗那几处 `px-5 py-4` 属于对话框自己的一套, 不动 | 新增 `components/PageShell.tsx`; 改 `pages/` 下 14 个页面的外壳(Today/Regime/Indices 换 PageShell, Data/Financials/Review/Concept/Industry/Monitor/Analysis/Settings/Branding/Dashboard/Backtest/Mining/Screener/StockAnalysis/LimitUpLadder 统一留白与宽度) | 低(只动外层容器的 className 与页头, 页内内容与数据路径逐字未改; tsc/eslint/build 全绿, 告警数与改前一致) | 逐页改回原 className |
| R59 | **AI 操盘手(模拟交易)**: 让模型只用本系统给的信息自己炒股, 长期观察下来回答一个问题 —— 这套系统给的信息, 够不够一个模型据以赚钱? 跑不出来, 差在哪一块就是下一步该改的地方。所以每一笔都留着当时的理由, 那份记录才是真正的产出。**三条硬约束都是结构上的, 不靠提示词客气地要求**: (1) 只能用系统里的信息 —— 模型拿到的是一段服务端拼好的纯文本, 没有工具、没有联网出口, 想查也没有手; 上下文里每一块都注明来自哪个模块, 复盘时对得上。(2) 操作员之间互相看不见 —— `build_context` 只按传进来的那**一个** trader_id 读持仓与历史, 别人的持仓/成交/理由一个字都不进来(有测试拿另一个操作员的独门理由去搜, 搜到就算漏)。界面上也刻意不做"所有人持仓一览": 我自己看完再去调提示词, 同样是把隔离破坏掉。(3) 撮合按 A 股规矩 —— 100 股一手、T+1 当天买的不能卖(加仓会刷新这个时钟, 否则可以靠"昨天买过一手"绕开)、按收盘价成交、佣金印花税滑点两边都按不利方向算。放宽任何一条, 跑出来的收益都不能拿来判断系统好不好。**操作员的名字就是模型名** —— 这张表要回答的是哪个模型用同一份信息做得更好, 名字里带别的只会看走眼; 每个操作员绑定自己的 AI 档位且**不走 R56 的兜底链**: 悄悄换成另一家会把成绩记到错误的名下。拒单也留痕("想买但钱不够"和"没想买"是两件事, 只记成交的话复盘时看到的是一段安静的空窗); 模型输出解析不出来就记成"这天没交易"并保留原文, 不硬猜(猜错一笔比空一天糟得多)。详情弹窗有一个「它看到了什么」页 —— 要判断信息够不够, 先得看清到底给了什么, 那里也是核对隔离的地方。测试 +27 | 新增 `services/paper_trader.py`(账本+撮合+解析)、`services/paper_trader_run.py`(上下文+决策)、`api/paper_trading.py`、`pages/PaperTrading.tsx`、`tests/test_paper_trader.py`; 改 `main.py`(挂路由)、`router.tsx`、`Layout.tsx`、`lib/api.ts` | 无(纯新增的一页与一套存储, 不碰任何既有路径) | 删这几个文件与路由注册 |
| R56 | **多 AI 档位 + 按优先级自动兜底**: 用户反馈"有时候 token 没能自动切换到下一个"—— 查下来**这个功能根本不存在**: 原来只有单个 provider / 单个 key / 单个模型, 一家用完额度整个 AI 功能就停摆。改成一个**有序列表**(顺序即优先级, 不另设 priority 字段: 两处表达同一件事迟早打架), 排在前面的先用, 那一档服务不了就顺位往下试。实现用 `ContextVar` 记住"当前正在试哪一档", 几个 `current_*` 读它优先 —— 这样 `_run_openai_once` / `_stream_openai` / `_openai_client` 里那一堆读配置的地方一行不用改; 用 ContextVar 而不是全局变量是因为并发请求可能各自在试不同的档, 全局会串档(那种 bug 只在有并发时才现)。**两条分寸是这个功能的全部要害**: (1) 只有"这一档服务不了"才换档 —— 401/402/403/429/5xx/连接类/模型不存在换档, 400/422 这种请求本身的错误**不换**: 换谁都一样失败, 挨个试等于把每个 key 都白烧一次往返, 还会把真正的报错埋进一句"都失败了"; (2) 流式只在**吐出第一个字之前**能换档 —— 已经流给用户的收不回来, 中途换档会把两家的输出接在一起, 读起来通顺但那是拼的, 比直接报错更糟。全部档位失败时错误消息逐档写明各自的原因(用户要能判断是该充值、该换 key 还是对面在抽风); 只有一档时原样抛原错, 不包一层。**老配置零改动照跑** —— 没配过多档时从旧字段合成一条, 否则升级这版等于把所有人的 AI 关掉。设置页给一个可上下移动的表, key 脱敏下发、留空 = 沿用原来那条(前端手里没有明文, 每次保存都要求重填等于逼人把所有 key 再贴一遍)。测试 +28 | 改 `secrets_store.py`(+`list_ai_profiles`/`save_ai_profiles`)、`services/ai_provider.py`(+`_ACTIVE_PROFILE`/`_should_try_next`/两个兜底循环)、`api/settings.py`(+2 端点)、`lib/api.ts`; 新增 `pages/settings/AiProfiles.tsx`、`tests/test_ai_failover.py` | 低(单档时行为与原来一致, 有测试守着; 兜底只在多档时才起作用) | 清空 `ai_profiles` 即回到单档行为 |
| R55 | **挖掘/回测的历史记录可以删了**: 三处历史原来都只进不出 —— 工作流(两页共用)、自动挖掘会话、挖掘运行。攒久了列表全是没用的失败记录, 而每次挖掘运行还各自带一个产物目录, 是真占盘。三处都补上删除(store 层 + `DELETE` 端点 + 界面按钮)。**唯一要守的规则是"正在跑的不给删"**, 三处的原因是同一个 —— 删的是记录, 跑的那一路并不知道: 工作流会被后台节拍下一拍写回来(表现是"删不掉"); 会话删了正在跑的那一轮没人收尾; 运行的目录 worker 还在写, 删掉后它会把目录重建成半个残骸, 之后 `list_runs` 扫到一条读不出来的记录。所以三处都是"先停/先取消, 再删", 按钮在跑着时就禁用并写明原因, 不让人点了才知道不行。工作流开的会话也不给单删(R53: 它是那条工作流的一次「重开」, 工作流还要靠它接着推)—— 要清就删工作流。删运行是 `shutil.rmtree` 整个目录(只摘 manifest 等于假删), 越界的 run_id 走既有的 `_run_dir` 校验, 有测试专门守这条(删除是破坏性操作)。顺带补上**「以前的 N 个会话」列表** —— 自动挖掘面板原来只跟随最新会话, 旧记录既翻不出来也删不掉。三条确认框都写明"候选方案不受影响, 删的只是这条记录"。测试 +14 | 改 `services/workflow.py`、`services/mining_autopilot_store.py`、`services/mining_jobs.py`(各 +`delete`)、`api/workflows.py`、`api/mining.py`(+3 个 DELETE)、`components/backtest/WorkflowPanel.tsx`、`pages/backtest/MiningAutopilot.tsx`、`pages/backtest/MiningWorkbench.tsx`、`lib/api.ts`; 新增 `tests/test_research_history_delete.py` | 无(纯新增端点与按钮; 既有读写路径未动) | 删掉三个 DELETE 端点与对应按钮 |
| R54 | **回测/挖掘改成「AI 拿内置的那套自己跑」为主, 手动配置默认收起; 顺带修掉「结果解析失败」**: 用户要的能力其实已经有了 —— 回测工作流本来就让 AI 从 `engine.list_strategies()`(作者给的内置策略, 排除 research_only)里自己挑、自己配风控, 挖掘工作流同理从内置因子里挑; 只是这两页都把一整列手动配置摆在主视野, 工作流被挤成边角一条, 读起来像"得先自己配完才能跑"。所以改的是版面与默认值, 不是能力: 两页的手动配置栏都做成可收起并**默认收起**(共用 `researchManualOpen`), 收起后工作流占满宽度成为第一屏。收起不是隐藏事实 —— 回测那条收起栏把仍在生效、且会原样透传给工作流的口径(本金/持仓上限/总仓位/佣金印花税滑点/建仓口径)写在一行里, 并注明"AI 只动策略与风控那几个旋钮"; 挖掘那条写资产/因子数/区间。不写出来就成了看不见的默认值, 那比多占一列更糟。展开后所有控件逐字未动, 状态也一直都在(收起只是不显示)。**另修一个真 bug**: 单次回测的 SSE `done` 负载漏了 `_json_safe`(优化器与 walk-forward 两路都有)—— 零波动区间算出的 nan sharpe/sortino 让 `json.dumps` 写出裸 `NaN`, 那不是合法 JSON, 浏览器 `JSON.parse` 直接抛, 界面上只剩一句"结果解析失败", 跑了几分钟的结果全丢。这个 bug 能一直活着是因为 Python 自己的 `json.loads` **默认认** NaN/Infinity(非标准扩展), 后端怎么自测都发现不了 —— 测试里专门用 `parse_constant` 模拟浏览器的严格口径, 并钉住"三路 done 都必须洗"。测试 +9 | 改 `api/backtest.py`(单次回测 done 加 `_json_safe`)、`pages/backtest/StrategyBacktest.tsx`、`pages/backtest/MiningWorkbench.tsx`、`pages/Mining.tsx`(hint 写明用内置的那套)、`lib/storage.ts`; 新增 `tests/backtest/test_sse_json_safety.py` | 低(版面与默认值; 回测/挖掘的计算路径未动) | `researchManualOpen` 默认改回 true 即回到原版面; JSON 那处是纯修复, 不该回退 |
| R53 | **工作流与「AI 自动挖掘」不再打架, 挖掘页排出层级**: 用户问这两块冲不冲突 —— 设计上不冲突(工作流的一次「重开」**就是**开一个自动挖掘会话, 是包含关系不是并列), 但操作上真会打架, 而且正是之前那个「跑了很久是不是卡死了」的来源: (1) **两路一起挖** —— 挖掘是重活, heavy_job_limiter 容量 2 而一个挖掘 run 独占两格, 再开一路不会更快, 它只会静默排队; (2) **两边推同一个状态机** —— 工作流开的会话照样出现在自动挖掘面板里(面板默认跟随最新会话), 用户对它按「AI 再调一轮」就和后台节拍同时 step 同一个会话, 轮次会错乱(两边都以为自己开的是第 N 轮)。修法是两侧对称装闸: 会话落盘时记 `owner_workflow_id`(工作流开的会话由 driver 认领), 有挖掘工作流在跑时手动开会话 409、有主的会话手动 step/stop 409(单停会话没用 —— 工作流会当成这次重开结束了转头再开一个), 反过来手动会话还开着时建挖掘工作流也 409。三条错误文案都写清"为什么"(不会更快, 只会排队)和"怎么办"(先停哪个), 不是干巴巴一句不行。已收工的会话和有主的会话都不占坑, 否则跑过一次就再也起不了工作流、或者工作流重启后把自己锁死。前端: 面板据 `owner_workflow_id` 停用手动按钮并说明是谁在跑(自动模式也强制关掉, 否则定时器一直去撞 409), 工作流跑着时「开新会话」置灰带原因; 页面顶部加一段**层级说明**(工作流 ⊃ 自动挖掘会话 ⊃ 单轮挖掘, 缩进画出包含关系)—— 三块并排平铺最容易读成"选一个用", 那正是同时开两路的起因。测试 +11 | 改 `services/mining_autopilot_store.py`(+`owner_workflow_id`)、`api/mining.py`(两处 409)、`api/workflows.py`(建挖掘工作流时的对称检查)、`services/workflow_drivers.py`(认领会话)、`pages/Mining.tsx`(+`LevelGuide`)、`pages/backtest/MiningAutopilot.tsx`、`lib/api.ts`; 新增 `tests/test_mining_workflow_conflict.py` | 低(只加拦截与展示; 手动那条路在没有工作流时逐字未变) | 删两处 409 检查与 `owner_workflow_id` 即可回到 R39 的行为 |
| R52 | **趋势视图里把「结论」徽标列加回来**: R51 拆两个视图时把它从趋势表里去掉了, 理由是"重复" —— 但那个判断的前提是当时结论视图跟趋势表长得一模一样。现在结论视图已经是摊开的卡片流(每一档说了什么、之后走成什么样), 两边内容分开了, 趋势表里那个 4 字徽标就不再是抄一遍, 而是让状态和当天的通道位置能**横着对上一眼**: 「这个板是在什么位置上出的」「转折那天通道在哪」—— 那正是这张表按时间轴排开的意义。徽标带 R49 的悬停卡片, 想摊开看仍切到「通道结论」视图(表尾写明) | 改 `components/stock-analysis/StockReviewDialog.tsx`(`TrendView` +1 列) | 无(展示层) | 删掉那一列 |
| R51 | **两列点开看的不是同一张表**: R48 那版两边弹出同一份逐日表, 从「结论」列点进去只是在趋势表最右边多一个 4 字徽标 —— 把列里已经有的东西又抄了一遍。拆成两个视图各答各的问题: **趋势视图**(从「趋势」列进)只讲状态怎么走到今天 —— 日期/收盘/涨跌(带涨停标)/六态状态 + 转折日整行标出, 表里不再混进通道那套; **结论视图**(从「结论」列进)把每天的**悬停卡片**摊开 —— 徽标背后那三段(怎么做/为什么/依据 + 当天三档位置)才是这一列真正的内容。连续同一档的天**合并成一段**: 「强势深调」连着 4 天时铺 4 张一模一样的卡片才是真重复, 合成一段标出天数反而看得出这一档持续了多久; 中间隔了没有结论的日子不并段(那是两次独立出现)。每段另给「期间累计涨跌」「含 N 次涨停」与「之后 5 日」—— 后者取**段内最后一天**的前瞻收益: 一档结论连着出现几天时该问的是「它最后一次说完之后怎么样了」, 拿段首那天算等于把段内涨跌也算进兑现里。为此后端给每一行单独带上 `fwd`(末尾不足 5 天为 null, 不拿半截数据凑)。顶部可互相切换 —— 从哪一列进来只决定默认视图, 不把人锁死。测试 +2(逐日 `fwd` 与 outcomes 汇总必须对得上, 两处对不上就说明有一处走了另一套口径) | 改 `components/stock-analysis/StockReviewDialog.tsx`(拆 `TrendView`/`VerdictView`/`SegmentCard`/`toSegments`)、`services/review_service.py`(+`fwd`)、`lib/api.ts`、`WatchlistDecisionBoard.tsx`(两列各带 tab) | 无(展示层拆分 + 后端一个新字段) | 合回单一视图即可回到 R48 |
| R50 | **「AI 战法」改名「AI 打板复盘」并搬到复盘页**: 它做的事本来就是复盘 —— 盘后拿当日梯队回看情绪走到哪一档、谁是龙头、明天该盯什么, 放在连板梯队页等于让人先切页面再复盘。而复盘页本来就有一份「AI 大盘复盘」: 大盘那条线看指数与情绪, 这条线看梯队与打板, 两份并排才构成一次完整的盘后复盘。名字也跟着改 —— 在复盘页上「AI 战法」看不出和旁边那份报告是什么关系, 「打板复盘」与「大盘复盘」一对照就清楚。抽成自带梯队查询的独立组件(复盘页没有梯队数据, 让它走 props 等于把两个页面绑死), 且**只在点开后才拉梯队** —— 进复盘页先看的是大盘报告, 没点开就先拉一份梯队等于白花一次请求。梯队还没就绪时「生成清单」禁用: 拿空快照调 AI 只会得到一份煞有介事的空报告。ext 字段(题材/行业)的读取与取值从梯队页抽到 `lib/ladderExtFields.ts` 两边共读 —— 各写一份的话梯队页显示的题材和喂给 AI 的迟早对不上, 而那正是这份清单的判断依据。后端存档格式与路由未动(改路由会让已有的 30 份存档失联), 只把用户可见的那句错误文案改成「无法生成打板复盘」 | 新增 `components/LadderAiReview.tsx`、`lib/ladderExtFields.ts`; 改 `pages/LimitUpLadder.tsx`(移除按钮/payload/弹窗, 改用 lib)、`pages/Review.tsx`(+按钮)、`lib/api.ts`、`api/screener.py`(仅文案) | 低(纯搬迁; AI 调用、存档、追问路径逐字未动) | 把按钮与 `LadderAiDialog` 搬回 `LimitUpLadder.tsx` |
| R49 | **「结论」悬停改自绘卡片 + 「到位了」改名「该止盈了」**: 原生 `title=""` 在这一列有三个治不好的毛病 —— 要悬停一秒多才弹(扫一列票根本等不及)、系统渲染不能分层次("怎么做/为什么/依据"三段被 `\n\n` 挤成同样大小的一坨灰字)、决策台表格在 `overflow-auto` 里靠边那几行会被裁掉。改为自绘 `VerdictHover`: 立刻弹出、tone 色条 + 三段分级排版(「怎么做」放最上且给底框)、`position:fixed` + portal 挂到 `document.body` 不受任何祖先 overflow 影响、空间不够自动上下翻面并水平夹进视口、滚动/缩放时收起(fixed 不跟着滚会浮在原地指着别的行)。额外把 tone 的字面意思写出来(徽标只有 4-6 字, 光看「候选池」分不出该动手还是先盯着)。`note` 由调用方给 —— 同一个"到上沿", 持仓侧是止盈时机、买入候选侧是追高, 这个差别写死在 verdict 里对不上两边。决策台/今日总览(买入候选与持仓两侧)共用同一个组件。另: `top_confirmed` 的标题从「到位了」改为「该止盈了」——「到位了」看不出是该买还是该卖 | 新增 `components/stock-analysis/VerdictHover.tsx`; 改 `WatchlistDecisionBoard.tsx`、`pages/Today.tsx`、`indicators/keltner.py`(仅标题字串) | 无(纯展示层; 判定与配色仍全部由后端 verdict 决定) | 删 `VerdictHover.tsx`, 两处改回 `title=` |
| R48 | **「趋势」「结论」两列可点开逐日复盘**: 那两列只显示**今天**的读数, 要判断它们靠不靠谱得能翻回去看 —— 上次说「强势深调」是哪天、之后走了什么、这只票的涨停都出现在什么状态下。新增 `/api/stock-analysis/review`: 把六态状态、Keltner 三档位置与结论、涨停/跌停/炸板/连板数按**同一条时间轴**对齐成一张逐日表(默认 120 个交易日, 可切 60/120/250)。**全部走既有模块**: 状态机走 `livermore.compute` 的 steps 且用这只票**自己配的阈值**(复盘用默认值而列里用调过的值, 两边就对不上); 通道走 `indicators.keltner`, 只是把输入换成当天的均线/ATR; 涨停直接取 enriched 的 `signal_limit_up`/`consecutive_limit_ups` 预计算列(涨跌停幅度按板块与 ST 分档、还有 2026-07-06 那次 ST 规则变更, 在这里自己按 9.9% 判一遍必然对不上)。多取 130 根做暖机, 只参与计算不进结果; 不足 120 根时长期档留空。弹窗给涨停/最高连板/炸板/跌停四个数、涨停出现在哪些趋势状态下、以及**各档结论出现后 5 日表现**(样本小, 只报次数与均值, 不折算成百分比胜率去装得像统计结论); 「只看有事的日子」一键滤掉没有结论、没有涨跌停、也没有趋势翻转的那些天; 趋势翻转日整行标出来。口径提醒写在弹窗底部: 均线与 ATR 按**当前**复权因子回算, 之后除权的话同一天算出的通道会和当时屏幕上略有出入。测试 +24(含路由级 —— 接线错了表现是 422 而不是报错, 只有真发一次请求才看得出来) | 新增 `services/review_service.py`、`components/stock-analysis/StockReviewDialog.tsx`、`tests/test_review_service.py`; 改 `api/stock_analysis.py`(+`/review`)、`lib/api.ts`、`lib/queryKeys.ts`、`WatchlistDecisionBoard.tsx`(两列改按钮 + 弹窗) | 无(新端点 + 新弹窗; 两列原有的显示与悬停内容未变) | 删 `review_service.py` + 弹窗 + `/review` 路由, 两列改回 `span` |
| R47 | **「值得关注」的把握分结合 Keltner 三档结论**: R43 那版只看 `pressure` 的 side/level, 有两个洞 —— (1) `pressure` 要求**短期档到轨**才有读数, 「候选池」那一档(短期还在中部、中期已到下沿)整个够不到, 而那正是低吸候选最典型的样子; (2) 只分"上沿/下沿 × 强/弱"四格, 分不出「强势深调」和「下跌途中」, 两者短中期读数一模一样、长期档一翻含义完全相反(一个是这套里最好的买点, 一个是越抄越套), 给同一个分等于把最好的和最坏的混在一起。改为按 `verdict` 的 code 打分(+10 … −15), 幅度对齐既有因子(量比 ±12、胜率 ±12、相对强度 −15), 不让通道位置一项压过量价本身; 两个陷阱档(超跌反弹/下跌途中)与大顶同档扣分 —— 它们都是**出了买信号但位置不该买**, 信号越像越危险。打分放在**两个候选来源合并之后**(与 R37 主线加成同一位置): 只挂趋势那一路的话, 一只在大顶区域的 AI「逼近突破」候选照样满分排在最前, 而它的结论标就在旁边写着"不该开新仓", 标签与排序自相矛盾。`verdict` 与 `pressure` 在 `_build_overview` 里**分开算**且对**全部自选**打标(不只趋势候选)。前端机会区换上 `VerdictTag`(与决策台「结论」列、导出件同一份 tone), 导出 HTML 一并带上。测试 +22 | 改 `api/today.py`(+`_VERDICT_SCORE`, 打分移到合并之后)、`pages/Today.tsx`、`lib/api.ts`; 新增 `tests/test_today_r47_verdict_score.py`; 删 `test_keltner_stance.py` 里过时的机会区打分段 | 低(只改把握分的一项加减与展示标; `pressure()` 与持仓侧的挡加仓/止盈减仓路径未动) | 删 `_VERDICT_SCORE` 与合并后那段循环即可回到 R43 的打分 |
| R46 | **决策台导出 HTML(只导有结论的行)**: 工具栏加「导出 HTML」按钮(带条数角标), 生成自包含浅色页面 —— 无脚本无外链, 存档/打印/转发都不依赖本应用, 与今日总览导出同一套排版。**只导「结论」列有内容的行** —— 三档通道都在中部的票没有位置信息, 导出来只占篇幅; 导出件首行明写"导出 N 只(自选共 M 只)", 免得看到 148 只自选只导出 4 行时以为漏了。**按当前排序导出** —— 界面上怎么排, 导出件就怎么排(结论列升序 = 候选池聚在一起, 降序 = 该减的在前)。列: 标的/现价/涨跌/仓位/浮盈/趋势/短中长三档/结论/怎么办, 最后一列带完整的 action + detail。导出件配色单独给一套浅色(深色主题那套搬过去看不清), 并在页首写明通道口径(MA20±2ATR / MA60±2.5ATR / MA120±3ATR, 收盘判定)与那条纪律(结论说的是位置不是涨跌, 清仓看止盈线/生命线)。无可导出行时按钮禁用并给 toast, 不静默下载一个空表 | 改 `components/stock-analysis/WatchlistDecisionBoard.tsx`(+`buildBoardHtml`/`exportRows`/`exportHtml`/按钮) | 无(纯新增, 不碰任何既有渲染与数据路径) | 删 `buildBoardHtml` + 按钮 + `exportRows` |
| R45 | **补上「候选池」观察档 + 列宽按比例自适应**: 拿用户截图那 8 行真实读数跑 R44 的结论层, 发现 **7/8 显示「—」** —— 短期档在通道中部就不给结论(设计如此: 短期是操作级别), 但寒武纪/兆易创新/宏和科技三只**中期已破下轨**、只是短期收回了通道内, 这恰恰是低吸候选池该有的样子(大级别跌到位、就等短期给入场点), 却被整批滤掉。补 `_watch()` 观察档: 短期在中部 + **中期**到轨时给「候选池 / 高位回落」, 用独立的 `TONE_WATCH` 与四个动作档区分开。**只报中期, 长期单独触轨不报** —— 半年通道太钝、一年难得触几次, 报了也没法据以行动, 只会把这一列填满噪音(有测试守着)。观察档 rank 排在动作档中间(比该吸的靠后、比该减的靠前), 且短期到轨时永远优先于观察档。同时把排序权重从前端自编的 `VERDICT_RANK` 改为后端随结论下发的 `rank` 字段, 消除两边各一套顺序。**注意 `pressure()` 未动** —— 今日总览的挡加仓/止盈减仓仍只在短期到轨时触发, 观察档只出现在决策台的展示列, 不改变任何操作建议。顺带列宽改 `colgroup` 百分比分配(标的12/现价5/涨跌5/仓位4/成本8/浮盈5/止盈线7/趋势7/三档各4.5/结论5/置信4/报告5, AI 信号吃剩余 19.5%), 去掉拍脑袋的 `min-w-[1040px]` 与 AI 信号格的 `min-w-[300px]` —— 原来富余空间会被浏览器全塞给 max-content 最大的那一列, 别的列挤成一团。百分比随视口走才叫自适应; 配合上一版的 nowrap(内容宽度即底线), 既按比例分又不会压变形。真实数据下亮起的行从 1/8 增至 4/8。测试 +6 | 改 `indicators/keltner.py`(+`_watch`/`TONE_WATCH`/每条结论加 `rank`)、`lib/api.ts`、`components/stock-analysis/WatchlistDecisionBoard.tsx`(+colgroup, 排序改用后端 rank)、`tests/test_keltner_verdict.py` | 低(观察档只加展示; `pressure()` 与所有操作建议路径未动) | 删 `_watch()` 与 `verdict()` 里的回落分支即可回到 R44 |
| R44 | **三档通道组合 → 一句话结论**(用户: "上下轨结合短中长期一起做出结论"): 原来界面只给得出每一档的位置(贴上轨/通道内/…), 三档怎么合成一个结论要用户自己脑补。新增 `keltner.verdict()` 纯函数, 把八种组合翻成带标题+动作+理由的结论: 偏贵侧「短线冲高/到位了/大顶区域/超跌反弹」, 偏便宜侧「短线回调/调到位了/下跌途中/强势深调」。**判定顺序是核心**: 先判"短期与长期反向"的两种矛盾组合 —— 短期到上轨+长期在下沿 = 跌深了反弹(卖点不是买点), 短期到下轨+长期在上沿 = 强势股洗盘(最好的低吸位置); 同样一个"短期到轨"长期档一翻结论完全相反, 放后面判会被"三档同向"粗规则先吃掉, 前者被说成止盈时机(把反弹当上涨), 后者被说成别抄(错过最好的低吸位)。语气分四档(sell/buy/hold/avoid)且**只谈加减不谈清仓** —— 清仓是出场线/生命线的事, 优先级在通道之上, 有测试守着结论文案里不出现"清仓"; 只有短期触上轨时给 hold 而非 sell(强势票沿上轨走是常态, 说该减会让人反复卖飞)。结论由后端 `channels_for_symbols` 一并返回, 决策台「结论」列(可排序: 降序=偏卖在前, 升序=偏买在前)、今日总览标签、导出 HTML 三处读同一份, 不各自拼规则。测试 +19 | 改 `indicators/keltner.py`(+`verdict`/`_VERDICTS`/`TONE_*`)、`services/keltner_service.py`、`api/today.py`、`lib/api.ts`、`components/stock-analysis/WatchlistDecisionBoard.tsx`(+`VerdictCell`+第 15 列)、`pages/Today.tsx`; 新增 `tests/test_keltner_verdict.py` | 低(纯增量: verdict 为 null 时所有路径与 R43 一致) | 删 `verdict()` + 决策台该列 + 两处 `verdict?.title` 回退即可 |
| R43 | **高抛低吸全面改用 Keltner 三档**(用户: "之前做的高抛低吸我不要了, 直接用 Keltner 短中长来决定" + "Keltner 必须是系统自带的, 不允许创造"): ①**撤掉 R41** —— 基于 RSI + MA20 乖离的 `services/overheat.py` 与其测试整体删除。②**新增内置策略 `keltner_reversion`(通道高抛低吸)** —— 本仓**第一个出场由价格位置驱动**的策略: 此前所有内置策略的 exit 都是趋势破位型(`ma20_breakdown`/`ma_dead`), 哪怕入场是低吸类, 跑出来也是「低吸进 + 破位出」, 所以"高抛低吸行不行"在回测里根本无法表达。入场 = 触下轨后**收回通道内**(不在触轨当天买, 那是接飞刀), 出场 = 到上轨止盈(可选到中轨), 默认带 MA120 趋势闸拦住"下跌趋势里反复抄底"这个均值回归最亏钱的场景; 硬止损 -8% + 最长持有 20 日兜底。③**今日总览决策层换源** —— `holding_stance` 的 heat 输入从 RSI 过热换成 `keltner.pressure`(短期档定方向、中期档定强弱只放大不改向、长期档仅参考); 止盈减仓要求短中期共振(只有短期贴上轨太常见, 每次提示等于天天喊减仓), 防卖飞两道闸保留(浮盈≥10%、刚转强头 2 天不减); 机会区共振在上沿扣 10 分, 仅短期贴上轨只标注, **贴下轨不扣分**(那正是低吸要的位置)。**三档参数与我动手前的 `levels.py` 逐字一致**(ma20/2.0、ma60/2.5、ma120/3.0), 未自创; 新增的只有"贴近"阈值 `NEAR_ATR=0.5` 与三档合成 —— 系统原本回答不了"贴着哪条轨"。为让策略沙箱能 import 共用常量, 把 `app.indicators.keltner` 加入策略安全白名单(纯常量+纯数学零 IO, 能力远小于已在名单的 `app.backtest.matrix`; 白名单对 builtin 也生效是刻意的纵深防御, 不绕过)。内置策略数 19→20, 相应清单计数同步。测试 +52(净 +28) | 新增 `strategy/builtin/keltner_reversion.py`、`tests/test_keltner_reversion.py`、`tests/test_keltner_stance.py`; 删除 `services/overheat.py`、`tests/test_overheat.py`; 改 `indicators/keltner.py`(+`pressure`)、`api/today.py`、`strategy/ai_generator.py`(白名单)、`lib/api.ts`、`pages/Today.tsx`、`tests/test_screener_etf.py`、`tests/backtest/test_matrix_strategy.py` | 低(新策略为纯增量; today 的 heat 字段形状变了但 None 时行为不变) | 删策略文件 + `holding_stance` 的 heat 分支 + 前端 `HeatTag` |
| R42 | **决策台加 Keltner 三档位置列**(用户: "想知道每个个股当前处于短/中/长期哪种状态, 贴着下轨、在轨下还是在轨上"): 自选决策台新增「短通道/中通道/长通道」三列, 五档判定 破上轨 / 贴上轨 / 通道内 / 贴下轨 / 破下轨, 悬停给真实上下轨价、通道内位置百分比、距上下轨各几个 ATR。**公式抽到 `indicators/keltner.py` 与个股分析图表共用** —— 原来只写在 `levels._keltner_band` 里, 决策台再写一份的话, 图表说"贴上轨"决策台说"通道内"那天没法查; `levels.py` 三档参数改为取自 `keltner.BANDS`, 有测试断言两边同源且给出同一组上下轨。两个口径判断: **"贴近"用 ATR 而非百分比**(通道宽度本身就是 ATR 倍数, 用百分比会让高波动票永远不贴、低波动票永远贴着); **判定顺序先轨外再贴近**(破上轨的票必然也在上轨 0.5 ATR 内, 反过来判会把突破说成贴近, 那是两件事)。批量服务两次读盘: enriched 快照取 close/atr_14/ma20/ma60, 一次批量日 K 只为算 MA120(唯一没有预计算列的一档, 不足 120 根则该档留空而不是拿 60 根算个假均线)。**收盘口径** —— 通道要 ATR 与均线, 实时叠加层只有价格, 拿实时价比昨天的通道会得到半新半旧的判定。测试 +33 | 新增 `indicators/keltner.py`、`services/keltner_service.py`、`tests/test_keltner.py`; 改 `indicators/levels.py`(改为委托共用公式)、`api/stock_analysis.py`(+`GET /keltner`)、`lib/api.ts`、`lib/queryKeys.ts`、`components/stock-analysis/WatchlistDecisionBoard.tsx`(+3 列 + `KeltnerCell`) | 低(新增端点与列; 对 levels.py 的改动是等价重构, 图表数值不变) | 删三列 + `KeltnerCell` + 端点; `levels.py` 回退到内联公式即可 |
| R41 | **短期过热 —— 系统缺的那句"涨太急了"**(高抛低吸评估的第一步): 今日总览原来只说得出"该买了"(转多/回升)和"该跑了"(破线/趋势转坏), 持仓体检的三个减仓触发全是**风险驱动**, 没有一条是**过热驱动**。新增测量层 `overheat.assess`, 两个口径要点: ①**用 ATR 归一化而非固定百分比** —— 乖离 15% 对主板是极端、对 20cm 的创业/科创很常见, `(close-ma20)/atr14` 自动按标的自身波动缩放, 不需要按板块维护阈值表, 也不会把高波动票系统性全判成过热; ②**RSI 超买"且"远离均线两个条件同时满足**才算数 —— 一轮像样的主升浪 RSI 常年 70 以上, 只看 RSI 会大批误伤健康趋势。测量层与决策层刻意分开("新转强第 2 天 RSI 80"和"横盘半年突然拉到 RSI 80"是同一读数、完全不同两件事)。决策接入两处: **挡加仓**(比减仓更要紧 —— 原来只看趋势+AI 不看价格冲到哪, 那是在最贵的位置加最重的注)、**补一档止盈减仓**但排在所有风险驱动减仓**之后**, 且加两道防卖飞闸(浮盈 ≥10% 才谈、刚转强头 2 天不谈)。机会区: `hot` 扣 10 分并说明理由, `warm` 只标注不扣分(一并扣会把系统推成专挑弱势票)。数据全部来自 enriched 已有列(`rsi_14`/`ma20`/`atr_14`), 一次批量读取, 零新计算源; 取不到时行为与 R41 之前逐字一致。测试 +24 | 新增 `services/overheat.py`、`tests/test_overheat.py`; 改 `api/today.py`(`holding_stance` 加三个可选参数 + 批量读取 + 机会区打分)、`lib/api.ts`、`pages/Today.tsx`(`HeatTag`) | 低(新增可选参数与字段, `heat` 为 None 时全部路径行为不变) | 删 service + `holding_stance` 里的 heat 分支 + 前端 `HeatTag` |
| R40 | **机会区板块过滤**(用户: "有时候我只想看主板的或者某个板的"): 「值得关注」标题行加一排板块筛选片(全部/沪主板/深主板/创业板/科创板/北交所, 可多选), 每条机会带板块徽章(20cm 的创业/科创用暖色、30cm 的北交所用红色 —— 同一个把握分, 20cm 的票波动天然更大)。**过滤在后端于 `max_show` 截断之前生效** —— 前端筛的话会漏掉被截掉的同板块票, 用户看到的"主板机会"是残缺的且他不会知道; 有专门的回归测试守这条。板块归属统一到 `price_limits.board_of`(原来 `market_overview_builder._board` 有一份私有副本 —— 板块决定涨跌停幅度, 两处各判一套迟早对不上), 判定顺序先后缀 `.BJ` 再数字前缀最后交易所, 否则 300/688 会被 `.SZ/.SH` 吞成主板。选择落 `today_prefs`(换设备/刷新都保持), 全选归一为空列表(否则以后加新板块时旧的"当时全选"会变成排除新板块), 传了但一个都不认识时保持原样而不是静默变成全看。导出 HTML 同步带板块。测试 +28 | 改 `price_limits.py`(+`board_of`/`BOARDS`)、`services/market_overview_builder.py`(改为委托)、`services/today_prefs.py`、`api/today.py`、`lib/api.ts`、`pages/Today.tsx`; 新增 `tests/test_today_board_filter.py` | 低(新增字段与可选参数; `boards` 为空时行为与从前逐字节一致) | 摘掉筛选片 + `rank_opportunities` 的 `boards` 参数即可 |
| R39 | **研究工作流**(用户: "做成工作流，一整套工作起来，还不影响手动"): 原来的 AI 代跑跑在页面里 —— 关页面就断、用满 5 轮就停, 是一次性脚本不是工作流。新增服务端工作流层, 三件原来没有的事: ①**服务端自己跑**(会话落盘 + 后台节拍线程 20s 一拍, 关页面照跑, 重启接着跑); ②**会重开**(一次跑完不达标不算结束, 换一批配置再开一次 —— 用户说的"抽卡"; 预算按"最多重开几次 / 每次几轮 / 最多跑多久"给); ③**记账**(`overfit_note` 按累计轮数给出这个结果该怎么看, 跟着赢家带到界面上)。**一定会停**是核心不变量: 达标停、次数用完停、到点停(存绝对截止时刻, 不存时长 —— 存时长的话每次重启等于重新计时)、连错 3 次停。分层刻意做死: `workflow.py` 只有状态机与账本(纯函数, 墙钟可注入), 真跑挖掘/回测的动作由注入的 driver 完成, 单测不需要真跑一次回测。两个 driver 走的都是手动路径同一批入口(挖掘走 `step`, 回测走 `run_worker_task`), 没有第二套实现。**手动路径一行没动**。顺带: 回测给 AI 的旋钮从 5 个加到 7 个(+持仓天数/分钱方式, 编造的分钱方式回落等权); 账户事实(费率/印花税/滑点/初始资金/撮合口径)由页面原样透传, AI 碰不到。为共用路径把 `api/mining.py` 里的 `load_run_result` / `build_autopilot_session` / `advance_autopilot_session` 抽了出来, `autopilot_start`+`autopilot_step` 改为调用它们。测试 +42 | 新增 `services/workflow.py`、`services/workflow_drivers.py`、`services/workflow_runner.py`、`api/workflows.py`、`components/backtest/WorkflowPanel.tsx`、`tests/test_workflow.py`、`tests/test_workflow_api.py`; 改 `api/mining.py`(抽公共路径)、`services/backtest_autopilot.py`(+2 旋钮)、`main.py`(挂路由+节拍器)、`lib/api.ts`、`pages/Mining.tsx`、`pages/backtest/StrategyBacktest.tsx` | 低(新增为主; 对 mining.py 的改动是等价抽取, 端点行为不变) | 摘掉路由 + 节拍器 + 两处面板即可; `workflow*.py` 整体删除不影响任何既有路径 |
| R38 | **长任务的"看得见"与"停得下"**(用户实测: 自动挖掘跑了很久、日志无报错、分不清是不是卡了, 且没有任何办法停): ①**进度可见** —— 会话接口给正在跑那一轮补 `live` 块(run 状态/阶段/百分比/入队与起跑时刻), 界面显示"排队中(已等 X)"或"第 3/8 折 · 37% · 已跑 X", 有百分比才画进度条(给不出就不画假的匀速条), 有 run 在跑时会话轮询从 15s 收紧到 4s。**排队与运行必须分得开**: 挖掘在 `heavy_job_limiter` 里独占全部 2 个槽位且 `acquire` 无超时, 只要别的重任务占着 1 个槽, 挖掘就会停在 `queued` 无限等下去 —— manifest 里一切正常、日志一行不报, 旧界面只转圈, 这正是"看起来卡死"的主因。②**中止** —— 自动挖掘新增 `POST /autopilot/sessions/{id}/stop`: 取消正在跑的 run + 把该轮落成 cancelled(不落的话下次 step 还以为它在跑) + 会话收成**独立的 `stopped` 而非 `failed`**("我按了停"和"它自己崩了"在复盘时是两件事); 幂等, 已收工的会话重复点不会抹掉赢家。`step()` 同步: 末轮 run 为 cancelled 时走 STEP_DONE→stopped, 且消息里的状态改中文(原来直接插英文 "第 1 轮挖掘 failed")。③**两个 AI 代跑循环也能停** —— 循环跑在前端, 用 `useRef` 不用 state(闭包里读 state 拿到的是点开跑那一刻的旧值); 回测代跑能真停(调 `stopBacktest` 触发后端 cancel_event, 按钮写「中止代跑」), 因子代跑的单次筛选请求发出去断不掉, 按钮如实写「跑完这轮就停」而不假装能立刻断。测试 +12 | 改 `api/mining.py`(+`_attach_live_run`/`autopilot_stop`)、`services/mining_autopilot.py`(+`status_cn`, cancelled 分流)、前端 `lib/api.ts`、`pages/backtest/MiningAutopilot.tsx`、`StrategyBacktest.tsx`、`FactorDiscovery.tsx`; 新增 `tests/test_mining_autopilot_stop.py` | 低(纯增量: 新端点 + 只读的 live 块 + 三个按钮, 原有 step/run 路径未动) | 删端点 + `_attach_live_run` 调用 + 三处按钮; `stopped` 回落为 `failed` 仅影响展示文案 |
| R37 | **今日总览补上中观层**(三层推导 `大盘 → 主线 → 个股` 里缺的那一层): 系统原来只有第一层(市场天气/大盘红绿灯)和第三层(六态 + AI 信号), 于是"这只票凭什么值得买"永远只能答到"它自己形态好", 答不了"它站没站在今天最强的那条线上"。①**机会区加主线归属**: 候选票命中当日主线(涨停梯队聚合出的概念排行)时打标并加把握分, 一票多概念取名次最靠前的当主归属、其余进 also。②**中观快照条**: 两市成交额 + 近 250 交易日分位、全市场涨跌家数、当日主线前三(带涨停家数/最高连板/龙头), 放在天气横幅与机会区之间。零新计算源 —— 两份数据都是既有的 `market_mainline` 与 `regime_builder` 日频时序。三条刻意的设计约定(都有测试守着): **不在主线内不扣分** —— 主线由涨停梯队推出, 只覆盖市场最躁动的一小撮, 一只慢牛票所在概念可以整年零涨停, 扣它的分等于系统性偏向妖股; **数据陈旧就不打分** —— 跑批停更几天后的"当前主线"其实是上周的主线, 超过 7 个自然日只展示并标 stale; **加成做小(12/10/8/5)** —— 主线归属是佐证不是主因, 第一主线里的烂形态不该排到第三主线里的好形态前面。加成在两个来源(趋势转强/逼近突破)合并**之后**统一加, 同一只票命中两路也只加一次。成交额分位把当天排除在分母外(自己不可能小于自己), 样本不足 20 天只报绝对值不硬凑分位。AI 导读侧: `meso` 随总览整体进提示词(PRD §10.2 AI 与界面看同一组数), 候选带「主线归属」字段, 并加一条纪律 —— 主线是佐证不许单独当选中理由, 只有量价质量相当时才用它分高下。测试 +29 | 新增 `services/today_mainline.py`、`tests/test_today_mainline.py`; 改 `api/today.py`(`rank_opportunities` 加主线项 + `_build_overview` 返回 `meso` + AI 提示词与 payload)、前端 `lib/api.ts`、`pages/Today.tsx`(中观条 + 机会区标签 + 导出件同步) | 低(主线/regime 数据缺失时 `meso` 为 null, 机会区打分与从前完全一致) | 删 service + `_build_overview` 里的 meso 块 + `rank_opportunities` 中的主线循环 + 前端两处渲染 |
| R36 | **补上前端 ESLint(以及它当场抓到的一个必崩 bug)**: `package.json` 一直有 `"lint": "eslint ."`, 但既没把 eslint 列进 devDependencies 也没有 `eslint.config.js` —— ESLint 9 起不认旧格式, 这个脚本从来跑不起来(之前误以为能跑, 是全局装的 eslint 被捡到了)。补配置后第一次跑就抓到 `react-hooks/rules-of-hooks` 一条: `MiniIntraday` 的 `useId()` 写在空数据早退**之后**, 分时数据从无到有时本组件 hook 数 0→1, React 会抛 "Rendered more hooks than during the previous render" 崩掉整棵子树 —— 已把 `useId()` 提到早退之前。配置取舍是「基线 error = 0」, 因为永远是红的 lint 等于没有 lint: 只有 `rules-of-hooks` 这类必崩的留 error; `exhaustive-deps` 存量 60+ 处几乎全是 `const rows = q.data ?? []` 喂给 useMemo 的性能味道(不是错误), 先降 warn 保持可见, 清完再提回 error; 命名/格式/any/未使用变量交给 tsc, `no-useless-escape` 关掉(存量全是字符类末尾多余的 `\-`, 为它改 4 个上游文件只会白白制造合并冲突)。顺手把 fork 自己那 5 处 `?? {}` 包进 useMemo。**依赖装在 pnpm 上**: 仓库用 `pnpm install --frozen-lockfile`(Dockerfile 有 `\|\| pnpm install` 兜底, release.yml 没有), 只更 `package.json` 不更 `pnpm-lock.yaml` 会让 release CI 直接挂。现状 0 errors / 58 warnings, tsc 与 build 均通过 | 新增 `frontend/eslint.config.js`; 改 `frontend/package.json` + `pnpm-lock.yaml`(+eslint/typescript-eslint/eslint-plugin-react-hooks/globals)、`components/stock-table/MiniIntraday.tsx`(hook 位置)、`components/stock-analysis/WatchlistDecisionBoard.tsx`、`pages/backtest/FactorDiscovery.tsx` | 低(除 MiniIntraday 一处 hook 位置外不改运行时行为) | 删 `eslint.config.js` 与 4 个 devDependencies |
| R35 | **多 key 轮询错峰 + 抖动**: 原来同一轮里 n_keys 批背靠背打成一个突发, 打完干等一个窗口。改为摊开在窗口内并加随机抖动 —— 刷新变连续滚动而非整块跳变; 一次网络抖动只毁一批(5 只)而不是整轮 70 只; 不与其他客户端形成同步节拍。key↔批次映射每轮重新洗牌(不再固定 `i % n_keys`) —— 固定映射下某 key 出问题永远伤同几只标的, 打散后失败面随机分布。**额度约束不变(有测试守着)**: 抖动系数恒为 [1, 1+jitter] 只增不减, 同一 key 两次调用间隔始终 >= 60/rpm(14 key + rpm=10 实测一轮 6.13s vs 窗口 6.00s)。另加**每轮随机只启用一部分 key**(偏好 `realtime_keys_per_round`, 0=全部): 摊开后一轮正好铺满窗口、余量几乎为零, 某批响应慢或重试时同一 key 的下次调用可能落进窗口内; 少用几个就谁都不贴上限跑(base 从 6/14=0.43s 放宽到 6/10=0.60s), 且单 key 失效只影响它被抽中的轮次。代价是每轮容量 5×启用数, 147 只仍 3 轮覆盖完。GET/PUT `/api/settings/preferences/realtime-keys-per-round`(返回带 `total_keys` 供界面显示「10 / 14」)。测试 +24 | 改 `tickflow/rate_limits.py`(+`spread_delays`/`shuffled_key_order`/`select_keys`)、`services/quote_service.py`、`services/preferences.py`、`api/settings.py`; 新增 `tests/test_rate_limit_spread.py` | 低(只在多 key 池化路径生效, 单 key 退化为原行为) | 循环里去掉两处调用, 恢复 `pool[i % n_keys]` |
| R34 | **策略回测 AI 代跑**(量化研究页→策略): 与 R33 同形态, 对象换成策略回测。点「不会选? 让 AI 帮我跑」, 最多 5 轮 —— AI 选策略定参数 → 把配置真的灌进左侧表单(策略/环境过滤/持仓上限/总仓位/区间当场变) → 跑 → AI 看结果 → 不行换一个再跑 → 满意收手给结论。**只让 AI 碰五个旋钮**(策略、环境过滤档位、最大持仓数、最大总仓位、区间天数): 费率/撮合口径/初始资金属于「用户的账户事实」不该由 AI 替改, 策略自有参数是各策略特有语义、盲调等于乱试。配套给 `backtestTask` 加 `waitForBacktest`/`currentBacktestId` —— `startBacktest` 是 fire-and-forget(结果走 SSE 回填全局任务), 代跑要串行跑多轮必须能 await; 只认发起后新产生的任务(id 大于调用时的 id)免得把上一轮旧结果当本轮, 并带超时兜底。规则层达标判定与提示词同一套(交易数≥30 且 回撤≥-30% 且 收益>0 且 夏普≥0.5)。提示词纪律: 结论须说清是历史回测不代表未来并引导去「验证」tab 做参数敏感性与滚动样本外; 达标即收手不许刷数字; 交易数少/回撤大/收益靠个别几笔要明说。测试 +17 | 新增 `services/backtest_autopilot.py`、`tests/test_backtest_autopilot.py`; 改 `api/backtest.py`(+POST `/strategy/ai-plan`)、前端 `api.ts`、`lib/backtestTask.ts`、`pages/backtest/StrategyBacktest.tsx` | 低(新增端点 + 按钮 + 过程条, 原「运行回测」路径未动) | 删 service + 端点 + 按钮/PilotTrace + `backtestTask` 尾部两个导出 |
| R32 | **因子批量筛选 AI 解读**(量化研究页→因子→批量筛选): 61 个因子 × 5 指标的表没人看得动。规则层(零 AI 成本)先算可用度分 = 区分度 |IC| 50 + 稳定性 |IR| 30 + 方向确定性(胜率偏离 50%) 20, 全用绝对值以免反向因子被符号埋没; IC 胜率用**双边死区**(50%±5%)判随机而非单边下限(单边会误杀反向因子); 短名单按可用度降序且**同组最多留 2 个**(MA5/10/20 乖离高度相关, 全放是虚假多样性); 另给漏斗统计回答「61 个里为什么只剩这几个」。AI 层只看短名单, 输出能用的 2-5 个(理由须落到指标)、同类冗余、当前市场风格、下一步拿哪几个去挖掘; 明确禁止把多空收益当可执行收益推荐(docs §成交和成本: 理论价差, A 股不能真做空), IC 高但 IR 低/胜率贴 50% 必须明说不稳。面板按可信度排序(规则层漏斗→AI 结论→冗余), 底部可把 AI 选中因子或短名单一键灌回勾选框重跑。**②AI 代跑**(R33, 用户真正要的): 点「不会选? 让 AI 帮我跑」, 最多 5 轮循环 —— AI 挑因子定参数 → 把配置真的灌进左侧表单(勾选框/调仓/分组数当场变, 用户看得见它在操作) → 跑 → AI 看结果 → 不满意换一批再跑 → 满意收手给结论。跑复用同一个 run mutation(加可选 override, 因为 setState 异步、代跑必须拿刚给的配置立刻跑), 结果自动进原来那张表; 手动「自己跑」按钮保留且行为不变。提示词纪律: 批量筛选是描述性统计不是验证过的策略, 结论必须说清只是「值得进一步研究的方向」并引导去挖掘页做样本外验证; 达标即收手不许刷数字; 因子须跨组挑。测试 +25 | 新增 `services/factor_ai.py`、`tests/test_factor_ai.py`; 改 `api/backtest.py`(+POST `/factor/ai-reading`、`/factor/ai-plan`)、前端 `api.ts`、`pages/backtest/FactorDiscovery.tsx` | 低(新增端点 + 面板, 未改原筛选逻辑) | 删 service + 端点 + ReadingPanel 与按钮 |
| R31 | **AI 自动挖掘**(挖掘页顶部新面板): 用户要的闭环 —— 跑 → AI 看结果 → 改配置 → 再跑, 直到 AI 认为够好。**关键设计: 锁一段终检窗口**。反复读样本外指标再改配置重跑同一段, 等于用样本外挑了 N 次, 试够多必然撞上一个"过门槛"的(多重检验), 自动化只会坏得更快; 所以区间切成 搜索窗口(AI 循环只在这里试) + 终检窗口(循环全程看不到, 日期都不进 payload), 赢家须**人工确认发布**后才在终检窗口跑一次定生死。收工附 `confidence_note`, 迭代越多提示越明确地要求打折。编排是单一入口 `step()` 状态机(running/iterated/done/error), 手动点一次调一次、自动模式即前端轮询同一入口, 两种模式无第二套逻辑。严格对齐 `docs/mining.md`: 档位只允许 balanced/strict(探索档不能发布)、搜索窗口下限按档位算(balanced≈1185 自然日、strict≈1756)并再走 `require_mining_availability` 数真实交易日、赢家按 candidate signature 标识、上一轮未终态绝不起新 run(挖掘独占 2 槽位)、循环绝不调 publish、AI 只能输出因子 id 与受控数值参数(编造丢弃/越界夹紧)。测试 +38 | 新增 `services/mining_autopilot.py`、`services/mining_autopilot_store.py`、`tests/test_mining_autopilot.py`、前端 `pages/backtest/MiningAutopilot.tsx`; 改 `api/mining.py`(+4 端点)、前端 `api.ts`、`pages/Mining.tsx` | 低(全新面板 + 新端点, 未改动原挖掘工作台任何逻辑) | 删两个 service + 4 个端点 + 面板, `Mining.tsx` 去掉一行 |
| R30 | **按 PRD 收口六态价位口径**: ①**建仓锚点钉死**(§10.1 决策可复盘)——金字塔计划原用 `up_pivot` 当「站稳价/作废价」, 而它在上涨趋势里随新高天天上移(昨天跌回 13.00 作废、今天变 13.40), 事后对不上账; 改用 `entry_pivot`=进入当前状态那天的关键点, 进入后不再变(`compute` 新增 `entry_up_pivot/entry_dn_pivot`, today.py 建仓路径改用之, 取不到回退 up_pivot)。②**盘中价位强制收盘口径**(§7.5 价格口径统一用正式收盘价)——开实时叠加时盘中冲高会顶高 `leg_high`, 转弱线跟着虚高, 等于拿未成立的高点放松出场纪律; 新增 `_overlay_closing_prices`: 状态/持续天数/信号仍是盘中临时口径(用户要看当前阶段), 但所有价位与操作建议一律用已收盘日线重算, 另附 `price_basis=closing`/`closing_state`/`closing_as_of` 供对照; 单只与批量两条路径共用; 收盘序列不足 `_MIN_DAYS` 时原值不动(§9.4)。前端悬停改为「实时价只参与状态判定, 价位一律按已收盘日线算」。测试 +5 | 改 `indicators/livermore.py`、`services/livermore_service.py`、`api/today.py`、前端 `api.ts`、`TrendStateBar.tsx`、`WatchlistDecisionBoard.tsx`; 扩 `tests/test_livermore_flip.py` | 低(纯增字段 + 盘中分支) | today.py 改回 `up_pivot`; 删 `_overlay_closing_prices` 两处调用 |
| R29 | **六态关键点改用「翻转触发价」**: 用户发现上涨趋势里上关键点天天等于当日收盘 —— 属实, 趋势途中 `up_pivot = max(up_pivot, 收盘)` 即本轮最高收盘价, 创新高当天就是当日收盘, 拿它当「突破 X 加仓」的触发价等于没给价位。补出状态机本就隐含、但从未暴露的两条前瞻价: `flip_down`(收盘跌破即转弱: 上涨/回升态 = 本轮最高×(1-阈值), 回撤态 = 下关键点)、`flip_up`(收盘站上即转强: 回撤/下跌态 = 本轮最低×(1+阈值), 回升态 = 上关键点), 另带 `leg_high/leg_low` 本轮高低水位。操作建议文案随状态换用对应价位(上涨趋势不再说「突破今天的收盘价」); K 线六态价位线优先画翻转价(与关键点重合则不重复); 趋势条 / 决策台悬停 / 六态汇总表与 HTML 导出同步换列, 关键点降级为悬停参考。四个中间态的关键点本就是触发价, 逻辑不动。**PRD 校对补丁**: ①补回 ADD 语义(§6.6 五信号)——上涨趋势建议里加回「可按金字塔计划加仓」; ②个股信号 AI 提示词同步喂 flip_down/flip_up(§10.2 决策可解释性: AI 看的数必须是界面主打的数); ③决策台悬停明示出场优先级「组合回撤风控 > 生命线 > 止盈线(ATR) > 六态转弱」(§7.2/§12.5 风控最高权限), 并提示盘中转弱线会随盘中高点虚高。测试 +12 | 改 `indicators/livermore.py`(+`_flip_prices`, `action_text` 增两参且向后兼容)、`services/livermore_service.py`、`api/stock_analysis.py`、前端 `api.ts`、`TrendStateBar.tsx`、`WatchlistDecisionBoard.tsx`、`TrendSummaryDialog.tsx`;新增 `tests/test_livermore_flip.py` | 低(纯增字段, 老调用方不传新参行为不变) | `action_text` 去掉后两参 + 前端改回读 up_pivot/dn_pivot |
| R28 | **个股分析改版 + 板块跷跷板**: ①自选决策台铺满整页(`fullPage` 模式, 表格高度吃满剩余视口), 点标的改为**弹出**关键价位分析(日K+压力支撑+六态趋势条, Esc/点遮罩关), 列表不再被 K 线图挤走、可连续扫多只; ②新增**板块跷跷板**(市场环境页→情绪周期 tab, 主线排行上方): 规则层零 AI 成本(用本地主线日度强度序列算反向相关 + 领先方换手轮数 + 双方活跃度, 反向相关 ≤-0.15 硬门槛挡掉『一边不动一边窜』的假跷跷板), 开页即有; 「AI 一键识别跷跷板」把候选对的真实日度序列送审, AI 只做甄别与解读(哪几对成立/存疑、当下轮到谁、对手盘什么条件接力); 结果**滚动留档 30 天**(同日同 kind 覆盖, 序列不入档), 面板内可展开逐日回看。③关键价位开关取消「更多价位」折叠, 14 个价位组一次全排开(用户要全展示)。测试 +22 | 新增 `services/seesaw.py`、`services/seesaw_store.py`、`tests/test_seesaw.py`、前端 `components/regime/SeesawPanel.tsx`;改 `api/regime.py`(+GET `/api/regime/seesaw`、POST `/api/regime/seesaw/detect`)、前端 `api.ts`、`pages/Regime.tsx`、`pages/StockAnalysis.tsx`、`components/stock-analysis/{WatchlistDecisionBoard,AnalysisKChart}.tsx` | 低(新增为主, 只有个股分析页布局是改写) | 删 seesaw 两文件+两端点+面板;个股分析页恢复内联 `StockAnalysisBoard` |
| R27 | **AI 结果常驻 + 定时自动运行 + 自动刷新**: ①导读·优选落盘(`services/today_ai_store` → `user_data/today_ai.json`, 只留最新一份), GET `/api/today` 带出 `ai` 字段, 前端进页面常驻显示并标注'定时生成/上次生成'与'数据已更新建议重新生成'; ②定时任务(照复盘动态注册模式, 默认关闭, 工作日): 导读·优选默认 18:30、个股 AI 信号批量默认 19:00(scope 只跑持有/全部自选, **逐只串行+可配间隔默认20秒/只**), GET/PUT `/api/settings/preferences/{today-ai,signal-ai}-schedule` 改配置即时重注册 job、开启校验 AI Key; ③今日总览 AI 逻辑抽 `generate_today_ai` 供手动与定时共用; ④门槛面板加定时配置区; ⑤今日总览每小时自动刷新+聚焦刷新, 决策台 AI 信号每小时拉一次。测试 +8 | 新增 `services/today_ai_store.py`、`tests/test_today_ai_store.py`;改 `api/{today,settings}.py`、`services/preferences.py`、`jobs/daily_pipeline.py`、前端 `api.ts`、`Today.tsx`、`WatchlistDecisionBoard.tsx` | 低(定时默认关闭) | 删 store+两个 job 注册+端点+面板段 |
| R26 | **AI 一键分组**(自选页工具栏 ✨): 新增 `services/watchlist_ai_group` —— 原料用本地概念/行业标签(复用 `_load_concept_map_df`), AI 只做归纳命名与合并(降幻觉、可解释), max_tokens=None 对齐上游; `parse_groups` 纯函数严格校验(编造代码丢弃/同票只进第一组/组名截断6字/单只组退回未分组/未覆盖如实报数); **两段式**: POST `/api/watchlist/ai-group` 只出方案不动数据 → 前端弹窗展示分组成员理由与未归类清单 → POST `/ai-group/apply` 才写入, 支持「应用方案」(只移动涉及的票)与「清空后重分」, 同名分组复用不重建。测试 +11 | 新增 `services/watchlist_ai_group.py`、`tests/test_watchlist_ai_group.py`;改 `api/watchlist.py`、前端 `api.ts`、`Watchlist.tsx` | 低(纯新增, 不改既有分组行为) | 删服务+两个端点+按钮弹窗 |
| R25 | **同步上游(自选分组完善)**: 作者 3 笔——`a8a573a` 分组卡片视图(组内按涨跌降序/前N条可展开/卡片设置)+ 分组统计条(中轴分叉条形图)+ 分组拖拽排序(后端端点+9测试, 顺序贯通标签栏/统计条/卡片/侧栏)+ **监控规则编辑器支持从自选/自选分组批量导入标的**(去重合并)+ 修自选添加菜单滚动误关 + 前端扩展插槽; 另 2 笔 README 二维码。**零冲突自动合并**(分组功能与 fork 的 weight/决策台增强不在同一区域)。验证: fork 118 项测试通过、总览冒烟通过、tsc+vite build 通过 | 全仓合并 | —(基线推进) | 回退到存档 v4(`fdad334`) |
| R24 | **情绪周期当前阶段**: ①空数据误判修正——当日未落盘时末行(0板0家)不再被判'退潮第1天', 阶段卡改用最近定稿日+'定稿口径'角标; ②盘中实时阶段 GET `/api/regime/phase/live`, 按 Cap.QUOTE_POOL 门控(Starter+ 全市场实时): 实时快照聚合当日涨停生态+昨日定稿分母算晋级率, 拼接定稿序列跑同一 classify_phase_series 得盘中临时阶段, 前端每分钟轮询、可用时琥珀'盘中'角标(免费档 available=False 零痕迹零额度)。会员位预留: 升级后自动点亮 | 改 `api/regime.py`、前端 `api.ts`、`Regime.tsx` | 低(免费档零行为变化) | 删端点+卡片改造段 |
| R23 | **合并上游 v0.2**(12 提交/147 文件/+2.1万行): 市场阶段与主线识别+因子挖掘全链路、情绪周期完善×5、自选分组等权涨跌幅、五档盘口依赖实时开关修复、通知配色、AI max_tokens 放开(与 R22 同坑, 以上游为标准)。冲突 4 处调和: daily_pipeline 上游 mining 包装叠加 fork 当晚重试; main.py 保 today 路由; market_recap 保复盘三模式+上游空正文守卫; Screener 保只看自选+上游 memo。fork AI 调用(信号/导读·优选/调参)max_tokens 改 None 对齐上游, <think> 剥离/容错解析/自动重试保留。上游 main 的 2 笔(热路径信号缓存+日K快路径性能/uv chore)随后一并合入(dev.sh 冲突取我方超集)——v0.2 与 main 全部同步。**合并后修上游 2 bug**: ①regime 重算 500 → 分段兜错带真实原因返回(核心/阶段重标/主线回填分开, regime.py 补 logger); ②`rps_rotation._load_concept_map_df` 缓存只存 df 未存元组, 命中时解包成 Series → 主线回填/概念轮动 600s 内第二次起必败, 已修+回归测试 | 全仓合并 | —(基线推进) | 回退到存档 v3(`8e7a329`) |
| R22 | **AI 输出容错解析统一化**(用户常换厂家模型, 格式纪律参差): ①新增 `services/ai_json.extract_json_object`(原文直解→最大花括号块→截断补括号修复, 纯函数), AI 信号/六态调参顾问/导读·优选三处统一改用; ②AI 信号 max_tokens 300→800、信号词兼容中文(买入/卖出/持有/观望), 解析失败报错带原文摘录; ③导读·优选 max_tokens 800→1500, `parse_ai_brief_response` 原文兜底当导读仅全空报错, 前端失败红横幅+重试按钮(不再只靠一闪而过的 toast)。④解析失败自动带'只输出JSON'指令重试一次(零星失败自愈); ⑤思考型模型兼容: 解析前剥 `<think>…</think>` 推理段(未闭合=截断返回 None), token 上限给思考段留余量(信号 2000/导读 2500/调参 1500)——上限非目标, 普通模型零影响。测试 +19 | 改 `api/today.py`、`services/{stock_signal,livermore_service}.py`、前端 `Today.tsx`;新增 `services/ai_json.py`、`tests/test_{ai_brief_parse,ai_json_tolerant}.py` | 低 | 还原三处解析+删 ai_json.py |
| R21 | **当日数据未出齐自动重试**: 盘后管道(默认15:30)早于数据源发布(17:30~20:00)时 enriched 缺当日分区、梯队/概念/行业停昨日且要等次日调度——现在调度管道检测到 today_daily_incomplete 每 90 分钟自动重跑(当晚≤3次, 补齐即停, replace_existing 不堆积); 完整性检测下限 15:30→15:00(修"15:10 调度恰好躲过检测"漏洞) | 改 `jobs/daily_pipeline.py` | 低(仅调度包装+阈值) | 还原 _pipeline_with_retry 包装与 15:30 下限 |
| R20 | **把握分量价+胜率因子**(对齐"选最值得关注胜率最好"初衷): ①量能质量——候选量比(盘后快照批取+实时叠加层覆盖)≥1.5 放量+8 / <0.8 缩量-12; ②个股历史胜率——livermore `_bullish_event_win_rate` 纯函数(历史转入多头侧后 5 日为正比例, 未完整事件不计, 样本<3 不给), ≥60% +8 / ≤40% -12, 中间只展示; 仅带新信号候选计算(≤40 只), 缺数据静默跳过。规则层粗筛=新鲜度+AI+RS+量能+个股胜率, 形态识别归 AI 优选。测试 +6 | 改 `api/today.py`、`services/livermore_service.py`;新增 `tests/test_today_r20_quality.py` | 低 | 删 extras 段+两个胜率函数 |
| R19 | **确定性刷新**: `QuoteService.refresh_full`(连续拉取至轮转偏移回绕=全自选覆盖一遍, 每轮保留按 key 限速, max_rounds=12 防阻塞, 没转完如实报 partial)+ POST `/api/intraday/refresh-full`(带覆盖轮数与叠加层覆盖数); 今日总览「刷新」= 先全量同步实时('同步中…')再刷页面, toast 报'全量同步 N 只'/'未全覆盖'; 实时不可用静默退回收盘刷新。测试 +3 | 改 `services/quote_service.py`、`api/intraday.py`、前端 `api.ts`、`Today.tsx`;新增 `tests/test_quote_refresh_full.py` | 低 | 删 refresh_full/端点/按钮逻辑 |
| R18 | **盘中使用框架进页面**("盘中是执行层, 收盘是决策层"固化进 UI): 今日总览时段提示条(北京时间: 盘前=计划/盘中=执行·新信号等收盘/盘后=落盘定稿·纪律以定稿为准/周末=复盘); livermore 在实时价确实参与判定时给 trend 标 `intraday`(收盘落盘后自然消失)——今日总览机会标"盘中·待收盘确认"、决策台趋势徽章带 * 角标、个股趋势条琥珀"盘中口径"徽章; "持有票转跌"行动项盘中降级 mid 预警, 收盘定稿才是 high 纪律 | 改 `api/today.py`、`services/livermore_service.py`、前端 `api.ts`、`Today.tsx`、`WatchlistDecisionBoard.tsx`、`TrendStateBar.tsx` | 低 | 删 intraday 标注段+提示条组件 |
| R17 | **推送渠道批量设置 + 一键创建点位就地选渠道**: ①`monitor_rules.batch_set_channels`(存储层纯函数)+ POST `/api/monitor-rules/batch-channels`——一次改全部/指定规则的 feishu/wecom/dingtalk, mode add/set/remove, 未变化不重写, 改后同步引擎; 监控中心规则栏铃铛按钮开批量弹窗(未配置渠道置灰, set+空选清空有提示); ②点位提醒「推荐点位配置」区内嵌渠道开关(与下方通知渠道同状态), 一键创建全部带什么渠道所见即所得。测试 +5 | 改 `api/monitor_rules.py`、`strategy/monitor_rules.py`、前端 `api.ts`、`Monitor.tsx`、`PriceAlertDialog.tsx`;新增 `tests/test_monitor_batch_channels.py` | 低 | 删 batch_set_channels/端点/弹窗/渠道开关段 |
| R16 | **今日总览实时化**: 自选实时叠加层接入决策汇聚层——livermore `append_live_bar`(当天实时价作临时收盘追加进六态窗口, 日期新于最后一根才追加, 收盘落盘天然去重), trends 接口加可选 live 参数; today `_watchlist_live_map` 读叠加层(股票+ETF), 实时价参与六态/机会距离/出场线现价距离/持仓浮盈/组合净值; **纪律判定(triggered/fatal)仍收盘口径**。头部绿点"实时中(N只)"/关闭时琥珀提示。开关关闭零回归。**补全**: 实时映射抽为 `services/live_quotes` 共享, 决策台趋势列(`/trends`)、个股趋势条(`/trend`)、止盈线距离(`/exit-lines`)同接实时价——决策台全列实时(现价/浮盈本就实时); 线位与 triggered 仍收盘纪律口径, 监控规则同步不受实时价影响; K线弹窗当日蜡烛注入改为叠加层优先(免费自选档此前永远注入不了, 只查了付费档的全市场缓存), 头部现价随蜡烛实时。测试 +12 | 改 `api/{today,stock_analysis,watchlist}.py`、`services/livermore_service.py`、新增 `services/live_quotes.py`、前端 `api.ts`、`Today.tsx`;新增 `tests/test_today_live.py` | 低(live 参数可选, 默认行为不变) | 删 append_live_bar/_watchlist_live_map 及调用点 |
| R15 | **金字塔建仓路径**(利弗莫尔式"趋势确认后快速上满"的约束版): `build_pyramid_plan` 纯函数把目标仓位拆三步——试仓(默认35%)→ 站稳关键点 N 日(默认2)加至确认档(默认70%)→ 回踩不破上满; 收盘跌回关键点清试仓、计划作废。关键点取六态上关键点/AI 触发价; 目标<1成不拆步, 较小自动两步, 确认档强制>试仓档。参数(试仓%/确认加至%/站稳天数)进门槛面板(today_prefs)。机会条目理由下显示"建仓路径:…", 导出 HTML 与 AI 送审摘要同步。测试 +6 | 改 `api/today.py`、`services/today_prefs.py`、前端 `api.ts`、`Today.tsx`;新增 `tests/test_pyramid_plan.py` | 低 | 删 build_pyramid_plan 及调用点+prefs 三字段 |
| R14 | **PRD 缺口补全五件套**(逐条校对 PRD 后补齐):①**年线斜率**——decide_mode 纯函数化; 破50日线+年线拐头向下 → 软防守(走 2 日确认), 硬防守(破年线/年动量负)仍立即生效;②**全市场宽度**——复用 regime 历史涨跌家数, 普跌日(跌>涨×2)把进攻压到谨慎, 理由带"全市场 X涨/Y跌", regime 无数据静默跳过;③**仓位比例**——positions 加可选 weight(0-100%), 决策台成本旁"仓%"输入, 不填不影响既有功能;④**组合回撤纪律**——新增 `portfolio_history`(净值=1+Σ仓位×浮盈, 同日覆盖, 记高点), 回撤超纪律线(默认10%, 门槛面板可配3-30%)→ 行动区置顶降仓提醒; 总仓位超姿态基调 → 建议减差额; 体检组合行显示总仓位/基调/距高点;⑤**模式切换推送**——大盘模式变化落 alert_store(进行动区与监控中心, 防守 critical)+ 桌面通知。测试 +9(软防守确认/decide_mode 分支/weight 边界/回撤计算) | 改 `api/{today,watchlist}.py`、`services/{market_mode,positions,today_prefs}.py`;新增 `services/portfolio_history.py`、`tests/test_portfolio_gaps.py`;前端 `api.ts`、`Today.tsx`、`WatchlistDecisionBoard.tsx` | 低 | 按文件还原;数据卷多 `user_data/portfolio_history.json`, positions.json 多 weight 字段(向后兼容) |
| R11-R13 | **AI-TIS PRD 移植三件套**(以用户提供的 AI 半导体趋势投资系统 PRD 为指南, A 股适配, 全部纯规则零 AI 成本):**R11 大盘红绿灯**——新增 `services/market_mode`: 基准沪深300(缺数回退上证指数, 走独立 kline_index parquet 不碰选股链路), 收盘<年线 或 年动量<0 → 防守(一票否决立即生效); >年线且>50日线 → 进攻; 破50日线守年线 → 谨慎; 非防守切换需连续 2 交易日确认(同日刷新不重复计数, 翻回清零), 状态存 `user_data/market_mode.json`; 历史<200根明说退化观察。今日总览最终姿态 = 大盘与自选广度取更保守者, 横幅加大盘灯 chip。**R12 仓位建议**——姿态定总仓位基调(进攻≤8成/谨慎≤5成/防守≤2成/观察≤3成); 单票建议 = 单票上限 × 把握分系数(≥80满额/60-79七折/40-59三成/20-39一成) × 波动率压缩 min(1, 目标日波动/ATR日波幅)只压不加, 向下取整到半成, 不足半成给"仅观察仓"; 单票上限与目标日波动进门槛面板(today_prefs 扩展)。**R13**——①相对强度: 趋势结果附 ret_20d, 跑赢大盘5点+8/跑输5点-15; ②持仓操作档位 holding_stance: 离场/减仓/加仓/持有(加仓要求趋势刚走强+AI看多+离出场线5%以上缺一不可), 体检表新增列; ③组合汇总(平均浮盈/已触发/逼近/空头数)。测试 33 项新增(市场模式16/仓位8/R13 9) | 新增 `backend/app/services/market_mode.py`、`backend/tests/test_{market_mode,position_advice,today_r13}.py`;改 `api/today.py`、`services/{today_prefs,livermore_service}.py`、前端 `api.ts`、`Today.tsx` | 低(livermore 仅加一个输出字段, 其余自包含) | 删 market_mode.py+还原 today.py 相应段;数据卷仅多 `user_data/market_mode.json` |
| R1 | **构建/CI 必需项**:GHCR PAT 凭据 + 本分支构建触发;Dockerfile/pyproject README 路径修复(hatchling 构建失败) | `.github/workflows/docker.yml`、`Dockerfile`、`backend/pyproject.toml` | 低 | 不可回退(镜像构建依赖) |
| R3 | **数据可靠性三件套**(用户新卷首拉即复发上游盲区后重加):①**无进展判死**——`progress()` 写 `progress_at` 心跳,`reap_stale` 按「最后进度距今」而非总时长判卡死(首次全量拉取 >20 分钟不再被误杀,真卡死照常回收);②**历史稀疏自愈**——同步前查近一年 `count(DISTINCT date)`,<120(正常约240)判定历史大洞,强制走"从一年前重拉"分支(被中断的首拉/实时快照拉高 max(date) 导致补缺口分支永不回补的上游盲区);③**盘后完整性提示**——交易日 15:30 后同步完成时检查今日全市场日线行数,不足(阈值 max(100, 池一半))则完成消息+result 标记+前端红 toast「今日日线尚未出齐(仅N只),数据源一般 17:30~20:00 发布」 | `backend/app/services/pipeline_jobs.py`、`backend/app/jobs/daily_pipeline.py`、`frontend/src/pages/Data.tsx`、`frontend/src/lib/api.ts` | 中(改上游任务/管道判定) | ①还原 started_at 计时;②删 history_sparse 段与两处条件;③删完整性检测段与 toast |
| R10 | **今日总览打磨(存档 v2 收尾)**:①**机会区把握分筛选**——`rank_opportunities` 纯函数打分(0-100):趋势刚转强底分最高,按信号第几天加减(第 1-2 天最佳,第 4 天起判"已过入场窗口"扣分),AI 信号同向按置信度加分/反向重扣(矛盾机会直接滤),逼近买入触发价按距离加分;低于门槛或超出条数上限的不显示只报数量,同票多来源合并取高分。**卖出/风险提醒永不过滤**(行动区不经过打分)。②**门槛可配置**——`services/today_prefs`(today_prefs.json,min_score 0-100/max_show 1-50,损坏按字段回落默认),GET/PUT `/api/today/prefs`,页面「门槛」面板滑块+输入框即调即存。③**AI 导读·优选合一**——POST `/api/today/ai` 一次调用:候选(≤8 只)附真实日 K 20 根(量比/换手/均线/MACD/RSI/ATR)+关键价位送审,提示词要求先量价横向对比选 1-3 只(禁止拿"规则分高/AI 看多"当理由,规则分只是粗筛门票,宁缺毋滥),再基于优选写导读,两者结论必须一致;取不到行情的候选标注 kline_error。④**排版/文案**——页宽 1500px+列表大屏双栏;姿态原因/横幅统计/AI 提示词全面大白话(禁"胶着"类行话);中文名改走 `repo.get_name_map()`(自选表只存代码)。测试 21 项:打分排序/陈旧信号滤除/AI 加减分/上限/门槛读写边界/送审必须含量价/提示词关键约束 | `backend/app/api/today.py`、新增 `backend/app/services/today_prefs.py`、新增 `backend/tests/test_today_{opportunity_rank,prefs,select_payload}.py`、前端 `api.ts`、`Today.tsx` | 低(今日总览自包含) | 还原 today.py+删 today_prefs.py 与三个测试;数据卷仅多 `user_data/today_prefs.json` |
| R9 | **生命线(=20 日均线,自动)+ 今日总览导出 HTML**:生命线恒为 **MA20(收盘口径),自动计算无需手填**——标了「持有」即自动生效(不填成本也盯),**优先级高于 ATR 三阶段线**:收盘跌破 → stage=fatal「无条件清仓离场」(AI 信号纪律第 8 条:必须 sell、禁止"再观察"类表述);未破但 20 日线高于阶段线 → 生效线抬到生命线。监控双保险:生效线规则(生命线生效时 critical)+ 20 日线低于生效线时另立 `exitlife_*` critical 规则(注:MA20 每日变动,规则值随决策台加载/日线管道刷新);推送文案「跌破生命线(20日线) X!按纪律无条件清仓离场,此票不再看」。今日总览行动区置顶破位、K 线图独立画线、决策台止盈线列「生命线破位!」。**今日总览页新增「导出 HTML」**:自包含单文件(内联样式无脚本无外链,浅色排版),含姿态/行动区/机会区/持仓体检表,已生成的 AI 导读一并带上,可存档/分享 | `backend/app/services/{positions,position_exit,stock_signal}.py`、`api/{watchlist,stock_analysis,today}.py`、前端 `api.ts`、`WatchlistDecisionBoard.tsx`、`Today.tsx` | 低 | 还原各文件生命线分支;删 Today.tsx 导出段 |
| R8 | **今日总览(决策汇聚层)**:侧边栏首位新页面,把各模块既有产出按"需要行动的紧迫度"聚合成一屏——①**需要行动**(出场线已触发/距离<3%、持有票转入下跌趋势、近24h监控触发;空时明示"今日无需操作,管住手");②**值得关注**(六态刚转多/回升、AI 看多且现价距突破预案价<2%);③**市场天气**(自选口径多空比+新转多/转空计数→规则版仓位姿态:进攻/谨慎/防守/观察,判定可复现);④**持仓体检表**(浮盈/出场线距离/阶段/趋势/AI信号,已触发排前)。全部复用既有服务零新计算;条目点击直达个股分析页(个股页补 ?symbol= URL 参数支持);**AI 导读**按钮可选(3-4句盘前导读,一次调用,未配 AI 不影响页面) | 新增 `backend/app/api/today.py`、`frontend/src/pages/Today.tsx`;改 `main.py`(+注册路由)、`router.tsx`、`Layout.tsx`(+导航项)、`api.ts`、`StockAnalysis.tsx`(URL参数) | 低(纯新增页面+聚合接口) | 删两个新文件+还原注册/导航/URL参数三处 |
| R7 | **持仓出场线(ATR 三阶段:止损/保本/移动止盈)**:对「持有+已填成本」的自选每日确定性计算出场线——①浮盈<1×ATR:止损线=成本−k×ATR(六态下关键点更近时取其为准,两道防线先触者算);②1~2×ATR:保本线=成本;③≥2×ATR:吊灯止盈线=持仓以来最高收盘−k×ATR(棘轮只上移,以逐日运行最大值实现,无状态可复现);进入③后线不低于成本。锚点是持仓最高价而非"成本×比例"(市场不知道成本;成本只定阶段)。k 默认 3.0、20cm 板 3.5;ATR14=TR 简单均值;"持仓以来"以标记成本日(positions.updated_at)为起点。**展示**:决策台「止盈线」列(线位+距离,逼近琥珀/跌破红+已触发)、K 线「持仓止盈」价位组、AI 信号纪律第 7 条(watch_points 必须含该线,AI 不得改数值,action 按阶段:跌破止损/保本离场/止盈了结)+持仓上下文注入。**推送**:出场线自动同步为价格监控规则(id exit_*,cooldown 1 天,收盘价口径),复用现有引擎盘中评估与通知;决策台加载与日线管道完成时刷新,平仓自动清理规则 | 新增 `backend/app/services/position_exit.py`;改 `api/watchlist.py`(+/exit-lines)、`api/stock_analysis.py`(levels 注入 exit 组)、`indicators/levels.py`、`services/stock_signal.py`、`jobs/daily_pipeline.py`(+刷新钩子)、前端 `api.ts`、`AnalysisKChart.tsx`、`WatchlistDecisionBoard.tsx` | 低(纯新增;管道仅加 try/except 尾钩子) | 删 position_exit.py + 按文件还原;监控规则里删 exit_* 前缀规则 |
| R6 | **AI 信号行动导向 + 六态汇总/导出**:①决策台 AI 信号列显示 watch_points 到价预案(↑涨至X→操作 / ↓跌至Y→操作;数据早已缓存,现有信号即刻可见);②信号提示词按原作者四维分析的骨架重写(资深人设+信号纪律六条+数据说话),明确 watch 仅限多空证据矛盾/不成熟时用、reason 必须点明下一步触发条件、关键价位存在时预案必须一上一下、逆六态趋势信号需更强证据;③生成信号时注入六态趋势上下文(状态/持续/关键点/参考动作);④决策台「六态汇总」按钮:统计卡(总数/多头/空头)+六状态页签+明细表(零 AI 成本,复用批量趋势),可**导出自包含 HTML**(内联样式无外链无脚本,浅色排版可存档分享) | `backend/app/services/stock_signal.py`、新增 `frontend/src/components/stock-analysis/TrendSummaryDialog.tsx`、`WatchlistDecisionBoard.tsx` | 低(提示词+纯新增 UI) | 还原 stock_signal.py 提示词段;删 TrendSummaryDialog + 按钮 |
| R5 | **六态趋势分析(利弗莫尔 Market Key)**:引入用户提供代码包的六态状态机(逐日状态+关键点序列与 JS 原版对拍完全一致),文案沿用原版表达(六状态名/操作建议/转多·转空·回升·回撤/上·下关键点/多头·空头);操作建议动态代入具体关键点价位(如「观望,下破 1028.00 确认转空」),拒绝"上一关键点"式模糊指代。只用日线收盘价(复权口径,与关键价位同源),窗口近 180 交易日。**显示**:①个股分析页趋势条(状态徽章+持续天数+关键点+操作建议);②K 线背景多空分段着色(复用图表预留 ranges 通道)+「六态关键点」新价位组(markLine+点位提醒弹窗自动可用);③决策台「趋势」列(批量接口一次拉取,可排序,悬停看详情)。**回测调参**:3%~15% 阈值网格回测(纯函数毫秒级,指标:翻转/假信号率/段长/跟随收益 vs 买入持有/前后半段一致性),规则建议保底(邻域平滑防尖峰过拟合+样本不足明示),可选 AI 调参顾问(一次调用,只准选网格内阈值,提示词内置防过拟合纪律;走 ai_provider 带重试);阈值每票可覆盖(livermore.json,default+overrides),弹窗一键应用/恢复默认。趋势判定零 AI 成本 | 新增 `backend/app/indicators/livermore.py`、`backend/app/services/livermore_service.py`、`frontend/src/components/stock-analysis/TrendStateBar.tsx`;改 `api/stock_analysis.py`(+4 端点+levels 注入)、`indicators/levels.py`(+livermore 类型)、`AnalysisKChart.tsx`、`StockAnalysis.tsx`、`WatchlistDecisionBoard.tsx`、`api.ts`、`queryKeys.ts` | 低(纯新增,仅 levels 响应多一组) | 删除两个新文件+按文件还原;数据卷仅多 `user_data/livermore.json` |
| R4 | **增强功能整批移植**(用户指示"还有什么能做的一起做了",从 `claude/rebuild-tickflow-enhancements` 一次性搬入全部剩余增强,均带 `[fork 增强]` 注释标记):**自选决策台**——自选页每票"AI 信号"卡片(WatchlistDecisionBoard,规则先行+可选 AI,含 stock_signal/positions 服务);**关键价位智能选择**(规则版一键选指标);**复盘三模式**(快速/标准/深度)+归档标注生成模式+历史报告整合(JsonReportStore);**连板梯队 AI 战法**+部分数据日防线(当日行数不足不生成误导梯队);**策略体检**(StrategyHealthDialog,胜率/期望/样本量);**参数扫描增强**;**钉钉 webhook 适配**;**监控规则编辑器增强**+仅自选开关;**AI 调用韧性**——429/5xx 重试(2s/5s 退避)+失败显式报错到前端(不再静默 200+error);**任务协作取消**(JobCancelled + is_cancelled 心跳检查);**数据持久化自检**(容器内 data_dir 非挂载点则启动告警+数据页红横幅,防拉新镜像丢数据);**select 崩溃修复**、**repository 叠加层/写盘守卫**、**stocksdk bridge 单位修正**(手/股,该插件默认不打包仅修源码)。⚠️ 移植时已剔除旧版 #47 事故代码:日K 拉取严禁多 key 池(free-api 无视 key,免费 key 打付费端点被拒致全量拉取失败),`sync_daily_batch` 保持上游单 client;每夜除权同步(旧#46)未搬入,除权保持上游管道阶段 | 后端 api/services/strategy/jobs 共 29 文件、前端 20 文件(见本次 commit)、`backend/app/main.py`(手工植入,保留作者同日新增的文件日志) | 中高(覆盖面大,但均为增量功能,数据管道核心仅 R3 已述改动) | 按功能对应文件 `git checkout upstream/v0.2 -- <文件>`;main.py 删 `data_dir_persistent` 段 |
| R2 | **免费多 key 池化(修正版)**:同一 key 字段填多个免费 key(逗号/换行/空格分隔),每 key 各 5 只自选实时额度凑成 5×N,自选超容量时轮转窗口全覆盖。`secrets_store` 加 `get_tickflow_keys()`(`get_tickflow_key` 改为取第一个,单 key 行为不变);`client` 加 `get_realtime_client_pool()`(**仅实时**;付费端点。历史日K的 free-api 路径严禁用池——免费 key 打付费端点会被拒,此为旧版 #47 事故教训);`preferences.get_realtime_watchlist_symbols` 解除 5 只截断;`quote_service._fetch_watchlist_quotes` 池轮询+轮转窗口+按 key 独立限速;Keys 页输入提示。⚠️ 多免费 key 池化可能违反 TickFlow ToS,用户知情自担 | `backend/app/secrets_store.py`、`backend/app/tickflow/client.py`、`backend/app/services/preferences.py`、`backend/app/services/quote_service.py`、`frontend/src/pages/settings/Keys.tsx` | 中(改上游 key/行情层) | `git checkout upstream/v0.2 -- <上述文件>` |
