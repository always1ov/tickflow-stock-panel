# Fork 改动台账(重建版)

> 本 fork 策略(2026-08-14 重建):基于作者 `upstream/v0.2` 干净代码,**逐个**重加增强功能,
> 每加一个经用户实测验证后再加下一个。数据源铁律:**只用 TickFlow,不引入第三方数据源**。
> 完整历史台账见封存分支 `claude/docker-image-build-trigger-7ryn7i` 的 FORK_NOTES.md(49 条)。

## 存档版本(可回退基线)

| 版本 | 分支 | 提交 | 镜像 | 功能范围 |
|------|------|------|------|---------|
| v6 | `claude/archive-fork-v6-20260829` | `24ce014` | `:24ce014` | R1~R81 全部(AI 操盘手/今日总览/多 AI 档位/盘面参考分组/分钟策略与分钟回测/牛来/异动监控/外部网页/并发与存储加固/测试全绿 1883) |
| v1 | `claude/archive-fork-v1` | `6df359c` | `:6df359c` | R1~R4(整批移植基线) |
| v2 | `claude/archive-fork-v2` | `5e862ec` | `:5e862ec` | R1~R10 全部(六态/回测调参/出场线/生命线/今日总览:把握分筛选+可配门槛+AI 导读·优选合一+导出) |
| v5 | `claude/archive-fork-v5-20260820` | `cfbc752` | `:cfbc752` | R1~R35 全部(个股分析改版/板块跷跷板/六态翻转触发价 + PRD 收口/AI 自动挖掘/量化研究与策略回测 AI 代跑/多 key 错峰抖动与每轮启用数) |
| v4 | `claude/archive-fork-v4-20260819` | `fdad334` | `:fdad334` | R1~R24 全部(合并作者分组功能之前: 含情绪周期阶段修正/盘中实时预留/版面重排) |
| v3 | `claude/archive-fork-v3` | `8e7a329` | `:8e7a329` | R1~R22 全部(合并上游 v0.2 之前的最后状态: 大盘红绿灯/仓位建议/RS/建仓路径/实时链路/批量推送/AI 容错) |

> 回退方式:`docker compose` 镜像 tag 改为对应版本, 或从存档分支重建 `claude/upstream-clean`。

## 前端设计体系改造的回退指引(R122~R126)

按用户提供的 frontend-design skill 做的这批改动**全部是展示层**, 每一项独立成一次
提交, 可以从 GitHub 单独回退, 互不牵连(靠后的不依赖靠前的)。想退哪一项就
`git revert <sha>`; 想整批退回改造前, 按**从新到旧**的顺序逐个 revert。

| 提交 | 内容 | 回退命令 | 退掉之后 |
|------|------|---------|---------|
| `12b43ed` | R126 skill 配色(灰阶去色 + 明度阶梯 + 亮色强调色压暗 + 圆角 10px) | `git revert 12b43ed` | 灰阶回到带蓝调的原配色, 圆角回 8px。字体与 oklch 坐标仍在 |
| `51a3a5f` | R125 色板换 oklch 坐标 + 字体打包自托管 | `git revert 51a3a5f` | 色值回 HSL、字体回系统栈(JetBrains Mono 又变成"装了才有") |
| `908d83b` | R123/R124 机会区两列 + 动效时长统一 + 焦点环 + 减少动效 | `git revert 908d83b` | 时长类名回到手写数字, 键盘焦点环消失 |
| `69797bc` | R122 今日总览版式(机会区表格化/横幅合并) | `git revert 69797bc` | 机会区回到 15 张卡片 |

**只想退其中一小块**(不整条 revert):
- 退字体: 删 `src/index.css` 顶部三段 @import/@font-face + `tailwind.config.ts` 里 sans 首项 `'Inter Variable'`
- 退 oklch 坐标: `src/index.css` 色板改回 HSL 三元组, 并把全项目 `oklch(var(` 全替换回 `hsl(var(`
  (**注意**: 有 6 个 tsx 里共 38 处内联色引用, 漏改会当场出乱色)
- 退圆角: `tailwind.config.ts` 的 borderRadius 改回 card 8 / btn 6 / input 6 / dialog 12
- 退焦点环: 删 `src/index.css` 末尾 `[R124]` 那段

**不要连带退掉的**: R121(AI 优选事实校验与命中率台账)是后端逻辑, 与这批展示层改动
无关, 单独提交在 `4cd12ae`。

## 上游基线与升级约定(按 docs/secondary-development.md §8)

- **当前上游基线**: commit `8ef3d66`(upstream/main, v0.2.3), 于 R172 并入; 此前 `4cb30e4`(R166)、 `de5d5a9`(R164)、`576850d`(R152)、`dab8a2c`(`v0.2.2-26-gdab8a2c`, R120)、`4d27f31`(tag `v0.2.2`, R92)、`afbf432`(R82)、`e346e25`(R78)、`196af2f`(R71)、tag `v0.2.1`=`17ca245`(R69)。
  每次同步上游后更新这一行 —— 守则要求二开分支记录确切 Tag/commit, 不能只写"基于 v0.x"。
- **升级预检**: 下次同步上游前先跑只读预演 `python3 scripts/upgrade_check.py <目标Tag>`,
  它会列出双方都改过的文件和可预见的文本冲突; 预演不动工作区, 但只看已提交内容。
- **高冲突热点复核清单**(守则 §5.2 点名的文件里, 本 fork 动过的 + 每次合并上游后必须复核的点):
  - `backend/app/main.py` — fork 接线: 3 个路由(today/workflows/paper_trading)、操盘手定时安装、
    工作流节拍器、数据持久化自检、操盘手账本 `StoreError`→503。复核: 路由都还挂着、scheduler 注入还在。
  - `backend/app/strategy/engine.py` — `watchlist_only` 注入(context.watchlist_symbols, 未注入退化为全市场)。
    对应守则里"按需扩展"的 `CandidateFilter` 接口 —— 出现第二个过滤类需求时应提取成正式接口。
  - `frontend/src/router.tsx` — 根路径→/today、/dashboard、fork 页面路由。
  - `frontend/src/components/Layout.tsx` — nav 数组的 fork 项、「盘面参考」分组(R57/R67)。
  - `frontend/src/lib/api.ts` — fork 端点全部加在此(守则要求的单一客户端, 增长是预期内的)。
  - `frontend/src/lib/queryKeys.ts` — fork 键集中在文件尾部 `[fork 增强]` 段(R70)。
  - `backend/app/backtest/engine.py` — R83 按作者分钟回测实现扩展了分钟数据入口；同步上游时复核分钟/日线回测路由与 fork 既有任务链均完整。

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R282 | **第 14 条规矩进 AGENTS.md: 发现问题直接修, 五类例外先确认**(用户: 「发现问题时直接自行修复, 无需事先征求我的同意」「对于其余一般性问题, 请自行分析并采用你认为最优的方案完成修复, 同时简要说明问题原因、修复方式和验证结果」)。**默认改成动手而不是请示** —— 顺手撞见的错、说假话的注释、静默失败、措辞不一致、空测试一律当场修掉, 事后汇报只需说清三件事: 问题原因 / 修复方式 / 验证结果。五类例外(架构调整、核心逻辑重构、删除或替换关键模块、修改公共接口或依赖、可能影响其他功能或带来明显风险)动手前必须先确认; **判据写明是影响面与可逆性, 不是改动行数** —— 一行改动动了公共接口要问, 两百行的措辞校准不用问。并写清**它不覆盖别的硬约束**: 第 3 条(作者策略只读)、第 13 条(打分冻结)是「根本不许动」而不是「问一声就能动」。**这一条明确不配机器守卫**, 而且在规矩里写明了为什么: 它管的是「什么时候该停下来问」, 那是判断不是可断言的事实; 硬编一道闸只会变成走过场的形式, **反而稀释另外那几条真能拦住东西的守卫**(第 12/13 条各有实测守卫)。**顺带记一次没查清的间歇失败**: 加完这条之后第一次跑全套出现 `1 failed`, 而随后 4 次(1+3)连续全绿, `test_pipeline_capacity` 单独压 5 次也全绿。**当时的命令把 FAILED 那一行滤掉了, 所以拿不到是哪一条** —— 照实记下来而不是当作没发生; 再出现时按名字追。 | 改 `AGENTS.md`(+第 14 条);无代码改动;后端 pytest 3617 passed / 11 skipped, 连续 4 次全绿 | 无(纯文档) | 可 |
| R281 | **核实打分系统自 R134 起有没有被动过 —— 没有, 而且是跑出来的不是看出来的**(用户: 「检查一次我的打分系统有没有被动, 稳定版本是 R134」)。**R280 那份冻结基线的出处当时没说清**: 它写的是「抄自当天的 opportunity_score.py」—— 那句话是真的, 但**没说清它凭什么算基准**; 万一 R134 之后漂过, 冻的就是漂完的值。逐条核实: ① `git diff 973b148 HEAD -- backend/app/services/opportunity_score.py` → **零字节差异**(`973b148` = 「R134: AI 优选评分 v2」那次提交; 中间有过一次 `dcef661 revert: [R230] 评分系统逐字节回退到 R134`, 那是它没漂的原因)。② **静态比对不够, 又跑了一遍**: 把 R134 那一版文件取出来直接 import, 与 HEAD 在系统性网格上逐点比对 —— `score_candidate` **2,439,360 个组合**(duration×state×rs×量比×换手×通道位置×near_breakout, 覆盖五条曲线的每个拐点与两侧), `score/dims/factors/coverage/partial` **全字段完全一致**; `check_gates` 3,024 个组合完全一致; `explain` 320 个组合完全一致。③ **真正的风险不在函数在调用点** —— 函数没变而喂进去的参数变了, 分数照样会变。逐行比过 `api/today.py` 里那 7 个入参的求值与调用行, **一字未变**; 算完之后也没有任何人再动过 `res["score"]`(它直接进输出)。**唯一查出的差异是排序的次级键**: R134 同分时按代码字典序, HEAD 按「数据齐全的在前 → 趋势强度高的在前 → 代码」(R139 改的, 注释写明了理由)。**分数一个没变, 变的是同分之间的先后** —— 这属于名次不属于打分, 但它确实改变了你看到的顺序, 所以照实说, 不含糊过去。其余 R134 那次涉及的文件(`score_ledger.py`、四份测试、`ScoreLedgerDialog.tsx`)都有后续改动, 但全部是**台账登记维度与展示**, 台账「只记不反馈」, 不回流进分。据此把 R280 基线的出处改写成**可复验的锚点**(写明 R134 与提交哈希 + 核实结论), 并加一条守卫钉住它 —— 出处写不清楚, 基线就只是「某天的快照」; 写清楚了它才是基准, 任何人都能自己跑一遍去对(与 R278「注释里的数字必须复算得出来」同一条纪律)。 | 改 `tests/test_scoring_frozen.py`(出处说明 + 1 条守卫, 21→22 条);**`opportunity_score.py` 零改动**;后端 pytest 全绿 | 无(只改测试里的说明文字并加一条断言) | 可 |
| R280 | **两条规矩进 AGENTS.md, 并各配一道机器守着的闸**(用户: 「加一条规矩进仓库, 以后所有改动都要注意全局的表达是一致的」「只要是最优解不动我稳定版本的打分系统就行」)。**第 12 条「全局表达一致: 一个东西只许有一个名字」** —— 执行处是 R279 那张名词表; 规矩本身写清四件事: 改名 = 加一行 `TERMS`(只改代码不加行, 下一处旧名照样漏出去)、别名只收核查过的(多收一个会把合法文案判成违规, 与 R272「宁可多收」取舍方向相反)、注释与 docstring 里复述旧说法是允许的(守卫扫的是渲染文本不是全文)、**同一个读数只许有一个产地**(措辞散在两处必然漂移 —— R277 的方向判据差一个边界点、R278 同一个量三套措辞加一套按词取的错配色)。**第 13 条「`opportunity_score.py` 冻结」** —— 新增 `test_scoring_frozen.py`, 把 11 组权重/曲线/门槛原样抄成基线。**它不阻止修改, 只是不许悄悄改**: 真要调参就同步改基线, 那一步会出现在 diff、FORK_NOTES 与提交信息里。**冻结的是数不是文件**(不用文件哈希 —— 那样加一行注释都会红, 很快就没人当回事)。打分参数比别处都危险: 改一个权重**界面不报错、测试不变红、用户看到的只是排名悄悄变了**, 等发现时已不知道是哪次改的。闸里最重要的不是逐项比对, 而是 `test_R280_基线覆盖了全部数值参数` —— **反向枚举模块里所有数值常量、逐个要求它在基线里**, 这样新增一个参数却忘了登记会立刻红(手写名单靠人记得维护, 而人不会记得: R272 注册表、R254 死键名单同一条教训); 另配两条**独立于基线成立**的不变量(权重和为 1、分段曲线横轴递增) —— 抄基线时抄错一位数, 逐项比对会全绿, 因为它们比的是同一份抄错的数。两条规矩各自钉住 AGENTS.md 里指向它的那一行(**指向不存在的文件 = 又一条说假话的注释**, 这仓库刚因为一句「与 SPREAD_CURVE 对齐」栽过 —— 那个名字全仓不存在)。**变异验证过程中查出的是我自己工具的问题, 不是闸的问题**: 第一轮「悄悄调一个维度权重」显示没被抓到, 复现后确认闸是红的 —— 那次替换**长度与原文完全相同**且与上一次运行同秒写入, Python 的 pyc 失效判据(mtime + size)没察觉, 跑的是旧字节码; 变异脚本改为禁用字节码缓存后 8 处全红。**这一条本身值得记**: 变异验证如果harness不可靠, 「守卫有效」这个结论就是假的。 | 改 `AGENTS.md`(+第 12/13 条);新增 `tests/test_scoring_frozen.py` 21 条、`tests/test_terminology.py` +1 条;**`opportunity_score.py` 零改动**;后端 pytest 3616 passed / 11 skipped, 退出码 0 | 无(只加文档与守卫, 不动任何实现) | 可 |
| R279 | **全系统名词普查 + 名词表守卫; 台账补落加速度原始值**(用户: 「全系统的表达都检查一次是否每个名词表述都统一了」「我不清楚, 拿捏不准要不要, 你帮我找个最优解」)。**普查方式是可复算的扫描, 不是肉眼**: 后端走 AST 只取字符串字面量并**排除 docstring**(注释里复述旧说法是允许的 —— 讲清楚「以前叫什么、为什么改」恰恰要写出旧名字), 前端走块级剥注释, 拿 13 组候选同义词跨 4501 条后端渲染串 + 6507 行前端代码对撞。**结论: 真冲突只有两个, 其余全是同形不同义。** ① **「贵不贵」后端还剩 2 处** —— R258 立的纪律是「界面上不再出现贵不贵」, 而它的守卫**只扫前端 .tsx/.ts**, 于是 `watchlist_urgency.py` 与 `keltner_geometry.py` 两处**照样渲染到界面**、在守卫眼皮底下活了下来: **纪律定的范围比守卫扫的范围大, 多出来的部分等于没人管**。② **同一个复盘弹窗里, 一个东西三个名字** —— 页签叫「通道结论」、逐日表列头叫「结论」、统计栏叫「位置结论」, 而**文件自己的注释就写着「这一层在别处一律叫通道结论」**。两个都改成正名。**其余核查为不冲突并写进排除表**(「评分」是策略因子评分 ≠ 把握分;「粘合」是作者策略的 MA 粘合 ≠ 三档重合;「利弗莫尔趋势」是外部页面默认名 ≠ 六态;`keltner` 只作字段名/接口路径/日志不作界面文案;「止盈线/止损线」是出场线在不同阶段的两个名字, 本来就该分开叫;「该动了/要动的」是同一判定的动词与名词形态)。**把 R258 那条单点断言升级成一张名词表 + 一次两侧扫描** —— 加一个概念只要加一行; 与 R272 注册表、R254 死键名单同一条教训: **靠人记得的地方迟早漏**。**这张表第一跑就抓到我自己多收的一条**: 我把「通道态势」当成旧名收了进去, 一跑红在导出列上 —— 查下来它labels 的是「阶段+间距+快慢+挤了几天」这一束几何量, 而同一份导出里另有一列就叫「结论」, 两者是不同的东西。**多收一个别名会把合法文案判成违规**, 与 R272「宁可多收」的取舍方向相反(那边漏一个会让体检说假话, 这边多一个只会挡住写文案的人), 已连同理由写进排除表。**变异验证暴露出守卫自己的一个洞**: 把扫描面缩回只看前端时**九条全绿** —— 后端清干净之后, 「只测现在没有违规」根本抓不到这种退化, 而**那正是 R258 当年的病**; 补 `test_R279_两侧都真的接在检查里`: 各塞一个只在那一侧存在的已知词, 确认两侧都真的被查。**台账补落 `accel_a1` 原始值**(只记不反馈, 不进分、不改判定): R278 量出界面分档(±0.05)与主升浪第⑤条(a1≥0)之间有一条 **16.1% 的缝**, 而台账只存三档码, 落进缝里的那些天全被压成同一个 `steady`, 事后无从回答它该归哪边。**落原始值而不落分箱** —— 现在定分箱边界又是一次没有证据的猜测, 而原始值事后可以任意分箱、信息不丢; 也不登记成分组维度(连续值分不了组)。**这就是「要不要做」的答案: 数据采集不可回溯**, 今天不记, 几个月后想回答时缺的就是这一段(与 R16「注记标签落台账 —— 补不了历史, 先做」同一个理由)。顺带修掉扫描时撞出的一个真问题: `ext_factors.py` 的 docstring 里有裸 `\w`, Python 3.12+ 已是 SyntaxWarning、3.14 会变成错误, 加 `r` 前缀。9 处变异逐一确认会红(后端两处贵不贵各自回来→1/3;后端位置结论回来→1;前端位置结论回来→1;台账不落原始值→1;扫描面缩回只看前端→1;把 docstring 也扫进来→1;作者禁区被扫进来→1;排除项不写理由→1)。 | 改 `services/watchlist_urgency.py`、`indicators/keltner_geometry.py`、`services/review_service.py`、`stock-analysis/StockReviewDialog.tsx`(旧名→正名)、`api/today.py`(+`accel_a1`)、`factors/ext_factors.py`(裸 `\w`);`tests/test_review_verdict_layout.py` 那条守卫重新指向正名(原意未变);新增 `tests/test_terminology.py` 9 条;**`opportunity_score.py` 与 `strategy/` 零改动**;后端 pytest 3594 passed, 前端 tsc + eslint(0 error) + build 全过 | 低(只改界面用语与新增一个只记不反馈的台账字段; 判定、阈值、打分口径全部未改) | 可 |
| R278 | **加速度措辞全系统校准 + 阈值认真检查(阈值一个没动)**(用户: 「校准整个系统的表达」「特别是涉及评分阈值的要认真检查」「v2 版本的打分系统是稳定版, 不能动」「禁止造假, 要有科学依据」)。**普查出同一个毛病的四份拷贝**: 加速度 `a1 = v1 − v2` 是**带方向**的(符号表示往多头还是往空头变), 可全系统有三套措辞在按「变快还是变慢」描述它 —— `ACCEL_CN`(加速/匀速/减速, 供决策台悬停、组合速查、今日总览、HTML 导出四处)、`_pace()`(R277 只修了这一套)、`explain()`(还在加力/推力在退); **第四份是颜色**: R277 的 `PACE_CLS` 按**词**取色, 其中「跌势在缓」(a1>0)与「正在放慢」(a1<0)两个相反符号给了同一个颜色。有意思的是 `ComboView` 的配色**从来没错过** —— 它按 `level`(符号)取色, 而颜色编的正是符号: **错的一直是词, 不是色**。修法是**收成一个产地** `accel_cn(level, spread)`: 给方向就按方向说(五档, 用户选的), 不给就用中性说法; `geometry()` 手上本来就有 spread, 让它直接输出方向感知的 `level_cn`, 四个下游自动跟着对。**中性说法不用「加速/减速」而用「往上加速/往下加速」** —— 台账按 `level` 跨多只票分组, 那里根本没有「这一只的方向」、纠正不了, 而「往上/往下」说的是符号, 四个象限字面永远成立。**方向判据也收成一处**: R277 手写的 `not nested and sp < 0` 与 `phase()` 判「下跌中」的 `sp <= -SPREAD_LAUNCH` **差一个边界点**(spread = −1.00 时阶段说「下跌中」而快慢说「正在放慢」), 抽成 `bear_structure()`; 顺带发现 `SPREAD_LAUNCH/SPREAD_MATURE` 在同一个文件里**定义了两次**(改上面那份而下面那份盖回去, 是最难查的一类错)。**阈值检查的结论是: 一个都没动。** ① `ACCEL_FLAT = 0.05` 的立论**复算不出来** —— 原注释写「|a1| 中位数约 0.12, 匀速约占两成、两侧各四成」, 拿本仓库 16 个具名场景逐日复算(1039 个交易日, 无随机抽取)得到的是**中位数 0.088 / 加速 36.9% / 平稳 27.8% / 减速 35.3%**, 三个数全不对; **数值本身没改**(没有后验证据支持换一个数), 改的是那段在用编造数字背书的说明, 并新增 `test_R278_门槛的立论必须复算得出来` —— **它把注释里的数字解析出来当场重算去对, 谁再塞一个没算过的数就红**, 这是「禁止造假」的直接守卫。② `ACCEL_FLAT = ±0.05`(界面分档)与 `MAIN_ADVANCE['min_a1'] = 0.0`(主升浪第⑤条)不一致, 实测**16.1% 的交易日**落在这条缝里(界面说「速度平稳」而判定说「不通过」); **没有改** —— 代码自述那五条阈值「全是先验, 一条都没有台账支持」, 无证据下动它只是拿一个猜测替换另一个, 改为把缝宽量出来钉成基线(`test_R278_界面与主升浪那条缝有多宽`), 哪天决定动阈值时那就是当时的对照。③ 顺带查实一句**假话**: 注释写着「与 opportunity_score 的 SPREAD_CURVE 甜区(1.5~3)对齐」, 而 **`SPREAD_CURVE` 全仓不存在**(`git log -S` 确认历史上也从未存在过), 打分层的位置轴走的是 `channel_pct` 配 `POS_CURVE`, **压根不看 spread**; 对应那条测试的 docstring 也在说「门槛必须与打分同源」而实际只比对了硬编码标签, **相对自己的立意是空的**。按用户「打分不能动」的约束, 修法是如实改写 + 新增守卫钉住**当前的真实约束**(几何确实不进把握分, 与 R229 的剥离一致), 而不是把 spread 接回打分。9 处变异逐一确认会红(geometry 丢方向→16;_pace 自己造词→3;方向判据改回平行式子→2;中性说法改回加速/减速→2;常量又定义两次→1;注释塞一个没算过的数→1;偷偷动阈值→3;那句假话回来→1;配色改回按词取→1)。 | 改 `indicators/keltner_geometry.py`(+`accel_cn()`/`bear_structure()`, 三套措辞并一套, 删重复常量与假注释, `explain()` 分方向)、`decision-board/cells.tsx`(配色改按 level 取);新增 `tests/test_pace_wording.py` 33 条;**`opportunity_score.py` 零改动**(v2 打分稳定版);后端 pytest 全绿, 前端 tsc + eslint(0 error 0 warning) + build 全过 | 低(纯措辞与配色; 阈值、判定、打分口径全部未改) | 可 |
| R277 | **「间距」从「走势」列拆出来独立成列, 并把加速度接回可见处; 顺带修了快慢在空头一侧说反话**(用户: 「那把走势列的分离度拆分出来成为完整的一列」)。**先量后改**: 合成路径 + 真均线实测确认「间距」与「加速度」不是两件事, 是同一个量的一阶与二阶 —— 匀速涨 0.10/天 → 间距 5.000、加速度 0;匀速涨 0.20/天 → 间距 10.00、加速度 0(**速度翻倍间距正好翻倍**); 而匀速时间距**不随时间增长**(第 150/300/399 天都是 5.000)。也就是 **间距 = 50 × 速度 ÷ ATR**, 加速度是它的变化率。所以两个读数同格(R219「一个话题一行」), 走势列则回到只讲方向(六态 + 三尺度对齐), 三行降两行, 11%→9%,匀 5% 给新列。**拆的过程里发现加速度此前在决策台上根本看不见**: 走势列那一行写的是`align ? align.cn : pace_cn`, 而 `alignment()` 只要六态/位置/间距三样都在就返回非空(几乎永远),于是快慢那一支轮不上; **悬停里被同一个三元顶掉**, 它只剩「结论」列悬停里的一行 —— 与 R210 路 C、R214 规则②、R215③ 同一族(写在那儿、看着像在用、其实轮不上)。**外加一个用户问出来的真错**:加速度是**带方向**的(`a1 = v1 − v2`, 符号表示往多头还是往空头变), 不是「变快还是变慢」, 可「还在加速/正在放慢」读起来正是后者 —— 实测 spread=-2.0 / a1=-0.30 出来是「下跌中 · **正在放慢**」,而那只票在**加速下跌**。空头结构换一套说法(跌得更急 / 跌势在缓), 上涨那一侧一个字没动;三条线挤在一起时不分方向(R224: 那时 spread 的正负是噪声, 拿它挑措辞会让同一只横盘票在两句话之间随机跳)。这与 R207(「走得过头了」把只对涨过头成立的话配给深跌票)、R215①(「涨势转弱」配给已经跌完的票)同一族:**措辞只考虑了上涨那一侧**。**这个方向错误一共有三处, 第三处在导出件里**: `decisionBoardExportColumns.ts` 的「通道态势」列自己从 `accel.level` 另算了一套(accel→提速 / decel→变慢), 于是一只加速下跌的票导出成「变慢」—— 而导出件是**拿去发给别人的**, 自己未必会看, 最不容易发现。根本毛病不是措辞而是**同一句话有两个产地**: 两个产地必然漂移, 修了一处另一处照旧说反话; 改成直接读 `ph.pace_cn`, 并补一条守卫钉住(那一列此前**没有任何守卫**, 所以第三处错才活到现在)。**守卫这一轮做了一处结构性升级**: R254 那条「点不到的排序键全删掉」列的是一张**手写死键名单**, 靠人记得维护 —— 这次 `spread` 复活就得手动把它从名单里挑出来, 说明名单本身是要人记得改的东西(R272 注册表漏登记同一形状)。改成反向枚举 `SortKey` 全部取值、逐个要求 thead 里真有`cycleSort('x')` 能选中(默认键除外, 它是第三下回落的目标)—— **死键一出现就红, 不需要谁先发现它死了**;这条第一次跑就抓到 `urgency` 并证明它不是死键(默认键), 于是判据补得更准。12 处变异逐一确认会红(快慢退回不分方向→3;挤在一起也按间距挑措辞→1;上涨侧被顺手改→4;间距列不印快慢→1;不印成熟度→2;成熟度挂条件配色→1;成熟度印回走势列→1;走势列回退到快慢→1;间距列没表头→1;BOARD_COLS 加列而 thead 没加→3;间距按绝对值排→1;间距缀回走势表头→2;导出自造快慢措辞→1)。**其中「按绝对值排」第一轮没被抓到**(取绝对值会让崩得最惨的票与走得最强的票并排顶在最前, 而表头写着「带符号」), 补了一条 R251 形状的「表头说的和实际做的一致」。另: 本仓库**第七次**「断言被自己的注释喂饱」—— 这次是反向(我在注释里复述了 `ph.pace_cn`, 让「不许出现」的断言红了), 改用共用的块级剥注释工具。 | 改 `indicators/keltner_geometry.py`(只动 `_pace()` 措辞, **阈值一个没动, 打分口径零影响**)、`decision-board/cells.tsx`(+`SpreadCell`, 走势列去掉成熟度与快慢回退)、`lib/decisionBoardExportColumns.ts`(快慢改读同一个产地)、`WatchlistDecisionBoard.tsx`(+「间距」列: BOARD_COLS / thead / tbody / 排序键);`tests/test_board_columns.py` 121→129 条、`tests/test_keltner_geometry.py` +4 条;后端 pytest 全绿, 前端 tsc + eslint(0 error 0 warning) + build 全过 | 低(措辞与版面; 判定、阈值、打分口径均未改) | 可 |
| R276 | **决策台加「只看某个分组」下拉**(用户: 「这里还要加个选择按钮, 能下拉菜单只看哪个分组」)。下拉本身没什么好说的 —— **复用「加入自选」那个 `WatchlistGroupMenu`**(定位/键盘/点外面关闭/配色全现成), 选中时按分组自己的颜色亮起, 与自选页同一套色板; 两份数据(`QK.watchlist` 取归属、`QK.watchlistGroups` 取名录)都走已有的 key, 与自选页/侧栏/监控共用同一份 React Query 缓存, **不新增接口、不多一次请求**。`/watchlist/enriched` 只出行情不带 `group_ids`, 归属必须另取 —— 想当然去 enriched 的行里读的话每一只都算「未分组」, 选任何分组都是空表**而且不报错**。**真正花力气的是「筛掉的东西必须有人交代」**: 加一个筛选就是加三个「东西会凭空消失」的入口, 而这个仓库里同一形状的毛病刚连着犯过三轮(R271 假孤儿 / R274 静默 except / R275 的 500)。① **记住的分组被删掉**: 选择记进 localStorage(分组是长期编队, 刷新就忘等于没做), 但记住一个筛选就必须管它过期 —— 自选页删掉那一组之后, 决策台会**永远是空表, 而底下写着「自选为空」**, 那句话既说错原因又把人指向完全错误的动作。名录到位后核对一次, 对不上就退回「全部分组」**并明说**(悄悄退回是另一种静默)。② **定位定不到**: 原来 `locate()` 只认「只看持有」, 连早就存在的「只看要动的」都没认 —— 票被挡住时点「定位」看起来就是没反应。改成三个筛选逐个判、**挡路的一次全撤掉**(只撤一个的话行还是不出现), 并说出撤了哪几个; 反过来**没挡路的不许顺手关掉**。③ **下拉里那些数字的口径**: 给的是「选它之后你能看到几行」而不是「这个组里有几只」—— 开着「只看要动的」时两者能差很远, 写 8 而点进去 0 行, 人只会以为它坏了。口径写在菜单标题上, 免得反过来被读成「这个组只剩 3 只票了」; 一票多组两边各计一次(与 `lib/watchlistGroupStats.ts` 同口径); **今天一只没命中的分组也要列出来**(先按名录铺 0 再累加, 否则它会从菜单里整个消失, 人会以为分组被删了)。归属还没加载完时**一律不筛** —— 空的归属表会让每一只都看着像「未分组」, 那一瞬间的空表不是真的。**顺带纠正三处早就在说假话的数字**: 表头「N 只 · 持有 M」两个数原本来自不同批次(N 筛过、M 是全自选), 加分组后天天撞见「12 只 · 持有 8」而那 12 只里一只持仓都没有 —— 改成同一批, 并把「自选共 N」写在后面; 导出页脚的分母原来传筛后行数, 于是永远写成「导出 12 只(自选共 12 只)」, 而那句话的用处正是让人知道筛掉了多少; 空表提示不再一律说「自选为空」, 改成点名是谁挡的。另外补掉 `scoped` 那个 useMemo 漏掉的 `phases`/`plays` 依赖(实际不串数据, 四份同源, 但漏依赖是下次拆查询才会爆的雷), 并删掉一条**早已失效的 `eslint-disable`** —— 留着一条不压制任何东西的豁免和留着一条假警报是同一件事(R275 那条教训)。10 处变异逐一确认会红(分组没了不退回→1;归属没到就开筛→1;数字改成组里有几只→1;空分组不铺 0→1;空表退回「自选为空」→2;表头持有退回全自选→1;导出分母退回筛后→1;定位退回只认持有→2;哨兵改回 string｜null→1;菜单不写口径→1)。 | 改 `stock-analysis/WatchlistDecisionBoard.tsx`、`lib/storage.ts`(+`boardGroupFilter`);新增 `tests/test_board_group_filter.py` 22 条;后端 pytest 全绿, 前端 tsc + eslint(0 error 0 warning) + build 全过 | 低(纯前端行筛选, 不改任何判定, 也不新增接口) | 可 |
| R275 | **修行业分析/概念分析 500 —— 一个用了没定义的变量, 是我 R145 引进来的**(用户: 「这几个页面好像不正常了」「都在报错 500」)。`api/screener.py` 的 `market_snapshot` 最后一行用了 `ext_value_maps`, 而**这个名字在函数里从来没被赋值** —— 走到那一行必然 `NameError` → 接口 500 → 行业分析、概念分析、成分股弹窗三处的行情数字全空。**引进它的正是 R145, 而那次提交的标题就是「修…500」**: 从别处(那边有这个局部量)复制一段过来, 变量没跟着来。这个接口**压根没有「扩展列」这个概念**(不收 `ext_columns`, 只出一份轻量行情快照), 所以修法是**整行删掉**, 而不是补个空 dict —— 补进去只会把一段没人要的逻辑留着。**排查过程里两处值得记的判断**: ① 截图副标题显示的是 `2026-09-10 14:04:48`(**带时分秒**), 而 `market_snapshot` 返回的 `as_of` 是 `str(date)` 不可能带时间 —— 那个值是 `?? rowsQuery.data?.date` 回退来的, 由此在没有日志的情况下断定「这个接口没给出 as_of」, 定位到它而不是去猜数据。② 同一把尺子扫全仓时 F821 报了两条, 第二条 `daily_pipeline.py` 的 `_date` 用在**函数签名注解**里, 而该文件有 `from __future__ import annotations`(注解不求值) —— **不会崩, 是假警报**; 差一点把数据管道报成大事。仍把它的 import 提到模块级: 假警报是真错的掩护。**顺带说清连板梯队那个「降级」不是故障**: 它是系统在正常告知「真假涨停判定降级 —— 盘中五档盘口未就绪, 收盘后自动恢复」, 那一页数据是全的。**真正的教训是这道闸本该早就存在**: 这类错 `ruff --select F821` 一秒钟就查得出来, 却在仓库里躺了九天 —— 原因不是没人跑 ruff, 而是**跑了也看不见**: 全仓默认规则下有两千多条中文标点告警(RUF001/002/003), 真错埋在噪音里。所以新增的闸**只选 F821 这一条, 期望值是零**, 不给「基线」留口子(基线一旦允许非零, 下一个真错又会混进去); 并且**给这道闸本身加了自校验** —— 塞一个已知的坏文件进去确认它真的会报, 否则规则名打错时它会永远绿而什么都没查。2 处变异逐一确认会红(把那个 bug 放回去→2;把闸的规则名写错→1, 正是自校验那条抓到的)。 | 改 `api/screener.py`(删一行)、`jobs/daily_pipeline.py`(注解用的 import 提到模块级);新增 `tests/test_no_undefined_names.py` 4 条;后端 pytest 全绿 | 极低(删掉一行永远会抛的代码) | 可 |
| R274 | **今日总览自检: 把十几处「静默跳过」改成显式上报**(用户: 「有没有办法验证今日总览的所有显示有没有问题、是否在正常工作」)。**查下来问题不在「会不会崩」, 在「崩了没人知道」**: `_build_overview` 里有 **15 处 `try/except`, 每一处都只写一行日志然后继续**。那个设计本身是对的(中观算不出来不该拖垮整页), 但它**缺了另一半** —— 失败之后页面照常渲染, 那个区块只是空的或少一列, 而看的人**根本分不出「今天真没有」和「算挂了」**; 日志在服务器上, 没人会去翻。**没有另写一个自检工具去重跑一遍** —— 重跑的那一次和你屏幕上看到的这一次未必是同一个结果; 这里记的就是**这一次渲染的真实情况**, 零猜测。加 `_Health` 收集器, 15 处逐一登记, `health` 随响应带回前端。**分两档因为后果不一样**: `block`(大盘红绿灯/通道/门槛/中观/ATR 快照 —— 整块内容没了, 界面上是空的, 要醒目)与 `detail`(注记/胜率/量能等 —— 主体还在, 少个标, 提一句就够)。同一个 key 反复失败(逐只算 ATR 那种循环)**只记第一条错 + 计数**, 否则一次网络抖动能刷出几百条一模一样的; 错误文本截到 200 字。**外加另一类问题: 数据陈了。** 什么都没报错, 但整页数字是几天前的(收盘后管道没跑就是这样)—— **这一条最阴, 页面看起来完全正常, 而你在用过期数字做决定**; 按 `as_of` 算出 `stale_days` 一并报。前端顶部一条自检带, **一切正常时一个像素都不渲染** —— 常驻一条「运行正常」的绿条, 看两天就成了背景板, 真出问题那天照样被忽略。守卫沿用 R272 学到的那条纪律: **登记表的完整性由测试保证, 不靠自觉** —— 扫 `_build_overview` 里每一个 `except`, 后面必须跟着 `_h.skip(...)`(漏一处就退回静默跳过, 而这种漏最容易发生: 加段新逻辑顺手包个 try/except 完全合理, 自检就此有个洞); 并双向核对 key 与名录(用了没登记的 → 界面上冒出英文 key; 名录有而代码没人用 → 名录开始说假话)。6 处变异逐一确认会红(漏掉一处登记→3;用了名录外的 key→3;反复失败每次都记→1;不报陈数据→1;health 不进响应→1;正常时也常驻绿条→1)。 | 改 `api/today.py`(+`_Health`、15 处登记、`health` 进响应)、`lib/api.ts`、`pages/Today.tsx`(+`TodayHealthBar`);新增 `tests/test_today_health.py` 31 条;后端 pytest 全绿, 前端 tsc + eslint(0 error) + build 全过 | 低(只加观测, 不改任何判定与显示逻辑) | 可 |
| R273 | **复盘三个页签各加一条状态时间轴**(用户: 「这三个部分搞状态时间轴, 类似图片的」, 指市场环境页那条色带)。**它补的是复盘弹窗一直缺的那一层**: 三个页签原本都是「今天怎么样」+「逐日/逐段明细」, **中间缺一层全景** —— 这半年到底多头段落多还是空头段落多、切换得频不频、最近这一段在整段里算长还是算短; 逐日表答不了(120 行滚下来记不住), 结论卡也答不了(它只说今天)。一条色带扫一眼就有答案, 而它只占约 40px —— 花在这上面的空间是值的。**三条各画各的, 不是同一张图画三遍**: 趋势状态画六态; 通道结论画合成后的**那一句结论**; **组合速查画三档各自(三条并排)** —— 那才是这一栏的主题(27 格讲的正是三档的组合), 合成后的单条带子看不出「短档先动、中档跟上、长档最后翻」这种节奏, 画结论就和通道结论那条重复了。**最容易错也最不容易发现的一处**: 复盘接口的 `rows` 是**新→旧**(表格要把最近的排在最前), 直接铺出来时间轴就是倒着的 —— **倒过来之后图还是好看的, 只是每一段的含义全反了, 光看颜色根本看不出来**; 统一过一个 `chrono()`, 三个构造函数逐个钉住。配色两条规矩: 六态用**同色相深浅**排成从最多头到最空头的梯子(六个杂色只会让色带看起来是花的, 读不出方向); 通道结论**直接引 `VERDICT_BAR`** 而不是另抄一份, 与决策台「结论」列同色。另外两处细节: 算不出来的那天给灰格子而**不是跳过**(跳过会让时间轴悄悄变短、后面日期全部错位); 格子给 `min-w-[2px]`(250 天挤一条, 纯 `flex-1` 在窄容器上会被压成 0 宽而整段消失)。图例与起止日期挤在标题同一行 —— 单独占一行的话三个页签一共多花 48px, 那正是 R269/R270 刚省下来的。为避免循环引用(组合速查在另一个文件里, 由弹窗引入), 配色与构造函数抽进 `lib/reviewTimeline.ts`, 只放纯数据与纯函数。6 处变异逐一确认会红(整体不反转→1;有一条漏走 chrono→1;六态改成杂色→1;算不出来的天被过滤→1;去掉最小宽度→1;组合速查改画合成结论→1)。 | 新增 `stock-analysis/StateTimeline.tsx`、`lib/reviewTimeline.ts`;改 `StockReviewDialog.tsx`、`decision-board/ComboView.tsx`;`tests/test_review_verdict_layout.py` 22→32 条;后端 pytest 全绿, 前端 tsc + eslint(0 error) + build 全过 | 低(纯展示层, 数据全部来自复盘接口已有的 rows, 不加接口) | 可 |
| R272 | **修数据体检自己的三个错 —— 是用户拿真机跑了一次体检才暴露的**。R271 上线当天在真实数据上就报了两条假警报和十个假孤儿, **而且我那条「注册表不许漏登记」的守卫全程绿灯**。① **格式和形状被我混成了一维**: 对任何非目录文件都是 `read_text(utf-8)` + 整份 `json.loads`, 于是二进制的 `watchlist.parquet` 报「codec can't decode byte 0xea」、逐行的 `alerts.jsonl` 报「Extra data: line 2」—— **两份完全健康的数据被判成读不动**。拆出 `fmt` 这一维(json/jsonl/parquet/dir/secret)逐种处理; parquet 走 `scan_parquet` 顺带能查缺列, JSONL 逐行读且**一行坏不判整份死**(追加写被打断只会留半行, 前面的好行还在)。**体检误报比不报更糟** —— 人看两次假警报之后就再也不看它了, 真问题跟着一起被忽略。② **守卫只认一种路径写法**: 第一版正则只匹配 `data_dir / "user_data" / 字面量`, 而 `JsonReportStore(filename)` 把文件名当构造参数传、`strategy_cache` 用模块常量 —— 六处**在用**的存储(AI 三类报告/连板复盘/策略缓存/耗时统计)因此没登记, 被当成「孤儿文件」报给用户, **差一点让人去删自己正在用的数据**。**补丁不是再加两条正则**(那只是把洞挪个位置, 下一种写法照样漏), 而是反过来**宁可多收也不能漏**: 扫所有形如 `xxx.json/jsonl/parquet` 的字面量, 每一个要么登记、要么进一张写明理由的排除表 —— **排除表过时是安全的**(顶多忽略一个不存在的名字), **注册表漏项是危险的**(体检会对着它说一切正常)。③ **各 store 自己留的 `.bak` 被当成孤儿**(只排除了 `.bak-时间戳`)—— 等于催人删自己的备份。**真孤儿只有两个**: `mining_autopilot.json`、`workflows.json`(全仓零引用, 退役功能的残留)。另外报告加上体积, 并单列一栏「占地方的派生数据」—— 真机上 `strategy_cache.json` 有 **10MB**, 不摆出来根本看不出该清哪个。(顺带: 这一轮那条旧守卫**第六次**被我自己注释里的标识符喂饱 —— 换成新写法时一并解决。)4 处变异逐一确认会红(parquet 退回按文本读→2;JSONL 退回整份解析→2;`.bak` 又当孤儿→1;漏登记一处在用 store→2)。 | 改 `services/data_doctor.py`(+fmt 维度、+6 处登记、备份判定)、`lib/api.ts`、`settings/System.tsx`(体积 + 占地方那一栏);`tests/test_data_doctor.py` 22→31 条;后端 pytest 全绿, 前端 tsc + eslint(0 error) + build 全过 | 低(仍然只读 + 补默认值) | 可 |
| R271 | **数据体检 —— 盘上那些跟着功能一起长出来的老文件到底缺了什么**(用户: 「我不断改这个系统, 不断沿用以前的数据, 但是本地的数据可能不完整 —— 我加了东西或者删除了东西, 但是系统没有重头开始, 会有残留的数据文件, 缺字段或者不完整」)。**这类问题最麻烦的地方是它不报错。** 这套代码读盘时几乎处处用 `.get()`, 缺字段不崩 —— 可那正是更阴的: **缺字段被静默当成「用户没设过」**。`positions.json` 就是活例子: 文档字符串到今天仍写着结构是 `{held, cost, updated_at}`, 而 `weight`(仓位比例)是后加的; 老记录没有它, 组合层面的「总仓位 vs 姿态基调」「当前→目标差额」全部按空值走, 一声不吭。**三类, 处置办法相反**: 用户数据(自选/持仓/账本/消息面/台账/偏好)**不可重算, 只能补齐**; 派生数据(行情分区/enriched/AI 报告/回测)**可重算, 坏了删掉重跑**(补它是在给假数据续命); 孤儿文件(功能删了文件还在)**只报不删**。**一个字节都不删**是刻意的: 判断「没人读」靠的是一张手写注册表, 表漏一项自动删除就等于删了用户数据。**注册表的完整性由测试保证而不是靠自觉** —— `test_R271_注册表不许漏登记` 扫源码里所有 `data_dir / "user_data" / "X"` 的引用, 没登记就红; 否则这张表迟早过时, 而体检会对着漏掉的那项说「一切正常」。**密钥两道门**: `auth.json`/`secrets.json` 一个字节都不读(报告会经接口、会被截图、会被贴进聊天), 外加一道「就算读了也不许进报告」。补齐**先备份、只加不改、没得补就不写盘**(否则每点一次多一份一模一样的备份)。**我自己在这一轮写了两条空断言, 被变异验证抓出来**: ①密钥那条即使去掉 `NEVER_READ` 也过 —— 因为那两处声明的是 opaque, 内容本来就不进报告, 测的不是真机制; 改成「写一份故意读不动的内容进去, 它若去读必然报读不动」。②「没登记的路径不许动」那条, 三个路径**都不存在**, heal 是因为「文件不存在」才拒的 —— 为错误的理由通过; 改成三个文件都真的存在, 并逐一核对内容没被改。6 处变异逐一确认会红(注册表漏登记→10;密钥去读盘→1;补齐覆盖已有值→2;补齐前不备份→1;没登记的路径也照写→2;派生数据也给补→1)。 | 新增 `services/data_doctor.py`、端点 `GET/POST /api/settings/data-doctor[/heal]`;改 `api/settings.py`、`lib/api.ts`、`settings/System.tsx`(新增体检面板);新增 `tests/test_data_doctor.py` 22 条;后端 pytest 全绿, 前端 tsc + eslint(0 error) + build 全过 | 低(只读 + 补默认值, 不删不改已有值) | 可 |
| R270 | **另外两个复盘页签照 R269 同法重排 + 折叠件收成一处**(用户: 「这三个都是要处理排版抓住重点, 都同样的问题, 尤其是组合速查好多内容都是展示和个股当前状态没关系的, 相当于很多说明了」)。三个页签得的是同一个病: **结论上面挂一句, 底下压着一大片测量与参考资料, 而真正要翻的正文被顶到屏外**。① **趋势状态**: 四张 `text-2xl` 的涨跌停计数卡 + 封板率 + 涨停出现在, 三百多像素横在结论与逐日表之间 —— 而**代码注释自己就写着「以下是依据(测量)」**: R191 那轮把测量挪到了结论后面, 却仍然让它常驻, 只解决了顺序没解决占地。全部收进「依据」默认收起(收起时那一行直接把涨停/炸板/跌停三个数写出来, 不展开也知道有没有事); 「六态在这只票上」压成一枚芯片贴到「现在」那一行右边, 完整说明进依据; 逐日表补上 `min-h-0 flex-1` 真正吃满剩余高度。② **组合速查**(用户点名的那一处): 除了「你现在在这一格」, 剩下 26 行讲的是**别的格子**什么样 —— 查表用的参考资料, 和这只票今天的状态没关系, 却铺满整屏。R225 当初摆开它们的理由是「有了对照才知道自己在贵贱谱系哪一端」, **但那个对照只需要一句话**: 收起时用一行说清「你这一格落在偏贵/中性/偏便宜哪一段、另外两段各几格」, 想逐格看再展开。顺带补了个空状态 —— `geo` 算不出来时原来整块「你现在在这一格」直接不出现, 人看到的是一张没头没尾的对照表。③ **折叠件收成一处**: 抽出 `ReviewDisclosure`, 三处(含 R269 的依据)统一走它 —— 同一段折叠逻辑抄三遍, **这个仓库上一轮刚为「抄了四遍的 `_code_lines`」付过一次代价**。收起一律**不渲染**而非 `hidden`(收的正是长表格与二十多行卡片); 展开状态**三处各记各的**(常看读数的人和常查 27 格的人不是同一种用法, 混成一个开关谁都不合适)。**一处 TS 查不出的错**: 类名里打错一个字(`textateform-muted`), 字符串里的错 tsc 不报 —— 靠肉眼复读发现的, 记一笔。6 处变异逐一确认会红(趋势统计改回默认展开→1;26 格改回默认展开→1;丢掉贵贱定位那一行→1;共用件改成靠样式藏→1;六态说明收进依据时丢掉→1;逐日表不给 flex-1→1)。 | 新增 `stock-analysis/ReviewDisclosure.tsx`;改 `StockReviewDialog.tsx`(SideEdgeCard→芯片、新增 TrendStatsPanel、EvidencePanel 改用共用件)、`decision-board/ComboView.tsx`、`lib/storage.ts`(+2 key);`tests/test_review_verdict_layout.py` 11→22 条;后端 pytest 全绿, 前端 tsc + eslint(0 error) + build 全过 | 低(纯展示层, 判定与文案都在后端) | 可 |
| R269 | **复盘·通道结论栏重排: 结论在上, 依据收起, 正文占满**(用户: 「排版不合理, 要抓住重点, 下面的都看不到了, 重新设计这个部分的前端显示」)。病根是**主次弄反了**: 这一栏从上到下摆着四个各自带边框的常驻区块 —— 阶段卡、位置结论卡、七行依据表、分档芯片 —— 加起来约 550px; 而**一个复盘面板的正文是下面那串历史段落**, 它只分到一屏的零头, 一次露一张半。**头部是用来「一眼定调」的, 不是用来读的。** 所以头部只留两样: 现在处在哪一段(定调)、该盯什么(**这一栏唯一的行动指引, 别的都能收起, 它不行** —— 收了就只剩「现在处在下跌中」这种定性词, 没有一条能照着做的)。位置结论压成一枚芯片: 样本够时它是个判断(偏买/偏卖差多少), 样本不够时它连判断都不是, 两种情况都没有理由占一整块。阶段成因、位置结论说明、七行读数全部下沉进「依据」并**默认收起** —— 那张表自己都写着「上面两条结论就是从这些读数出来的」, **依据不该压在结论和正文中间**。**收起不等于删掉**: 依据里那三样(名称/数值/解释)是 R212 改了三版才定下来的(只给数字看不懂; 只给状态词「然后呢」答不了; 最后用户自己定的是「保持数据, 后面加一行解释」), R269 只挪位置, 内容一个字没动。收起走的是**不渲染**而不是 `hidden` —— 后者照样进 DOM、照样参与布局。展开状态记在本地。顺序即优先级: 一眼定调 → 复盘正题(各档结论好不好使) → 依据 → 历史段落。**顺手拆掉一个雷**: 扫前端源码的那个 `_code_lines` 已经被复制到 4 个测试文件里, 而这个函数我刚踩过坑(只剥注释首行, 跨行 `{/* … */}` 的后续行照样喂饱断言, 这个仓库里已经发生五次); 抽成 `tests/frontend_source.py` 一处实现, 顺带补上行内 `//` 的剥离(且不误伤 URL 里的 `//`)。**改名撞断了一处守卫锚点**: `test_glossary_hides_math`(界面不许泄露算法那条纪律)按 `function ChannelPanel` 定位显示区, 改名后找不到 —— 锚点挪到 `VerdictHeader`, 这反而**扩大**了扫描面(老锚点排在阶段卡与位置结论卡之后, 那两块的文案一直没被扫到), 挪完照样全绿。5 处变异逐一确认会红(依据改回默认展开→1;「该盯什么」也收进依据→1;依据摆回芯片之前→1;丢掉解释列→1;收起改成靠样式藏→1)。 | 改 `stock-analysis/StockReviewDialog.tsx`(三个区块合成 `VerdictHeader` + `EvidencePanel`)、`lib/storage.ts`(+1 key);新增 `tests/frontend_source.py` 与 `tests/test_review_verdict_layout.py` 11 条;后端 pytest 全绿, 前端 tsc + eslint(0 error) + build 全过 | 低(纯展示层, 判定与文案都在后端) | 可 |
| R268 | **前端脱敏: 拆掉能找到上游作者的一切入口**(用户: 「前端页面脱敏掉作者的信息, 包括能找的作者的任何入口」「我打算给别人用, 所以要隐藏不暴露信息」)。这台机器要交到别人手上, **界面就是别人能看到的全部**。清了七处: 5 条指向上游仓库的链接(策略生成器的「策略开发指南」×2、登录页的「访问密码部署文档」、财务页的「前往 Issues 推荐」、设置·关于的「检查更新 → GitHub Releases」、数据源编辑器的「AI 帮你整理映射」)、**2 处上游作者的 TickFlow 推荐返佣码 `?ref=V3KDKGXPEA`**(引导页与 Keys 设置页)、以及引导页「个人开源项目」「基于 MIT 协议开源」的表述。**返佣码那两处性质和别的不同**: 它不只是「能找到作者」, 而是**别人在这台机器上注册就把佣金记到他名下** —— 注册入口本身留着(TickFlow 是这套系统唯一的数据源), 去掉的只是 ref。**删链接不留断头**: 只摘 `<a>` 会留下「点上方链接」「详细配置说明见」这种指向空气的句子, 比留着链接更糟 —— 用户照做却什么也找不到; 所以每处都把有用的信息就地内联(忘记密码怎么办、映射不会填怎么办)。**查过但没动的两处**: 企业微信/飞书/钉钉的开发者文档链接是第三方数据, 与作者无关; `ExtPages` 里那个 netlify 默认网址查了 git 记录是**用户自己**(`always1ov`)加的, 不是上游的东西。守卫**扫整个前端源码目录而不是几个已知文件** —— 上游作者会继续往他的页面里加指向自己仓库的链接, 每次 merge 都可能带回来一批, 靠人肉复查一定会漏; 新加的、新 merge 进来的文件一样跑不掉。**注释里也一并禁**(源码会进 sourcemap, 也会被人直接翻)。反向也守一条: 一刀切把 `tickflow.org` 全删掉, 别人就不知道去哪拿 Key 了, 所以钉住注册入口必须还在。5 处变异逐一确认会红(Issues 外链放回来→4;返佣码放回来→1;「开源项目」放回来→1;只删链接留断头文案→1;把注册入口也一刀切删掉→1)。**打包产物复扫干净**, `dist/` 不进仓库、每次构建重新生成, 源码干净页面就干净。 | 改 `screener/StrategyBuilderDialog.tsx`、`pages/Auth.tsx`、`pages/Financials.tsx`、`pages/Onboarding.tsx`、`settings/System.tsx`、`settings/DataSourceEditor.tsx`、`settings/Keys.tsx`;新增 `tests/test_frontend_no_author_traces.py` 8 条;后端 pytest 全绿, 前端 tsc + eslint(0 error) + build 全过 | 低(纯展示层, 不碰任何逻辑) | 可 |
| R267 | **拆掉「AI 一键分组」, 改成照着文章自己分好的小分队导入**(用户: 「ai 不再自动分组, ai 分组的功能按钮也不要了; 我的内容只会是 md 格式, 里面其实已经分类好了小分队, 需要划分好导入到不同分组」)。**方向的转变是这条的全部**: 分类不再由模型按题材猜, 而是**读作者已经分好的那一份** —— 作者分好的类比模型归纳的准, 而且是用户自己认的那套。① **拆干净 AI 一键分组**: 删 `watchlist_ai_group.py`、`/ai-group` 与 `/ai-group/apply`、`AiGroupApplyRequest`、api.ts 两个函数、Watchlist.tsx 里的按钮与整个方案确认弹窗、`test_watchlist_ai_group.py`。**顺带解掉一个真隐患**: `apply` 用的是互斥的 `set_group`, 手工打的多组标签会被它无声抹平 —— 那正是上一轮跟用户确认「一只票能不能同时在多个分组」时查出来的唯一破口, 功能拆了, 破口也就没了。② **解析层按真实输入重做**(用户给了一整篇公众号文章做样本): 加粗小标题就是小分队(`**船舶**`/`**军工**`), **group 取最细的那一层**(剑桥科技归「光模块」不是「科技」—— 想合并是导入界面上点一下的事, 粗粒度分完再想拆开就没依据了); 原文**加粗的股票名**是作者的重点票, 用 `starred` 带出来; 一只票在两个小分队都出现就带两个分队, 导入时同时进两个分组。新增**唯一前缀简称**: 正文写「中际」而主数据是「中际旭创」—— **只认全库唯一命中的前缀**, 「中航」对上中航成飞/沈飞/电子一堆就不认, 退回去让人自己定。唯一性这道门是它能存在的全部理由: 没有它, 前缀匹配就退化成「挑一只最像的」, 而**挑错了是无声的**。全名永远优先于简称。送审上限 6000→20000 字 —— 这类文章的板块清单往往排在后半段(前半段是时事闲聊), 截短等于专挑重点丢。③ **导入弹窗加「小分队 → 目标分组」映射**: 每队默认同名并入、没有就新建同名, 可改选已有或「不导入这一队」; **建组推迟到点导入那一刻**(在映射界面上改主意的过程里建组会留下删不掉的空分组); 目标**按行算而不是全局一个** —— 一个全局目标说不清「船舶那队进 A、军工那队进 B」; 写入按目标分组集合分批(`batchAdd` 一次只能给一批标的挂同一组分组)。**另外补上 R265 的一个真漏**: `watchlistImportText` 会调模型却在用默认 30s 超时(读完整篇文章必超), 是被既有的 `test_ai_endpoint_timeouts` 抓出来的 —— 那张表当初漏登记了。**顺手修好一个反复踩的测试坑**: 断言用的 `_code_lines` 只剥注释首行, 跨行 `{/* … */}` 的后续行照样留下, 断言又一次被自己的注释喂饱(第五次), 改成按块剥。8 处变异逐一确认会红(前缀表不做唯一性剔除→1;简称抢在全名前→3;一只票拆成两条候选→2;重点标记被后来的抹掉→1;每行目标退回全局→1;不分批写→1;不导入的队仍可勾→1;同名分组不复用→1)。 | 删 `services/watchlist_ai_group.py` 与其测试;改 `services/watchlist_ai_text.py`(提示词/前缀表/分队合并)、`api/watchlist.py`(-2 端点 -1 模型)、`lib/api.ts`、`components/WatchlistImportDialog.tsx`、`pages/Watchlist.tsx`、`tests/test_ai_endpoint_timeouts.py`;`tests/test_watchlist_ai_text.py` 37→63 条;后端 pytest **3412 全绿**(11 skipped), 前端 tsc + eslint(0 error) + build 全过 | 中(拆了一个功能, 换了另一个的输入形态) | 可 |
| R266 | **消息面重排: 长文一律收起, 超出了才给展开**(用户: 「消息面页面重新排版, 太占用空间了, 显示的时候也看不全」)。病根是这一页把 **AI 综合出来的一大段和每条笔记都全量铺开渲染** —— 一段综合动辄几十行, 一进页面整屏被它吃光, 输入框和卡片墙全被顶到屏外; 卡片墙里一条长记录能撑出上千像素的卡片, 同一行旁边那条只有一句话, 高度差到整面墙没法看。新增可复用的 `CollapsibleText`, 总览(收 6 行)、笔记凝练与原始正文(各收 8 行)三处都过它。**两个判断值得记**: ① **给不给「展开」按内容真实溢出决定, 不按字数猜** —— 列宽会变(窄屏一列/宽屏三列), 同一段话在窄列里行数翻倍, 字数阈值必然在某个宽度上判错: 要么截断了却没有展开入口(**那正是用户说的「看不全」**), 要么没截断还挂个没用的按钮; 所以量 `scrollHeight` 并挂 `ResizeObserver` 跟着尺寸重量。而且**要拿收起高度去比, 不能比 `clientHeight`** —— 展开时 clientHeight 就是全高, 永远比不出溢出, 按钮会当场消失, 人就再也收不回去了。② **收起用 max-height 不用 `line-clamp`**: 这一页正文是 `whitespace-pre-wrap`(AI 凝练带换行缩进), line-clamp 要把元素变成 `-webkit-box`, 与 pre-wrap 同用各浏览器表现不一致; 截断处让**文字自己**淡出(遮罩)而不是盖底色渐变 —— 这一页三处底色各不相同, 盖底色就得每处传一个对应颜色, 传错就露馅。**收起是藏起来不是砍掉**, 展开一个字不少。另外三处零碎: 总览展开状态记在本地(默认收起, 但看惯展开的人不用每次重点)、那段「不会覆盖规则层」的边界说明只在展开时出现(空状态下仍然照说, 那是功能前提)、新增框写死 `rows={1}`(textarea 默认 2 行而 `min-h` 只管下限, 空着时白占一行高)。展开的原文小框加 `max-h-48` 自滚。6 处变异逐一确认会红(总览改回铺开→1;卡片改回铺开→1;改成按字数猜→2;去掉 ResizeObserver→1;拿 clientHeight 比→1;脚注永远显示→2)。 | 新增 `components/CollapsibleText.tsx`;改 `pages/UsageNotes.tsx`、`lib/storage.ts`(+1 key);新增 `tests/test_news_desk_layout.py` 15 条;后端 pytest **3398 全绿**(11 skipped), 前端 tsc + eslint(0 error) + build 全过 | 低(纯展示层, 数据与注入链一个字没动) | 可 |
| R265 | **批量导入自选加第五条路: 粘一段话交给 AI 认票**(用户: 「粘贴一段话就能发给 ai 解析好导入到指定分组, 要处理好我历史可能已经导入过」)。已有四条入口(截图/CSV/TXT/粘贴代码)都假设**代码就摆在文本里**, 可日常真想导的东西是「今天复盘: 光模块方向中际旭创、新易盛继续走强, 消费电子里立讯精密(002475)补涨」—— 三只票只有名字, `extract_codes` 只认六位数字, 老路一只都抽不出来。**这条新路只做一件事: 让 AI 回答「这段话提到了哪几只」, 匹配交给系统。** ④ **产出与截图/CSV 完全同构的候选**, 于是勾选确认、目标分组、「已在自选就并组不新增」整套现成流程一行都不用改 —— **用户点名要处理的「历史已导入过」是白捡的**: 候选带 `already_in_watchlist`, 前端 `rowState` 把已在自选的默认不勾、已在目标分组的划进「已略过」, 写入走 `add_batch`(重复添加保留既有分组, 只并入尚未属于的)。本模块自己只管一段话里同一只提到多次按首次去重。**安全底线在「AI 给的代码一律不直接采信」**: 模型把「立讯精密」的代码记成 600519 这种错, 光看代码是合法六位数, 照单全收就把一只完全无关的票导进自选、事后根本查不出来 —— 所以名称与代码各回主数据查一次, **两边都中却指向不同的票就判存疑不匹配**, 原因写在候选上给人看; 名称**重名也不许先到先得**地静默挑一只, 一样退回来由人定夺(带了代码就能定下来)。提示词里同步钉死「不要凭记忆补代码」「指数/板块/题材不算个股」。前端同一个输入框两个按钮(**解析代码** 不调 AI / **AI 认股票** 走整段), 候选行显示原文那半句 `quote` 让人核对 AI 有没有抽错。3 处变异逐一确认会红(名称代码打架改成信代码→1 条红;重名改回先到先得→1 条红;已在自选的直接剔除→2 条红)。 | 新增 `services/watchlist_ai_text.py`(除 `generate` 外全纯函数)、端点 `POST /api/watchlist/import-text`;改 `api/watchlist.py`(+1 端点 +1 模型)、`lib/api.ts`、`WatchlistImportDialog.tsx`;新增 `tests/test_watchlist_ai_text.py` 37 条;后端 pytest **3383 全绿**(11 skipped), 前端 tsc + eslint(0) + build 全过 | 低(纯新增一条入口, 老四条一行没动) | 可 |
| R264 | **`TARGET_NAG_LIMIT` 定案: 不接, 删掉**(R262 普查留的待定项, 用户问「你觉得怎么处理最好」)。常量和注释都在(「止盈提醒连刷三轮之后不再重复写进上下文」), 限流逻辑从来没写。**结论是不接**, 三条理由: ① **在趋势系统里这条限流是说反了** —— 一只票连着二十天挂在止盈线上方那正是主升浪, 是最该让模型每天都清楚知道「这只在赚钱、纪律没要求你走」的一只; 按轮次静音等于**对最该拿住的票减少信息**, 与「尽可能减少买卖次数, 趋势为王」相悖。② **它想省的东西不存在** —— 一只触线一行, 比候选清单小两个数量级, 上下文压力从来不在这儿。③ **接上的代价远大于收益** —— 要真做对得往账本存**按标的的连续轮次计数**, 还得定清归零规则(掉回线下? 加仓改了成本因而改了止盈线? 清仓又买回?), 是个要持久化要迁移要测的新状态; 且**不能在 `check_plans` 里砍**, 砍了前端那个「已到止盈线(N 只)」也会在第四天消失 —— 人是要一直看见的, 所以只能在 `build_context` 里单独砍一刀, 同一份列表两个消费者两套口径。**删掉常量, 原地留一段「为什么不接」**, 免得下一个人又当成漏掉的功能。现状(每轮全量重摆, 不随轮次衰减)加了 3 条守卫钉住, 3 处变异逐一确认会红(常量加回来→1 条红;上下文里砍掉止盈那一段→1 条红;在 `check_plans` 里接上三轮限流→2 条红)。 | 改 `services/paper_plan.py`(删常量 + 决策注记);`tests/test_paper_plan_flow.py` 17→20 条;后端 pytest **3346 全绿**(11 skipped) | 极低(删一个零引用常量) | 可 |
| R263 | **补上「有结论却没天数」的漏网之鱼**(用户: 「有漏网之鱼不显示天数」, 截图指着 688797.SH 臻宝科技 —— 结论写着「调整到位」, 后面那个「已N天」是空的)。**根因是我这一层比作者严**: 作者的 `verdict()` 用 `(bands or {}).get(...)` 取档, **缺一档照样出结论**; 而我 R246 写的 `state_key()` 要求短中长三档齐全, 缺一档就返回 `None` —— 于是 `_state_run` 拿不到今天的键, 天数整个消失。**结论和天数出自同一套档位, 却各有各的门槛, 就等于自己造了一类「有结论但没天数」的票。** 三处改到位: ① `state_key()` 的结论分支跟着作者走, 有结论就认, 不再自设「三档必须齐全」; 缺档时的组合键改成带档位字母(`s=下|m=下` 与 `s=下|l=下` 不再撞成同一个键, 否则两种不同的形态会被当成同一段连续)。② `state_series()` 改成**能算几档算几档**, 与徽标那边同口径, 不再因为缺一档就把整天作废。③ 逐日状态序列从 `len(closes) >= 120` 那道门里挪出来, 降到 `WINDOW["s"]`(20) —— **那道门是给几何量留的**(`series`/`runs`/`band_energy` 确实要长档才有意义), 天数用不着长档, 却被一起挡在门外。**几何量那道 120 根的门原样留着**, 并加了一条守卫钉住它不许一起挪。端到端在一只 80 根K(长档算不出来)的票上验过: 「没天数」→(改①后)「已1天+」→(改③后)**「调整到位 已19天 自 2026-08-14」**。4 处变异逐一确认会红(`state_key` 改回要三档→4 条红;序列改回「缺一档整天作废」→2 条红;序列挪回 120 门内→1 条红;几何量挪出门外→1 条红)。**没碰作者的 `verdict()` 与三条通道的定义。** | 改 `indicators/keltner_geometry.py`(`state_key` / `state_series`)、`services/keltner_service.py`(状态序列出 120 门);`tests/test_state_run.py` 27→32 条;后端 pytest **3343 全绿**(11 skipped), 前端 tsc + build 全过 | 低(只放宽取键条件, 不改判定) | 可 |
| R262 | **全项目死代码/孤儿代码普查**(用户: 「检查一次整个项目有没有废弃的孤儿代码死代码」)。扫了四个面: ①孤儿模块 ②导出但无人用的符号 ③未用导入/变量 ④注册了却没人调的接口。**结论是这个仓库很干净**, 真能删的只有 3 处, 都在 fork 自己的文件里: `_ma120_map`(R195 起调用方改用整份 `long_trend_map`, 这个投影没人要了)、`KIND_CN`(三个中文标签, 全仓零引用)、`V34_FACTOR_LABELS`(描述 `SCORING_VERSION` 3/4 的历史记录, 而 git 历史里**那两个版本从没上线过**)。**扫描本身要防误报, 我这一轮栽了两次**: 后端 238 个模块里报出 10 个「孤儿」, 查下来全是 `strategy/builtin/*.py` —— 引擎按 `sorted(d.glob("*.py"))` 动态加载, 不是死代码; 端点第一版报出 44 个「前端没调」, 而模拟盘那些明明在用 —— 前端拼的是模板字符串, 我取的关键词对不上, 改成「路径里每个字面段都要在前端出现」之后只剩 4 个存疑, 且全在作者文件里。**没动作者的东西**: 前端 21 个死导出分布在 9 个文件, 首次提交全是上游 —— 删了只会让每次同步都冲突, 收益为零; 4 个存疑端点同理。**留一个待定**: `TARGET_NAG_LIMIT`(止盈提醒连刷三轮后不再进上下文)**不是死代码, 是设计了没接上** —— 常量和注释都在, 限流逻辑没写。那是功能决定, 报给用户。 | 改 `services/keltner_service.py`、`services/paper_plan.py`、`services/score_ledger.py`(各删一处死符号) + `ruff --fix` 清掉 1 个未用导入;后端 pytest 3338 全绿 | 低(删的都是零引用符号) | 可 |
| R261 | **修走势列「走了多远」那一行配色挂错对象 —— R257 留下的错**(用户: 「趋势列的描述都没有统一颜色, 有些是白色字体」)。R257 把这一行的文字从「阶段」换成了「走了多远」(成熟度), **却忘了换配色** —— 颜色仍按阶段码从那张表里取, 于是同一句「走到中段」在上升中的票上是红的、在下跌中的票上是绿的、在横盘中的票上是**近白**(`text-secondary`)。**颜色在说阶段, 文字在说走了多远, 两码事。** 成熟度是一句事实读数(这一段走了多远)不是判断, 所以统一给次要色; 那张配色表改完只剩零处引用, 按守则一并删。**没有把整列刷成一个颜色** —— 六态徽标按状态、力度按排列层级, 那两处的颜色说的正是它们自己的意思, 有守卫钉着不许一起刷掉。3 处变异逐一确认会红。(另: 写这条时**第四次**被自己的注释绊住 —— 断言里查的标识符正好写在了我加的说明文字里, 文件没写入; 已改成只扫代码行。) | 改 `decision-board/cells.tsx`(一处 className + 删掉那张配色表);`tests/test_board_columns.py` 20→22 条;后端 pytest 3338 全绿, 前端 tsc + eslint(0 error) + build 全过 | 低(纯配色) | 可 |
| R260 | **同步作者 v0.2.4(41 个提交)**。20 个 `fix` + 2 个 `feat` + 1 个 `perf` + 1 个 `ops`, 外加 `backend/tests` +2018 行。作者这一轮修的都是他自己的地盘: 分钟K 补全空洞与时区、停牌分区反复误删重算、实时行情覆写日K 分区、财务因子换报告期丢值、轮动信号缺失日归位、AI 报告时间戳时区、告警查询范围约束、回测 SSE 非法参数返回 500、内存峰值与重任务并发、定时拉取清空策略缓存; 两个新功能是回测「环境数据缺口一键补算并自动重跑」与筛选器「叠加策略创建与配置体验优化」; `perf/fix(strategy)` 把渐进式 `run_all` 改成只构建一次共享矩阵 + 逐策略隔离失败。**2 处冲突, 都是「作者新加的东西」与「fork 增强」撞在同一行, 两个都保住**: ① `api/kline.py` —— 作者给详情响应加了 gzip 包装, fork 给响应挂 `live_refresh` 标记; 解成**先挂标记再包装**(顺序反了标记会被压进去看不见)。② `StrategySettingsDialog.tsx` —— 纯 import 撞行, 查过两边的函数在这个文件里都还在用(fork 的 `paramOptionValue/Label` 在第 158 行, 作者的 `toPercentages/normalizeWeights` 在 246/295 行), 两行都留。**验证**: 15 项 fork 增强逐项核对全在; 评分仍与 R134 逐字节一致; 12 个 fork 服务文件一个不少; 作者动到的 `strategy/engine.py` 里 fork 那 20 行增量一行没丢(逐行核过); `monitor.py` / `monitor_rules.py` / `ai_generator.py` / `levels.py` 合并根本没碰。后端 pytest **3336 全绿**(作者带来 +122 条, 一条没红), 前端 tsc + eslint(0 error) + build 全过。**留一个已知项**: 作者新增了 `frontend/src/lib/neighborPrefetch.test.ts`, 但 `package.json` 里没有测试运行器 —— 那是作者那侧的事, fork 不代劳。 | 合并 `upstream/main` 41 个提交(62 个文件, +3057/-368);冲突解在 `backend/app/api/kline.py` 与 `frontend/src/components/screener/StrategySettingsDialog.tsx` | 中(上游改动面广) | 可(revert 这个 merge) |
| R259 | **删掉决策台角上那个感叹号(词汇表)**(用户: 「删除掉感叹号」)。全仓只有一处引用, 删掉按钮之后 `GlossaryDialog.tsx`(R198 加的, 约 200 行)就成了孤儿文件, **一并删**(守则 R198: 不留没人调的死代码)。**关键是没把纪律一起删掉**: `test_glossary_hides_math.py` 守着两件事 —— ① 词汇表本身不泄露算法, ② **界面上其它所有通道文案同样不泄露**(决策台悬停、复盘、今日总览、导出 HTML)。①随文件退役, ②完整保留并改了路径推导。那一半才是要紧的 —— **藏不藏得住取决于最松的那一处**: R200 曾在决策台悬停里翻出「破轨门槛 2/2.5/3」, R201 在**导出的 HTML** 里翻出「MA20±2ATR / MA60±2.5ATR / MA120±3ATR」, 而导出件是拿去发给别人的。变异确认它还在干活(往悬停里塞「MA20」、塞行话「脱开」, 都会红)。测试 34→25 条, 减掉的 9 条全是词汇表专属。 | 改 `WatchlistDecisionBoard.tsx`(去掉按钮与 import);删 `decision-board/GlossaryDialog.tsx`;改写 `tests/test_glossary_hides_math.py`(退役词汇表那半, 留下界面泄露扫描);后端 pytest 3214 全绿, 前端 tsc + eslint(0 error) + build 全过 | 低(删一个只读弹窗) | 可 |
| R258 | **界面上不再出现「贵不贵」, 统一叫「结论」**(用户指着复盘逐日表那个列头: 「别用这么傻逼的描述」)。这一层在别处一律叫**通道结论**(`keltner.verdict` / 决策台那一列 / 复盘上方那个页签 / 复盘统计口径), 只有几处自己起了个口语名字 —— 一个东西两个名字, 读的人得先确认它们是不是一回事。**扫了全仓, 渲染出去的一共 11 处**, 一次改完: 复盘表列头与悬停、复盘脚注、词汇表条目名与四处正文、结论徽标悬停、今日总览的权重说明、导出件、决策台表头悬停。改法不是全部换成同一个词, 而是**分开两种用法**: 当**名字**用的一律叫「结论」, 当**解释**用的写成「这个价算高还是算低」。**顺带修了一个陈述失效**: 复盘脚注写着「切到上方的『这个价贵不贵』」—— 那是 R200 清行话时的旧页签名, R223 早就改回「通道结论」了, 这句话一直指着一个**不存在的页签**。守卫扫的是全仓渲染文本(注释里复述历史说法允许), 2 处变异确认会红。 | 改 `StockReviewDialog.tsx`、`GlossaryDialog.tsx`、`VerdictHover.tsx`、`WatchlistDecisionBoard.tsx`、`today/OpportunityTable.tsx`、`lib/decisionBoardHtmlExport.ts`;`tests/test_board_columns.py` 18→20 条;后端 pytest 3223 全绿, 前端 tsc + eslint(0 error) + build 全过 | 低(纯文案) | 可 |
| R257 | **走势列: 把「阶段」那个词从徽标上撤下来, 三行各管一件事**(用户: 「走势我也想重排描述, 不知道是换行还是重新换文本更好, 现在的版本我觉得抓不住重点」)。**诊断是文本问题不是排版问题**: 三行来自三层, 而**第 2 行的「阶段」和第 1 行的「六态」在抢同一件事 —— 方向**。用户截图那只票: 六态说「自然回升」(在涨)、阶段说「横盘中」(没走), 两句话打架而界面不提。更糟的是阶段与成熟度量的根本不是同一个东西(前者看三线重合度、后者看短长线间距), **穷举确认「横盘中」真的会配上「走到中段」/「走了很长」/「走过头了」** —— 那读起来就是自相矛盾。给了三个方案(去掉阶段 / 加行标签 / 只压行), 用户选了去掉。现在三行是 **方向(六态 已N天) / 走了多远(成熟度) / 还有没有劲(排列或快慢)**, 每行一件事。**信息一个字没丢** —— 阶段的完整说明与「该盯什么」照旧在悬停里, 有守卫钉着。另有一条守卫把这次改动的**依据**钉住(横盘中确实会配上走了一段): 哪天底层改得不再打架, 那条会红, 提示可以回头把阶段加回徽标。3 处变异逐一确认会红。 | 改 `decision-board/cells.tsx`(`ChannelStateCell` 的一行);`tests/test_board_columns.py` 15→18 条;后端 pytest 3221 全绿, 前端 tsc + eslint(0 error) + build 全过 | 低(纯显示, 判定未动) | 可 |
| R256 | **修「有的个股怎么没显示完整」—— 查出来两件事, 一件比截图里指的更严重**(用户附图: 两行印着光秃秃的 `— 已1天+`)。**① 全表几乎都是「已1天+」, 这才是主症。** R246 的提前退出绑在**序列自己的今天**上(`key != out[0]` 就停), 于是序列里只剩它自己那一档的连续段。可数天数的是调用方, 它手上的是**快照那一档** —— 两边一对不上(日线末根比快照晚一天、同一根读数不同), 序列里**根本找不到要数的那一档**, 全部落到 R248 的保底分支。改成**只在「算不出来」时停**(长期档 120 根暖机), 那既是真的没得数了, 也把循环钉在几十次以内。**顺带修了一个差一天**: 拿 enriched 快照的 `as_of` 与序列最新一根的日期对一下, 把三种情形分开 —— ①同一天且对得上(直接数) ②同一天但序列判成别的档(**跳过那一根**, 今天以徽标为准) ③日线滞后(序列里没有今天, **今天单算再接着数**)。修之前第③种少数一天。实测四种情形: 完整=5天、读数不同=1天、滞后一天=5天、滞后两天=4天(数据只剩这么多), 全对。**② 「中中中」那一格给了名字。** 27 种组合里只有它是光秃秃的 `—`(120 格有结论、4 格有补充层注记), 而旁边还跟着「已N天」, 读起来是「什么都没有, 已经 1 天」。现在印「通道中部」—— 它其实有含义: 价格落在三条通道都认可的公共区间里。**底层判定一个字没动**, 改的只是这一格印什么(与 R203 同一条路子)。6 条新测试, 四处变异逐一确认会红 —— 其中一条第一版是**空的**(只数「有几种不同的档」, 而老写法也有两种: 连续段 + 哨兵), 变异当场证明后改成盯序列长度。 | 改 `indicators/keltner_geometry.py`(`state_series` 的退出条件)、`services/keltner_service.py`(`_state_run` 多收 `as_of` + 三情形)、`decision-board/cells.tsx`;`tests/test_state_run.py` 21→27 条;后端 pytest 3218 全绿, 前端 tsc + eslint(0 error) + build 全过 | 中(动了天数的数法) | 可 |
| R255 | **结论列改成与「AI 信号」同一套排版**(用户: 「结论列也要像 ai 信号列那样排版」)。竖排、一个一行、左对齐, 说明整段折行不再单行截断: `贵不贵 已N天` / `怎么办` / `说明文字…`。**R217 当初把这一列压成固定两行**(徽标横排 + 说明 `truncate`)是有理由的 —— 那时它会摞到五层、每行高度还不一样, 行与行糊成一片。**那个顾虑现在不成立了**: 隔壁 AI 信号列 R253 起就是固定竖排三行到价预案, 行高本来就由它撑着, 结论列竖排不会再让任何一行变高; 反而两列的读法终于一致(都是从上往下一件一件读)。守卫写的时候自己栽了两次: 先用 `\n}` 收尾, 停在了 props 类型那个 `}) {` 上, 整段渲染代码根本没被检查到(断言全落空还显示通过); 改 `\n}\n` 又因为这个函数正好在文件末尾而找不到。最后改成从 `return (` 起截到下一个顶层声明。3 处变异逐一确认会红。 | 改 `decision-board/cells.tsx`(`ConclusionCell` 的渲染块);`tests/test_board_columns.py` 14→15 条;后端 pytest 3212 全绿, 前端 tsc + eslint(0 error) + build 全过 | 低(纯排版) | 可 |
| R254 | **每列排序改成三下一圈, 排序目标从 18 个砍到 10 个**(用户: 「我也不想切换那么多下」「点击第三下就恢复原状」)。**根在「一列塞多个排序目标」** —— 走势列有 5 个(六态/间距/短中长, R211 把三列并成一列时带来的)、结论 2 个、现价/涨跌 2 个: 轮换一圈要点 6 下, 而且**一个方向永远点不到**(只在目标之间乒乓, `dir` 恒为首次方向 —— 结论列因此点不出「最偏买在前」, 而注释专门写着「升序把该吸的顶上来」)。现在**一列一个目标**, 统一成 `cycleSort`: 第 1 下最该看的在前 → 第 2 下反过来 → **第 3 下回默认(该动了)**。走势=多头↔空头、结论=最急↔最闲、现价/涨跌=涨最多↔跌最惨, 其余七列同理。取代了两套并存的老写法(`toggleSort` 永远回不到默认 + `TREND_SORTS` 六下一圈)。**顺带清了死代码**(守则 R198): 砍掉 close / spread / ks·km·kl / verdict —— 它们原来靠「轮换目标」才够得着; `exit` 与 `confidence` 更早就**没有任何表头能选中**(止盈线那一列 R212 撤了, 置信度 R178 换掉了), 一直是死代码。逐列模拟四次点击核对过行为; 3 条新守卫 + 更新 2 条老的, 四处变异逐一确认会红。 | 改 `WatchlistDecisionBoard.tsx`(SortKey / `cycleSort` / 比较器 / 九处表头 / 三处 title);`tests/test_board_columns.py` 13→14 条;后端 pytest 3211 全绿, 前端 tsc + eslint(0 error) + build 全过 | 中(改了排序行为并删了六个排序目标) | 可 |
| R253 | **AI 信号的到价预案改成固定竖排, 一个一行**(用户: 「ai信号显示成这样换行」)。原来是 `flex-wrap` —— 同样三个预案, 列宽够时挤成一行、不够时折成两三行, **每一行高度都不一样**, 一屏扫下去行与行对不齐。用户在四个方案里选了「固定每个一行」: 行是高了点, 但**高度一致**, 而且价位天然对齐(方向词都是三个字 + 等宽数字), 眼睛顺着一列往下扫就行。顺带把间距从 `gap-1` 放到 `gap-1.5`、价格加 `tabular-nums`。变异改回 `flex-wrap` 会红。 | 改 `WatchlistDecisionBoard.tsx`(一处 className);`tests/test_board_columns.py` 12→13 条;后端 pytest 3210 全绿, 前端 tsc + eslint(0 error) + build 全过 | 低(纯排版) | 可 |
| R252 | **修粘性表头被行内容穿透**(用户: 「怎么背后的东西也显示出来了, 层级是不是不对」, 附截图: 「结论」表头上叠着行里的「已1天」和第二行那句说明)。**层级确实不对**: `<thead>` 只写了 `sticky top-0`, **没有 z-index** —— 而行里那个天数徽标带 `opacity-70`, `opacity < 1` 会自己造一个层叠上下文, 于是画到表头上面去了。同一个仓库里模拟盘那张表写的就是 `sticky top-0 z-10`, 这里漏了。加 `z-20`; 背景也从 `bg-surface/95` 半透明改成实心 —— 表头底下本来就是正在划走的行, 让它透出来没有任何好处, 只会把表头读成花的。**全仓扫了一遍, 同样的毛病一共三处**(决策台、因子发现、因子库), 一并修掉。守卫扫的是全仓所有 `sticky top-0`, 不只决策台 —— 它自己先栽了一次: 不去注释的话会被我写的那段说明文字骗到(说明里就含着「没有 z-index」这几个字), 现在扫描前先剥注释。3 处变异逐一确认会红。 | 改 `WatchlistDecisionBoard.tsx`、`pages/backtest/FactorDiscovery.tsx`、`pages/factors/FactorLibrary.tsx`;`tests/test_board_columns.py` 10→12 条;后端 pytest 3209 全绿, 前端 tsc + eslint(0 error) + build 全过 | 低(纯样式) | 可 |
| R251 | **修每列排序的三个 bug**(用户: 「检查每列的排序, 我感觉有点不对劲」)。**① 箭头永远亮着** —— 多目标那三列(现价/涨跌、走势、结论)传给箭头函数的是**当前排序键本身**, 判等恒真; 按「成本」排时四个箭头一起亮, 看不出按哪列排。**这是 R250 把表头缀字去掉之后才裸出来的** —— 缀字之前一直在替它遮丑(缀字只在该列选中时才出现), 所以这个 bug 藏了很久。改成收可变参数, 每列把自己的目标全列上。**② 「怎么办」排序方向完全反了** —— `order` 越小越急(按纪律走=0 … 没事=5), 首次点击却是降序, **把「没事」顶到最前面**, 而表头 title 明明写着「按急迫程度排: 按纪律走 > 今天就得动 > …」。**③ 「AI 信号」同理** —— `SIGNAL_RANK` 买入=0…观望=3, 降序把「观望」顶到最前。②③ 是**看不出来的错**: 表面上排了序, 排出来的却是最不该先看的那些。根因是老规矩「除了名称一律降序」——对「越大越好」的列没问题, 对**越小越要紧**的那几个恰好是反的。现在逐个键定 `FIRST_DIR`, 口径是「第一下就把最该看的顶到最前面」, 与默认排序(该动了, 升序)同一个方向感。5 条新测试逐一变异确认会红, 其中一条专门盯「表头说的与实际做的一致」(R214 的教训: 判定写对了、接线接错了, 而测试恰好只测了判定)。**还剩一个设计问题没动**: 多目标的列只能在目标之间乒乓, 一个方向永远点不到 —— 结论列点不出「最偏买在前」, 而注释专门写着「升序把该吸的顶上来」; 用户说不想点太多次, 方案待定。 | 改 `WatchlistDecisionBoard.tsx`(`caret` 收可变参数 + 三处调用 + 新增 `FIRST_DIR`);`tests/test_board_columns.py` 5→10 条;后端 pytest 3207 全绿, 前端 tsc + eslint(0 error) + build 全过 | 中(改了排序行为, 但每一处都是往「表头说什么就做什么」的方向改) | 可 |
| R250 | **表头只印列名, 不印当前排序目标**(用户: 「别搞贵不贵怎么办, 我就只想显示结论两个字」→ 问到走势列时「要」)。三列排序时会在表头缀出当前按哪个排 —— `结论 贵不贵`、`走势 六态/间距/短中长`、`现价/涨跌 价`。那是**把内部分层摆到表头上**, 而这几列对外就叫「结论」「走势」。排序行为一点没动(点击仍在各目标之间轮换), 说明留在悬停的 title 里, 只是不印出来。箭头留着 —— 每一列都有, 是排序方向。守卫得**直接盯源码**: 表头提取那个辅助函数会把 JSX 表达式整个剥掉, 那些缀字在它眼里是隐形的, 靠它守不住; 现在的写法是去注释、去 title 属性之后在渲染区里查禁用词, 三列各自变异加回去都会红。 | 改 `WatchlistDecisionBoard.tsx`(三处表头);`tests/test_board_columns.py` 4→5 条;后端 pytest 3202 全绿, 前端 tsc + eslint(0 error) + build 全过 | 低(纯文案) | 可 |
| R249 | **决策台按「读的顺序」重排列 + 天数说法统一**(用户: 「我有点乱, 是否有好办法整理好顺序调整显示和列」)。诊断出四条: ① **账目把判断切开了** —— 仓位/成本/浮盈 三列横在「现价」与「走势」之间, 而扫表时要连着读的是「走势→结论」; ② 「结论」一列装了两个层级(左半是事实、右半是结论); ③ 最该先看的「怎么办」是唯一收敛层, 却在第 7 列的右半; ④ 两列都叫 AI(一个是按钮一个是内容)。**用户选了改动最小的那个方案**: 只挪位置, 不拆不并、内容一个字不动 —— 顺序改成 `标的 · 现价/涨跌 | 走势 · 结论 | 仓位 · 成本 · 浮盈 | AI 分析 · AI 信号`, 也就是**认票 → 凭什么 → 我的账 → 别人的意见**。②③④ 按用户的选择留着没动。顺带统一天数说法: 六态徽标 `上涨趋势 3天` → `已3天`, 复盘条 `第 N 天` → `已N天` —— 同一行里两个天数写成两种说法, 读的人得先判断它们是不是一回事(是的: 都是尾部连续段、都按交易日、都是「到今天还在」)。这是 R246 回退到 2a3a5d1 时把 R241 的文案一起退掉了。**新增守卫**: `BOARD_COLS` 的注释从 R194 起就写着「顺序必须与 thead 一一对应」, 可**没有任何东西在守它** —— 对不上时列宽会整体错位一格, 而且看不出是哪一列的问题(重排要同时改三处: BOARD_COLS / thead / tbody, 这是最容易漏的一步)。4 条测试逐一变异确认会红; 其中表头提取那段自己先栽了一次 —— `title="… > … < …"` 里带尖括号, 直接剥标签会被骗到, 改成先剥表达式、再剥属性值、最后剥标签。 | 改 `WatchlistDecisionBoard.tsx`(BOARD_COLS + thead + tbody 三处同步)、`decision-board/cells.tsx`、`TrendStateBar.tsx`;新增 `tests/test_board_columns.py`;后端 pytest 3201 全绿, 前端 tsc + eslint(0 error) + build 全过 | 低(纯版面) | 可 |
| R248 | **修「怎么不显示天数了?」—— R246 我自己埋的静默失效**(用户原话)。徽标那一档来自 enriched 快照, 逐日序列来自日线批量, 两者在**最后一根上本来就可能不一样**(复权口径、快照与日线不是同一天收的)。R246 的 `_state_run` 在对不上时返回 `None`, 注释里写着「让上层退回今天刚变」—— **那个兜底我根本没写**, 于是对不上的行天数整个不显示。**这是同一件事第二次栽**(R234 一模一样), 所以规矩钉死在 `_state_run` 的文档串里: **徽标上有一档, 就一定有天数**。徽标印的是今天的真相, 说它「已1天」是真话(按历史那条路昨天不是这一档), 并标 `capped` 表示这是下界 —— **一个偏保守的数字远好过一个消失的字段**: 「算不出来」和「功能没部署」在界面上长得一模一样, 而这两种要做的事完全不同。造了四种情形实测(完全一致 / 快照价差一点 / 日线少一根 / 历史很短), 修之前第二种是空的, 现在全都有数。5 条新测试, 其中一条端到端复现用户撞到的那个现象; 变异回 R246 的写法 4 条红。顺带改掉一条**守着错行为**的老测试(R246 那条断言的正是「对不上就不给天数」)。 | 改 `services/keltner_service.py`(`_state_run` 的兜底 + 文档串);`tests/test_state_run.py` 16→21 条;后端 pytest 3197 全绿 | 低 | 可 |
| R247 | **模拟盘: 撤掉净值曲线、两本账左右分栏、个股带上名称**(用户: 「模拟盘我不需要显示图形, 分割为左右, 分别是全市场和自选, 然后像交易软件那样展示 ai 操作的情况, 而且我注意到个股没有显示正确的名称只是代码」)。① **净值曲线整块撤掉**, `PaperEquityChart.tsx` 一并删掉(只有这一处在用, 不留死代码)。② 两本账从纵向排开改成**左右各半**(`xl:grid-cols-2`) —— R190 当初纵排的理由是「对比靠上面那张净值曲线」, 曲线撤了, 对比就得靠两栏并排; 窄屏 <1280px 退回上下, 否则表挤成一团。③ **名称**: 新增 `_name_map()` 走 repo 的统一映射(与自选、今日总览同一份, 这一层不自己维护), 持仓 / 操作记录 / 批次三张表一次查完; 前端新增 `<Sym>` 单元格, **名称在上、代码在下** —— 一屏 `600584.SH` 谁也认不出是哪只票, 交易软件从来都是名称当主。取不到名称就只显示代码, 不编一个; 名称表读不到时安静退回而不是把整页搞崩(名称是锦上添花, 为一个名字让持仓和流水打不开是本末倒置)。**测试当场揪出一个真 bug**: 去重那行写的是 `str(x).upper() for x in symbols if s`, 而 `str(None)` 是 `"NONE"` —— 空代码会变成一个叫 NONE 的标的去查表, 已改成先判空再 str()。6 条测试, 三处关键守卫逐一变异确认会红。 | 改 `api/paper_trading.py`(`_name_map` + `get_book` 接线)、`lib/api.ts`(三个类型加 `name`)、`pages/PaperTrading.tsx`(删图 + 左右分栏 + `<Sym>`);删 `components/paper/PaperEquityChart.tsx`;新增 `tests/test_paper_names.py`;后端 pytest 3192 全绿, 前端 tsc + eslint(0 error) + build 全过 | 低(纯展示层 + 一个名称查询) | 可 |
| R246 | **结论列显示「最近这一档 + 连着几天」**(用户: 「显示每个个股的通道结论里面的最近的状态和持续时间」)。徽标从 `候选池` 变成 `候选池 已25天`, 悬停给起始日。**口径**: 交易日、含今天、**中断即重算不累计**(出现 3 天→隔一天→再 2 天 报的是 2 天, 不是 5 —— 那是两次独立的出现);数不到头时标 `+`(序列到尽头, 或再往前那天长期档暖机不够判不了)。**没有结论的那一格同样有天数** ——三档都在通道中部时徽标印的是组合注记标题(「半年低位」这类带时间词却没有天数的话)。实现只有两件东西: `kg.state_series()` 交出逐日状态(新→旧, **到第一个变化就收手** —— 调用方只要「今天这一档连着几天」, 一段 25 天的候选池不必算满 250 天; 多留一天当哨兵, 用来区分「段自己结束了」与「没得数了」), 和 `ks._state_run()` 数那一段。**判定一个字没写** —— 走作者的 `assess`/`verdict`。日线批量多要 `ma20`/`ma60` 两列: 逐日状态必须与复盘、徽标**同源**, 自己滚均线会和界面另外两处对不上。端到端对齐复盘弹窗那张卡片(同一个天数、同一个起始日), 有测试钉着。**这一版是重写的**: 前一版为同一个需求铺了 1823 行(逐日结论序列 + 双取数窗口 + 六态/磨底/在轨外的下界标记 + 一个 306 行的口径文档 + 47 条测试), 用户: 「就从 2a3a5d1 的版本开始改, 别残留什么垃圾了,最简单的办法实现」—— 全部回退重做, 现在 **189 行 + 16 条测试**。4 处关键守卫逐一变异确认会红, 其中一条变异当场证明「中断即重算」那条夹具造得不对(断档后面没再出现同一档, 与累计口径给的是同一个数),已修。 | 改 `indicators/keltner_geometry.py`(新增 `state_key` / `state_series`)、`services/keltner_service.py`(批量多两列 + `_state_run` + 接线)、`lib/api.ts`、`decision-board/cells.tsx`(`<Days>`)、`WatchlistDecisionBoard.tsx`、`today/VerdictTag.tsx`;新增 `tests/test_state_run.py`;后端 pytest 3186 全绿, 前端 tsc + eslint(0 error) + build 全过 | 低 | 可 |
| R243 | **结论徽标「调到位了」改成「调整到位」**(用户原话)。纯文案 —— 判定、`code`、语气、排序权重全没动。这个 4 字标题在 `keltner.py` 的 `_VERDICTS` 表里, 是 R44 fork 自己加的(作者那部分是 `assess` 与三档定义, 未触及)。同一条里的说明文字「季度尺度上也调到位了」跟着改。全系统没有一处按标题字符串取值(读的都是 `bottom_confirmed` 这个 code), 无连带影响。 | 改 `indicators/keltner.py`、`tests/test_keltner_verdict.py`、`docs/rule-layers.md` | 低 | 可 |
| R232 | **剥掉模型返回里的「思考过程」, 只留正文**(用户: 「所有 ai 的返回结果要检查是否有 `<think>` 开头这样分析过程也返回了, 反正目前 minimax 会有返回分析过程, 要剥离只保留正文」)。**为什么不能靠提示词**: "别输出思考过程"对推理型模型无效 —— 那是它的输出格式, 不是听不听话。MiniMax 一类把推理**内联在 `content` 里**包在 `<think>…</think>` 中(区别于 DeepSeek 系放独立的 `reasoning_content` 字段, R22 已单独处理过)。所以剥离做在收到文本的那一刻, 且**两个出口都要接**: `generate_ai_text`(非流式: 信号/优选/凝练/提炼)与 `stream_ai_text`(流式: 个股分析/复盘/财报/概念轮动/策略生成)—— 漏一个, 那条链路就照样打上屏。逻辑单独放 `services/ai_reasoning.py`(fork 自己的文件), `ai_provider` 只加两处调用, 免得下次同步上游冲突。**两条安全线**: ① **剥完不能是空的** —— 模型被 `max_tokens` 掐断时常常只有半段推理、正文一字没有, 剥成空白等于让用户看到"点了没反应", 比看到推理更糟且查不出原因, 所以剥完为空退回原文(与 R22 同一条纪律); ② **流式不能把标签切两半** —— `<think>` 七个字符可以正好跨在两个 chunk 之间, 逐块 replace 必漏, 于是 `StreamStripper` 是有状态的, 尾部凡可能构成标签前缀的一律扣住等下一块。写完自己复查改了两处隐患: 思考段内扣住的尾巴从 24 提到 64(`</ reflection >` 带空格比 `</think>` 长得多, 切开就再也合不上、后面正文全被当推理丢掉), 以及"还没发过正文时"改为攒到 4000 字(只留 64 字的话被掐断时兜底给的是半句话, 判断不出是 token 不够)。38 条测试含**逐字符喂**与开/闭标签**每一个切点**的参数化, 外加不误伤正文里 `MA5 < MA20` 这类尖括号。 | 新增 `services/ai_reasoning.py` + `tests/test_ai_reasoning.py`(38 条);改 `services/ai_provider.py`(两处出口各接一行) | 低(只动出口文本) | 可 |
| R231 | **修「AI 用不了, 一直报请求超时(30s)」—— 是同步上游带进来的回归, 不是 fork 写坏的**(用户: 「检查一遍我的 ai, 用不了了, 一直报错请求超时(30s)· /api/stock-analysis/signal/688802.SH」)。**根因**: 作者在 `f03bc38`(2026-09-07)给 `api.ts` 的 `request()` 加了全局 30 秒超时, 理由完全正当(后端依赖 polars, 偶发挂起时无超时会占满浏览器同源连接池, 表现为整页请求全部排队"已停止"), 他也给自己的回测/筛选接口开了 `COMPUTE_REQUEST_TIMEOUT_MS = 300s` 豁免 —— **但 fork 的 AI 接口一个都没拿到豁免**, `b72e6a5`(09-08 11:59 合并 v0.2.3)之后就被静默套上 30 秒, 而一次 LLM 出文本超过 30 秒是常态不是异常。**症状为什么是"一半 AI 能用一半不能"**: 个股分析 / 复盘 / 财报 / RPS 轮动走的是**原生 `fetch`**(压根不过 `request()`)没有闸, 其余走 `request()` 的全被掐 —— 这种一半好一半坏最难查, 因为它不像配置问题。**修法**: 加 `AI_REQUEST_TIMEOUT_MS = 300s`, 给 9 个真正会让模型出文本的方法(信号 / 今日总览 AI / 梯队 AI / 自选分组 suggest / 自定义信号生成 / 跷跷板甄别 / 消息面单条凝练与一大段总的 / 台账提炼)。**反向也守住**: `/ai-group/apply`、`/ai/status`、`/ladder-ai/reports`、`/today/ai/track-record`、`usageNotesSummaryGet` 继续留在 30s(压根不调模型), `strategies/ai/test` 也留在 30s 且写明是**有意的例外** —— 它发的是「Reply exactly: OK」, 连通性测试要的就是快失败。**加了自动检查**: 前端没有测试框架, 所以写成后端 pytest 读 `api.ts` 文本比对(17 条), 因为这**恰恰是人看 review 看不出来的回归** —— 上游那次提交里没有一行提到 AI, diff 里也看不见任何 fork 文件。按**客户端方法名**而不是 URL 定位(第一版按路径写完就撞坑: `/api/usage-notes/summary` 挂着一读一写两个方法, 按路径会命中先出现的读接口)。做过变异测试: 手工拿掉那个超时, 检查确实变红。 | 改 `frontend/src/lib/api.ts`(新增常量 + 9 处调用);新增 `backend/tests/test_ai_endpoint_timeouts.py` | 低(只改客户端超时) | 可 |
| R230 | **评分系统逐字节回退到 R134(2026-09-01),那是唯一一段"几天没动过"的版本**(用户: 「我是要完整回退到上一个版本的评分系统, 你先追溯以前看看多久没变动过这个打分系统, 反正我是最近才改动的, 所以起码有几天没动过的版本才对」)。**先追溯**: `git log --follow` 拉出打分核心的全部改动史 —— `973b148`(R134, 09-01 10:02)之后**整整七天零七小时没人动过**, 直到 `9193ec4`(R189, 09-08 19:13)开始一天之内连改六次(R189 两轴 / R196 通道延申融入 / R197 磨底换 ATR / R200 / R201 v3+路C+置信 / R204·R218 因子自选)。**我上一轮 R229 退到 R189 是错的** —— R189 本身就是这两天大改动的第一刀, 不是稳定基线。**这次的验收标准就是「文件和当时那一份一模一样」**: `git checkout 973b148 -- opportunity_score.py`, 并写了一条测试直接 `git show 973b148:…` 逐字节比对; R134 当年那 38 条测试原文照抄回来, **全绿, 一条没改**。**六次改动往打分里塞过、现在全部退出的**: 趋势模板(R189)、红绿节拍与磨底节拍(R189, 整层已于 R229 删除)、硬门槛 G4(R189)、三线间距与加速度(R195/R196)、压缩天数(R197)、候选路 C 及其中性新鲜度(R201)、置信系数(R201)、两轴几何平均(R189)。**除红绿节拍整层删除外, 其余在界面上一个没少**, 只是身份改成注记(一分不加一分不减, 与 AI 信号/主线/胜率同栏), 趋势模板与量化波动通道两处都在界面上明写了「不进把握分」, AI 提示词里也从「把握分分解→原始输入」搬进「注记」栏 —— 留在原处等于在骗 AI 说这些数算出了那个分。**台账 `SCORING_VERSION` 4→2**(全仓库唯一一次版本号往回走): 打分代码逐字节还原, 算出来就是同一个数, 该和 09-01~09-08 那七天的记录归在同一把尺子下 —— 换个新号等于白扔掉那七天的真实样本; v3/v4 的记录留着, 读侧按 `_version_of` 分档。**没动的**: 展示筛选(板块过滤 / 历史分位门槛 R220 / 保底 FLOOR_ROWS)—— 那是"算完之后给你看几条", 不是"怎么算分"。9375 格系统性网格实测: 带宽 p10~p90 = **36**, max 100, mean 67.8。 | `git checkout 973b148` 还原 `services/opportunity_score.py` 与 `tests/test_opportunity_score.py`(后者追加 5 条守回退的断言);改 `api/today.py`(打分调用/`axes`→`dims`/删路 C 三处接线/排序键/AI 载荷与提示词)、`services/score_ledger.py`(版本与归因标签);改 `tests/test_score_ledger.py` / `test_today_opportunity_rank.py` / `test_today_r47_verdict_score.py` / `test_today_select_payload.py`;前端改 `today/OpportunityTable.tsx`(两轴条→三维度条、因子表、文案)、`today/MarketStatusCard.tsx`、`lib/api.ts`、`lib/todayHtmlExport.ts`;文档改 `docs/rule-layers.md` / `docs/scoring-and-rules.md`;后端 pytest 3115 全绿, 前端 tsc + eslint + build 全过 | **高(改的是打分口径)** | 可(`git revert`;台账 v2 新记录要与 09-01~09-08 那批区分开) |
| R229 | **评分系统退回 R189 那一套: 红绿节拍整层退役 + 量化通道延申从打分里完整剥离**(用户: 「红绿节拍移除掉」「撤销所有量化通道延申有关的东西」「ui 显示肯定是不变了」「也就是说把评分系统变回前天的时候的模样, 现在的评分系统被改崩了, 很混乱」「反正评分系统最近已经稳定运行一阵子的了, 然后这两天被我大改动」)。**目标状态 = R189(`9193ec4`, 2026-09-08 19:13)刚定下的两轴, 再减去同一天 R189 加进去的红绿节拍。** 这两天进过打分又退出的三项: 磨底节拍(`base` .2125)、三线间距(`spread` .15)、加速度(`accel` .15)。① **红绿节拍整层删**: `services/trend_rhythm.py` 与它的测试文件删除, 硬门槛从四道减到三道(G4 `rhythm_failing` 取消), 质地轴的 `base` 因子、`RHYTHM_SCORE`/`BASING_DAYS_CURVE`/`BASE_LEVEL_W`/`base_score()` 全删, 复盘弹窗的磨底块、今日总览展开行的磨底节拍块、台账两个分组维度(`rhythm`/`basing_days`)与 `TrendRhythm` 前端类型一并删。② **通道延申只剥离"进分的那一半"**: 删 `SPREAD_CURVE`/`ACCEL_CURVE`/`spread_score()`/`accel_score()`, `score_candidate` 去掉 `rhythm`/`runs`/`geo` 三个形参;**阶段/事件/三尺度对齐/27 组合速查/匀速基准/频段能量一个没删**, 界面原样, 只是身份从"进分"降成"注记"(一分不加一分不减, 与 AI 信号/主线/胜率同栏)。AI 提示词里那一组读数因此**从「把握分分解→原始输入」搬进「注记」栏** —— 留在原处等于在骗 AI 说这些数算出了那个分。③ **权重直接写回原值不是反推**: 质地 `.40/.20/.15`、时机 `.31/.32/.26/.11`, R195 那趟 ×0.85 缩放整个脱掉。质地轴的和是 0.75 而不是 1.00 —— `_blend` 按覆盖到的权重之和除, 只有比例进得了分数, 凑回 1.00 反要引入无限小数, 把「这些数就是当初定的」变成近似;新增测试 `test_only_the_ratios_of_the_weight_table_reach_the_score` 把这条证出来。④ 台账 `SCORING_VERSION` 3→4(v3 样本从统计里退场, 记录留着)。**28125 格系统性网格实测**(不随机抽取): 把握分带宽 p10~p90 **25 → 33**(+32%), 满分重新拿得到(max 89 → 100), 均值几乎不动(62.4 → 63.7)。**没动的东西**: 两轴结构(R189)、置信系数(R201)、保底条数、历史分位门槛(R220)、候选路 C —— 路 C 是候选**入口**不是打分, 而且删了会让「值得关注」比现在更少, 那正是用户 R226 抱怨过的。 | 删 `services/trend_rhythm.py` + `tests/test_trend_rhythm.py`;改 `opportunity_score.py` / `api/today.py` / `score_ledger.py` / `livermore_service.py` / `review_service.py` / `trend_template.py` / `keltner_geometry.py`(注释)/ `tests/test_opportunity_score.py`;前端改 `OpportunityTable.tsx` / `StockReviewDialog.tsx` / `GlossaryDialog.tsx` / `lib/api.ts` / `lib/todayHtmlExport.ts` / `pages/Today.tsx`;文档改 `docs/rule-layers.md` / `docs/scoring-and-rules.md`;后端 pytest 3142 全绿, 前端 tsc + eslint + build 全过 | **高(改的是打分口径)** | 可(`git revert`, 但台账 v4 的记录要一并作废) |
| R228 | **两个全屏弹窗并成一个, 决策台那一格只剩一个按钮**(用户: 「这两个弹窗也整合到一起, 外部入口就变成一个按钮了, 这样打开好看」)。原来「走势」那一格里有**两个挨着的按钮通向两个不同的全屏模态** —— 点六态徽标出逐日复盘、点右边的阶段出 27 种组合速查, 而格子里没有任何东西说得出这件事(两段文字长得一样、挨在一起)。两个弹窗讲的其实是同一只票的同一件事, 只是切法不同: 复盘是**纵向**(同一个判定在时间轴上怎么走), 组合速查是**横向**(同一天里 27 格各是什么样)。三处改: **① 组合速查从弹窗降成视图**(`ComboTableDialog.tsx` → `ComboView.tsx`, 去掉遮罩/标题/关闭, 只留内容), 并进复盘弹窗当第三个页签「组合速查」; **② 它的 `geo`/`runs` 改由复盘接口的 `channel` 给**, 不再由决策台透传行数据 —— `review_service._channel` 的末日读数与逐日表末行走同一条路, 三个页签因此看的是同一天的同一份读数, 不会出现"这个弹窗说挤了 12 天那个说 9 天"; **③ 决策台那一格整格变成一个 `button`**(内部徽标降成 span, button 套 button 是非法 HTML), 悬停整格一起亮, 两半各自的悬停也并成一份(六态 → 通道 → 三尺度 → 依据 → 去处)。组合速查页签**不等复盘请求**: 27 格表是另一个 query、缓存一天, 让它陪着转圈是白等。 | 重命名 `decision-board/ComboTableDialog.tsx` → `ComboView.tsx`; 改 `StockReviewDialog.tsx`(`ReviewTab` 加 `combo`)、`decision-board/cells.tsx`、`WatchlistDecisionBoard.tsx`; 前端 tsc + eslint + build 全过 | 低(纯展示) | 可 |
| R227 | **规则层全表 `docs/rule-layers.md`**(用户: 「列出所有规则层我要梳理」)。**穷举 28 层, 逐条从代码里扫出来并跑一遍取真值, 不凭印象写。**分八组: ① 底层指标(作者的, 禁改: 六态/通道位置/通道压力/通道结论 27→10) ② 补充层(fork 只读: 阶段/事件/三尺度对齐/组合注记/匀速基准/频段能量) ③ 质地(红绿节拍/趋势模板) ④ 买入侧(硬门槛→候选三路→两轴打分→展示筛选→空页归因→出手时机→仓位→建仓路径) ⑤ 持仓侧(出场线五阶段/持仓档位) ⑥ 收敛层(该动了 5 档 / 怎么办 6 档) ⑦ 名单与市场(焦点/情绪周期/市场环境) ⑧ 台账。另附**「谁压谁」优先级总表 + 跨层三条硬规矩 + 6 行「已知的层间相冲」**(G2 两日规则只放行 32% 的路 C 票、G2⟹位置≥0.5 恒等式、六态双重投票、`compress_days` 只在蓄势档进分、打分层与判定层可给相反印象、长期档门槛最松 1.22σ)。写的过程里比对代码校出四处口径写错: 层数 24→28、出场线阶段名混用了 `STAGE_CN`/`STAGE_LINE_CN`、`max_show` 默认 10→15、仓位层"已撤掉"其实还在跑。 | 新增 `docs/rule-layers.md`; 无代码改动 | 无(纯文档) | 可 |
| R226 | **修「值得关注还是很少结果」—— 是我 R215 缩窄了候选路 C**(用户: 「有问题, 值得关注还是很少结果, 到底和以前哪里不一样了」)。**查出来是我的锅, 而且我在 R215 的台账里把方向写反了。** R215 之前, `phase()` 里「挤在一起」是整整一档, 全部返回 `coiling`、全部能进路 C; R215 按重合度与快慢把它拆成 横盘中 / 刚启动 / **看不出** 三种, 而 `_COILING_PHASES` 只收了前两种 —— 第三种就此被挡在候选之外。系统性网格(spread × 重合度 × 快慢, 3087 格)量下来: **能进路 C 的从 2940 格掉到 1660 格, 取材面缩小 44%**。我当时写的是「路 C 的出票量因此会上升」(理由是 launching 变得可达), **那是错的** —— launching 本来就在同一个分支里、本来就算 coiling, 让它可达一格没多。**修法**: 把 `unclear` 补进 `_COILING_PHASES`。为什么它该在里面 —— 它是「三条线还挤着, 重合度已经松开, 但没在加速」, 原话「分是分开了, 可是没有力气跟上」, **这正是酝酿**, 甚至比「刚启动」更早一步; 而路 C 的定义就是「谁正在酝酿」(R201), 且路 C 的新鲜度是中性的 55 分, 进来也不会被抬分。补回之后网格数与 R215 之前**精确相等**(2940 格)。**测试按"分支能产出什么"写**, 不按当前三个名字写死 —— 以后那个分支再细分出第四种会红, 而不是又悄悄丢一批候选。**顺带量出一件一直如此、但从没说过的事**: 「三条线挤在一起」⟺ 价格贴着 MA20, 于是这种票每天都在 MA20 上下来回穿 —— 逐相位扫 400 个位置, **过得了 G2「连续两日站上」的只有 32%**。而路 C 存在的理由恰恰是去捞这批酝酿中的票。这不是 bug(G2 的两日规则有它的道理: 免得在 MA20 附近反复把同一只票踢进踢出), 但它与路 C 的目的天然相冲, 值得知道。 | 改 `api/today.py`(`_COILING_PHASES` 补 unclear); `tests/test_today_opportunity_rank.py` +2 例(按分支产出写的守护 + 「看不出」确实是酝酿); 全量 3165 passed | 中(候选路 C 的取材面回到 R215 之前 —— 是**恢复**不是放宽) | 可 |
| R225 | **组合表弹窗从 27 行字墙改成分组卡片**(用户: 「把这部分做好看一点, 好丑。看看怎么显示更有价值而不是一堆数据」)。原来是一张 27 行的表, 每行两三行字, 而「几何含义」开头那半句(「短期在上沿、中期在上沿、长期在上沿」)与左边的「短中长」列**说的是同一件事** —— 27 行里印了 27 遍纯重复, 整体就是一堵字墙。三处改: **①「你现在在这一格」提成顶上的主卡** —— 打开这个弹窗第一件想知道的就是它, 不该是列表里一行高亮; **② 其余按「偏贵 / 中性 / 偏便宜」分三组**而不是按字典序摊平 —— 要的是"我这一格在贵贱谱系的哪一端, 旁边是什么", 字典序回答不了; **③ 重复的那半句退到悬停, 常见度画成三颗点不写字, 七个「几乎不出现」的默认收起来**(点一下展开)。 | 改 `decision-board/ComboTableDialog.tsx`(新增 Dots / Row / ComboGroups); 前端 tsc + eslint + build 全过 | 低(纯展示) | 可 |
| R224 | **打架太多是我造成的 —— 成对报警换成一次收敛 + 立具名场景样本集**(用户: 「组合表和六态商量确认一遍, 看看怎么匹配使用, 怎么那么多打架的?」「不允许随机抽取, 必须是精心挑选」)。**① 先量, 结论是我的锅**: 六态 vs 通道结论 29.8%、六态 vs 阶段 33.2%(R223 我刚加的)、**至少报一个 51.5%** —— 超过一半的行挂着警告。**N 个判定做成对检查就有 N(N−1)/2 个警报**, 而它们并不独立。R205 立的规矩(「一半的票都显示打架, 用户三天后就学会无视它」)被我自己破了。**② 正确的看法: 三者不是三个意见, 是一个滞后阶梯。** 价格 vs 短期中线最快(今天就能翻) → 六态(要走出新的高低点) → 均线中枢最慢(要等均线交叉)。所以**不一致不是矛盾, 是转折正在发生**, 而且不一致的样子还能说出转到第几步了。删掉 `trend_phase_gap`, 换成 `alignment()`: 数三个尺度里有几个朝上(0~3), **报进度不报警** —— 永远给一句话, 从不沉默也从不叫人"当心"。三条线挤在一起时(`nested`)返回 `level=None`「三条线还挤在一起」, 因为那时 spread 的正负是噪声不是方向(具名场景「蓄势突破前」正是被这个报成了「正在转空」)。**③ 立 `tests/fixtures/market_archetypes.py`** —— 用户: 「不允许随机抽取, 必须是精心挑选」。**这条批评是对的, 我已经栽过两次**: 用随机游走量红绿节拍档位分布, 400 条**全部**判成 `failing`(随机游走结构上产不出「蓄势」); 用随机抽的因子量相关性, 而其中六个是我**独立抽的** —— 测的是我的抽样, 当场作废。随机游走还有个更根本的毛病: **它不含"行情"** —— 蓄势/突破/主升/钝化/破位一个都没有, 拿它检验一个信号系统等于在没有信号的数据上做检验。改成 16 条手工构造的具名场景(蓄势突破前 / 刚突破第一天 / 突破站稳 / 主升浪 / 涨势钝化 / 涨过头 / 强势回调 / 高位横盘 / 顶部转折初期 / 破位下跌 / 下跌途中 / 超跌反弹 / 跌过头 / 底部横盘 / 底部转多第一天 / 缓慢爬升), 摆动用三个不可通约的正弦叠加 —— 完全可复现, 但形状不规则到能产生像样的 ATR 与高低点。**换成具名场景之后第一轮就又抓到我自己一个错**: 摆动幅度给了 0.4%, ATR 塌到接近 0, 于是「缓慢爬升」被判成"大顶区域 + 涨过头"、六态 16 条里一个 UT/DT 都没有(摆幅够不到阈值)。真实日线波动约 2%, 改过来才对 —— 这条教训写进 fixture 并配了一条测试(ATR 占价格必须在 0.8%~6% 之间)。 | 新增 `tests/fixtures/market_archetypes.py`; 改 `indicators/keltner_geometry.py`(删 `trend_phase_gap`, 新增 `alignment`)、`api/stock_analysis.py`、`decision-board/cells.tsx`、`lib/api.ts`; 测试删 R223 那节 67 行、新增 6 条场景断言; 全量 3163 passed | 中(走势列第二行的语义变了 —— 从警告变成进度; 判定与打分口径零改动) | 可 |
| R223 | **六态与通道阶段对不上时把话说出来 + 页签名改回「通道结论」 + 评分文档更到当前状态**(用户: 「好多不对的, 可能是因为一些组合没有结论的, 像这些特殊的你要描述清楚情况说清楚, 不然图形对不上结论啊」「名称改回原来的通道结论」「当前评分系统是怎样运行的, 都有哪些规则, 梳理给我」)。**① 用户截图里并排着这样几行**: 「自然回升 10天(多头) + 下跌中」「下跌趋势 6天(空头) + 涨势转弱」。**不是某一边算错了 —— 两边量的根本不是同一个东西**: 六态看的是**价格的高低点**(利弗莫尔关键点), 价格一转向它就转; 阶段看的是**三条均线的中枢**, 而均线是滞后算子。所以在**转折那一段**它们必然对不上, 而那恰恰是最值得知道的一段(价格已经转了、均线还没跟上, 或者反过来)。R215 修过其中一个特例(价格跌穿三条线还叫「涨势转弱」), 但一般情形直到这轮才处理。**新增纯函数 `trend_phase_gap()`**: 六态在多头侧而阶段落在跌势侧(下跌中 / 跌过头)、或六态在空头侧而阶段落在涨势侧(上升中 / 刚启动 / 涨势转弱 / 涨过头)时, 返回一句「六态与通道不一致」外加**为什么会这样**的解释; 走势列第二行就地把「快慢」换成这句话。**只认真正相反的** —— 横盘中 / 看不出这类不表态的一律不算, 否则一半的行都会挂标记, 三天后就没人看了(R205 立的规矩)。**不替用户选边**: 六态是作者的判定(禁止动), 阶段是从均线几何推出来的(它没算错), 两个都对、只是看的东西不同 —— 与 `stock_playbook._conflicts` 同一条纪律。**② 复盘弹窗页签名改回「通道结论」**: R200 那轮清行话时换成了「这个价贵不贵」—— 那是在解释它**说什么**, 可页签要的是**这一栏叫什么**, 换掉之后反而与这一层在别处的名字(`keltner.verdict` / 感叹号说明 / 复盘统计口径)对不上。**③ `docs/scoring-and-rules.md` 更到当前状态**: R216 写的那份已经有多处过期 —— 因子勾选面板(R218 已删)、门槛单位(R220 换成历史分位)、C 级三项(R217 全部处理完)、路 C 判定重复(R217 已收成一处)。C 级那一节改成"曾有已知缺陷、已修"并保留当时错在哪(那三种错法还会再犯), 另加 D 级记 R221 挖出的三档门槛松紧不一。 | 改 `indicators/keltner_geometry.py`(新增 `trend_phase_gap` 与 `_PHASE_SIDE`)、`api/stock_analysis.py`(phase 带 gap)、`decision-board/cells.tsx`(走势列第二行)、`lib/api.ts`、`StockReviewDialog.tsx`(页签名)、`docs/scoring-and-rules.md`; 测试 +25 例(含穷举一致/不一致/不表态三类); 全量 3183 passed | 低(新增一句提示 + 改名 + 文档 —— 判定与打分口径零改动) | 可 |
| R222 | **逐行核对 27 条结论**(用户: 「组合表的结论都正确的吗」)。R221 验的是**原则**(站上上轨=延续 / 长周期为准 / SMA 是前提)和**频率**, **没有逐行核对结论本身** —— 这两件不是一回事, 这一轮补上, 并且做成**机器可查的性质**而不是人工过一遍(底层哪天改了措辞要能红)。**漏洞分两类, 判据不同。** **甲类: 正文把这一格的事实说反了。** 把底层正文里可以被组合直接证伪的断言抽出来(「只有短期到上沿」「大级别还早」「只有短期到下沿」「长期还在下沿」「三档同时到上沿」等 10 条), 拿 27 格逐一去对 —— 命中 6 处(上中上 ×2、上下上 ×2、下上下、下中下), **全部已有补充注记兜着**, R195/R203 那层在起作用 ✓。**乙类: 正文每句都对, 但漏掉了长期档 —— 这一类原来没人查过。** 「中下中」与「中下下」在底层拿到**逐字相同的正文**, 而那段话只讲短期与中期; 差别恰恰在长期: 前者长期在通道中部(「大级别跌到位、等入场点」说得通), 后者**长期也在下沿**(半年尺度本身还在低位, 更接近下跌途中)。而「中下下」是**第四常见**的格子(约 12%), 不是边角情形。同型的还有「中上上」(7.4%, 与「中上中」共用正文, 而长期也在上沿, 正文末尾「等回落到下沿再谈」在大级别也走高时要打折扣)。两格都补了注记并移出 `COMBO_CLEAN`。**立了一条新规矩**: 长期档只要在轨上, 这一行就**必须有人提到它** —— 底层正文提了算, 补充注记的标题或正文提了也算, 但不能两边都不提。这条测试写完立刻又抓出「上下上」「下上下」两格 —— 查证后是**我的测试查窄了**(只查了注记正文, 而它们写在注记标题里, 标题在弹窗上也是显示的), 改测试不改数据。新增 4 条测试(甲类必须有注记兜 / 长期档在轨必须有人提 / 注记与干净两份名单恰好分完 27 格 / 注记只补话不改底层结论且底层判定是现调的)。 | 改 `indicators/keltner_geometry.py`(COMBO_NOTES +2, COMBO_CLEAN −2); `tests/test_keltner_geometry.py` +4 例; 全量 3158 passed; `keltner.py` 零字节改动 | 低(纯注记 —— 底层判定与打分口径零改动) | 可 |
| R221 | **联网交叉验证 27 组合表**(用户: 「去网上找更多关于这个通道的信息验证交叉验证这组合表的组合是否真的对, 修复所有错误」)。查了 Keltner 通道的公开资料(StockCharts、Raschke 版定义、多周期分析的通行做法), 再用蒙特卡洛量了组合表的真实频率。**三条对上了、一条挖出新问题:** ✓ **① 「收盘站上上轨」在上升趋势里是强势延续而不是超买卖点** —— 资料原话是 fading a band touch 只在横盘里才成立, 趋势里"walking the band"是常态。与本系统 `high_short_only`「拿着, 别在这加仓」和 R212「上沿+多头不是卖」**完全一致**。✓ **② 多周期冲突时长周期为准** —— 与 `verdict()` 把「短期与长期反向」放在最前面判(超跌反弹 / 强势深调)一致。✓ **③ 均线用 SMA 而不是标准的 EMA** —— 这**不是疏漏而是几何层的前提**: 「MA_n ≈ (n−1)/2 天前的价格」这条恒等式、以及由它推出的「匀速时 a1 精确为 0」, 只在简单均线下成立; 换 EMA 整个二阶推导就只剩近似。补了一条测试钉死, 免得日后有人"顺手改成 EMA"。⚠ **④ 新发现: 三档的门槛松紧根本不一样, 而这件事原来哪儿都没说过。** 价格绕 MA_n 的离散度按 √n 增长(√20:√60:√120 = 1 : 1.73 : 2.45), 而 ATR 倍数只从 2 涨到 3(1 : 1.25 : 1.5)—— **倍数追不上离散度**, 于是按各自波动折算, 三档门槛其实是 **2.00σ / 1.44σ / 1.22σ, 越长越松**。后果: 「长期到上沿」是三档里最容易发生的那一个, 它那一票最不值钱; **「三档同时到上沿」因此不是三重确认**。蒙特卡洛证实(40 组 × 900 天纯随机游走, 零漂移): 上上上 12%、下下下 17%、中中中 16%、中下下 12% —— **四格占了一多半**, 而「上上上 = 大顶区域」这种听着像罕见极端信号的格子, 在毫无趋势的噪声上就有一成多。另有七格几乎不出现(下上下 0%、上下上 0.01%……), 对着它们研究"该怎么办"是白费功夫。对 ATR 估算做了 1.0/1.4/2.0/2.8 四档敏感度检验: 绝对频率随缩放而变, 但**松紧次序与"同向组合并不罕见"两条结论不变**。**修法守底层禁令**: `keltner.py` 一个字节没动(它的倍数是根本, 而且改了整套历史读数与台账就断了), 全部落在补充层 —— 新增 `COMBO_RARITY`(27 格各标一个常见度档位, 只给档位不给假精度的百分比)、把「上上上」「下下下」从 `COMBO_CLEAN` 移进 `COMBO_NOTES` 各补一句「这不是三重确认」、组合表弹窗加「常见度」一列并在表尾写明闭式推导。补 4 条测试(门槛折算次序 / 每格都标了常见度 / 同向两格必须带那句话 / 均线必须是 SMA)。 | 改 `indicators/keltner_geometry.py`(COMBO_RARITY、COMBO_NOTES ±2、combo_table 加 rarity)、`ComboTableDialog.tsx`(常见度列 + 表尾说明)、`lib/api.ts`; `tests/test_keltner_geometry.py` +4 例; 全量 3154 passed | 低(纯注记与展示 —— 底层判定与打分口径零改动) | 可 |
| R220 | **入选门槛从「绝对把握分」换成「历史分位」**(用户: 「要啊, 必须处理啊」)。旧旋钮几乎没有作用: 把握分是十个因子加权平均再取几何平均, "平均"本身就把取值挤向中间 —— 实测同一天横截面 p10~p90 只有 **26 分**(两根轴分别也只有 34 / 39 分, 换成「质地 ≥ N」同样救不了), 于是 `min_score = 60` 只挡掉约 **1.6%** 的候选, 拖到哪儿都差不多。**为什么不是「今天前 N%」**: 那只是 `max_show` 的重复(两个旋钮都在截条数), 而且它永远有票 ——「今天没有够格的」这个信号就没了。**历史分位同时解决两件事**: 分位天然均匀 → 每一格都有抓手, 拖到 30 就真的挡掉七成; 跨日可比 → 「今天最好的一只也只排到历史第 20 百分位」这句话才说得出来。实现: `score_ledger.score_distribution()` / `percentile_of()` 只取同一个 `SCORING_VERSION` 的定稿日, 样本 < 400 条返回 None; `today` 每行带 `hist_pct`; `filter_opportunities` 按 `min_hist_pct` 判。**台账没攒够时门槛整个失效并明说** —— 送 `hist_pct_ready` 给界面, 滑块置灰 + 一句「台账还没攒够, 暂不起作用」。静默失效正是这一轮反复栽的那类跟头。 | 改 `services/score_ledger.py`(+2 函数)、`api/today.py`、`services/today_prefs.py`、`pages/Today.tsx`、`lib/api.ts`、`lib/todayHtmlExport.ts`; 三条新断言 + 六个旧测试文件按新机制改写; 全量 3150 passed | 中(筛选行为变了 —— 但默认 0 = 不过滤, 且台账攒够前它本来就不起作用) | 可 |
| R219 | **悬浮与弹窗按话题分块 —— 同一件事不再拆到上下两处**(用户: 「各自整理自己的列好看一点, 每一列的内容应该就是一部分, 而不是内容上面一部分下面一部分」「我说的是点击打开的内容和悬浮内容这些」)。**先改错了地方**: 我先动的是表格列(那个也确实修掉了行与行糊在一起的问题, 留着), 用户随即指明说的是**弹窗与悬浮**。**悬浮(`geoLines`)**: 原来是流水账 —— 快慢在最上面, 「三条线还有 X% 重合」在第三行, 而同属重合这个话题的「已经这样 N 天」「这季平均 Y%」掉到第五六行, 中间隔着「价格离各自中线」; 读的人得自己把同一件事从两处捡回来拼上。改成每行以 `[话题]` 开头, 顺序按"先看什么"排: 位置 → 间距 → 重合 → 快慢 → 起伏 → 在轨外。顺带**更正一处 R217 遗留的过期说法**: 能量尾注原来写「各 33% 是一路匀速走的样子」, 那是老基准的话, 现在基准是零漂移随机游走, 改成「和纯噪声比, 各 33% 是不偏不倚」。**点击弹窗(`ComboTableDialog` 读数带)**: 原来七个等宽格子平铺 + 底下一段独立的匀速基准, 于是**「快慢」这一个话题被劈成两半** —— 数字在格子里, 而它唯一能被核对的那句解释在下面段落里, 中间还隔着「横了 12 天」。改成四块(位置 / 间距 / 快慢 / 重合), 每块自带标题, 匀速基准那句收进「快慢」块内。 | 改 `decision-board/cells.tsx`(geoLines 分话题)、`decision-board/ComboTableDialog.tsx`(读数带四块); 前端 tsc + eslint + build 全过, 措辞泄漏扫描 34 passed | 低(纯展示) | 可 |
| R218 | **删掉「参与打分的因子」勾选面板(R204)**(用户: 「不搞自选了」)。R204 加它是为了对付「因子越多挤得越狠」—— 少平均几个, 带宽就回来了。**那个道理本身没错**(本轮实测: 10 因子 p10~p90 = 23 分, 关掉 spread/accel 还原成 8 因子 = 26 分, 两套排序 Spearman 0.911), 但用户不要这个旋钮。**带宽的出路仍然在名次与分位** —— R201 早就改成那样了, 绝对分本来就不适合当筛选旋钮。**删就删干净**: 前端 `FactorPicker` 组件与调用点、`TodayPrefs.factors` / `factor_catalog` 类型、后端 `today.factor_catalog()`、`score_opportunities(factors=)`、`PrefsModel.factors`、`today_prefs` 的 `factors` 字段与 `_factors()` 归一化、`opportunity_score` 的 `enabled_weights()` / `MIN_ENABLED` / `score_candidate(enabled=)`, 以及 `test_opportunity_score` 里那一整节 5 条断言, 一处不留。**留一半是最糟的选择** —— 这一轮已经在四处「没人调的代码看起来像在用」上栽过跟头(`_conflicts` 规则②、`PH_LAUNCHING`、`lib/button.ts`、`_VERDICT_SCORE`), 不再添第五处。共 −259 行。想要回来: `git revert` 这次提交, 每一处都是整块加的。 | 改 `pages/Today.tsx`、`lib/api.ts`、`api/today.py`、`services/today_prefs.py`、`services/opportunity_score.py`、`tests/test_opportunity_score.py`; 全量 3148 passed | 低(打分口径零改动 —— 删掉的是"让用户少算几个因子"的能力, 默认行为本来就是全开) | 可 |
| R217 | **修 baseline 符号误判 + band_energy 换参照基准 + 删死代码 + 决策台两列定高**(用户: 「有问题就解决问题, 找最优解」「升级平替系统就好了」「不能这样挤那么狠, 一个结果都没有了」「各自整理自己的列好看一点, 每一列的内容应该就是一部分」)。**① `baseline()` 符号翻转是真错**: 短期与中期偏离方向相反时 `ratio` 为负, 而 `ratio < 1−容差` 这一支把它判成「冲在前面 —— 最近这一段明显比之前快」。实测 800 次随机游走, 判「冲在前面」的 536 例里 **123 例(23%)是这种翻转**; 而且它**错得看得见** —— 弹窗上并排显示「中期该到 −4.0, 实际 1.5」, 数字自己在打脸结论。修法**不返回 None**(那样这一格就空了, 信息反而更少), 给它自己的一档「还没走到一边」: 两个尺度分别在价格两侧本身就是个有意义的状态, 常见于回调刚开始或反弹刚起来。**② `band_energy` 的参照基准整个换掉 —— 这是这一轮最实质的升级。** 老基准拿「匀速直线」当分母, 可真实价格离匀速直线远得很, 它更接近随机游走; 拿匀速当分母, 中长频天然被高估, 于是**实测 300 条纯随机游走(既没消息也没趋势)有 66% 被判成「短波动主导」**。那不是这些票的特征, 是分母选错了。新基准问的是有意义的那个问题:「和纯噪声比, 哪一段更突出」。基准常数 `(9.5, 13.3, 12.4)` 由 **2400 条零漂移随机游走**(σ 五档 × 12 个种子)定标, 分四组交叉验证稳定。校准检验: **无趋势时三份 0.32/0.33/0.34(各 1/3, 基准对了), 强趋势低频升到 0.42、下跌到 0.51(它在做功)**。措辞同时收掉「消息驱动」这类因果话 —— 涨跌的原因不是三条均线回答得了的。**③ 「主导段」不设门槛。** 我一度想用 0.45 的门槛把弱信号捂住, 用户当场否掉:「不能这样挤那么狠, 一个结果都没有了, 没意义的」。**说得对, 而且这正是这个系统一路的规矩** —— R201 保底、R210 `empty_reason` 都是「不吭声永远不如说清楚有多少把握」。改成: 三份占比与最高段**永远照给**, 另出一个 `lead`(明显 / 略偏 / 三段差不多)只管措辞强弱。**④ 删 `_VERDICT_SCORE`**(today.py, 10 条通道结论配 +10~−15 分值, **全项目零引用**, v1 遗留)—— 危害不是占地方, 是读代码的人会以为通道结论在参与打分。**⑤ 路 C 判定去重**: `coiling_candidates()` 与 `score_opportunities()` 各判一次同样条件, 收成 `coiling_phase()` 一处定义 —— 那正是 R201 死代码的土壤。**⑥ 决策台「走势」「结论」两列固定两行**: 原来结论列最多摞五层(结论徽标/事件行/怎么办徽标/两行折行理由/另有 N 处)且行行不等高, 于是上一行的尾巴挂到下一行表头底下, 行与行糊成一片。现在行 1 两个徽标横排、行 2 一行截断说明(事件与分歧并进去), 全文在悬停。**测试也在打掩护**: `_geo()` helper 默认 `nested=False`, 造得出 `spread=0.7 且 nested=False` 这种真 `geometry()` 永不返回的形状 —— 与 R214 的 `verdict={"side":"sell"}` 同一个毛病, 已改成按 spread 推。**打分口径零改动**: 门槛/两轴/几何平均/倒 U 曲线/置信系数一条没动。 | 改 `indicators/keltner_geometry.py`(baseline 新增 crossing 档、ENERGY_REF 换基准 + ENERGY_REF_STEADY 留对照、lead/lead_cn、explain 措辞)、`api/today.py`(删 `_VERDICT_SCORE`、新增 `coiling_phase()`)、`decision-board/cells.tsx`(两列定高)、`lib/api.ts`、`today/OpportunityTable.tsx`; `tests/test_keltner_geometry.py` 四条能量测试按新语义重写 + helper 修正; 全量 3153 passed | 中(band_energy 显示的数会变 —— 那是修正不是调口径; 版面改动) | 可 |
| R216 | **评分系统与规则层完整梳理**(用户: 「请梳理我当前评分系统的完整运转机制与规则层……特别是量化通道的延申, 这些东西宁可只保留 100% 正确的」)。通读 `opportunity_score` / `today` / `keltner_geometry` / `watchlist_urgency` / `stock_playbook` 全链, 产出 `docs/scoring-and-rules.md`: 计算流程、数据来源与权重全表、规则层逐条(门槛/候选/展示筛选/该动了/怎么办/持仓)、规则之间的依赖与覆盖、以及**量化通道延申量按"有多确定"分成 A/B/C 三级**。**核心结论都是量出来的**: 通道延申真正进分的只有 `spread`(质地 0.15)、`accel`(时机 0.15)、`compress_days`(进 base 时长那一半)三个; 而 `spread_score` 与 `accel_score` 的 p10~p90 分别是 **87 / 79 分**, 是全系统区分度最好的两个因子(合成把握分只有 17 分)—— **延申不但没稀释评分系统, 它是撑起区分度的那一部分**。被标 C 级的三个(`baseline` / `band_energy` / `_VERDICT_SCORE`)**全部不进分**, 有硬证据: `opportunity_score.py` 全文没有 `baseline` / `energy` 这两个词, 把 baseline 置空或塞垃圾把握分一分不变。同时列出薄弱点与优先级(P0 两条已在 R217 修掉)。**最该动而还没动的是 P2-7**: 所有曲线都是先验, `score_ledger` 天天攒数据却没有回流去验证 —— 这是整个系统最大的开环。 | 新增 `docs/scoring-and-rules.md`; 本轮零代码改动 | 无(纯文档) | 不适用 |
| R215 | **阶段表全组合核对: 「下跌中贴下轨」不该叫「涨势转弱」+ 撤掉仓位比例输入**(用户: 「不需要仓位比例」「分析组合表的每一个组合是否合理, 我遇到了不少错误, 比如下跌中贴下轨的怎么会是涨势转弱, 正常吗」)。**不正常。** 截图那一格是「下跌趋势 7 天 / 涨势转弱·走到中段 / 正在放慢」。按 R214 那套路子把 `phase()` 的整个输入空间穷举一遍(7 × 7 × 7 个均线组合 × 5 个收盘位置), 跑出**三类毛病、731 例**, 根只有一个: **这个函数只读了短线中枢与长线中枢的间距和快慢, 措辞却在替它压根没看过的两件事打包票** —— 三条线是不是真排成了一列, 以及**价格现在在哪儿**。**① 用户撞见的那一格。** 「涨势转弱」说的是"有一段涨势, 正在转弱"; 而那只票价格早已跌到三条线之下、六态也空了 7 天 —— **转弱这件事已经完成了**, 现在是"涨完了在跌"。只看中枢看不出来, 因为**中枢是滞后的**: 均线还没交叉, 价格已经走完了。现在价格跌穿三条线时换一套说法「涨势已走完」, 并直说「均线还没掉头, 价格先走完了」(沿用 `PHASE_OVEREXTENDED_CN` 那套同码两说法的做法, 不新增阶段码, 下游 `stock_playbook` 对 `stalling` 的处置照旧正确)。**② 「三条线稳稳朝上/朝下散开」是句假话** —— 间距只比了短和长两条, **中线在哪儿它没看过**。中线没排到中间时这句就不成立, 穷举里 644 例。现在排列不干净就不说「散开」, 改说「中线还没排到中间 —— 三条线还没理顺」; 顺带补上「下跌中但价格已翻回三条线之上」= 跌势里的反弹(与作者 verdict 的「超跌反弹」同一件事, 用同一套词)。**③ 又一个死档: 「刚启动」和「看不出」两个阶段一次都出不来。**〔R226 更正: 这一条的**方向我写反了** —— 下文说「路 C 的出票量因此会上升」是错的。`launching` 本来就在同一个分支里、本来就算 `coiling`, 让它可达一格没多; 真正发生的是 `unclear` 被切出去、而 `_COILING_PHASES` 没收它, 路 C 的取材面**缩小了 44%**。见 R226。〕 `NESTED_ATR` 恰好等于 `SPREAD_LAUNCH`(都是 1.0), 于是「挤在一起」那一档把「刚走出来」该占的区间**整个吞掉**。而 `today.py` 的 `_COILING_PHASES` 里正列着 `launching` —— 等于 R210 刚修好的候选路 C **又瘸了另一半**。修法**不动任何阈值**(`SPREAD_LAUNCH` 与打分层共用, 一动就是改口径), 改成在「挤在一起」这一档**里面**按现成的重合度门槛与快慢档再分一层: 还高度重合 = 横盘中 / 重合已松开且在加速 = 刚启动 / 松开却没劲 = 看不出。修完复跑: **七个阶段全部到得了, 不合理组合 0 例。** **这次也是测试自己在打掩护**: `test_keltner_geometry` 的 `_geo()` helper 默认 `nested=False`, 于是造得出 `spread=0.7 且 nested=False` 这种**真的 `geometry()` 永远不会返回**的形状(|间距| ≤ 1 必然 nested)——「刚启动」就是靠这个假形状显得有人走。与 R214 里 `verdict={"side":"sell"}` 那条**一模一样的毛病**: 假数据自己对得上, 死路看着像活路。helper 改成按 spread 推 nested, 调用方填不了假的。新增 5 条性质测试(每个阶段都到得了 / 排列不干净就不许说散开 / 价格位置与阶段名不许打架 / 用户那一格 / 刚启动要的是松开加提速)。**④ 决策台「仓位」格撤掉仓% 输入框**(用户: 「不需要仓位比例」)。R212 刚把它从「成本」格挪过来, 这轮索性不显示 —— 这一格只回答"这只票拿没拿"。**存着的值没删**(`setPos` 一直原样带着 `weight` 回写), 今日总览的组合总仓位、净值回撤纪律照旧按它算, 只是不再有新填的入口; 总览那一列的提示词跟着改, 不再让人去一个已经没有的地方填。 | 改 `indicators/keltner_geometry.py`(`phase()` 引入 `d`/`stack`, 新增 `PHASE_STALLING_DONE_CN`, 「挤在一起」档内再分层)、`WatchlistDecisionBoard.tsx`(撤仓% 输入)、`pages/Today.tsx`(提示词); `tests/test_keltner_geometry.py` helper 修正 + 5 例性质测试; 全量 3153 passed | 中(阶段措辞与分档变了 —— 修的是说错话, 不是改口径; 一个阈值都没动, 打分零影响) | 可 |
| R214 | **穷举组合表把剩下的矛盾全找出来 + 修 parquet 并发读写(整套测试 1/3 概率随机挂的那个) + 今日总览列结构重建 + 删孤儿文件**(用户: 「我觉得你没处理好组合表的所有情况」「全都要解决」)。**① 穷举, 不再等着被撞见。** R212 只修了用户当场撞见的那一格。这轮写了个枚举: 5 短 × 5 中 × 5 长 = 125 种通道组合 × 7 种趋势 × 持有/空仓 × 4 种 AI 信号 = 7000 例, 逐例问「结论」这一格的上下两行方不方向相反。**跑出 7896 处、7 种形态**, 归到两个根因: **(a) `stock_playbook._conflicts` 的规则②「趋势 vs 通道位置」从写下那天起一次都没触发过** —— 它读 `verdict["side"]`, 而那个字段的取值域是 `("high","low")`(贴的是上轨还是下轨), 代码却拿 `"sell"`/`"buy"` 去比; 偏买偏卖在 verdict 里叫 `tone`。**条件恒假, 全枚举命中 0 次。** 于是「趋势往上但位置已经该止盈」「趋势往下但位置看着便宜」这两类最典型的打架, 系统一次都没报过。**病因与 R210 的路 C 一模一样: 判定写对了、接线接错了, 而测试恰好只测了判定** —— 更巧的是, `test_趋势往上而位置已经偏卖算打架` 这条测试**自己就是藏身处**: 它传的 `verdict={"side": "sell"}` 是 `keltner.verdict()` **永远不会返回**的形状, 假数据对得上, 所以天天绿。 **(b) `_band_call` 的「到下沿 + 多头 = 买」漏看了另外两档**: 三档全在下沿时作者的 verdict 说的是「下跌途中 —— 别抄, 下轨会一路下移」, 这里还在说「相对便宜」。R212 漏看的是趋势, 这次漏看的是通道结构 —— 「趋势没坏而掉到下沿 = 甩人下车」成立的前提是大级别还在上面。**修法不另立判据**: 直接问一句作者 verdict 的语气, 想给的方向与它相反就降级中性(少动一次, AGENTS.md 第 10 条)。`avoid` 与 `sell` 同属偏卖但**话不一样**(「下跌途中」不是"贵"), 分开写, 否则会说出「位置上已经是『下跌途中』—— 贵了」这种不通的句子(这一条是新写的测试当场抓出来的)。修完复跑: **0 例**。新增 `test_playbook_combo_matrix.py`, 守的是两条**性质**而不是若干个用例: 性质 A「没有未标记的方向矛盾」, 性质 B「**每条打架规则都必须至少触发一次**」—— 一条永远不触发的规则和没写是一回事, 而且看起来还像写了。**② 修 parquet 并发读写。** 之前只报告没修, 这轮挖到底, 是**两个独立缺陷**: **(a) 写 vs 写** —— `_atomic_write_parquet` 的临时文件名是**固定**的 `part.parquet.tmp`, 两个并发写入者写进同一个临时文件, 大的写到一半被小的覆盖并抢先 replace, 发布出去就是半截文件; 而且 `_write_lock` 是**实例级**的, `save_index_instruments`/`save_etf_instruments` 两个调用点**根本没持锁**, 靠锁堵不住。改成带 uuid 的唯一临时名(`EnrichedPublication.write_parquet` 早就这么做, 这里补齐)+ finally 清理。**(b) 读 vs 写** —— `replace_with_retry` 的注释写着「Linux 的 inode 交换语义则无此限制」, 那句话对 `os.replace` 成立, **对 `pl.read_parquet(路径)` 不成立**: polars 的流式读会先取 footer、再按字节区间**回头按路径重新取数**, 替换插在中间就是新字节配旧偏移。Rust 侧的报错直接印出了证据(`FetchedBytes::get_range: range end index 260625 out of range for slice of length 833`), 最坏情况是 `PanicException` —— `BaseException` 的子类, `except Exception` **兜不住**。改成先 `open` 再把**文件对象**交给 polars, 句柄钉住 inode。实测: 一读一写并发 4 轮, 按路径读 24 次异常、钉住句柄 **0 次**。全量测试连跑 3 遍 3148 全绿(改之前约 1/3 概率随机挂)。反例用例留在仓库里但默认不跑(`TICKFLOW_RACE_REPRO=1` 才开)—— 断言「必须抢到竞态」在失败方向上不确定, 拿一种随机失败换另一种不划算。**③ 今日总览「值得关注」列结构重建**: 10 列 → 6 列(开实时 7 列)。两件事都是把决策台 R211/R212 的结论搬过来 —— 同一套东西两个页面不该有两种说法: **结论提到依据前面**(原顺序是 信号→位置→量比→距关键点→出手, 也就是先读四格证据再读结论); **三列数字并成一格「走势」状态词**(68% / 1.82 / -2.55% 每一个都得先知道"多少算多"), 数字全退到悬停。阈值函数 `posWord`/`volWord` **export 给导出件共用** —— 分界只该有一处定义, 抄两份就会有屏幕说「放量刚好」而存档说「量太大」的那天。撤掉的只有「注记·不计分」那一列(它自己写着不计分, 展开行里有全文, 导出件挪到每行下面的小字)。**④ 删 `frontend/src/lib/button.ts`**(R128 配方, 全项目 0 处引用), 顺带删掉因撤列而无人调用的 `NoteChips` —— 这一轮在「没人用的代码看起来像在用」上栽了两次, 不留第三处。 | 改 `services/stock_playbook.py`(规则② 读 tone; avoid 单独措辞)、`services/watchlist_urgency.py`(`_band_call` 收 bands + `_verdict_tone`)、`tickflow/repository.py`(唯一临时名 + `_read_parquet_pinned` + `_partition_rows`)、`today/OpportunityTable.tsx`(列重建)、`lib/todayHtmlExport.ts`(跟随); 删 `lib/button.ts`; 新增 `tests/test_playbook_combo_matrix.py`(9 例, 含 7000 例穷举)与 `tests/test_partition_concurrent_replace.py`(9 例, 2 例默认跳过); 全量 3148 passed | 中(判定层修的是真 bug 不是调口径; 存储层是并发正确性; 版面大改) | 可 |
| R213 | **镜像构建按约定全量落地(P0/P1/P2)**(用户: 「以后这个项目构建都要按照这个要求」「全都要解决」)。R212 那轮只把约定写进了 `docs/docker-build-convention.md`, **三个文件一个都没动** —— 这轮补上。**P0 ①矩阵原生构建**: 原来单台 `ubuntu-latest` 用 QEMU 模拟 arm64, 前端 `tsc`+`vite` 这类 CPU 密集步骤慢 5~20 倍。改成 amd64 走 `ubuntu-latest`、arm64 走 `ubuntu-24.04-arm`, **两架构并行**, 各推一份 by-digest 镜像(不打 tag, 否则两边会互相覆盖 `:latest`), 最后 `merge` job 用 `docker buildx imagetools create` 合成多架构 manifest。`setup-qemu-action` 随之删掉。**P0 ②缓存加固定 scope**: `type=gha` 默认按分支隔离, `main` 与 `claude/upstream-clean` 各存一份互不相通; 现在按架构固定成 `tickflow-amd64` / `tickflow-arm64`, 跨分支复用同一份, 并加 `ignore-error=true` —— 缓存服务抽风不该连累构建本身。**P1 ③基础镜像 pin 到具体 minor**: `node:20-alpine`→`20.20.2-alpine`、`node:20-bookworm-slim`→`20.20.2-bookworm-slim`、`python:3.11-slim`→`3.11.16-slim`。floating tag 上游一动整层缓存就失效。三个版本号都是**查了 Docker Hub 实际存在且有 arm64/v8 变体**才写的(约定里举例的 `python:3.11.11-slim` 已经过期, 没照抄)。**P1 ④依赖装成严格 frozen**: 去掉 `pnpm install --frozen-lockfile || pnpm install`、`npm ci || npm install`、`uv sync --frozen || uv sync` 这三处容错 —— 它们会**掩盖 lockfile 漂移**, 静默构建出与本地不一样的东西, 而且退化路径是一次完整的慢速重解析。改之前先验过两个锁都是同步的(`uv lock --check` 与 `pnpm install --frozen-lockfile` 均通过)。npm 三处加 `--no-audit --no-fund`; pnpm 版本从 `pnpm@9` 锁到 `9.10.0`(与 `frontend/package.json` 的 `packageManager` 对齐)。**P1 ⑤新增 `STRIP_NUMBA`(默认 1)**: numba+llvmlite 约 150MB, 只有回测 Matrix 引擎用, 且 `app/backtest/matrix.py:39-51` 对缺失有纯 Python 降级(`njit` no-op + `prange=range`), 功能不变只是回测变慢; 需要时传 `--build-arg STRIP_NUMBA=0`。**这里有两个坑, 都填了**: (a) 卸载必须与 `uv sync` **同一个 RUN** —— Docker 分层是叠加的, 换下一层再删那 150MB 还留在上一层, 镜像一点没小; (b) `CMD` 原本是 `uv run uvicorn`, 而 **`uv run` 默认会先把环境同步回 `uv.lock`** —— 那会在容器启动的一瞬间把刚裁掉的 numba 装回来, 既白裁了, 又会在离线/国内网络下卡死启动(文件里原有的注释正是在说这个风险)。所以 `CMD` 加 `--no-sync`, 直接用构建好的 `.venv`, 跑的仍是同一个 uvicorn、同一个 app。**P2 ⑥`.dockerignore` 补 7 项**: `gui-test-screenshots/`、`assets/`、`.claude/`、`community-qr-code.jpg`、`操作说明书.md`、`CONTEXT.md`、`CONTRIBUTING.md`(都不在任何 `COPY` 的路径上, 确认过)。**P2 ⑦加 `HEALTHCHECK`**: 用 `.venv` 自带的 python+urllib 探 `http://127.0.0.1:3018/health`(该路径在 `main.py` 的 `_AUTH_WHITELIST_EXACT` 里, 免认证), 30s/5s/30s/3 次, 不额外装 curl。**硬约束逐条守住**: `USE_CN_MIRROR` / `INCLUDE_STOCKSDK` / `BACKEND_EXTRAS` / `CODEX_CLI_VERSION` 与三重兜底的国内镜像源逻辑一个没删; `docker-compose.yml` 没碰; CI 里仍传 `USE_CN_MIRROR=0`; GHCR 仍用 `secrets.GHCR_PAT`。**一个已知前提**: `ubuntu-24.04-arm` 这类托管 arm64 runner 对**私有仓库**要 Team/Enterprise 方案, 用不了会在 job 启动时报 runner 不可用 —— workflow 里就地写了注释说明怎么退回 QEMU。 | 改 `Dockerfile`(pin/frozen/STRIP_NUMBA/HEALTHCHECK/CMD --no-sync)、`.github/workflows/docker.yml`(整份重写成 build 矩阵 + merge manifest)、`.dockerignore`; 运行时功能零改动 | 中(构建链路整体重做; 镜像内容除裁掉 numba 外一致) | 可 |
| R212 | **修「到上沿又叫卖」这个真矛盾 + 依据区改成「数据 + 一句解释」+ 决策台收到 9 列 + 立下交易哲学**(用户: 「有矛盾, 马上逼近上沿了又叫买, 到上沿了又叫卖」「仍旧看不懂获取不到结论性信息」「你干脆保持数据, 然后在后面加一行解释」「怎么办和贵不贵合成为一列叫结论, 贵不贵在上」「结论这行放在 ai 分析前一列」「现价涨跌合成一列」「止盈线这一列不要了」「成本这一列去掉仓位比例」「尽可能减少买卖次数, 大道至简, 趋势为王, 核心是利弗莫尔的心法」)。**① 修了一个真矛盾**: 「到轨」那一档原来是无脑的 `上沿→卖 / 下沿→买`, **一眼都不看趋势**。于是同一只正在上涨的票, 昨天离转强价 1.5% 报「逼近·买」, 今天站上去顺带碰到上沿, 立刻翻成「到上沿·卖」—— 前后两天相反的动作, 而这两件事说的其实是**同一次突破**。错因: 转强价(六态关键点)与短期通道上沿常常挨得很近, 站上去在六态那套里是「转强」、在通道那套里是「到顶」, **谁对取决于趋势在哪一侧**, 不取决于哪一档判定先命中。`keltner_geometry.event` 早就写对了(破上轨在 UT 里是趋势内加速、在空头侧才是反弹遇阻), 只是「该动了」这一层当时没跟上。现在四种情形分开: 上沿+多头=不是卖(沿上沿走是常态, 要减看止盈线) / 上沿+空头=卖 / 下沿+多头=买但提醒别当破位 / 下沿+空头=不是买(下沿会跟着一路下移) / 趋势读不到=中性不替用户猜。**② 复盘依据区第三版: 数据 + 一句解释。** 这一块改过三版, 每版错在同一地方的不同侧面 —— v1 只给数字(「用数字看不懂」: 得先知道多少算大) → v2 只给状态词(「仍旧看不懂」: 「走到中段」**然后呢**) → v3 用户给出答案「保持数据, 后面加一行解释」。三样缺一不可: 名称(在说什么) + 数值(能核对) + 解释(所以呢)。格子放不下第三样, 所以从格子墙改成一张三列的表; 文案在后端(`explain` 返回 {label,value,why}), 前端只排版 —— 「多少算大」的分界只该有一处定义。**③ 决策台 12→9 列**: 「贵不贵」+「怎么办」竖排合成「结论」并挪到 AI 之前(上面是位置、下面是动作, 自上而下读); 「现价」「涨跌」合成一列(两个数天生一起读); 「止盈线」撤掉(**信息没丢** —— 破了或逼近时「结论」列会直接判成「按纪律走」/「盯着」并把线价写在徽标上, 比单独一列更早进视线); 「仓%」从「成本」格挪到「仓位」格(**不是删掉这个能力** —— 今日总览的组合总仓位、回撤纪律、超配提醒全靠它, 没别的地方能填)。**④ 立下交易哲学为硬约束**(AGENTS.md 第 10 条): 大道至简、趋势为王、利弗莫尔心法, **判定层在两个方案之间犹豫时选「让人少动一次」的那个** —— 趋势没坏就不给卖出理由; 逆势的「机会」一律不给; 「没事」是常态; 新增任何一档判定之前先问它会让人多动还是少动。①正是这条纪律的第一次应用。 | 改 `services/watchlist_urgency.py`(新增 `_band_call`, 到轨分四种情形)、`indicators/keltner_geometry.py`(explain 重写成 {label,value,why})、`services/review_service.py`、`decision-board/cells.tsx`(ConclusionCell)、`WatchlistDecisionBoard.tsx`(9 列重排)、`StockReviewDialog.tsx`、`lib/api.ts`、`AGENTS.md`; 测试 +13 例, 两条旧断言按新语义改写 | 中(到轨的方向判定变了 —— 这是修 bug 不是调口径; 版面大改) | 可 |
| R211 | **决策台版面收敛 14→12 列 + 感叹号说明重写(几何含义打头) + 复盘依据区改成状态词**(用户: 「这个叹号里面的东西不对, 解释含义才放里面, 比如几何解释这些」「趋势通道和通道态势可以放在一起吗」「怎么办和贵不贵这两个当作结论」「量化通道我不喜欢这样搞, 不美观」「趋势和通道态势整合一起」「用数字看不懂, 还是直接告诉当前数据代表什么的状态更好」「通道态势点击应该是一直循环所有形态, 遍历完成后会轮到取消」「ai 信号这一列里面的文字都是靠左对齐才好看」「横着憋改成横盘中」)。**① 感叹号说明重写。** 原来那份是**词典**(逐个解释列名), 而且**跟界面脱节了** —— R209 删掉的「该动了」还在里面, R205 新增的「怎么办」反而没有。现在第一节直接讲**几何含义**: 三条线是同一个价格的三种看法 / 它们之间的距离就是分歧有多大 / 谁在上面就是方向 / 分歧太大就是没有共同的合理价 / 为什么用「倍日常波动」当尺子。**不先讲这个, 底下那些「间距」「挤在一起」都只能死记。** 依旧守着只讲含义不讲算法。**② 三列并成一列「走势」**: 「量化通道」(测量) + 「通道态势」(结论) + 「趋势」(六态)。前两者是同一套指标的两个层次, 拆开等于让人左右对眼把结论和依据接起来; 六态与通道阶段答的是同一个问题(往哪走)只是方法不同, **放一格里它们什么时候打架上下一对就看见了**。三行、一个数字都没有。**③ 「怎么办」+「贵不贵」并成结论区**(相邻 + 一道右分界线), 但**刻意不合成一格**: 「贵不贵」是「怎么办」的五个输入之一, 而它们打架时恰恰最该被看见(那时「怎么办」判「先别动」)—— 并排才对得上, 挤进一格反而让人以为结论主要来自它。**④ 表头排序标记压成一行**(原来那个彩色徽标会换行, 表头断成两截); **点击改成循环所有目标, 走完一圈回到默认顺序** —— 六态→间距→短→中→长→取消。五个目标各带一个天然合理的方向, 所以这一列不再走「再点一下翻方向」。**⑤ 复盘「依据」区: 数字换成状态词。** `+1.4 / -1.3 / 10% / 中下中` 扫一眼没有意义 —— 得先知道「多少算大」才读得出好坏, 而那正是不该逼人记的。现在是 比之前快 / 走到中段 / 几乎一致 / 现在没挤在一起 / 多数时候是分开的 / 几天的短波动 / 三档都在中部。数字全退到悬停。**⑥ 修了一处 R200 的漏网**: `dominant_cn` 还写着「高频(消息驱动)」—— 它是后端直接送到界面的一个词, 不在任何被扫描的文件里, 所以那轮清行话时漏了。**⑦ 「横着憋」→「横盘中」; AI 信号列改左对齐**(别的列是短标签, 居中合适; 这一列是整段会换行的文字, 居中之后每行起点都不一样, 读起来像被撕开的)。 | 改 `indicators/keltner_geometry.py`(dominant_cn / 横盘中)、`decision-board/GlossaryDialog.tsx`(整份重写)、`decision-board/cells.tsx`(删 ChannelStackCell 与 KELTNER_CLS, ChannelStateCell 吞并三档与六态)、`WatchlistDecisionBoard.tsx`(列宽表/表头/排序循环/行序)、`StockReviewDialog.tsx`(依据区状态词); `tests/test_glossary_hides_math.py` 覆盖词跟着界面改 | 中(版面大改, 判定与打分口径零改动) | 可 |
| R210 | **修「一个票都不显示」—— 候选路 C 是死代码, 一次都没跑过**(用户: 「重新甄选打分因子, 一个票都不显示出来了, 这样不行」)。**根因**: R201 加路 C(通道憋着劲/刚走出来)时, 判定写在 `score_opportunities` 里、遍历 `extras`。可 `extras` **只给 `cand_syms` 备料**, 而 `cand_syms` 恰恰就是路 A/B 已经选出来的那批 —— 于是路 C 的第一行 `if sym in cands: continue` 把**每一个都跳过了**。后果正是用户看到的: 熊市里路 A(当天转多/回升)和路 B(贴到买点 2% 内)可以连着几天一只都没有, 候选池整个是空的 —— 而保底 FLOOR_ROWS 是从候选池里取的, 池子空了它也保不出东西。**当时也「有测试」**: `test_憋着劲那一路拿到的是中性新鲜度` 测的是 `score_candidate(coiling=True)` 这个**纯函数**, 它当然是绿的 —— 但**没有任何一条测试问过「路 C 到底选不选得出票来」**。教训: 纯函数测试证明不了接线, 新增一条数据通路必须配一条**端到端**的。**修法**: 新增纯函数 `coiling_candidates(trends, bands_map)`, 提到 `cand_syms` **之前**跑 —— `bands_map` 本来就是按 持仓 ∪ 全部自选 算的, 早就覆盖了全体, 零新增取数。路 C 只填 A/B 用剩的额度(它连方向都还没出来, 比 A/B 都弱一档, 不该跟它们抢)。补 6 例测试, 其中一条把整条链走完。**顺带堵掉第二个洞 + 让空页自己说话**: 板块过滤会把保底也吃掉(保底从过滤后的池子里取, 池子被滤空就无从保起)。新增 `empty_reason()` —— 机会区为空时**必须说清是空在哪一步**, 因为那是三件完全不同的事: 候选池空(市场状态) / 门槛全挡(门槛在干活, 并列出被哪几道挡了多少只) / **板块过滤滤没了(你自己的筛选, 一键就能撤)**。第三种最冤 —— 明明点一下「全部」就有票, 原来却和前两种共用一句「今日没有把握足够的买入机会」。前端空状态改成显示这句原因, 并在板块过滤导致时直接给一个「去掉板块过滤, 看全部」的按钮。 | 改 `api/today.py`(新增 coiling_candidates / empty_reason, cand_syms 接上路 C)、`pages/Today.tsx`(空状态)、`lib/api.ts`; `tests/test_today_opportunity_rank.py` +9 例 | 低(只是让本来就该跑的路真的跑起来; 判定与打分口径零改动) | 可 |
| R208 | **「方向反过来了」改成直接陈述 + 图例统一补「期」 + 关键价位默认勾上量化通道短期**(用户: 「方向反过来了不够明白」「比如量化通道中期, 都要加个期字」「关键价位的指标默认选量化通道短期」)。**①「方向反过来了」→「说买的反而更差」。** 原名说的是抽象的方向, 读的人还要自己想「什么的方向、反过来之后我该干嘛」; 换成直接陈述发生了什么, 一句话读完就明白。**措辞刻意用「更差」不用「反而跌」**: 判据是 `多头侧均值 < 空头侧均值`, 而 **+1% 对 +3% 同样命中** —— 写成「跌」就是在没跌的时候说它跌了。这一档本来就常常是小样本下的巧合(正文里明说了「多半是巧合」), 标签再把话说过头, 用户真反着做就是被它坑的。两层用同一个标签, 有测试钉住。**② 日 K 图例补「期」**: 「量化通道短/中/长」→「量化通道短期/中期/长期」; 曲线右端标签「量通道短上」→「短期上沿」(端点挤在 K 线右缘, 组名图例里已经说了是量化通道, 再重复前缀只会把字压窄); 导出列「短通道」→「短期通道」。至此三档在**所有**露面的地方都带「期」。**③ 关键价位默认勾上量化通道短期。** 原来 `defaultLevelTypes = []` —— 图上一条线都没有, 每次打开都要自己点。十几个价位组里, 短期通道是唯一**每天都在动、且决策台整张表都在用**的那一个(「贵不贵」「该动了」「通道态势」三列的位置判定都以它为准)。**④ 顺带修了自己上一轮写的一个测试夹具**: 价位层是按 `tone` 分侧的(不是按 `key`), 夹具没给 tone, 于是永远落在「样本不够」—— 那条测试当时是绿的但什么也没测到。 | 改 `services/review_service.py`、`AnalysisKChart.tsx`、`lib/decisionBoardExportColumns.ts`、`WatchlistDecisionBoard.tsx`; `tests/test_review_verdict.py` +2 例 | 低(纯文案与默认值) | 可 |
| R209 | **撤掉标的格里重复的「该动了」+ 重做「通道态势」列 + 到轨说清哪条轨 + 在酝酿→酝酿中**(用户: 「已经有怎么办的列了, 标的里面的那些就不多余了」「通道态势这一列得重新做, 看不懂这样的表述」「到轨要说清楚到什么轨」「在酝酿改成酝酿中」)。**① 标的格里那两行「该动了」删掉, 连同 `UrgencyLine` 与它的两张配色表。** 那是**同一句话印了两遍** —— 「怎么办」列本来就是把「该动了」当成五套判定之一合成进去的, 于是同一只票的「生命线跌破 399.85 已跌破 3.6%」左右各印一份。R198 当初把它挪进标的格是为了省一列, 现在收敛层顶上了它就成了纯重复。**R193 那条纪律没丢只是搬了家**: 「方向要直接写出来」现在由「怎么办」的标题承担。判定本身一个字没动, 排序键与「只看要动的」照旧走它。标的列 11%→8%, 让给「怎么办」。**② 「通道态势」重做 —— 问题不在措辞, 在种类。** 上一版摆的是「下跌中 / 短线低 1.7 倍波动 / 速度没变」, **后两行是测量值**, 而这一列挨着「贵不贵」和「怎么办」, 它的位置决定了它该出结论。现在两行、**一个数字都没有**: 阶段(七档带方向) + 「走到中段 · 速度平稳」。「走到哪一步」的门槛直接用打分那一层已经在用的 SPREAD_LAUNCH / SPREAD_MATURE / TORN_ATR, **不另编一套** —— 界面上说「走了很长」的那一刻打分那边也正好在扣分, 两边永远对得上。数字全退到悬停。**③ 「到轨」→「到上沿」「到下沿」。** 上沿和下沿是**两个相反的动作**(一个偏卖一个偏买), 共用一个「到轨」等于把它们画成同一件事; 旁边虽有买/卖色块, 标题本身读起来仍一模一样。**④ 「在酝酿」→「酝酿中」。** | 改 `services/watchlist_urgency.py`(_mk 支持逐档 label)、`services/stock_playbook.py`、`indicators/keltner_geometry.py`(phase 加 maturity_cn/pace_cn)、`decision-board/cells.tsx`(删 UrgencyLine + 重做 ChannelStateCell)、`WatchlistDecisionBoard.tsx`、`lib/api.ts`; `tests/test_keltner_geometry.py` +12 例 | 低(删的是重复显示; 判定层零改动) | 可 |
| R207 | **把残句改成完整的话 + 修「走过头」不分方向这个真错误 + 两层判定统一成同一套买卖标签**(用户: 「一路往下走、间距 -1.7 匀速 这类描述太含糊」「每个类别都要明确说明只能用于买入还是只能用于卖出」)。**① 「间距 -1.7 匀速」是两个残句拼的**: 「间距」没说是谁和谁之间、没有单位、负号要人自己想是什么意思; 「匀速」单独摆着不知道在讲什么(匀速地涨? 匀速地跌?)。现在每一行都是一句完整的话 —— 「短线低 1.7 倍波动」/「速度没变」, 决策台、今日总览、悬停、速查表读数带一并改。**② 阶段名改成带方向的短判断**: 一路往上走→上升中、一路往下走→下跌中、后劲不足→涨势转弱、刚开始分开→刚启动、挤在一起等方向→横着憋、看不出来→看不出。原来那批是大白话没错, 但**没说清是什么在走、往哪走** —— 在一张几十行的表里「一路往下走」既可能被读成「这只票在跌」也可能被读成「某个指标在降」。**③ 修了一个真错误: 「走得过头了」不分方向。** 它由 |间距| ≥ 5 触发, 所以一只**深跌**的票也会落到这一档, 却配着「追进去的性价比很低」—— 对一只已经崩下去的票, 根本不存在「追」这个动作。现在按符号分成**涨过头 / 跌过头**, 文案各说各的: 涨过头说「再争论贵不贵没有意义, 追进去性价比很低」, 跌过头说「别急着抄, 等三条线重新靠拢或短线站回长线上方再谈买点」。有测试逐条钉住。**④ 价位那一层与六态层统一成同一套六个标签**: 买卖都能用 / 只能用来买 / 只能用来卖 / 看不出差别 / 方向反过来了 / 样本不够。原来价位层用的是「只能用来找便宜 / 只能用来躲贵」—— 两层问的本来就是同一个问题(这套判定我能拿来买、拿来卖, 还是两头都行), 两套说法只会让人以为它们是两种不同的东西; 区别留给下面那句正文说。**⑤ 修了一处自己的手误**: 拼字符串时把 `f` 前缀拼进了正文, 界面上会显示成「—— f短、中、长」。这类错不会报错只能扫, 所以补了一条正则测试守着。新增测试 11 例(方向区分 / 跌过头不许说追高 / 阶段文案不许出现裸「间距」/ 数字必须带单位 / 阶段名 ≤4 字 / 无漏 f)。 | 改 `indicators/keltner_geometry.py`(PHASE_CN + phase 分方向 + 各档 why 改完整句)、`services/review_service.py`(价位层档位名)、`decision-board/cells.tsx`(ChannelStateCell + geoLines)、`decision-board/ComboTableDialog.tsx`、`components/today/OpportunityTable.tsx`; `tests/test_keltner_geometry.py` +11 例 | 低(纯文案与档位名; 判定逻辑只有「走过头」分了方向, 档位本身没变) | 可 |
| R205+R206 | **个股分析页信息架构: 新增「怎么办」收敛层 + 撤掉「置信」列 + 档位名与「通道结论」改大白话**(用户: 「把零散的数据堆砌转化为具备明确投资参考价值的结论性信息」「把只有进攻灵、两头都灵、反着来这类含糊术语化的描述换成正常人日常使用的自然表达」「通道结论这类难以理解的表述改用直白明确的说法」)。**诊断: 这页的问题不是数据多, 是没有收敛层。** 决策台上有**五套彼此平行的判定** —— 该动了(紧迫度) / 六态趋势(方向) / 通道结论(位置) / 通道阶段(成熟度) / AI 信号(买卖), 每一套单独看都对, 摆在一起就是让用户每天在脑子里做一次五路合成。**而且系统从来不说这五套什么时候打架** —— 「六态在多头侧 + AI 说卖出」这种组合每天都在发生, 界面把它们并排摆着谁都不提一句; **打架恰恰是最该停手的时刻, 现在反而是最容易被忽略的时刻。****① 新增 `services/stock_playbook.py`(纯函数, 零新增取数)** —— 五套判定 → 一句「怎么办」, 六档: 按纪律走 / 今天就得动 / 先别动 / 盯着 / 留意 / 没事。**优先级里最关键的一条是「判定打架」排在「逼近」之前**: 「还差 1.2% 到买点」这种话会诱人下手, 而判定分歧时正是不该下手的时候 —— 先说分歧, 再说距离。**刻意不滥报**: 「观望」「持有」「无结论」这类不表态的一律不算矛盾, 否则一半的票都会亮, 三天后用户就学会无视它。这一层**不产生任何新判定**, 每句话都能追到某一层的原话。21 例测试。**② 决策台加「怎么办」为第一列(13%), 撤掉「置信」列** —— AI 置信度 R134 就已明确「退出评分、只作注记」, 一个不参与任何决策的数字却独占一列; 而它旁边的 AI 信号列本来就有徽标。14 列不变。**③ 档位名全面改大白话**: 「只有进攻灵」→「只能用来买」、「只有避险灵」→「只能用来卖」、「两头都灵」→「买卖都能用」、「分不开」→「看不出差别」、「反着的」→「反过来了」、「只有低吸灵」→「只能用来找便宜」、「只有高抛灵」→「只能用来躲贵」; 「偏买档 / 偏卖档」→「说便宜的 / 说贵的」。理由: 「进攻」是买还是加仓?「灵」是准还是有用? 这些词要读的人先在心里翻译一道。**④「通道结论」→「贵不贵」**: 决策台列名、复盘弹窗页签(「这个价贵不贵」)、逐日表表头、感叹号说明、台账维度名(「价位贵贱」)一并改。它答的本来就是「现在这个价算贵还是算便宜」, 原名却要人先知道「通道」是什么。 | 新增 `services/stock_playbook.py` + `tests/test_stock_playbook.py`(21 例); 改 `api/stock_analysis.py`(/urgency 返回 playbook)、`services/review_service.py`(档位名与文案)、`services/score_ledger.py`、`api/today.py`、`decision-board/cells.tsx`(PlaybookCell)、`WatchlistDecisionBoard.tsx`(列宽表/表头/排序/删置信)、`StockReviewDialog.tsx`、`GlossaryDialog.tsx`、`lib/api.ts` | 低(收敛层是新增的一列, 不改任何既有判定; 撤掉的置信列本来就不参与决策) | 可 |
| R203+R204 | **27 组合速查表进系统 + 匀速基准补上 + 因子自选 + 删 3 个孤儿文件 + 建立镜像构建约定**(用户: 「我要看到系统结论和几何含义、偏离基准加速度等等」「没用的孤儿文件删除就行了」「为了解决因子越多挤得越狠, 因子我可以在今日总览页面自定义选择哪些开启」)。**① 27 种组合速查表**(`keltner_geometry.combo_table` + `/api/stock-analysis/combo-table` + `ComboTableDialog`)。**结论那一列是直接调作者的 `verdict()` 生成的, 不是誊抄** —— 誊抄的表会漂: 底层哪天改了措辞, 对照表就开始说假话, 而且**没有任何东西会报错**, 那是最难发现的一类错。「几何含义」同样是由三档位置**推**出来的, 不是 27 句手写。入口挂在决策台「通道态势」列上, 点开顶部先给这只票的读数带(三档位置 / 离三条中线各多远 / 三线间距 / 快慢 / 挤了几天), 再高亮它在 27 行里的那一格 —— **「我现在在哪、这意味着什么、旁边是什么」三件事一起才有用**。**② 匀速基准补上**(`keltner_geometry.baseline`)。这一层推导 R195 就写在模块头里, 却**五轮没算出来过**: 匀速时 `d短 : d中 : d长 = 1 : 3.105 : 6.263` 是精确成立的, 偏离这条线就是加速/减速。它与打分用的差值形式 `a1` 是同一件事的两种写法, 但比值这版**在界面上好讲得多**: 「短期离中线 1.2 时中期该到 3.7, 实际只有 2.1 —— 跟不上」这句话用户能自己核对, 而 `a1 = −0.08` 不能。所以打分照旧走差值(比值在 d短 跨零时会翻转解释), 界面走比值; `|d短|` 太小时**不给结论**而不是给个看着像真的假数。有测试钉住两种写法的方向一致。**③ 补掉问题①的最后一半**: 「中中上 / 中中下」底层返回「无结论」, R195 已经给它们写了注记, 但决策台那一列**仍然显示 `—`** —— 注记只在悬停里。底层禁止改, 所以改的是显示: 有注记时把注记标题当徽标摆出来。「中中中」照旧 `—`(它是真的零信息)。**④ 因子自选**(`opportunity_score.enabled_weights` + `today_prefs.factors` + `FactorPicker`)。用户自己想到了对的解法 —— 带宽窄是「平均」的固有性质, 少平均几个带宽就回来了。**实测: 全开 20 分 → 留 6 个 29 分 → 留 4 个 38 分 → 只留 2 个 70 分。** 两条守住的: **关掉 ≠ 没读到**(关掉的不进覆盖率分母, 不被置信系数罚, 否则每关一个掉几分等于告诉用户别动这个开关); **两根轴各自至少留一个**(整根轴关空会让合成分退化成只看另一根, 那不是自定义因子是把两轴结构关掉了)。因子目录**从权重表推**, 前端不写死 —— 写死的话哪天加了新因子而目录忘了跟, 界面少一个勾选框而那个因子照样在打分。**⑤ 删掉 3 个孤儿文件**(`pages/Analysis.tsx` / `components/CandlestickChart.tsx` / `lib/ocrInstallHint.ts`, 共 512 行)。三个都是**上游文件且上游自己也没接线**, 已核实无任何引用。**保留的两个不是死代码**: `services/auction_scan.py` 是 R144 明说「暂时不要了、只摘 UI」的休眠件; `lib/button.ts` 是 R128 立的按钮配方(0 采用, 但删了等于抹掉那次决定)。**⑥ 建立镜像构建约定**: 用户「以后这个项目构建都要按照这个要求」→ 落到 `docs/docker-build-convention.md`, 并在 `AGENTS.md` 的 Fork 硬约束加第 10 条。**本次只立约定未动构建**。 | 新增 `docs/docker-build-convention.md`、`decision-board/ComboTableDialog.tsx`; 改 `indicators/keltner_geometry.py`(baseline / combo_table)、`api/stock_analysis.py`(/combo-table)、`api/today.py`(factor_catalog + factors 透传)、`services/opportunity_score.py`(enabled_weights)、`services/today_prefs.py`、`pages/Today.tsx`(FactorPicker)、`decision-board/cells.tsx`、`WatchlistDecisionBoard.tsx`、`lib/api.ts`、`AGENTS.md`; 删 3 个孤儿文件 | 低(打分口径只在用户主动关因子时变; 底层判定零改动) | 可 |
| R202 | **决策台新增「通道态势」列 + 补掉导出 HTML 里的公式泄露**(用户: 「评估现有延伸指标, 明确哪些新计算值值得作为新增列插入个股分析的列, 并说明其分析价值」)。**逐个问「它能不能改变我今天的动作」, 只有四个过关, 合成一列**: **阶段**(唯一直接回答「现在该盯什么」, 而且它是压缩/间距/快慢三个量合起来的判定, 一个词顶三个数)、**三线间距**(一个带符号的数同时给方向与成熟度, 而且**可排序** —— 升序扫就是「刚立住的」, 降序扫就是「走得最远该收的」)、**快慢变化**(与间距正交: 间距说走了多远, 快慢说还有没有劲; 同样间距 2.5, 提速和变慢是两个处境)、**挤了几天**(只在有值时显示, 不占固定行高)。**明确不进列的与理由**: 压缩指数与间距单调对应(同一件事投两次票); 频段能量回答的是「这波是谁在推」, 属于研究不属于今天的动作; 离各自中线与旁边「量化通道」列讲的是同一件事; 三档位置码已经由「结论」列翻成人话了。R193 那条纪律在这里同样成立 —— **一列 80 行是用来扫的, 扫的时候没人会悬停**, 所以列里只放能被扫出来的东西(一个词 + 一个带符号的数), 细节留悬停。决策台 13 → 14 列, 列宽表与 colgroup 同源, `<th>` 数量已核对一致。**导出同步**: 列驱动的导出里也加了这一列, 但**默认不勾选** —— 导出是拿去发给别人的, 带上它该是一次有意识的选择。**顺带补掉一处比屏幕更该守的泄露**: 导出 HTML 的页头说明里白纸黑字写着「短期 MA20±2ATR / 中期 MA60±2.5ATR / 长期 MA120±3ATR」。R200 那一轮的扫描面只盯着屏幕, 漏了它 —— 而**导出的文件恰恰是唯一会离开本机的东西**。扫描面因此改成跟着「能外传的东西」走, `test_glossary_hides_math` 加入 `decisionBoardHtmlExport.ts`(34 例)。 | 改 `decision-board/cells.tsx`(新增 ChannelStateCell)、`WatchlistDecisionBoard.tsx`(列宽表/表头/排序键 spread/行)、`lib/decisionBoardExportColumns.ts`、`lib/decisionBoardHtmlExport.ts`、`tests/test_glossary_hides_math.py` | 低(纯展示层, 判定与打分零改动) | 可 |
| R201 | **评分系统 v3(置信系数 / 中性锚点 / 名次取代裸分 / 保底不空页) + 候选路 C + 量化通道补「期」**(用户: 「分析现有评分系统逻辑与缺陷, 重新设计一套合理、有逻辑且依据充分的升级版评分系统, 要求筛选标准不能过严导致今日总览页面无任何个股显示」「量化通道都要补一个期, 让别人明白是短期中期长期」)。**先量后改, 三个缺陷都是跑出来的不是猜的**: ① **缺数据反而排在前面** —— 同样条件下只有 state+fresh 两个因子(覆盖率 .13/.26)拿 100 分排第 1, 十个因子全齐的同类票只有 82。`_blend` 把缺失因子的权重让给还在的那些, 而缺掉的往往正是会把分拉低的那几个; `partial` 只在同分时参与排序, 挡不住。修法是乘一个**置信系数** `(cov_q × cov_t) ** 0.25` —— 四次根是刻意的: 缺一个因子扣 2 分(无感), 只剩两个因子 100 → 43(显著)。**衰减而不是否决**, 与门槛层「缺数据一律放行」是同一条纪律的下半句: 不判你坏, 也不让你占便宜。整根轴缺席时只按活着的那根算, 否则会把票乘成 0 分凭空消失。② **「无信息」的默认值散在 55~60**, 每只票的底子都被垫高一截。统一锚到 50(与大盘同步 / 没有循环 / 速度没变 / 方向还没出来 全部 = 50), 震荡是负面信息压到 42, 蓄势仍是 100。③ **把握分不适合当筛选旋钮, 而且这是数学性质不是 bug** —— 实测 p10~p90 只有 17 分(65~82), `min_score = 60` 实际只挡掉 1.6% 的候选。**拧曲线修不好**: 拿满量程均匀随机因子跑同一套「5 因子加权平均 → 两轴几何平均」, p10~p90 也只有 24 分 —— 是「平均」这件事本身在把取值挤向中间。所以不硬掰分数, 改三条: 绝对分保留(台账要它跨日比较)但界面主显**名次与分位**(天然相对, 熊市里前 20% 仍然有票); 门槛语义不变, 另加**保底条数** `FLOOR_ROWS=3`(够格的不足时把分最高的几只摆出来并标 `below_bar`, 界面上用一条虚线分开并写明「矮子里拔高个, 不是推荐」) —— **页面永远不空, 而「今天没有够格的票」这个事实也没被掩盖**; `filtered` 的含义收紧成「只数没过门槛的」, 与它在界面上的说法对上(原来把 max_show 截掉的也算了进去, 用户读到的是个错的理由)。**④ 空页最常见的成因其实在更上游**: 候选池本身是空的 —— 路 A 要六态当天转多/回升, 路 B 要贴到买点 2% 内, 熊市里可以连着几天一条都没人走。新增**候选路 C**: 三条线挤在一起(憋着劲)或刚走出来, 且六态在多头侧 —— 它**不依赖当天发不发信号**。原料是 keltner 那趟批量已经算好的 geo/runs, **零新增取数**; 新鲜度给中性档 55(低于 near_breakout 的 82, 因为它更早、更未确认), 并在 ctx 标 `fresh_from=coiling` 让台账单独检验这条路。**⑤ 量化通道补「期」**: 界面上的 短/中/长 一律改成 短期/中期/长期 —— 与作者在 `keltner.BANDS` 里本来就写着的中文名对齐。**⑥ 修了一处陈旧文案**: 今日总览的把握分说明还写着 R134 的「趋势强度 45% + 量能确认 30% + 位置成本 25%, 先过三道硬门槛」, 早在 R189 就不是这样了; 空状态那句「已替你滤掉」在保底之后也不再成立(走到那里只剩「候选池是空的」一种情形)。测试从 3036 加到 3045, 新增覆盖: 置信系数的单调性/封顶/温和度、稀薄票不再压过齐全票、保底的三条性质、路 C 的新鲜度优先级、中性锚点。 | 改 `services/opportunity_score.py`(confidence + 六条曲线锚点 + FRESH_COILING)、`api/today.py`(路 C / FLOOR_ROWS / rank·pct_rank / filtered 语义)、`components/today/OpportunityTable.tsx`(ScoreCell 改名次优先 + 保底分段)、`pages/Today.tsx`、`lib/api.ts`、`decision-board/cells.tsx` 等补「期」; 5 个测试文件跟着改口径 | 中(打分口径变了, 名次会重排; 门槛判定与四道硬门槛一个字没动) | 可(整体 revert 回到 R200 的打分) |
| R200 | **界面文案全面改成大白话 + 把「现在处在哪一段」铺到三个界面 + 补掉三处参数泄露**(用户: 「别用拉开脱开这种词, 不够通俗易懂」「有什么要修复的都修好」「系统结论和几何含义都升级替换用上了吗」「这些大白话的描述很适合放到系统里面展示」)。**① 换词。** 屏幕上一律不再出现 拉开/脱开/粘合/撕裂/分离度/偏离度/频段/归一化/带通, 换成 挤在一起 / 走出来 / 散开 / 离得太远 / 三线间距 / 离中线多远 / 波动主要来自。事件名同步: 末端钝化→涨势没劲、压缩待变→憋着劲、趋势内加速→趋势中提速、强势洗盘→强势甩人。**单位一并换成「倍日常波动」** —— 读法与原来一模一样, 但不点名 ATR, 与把 Keltner 改叫「量化波动通道」是同一个目的。**② 把 `explain()` 接上界面。** 它从 R195 起就在算, 却一直只躺在接口里没人显示 —— 现在摆在复盘弹窗「依据」区**那几个数字的前面**: 先读三句人话就够了, 数字是留给要核对的时候看的。**③ 阶段(现在处在哪一段 + 该盯什么)从只有复盘弹窗, 铺到决策台悬停与今日总览。** 事件回答「今天发生了什么」, 阶段回答「整体走到哪一段、该盯什么」—— 后者才是能直接照着做的那句。`kg.phase()` 是纯函数, geo/runs 两个界面本来就都拿着, **零新增取数、零新增查询**。**④ 修了三处真泄露**(都是这一轮才翻出来的, 全在用户眼睛能看到的地方): 决策台悬停写着「破轨门槛 2 / 2.5 / 3」、三档格悬停写着「距上轨 N 个 ATR」、今日总览的 title 里写着「三条带的交集 / 短带宽度」「最高/最低收盘 ≤ 1.35」「(9.5:20:30)」。**⑤ 测试跟着补。** `test_glossary_hides_math` 由 8 例扩到 28 例: 原来只扫感叹号那一份, 而**藏不藏得住取决于最松的那一处**, 所以同一把尺子量到决策台/今日总览/复盘弹窗三处通道文案, 外加一条扫后端 `explain()/phase()/event()/COMBO_NOTES` 正文的。扫描器本身也改对了两处: **先去掉注释再扫**(注释是写给维护者的, 该讲算法; 第一版整段扫下去会逼着人删注释去迁就测试), **只留带中文的行**(`KeltnerBand` 这类 TS 类型不在屏幕上, 卡它们只会逼人给类型改名)。 | 改 `indicators/keltner_geometry.py`(explain/event/phase/COMBO_NOTES 文案)、`api/today.py`(channel_phase + AI 提示词)、`api/stock_analysis.py`(/urgency 返回 phase)、`services/opportunity_score.py`、`services/score_ledger.py`(标签名)、`components/stock-analysis/decision-board/cells.tsx`、`GlossaryDialog.tsx`、`WatchlistDecisionBoard.tsx`、`StockReviewDialog.tsx`、`components/today/OpportunityTable.tsx`、`lib/api.ts`(ChannelPhase); 新增测试 28 例 | 低(纯文案与展示层, 判定与打分一个数没动) | 可(整体 revert 即回到 R199 的措辞) |
| R198+R199 | **决策台重排(该动并进标的/三档合一/全表居中/感叹号说明) + 通道结论栏从"摆数据"改成"给结论"**(用户: 「该动这一列撤掉, 放到每个标的名称的第二行」「这几列的值通过换行显示变成同一列, 列名你重新想一个」「内容都要居中对齐每一行每一列」「用个感叹号在合适的角落当按钮, 点击显示这些东西的含义, **但是不能告诉别人具体是怎么算出来的**」「通道结论这部分也思考一下, 如何排版和内容的显示才能更有价值, 而不是展示单纯的数据, 对我有指导性意义」)。**① 决策台由 16 列减到 13 列**: 「该动」不再单独占一列, 挪进标的格的第二行(判定一个字没改, 只是从"另一列"变成"名字底下的一句话" —— 自选一多, 左右对眼比上下读一行累得多; R193 那条"方向要直接写出来"的纪律照旧带过去); 短/中/长三档合成一列**竖排三行**, 列名取「量化通道」—— 它们本来就是同一个指标在三个尺度上的读数(共用同一个 ATR 分母), 拆成三列是把一件事摊成三份看, 合成一格之后上下一对比就知道三个尺度是不是同向。**排序键一个没丢**: 点表头在 短→中→长 之间轮换。原来的 `KeltnerCell` 随之删掉, 没留没人调的死代码。**② 全表居中**: `TD_BASE` 由 `align-top` 改成 `align-middle text-center`, 13 个表头一并统一。R194 当时选顶对齐是因为那时只有三列天然多行、行高差得远; 这次把该动并进标的、三档并成一列之后**多行的格子反而每行都有且行数接近**, 居中更稳; 而且水平居中写进常量之后, "逐列核对表头与单元格是否同向"那件活儿从此不存在。**③ 感叹号说明**: 新增 `GlossaryDialog`, 挂在决策台标题旁。**一条硬规矩: 只讲怎么读, 不讲怎么算** —— 文案里不出现均线周期、ATR 倍数、任何阈值、任何公式或比例、也不出现 Keltner/利弗莫尔/Minervini 这些本名, 与改名隐匿是同一个目的。**有测试扫这个文件**(`test_glossary_hides_math.py`, 8 例, 分五类词逐类断言), 因为文案是会漂的 —— 下次改一句话时顺手补个"(即三带交集除以短带宽度)"就泄了。反过来也守: 每条必须有"含义 + 读法"两段, 且覆盖本次所有新词, 光藏不说等于没有这个按钮。**④ 通道结论栏改成三层** —— 这是 R191 那个教训的第二次应用: R198 我在这栏摆了八个指标格, 那**正是 R191 之前「趋势状态」栏犯过的错(全是测量, 没有一条是结论)**。压缩 0.9 是好是坏? 分离度 2.4 呢? 单看每一个都答不了"我该怎么办"。现在: **①阶段判定**(新增 `keltner_geometry.phase`) —— 压缩/分离/加速三个量合起来落到七个阶段之一(蓄势待变/启动初期/趋势推进/末段钝化/极端拉伸/下行途中/说不清), 每段配一句"该盯什么"(不是买卖指令 —— 同一阶段对持仓与空仓要做的事不同); **②这套结论灵不灵**(新增 `review_service._verdict_edge`) —— 与 R191 给六态做的那层完全平行: 偏买档 vs 偏卖档按已兑现段数加权合并再相减, 给出 两头都灵/只有低吸灵/只有高抛灵/分不开/反着的/样本不够。**watch/hold/avoid 三档不进任何一侧** —— 它们本来就不构成方向主张, 塞进去会把两侧都稀释; **③那八个数降为"依据"**, 标题直接写"上面两条结论就是从这些数读出来的"。**⑤ 复盘弹窗接上几何层**(`review_service._channel`), 这是本轮之前一直欠着的那块 —— 决策台上这些只挤得下一格悬停, 复盘是唯一有地方摊开的位置。**⑥ 修了一处自己写的测试太粗**: `test_只读底层不改动它` 原来用字符串扫 `= 2.0/2.5/3.0`, 撞上了阶段层新加的 `SPREAD_MATURE = 3.0`(那是分离度门槛, 与通道倍数毫无关系)。换成精确断言: 直接比对 K/WINDOW 常量, 并要求 TORN_ATR/NESTED_ATR/LAG 三个派生量真的由 K 与 WINDOW 推出来而不是写死。 | 改 `indicators/keltner_geometry.py`(`phase` + 七个阶段常量)、`services/review_service.py`(`_channel` + `_verdict_edge`)、`components/stock-analysis/WatchlistDecisionBoard.tsx`(列表重排)、`components/stock-analysis/decision-board/cells.tsx`(`ChannelStackCell`/`UrgencyLine`, 删 `KeltnerCell`/`UrgencyCell`)、`components/stock-analysis/StockReviewDialog.tsx`(三层重排)、`lib/api.ts`; 新增 `components/stock-analysis/decision-board/GlossaryDialog.tsx`、`tests/test_glossary_hides_math.py`(8 例)、`tests/test_keltner_geometry.py` +7 例 | 中(**决策台列数与版式大改** —— 16 列减到 13、全表居中、该动换位置; 判定与打分一个字没动。作者内置指标与通道底层仍零改动, 有 diff 自证) | 列表 → `BOARD_COLS` 与 thead/tbody 各恢复三处即可; 居中 → `TD_BASE` 改回 `align-top py-2`; 说明按钮 → 删组件与那一行挂载; 通道结论三层 → `VerdictView` 里去掉前两张卡片 |
| R197 | **补齐三个只写在文档里、从没算过的量: O 的时间积分 / 频段能量分布 / 磨底判据换成 ATR 归一化**(用户: 「O 的含义呢? O 的时间积分呢? 压缩指数呢? 敏感度、加速度呢? 都检验一遍这些指标」)。**先做了一次逐项校对**(算了 / 显示 / 进分 / 台账四栏), 查出三个真缺口 —— 「O 的时间积分」四栏全空、「频段能量/敏感度」只在模块头的文字里出现过一次从没实现、「压缩持续天数」算了显示了却没进分也没替换磨底。**① O 的时间积分 `compress_avg`**: 用户问的"重叠面积"落到实处就是它 —— 单时点上三条带是区间(交集是长度), 一段时间上扫出带状区域, **交集面积 ÷ 窗口长度**就是这个均值。**它与 `compress_days` 量的不是同一件事, 两个都要**: 连续天数会被中间一天的脱开清零, 均值只是被拉低一点。一只 `compress_days=0` 而 `compress_avg=0.9` 的票是"刚刚启动"; 反过来是"反复脱开又粘回来"。**② 频段能量分布 `band_energy`** —— 敏感度那条推导的 per-stock 落地。三档通道本质上是一组带通滤波器(`C−MA20` 高通 / `MA20−MA60` 带通 20~60 / `MA60−MA120` 带通 60~120), 这里量各频段的 RMS 能量占比。**关键是必须扣掉匀速趋势基线**: 匀速下三个带通的幅度天然正比于各自覆盖的天数(9.5:20:30), 不扣的话这个指标会**恒定说"低频占优"** —— 那是均线的定义不是这只票的特征。各自除以 `ENERGY_REF` 再归一化后, **纯趋势恰好三份各 1/3**(有测试钉住), 偏离 1/3 的才是信息: 高频超出 = 消息驱动, 中频超出 = 行情主体, 低频超出 = 老趋势余温、动能在衰减。三种合成路径都验过。**③ 磨底判据换掉**: `base_score` 的时长那一半改用 `compress_days`, **只换判据不换问题** —— 回答的仍是用户那句「我想知道一个票磨底磨了多久」, 但尺子从"最高/最低收盘 ≤ 1.35"(绝对幅度, 对 ATR 3% 与 1% 的票意义完全不同)换成"三带交集 ≥ 80% 连续几天"(全程 ATR 归一化)。档位那一半照旧走红绿节拍(它量"磨得好不好", 与"多久"正交)。压缩天数取不到时回落原判据, 不因为换尺子就丢结果。实测: 绝对箱体说磨了 90 天、按 ATR 只粘了 15 天的票, base 由 100 降到 84.6。**④ 三处都接上了**: 台账新增「主导频段」维度; AI 上下文新增压缩持续天数/季度平均压缩度/波动主导频段; 界面上决策台结论列悬停与今日总览候选展开行都摊开(平均压缩度与连续天数**并排显示并写明二者的区别**, 频段占比标注"纯趋势各 33%")。**⑤ 傅里叶/拉普拉斯只取了真有用的那部分**: Z 变换说明了均线是线性相位 FIR、群延迟 `(n−1)/2` 对所有频率是常数 —— 这把前面「MA_n ≈ (n−1)/2 天前的价格」从"局部线性近似"升级成了**精确性质**, 整个二阶导推导的地基因此更结实; 带通结构也来自同一个推导。**没有做 FFT**: 价格是随机游走主导、功率谱 ≈ 1/f², 直接做谱只会得到"低频占绝对主导"这个对所有股票都成立的废话, 要有信息必须看相对 1/f² 基线的偏离 —— 而 band_energy 扣基线做的正是这件事, 且不需要 FFT。拉普拉斯是连续时间工具, 日线是离散采样, 不适用。 | 改 `indicators/keltner_geometry.py`(`compress_avg`/`band_energy`/`ENERGY_REF`)、`services/keltner_service.py`(附 compress_avg 与 energy)、`services/opportunity_score.py`(`base_score` 收 compress_days)、`api/today.py`(喂 runs、行上带出、台账、AI)、`services/score_ledger.py`(主导频段维度)、`tests/test_keltner_geometry.py`(+8 例)、`lib/api.ts`、`components/stock-analysis/decision-board/cells.tsx`、`components/stock-analysis/WatchlistDecisionBoard.tsx`、`components/today/OpportunityTable.tsx` | 中(**磨底判据变了** —— base 因子的时长口径由绝对箱体换成 ATR 归一化, 名次会动; 但取不到压缩天数时自动回落原判据, 不会因此丢分。作者内置指标与通道底层仍零改动, 有 diff 自证) | 磨底 → `base_score` 里删掉 `compress_days` 那三行即可回到绝对箱体; 两个新量 → 删 `compress_avg`/`band_energy` 及其调用, 前端按可选字段处理会自动不画 |
| R196 | **量化波动通道: 改名隐匿 + 几何层全面融入系统(进把握分/台账/AI/界面)**(用户: 「改名变成量化波动通道, 目的是隐匿我的指标」「这套指标都靠谱吗, 都应用了吗」「波动通道延伸的那些组合含义压缩指数什么的都融入到系统中」)。**① 改名**: 用户可见的「Keltner」字样全部换成「量化波动通道」——K 线图例三档改「量化通道短/中/长」、六条端点标签改「量通道短上」等、今日总览两处悬停、导出说明、台账维度名。**刻意避开一个撞名**: 现有 `atr_stop` 的标签已叫「ATR波动通道」, 三档若直接叫「波动通道」会撞, 所以短名用「量化通道」、全称用「量化波动通道」。**作者文件里的 Keltner 字样一律不动**(`indicators/levels.py` 的 `LEVEL_TYPES` 内部分组名、`strategy/builtin/keltner_reversion.py`) —— 前者经查**根本不显示给界面**(前端 `LEVEL_GROUPS` 有自己的标签表), 后者是策略文件只读。**② R195 那层此前是"算了但没用"**, 这次真正接进系统四处: **打分** —— `spread`(短长均线间距, 带符号)进质地轴、`accel`(加速度)进时机轴, 各占 .15, **原有因子按精确 0.85 倍缩放**(不是两位小数圆整: 0.11×0.85=0.0935 写成 0.10 会把换手率抬高 7%), 有测试断言老四项恰为 0.85 倍, 保住 R189「相对比例是从 R134 推出来的」那个性质; **台账** —— 新增通道事件/加速度/组合三个分组维度, 其中**事件那条是最该被验证的**(「主升浪特征之后是不是真的更好」直接决定那五条阈值该不该这么定); **AI 上下文** —— 两轴说明补上新因子, 并新增一节教它怎么读这几个数(尤其点破「突破尝试与突破站稳差的只是那两天」); **界面** —— 决策台「结论」列徽标下**明面显示事件**(未确认的加 `?` 且用虚一档颜色, 依 R193 的教训不塞悬停), 悬停给完整几何; 今日总览候选展开行新增一整块通道几何(事件/加速度/分离度/重叠/组合/偏离度 + 组合补充注记)。**③ 为什么打分用 spread 不用压缩指数**: 压缩指数**没有方向** —— 下跌趋势的重叠度与上涨趋势一样是 0; spread 一个数同时表达方向与分离度。**两条曲线都是倒 U 不是单调**, 与 R134 那三条同一条哲学: 分离度 1.5~3 个 ATR 是甜区(趋势确立但没走过头), >5 是尺度撕裂; 加速度峰值在 0.12(约合成样本 75 分位), 过度加速是拉升末端的赶顶。**④ 修了一处自己的检查失效**: 权重那一步第一次其实**没落盘**(脚本在后面的断言处中止, 而我看到的「和=1.0」对新旧权重都成立, 这个检查根本区分不了新旧) —— 补测后改成断言「老四项恰为 0.85 倍」, 这个才真的能区分。 | 改 `services/opportunity_score.py`(两条曲线 + 两个因子 + 权重重排)、`api/today.py`(喂 geo/事件、行上带出、台账 ctx、AI 提示词)、`services/score_ledger.py`(三个新维度 + 维度名改名)、`tests/test_opportunity_score.py`、`tests/test_score_ledger.py`(fixture 补通道读数)、`lib/api.ts`、`components/stock-analysis/decision-board/cells.tsx`(事件上脸 + 几何悬停)、`components/stock-analysis/WatchlistDecisionBoard.tsx`、`components/today/OpportunityTable.tsx`、`components/stock-analysis/AnalysisKChart.tsx`、`lib/todayHtmlExport.ts` | 中(**把握分口径又变了** —— 两轴各多一个因子, 名次会与 R189 不同; 但 `SCORING_VERSION` **没再跳**: R189 刚跳到 3 且台账还没攒够样本, 再跳一次等于把两次改动混在同一个空样本里, 反而分不清是谁的功劳。几何算不出来时两个因子缺席、`partial` 标出, 老路径照常) | 打分 → `QUALITY_WEIGHTS`/`TIMING_WEIGHTS` 删掉 spread/accel 并把老四项还原成 R189 的值; 显示 → cells.tsx 的 `evLine` 与 OpportunityTable 那一整块删掉; 改名 → 字符串替换回去 |
| R195 | **三档 Keltner 的几何补充层: 速度 / 加速度 / 压缩指数 / 事件分类 —— 底层一字未动**(用户: 「我想知道这三个指标互相之间的排列组合都代表着什么, 重叠部分面积又代表着什么, 比如 K 线那些有二阶导是加速度的说法」「这些结论都要用上, 显示到每个个股」「原本系统的 Keltner 短期/中期/长期**禁止动, 这是根本**」「这一套组合是它的补充」)。**推导的地基**: 三条带共用同一个 ATR14 分母, 所以三档读数是**同一个量的三次采样**, 可以直接做加减 —— 这是后面一切的前提。`d_n=(收盘−MA_n)/ATR`, 于是五档位置在 ATR 尺上是硬阈值(短破轨 2 / 中 2.5 / 长 3), **三档门槛不同**, "三档同时破轨"不是三个独立事件而是递增门槛的嵌套。**一阶**: 均线是滞后算子, `MA_n ≈ (n−1)/2 天前的价格`, 所以收盘与三条均线是价格在滞后 0/9.5/29.5/59.5 天处的四次采样, 相邻两档之差就是那一段的平均速度(统一成 ATR/天才可比)。推论: 三个通道位置的大小顺序**就是**均线排列, 两者是同一件事的两种写法。**二阶(用户要的那个)**: `a1=v1−v2`, 匀速时 a1=0 是**精确成立**(匀速 v 下 C−MA20=9.5v 而 MA20−MA60=20v, 各除自己的天数都得回 v) —— 有测试钉住这条恒等式。等价的比值形式是 `d60/d20` 比 3.105(=59/19), 但比值在 d20 跨零时解释会翻转, 所以用差值。门槛 0.05 ATR/天(=10 天累积半个 ATR)是**量出来的**: 合成日线上 |a1| 中位数约 0.12, 这个门槛把"匀速"切出约两成。**频率分解**: 同一个价格变化, 单日冲击的 Δpct 之比是 1:0.80:0.67(短期最敏感), 持续趋势的 (pct−0.5) 之比是 1:2.48:4.18(长期最敏感) —— 短期档是高通、长期档是低通, **三档读数之间的不一致本身就是这波行情的频率成分**。**重叠(用户问的"面积")**: 单时点上三条带是区间, 交集是**长度不是面积**; 压缩指数 `O=三带交集/短带宽度∈[0,1]`。两个临界是闭式解并已数值验证: 短带被长带完全包住 ⟺ |MA20−MA120| ≤ 1 ATR; **完全脱开** ⟺ ≥ 5 ATR(此时不存在一个价格同时对"一个月视角"与"半年视角"都算正常, 命名为**尺度撕裂**)。一段时间上的"面积"就是 O 的时间积分。**O 不能替代磨底**(用户问过): 磨底给的是「多久」、O 给的是「多紧」, 是两个维度; 但 O 的**判据**更好(原磨底用绝对箱体 1.35 倍, 对 ATR 3% 与 1% 的票意义完全不同), 所以正确的合并是「磨底时长 = 连续 O ≥ 0.8 的天数」, 保留问题换掉判据。**打分要用带符号的 spread 而不是 O** —— O 没有方向, 下跌趋势的 O 同样是 0。**事件分类器(回答用户最关键的那个困惑)**: 「K 线穿过短期上轨算站稳还是突破还是主升浪」—— 这个问题混着三个独立维度, 少一个都答不了: 位置(通道) × 方向(六态) × **确认(在轨外连续几天)**。破上轨本身只是统计陈述, 同一个破上轨在 UT 里是趋势内加速、在 DT 里是反弹撞阻力、在 NR/SR 里才是真突破; 而**突破与站稳的区别根本不在位置上而在时间上**(第 1 天 vs 连续 ≥2 天)。主升浪给了**五条可回测的定义**(UT + 多头排列 + 分离 ≥2 ATR + 连续 ≥3 天守在上轨 + 未减速), 有测试逐条钉住缺一不可。**边界: 底层零改动**。中途我一度把 `keltner.py` 的 10 条结论扩到 18 条来修"3 种组合无结论 + 6 种文案说谎", 用户指出那是根本不能动 —— 已 `git checkout` **完全复原**, 改成补充层的 `COMBO_NOTES` 旁注: 27 种组合里 11 种补一句、16 种明确标注"底层已说准", 无遗漏无重叠, 底层结论照旧显示。**零新增取数**: 几何量全部从已算好的上下轨反推(轨 = MA ± k·ATR 是恒等式); 历史序列搭 `_ma120_map` 那次本来就在发生的批量读(全自选每天都要滚 MA120), 只多要一列 atr_14。**顺带做了一次作者内置指标的全面校对**: `pipeline.py` 与 `indicators/__init__.py` 与上游**完全一致(0 差异)**; `levels.py` 有 24+/5− 是**历史 fork 提交**(b0969b6 等)造成的, 内容是加两个 level 类型 + 让 `_keltner_band` 改调 `indicators.keltner` 以保证图表与决策台同一组公式 —— 已逐一核对 fork 的 BANDS 常量与作者原来硬编码的三组值**完全相同**(ma20/20/2.0、ma60/60/2.5、None/120/3.0), 数值行为不变; `strategy/builtin/keltner_reversion.py` 是 fork 新增的策略文件(历史提交 25dc8d4)。本次会话对作者文件的唯一改动是 `api/stock_analysis.py` 的 `/urgency` 多返回一个 `event` 字段(纯新增键)。**验证**: 后端 2991 passed / 3 skipped(新增 27 例); `git diff` 对 `strategy/`、`levels.py`、`pipeline.py`、`keltner.py` 全部为空。 | 新增 `indicators/keltner_geometry.py`、`tests/test_keltner_geometry.py`(27 例); 改 `services/keltner_service.py`(批量读多一列 atr_14 + 附 geo/runs 两个新键)、`api/stock_analysis.py`(/urgency 多返回 event) | 低(**纯补充层** —— `indicators/keltner.py` 的三档定义与 10 条结论一字未动并有 diff 自证; 新加的全是新键, 老字段一个没变; 几何算不出来一律返回 None 不给半成品) | 删 `keltner_geometry.py` + `keltner_service.py` 里 geo/runs 两处 + `/urgency` 的 event 块即可, 底层不受影响 |
| R194 | **决策台每列每行对齐: 统一垂直基线 + 表头与单元格逐列对上 + 列宽表驱动**(用户: 「个股分析的每一列和每一行都要整齐对齐」)。**根因是没有一处统一定义**: 行内 td 写 `py-2.5` 走默认 `align-middle`、Keltner 与结论写 `px-1.5 py-2.5`、R193 的「该动」写 `py-1.5 align-top` —— 三套各写各的。平时看不出来, 一旦某行的「该动」是三行、「AI 信号」是三行, **单行的那几列(现价/涨跌/浮盈/置信)就飘到了行的垂直中间, 而多行的那几列贴着顶**, 一屏扫下来没有任何一条共同的基线。**① 统一到 `TD_BASE = 'align-top py-2'`, 全表 16 个单元格无一例外**, 定义只有 cells.tsx 一处(它本来就被主文件 import, 不新增依赖)。选顶对齐而不是居中: 这张表有三列天然多行(该动 / AI 分析 / AI 信号), 居中会让"这一行从哪儿开始读"每行都不一样; 顶对齐之后每一行的所有列都从同一条线起笔。连带把标的按钮的 `items-center` 改成 `items-start` —— 整表转顶对齐后它还居中的话, 标的名会在 2.25rem 的框里往下沉半行, 与同行其余列的第一行文字错开, 那是最刺眼的一种不齐。**② 表头与单元格逐列核对过**: 16 列的水平对齐(left/right/center)与水平内边距(px-2 / px-4 / px-1.5)现在两两相同 —— 唯一不一致的是「该动」, 表头写 `text-center` 而 R193 的内容是左起, 改表头为 `text-left`。**③ 数字统一 `NUM = 'font-mono tabular-nums'`**: 这是列对齐的关键 —— 没有 `tabular-nums`, 比例字形下 `1` 比 `8` 窄, 386.50 与 1088.00 的小数点在列里对不齐, 一列数字看着像波浪线; 只给 `font-mono` 不够, 有些等宽字体的数字仍走比例宽度。补齐了成本/仓% 输入框、止盈线、该动的距离这三处漏网的。**④ 列宽改表驱动 `BOARD_COLS`**: colgroup 与空表提示的 colSpan 从此同源。这一处是有前科的 —— R178 加「该动」列时只加了 `<th>` 没加 `<col>`, 15 对 16, 从那天起每个宽度都串了一位(R193 才发现), R184/R189 改列数时又要手动同步 colSpan。改成从一张表渲染之后只剩「th 数量要跟上」这一处需要人盯。**前端没有测试运行器**(package.json 里没有 vitest/jest), 所以这条不变量靠两样守: 单一来源的常量, 以及 BOARD_COLS/th/td 三处数量本身可被一眼核对(已核: 16/16/16)。 | 改 `components/stock-analysis/decision-board/cells.tsx`(导出 `TD_BASE`/`NUM`, 四个单元格改用)、`components/stock-analysis/WatchlistDecisionBoard.tsx`(16 个单元格改用基线 + BOARD_COLS + 表头该动改左对齐 + 三处补 tabular-nums) | 低(**纯样式** —— 没有一处逻辑、排序、取数、判定改动; 行高比原来紧 4px(py-2.5→py-2)是有意的) | `git revert` 即可 |
| R193 | **「该动」列把话说清楚: 补方向、补哪条线、补该干什么; 顺带修 R178 起的列宽错位**(用户: 「这一列要把话说清楚, 太简洁了, 这也不行, 会误人子弟」)。**问题是真的会害人, 不是嫌简陋**: 原来单元格只画一个胶囊「逼近 0.5%」, 其余全在悬停的 title 里。而**一列 80 行是用来扫的, 扫的时候没人会悬停** —— 于是那句解释等于不存在。缺了它, 「再跌一点就破止损, 准备卖」和「再涨一点就转强, 是个买点线索」这两个**相反的动作**在这一列里完全同形: 同一个词、同一个琥珀色、连距离都可能一样。截图里连着三行「逼近 0.0% / 0.2% / 0.5%」正是这种情况。「已触发 0.0%」更糟 —— 触发档的 `distance` 被写死成 `0.0`, 显示出来像「离触发还有 0%」(还没破), **意思正好反了**。**改法**: 档位(急不急)与方向(买还是卖)是两个正交的维度, 原来只显示了前者。判定层现在每一档都出四样 —— `kind`(哪条线促成的)/`side`(sell/buy/info)/`what`(哪条线、什么价、差多远)/`action`(该干什么), `reason` 仍是二者拼成的整句留给悬停与导出。单元格改成三段: 档位胶囊 + **单独一个方向色块**(不靠语义色暗示, 直接写「买」「卖」) + 两行小字(事实一行、建议一行)。**价位进话里**: 「离转强价 412.30 还有 0.5%」比「离转强价还有 0.5%」多的那个数, 是能直接照着挂单的那个。**已触发档改报"已经破了多少"**, 真实距离取自出场线。**几处方向判断是有讲究的**: 空仓票逼近转弱标 `info` 不标 `sell`(本来就没拿, 标"该卖"是无中生有); 通道上轨是卖方向、下轨是买方向; 转多第一天是买、转空第一天是卖。**导出同改, 而且那边更要紧** —— 导出的 HTML 拿去存档打印转发, 纸面上的「逼近 0.5%」连悬停都没有。**顺带修一个 R178 埋下的错位**: R178 加了「该动」列的 `<th>` 却**没加对应的 `<col>`**, 15 个 col 对 16 个 th, 从那天起每个列宽都串了一位 —— 本该给标的的 8% 落在了该动上, 标的只拿到现价那份 3.5%(名字被挤到换行就是这么来的), 一路顺延到最后。补上 col 并给该动 7%(要放两行说明)。R189 我核对过"16 col / 16 th"却没发现 —— 因为 `<col` 是 `<colgroup` 的前缀, 那次的计数把 `<colgroup>` 也数进去了, 15+1 正好凑成 16。**验证**: 后端 2964 passed / 3 skipped(新增 12 例, 含一条专钉"逼近止损与逼近转强必须分得出方向"), 前端 build 退出码 0, lint 无新增 warning, `git diff backend/app/strategy/` 为空。 | 改 `services/watchlist_urgency.py`(`_mk` 签名 + 各分支带方向与人话 + `_nearest_flip` 返回整份说明 + `_num`)、`tests/test_watchlist_urgency.py`(+12 例)、`lib/api.ts`、`components/stock-analysis/decision-board/cells.tsx`(UrgencyCell 重写)、`components/stock-analysis/WatchlistDecisionBoard.tsx`(补 col)、`lib/decisionBoardExportColumns.ts` | 低(判定的**档位与排序一个字没动** —— 新增的全是描述字段; 前端按可选字段处理, 老数据只是少显示两行) | 单元格 → UrgencyCell 恢复成单胶囊(后端多给几个键不影响); 列宽 → 删掉那个 `<col>` 即可 |
| R192 | **模拟盘改成一次只看一个操作员, 下拉切换, 内容全摊开**(用户: 「每个操盘手在模拟盘这页显示完整所有东西, 不同操盘手通过下拉菜单切换」)。**① 下拉切换取代平铺**: 原来是把所有操作员的卡片一路铺下去 —— 配三个模型就是三份净值图 + 六本账, 一屏放不下, 而任一时刻真正在看的只有一个。下拉的每个选项**带上两本账的收益率**(如「deepseek-v4 — 全市场 +12.3% / 我的自选 -1.2%」), 所以「切之前先看一眼谁领先」这件事不需要靠铺开来完成, 平铺唯一的好处就此保住。选中项落 localStorage, 刷新回到原处; 存的那个已被删掉时退回第一个, 不留白屏。**② 持仓明细展开**: R190 刚把它收进 `<details>`(照参考项目那个单标的面板), 现在改回展开 —— 这一页每天要回答的就是「它现在拿着什么、当初打算怎么办」, 那不该点一下才看得见。用的是**账本的 positions 而不是 R183 那份批次派生视图**, 因为前者是超集: 多了市值、建仓日, 以及买入时立的三条线(止损/止盈/到期)。R183 那份「批次表」保留为一个 `<details>`, 但**只在真的拆开过时才画**(`lots.length > positions.length`) —— 同一只票只买过一次的话, 它就是把同样的行再列一遍。**③ 操作记录展开**: 模块头从 R59 就写着「每一笔都留着理由, 那份记录才是这个功能真正的产出」, 可它一直藏在弹窗里要点两下。现在全部记录直接在页面上, 超过一屏在框里滚, 不分页不截断 —— 被拒的单子与纪律强平那几笔恰恰是最该翻的。`OrderRow` 加 `dense` 参数只收窄内边距, **内容一个字不减**。**④ 弹窗只剩一个理由**: 「明细」按钮改成「它看到了什么」, 弹窗默认落在 context tab(一大段 prompt, 不适合摊开); 另外两个 tab 留着当放大镜(表格更宽、能滚全屏)。**代价与取舍**: 每本账多一次 `paperBook` 查询(60 秒轮询) —— 但一页只显示一个操作员, 所以是**两次**, 而原来平铺三个操作员时这些数据一次都不取。净支出很小, 换掉的是每天点四下(两本账 × 开弹窗 + 切 tab)。 | 改 `pages/PaperTrading.tsx`(下拉切换 + 选中项持久化 + BookPane 内嵌 positions/orders 两张表 + OrderRow dense + 弹窗默认 tab)、`lib/storage.ts`(注册 `paperTrader` 一个 key) | 低(**纯前端读法** —— 接口、撮合、纪律、AI 上下文一个字没动; 新增的是既有 `paperBook` 端点的两次调用, 那个端点本来就在被弹窗用) | 下拉 → 把 `current &&` 换回 `traders.map`; 摊开 → 把两张表包回 `<details>` 即可 |
| R191 | **复盘「趋势状态」加判定层: 从测量变成结论**(用户: 「思考趋势状态这个部分怎么显示更有价值, 拿到的结果更有指导性, 不喜欢单纯的展示」)。**诊断**: 这一栏五块内容(涨停四计数 / 涨停出现在 / 磨底 / 各状态 5 日均值 / 逐日表)**全是测量**, 没有一条回答用户打开复盘时带着的那个问题 ——「这套六态在**这只票**上到底灵不灵, 我该怎么用它」。四个状态的均值并排摆着, 得自己在脑子里两两相减才读得出结论, 而那个结论恰恰是可以算出来的。**① 最值钱的一条: 多头侧 vs 空头侧的分离度(`_side_edge`)**。把 UT/NR/SR 与 DT/NREA/SREA 各自按**已兑现段数加权**合起来(加权不是简单平均 —— 4 段的一档和 7 段的一档不能一样重), 得出六档判定: 两头都灵 / 只有避险灵 / 只有进攻灵 / 分不开 / 反着的 / 样本不够。**「只有一半灵」是这一层真正的产出**: 截图里那只兆易创新, 多头侧 11 段合起来 +0.0%、空头侧 13 段 -7.2% —— 结论是「在这只票上六态是离场信号不是买入依据, 买点另找」。这件事在四个并排的均值里根本看不出来(单看「上涨趋势 +7.9%」会得出相反结论), 必须合侧才显形, 所以得由系统算, 不能指望人去减。**② 「现在」条(`_now`)**: 当前状态第几天 + 这只票上这个状态历史平均持续几天 + 你在前/中/后段 + 历史 N 次之后平均怎么走、几次收涨 + 再走到哪个价换状态。这些数原来**全在页面上, 只是散在三个地方**(今天在表格第一行、平均时长藏在小结的悬停里、翻转价压根没有), 要回答「我现在在哪」得来回对三次。**③ 封板率(`_seal`)**: 「涨停 6 / 炸板 5」两个并排的计数, 要自己去除才读得出「冲板 11 次只封住 6 次 = 这票封不住板」, 而那是这四张卡片里**唯一能直接改变操作**的信息。冲板少于 3 次不给比率(那个数会被当成结论)。**④ 一致性摆到脸上**: 小结胶囊补上「几段收涨/几段已兑现」。「+7.9%」可能是一次 +40% 拉起来的, 也可能是四次都涨两个点 —— 该做的事完全不同, 只给均值看不出是哪一种。**⑤ 版面顺序倒过来**: 结论(现在 / 分离度)在最上面, 测量降到下面当依据, 小结的说明文字改成「分档依据」。**⑥ 顺带修一处 R189 之后就过期的文案**: 磨底那段还写着「这一条不参与把握分, 还在等台账验证」—— 现在「反复失败」是硬门槛 G4(直接否决)、「蓄势」进了质地轴。过期的口径说明比没有说明更坏, 它会让人按错的规则读这个标; 改成按当前档位分别说清它进了哪一层。**判定层零新增取数** —— 全部用 `_trend_outcomes` 已经算好的段统计(n/scored/avg_fwd/win/avg_days), 有测试用 `inspect` 钉住它不碰 repo/polars。**门槛写成常量**: 一侧至少 3 段已兑现才下结论(1~2 段是纯噪声), 一侧算「有方向」要 5 日均值达到 3%(A 股 5 日振幅本来就大, 门槛太低会把噪声读成信号)。**验证**: 后端 2952 passed / 3 skipped(新增 26 例), 前端 build 退出码 0, `git diff backend/app/strategy/` 为空。 | 改 `services/review_service.py`(`_side_edge`/`_side_stats`/`_now`/`_seal` + `_trend_by_date` 带上翻转价 + payload 三个新键)、`lib/api.ts`、`components/stock-analysis/StockReviewDialog.tsx`(NowCard/SideEdgeCard + 版面重排 + 胶囊补一致性 + 修过期文案); 新增 `tests/test_review_verdict.py`(26 例) | 低(**纯新增读法** —— 六态状态机、阈值、段聚合口径一个字没动; 判定只读已算好的段统计, 样本不够一律返回 thin 不下结论; 三个新键前端都按可选处理) | 判定层 → payload 里删 `side_edge`/`now`/`stats.seal` 三个键, 前端按可选处理会自动不画; 版面顺序 → TrendView 里把两张卡片挪回去即可 |
| R190 | **模拟盘版式严格照参考项目重排, 并删掉顶头两段说明**(用户: 「模拟盘你严格按照参考项目的排版, 现在的排版我还是不满意。也不说顶头那两个说明, 太多废话了」)。**① 删掉的两段**: PositionsHub 顶上那条黄色「模拟盘 · 非真实资金」横幅, 与 PaperTrading 顶上那段二十来行的折叠说明(这一页干嘛的/禁什么/撮合规矩)。两段都是**读一次就够**的东西, 却天天占着首屏, 把每天真要看的净值曲线和指标挤到下面。「非真实资金」并进页头副标题一句话带过, 规则本身一条没改。顺带收掉第三处废话: 定时那一行的「建议收盘后 —— 收盘价出来了才有得算…」挪进 title 悬停。**② 指标条改成参考项目的 `.mp-paper__metrics` 瓦片条**: 外层 `gap:1px` + 线色底, 内层每格自己的底色 —— 那 1px 缝隙就是分隔线, 不画 border; 每格 `flex:1 1 110px` 平分整行, **标签在上、值在下**。R186 我做成了「标签 值」并排、一行 wrap 下来的样子, 那**是行内文本不是仪表盘**: 九个数挤成一段话, 扫一眼说不出哪个是哪个。同时把原来单独放大一行的「总资产」收回瓦片里 —— 它和收益、回撤本来就是同一个视觉重量, 单独放大是两套排版混在一起。指标补齐到九项(收益/总资产/最大回撤/夏普/下场率/交易/天数/持仓/现金), 与参考项目 `MetricRow` 的密度一致。**③ 单栏 + 高图**: 参考项目的 `.mp-paper` 是**单栏面板**, `.mp-equity` 固定 260px。原来操作员卡片 xl 两栏、两本账再左右各半、图只有 168px —— 指标条被压到四分之一屏宽, 净值曲线两条线挤在一起看不出谁在什么时候领先, 而「两条曲线的差」正是这一页要回答的问题。改成一行一个操作员、图 260px、两本账**纵向排开**(对比靠图, 那两条线本来就画在同一张里)。**④ 头部与动作位置照 `.mp-paper__head`**: 操作员的名字/持仓上限/定时由两行合成一行; 每本账的动作按钮(跑一次/查生命线/过计划/明细/重置)从整段最底下提到账名那一行的右侧 —— 纵向排开之后, 沉在底部的按钮离标题隔着一屏。**⑤ 持仓批次表收进 `<details>`**, 对应参考项目底部那个默认收起的「成交记录 N 笔」。 | 改 `pages/PaperTrading.tsx`(删说明、MetricStrip/Metric 重写、TraderCard 头部合并、BookPane 重排)、`pages/PositionsHub.tsx`(删横幅、副标题带上非真实资金) | 低(**纯版面** —— 撮合、纪律、AI 上下文、接口一个字没动; 删掉的两段说明只是文案, 规则不在那里) | `git revert` 即可; 想只要回说明, 把两段 JSX 贴回去 |
| R189 | **把握分重构成「质地 × 时机」两轴 + 趋势模板进质地 + 红绿节拍进门槛; 决策台六列撤销合并**(用户: 「不管是前面的红绿节拍还是现在的六态升级, 只要能有提升我不在乎台账重新开始验证」「蓄势该加多少分也是你定, 你要重新思考怎么规划这个评分系统」「这样显示我非常不满意, 完整显示所有数据」)。**① 重新规划的核心判断**: 原来 trend/volume/position 三个维度是**按数据来源分的**, 于是把两类性质完全不同的信息混进了同一个数 —— 慢变的「这只票好不好」(六态/相对强度)与快变的「今天是不是那一天」(新鲜度/量比/换手/位置)。结果是「结构极好但信号已第 8 天」与「结构一般但今天刚放量」拿到同一个分, 而这两种情况该做的事完全相反。改成**按变化速度**分的两根轴: 质地(趋势模板/磨底节拍/相对强度/六态)与时机(新鲜度/通道位置/量比/换手)。**② 合成用几何平均 √(质地×时机), 这是本次唯一一处「少编了一个参数」的地方**: 加权平均要再编「质地占几成」, 而且允许一边补另一边(质地 95/时机 15 会和两边都 55 打平, 而那是两件不同的事)。几何平均零新参数, 含义正好是用户原话「高概率的有苗头的东西」—— 高概率是质地, 有苗头是时机, 缺一个就不成立。**③ 时机轴的四个权重是从 R134 旧权重推出来的, 不是重编的**(旧有效权重 fresh .2475 / vol .21 / turnover .09 / pos .25 归一化), 所以时机侧的排序行为与 v2 一致; **只有质地轴的四个数是新编的**(template .40 / base .25 / rs .20 / state .15), 逐条理由写在 QUALITY_WEIGHTS 上面: 模板是唯一被公开检验过且分辨率最高(0~8 档)的那组, 给最大份; state 与模板的均线排列讲同一件事且 G1 已经用它筛过一道, 同一个事实投第二次票该轻。**④「蓄势该加多少分」的答案**: 落在 base 那 .25 里 —— 蓄势与震荡在 base 上差 45 分, 折进质地是 11 分, 再经几何平均落到把握分约 4~6 分。够让同档次的票分出先后, 不够让它一个人把一只结构不行的票抬进前排; 有测试把这个边际钉在 2~12 分之间。**⑤ 磨底时长只在「蓄势」时才加分** —— 这一条是写代码时才发现的: 不加这个条件, 一只震荡了 90 天的票会排在一只一路上涨从没磨过底的强势股前面, 而后者恰恰是这套系统最该抓的。同理 RHYTHM_SCORE 里「没有循环」给的是中性偏上的 60 而不是低分。**⑥ 趋势模板进的是质地, 不是门槛**: 原本设想拿它加强 G3, 落到具体代码后判断这是错的 —— 要么设成「至少过 7 条」把早期中继全杀掉(与系统「只做上升趋势中继的早期」自相矛盾), 要么设成一个不否决任何人的摆设。它 0~8 档的分辨率在打分层才有用。**周期保留 50/150/200 原值**不改成本地的 20/60/120: 引入外部标准的全部价值就是它被公开检验过, 换周期得到的只是个长得像的东西。52 周高低点按**收盘**算, 与六态口径一致; 八条判不全就整个因子缺席, 不按 known 缩放(次新股 3 条全过缩放成满分 = 凭空造出来的质地)。**⑦ 唯一真进门槛的是红绿节拍 G4**(failing = 至少两轮进多头又跌出且低点不抬高): 这是唯一一种「次数越多越该躲」的形态, 打分压不住它(次数正是它最多), 只能否决; 而且它**零新参数**。**刻意没有「必须蓄势」门槛** —— 蓄势是加分项不是准入条件, 一路上涨从没磨过底的强势股压根没有循环。**⑧ 数据是白捡的**: MA150/MA200/52 周高低点原来都不存在(pipeline 只有 ma5/10/20/30/60)。做法是给 long_trend_map 加 with_closes 开关, 只有候选那一小撮把窗口从 260 拉到 420 天并带回收盘序列 —— Keltner 长期档那条全自选路径**一行不受影响**。**⑨ SCORING_VERSION 2→3**: 用户明确接受台账重新验证。归因表的算法一个字没改(轴分与维度分同为 0~100), 换的只是记哪几个键; 新增 tpl_passed 分组维度 —— 「8/8 全过的票是不是真的更好」直接决定 TEMPLATE_CURVE 那条上凸曲线该不该继续凸。**⑩ 决策台撤销 R184 的六列合一**: 用户看到实物后不满意。合并把成本/浮盈/止盈线折进了「只有持有才画」的第二行、六个排序键折进了下拉菜单, 省下的那点宽度换不来这个。恢复 16 列(col/th/td 三处已核对), PriceHoldingCell 一并删掉。**验证**: 后端 2921 passed / 3 skipped, 前端 build 退出码 0, `git diff backend/app/strategy/` 为空。 | 新增 `services/trend_template.py`、`tests/test_trend_template.py`(18 例); 改 `services/opportunity_score.py`(两轴 + G4 + base_score/template_score)、`services/keltner_service.py`(with_closes)、`services/market_mode.py`(ret_120d)、`api/today.py`(喂模板/节拍、axes、AI 提示词、ctx)、`services/score_ledger.py`(v3 + FACTOR_LABELS + tpl_passed)、`lib/api.ts`、`components/today/OpportunityTable.tsx`、`components/today/MarketStatusCard.tsx`、`lib/todayHtmlExport.ts`、`components/stock-analysis/WatchlistDecisionBoard.tsx`、`components/stock-analysis/decision-board/cells.tsx` | 中(**把握分口径变了**, 名次会与 v2 不同; 台账 v2 样本退出统计, 用户已明确接受。门槛只多了 G4 且只否决 failing 一种。数据层只在候选路多读半年行情, 全自选路径不变) | 分三段独回: ①两轴 → score_candidate 恢复三维度并把 axes 改回 dims; ②趋势模板 → QUALITY_WEIGHTS 删 template 一项 + long_trend_map 关掉 with_closes; ③G4 → check_gates 里删 rhythm_level 那三行 |
| R188 | **红绿节拍接进系统: 零成本算、界面显示、落台账验证**(用户: 「最优解实现」)。**最优解是两件一起做且有先后**: 先让判定**可核对**(拿图一看就知道对不对, 判定看着就不对的话再多统计也没意义), 再让它**可验证**(落台账看有没有区分度)。**接法找到了零成本的位置**: `livermore_service._trend_payload` 里 `compute` 已经跑完、`steps` 就在手上、原来用完即弃 —— 节拍判定只是对它做一次 O(n) 遍历。另起一条取数路的话, 决策台每只票就要多扫一遍盘。`review_service` 同理(它也在 compute)。**三处显示, 零新增列**: ①**个股复盘弹窗趋势 tab 顶部** —— 这是看一只票历史的地方, 用户那句「我想知道一个票磨底磨了多久」的答案就该在这儿; 显示「磨底 N 天 · 箱体 X~Y · 档位 · N 轮红绿」并把判定理由写出来; ②**决策台趋势列的悬停**, 挂在既有 tooltip 里 —— R184 刚把六列合成一列, 不该马上又加回去; ③台账「回头看」自动获得两个分组维度。**台账那两行正是 R175 那句话第一次兑现**: 当时写「以后往界面加什么结论, 落进 ctx 再登记一行就能回答"我历史上好不好使"」—— 这次真的只加了一行注册。磨底天数是连续量, 直接当维度会碎成几百档, 所以分成四段(没在磨/20-60/60-120/120天以上)才看得出"磨得久的是不是真的更好"。**修了一个自己刚写出来的并发 bug**: 起初把节拍挂在 `_trend_by_date.last_rhythm` 函数属性上传出来 —— 那是**并发不安全**的, 两个复盘请求同时进来, A 票会拿到 B 票的节拍。改成独立函数就地重新 `compute` 一次: 它是纯函数、输入是已经在内存里的 list, 贵的是加载 dataframe 那一步而那步已经做过了。另外把 JSX 正文里的 `**加粗**` 改成真 `<b>`(R175 犯过一次的坑, 会原样显示成星号)。**评分核心仍然一个字没动** —— `git diff opportunity_score.py` 为空, 节拍只是注记与台账维度, 不进把握分、不进门槛。 | 改 `services/livermore_service.py`(`_rhythm` + payload)、`services/review_service.py`(`_rhythm`)、`api/today.py`(ctx 落两个键)、`services/score_ledger.py`(注册两个分组维度 + 两个 fmt 助手)、`lib/api.ts`、`components/stock-analysis/StockReviewDialog.tsx`、`components/stock-analysis/WatchlistDecisionBoard.tsx` | 低(纯注记与展示; 把握分/门槛/出场线一个字没动, 有 diff 自证; 节拍算失败一律降级为 None, 不拖垮趋势链或复盘) | 停用: `_trend_payload` 里 `"rhythm": _rhythm(res)` 改成 `None`, 界面自动不显示; 台账那两行注册删掉即可 |
| R187 | **红绿节拍与磨底时长 —— 规则层新增, 但先不进把握分**(用户: 「一个票经过多次进入上升趋势后又跌出, 反复这样, 就意味着多头力量变强, 应该对这个票加分」「红绿红绿红绿这样力量变强就有可能一直上涨吃到主升浪」, 追加澄清「其实我是想知道一个票磨底磨了多久」)。**先纠了直觉里的一半错**: 「反复进多头又跌出」**同时对应两种完全相反的形态, 而它们的次数一模一样** —— 蓄势(低点抬高/回撤变浅/红段变长)与反复失败(同一位置撞五次没过去、低点走平或下移)。只数次数会把后者排到最前面, **恰好是最该躲开的那种**; 有测试专门钉这一条: 一个循环 4 次但低点下移的样本, 必须判 `failing`, 而循环只有 3 次、低点抬高的样本判 `building`。所以数的不是次数, 是**次数 × 几何形状**, 低点不抬高是**否决项**。新增 `services/trend_rhythm.py`: ①`_runs` 把六态序列按多头侧(红)/空头侧(绿)压成交替段, **过短的段并进前一段而不是丢弃** —— 丢弃会让前后两个同色段接不上、凭空多出一个循环, 把次数灌水; ②`_cycles` 配成"一红一绿"的循环, 每循环量出红段高点/绿段低点/红段占比/回撤幅度; ③`assess` 给**档位**(蓄势/震荡/反复失败/样本不足)**而不是分数**; ④用户追加那句之后补的 `basing`: **磨底磨了多久** —— 从今天往回找最长的一段使 最高收盘/最低收盘 ≤ 1.35, 同时给出箱体上下沿(那就是突破价与破位价, 只给天数用户还得自己去图上量)与 `is_basing`(天数与"够不够格叫磨底"分开, 界面能显示「横了 12 天(还不算磨底)」而不是一个含糊的 0)。**刻意不给连续分数、刻意先不进把握分**, 三条理由写在模块头: ①把握分那三个权重(45/30/25)本身还在等 R133 台账验证, 再编一个数进去会让归因彻底读不了; ②这条路走过一次(R173 注册 5 条把握分因子 → R174 全删); ③**性质不同** —— 把握分三维度全是当下快照, 这个是历史过程, 两种时间尺度硬揉进同一个加权里没法读。路径是: 先当注记算出来 → 落台账当分组维度 → 台账说它有区分度再决定进门槛还是维度。**可从历史 K 线回算**(livermore 给完整 steps), 所以不像 R175 的通道结论有"不记就补不了"的时间性, 台账能直接拿历史跑。**修了两处自己的问题**: 测试文件里两个 `test_空输入不崩` 重名(后者会静默盖掉前者); `assess_for_symbol` 原来调了一个**不存在**的 `ls.closes_window`, 会被 except 吞成"数据不足" —— 改走 `_load_symbol_window` + `get_effective_threshold`, 与决策台「趋势」列、复盘弹窗**同一套状态序列**(否则用户没法拿界面核对这个判定)。 | 新增 `services/trend_rhythm.py`、`tests/test_trend_rhythm.py`(21 例) | 低(**纯新增, 还没有任何调用方** —— 把握分、门槛、决策台一个字没动; `git diff opportunity_score.py` 为空) | 删这两个文件即可 |
| R186 | **模拟盘补上净值曲线, 版面按 MarketPulse 重整**(用户: 「模拟盘不是我想要的样子, 你参看 MarketPulse 这个项目的」)。**对照参考项目找出的最大缺口是一样东西: 净值曲线。** 那个项目的 `PaperPanel` 核心就是 `EquityChart` + 一排九项密排指标; 而本系统的 `nav_history` 从 R59 起就一直在存, **却从来没画出来过** —— 界面上只有「总资产 98,933」一个数。那个数只说明现在几块钱: 一路冲到 +30% 又跌回来, 和一路平着走, 在总资产上看不出任何区别, 而这是两回事。更讽刺的是代码注释一直写着「这两条曲线的差就是我这份自选有没有价值」—— **曲线根本没画**。于是新增 `components/paper/PaperEquityChart.tsx`(echarts, 项目里已有): 两本账画在同一张图, **归一到各自本金**(两本本金可设成不同的数, 画绝对值没法比), 带 0% 基线, **不做平滑**(平滑会把回撤的尖角磨圆), 少于两点显示"还没跑够"而不是硬画一条平线。后端 `paper_lots.nav_curve` 只出 date/nav 两字段、最近 260 点并且**刻意不做等距抽稀** —— 抽稀会把最大回撤那个谷底抹掉, 而那正是这条线最该让人看见的地方; 列表接口是多操作员 × 两本账一起返回的, 收着点才不会撑回当初 `_summary` 刻意不带 nav_history 全量时的那个问题。**指标行改成参考项目那种一排密排**(小标签 + 加粗值横向平铺), 原来是三格居中网格, 三个数占一整行、既看不出多少又浪费高度; 现在排开收益/最大回撤/夏普/下场率/交易天数/持仓现金, 总资产单独放大一行。**顺带收拾两处我自己造成的问题**: ①R183 之后外壳(PositionsHub)已经画了一条"非真实资金"横幅, 而 `PaperTrading` 嵌入分支里还留着旧的一条, 两条并排重复, 且旧那条文案已过期(还在说「与我的批次互不相干」, 批次早就不是并列 tab); ②那段二十来行的「这个页面是拿来体检的」说明改成**默认折叠** —— 它是读一次就够的东西(这一页干嘛的、禁什么、撮合按什么规矩), 却天天占掉首屏一半, 把每天真要看的曲线和账本卡片挤到下面。③R183 加的批次表用了 `min-w-[22rem] + overflow-x-auto`, 两本账并排时每边只有半屏, 于是它永远卡在横向滚动条里, 改成 `table-fixed` 自适应。 | 新增 `components/paper/PaperEquityChart.tsx`; 改 `services/paper_lots.py`(`nav_curve`)、`api/paper_trading.py`、`lib/api.ts`、`pages/PaperTrading.tsx` | 低(纯展示层新增与版面调整; 账本数据、撮合、决策链一个字没动) | 去掉曲线: 删 `PaperEquityChart` 那一块与 `nav_curve` 字段; 说明想常驻展开的话把 `<details>` 改回 `<section>` |
| R185 | **个股弹窗默认视图从「日K」改成「关键价位」**(用户: 「点进去的默认显示应该是关键价位而不是日K」)。这个弹窗本来就是拿来做决策的 —— `AnalysisKChart` 自己的文件头就写着「本图表面向**分析决策**, 核心是【关键价位】(压力/支撑/密集区/枢轴/前高前低)」, 默认却落在日K上, 等于每次点进来都要自己先切一下。**要改的是两处, 只改一处不管用**: `useState<PreviewView>` 的初值, 以及「弹窗首次打开时重置视图」那一条 —— 后者是为了"弹窗内切股保留当前视图, 但重新打开要回到默认"而设的, 漏了它的话关掉再打开又会跳回日K。**核实过没有副作用**: ①分时图只在 `view === 'intraday'` 时才挂载, 所以默认改成关键价位**反而少了一路轮询**, 不是多; ②日K/财务/分时三个查询的 `enabled` 都只看 `!!symbol`, **不受 view 门控** —— 日K 照常预取, 用户切过去不会多等; ③`StockLevelsPanel` 自己按 symbol 拉数, 默认落在它身上不会空白。 | 改 `components/StockPreviewDialog.tsx`(两处默认值) | 极低(纯默认值; 三个 tab 都还在, 手动切换行为一个字没动) | 两处改回 `'daily'` |
| R184 | **决策台六列合一(现价/涨跌/仓位/成本/浮盈/止盈线), 并修 R183 引入的一处回归**(用户: 「你分析后替我决定一个最优解, 真的有用才动。图片里面圈出来的合并成一列」)。**先判断值不值得动 —— 结论是值得, 理由在数据里**: 后四列(仓位/成本/浮盈/止盈线)要「持有 + 手填成本」才有值, 而自选里空仓票占大头, 于是**四列宽度长期空着**; 更要命的是成本与仓% 是**输入框**, 空着也得留住那份宽度, 还占着表格最中间的黄金位置。这是 R178 分析里第 ⑤ 条(「这张表其实是两件事」)的一个更小、更安全的切法。**合并唯一的真代价是丢 6 个排序键**, 所以表头做成可选排序目标的下拉(点表头选按哪个排, 再点同一个翻方向, 与别的列行为一致)——**一个键没少**。**单元格设计**: 第一行每行都有(持有开关 + 现价 + 涨跌), 第二行**只有持有才画**(成本框/仓%框/批次入口/浮盈/止盈线) —— 空仓行因此只有一行, 行高变矮, 一屏多看好几行, 这才是省下来的东西。一个刻意的细节: **现价与涨跌固定宽度 + tabular-nums** —— 合并进同一个单元格之后, 纵向对齐全靠它, 数字不对齐这一列就没法一眼扫下去, 那是合并最大的风险。`colgroup` 从六列 24.5% 收成一列 20%, 少的让给趋势/通道/结论那几列(它们才是每行都有内容的); `colSpan` 16→11。单元格搬进 `decision-board/cells.tsx`(与 R167 的先例一致, 配色与布局是实现细节不该堆在主文件)。**顺带修了 R183 的一处回归**: R183 把 `/lots` 改成模拟盘、批次页搬到 `/lots-registry`, 但决策台的 `LotsLink` 仍跳 `/lots?symbol=` —— 点进去会落到模拟盘上看不到批次。这是我上一条改动引入的, 一并修掉。**核对过列数**: 11 `<col>` / 11 `<th>` / 11 个数据单元格, 三处对齐; R182 的导出用的是自己的列注册表, 与表格列解耦, **不受这次合并影响**。 | 改 `components/stock-analysis/WatchlistDecisionBoard.tsx`(表头/单元格/colgroup/colSpan/排序菜单)、`decision-board/cells.tsx`(新增 `PriceHoldingCell`)、`decision-board/LotsLink.tsx`(修回归) | 低(纯前端布局; 排序键与导出均无损失, 数据与判定一个字没动) | 拆回六列: `git revert` 本提交 |
| R183 | **仓位中心并成单一「模拟盘」, AI 持仓按批次表呈现, 补绩效指标**(用户: 「ai操盘手可以删除了…我的批次改造成模拟盘, 全权交给ai打理, 也就是说ai操盘手的功能移植融合到我的批次, ai操盘手现有的数据也要录入」; 追问后澄清「真实成本和我持仓是分离的, 这个部分是专门给ai自己操作的」「要把操盘手的持仓写进批次表」, 并给了参考项目 `always1ov/MarketPulse@claude/ui-redesign-md3-hxamko` 的模拟盘结构)。**动手前先查了所有权, 查出一件必须先问的事**: 「我的批次」是**上游功能**(`strategy/lots.py`/`api/lots.py`/`pages/Lots.tsx` 全是作者代码, AI 操盘手才是 fork 的), 而且它经 `effective_positions` 喂着**决策台的成本/浮盈、今日总览的持仓体检与组合回撤、以及出场线** —— 直接改造会让那几列变成模拟盘数字; 另外作者每条批次会**派生两条监控规则**(止盈止损 + 到期), 几十只模拟持仓写进去就是上百条真实推送。问过用户确认真实持仓与这块分离后才动手。**因此做法是: 批次表用派生视图, 不写进作者的 `lots.json`。** 新增 `services/paper_lots.py` 把账本持仓读成批次的形状(字段名照抄 `cost_price`/`qty`/`buy_date`, 界面上就是批次表), 另补模拟盘特有的现价/市值/浮盈(作者的批次「只生成监控规则, 不做会计」)。**派生而不是复制的理由**: 唯一真相仍在账本里, **没有同步漂移** —— 复制一份到另一个文件, 迟早出现两边对不上的那天; 有测试用 `inspect` 钉住这个模块**不许 import 作者的 lots 领域层**(一旦 import 就有写进去的可能)。**参考 MarketPulse 补的是指标口径**: 那个项目是单票参数化回测器(纯规则、可调参寻优), 与本系统"多个 LLM 每天各自决策"是两种东西, 照搬没有意义; 但它的 `Metrics` 有一套完整的(总收益/年化/最大回撤/夏普/胜率/盈亏比/曝光度/买入持有基准), 而本系统原来只有总资产、持仓现金、交易天数 —— **没有回撤就不知道过程多难受**(一条从 +30% 回撤到 0 的曲线和一条稳稳 +5% 的, 只看总资产看不出区别)。于是补了能从既有 `nav_history` 算出来的: 最大回撤、夏普(少于 20 个点不给 —— 那个数是噪声)、曝光度(空仓躺着跑平不叫本事)、以及基准/超额的位置; **算不出的一律 null 不用 0 顶替**(回撤 0 会被读成"从没回撤过")。前端: `PositionsHub` 双 tab 去掉, 整页就是模拟盘, 顶部常驻"与真实持仓完全分开"的横幅; 导航「仓位中心」→「模拟盘」。**上游的批次页保留一条自己的路由 `/lots-registry`**, 代码一行未动 —— 上游同步在批次那块依然零冲突, 哪天想用回真钱批次登记它原封不动还在。 | 新增 `services/paper_lots.py`、`tests/test_paper_lots.py`; 改 `api/paper_trading.py`、`lib/api.ts`、`pages/PaperTrading.tsx`、`pages/PositionsHub.tsx`、`router.tsx`、`components/Layout.tsx`、`pages/settings/MenuSettings.tsx` | 中(**导航项换了名字与内容**, 打开 /lots 看到的从双 tab 变成整页模拟盘; 但上游 `strategy/lots.py`/`api/lots.py`/`pages/Lots.tsx` 与 `effective_positions.py` **逐一自证零改动**, 真实持仓口径完全不变) | 恢复双 tab: `git revert` 本提交; 上游批次页从未改动, 随时可从 `/lots-registry` 打开 |
| R182 | **决策台导出合并成一个、改成列驱动可选列, 六态汇总并入, 按钮重排**(用户: 「六态汇总改成导出, 重新排序这些按钮。导出格式要跟随列的格式, 而且能选择哪些列导出」, 追问确认「原来的导出 html 是被整合的了」)。**改之前有两套导出**, 各自把列写死在自己的 HTML 模板里: 决策台的「导出 HTML」(11 列, 且只导「结论」列有内容的行)、六态汇总弹窗里另一套(7 列、另一份模板)。两套必然长成两个样子, 加一列要改两处, 用户还没法决定导什么。**现在合成一个**: 新增 `lib/decisionBoardExportColumns.ts` 列注册表(key/label/对齐/分组/默认开关/取值函数), 模板只负责按选中的列渲染。**六态汇总因此不必单独存在** —— 但它有 4 个决策台屏幕上没有的字段(持续/跌破转弱/站上转强/近期信号, 原本藏在「趋势」列的悬停里), 所以**做成默认不勾的可选列保留下来**, 并在选列面板给了「六态汇总」一键预设: 勾上那几列、取消其余, 导出的就是原来那个弹窗。功能一个没丢, 少一个弹窗、少一套模板(`TrendSummaryDialog.tsx` 315 行删除, 确认过全仓无引用)。**一条贯穿的约束**: **导出的读法必须与屏幕一致** —— 同一个字段界面上显示「贴上轨」, 导出件里就不该变成 0.87; 对不上时用户会怀疑哪个是错的。所以每列的取值函数直接产出屏幕上那个读法, 需要上色的单独给浅色样式(导出件要能打印, 深色那套配色搬过去看不清)。**顺带纠正一处**: 导出行原来写死"只导有结论的"、R178 又补了"或要动的" —— 那是列写死时只能靠行筛选控制篇幅的产物; 列可选之后改成**导出当前列表所见**, 篇幅由已有的「只看要动的」「只看持有」两个开关决定, 不再有第三套隐藏规则。**按钮重排**按"改变我看到什么 → 无副作用的工具 → 花钱的动作"分三组: `只看要动的 · 只看持有 ‖ 刷新 · 导出 ‖ AI 分析持有 · AI 分析全部`, 两处竖线分隔。理由: 筛选最常用且无副作用, 直接改变下面这张表, 放最左; AI 两个**按只计费**, 放最右并隔开 —— 误点它们代价最大。重排时三个不改的按钮是从原块里**原样摘出只换位置**的, 没有顺手改它们的行为。选列记在 localStorage, 下次打开还是这套。 | 新增 `lib/decisionBoardExportColumns.ts`、`components/stock-analysis/decision-board/ExportColumnsDialog.tsx`; 重写 `lib/decisionBoardHtmlExport.ts`; 改 `components/stock-analysis/WatchlistDecisionBoard.tsx`、`lib/storage.ts`; 删 `components/stock-analysis/TrendSummaryDialog.tsx` | 低(纯前端展示层; 导出内容变多变少由用户勾选决定, 不影响任何判定与数据) | `git revert` 本提交可恢复六态汇总弹窗与两套导出; 只想换回旧按钮顺序的话改 `WatchlistDecisionBoard` 那段拼装即可 |
| R181 | **消息面加「时效」维度: 时效 / 埋伏 / 规律 —— 修 R180 的一刀切过期**(用户: 「有一些消息是看公司业绩的, 这种就是埋伏, 肯定不会立马兑现, 这种怎么办, 有些是类似市场规律的又怎么办」)。**这是我 R180 设计里的一个真错误**: 给所有消息用了同一个 7 天过期。对用户说的这两类都是错的 —— 业绩埋伏 7 天后就不注入了, 可正是三个月后快忘了的时候最需要它提醒「这只是为业绩埋的, 别被短期波动震出来」; 市场规律(「凯特纳贴下轨胜率高」)根本不该过期。更深一层: **这三类在决策里的用法根本不同**, 混成一段总的, 下游 AI 分不出哪句是「今天的事」、哪句是「埋着的」、哪句是「方法论」。**加的是与 `status`(成立了吗)正交的第二个轴 `horizon`(多久有效)**: `news` 时效(政策/突发, 天级, 过期即淘汰, 影响今天买不买) / `thesis` 埋伏(业绩/基本面, **不按天数淘汰**, 影响持有耐心, 带一个兑现检查点) / `rule` 规律(方法论, **永不过期**, 不针对某只票)。**分类由凝练时的 AI 顺手判** —— 它已经在读这条内容了, 判类型是分类不是决策; 输出改成 JSON(digest + horizon + due_days)走既有的 `ai_json.extract_json_object`, 并且**解析失败不算失败**: 拿不到 JSON 就把原文当要点、类型退到时效 —— 分类是锦上添花, 要点才是用户要的, 为一个分类把整条判死没道理。用户可点芯片循环改(AI 判错要能一键改)。**四处连锁改动**: ①`_still_relevant` 逐条按自己的时效判去留(置顶一律留着 —— 手动钉住是最强的"我还要它"); ②`build_payload` 从平表改成**按时效分区的 dict**, 埋伏那区还带上「该核对的时间」; ③综合提示词要求**分区分别写**, 并明确写「埋伏这些不构成今天的买卖理由」; ④注入文本补上三区用法说明 —— 埋伏「**不能当作今天的买入理由**」但「**出场线与生命线仍然优先, 埋伏不是死扛的借口**」(两头都堵死, 只写一头都会被下游读偏)。整段总结的过期改用 `SUMMARY_STALE_DAYS=30`(判的是"太久没重新综合"), 不再是 7 天一刀切。**另加到期提醒**: `due_theses` 找出到检查点、还没给结论的埋伏, 页面顶部横幅顶到总览之前 —— **埋伏最容易失败的方式不是记错, 是记了之后忘了**, 三个月后财报出来人早忘了当初为什么买; 不做这一步, 消息面就只是个只进不出的垃圾桶, 那段总的也会越来越脏。标了结论后横幅自动消失。**改写了 R180 的 6 条测试** —— 它们钉的是旧形状(list)与旧过期规则; 顺带把测试夹具的写死日期改成"当天", 原来那个 `2026-09-01` 随"今天"漂移正好压在 7 天边界上, 是个会随机失败的雷。 | 改 `services/news_desk.py`(时效档/分区/分档过期/到期提醒)、`services/usage_notes.py`(horizon/due_at 落盘与手改)、`api/usage_notes.py`(`/due` 端点)、`lib/api.ts`、`pages/UsageNotes.tsx`(时效芯片 + 到期横幅); 扩 `tests/test_news_desk.py`(+15) | 中(注入内容的结构变了, 但注入边界与三道闸一条没动; 老数据无 `horizon` → 当时效处理, 与它们本来的性质一致) | 回到一刀切: `_still_relevant` 恒返回 True 并把 `SUMMARY_STALE_DAYS` 改回 7; 时效字段可留(纯新增) |
| R180 | **「我的使用观察」改造成消息面: 上传→AI 凝练→综合成一段总的→注入每次 AI 决策**(用户原话: 「当作是个消息面, 我上传的数据或者图片都会调度 ai 凝练后保存, 然后还要凝练一大段总的, 以后每次做出决策性的结论都得参考一次这一大段总的」)。**四层**: ①`ai_provider` 支持多模态 —— `Message` 的 content 放宽成 `str | list`, 并且**修了一个会立刻炸的坑**: `_estimate_input_tokens` 原来 `str(content)`, 遇到 part 数组会把几十万字符的 base64 当文本算, 一张图就顶出十万"token"、被上下文保护直接拒掉; 改成只数文本 part、图片按固定量(1500, 故意偏高)估, 实测同一请求从 10 万+ 降到 1508。②`usage_notes` 扩 `kind/attachment/digest/digest_at`, **旧数据零迁移**(读时补默认, 老笔记就是一条 kind=text、没凝练过的消息); 新增 `set_digest`(**刻意不刷新 updated_at** —— 凝练是系统行为不是用户编辑, 把旧消息顶到"最近编辑"最前会打乱用户自己的整理顺序, 与既有的标状态/置顶不刷新同一个道理); 删记录时**级联删附件**, 免得盘上攒孤儿文件。③新增 `news_desk`: 逐条凝练(图片走多模态, 模型不支持读图时给一句人能看懂的话而不是甩原始报错)、综合成一段总的(白名单只出日期/状态/要点, **附件路径不进 payload**, 有测试钉)、`context_for_ai()` 供注入。④注入 **只有三处**: 今日 AI 导读·优选、AI 个股信号、模拟交易两轮 —— 全是**本来就由 AI 下结论**的地方。**这一条是整个改造最需要克制的地方, 所以边界写进测试**: 有一个用例扫全仓 `context_for_ai()` 的调用点, 断言它 ⊆ 那三个文件, 并逐个断言 `opportunity_score` / `position_exit` / `keltner` / `livermore` / `watchlist_urgency` / `score_ledger` 里**一次都不许出现** —— 规则层吃了这段 AI 写的话就再也不能自证、不能回测了。**[本轮追加] 只保存凝练, 不保存图片; 文字保留原文**(用户中途补充的口径): 凝练**成功之后**才删原件 —— 一张图删了就再也凝练不了, 失败时文件原样留着让用户重试, 界面上那条会标「待凝练」; 文本文件的内容并进正文(那就是要保留的原文), 且**接在用户自己那句备注后面而不是覆盖** —— 「这是某某的调研纪要」往往比正文还重要; 记录里只留文件名与大小, 路径去掉(不该再指向一个已经不在的文件)。这样消息面攒几年也只是一堆文字, 盘上不长期堆附件。**代价已写进代码与界面提示**: 图片删掉之后没法重新凝练, AI 读错了或以后换更好的模型想重跑, 源都没了。另外三条自我约束: **注入文本自带时效**(生成于何时、距今多久、基于几条)且写死了优先级「不要拿它推翻价格与规则层给出的事实, 冲突时以价格与规则为准」—— 下游是 AI, 它看不见我们这边的判断, 只能靠这段话本身知道分寸; **超过 7 天不注入**(与其拿上个月的消息面影响今天, 不如没有); **可整体关掉**(`settings.news_desk_inject`), 关掉即回到改造前。任何一步失败都只是"这次没带上", 不拖垮今日总览或个股信号。前端: 顶部一段总的(标题行直接写"综合自几条、什么时候、过期没有"), 上传支持选文件与**直接 Ctrl+V 粘贴截图**(研报/公告截图是最常见输入), 传完自动触发一次凝练; 卡片有凝练时以**要点**为主体、原文收进可展开一行(**凝练是多一层不是替换** —— AI 会漏会读错, 而这条之后要进综合进决策, 得能翻回去核对)。状态链一个字没改, 而且标了「不成立」的会被明确喂给综合并要求写清已被证伪 —— 留着一条已经错了的判断比没有更糟。菜单名「我的使用观察」→「消息面」。 | 改 `services/ai_provider.py`(多模态 + token 估算)、`services/usage_notes.py`、`api/usage_notes.py`、`api/today.py`、`services/stock_signal.py`、`services/paper_trader_run.py`、`lib/api.ts`、`pages/UsageNotes.tsx`、`components/Layout.tsx`、`pages/settings/MenuSettings.tsx`; 新增 `services/news_desk.py`、`tests/test_news_desk.py` | **高** —— 这是本 fork 第一次让一段 AI 文本参与**每一次** AI 决策。三道闸: 只进 AI 层(有测试钉死)、7 天过期即停、总开关可关。规则层零影响, `git diff backend/app/strategy/` 为空 | 关注入: `settings.news_desk_inject=false`, 立刻回到改造前; 整条回退: `git revert` 本提交(数据文件 `usage_notes.json` 向后兼容, 老版本代码读新数据只是多几个不认识的字段) |
| R179 | **今日总览: 风险排到机会前面, 行动区重新分级**(用户: 「今日总览这个页面也需要评估和升级」→「也是找最优解」)。**评估出的问题**: ①**版面顺序与紧迫度倒挂** —— 真实渲染是 AI 导读 → 市场状态 → 值得关注(占 300 行) → 需要行动 → 持仓体检, 于是「已跌破生命线, 按纪律无条件清仓」要往下滚过整张机会表才看得见。**而代码自己早就认了相反的优先级**: 区块注释一直写着「① 行动区」「② 机会区」, 只是版面没兑现(文件头注释和区块编号互相矛盾)。②`sev_rank` 只有 `{"high":0,"mid":1}` 两档, 于是「跌破生命线·无条件清仓」和「持有票转下跌趋势」并列 —— 前者无条件、后者看情况, 不是一个量级, 谁排上面全靠 dict 迭代顺序。③监控触发最多灌 **10 条**进行动区, 而它们是**近 24h 已经发生过的事**, 混在一列"此刻状态"里, 设了几个点位提醒的用户行动区就被历史记录占满。**最优解是重排不是加横幅**: 横幅要把同样的内容说两遍, 而重排一分不多花; 且把**行动区与持仓体检整块一起上移**, 保住原设计「两者都是持仓管理, 连着看不用来回滚」那条约束 —— 不是把行动区单独插到中间。重排用脚本做并**逐行 Counter 校验**(内容完全一致, 只是顺序变了, 132 行)。`severity` 扩成四档 `fatal > high > mid > low`: 跌破生命线独占 fatal(界面上标的名也跟着变红, 必须一眼跳出来), 监控触发降为 low 并**限 3 条 + 溢出如实报数**(「还有 N 条未列出 —— 去告警页看完整记录」)。**HTML 导出件同步重排** —— 它常被打印出来照着做, 顺序错了后果和屏幕上一样; 两处顺序必须一致, 否则等于维护两套优先级。**为什么不能指望 AI 导读兜底**(已在文件头注释写明): 它确实收到了行动区内容、也被提示词要求点名最需处理的 1-2 件事, 但它是**手动点击才生成**的 —— 没生成时页面顶部就是市场状态, 规则层的行动区必须自己站在显眼位置。 | 改 `api/today.py`(`SEVERITY_RANK`/`_MAX_ALERT_ACTIONS`/生命线档位/监控降级限流)、`pages/Today.tsx`(版面重排 + 四档配色)、`lib/todayHtmlExport.ts`(同步重排)、`lib/api.ts`(`severity` 四档); 新增 `tests/test_today_action_severity.py` | 中(**打开今日总览看到的版面顺序变了** —— 这是本次主要意图; 后端仅改档位与限流, 判定逻辑一条没动) | 版面回退: `git revert` 或把 Today.tsx 的两块移回原位; 档位回退: `SEVERITY_RANK` 改回 `{"high":0,"mid":1}` 并把生命线那条的 severity 改回 high |
| R178 | **决策台默认按「该动了」排, 并补上买点侧缺的距离数**(用户: 「分析我整个个股分析页面…尤其是我个股很多, 必须抓住重点和买卖点机会」→「根据最优解改进」)。**先分析出的三条**: ①决策台默认排序是 `confidence` 降序 —— 置信度是"AI 有多确定"不是"这只有多急", 一只 AI 95% 确信「观望」的票会压在刚跌破止损线的票上面; 而且**拿 AI 决定用户先看谁, 与本项目别处立的规矩矛盾**(台账「只记不反馈」、R175「AI 只念表」), 15 个排序键每个都是单一维度, 没有一个回答"今天该动谁"。②**一个不对称**: 出场线一直有 `distance_pct`(于是卖点能排序、能盘中推送), 而趋势翻转价**只有价位没有距离** —— 买点侧因此一个紧迫度数字都没有, 这才是"抓不住买卖点"的结构原因, 不是信号不准。③15 列全是"今天的读数", 一只票在上涨趋势里待了 30 天和今天刚翻进去长得一模一样。**做法**: 补 `flip_down/up_distance_pct`(口径与 `position_exit` 完全一致的 `(线−现价)/现价`, 两处摆在一起比必须同口径); 新增纯规则服务 `watchlist_urgency`, 五档**首个命中即定档** —— 已触发(出场线破了) > 逼近(离出场线≤1.5% 或离翻转价≤2%) > 刚变盘(`duration==1`) > 到轨(短通道贴/破轨) > 无事; 决策台默认排序改为它, 同档内按"离触发还有多远"升序(与「止盈线」列既有的按距离排语义一致)。**关于③, 找到了比落快照更好的办法**: `_trend_payload` 里本来就有 `duration` 与 `entered_from` —— **`duration==1` 就是"今天刚翻转"**, 不用存昨日快照, 也就没有"补不了历史"的问题(我上一轮跟用户说要落快照, 那个说法作废)。**两条刻意的取舍**: 到轨口径**直接 import `focus_list._BAND_POS`** 不另立阈值(CONTEXT.md 写死: 决策台写"贴上轨"的那天推送门也必须认, 否则用户会看到"界面说贴轨了却没推送"), 有测试遍历该集合钉住; **判定函数签名里没有 AI 信号**, 并写了测试用 `inspect` 钉住参数集合 —— AI 照旧显示在自己那列, 只是不再决定顺序。另加「只看要动的」开关(带计数, 与「只看持有」并列)、行首档位徽标(悬停给后端原话的判定理由 —— 只给标签不说凭什么, 就跟 AI 随口说一句没区别)。**顺带修的两处自己引入/暴露的问题**: 新增一列后 `colSpan={15}` 失效改 16; HTML 导出原来只导「有结论」的票, 加了「该动」列之后这个条件就漏了 —— 一只已跌破出场线但三档通道都在中部的票没有结论, 却正是最该出现在导出件里的那只, 改成"结论 ∪ 要动的"取并集。 | 改 `services/livermore_service.py`(距离字段 + `_distance_pct`)、`api/stock_analysis.py`(`/urgency` 端点)、`lib/api.ts`、`lib/queryKeys.ts`、`components/stock-analysis/WatchlistDecisionBoard.tsx`、`decision-board/cells.tsx`(`UrgencyCell`)、`lib/decisionBoardHtmlExport.ts`; 新增 `services/watchlist_urgency.py`、`tests/test_watchlist_urgency.py` | 中(**默认排序变了** —— 打开决策台看到的顺序与以前不同, 这是本次的主要意图; 判定与端点均为新增, 任一路数据取不到只降级不报错) | 恢复旧顺序: 把 `useState` 默认值改回 `{key:'confidence',dir:'desc'}`(一行); 整条回退再删「该动」列与 `/urgency` 端点; 距离字段可留(纯新增, 无副作用) |
| R177 | **个股复盘两个 tab 都加「回头看」小结, 并把统计单位从「天」改成「段」**(用户: 「这两部分也要有类似回头看的总结功能」, 指 R48 复盘弹窗的趋势状态/通道结论两个 tab)。**做的时候发现一个统计错误, 顺带修掉**: 原来的 `_outcomes` 是**按天**聚合的 —— 一段持续 8 天的「上涨趋势」会被算成 8 个样本, 可这 8 天各自的"之后 5 日"互相共享 4 天, 根本不独立。n 被重叠窗口撑大, 让一个很薄的结论显得挺扎实, 恰恰是这套复盘最该避免的事(它自己的注释还写着"样本小得很, 不折算成胜率去装得像统计结论")。所以新加 `_episodes` 把连续同值的行压成段、**每段只在起点取一次前瞻收益**, 两个 tab 统一走它: ①**趋势状态 tab 从无到有** —— 那栏原来只有「涨停出现在什么状态下」, 回答的是另一个问题; 现在补上"每种六态之后普遍怎么走"(`_trend_outcomes`)。②**通道结论 tab 从按天改按段**, 数字会变小, 但那个更小的数字才是真的。`_agg_episodes` 的一个刻意取舍: 末尾不足前瞻期的段**仍计次数**(它确实发生过)但不计收益, 分成 `n`/`scored` 两个数 —— 把它从次数里也抹掉会答错"这只票出现过几次"这个最基本的问题。另外按段还顺带改善了尾部丢数据: 按天时末尾 5 天全丢, 按段只有"最后一段的起点落在末尾 5 天内"才丢。前端两个 tab 共用一个 `OutcomeChips`, 显示"N 次 + 平均涨跌", 悬停给全文(几段、平均持续几天、几段已知结果、为什么单位是段); 一段都没兑现的档显示「待定」而不是编一个数。**没有在这里加 AI 提炼**: 单只票半年内同一档常只有 3~5 段, 这个量级上 AI 说什么都是在给噪声编故事 —— 与 R175 台账那边的定位不同, 那边是完整候选池跨多日, 样本量撑得起。**改了 3 个既有测试**: 它们断言的正是被刻意改掉的按天语义(如 `n == 33`), 逐个改写成按段并保留原意图; 其中一条"末尾必须有没兑现的段"的前提在按段下不再成立(这正是改动的好处), 改成只验不变量, 机制本身由新测试单独覆盖。 | 改 `services/review_service.py`(`_episodes`/`_agg_episodes`/`_trend_outcomes`, `_outcomes` 改按段)、`lib/api.ts`、`components/stock-analysis/StockReviewDialog.tsx`、`tests/test_review_service.py`(3 处); 新增 `tests/test_review_episodes.py` | 中(改了既有统计口径 —— 界面上同一档的数字会明显变小, 这是修正不是回归; `avg_fwd` 可能为 null, 前端已处理) | 恢复按天: `_outcomes` 回退到 git 历史版本并删 `trend_outcomes` 一行; 但不建议 —— 按天那个 n 是假的 |
| R176 | **删掉策略页的「策略体检」**(用户: 「策略体检删除掉, 我不要了」)。这是 R174 清理时**特意留下**的那个 —— 当时用户说的是"因子和回测两页我加的全删", 而策略体检长在**策略页**上, 用的两个 AI 端点(`/strategy/health-interpret`、`/strategy/diagnose`)虽然写在 `api/backtest.py` 里、看着像回测页的, 实际只被策略页的 `StrategyHealthDialog` 调用, 所以当时按范围留了下来。现在用户明确不要, 整条链一次删干净: 前端组件 `StrategyHealthDialog.tsx`、策略页的按钮与弹窗挂载、`showHealth` state、只此一处用的 `Activity` 图标 import; 后端 `api/backtest.py` 尾部整个「策略体检 · AI 解读」区块(两个请求模型 + 两个端点, 1237 行到文件尾全是它); `api.ts` 的 `backtestHealthInterpret` 与 `backtestDiagnose`。删除前确认过引用面: 这两个端点没有别的调用方, 也没有对应测试。至此 fork 加的 AI 填表/解读类功能(R32/R33/R34 + 本条)已全部清空, 只剩「回头看」那条 AI 提炼 —— 那条的定位不同: 它只念代码算好的统计表, 不生成配置也不下判断。 | 删 `components/screener/StrategyHealthDialog.tsx`; 改 `pages/Screener.tsx`、`api/backtest.py`、`lib/api.ts` | 极低(纯删除, 无其他调用方) | `git revert` 本提交 |
| R175 | **台账「回头看」: 不参与打分的那批标签到底灵不灵 + AI 提炼**(用户: 「我就是有时候想回头看结果怎样了, 这个东西正在遵循什么规律, 想 ai 试着提炼」)。**动机**: 界面上有一批"说了句话但一分不参与打分"的注记 —— 通道结论、六态趋势、主线归属、龙虎榜。**恰恰因为不进分数, 它们从来没被验证过**: 用户看着「强势深调」四个字, 不知道该不该信。**三层, 每层的边界是刻意划的**: ①`today.py` 的 ctx 补记 `verdict`(结论 code)/`mainline_rank`/`win_rate`/`dragon` —— 查出来趋势状态(`ctx.state`)本来就在存、**能回溯**, 但**通道结论没存**(只有 `channel_pct`), 意味着这个字段**补不了历史**, 所以先做它, 早一天补就多一天数据; 龙虎榜存 `True or None` 让 ctx 的 None 过滤把未上榜的丢掉(绝大多数行都没上榜, 存一堆 false 白占盘)。②`score_ledger._by_label` 按标签分组, 每档给**全期与最近 20 个记录日两列**, 外加 `_shift_note` 算两者背离 —— **「正在遵循什么规律」落到实处就是这一列**: 全期是长期成色, 最近是当前成色, 背离才是要看见的东西(单看全期, 一年的均值会把最近一个月的转向稀释掉)。`LABEL_DIMS` 是**注册表**, 以后往界面加什么新结论, 只要落进 ctx 就在这登记一行, 自动能回答"我历史上好不好使"。背离判定两边各需 15 个样本、差值小于 10 个百分点当噪声, **样本不足就明说看不出来**。③`pattern_digest` 让 AI 念表: **输入只有已经算完的统计表**(白名单, 不给个股不给价格 —— 看不见就编不出来), 提示词写死"只能引用表里出现过的数字""样本不足的档不许给倾向性判断"; **一天只跑一次并存档**(同一批数据问两次 AI 会给两套说法, 而"今天和昨天说的不一样"会被读成行情变了, 其实只是采样噪声); **AI 说的话连同当时那张表一起存**, 三个月后能回看它准不准 —— 不存的话, 「上个月 AI 说主线在轮动」就又是一句说了不算数的话, 跟用户要解决的问题一模一样; 失败只记 WARNING 返回 None, 不拖垮台账统计。**贯穿三层的一条规矩**: 提炼结果**不参与打分、不进任何提示词**, 延续 score_ledger 与 ai_pick_ledger 那条「只记, 不反馈」。**分工要害**: 分组、胜率、背离全部由代码算; AI 只做措辞, 说错了不影响任何一个数字。前端在 `ScoreLedgerDialog` 加第五栏(不新开页面), 全期/最近并排、背离列上色、样本不足的档压暗但仍列出(要让用户看见"这档还没攒够"); 提炼**只在点按钮时**跑, 打开弹窗显示的是存档。**顺手修**: 该文件里四处 JSX 正文中的 `**加粗**` 会原样显示成星号(其中一处是 R134 时留下的), 改成真的 `<b>`。 | 改 `api/today.py`(ctx 补记 + 两个端点)、`services/score_ledger.py`(`_by_label`/`_shift_note`/`LABEL_DIMS`)、`lib/api.ts`、`components/ScoreLedgerDialog.tsx`; 新增 `services/pattern_digest.py`、`tests/test_score_ledger_labels.py`、`tests/test_pattern_digest.py` | 低(纯新增读路径; ctx 多存四个键, 老记录缺这些键会归进"未标注"档不报错; AI 层失败降级为不显示) | 关掉 AI 提炼: 不点那个按钮即可, 不影响统计表; 整栏回退删 `_by_label` 调用与前端第五栏; ctx 补记可留着(继续攒数据, 不占多少盘) |
| R174 | **因子页与回测页整体还原成作者那份, fork 在这两页加的东西全部删除**(用户: 「因子和回测这两个页面的东西, 完全恢复成作者的样子, 我之前在这两个页面搞的 ai 都不要了」→ 追问边界项时用户澄清「我只是不想要我自己做的, 作者的一切都保留啊」——**范围是"我加的全删", 不是"只删 AI"**, 我第一次问偏了)。**删掉的 fork 功能**: R32 因子 AI 解读、R33 因子 AI 填表、R34 策略 AI 填表、R39 研究工作流(节拍器 + 两个 driver, 让挖掘/回测工作流不依赖页面开着)、AI 自动挖掘会话、R53 挖掘三层关系说明、R54 手动配置默认收起、R55 挖掘运行/会话/工作流的历史删除、以及 **R173 刚注册进因子平台的 5 条把握分因子**(它挂在因子页的因子库/检验里, 同属"我加的")。**做法是按缝整份还原而不是逐处抠**: 8 个前端文件 `git checkout upstream/main --` 回作者版, 3 个 fork 独有组件(`WorkflowPanel`/`MiningAutopilot`/`LevelGuide`)删除; 后端 `api/mining.py` 整份还原(核实过它的 fork 改动**全部**是 autopilot + delete_run + load_run_result, 没有别的东西夹在里面), `strategy/scoring.py` 还原(去掉 R173 的 fk_ 转接点), `api/backtest.py` 只摘掉三个 AI 端点、**保留 `/strategy/health-interpret` 与 `/strategy/diagnose`** —— 那两个是**策略页**的 StrategyHealthDialog 在用, 不在这两页范围内。删除前逐个查过引用面: 三个 fork 组件只被这两页用; 工作流引擎只驱动 `backtest`/`mining` 两种 kind(都在范围内); `backtest_autopilot` 只被 workflow_drivers 与 `/strategy/ai-plan` 用; 前端已无任何页面引用 workflow —— 所以整条链可以干净地摘掉。**唯一没能还原成逐字节一致的一行, 而且它不是 fork 功能**: `StrategyBacktest.tsx` 里 select 选项的渲染。作者那版写的是 `<option key={opt} value={opt}>{opt}</option>`, 假定 options 是字符串数组; 但**你的内置策略**(`strategy/builtin/keltner_reversion.py` 等)吐的是 `{value,label}` 对象, 按作者原样渲染会出 `[object Object]`。那个文件是只读红线, `StrategyParamOption` 这个放宽的类型又被**策略页**的 StrategySettingsDialog 依赖着(改回 `string[]` 会连带弄坏策略页), 所以这一行用 `paramOptionValue`/`paramOptionLabel` 兜住, 并在代码里写明了原因。**顺带清掉的死物**: `api.ts` 15 个指向已删端点的方法与 6 个随之作废的类型、`queryKeys` 两条、9 个 fork 独有的后端服务/端点文件、8 个只覆盖已删功能的测试文件(其中 `test_json_store.py` 是 fork 的但同时覆盖别的东西, 只删掉工作流与自动挖掘那两节)。净变化 **−6506 行 / +71 行**。验证: 两页范围与上游 `git diff` 只剩上面那一行; `api/mining.py`、`strategy/scoring.py` 与上游零差异; 后端 **2729 passed, 2 skipped**(删功能前 2911, 差值即被删功能的测试); 前端 build 退出码 0; `git diff backend/app/strategy/builtin/` 为空; 别的页面零改动。 | 删 9 个后端服务/端点 + 8 个测试 + 3 个前端组件; 还原 8 个前端文件与 `api/mining.py`、`strategy/scoring.py`; 改 `api/backtest.py`、`main.py`、`api.ts`、`queryKeys.ts` | 低(删的全是 fork 独有物, 还原的全部对齐上游 —— 这两页以后同步上游是零冲突) | `git revert` 本提交可整体恢复; 单项恢复从本提交的父提交 `afdf82f` 取对应文件 |
| R173 | **把把握分的维度注册进作者的因子平台, 让它第一次能被独立检验**(用户: 「要啊, 我的增强肯定是要保留的, 但是作者更新一定也要有」; 在选项里选了「把握分去受因子平台检验」)。**动机**: 把握分从 R134 起就有一个悬着的问题 —— **它到底准不准, 只能靠 R133 台账慢慢攒样本**, 而调权重的门槛是"每个分桶 ≥10 个样本且前后半段一致", 那是以月计的等待。上游 v0.2.3 带来的因子平台(IC/IR/胜率/相关性去重)正是回答"这个数有没有预测力"的工具, 于是把维度以**同口径**注册成因子走它的检验管线。**三条纪律**: ①**评分实现一行不改** —— 新模块只 import `opportunity_score` 的曲线常量, 不改它、不包装、不替代, 今日总览算分走的还是原来那条路(已用 `git diff` 自证 `opportunity_score.py`/`today.py`/`score_ledger.py`/`strategy/builtin/` 全为空); ②**口径由代码保证而不是靠我抄得准** —— 曲线直接引进来, 那边哪天调了这边自动跟着变, 结构上不存在"两处数字对不上"; ③**表达不了的就不注册**。**能注册的只有 55% 的权重, 这点必须说清楚**: 把握分六个输入里只有四个是 enriched 的物化列 —— 量能维度(30%)的量比 `vol_ratio_5d` 与换手率 `turnover_rate`、位置维度(25%)的 `close`/`ma20`/`atr_14` 都在; 而**趋势维度(45%)注册不了** —— 新鲜度与六态状态来自 `livermore_service` 的逐日状态机(不是列), 相对强度要减大盘同期收益(因子矩阵里没有指数列)。硬凑一个"近似版"会让检验结果指向一个并不存在的因子, 比不检验更糟, 所以如实不做; 那 45% 仍然只能靠台账。**门槛 G1/G2/G3 也不注册**: 它们是**否决**不是打分, 因子检验的是连续值的单调性/IC, 对布尔否决没有意义 —— 门槛松紧要看台账里"被挡掉的那些后来涨没涨", 是另一件事。**注册了 5 条**(单独成组「把握分(fork)」, 在因子库里与作者的内置因子一眼分开): `fk_vol_ratio_score`/`fk_turnover_score`/`fk_dim_volume`(=前两者按 0.7/0.3 合成)/`fk_keltner_pos`(过曲线之前的**原始位置**, 便于分别看原值与得分的 IC)/`fk_dim_position`。**方向留 `none`**: 按作者的规矩方向以最近一次检验的 IC 符号为准 —— 预填等于先替检验下了结论, 那这次检验就白做了(有测试守着)。**接入点只有两处, 都极小**: `strategy/scoring.py` 加 6 行转接点(只认 `fk_` 前缀, 表达式全在 fork 模块里, 让同步上游时这个文件冲突面尽可能小)、`main.py` 启动时注册一次(失败只记 WARNING —— 因子库少几条是小事, 把启动搞挂是大事)。验证: 新增 **17 个用例**, 其中 `test_piecewise_expr_matches_scalar_version` 对**五条曲线**(含没注册的 FRESH/RS 两条)在拐点上、拐点之间、两端之外逐点比对表达式版与 `opportunity_score._piecewise` 标量版, 差值 <1e-9 —— 这条是整件事的地基, 口径不一致的话检验结果毫无意义; 另有破轨位置不夹(夹了 POS_CURVE 右端"破上轨扣分"那截就失效)、ATR 缺失给 null 不除零、依赖不齐返回 None 不拿 null 拼假值、幂等注册、只挂个股、预热覆盖 MA20 等。后端全量 **2911 passed, 2 skipped**(上轮 2894 + 17)。**怎么用**: 因子页 → 检验 tab → 选「把握分(fork)」组里的因子 → 看 IC/IR/分层胜率; 也可以在组合 tab 里和作者的 77 个内置因子查相关性, 看量能/位置这两维是不是在重复造轮子。 | 新增 `backend/app/services/fork_score_factors.py`、`backend/tests/test_fork_score_factors.py`; 改 `backend/app/strategy/scoring.py`(+6 行转接点)、`backend/app/main.py`(+8 行启动注册) | 低(转接点只认 `fk_` 前缀, 与上游任何 id 都不会撞; 表达式全在 fork 独有文件里) | 删掉 `scoring.py` 那 6 行转接点与 `main.py` 那段注册即可完全复原(fork 模块留着也不影响任何东西); 或 `git revert` 本提交 |
| R172 | **同步上游 `4cb30e4..8ef3d66`(63 提交, 167 文件, +19082/−3354 —— 上游 v0.2.3 整块发布)**(用户: 「评估设计最优方案再合并」)。**先评估后动手**: 合并前用 `git merge-tree` 空跑出 25 处冲突, 逐个比对两边改了什么, 分成四类才开始解 —— **A 纯追加**(两边往相邻位置各加各的, 但每处都核实过"不是上游删了 fork 的东西")、**B 结构性移动**(2 处, 最危险)、**C 导航路由三方交汇**(3 处)、**D 我上一轮刚动过的**(2 处)。**上游这批的实质**: ①新增**因子平台**(`/factors` 一级路由: 因子库/编辑器/组合/检验四个 tab, 因子↔策略双向联动, 可由因子直接生成策略); ②信号库从设置里提为独立页 `/signals`, 并新增盘中信号(分钟K特征)、扩展表字段接入信号与因子; ③新增**自定义 JSON webhook 与邮件**两条推送通道(社区 PR #244 + 维护者补丁); ④一批**峰值内存**修复(fuyao 流式同步历史日K、enriched 全量重建流式化 #208/#174、regime 批次增量聚合、CSV 编码转换分块); ⑤**Docker 启动卡死**修复(uv 锁过期触发运行时重装 + 运行时无镜像源); ⑥盘后管道默认 15:35、定时复盘 15:40(**会改变既有定时行为**); ⑦回测行点击弹K线、分时轮询停表、AI 策略草稿分区与发布闸、复盘推送门控等。**B 类两处是这次最容易静默丢东西的地方**: 上游**删除了 `pages/Mining.tsx`**(挖掘并入 `/factors?tab=mining`, `/mining` 变重定向)——**文件被删时 git 不报冲突, fork 对它的改动只会没了**, 所以 R53 的三层关系说明(工作流/自动挖掘/挖掘配置)被单独抽成 `components/backtest/LevelGuide.tsx` 并接进上游的 mining tab(抽成独立文件而不是塞进上游的 `Factors.tsx`, 免得以后每次同步都在那儿解冲突); 另一处 `settings/CustomSignals.tsx → pages/Signals.tsx` 是重命名+重写, fork 那 1 行排版调整所在的 hero 区块已被整段重写, **无内容可保留**, 如实取上游。**推送渠道取并集**是这次的主线: fork 有钉钉, 上游加了 custom/email —— 任取一边都会少渠道, 于是 `PUSH_CHANNELS`/`monitor_rules` 白名单/`quote_service` 分发/`settings` 测试端点/`api.ts`/`Monitoring.tsx`/`RuleEditor.tsx`/`PriceAlertDialog.tsx`/`Review.tsx`/`LimitUpLadder.tsx` 十处统一成五条并存(上游还把 `REVIEW_PUSH_CHANNELS` 改名为 `PUSH_CHANNELS` 并换掉了全部引用, 跟随作者, 老名字删掉免得留一个永不读取的死常量)。**关于 R160 的一个好消息**: 事前担心新通道漏掉焦点门控, 查下来**不用改** —— `focus_muted` 那段 `muted += 1; continue` 坐落在所有渠道分发**之前**, 被静音的事件根本走不到分发, 新通道自动继承; 只需保住 `muted` 计数与那行日志(没有它就看不出"今天有多少条被焦点名单挡下", 而那是判断门槛松紧的唯一依据)。**其余需要改写而非拼接的**: `ai_provider` 保留 R56 多档兜底循环并把上游新加的 `prefer_final_answer` 穿进循环内那次调用(顺手删掉 fork 一个从未被调用的死代码 `_iter`); `kline.py` 把 fork 的三级取值(自选叠加层 → 全市场缓存 → R77 单票缓存)改写成上游新的 `dict | None` 契约并接住 `refresh_asset`——**只取上游那版的话免费自选档就再也拿不到当日实时行**; 导航三件套(`Layout`/`router`/`MenuSettings`)按"上游顺序为骨架 + fork 独有条目插回原位 + 两边都有的只留一条"重新编排, 避免 `/lots`、`/regime`、`/abnormal`、`/review`、`/stock-analysis` 出现两次, `/lots` 仍挂 fork 的 `PositionsHub` 且沿用 R170 的「仓位中心」称谓, `/mining` 导航项删掉(页面已不存在)但路由保留重定向。**两处有意取作者版**: `rps_rotation` 概念/行业映射缓存(同一个 bug #186 两边各修一遍, 且 fork 那版在合并后的上下文里直接 NameError)、`api.ts` 的 `ApiError` 类(上游把 R80 那个临时做法正式化了, 已核实 `(err as Error & {status?}).status` 与 `err?.status` 两种读法都兼容)。**踩到并记下的坑**: 有三处冲突是**两边各写了一个结构相同的组件, git 只把不同的几行标成冲突, 中间相同的行算公共行** —— 逐块 both 会把两个组件的碎片穿插起来, 编译都过不了。`Review.tsx` 的按钮用"我方走一遍 + 对方走一遍(公共行各走一次)"重建; `Monitoring.tsx` 的三个渠道面板改为**直接从 HEAD 与 upstream/main 各取整块面板**拼装(按行拼接会把钉钉面板的收尾标签丢在最后一个冲突块之后的公共区)。另有 `LimitUpLadder` 的 ext 助手: R50 已把它们抽进 `lib/ladderExtFields.ts`, 上游那份仍在页内 —— 已核实**上游这批没改过它们**, 删掉页内重复定义保留 fork 的 import。**上游的新测试还替我抓出一个真问题**: R77 兜底直接点 `qs.get_single_live`, 而上游新增的 `test_kline_daily_latest` 用的替身没有这个方法 —— 改成 `getattr` 防御式取用(兜底本身不该成为新的崩溃点)。验证: 后端 **2894 passed, 2 skipped**; 前端 build 退出码 0; `git diff backend/app/strategy/builtin/` 为空; **AI 分析链 13 个红线文件逐个核对零改动**(`today.py`/`opportunity_score.py`/`score_ledger.py`/`action_timing.py`/`focus_list.py`/`today_annotations.py`/`effective_positions.py`/`paper_plan.py`/`paper_trader.py`/`paper_trader_run.py`/`Today.tsx`/`PaperTrading.tsx`/`PositionsHub.tsx`); 合并相对 fork HEAD 的删除行里带 fork 标记的共 7 行, **逐行核对去向**(5 行是措辞改写或搬家后仍在, 2 行是上面说明的有意取作者版)。基线锚点前移 `4cb30e4 → 8ef3d66`。 | 合并提交; fork 侧 25 处冲突解析 + 新增 `frontend/src/components/backtest/LevelGuide.tsx` | — | `git revert -m 1 <merge>` |
| R171 | **AI 操盘手升级: 买入即立交易计划**(用户: 「我觉得我的 ai 操盘手太落后老土了, 借鉴我的批次的功能『记录一笔买入后, 系统会按成本价 ± 止盈/止损% 生成价格监控; 填了到期日则自动生成到期提醒』做出升级优化」)。**升级前的病根**: 模型每天重新自由决定买卖, 唯一机械的只有 [R61] 生命线强平; 它下单时不必说自己打算持有多久、赚多少走、亏多少认 —— 于是复盘时**根本没法回答「它的计划靠不靠谱」**, 赚了说是眼光, 亏了说是运气。**批次那句「只用于生成提醒, 不是持仓记账」在模拟盘里落不了地**, 因为那边是真钱由人拍板, 这边没有人, 「提醒发给谁」得先定。用户选了**止损/到期硬执行, 止盈只提醒**。①**新增纯函数模块 `services/paper_plan.py`**: `derive_plan(成本, 止盈%, 止损%, 持有天数, 买入日)` → 三条线, **算法与批次页同口径**(成本 ×(1±x%), 到期按自然日); `check_plan(持仓, 收盘价, 今天)` → 触了哪条线, 优先级 **止损 > 到期 > 止盈**(同一天既跌破止损又到期记成止损 —— 那才是这笔亏在哪的真实原因, 记成到期会让出场分布失真); `plan_line()` 把三条线写成人话给模型看。**越界参数夹到边界而不是丢掉**: 模型偶尔写「止损 0.5%」(一个跳就打掉)或「止盈 500%」(等于没设), 丢掉的话这笔就完全没有计划, 反而退回升级前。②**接进账本**: `apply_order` 收 `plan`/`exit_reason`; **加仓按新的加权成本重算三条线**(成本变了还挂旧线, 那条线就不再是"成本±x%"), 但**到期日锚在第一次买入那天**(否则每加一次仓就把期限往后推, 期限就成了空话); 卖出记下成本与已实现盈亏(不记的话出场归因只能数条数, 回答不了"止损那几笔平均亏多少")。③**纪律检查 `check_plans`**: 与生命线那一路成对, 同样不问 AI, 但**归因分开记**(生命线是系统定的纪律, 计划是模型自己立的, 复盘要能分清); 全程收盘口径(拿盘中价比会在同一天反复触发又回来); T+1 挡住当天买当天卖时留一条拒单痕, 明天再触发。定时任务里排在生命线之后(与出场线体系的优先级一致)。④**提示词与上下文**: `SYSTEM_PROMPT` 要求每笔买入必须给 `target_pct`/`stop_pct`/`hold_days` 并说明为什么是这三个数; `build_context` 把持仓的三条线与"离线还差多少"摆出来 —— 不摆的话模型看不见自己的承诺, 第二天又凭感觉重来; 止盈提醒落在账上并写进下一轮上下文(止盈不硬执行, 不让它看见就等于没提醒)。⑤**产出: 出场归因**。`exit_stats()` 给出 止盈/主动卖/到期/止损/生命线 五类分布 + 赚钱出场占比 + 在仓计划覆盖率。**这才是逼模型先立计划真正的收益**: 止盈占比高 = 目标定得够得着, 止损/生命线占比高 = 买入那一刻常判断错, 到期占比高 = 老是拿着不动等时间到。前端新增 `components/paper/PlanCells.tsx`(持仓表「计划(损/盈/期)」列 + 成交行出场归因标 + 账户卡出场分布条 + 止盈提醒条)与「过计划」按钮(与「查生命线」成对)。**兼容性是当作硬要求测的**: 升级前建的仓没有 `plan` 字段, 跌到地板也不会被这一路碰(`test_planless_position_is_untouched`), 模型没给参数就是这一笔没立计划、退回原行为。验证: 新增 `test_paper_plan.py` **19 个**(纯算法, 含夹边界、bool 不当数字、优先级)+ `test_paper_plan_flow.py` **17 个**(接账本后的行为, 含加仓重算/到期日不后推/止盈绝不代劳/无价跳过/T+1 拒单/归因统计); 后端全量 **2640 passed, 2 skipped**(上轮 2604 + 36), 既有 111 个 paper 测试全绿; 前端 build 退出码 0; `git diff backend/app/strategy/builtin/` 为空。 | 新增 `backend/app/services/paper_plan.py`、`backend/tests/test_paper_plan.py`、`backend/tests/test_paper_plan_flow.py`、`frontend/src/components/paper/PlanCells.tsx`; 改 `backend/app/services/paper_trader.py`、`paper_trader_run.py`、`paper_trader_schedule.py`、`backend/app/api/paper_trading.py`、`frontend/src/pages/PaperTrading.tsx`、`frontend/src/lib/api.ts` | 低(AI 操盘手整套是 fork 独有的 R59 体系, 上游没有对应文件) | 计划与纪律可整体停用: `check_plans` 不调用即可(账上的 `plan` 字段只是多余数据, 不影响任何既有逻辑); 或 `git revert` 本提交 |
| R170 | **把 [R59]「AI 操盘手」并进「持仓提醒」页, 合成「仓位中心」双 tab**(用户: 「ai操盘手页面也想办法融合到持仓提醒页面」; 选项里选了「单页双 tab + 对照标记」)。**先说清为什么不是揉成一张表**: 一边是**真钱**(每笔买入的成本/数量/止盈止损/到期), 一边是**模拟盘**(模型拿虚拟资金跑, 目的是体检这套系统给的信息够不够模型做决定)。在看盘系统里把真仓假仓混排, 迟早某个早上会把 AI 的模拟持仓当成自己的仓 —— 所以两侧内容**永不同表**, AI 那侧常驻一条琥珀色「模拟盘 · 非真实资金, 与我的批次互不相干」横幅。①**外壳单独成文件** `pages/PositionsHub.tsx`(fork 独有): tab 条 + 按 `?tab=` 切换, 两个 tab **各自挂载/卸载而不是 CSS 隐藏**(模拟盘那侧有轮询与定时配置, 藏着不卸载等于在批次页后台一直跑)。**上游的 `Lots.tsx` 只加了一个 `embedded` 开关**(嵌入时不画自己的页头), tab 逻辑一行都不写进去 —— 否则每次同步上游都要在这块解冲突。②**路由与菜单**: `/lots` → `PositionsHub`; **旧路径 `/paper-trading` 保留并重定向**到 `/lots?tab=paper`, 书签不断; `MenuSettings` 那条**故意不改 id**(用户已保存的菜单排序/显隐以 id 为键, 换 id 会让那份配置对不上), 只把 label 标成「AI 操盘手 (仓位中心)」; 导航栏两项并成一项「仓位中心」。顺带**补上 R166 记过的那个上游疏漏**: `/lots` 没进 `CORE_ROUTE_PATHS` —— 当时按「不发明新行为」保持与上游一致只做记录, 现在它托管 fork 的仓位中心, 被前端扩展影子覆盖会直接打不开, 有了具体理由才补。③**对照标记**: 新增 `paper_trader.holdings_overlap()` 与 `GET /api/paper-trading/holdings-overlap`, 「我的批次」每行显示「AI n」= 有几个操作员的模拟盘也持有这只。**这里有一条刻意的自我限制**: 作者在 `paper_trader.py` 开头写明界面**不做「所有人持仓一览」**, 因为看完所有操作员的持仓再去调提示词, 操作员之间的隔离就没了, 而隔离正是这个实验的全部价值。所以接口**只回计数, 不回操作员身份/成本/理由** —— 能回答"AI 那边也看上这只了吗", 又不泄露任何一本账; 想知道具体是谁怎么做的仍要切到 AI tab 点进某个操作员, 那道门槛是作者留的, 不该由一个小标绕过去。标记做得很轻(灰底小字)且**不参与任何排序与打分**, 免得日子久了被当成选股推荐。同一操作员两本账都持有只算一次(问的是"几个人"不是"几本账")。验证: 新增 `tests/test_paper_holdings_overlap.py` **7 个用例**, 其中 `test_leaks_nothing_but_counts` 直接断言返回值里不出现操作员 id 与成本 —— 把上面那条约束**钉成回归测试**, 以后谁想顺手加字段会被它拦下; 另有一条坏数据用例**当场揪出实现漏洞**(`books` 不是 dict 时会抛 AttributeError), 已修成跳过。后端全量 **2604 passed, 2 skipped**(上一轮 2597 + 7); 前端 build 退出码 0; `git diff backend/app/strategy/builtin/` 为空。前端本轮**未新增或修改任何动效**(tab 切换属高频键鼠操作, 按 `emil-design-eng` 的频次规则只给 `transition-colors`, 不加入场动画)。 | 新增 `frontend/src/pages/PositionsHub.tsx`、`frontend/src/components/lots/AiHoldMark.tsx`、`backend/tests/test_paper_holdings_overlap.py`; 改 `backend/app/services/paper_trader.py`、`backend/app/api/paper_trading.py`、`frontend/src/pages/Lots.tsx`(上游文件, 仅 embedded 开关 + 对照标记 3 处)、`frontend/src/pages/PaperTrading.tsx`、`frontend/src/router.tsx`、`frontend/src/components/Layout.tsx`、`frontend/src/pages/settings/MenuSettings.tsx`、`frontend/src/lib/api.ts`、`frontend/src/lib/queryKeys.ts` | 中(`router.tsx` / `Layout.tsx` 是上游常改的两个文件, 这次动了导航项与路由表, 同步上游时这两处大概率要手工合; `Lots.tsx` 只碰了 2 行, 冲突面小) | 三块可分别回退: 双 tab(`router.tsx` 把 `/lots` 改回 `<Lots />`、`/paper-trading` 改回 `<PaperTrading />`, `Layout.tsx` 恢复两个导航项)/ 对照标记(删 `AiHoldMark` 引用与那个 query)/ 后端聚合接口(纯新增, 不删也无副作用); 或 `git revert` 本提交 |
| R169 | **把上游「持仓提醒」(批次)与 fork 决策台的「仓位」列打通**(用户: 「作者加了一个仓位体系页面, 个股分析页面也有我做的仓位那一列, 你看怎么整合起来」; 选项里选了「派生 + UI 打通」)。**先查清两边到底是什么关系, 结论是它们不是重复功能, 是同一件事的两个口径**: 上游 `strategy/lots.py` 是**批次级**(一只票可有多笔买入, 每笔带 `cost_price`/`qty`/`buy_date`/止盈止损/到期日, 派生 `{id}_p`/`{id}_d` 两条监控规则), 作者在模块开头写明「只生成监控规则, 不做会计」, 会计属交易口径(上游 issue #230); fork `services/positions.py` 是**标的级**(一只票一条 `held`/`cost`/`weight`), 供决策台浮盈、今日总览的组合总仓位与净值回撤、焦点名单 held 档、出场线(生命线)。**真正重合的只有成本价一个字段** —— 用户在批次页填一次, 在决策台又填一次, 加仓后批次两条而决策台一个数还要自己算加权平均。所以整合形态定为**单向派生**, 不做双向同步(标的级的一个成本改不回多个批次, 反向在语义上不成立)。①**新增 `services/effective_positions.py`**(深模块: 对外只有一个 `load_all()`, 合并规则全在实现里)。规则: **手填永远压过派生**(决策台敲进去的 `cost`/`held` 不被批次覆盖, 派生值另存 `lot_cost` 并算出 `cost_drift_pct` 交给 UI 提示); 批次只填空(标了持有没填成本 → 用**数量加权平均**补上, `cost_source='lots'`); 只在批次页登记过的票也算持仓(否则那边记了这边看不见); 明确标了空仓就是空仓(卖出后忘删批次是常态)但 `lot_count` 如实报出; `weight` 只能手填(批次不知道总资金)。另给 `next_remind_date`(最近的**未过期**到期日 —— 过期的不报, 上游引擎自己会处理)。批次读盘失败**降级为纯手填而不是抛**, 因为决策台/出场线/今日总览三条链都靠它。②**读侧改接 5 处**: `api/watchlist.py` 的 `GET /positions`、`services/position_exit.py`(2 处)、`jobs/daily_pipeline.py`、`api/today.py`。**`today.py` 只换了那一行 import**, 评分/门槛/注记/姿态一字未动; holdings 里另加 3 个**纯展示字段**(`cost_source`/`lot_count`/`lot_remind_date`), 不参与任何判定。**写侧一行未改** —— 仍只走 `positions.set_position()` 落 `positions.json`, 批次文件由上游端点独占。③**UI 打通**: 决策台成本框改为只装**手填值**, 批次派生的走 placeholder 显示「批 1150」并给蓝边, 一眼分得清"我填的"和"批次算的"; 新增 `decision-board/LotsLink.tsx` —— 有批次时给跳转(`/lots?symbol=`), 手填与批次均价偏离 ≥1% 时把偏离标出来(多半是加减仓只更新了一边), 标了空仓但批次未清时给琥珀色提示(那两条监控规则还在跑); `pages/Lots.tsx` 支持 `?symbol=` 落地定位(滚到该票批次并描边, 用 `matchMedia` 显式尊重减少动效 —— `index.css` 那条只压 CSS `scroll-behavior`, 压不住 JS 传的 `behavior:'smooth'`); 今日总览持仓体检新增 `today/LotBadge.tsx`(成本派生标 + 14 天内的到期倒数, 3 天内转警示色)。④**修掉一个自己会引入的 bug**: 决策台原来回写时传 `cost: r.cost`, 而 `r.cost` 现在可能是派生值 —— 直接回写等于把"批次算的"固化成"我填的", 之后改批次就不跟着动了。改为一律传 `manualCost`(仅 `cost_source==='manual'` 时才是数值)。验证: 新增 `tests/test_effective_positions.py` **15 个用例**(含"无批次时与原 positions 逐字段一致"这条回归护栏、大小写归一、qty=0 退简单平均、混合 qty 只用填了的、脏数据 cost_price<=0 不参与均价、读盘炸了不抛、写仍只落 positions.json); 后端全量 **2597 passed, 2 skipped**(基线 2582 + 15); 前端 build 退出码 0; `git diff backend/app/strategy/` 为空。 | 新增 `backend/app/services/effective_positions.py`、`backend/tests/test_effective_positions.py`、`frontend/src/components/stock-analysis/decision-board/LotsLink.tsx`、`frontend/src/components/today/LotBadge.tsx`; 改 `backend/app/api/watchlist.py`、`backend/app/api/today.py`(1 行 import + 3 个展示字段)、`backend/app/services/position_exit.py`、`backend/app/jobs/daily_pipeline.py`、`frontend/src/lib/api.ts`、`frontend/src/components/stock-analysis/WatchlistDecisionBoard.tsx`、`frontend/src/pages/Lots.tsx`、`frontend/src/pages/Today.tsx` | 中(上游若改 `lots.py` 的字段名, 合并层的 `cost_price`/`qty`/`remind_date` 读取要跟着改 —— 但只集中在 `effective_positions.py` 一个文件里, 且批次读失败会自动降级为纯手填, 不会连累决策台) | 把 5 处 `effective_positions` 改回 `positions` 即完全复原(合并层是纯新增文件, 删不删都不影响); 或 `git revert` 本提交 |
| R168 | **装入第二套技能(emilkowalski/skills 取 2 个)并用它做了一轮前端动效升级**(用户: 「把必装写进仓库，以后任何 ai 改代码都要用这三套技能，完成后里面使用必装的两个升级一次项目」)。①**技能落库**: `.claude/skills/` 新增 `emil-design-eng`(设计工程主技能)与 `review-animations`(动效评审, 附 `STANDARDS.md`), 来源 commit `d23d7f8`, MIT; 原 `LICENSE` 改名 `LICENSE.mattpocock`, 新增 `LICENSE.emilkowalski`(两个上游都要保留版权声明)。**只取这 2 个**的理由逐条写在 `.claude/skills/README.md`: 上游另 10 个里 `animate-expo`(RN)/`write-swift`(Swift)/`ask-sonner`(本仓库用作者自带 Toast)不适用, `apple-design` 面向手势驱动的消费级界面(弹簧/惯性套到密集看盘台只会拖慢), `prototype` **与 mattpocock 那套重名会冲突**且定位不同, `animate` 与主技能重叠, `animation-vocabulary` 是给人看的词表。已核对两个来源之间无其他重名、无 `../` 跨目录引用。`AGENTS.md` 写入「三套技能」对照表 + 8 条前端动效硬规则, 并加为 Fork 硬约束第 9 条。**同时记下一条实测限制**: `review-animations` 的 frontmatter 是 `disable-model-invocation: true`, agent 调它会被直接拒绝且禁止复刻其流程 —— 所以 agent 的义务改成「收尾时提醒用户自己敲 `/review-animations`」, 不许假装评审过。②**用 `emil-design-eng` 做的升级**(先量后改, 不是照着规则空改): **a.** 全项目 105 处 `transition-all` → 新增的 `transition-ui`。`all` 会把 height/width/padding/margin 这些**布局属性**一起过渡, 每帧重排重绘; 但直接换 `transition-colors` 会改掉现有观感(阴影/透明度/位移就不动了), 所以 `transition-ui` 列出「现状实际用到的全部非布局属性」—— **视觉是 no-op, 唯一差别就是布局属性不再参与过渡**。**b.** 先扫出**真心想动布局的 2 处**并单独保留: `ExtDataStatCard` 字段区展开写成 `transition-[height]`、`AbnormalMoves` 接近度进度条写成 `transition-[width]`(没改 scaleX: 只有 80px 宽且 `rounded-full` 会被横向缩放拉扁)。**c.** `tailwind.config.ts` 开 `future.hoverOnlyWhenSupported` —— 全部 `hover:`/`group-hover:` 自动包进 `@media (hover: hover) and (pointer: fine)`, 修掉「平板上点一行 hover 高亮就粘住」; 桌面端零变化。**d.** `main.tsx` 套 `<MotionConfig reducedMotion="user">` —— `index.css` 那条 `prefers-reduced-motion` 全局兜底**管不到 framer-motion**(它是 JS 逐帧改行内 style, 媒体查询碰不到), 115 处 motion 动效此前对开了「减少动态效果」的用户完全无效; 放 main.tsx 不增首屏体积(framer-motion 本就在入口 chunk, 见 R153)。**e.** Dashboard 与 ActiveJobCard 两条进度条从动 `width` 改成完整 `transform: scaleX()` 字符串(不用 framer-motion 的 `scaleX` 简写 —— 简写走 rAF 在主线程跑, 而进度条恰恰只在主线程最忙时出现), 时长 0.4s → 0.25s; 内层是直角矩形、圆角由外层裁出, 故视觉无差别。**刻意没改的**: `Monitor` 新告警那 1.2s 闪烁(超了 300ms 上限, 但它是"新告警到达"的状态提示, 在看盘场景里这点显眼是功能而非装饰)、`Onboarding`/首次欢迎弹窗的 0.4~0.5s(技能的频次表明确写了 rare/first-time 可以慢一点)、以及 9 个文件里 46 处 framer-motion 动 `height: 0 ↔ auto`(每处都要单独验证收起展开的观感, 属于逐个改的活, 留作后续)。**验证看的是构建产物不是源码**: `pnpm build` 退出码 0; 产物 CSS 里 `transition-property:all` **归零**, `.transition-ui` 属性列表正确, `@media (hover: hover) and (pointer: fine)` 已生成。后端零改动。 | `.claude/skills/`(新增 2 个技能 + 改 README + 拆 LICENSE 为两份)、`AGENTS.md`、`frontend/tailwind.config.ts`、`frontend/src/main.tsx`、`frontend/src/pages/Dashboard.tsx`、`frontend/src/components/data/ActiveJobCard.tsx`、`frontend/src/components/ext-data/ExtDataStatCard.tsx`、`frontend/src/pages/AbnormalMoves.tsx`, 以及 44 个文件的 `transition-all → transition-ui` 机械替换 | 低(技能与 `AGENTS.md` 是 fork 独有; `transition-ui` 是新增工具类, 上游若同文件改样式仍会正常冲突提示, 但语义不冲突) | `git revert` 本提交; 或单独回退某一项(四项互相独立: 类名替换 / tailwind 两个开关 / MotionConfig / 两条进度条) |
| R167 | **前端两个巨型文件按缝拆开**(用户: 「用之前安装的那套技能重构项目」, 并在选项里选了「前端拆分」)。按 R165 装入的 `/codebase-design` 的语汇来划: 缝按**对外暴露什么**定, 每个模块只导出 1~2 个组件, 其余全是内部实现 —— 而不是按行数硬切。①`pages/Today.tsx` **1922 → 774 行**, 拆出 6 个模块: `lib/todayHtmlExport.ts`(185, 纯函数不碰 React, 所以进 lib 不进 components)、`components/today/OpportunityTable.tsx`(456, 对外只有 `OpportunityTable`/`GateFunnel`, 把握分格/通道位置格/盘中格/注记胶囊/出手结论/展开详情全是内部)、`MarketStatusCard.tsx`(231)、`AiPickPanel.tsx`(174)、`AiAskDialog.tsx`(74)、`VerdictTag.tsx`(32, 机会区详情与持仓体检共用, 故单独成模块)。②`components/stock-analysis/WatchlistDecisionBoard.tsx` **933 → 747 行**, 拆出 `lib/decisionBoardHtmlExport.ts`(94, 与 todayHtmlExport 同一模式)与 `decision-board/cells.tsx`(94, 两个只读单元格连同各自的配色表 —— 配色表是实现细节, 不该摆在主文件顶部让人当成全局约定)。**这是纯搬运, 且证明过**: 写了校验脚本, 把每个新模块去掉我加的 import 头之后与原文对应行区间**逐字节比对**, 9 块全部 `一致`; 并核对了原文每一行的归宿 —— Today 只有 20 行未搬(正是被各模块 import 头取代的旧 import 块), 决策台 13 行(同理)。一行逻辑没改, 一个行为没动。**没做的部分与原因**: `WatchlistDecisionBoard` 剩下的 703 行主组件体没有再拆 —— 把表格行抽成组件需要往下传约 15 个 prop, 那是一次**接口设计**而不是搬运, 技能里 `/resolving-merge-conflicts` 与 `/codebase-design` 都强调不在搬运里夹带设计决策, 留待单独一次带评审的改动。同理后端 `_build_overview`(520 行单函数)本轮**未动** —— 它属于用户划过红线的 AI 分析链, 已单独问过并按用户选择跳过。验证: 前端 tsc 干净 + build 通过(看真实退出码); 后端零改动。 | 前端新增 `lib/todayHtmlExport.ts`、`lib/decisionBoardHtmlExport.ts`、`components/today/*`(5 个)、`components/stock-analysis/decision-board/cells.tsx`; 改写 `pages/Today.tsx`、`components/stock-analysis/WatchlistDecisionBoard.tsx` | 低(全是 fork 独有文件, 上游没有同名文件, 零合并代价; 且为纯搬运) | 两个原文件已存快照, 逐字节可还原; 或 `git revert` 本提交 |
| R166 | **同步上游 `de5d5a9..4cb30e4`(9 提交, 38 文件, +2459/−132)**。用户要求用 R165 装入的技能来做, 因此走 `/resolving-merge-conflicts` 的五步(看现场 → **找每处改动的出处与意图** → 逐块解 → 跑项目自己的检查 → 收尾)。冲突仅 3 处, 且**三处都是"两边往同一位置各自追加"**, 没有一处是谁删了谁 —— 但这个判断是查过才下的, 不是看形状猜的: ①`strategy/monitor.py::_rule_state_signature` 是"规则状态要不要重置"的判定键, 上游 `adc7ab5`(持仓提醒)往里加了 `remind_date`/`lead_days`, fork `16e1c41`(轮询放量)加了 `metric`/`threshold_volume`/`threshold_amount`/`basic_filter`。**先核实了 fork 那四个字段不是被上游删掉的**(上游 monitor.py 1471~1528 仍在用它们, 只是没进这个 tuple), 确认属于纯追加, 于是两边都留 —— 少任何一边, 那类规则改了参数就不会被认出"变了", 状态不重置。②`pages/Monitor.tsx` 上游给筛选联合类型加了 `'date'`, fork 加了 `focusOnly` 状态(R160), 都留。③`components/Layout.tsx` 上游加了「持仓提醒」导航项, fork 有「AI 操盘手」, 都留(上游那条排前, 与作者的菜单次序一致)。**顺带查出一个上游疏漏但按规矩没动**: 上游给 `/lots` 加了路由却没把它加进 `router.tsx` 的 `CORE_ROUTE_PATHS`(该集合用于给前端扩展保留核心路径), 已核实**上游自己那份也缺**, 不是合并弄丢的 —— 技能明确要求"不发明新行为", 且 fork 原则以作者为尊, 故保持与上游一致, 仅在此记录。**上游这批的实质内容**: 新增「持仓提醒」页(`/lots` + `strategy/lots.py` + `api/lots.py`, 登记买入批次并自动派生止盈止损与到期两条监控规则, 监控引擎新增 `date` 规则类型), 两批 issue 修复(#225/#226/#201/#188/#200/#196 与 P1 的 #224/#232/#223/#215), 筛选器 JIT 透传换手率(#187) + **PullScheduler 线程安全**(#203), ETF 矩阵回测不再因缺换手率字段失败, 以及把 #201/#224 的回归测试加强到可复现级别。验证: 后端 **2582 passed, 2 skipped**(含上游新增的 `test_lots.py`/`test_date_rule.py` 等), 前端 tsc 干净、build 通过(均看真实退出码); 合并对 fork HEAD 的删除行中**没有一行带 fork 标记**; AI/评分那一整套(`today.py`/`opportunity_score.py`/`score_ledger.py`/`action_timing.py`/`focus_list.py`/`today_annotations.py`/`Today.tsx`)逐个核对零差异; `git diff backend/app/strategy/builtin/` 为空。基线锚点前移 `de5d5a9 → 4cb30e4`。 | 合并提交; fork 侧 3 处冲突解析 | — | `git revert -m 1 <merge>` |
| R165 | **装入 mattpocock/skills 并把工作方式写进仓库**(用户: 「以后改动都要用上这个项目, 写进仓库里面记住 https://github.com/mattpocock/skills」)。该仓库是 Matt Pocock 的 agent skills 集(MIT), 作者给了两条安装路: Claude Code 插件(托管只读、自动更新)与抄进仓库(自己拥有、可改)。**选后者**, 因为用户要的是"写进仓库里面记住" —— 抄进去之后任何一台机器、任何一个会话打开就有, 不依赖各自装没装插件; 代价是不自动跟进上游, 已在 `.claude/skills/README.md` 写明更新方法(对照 `plugin.json` 逐个 diff)。**取哪些**: 严格按作者 `.claude-plugin/plugin.json` 声明的 **25 个**(engineering 18 + productivity 7) —— 那就是作者自己认的稳定集; 不取 `deprecated`(已弃用)、`in-progress`(作者标注未完成)、`misc`(`migrate-to-shoehorn`/`scaffold-exercises` 是 TS 课程专用, `setup-pre-commit` 是 Husky+lint-staged, 与本仓库 pnpm+pytest 的流程不符)。**布局**: 作者按 `skills/<category>/<name>/` 分类, 这里**扁平**成 `.claude/skills/<name>/` 与 frontmatter 的 `name` 对齐(已核对 25 个目录名与 name 全部一致, 且这些 SKILL.md 之间**没有跨目录相对引用**, 扁平化不打断任何链接)。MIT LICENSE 按许可要求一并留存。**跑了 `setup-matt-pocock-skills` 该产出的东西**: `docs/agents/issue-tracker.md` 选**本地 markdown**(本 fork 硬约束是永不开 PR、单人协作, GitHub Issues 那套在这里没有对象; 权威记录仍是 FORK_NOTES 的 R 行, `.scratch/` 只承载跨会话中间产物)、`docs/agents/triage-labels.md`(五个标准角色, 落地方式是写进 issue 文件的 `Status:` 行)、`docs/agents/domain.md`(单上下文, 照模板)。**新增根目录 `CONTEXT.md`** —— 技能层动手前都会先读它, 而本项目术语密度极高(收盘口径 vs 盘中口径、六态、生命线、Keltner 三档五档措辞、把握分三门槛三维度、注记·不计分、出手时机、姿态、焦点名单、出场线、台账), 用词错了讨论就会错; 只写会话中已验证过的口径, 并把几个踩过的坑写成规矩(如「别另立百分比阈值」「缺维度不给 0」)。**「记住」落在 `AGENTS.md`**: 按该技能自己的规则(`CLAUDE.md`/`AGENTS.md` 谁在就改谁, 不另建)追加两节 —— `## Agent skills`(场景→技能对照表 + 三份 docs/agents 指引)与 `## Fork 硬约束`(永不开 PR / 中文 / 策略因子只读 / 每改一条 R 行 / deploy 脱敏 / 只用 TickFlow / 同步上游三原则 / 验证看真实退出码)。追加段落前后加了显式注释标出"上游原文到此为止", 上游再改 AGENTS.md 时冲突只会落在上方原文, 本段整体保留即可。 | 新增 `.claude/skills/`(25 技能 + README + LICENSE)、`CONTEXT.md`、`docs/agents/{issue-tracker,triage-labels,domain}.md`; 追加 `AGENTS.md` | 低(纯文档与技能文件, 不含任何可执行改动; `AGENTS.md` 是上游文件但改动是纯追加) | 删 `.claude/skills/`、`CONTEXT.md`、`docs/agents/`, 并还原 `AGENTS.md` 的追加段落 |
| R164 | **同步上游 `576850d..de5d5a9`(28 提交, 46 文件, +2899/−686)**。原则不变: 以作者为尊, fork 增强一个不丢, AI 分析系统不动。冲突 11 个文件, 逐个处置: ①`Layout.tsx`(5 处) —— **作者的侧栏三态(展开/rail/隐藏 + 移动端抽屉)为骨架**, fork 的「盘面参考」分组(R67)、`PlainNavLink`、紧凑间距嫁接回去, `navCollapsed` 全部换成作者的 `railMode`; fork 里 R102/R148/R154 的状态卡与指数卡还用旧名, 给了别名 `navCollapsed = railMode`。②`StockPreviewDialog.tsx`(3 处) —— 作者的 `navList/onNavigate` 方向键切股与 fork R100 的最近查看/弹窗内切换**并存**: 签名合并, 父级 onNavigate 更新 symbolProp 后 fork 的 useEffect 会清掉内部 override, 两套不打架; 弹窗宽度取作者的 1200px。③`settings.py` + `api.ts` + `Monitoring.tsx` —— 作者新加了「Webhook 发测试消息」(200 + detail, 飞书单次尝试), 而 fork 早就有一版(400 抛错, 且多一个钉钉渠道): **取作者的结构与返回, 保留 fork 的钉钉渠道**(Literal 三值, 多一个 dingtalk 分支; 前端钉钉测试改走作者的 `sendTestWebhook`, 读 detail 弹 toast)。④`Watchlist.tsx`(5 处) —— 作者用统一的批量导入弹窗(截图/CSV/粘贴)取代了 fork 的 OCR 截图入口, **取作者的**, OCR 可用性检查随之下线(弹窗内自理); fork 的 AI 一键分组(状态 + 两个 mutation + 按钮)保留。⑤`Monitor.tsx` —— 两边各加了东西(fork 的焦点栏 / 作者的切股导航), 都留。⑥`ConceptAnalysis` / `IndustryAnalysis` —— 作者在页头加了「RPS 轮动」按钮, 但 fork R105 早已把 RPS 入口**收成复盘页全站唯一入口**(用户定的), 这里**维持 fork 的决定**, 去掉作者那两处未用的声明(不然 tsc 报未使用)。⑦`Screener.tsx` —— 作者新加「批量加自选」菜单(`WatchlistAddMenu` + `useWatchlistBatchAdd`), 用它的 JSX 落在 fork 重写过的工具栏里没接上, **手工移植**到列表配置按钮旁。⑧`LimitUpLadder` —— 作者加了当前预览卡高亮 + 切股导航单源排序, 取作者的, 只把 `duration-200/150` 换回 fork 的 `duration-expand/hover` 令牌(全站统一动效时长, R-系列早定的)。删除行扫描: 合并对 fork HEAD 删掉的行里**没有一行带 fork 标记**。上游这批的实质变化: 个股弹窗方向键切股/预取/外链、自选批量导入 CSV、Webhook 测试消息、策略可读指数/ETF 日K、stocksdk 实时归一化修复、侧栏三态 + 移动端抽屉、信息条弹窗性能。验证: 后端 **2537 passed, 2 skipped**; 前端 tsc 干净, build 通过; `git diff backend/app/strategy/` 为空; AI/评分那一整套文件零差异。基线锚点前移 `576850d → de5d5a9`。 | 合并提交; fork 侧 11 个文件冲突解析 + `Screener.tsx` 移植 | — | `git revert -m 1 <merge>` |
| R163 | **暗色回到作者原版**(用户: 「还是用回以前作者的黑吧」)。R126→R162 在暗色上试过四版(Modern Dark / 分层重调 / Radix Slate / 近黑底), 用户最终选作者的。`html.dark` 块 12 个值 = upstream/main 同名块逐个由 HSL/hex **精确换算**成 OKLCH(#0A0A0B / #18181B / #212126 / #353539 / #FAFAFA / #C4C4CB / #8E8E96 / #3B82F6 / #F04438 / #12B76A / #F79009), 视觉与作者一致; 图表调色板 `theme.ts` 的 DARK 也还原成作者的 Zinc 值。R155 的 `--accent-text` 变量保留(tailwind 的 textColor 覆盖还在), 值与 `--accent` 同(作者本来就是一个电光蓝两用)。**亮色不动**, 仍是 R155 的 Radix Slate。作者口径的对比度核对: muted/surface 5.45, accent 4.82, 红涨 4.72, 绿跌 6.76, 都过 4.5。这一块从此不再由 `gen-radix-theme.cjs` 产出(脚本仍管亮色), CSS 注释里写明了别用脚本覆盖。 | 前端 `src/index.css`(仅 `html.dark` 块)、`src/lib/theme.ts`(DARK 块) | 低(只换暗色 token 值; 与上游同块结构一致, 以后作者再改这一块可直接照抄) | 无需回退 —— 它就是上游的值 |
| R162 | **暗色"灰得无力"**(用户: 「主题是灰色有种无力感」)。R155 那版底取 Radix slateDark1(`#111113`), 卡片 5 档、边框 8 档 —— 整屏是灰底、灰卡、灰边, 深浅拉不开, 而 Radix 最深那档本身就偏灰不是黑。**有力感来自深与对比, 不是彩度**(加彩度只会抢红绿涨跌的戏)。改两处: ①底压到 L 0.135 的近黑 `#06080d`, 带一点靛蓝冷调(C 0.012) —— 这是暗色里**唯一不取自 Radix 档位的值**, 原因写在注释里; ②卡片/悬浮/边框改取更低的 4/6/7 档(`#272a2d` / `#363a3f` / `#43484e`), 卡片不再是一大块中灰。分层预算反而更好: `bg-surface/40` 叠底 ΔL 0.059(R141 底线 0.05), `border-border/40` 0.106, 悬浮比卡片高 0.063。文字/强调/涨跌按新 surface 重新反解(muted 5.20:1, accent-text 4.70:1, 红涨 4.70:1, 绿跌 5.22:1), 全部仍由 `gen-radix-theme.cjs` 生成。图表信息条底色跟着卡片档位走。亮色零改动。 | 前端 `src/index.css`(仅 `html.dark` 块)、`src/lib/theme.ts`(一行)、`scripts/gen-radix-theme.cjs` | 低(只换暗色 token 值) | 还原 `html.dark` 块的 base/surface/elevated/border 四行 |
| R161 | **短期贴/破上下轨的票进焦点名单**(用户: 「我经常关注 Keltner 短期处于上轨和下轨状态的票」)。焦点名单加第三档「贴轨」: 短期 Keltner 档位在 贴上轨 / 破上轨 / 贴下轨 / 破下轨 的自选 —— 就是高抛低吸候选。**到轨判定复用 `indicators/keltner.classify` 的五档口径**(有用例钉住), 本模块不另立百分比阈值: 决策台「短通道」列写"贴上轨"的那天, 名单里也必须是贴轨, 两处不许各说各话。数据来自今日总览本来就算好的 `bands_map`(持有 + 全部有趋势的自选, 收盘口径), 零新增 IO。优先级 持有 > 计划中 > 贴轨 > 观察, 已在高档的不被覆盖; 贴轨档过推送门(弹窗 / 系统通知 / 外部 / 徽标都推)。理由写成"短期贴上轨 92%"这种能一眼核对的话。监控中心「推送焦点」栏加第三列。 | 后端 `services/focus_list.py`、`api/today.py`(传 bands_map)、`tests/test_focus_list.py`; 前端 `components/monitor/FocusBar.tsx`、`lib/api.ts` | 低(快照多一档; 判定口径复用既有常量) | 从 `PUSH_TIERS` 拿掉 `TIER_BAND` 即回到 R160 行为 |
| R160 | **焦点名单管住所有打扰通道 + 默认开**(用户: 「比如我开了推送跌破生命线, 就会一堆推送。类似这样的问题, 我需要真的聚焦」)。R159 只拦了外部渠道 —— 但"一堆推送"的感受来自**所有会打断人的通道**: 弹窗、提示音、系统通知、侧栏徽标、外部 IM。改法是**盖一次章、四处认章**: 监控评估处对每条告警算一次"这只票在不在焦点名单"(`focus_muted`), 章盖在事件上再落盘, 于是 alerts.jsonl / SSE / 系统通知 / Webhook 读的是同一个结论, 不会出现"弹窗被拦了、企微却推了"这种各说各话(inspect 用例钉住"章在落盘之前盖")。四处的行为: 弹窗与提示音跳过盖章的; 系统通知跳过; Webhook 只认章(没盖到章的异常路径才现算); 侧栏徽标改用 `/api/alerts?focus=1` 只数焦点内的(total 按过滤后算); 监控中心触发记录**默认只看焦点内**, 一个 chip 切「含焦点外」后灰显显示被静音的那些 —— 记录一条不丢, 只是不再打扰。老记录没章 → 算焦点内(不能因为旧数据没字段就把它藏起来)。**默认开**: R159 默认关是怕推送行为悄悄变, 现在用户明确说"我需要真的聚焦", 那就开 —— 名单失效或任何异常时门自动放行、关掉开关即回到全推, 这两条保护不变。开关文案从「只推送焦点名单」改成「只认焦点名单」, 因为它管的已不只是推送。测试 +2。 | 后端 `services/quote_service.py`(盖章 + 系统通知/Webhook 认章)、`api/alerts.py`(focus 参数)、`services/preferences.py`(默认值); 前端 `components/AlertToast.tsx`、`components/Layout.tsx`(徽标)、`pages/Monitor.tsx`(默认只看焦点 + 灰显)、`components/monitor/FocusBar.tsx`、`lib/api.ts`; `tests/test_focus_list.py` | 低(章为 bool 附加字段; 默认值可一键关) | 关掉开关即回全推; 彻底移除则删盖章那段与四处 `focus_muted` 判断 |
| R159 | **推送焦点名单**(用户: 「我自选个股太多了, 不知道哪些才是我真的要推送通知的」)。150 只自选, 广域规则(全市场 / 自选分组 / 板块)一命中就推, 推送成了噪音。**做法不是让用户逐只勾**(150 只没人勾得动), 而是让系统按**已有的决策产出自动分档**, 用户只在例外处动手: ①**持有** —— positions 标了持有的, 任何时候都推; ②**计划中** —— 今日总览「值得关注」里过了门槛、分数达标显示出来的那几只(今天/收盘打算动手的); ③**观察** —— 其余自选, 只记应用内触发记录, 不打外部渠道。分档来自每次构建今日总览时落的一份快照(`user_data/focus_snapshot.json`, 盘后管道也会跑一次, 所以每天自动刷新; 内容不变不写盘); 用户可对任意一只**钉住**(永远推)或**静音**(永远不推), 存 `focus_overrides.json`。**推送门只拦广域规则**: 用户给某只票单独设的规则(scope=symbols, 如点位提醒)是明确的"我关心这只", 永远放行 —— 门插在 `quote_service._maybe_send_webhook` 里"规则没勾渠道就跳过"之后、真正投递之前(有 inspect 用例钉住位置), 应用内 alerts.jsonl 与 SSE 不受影响。**总开关 `push_focus_only` 默认关** —— 推送行为不能悄悄变, 用户看过名单觉得对了再打开。**失败一律放行**: 快照读不到 / 超过 7 天没刷新(管道停了) / 任何异常 → 门打开, 宁可多推, 不能因为一个辅助名单把出场线告警吞掉(每种情况一个用例)。轮询线程里每条告警都要过门, 快照与覆盖按 mtime 缓存, 不重复读盘。监控中心顶部加一条「推送焦点」栏: 三档计数 + 总开关 + 展开名单(持有/计划中并列, 观察默认收起), 每行可钉住/静音; 名单过期时黄字提示"放行中"。新增 `/api/focus`(GET 名单 / PUT override / PUT prefs)。测试 +13。 | 后端新增 `services/focus_list.py`、`api/focus.py`、`tests/test_focus_list.py`; `api/today.py`(落快照)、`services/quote_service.py`(推送门)、`services/preferences.py`(开关)、`main.py`(注册路由); 前端新增 `components/monitor/FocusBar.tsx`, `pages/Monitor.tsx`、`lib/api.ts`、`lib/queryKeys.ts` | 低(默认关; 门失败即放行; quote_service 只加一个 if) | 关掉开关即回到原行为; 彻底移除则删 quote_service 那段 `focus_list.should_push` 与 today.py 的 `save_snapshot` 调用 |
| R158 | **「出手」列: 今天动手 / 收盘再动 / 不动手**(用户: 「距触发有点不够明白, 可以再加一列, 今天能不能动手还是收盘后动手或者不动手, 你帮我加定义好这一套东西」)。**与评分的分工不变**(R134): 把握分说的是「这票值不值得进候选」, 冻在收盘口径; 出手时机说的是「今天这一天能不能下手」, 允许看盘中价, 因为它回答的正是盘中的问题 —— 所以它是**注记**: 一分不进评分, 不改名次, 只多一列。规则在新模块 `services/action_timing.py`(纯函数, 可回测), **阈值全部对齐 opportunity_score 的曲线常数, 不另立口径**: 今天动手的天数上限 3 = FRESH_CURVE 的拐点(第 3 天 72 分, 第 4 天 46); 贴上轨 0.85 = POS_CURVE 掉到 50 分的位置; 距触发 2% = 逼近突破的既有阈值; 追高 5% 高出关键点; 当日涨幅到**板幅 × 70%**不追(主板 7%, 双创 14%, 北交所 21%, 用 `board_limit_pct`)。**三个结论**: ①今天动手 —— 六态 UT 已确认、≤3 天、高出关键点 <5%、没贴上轨、盘中没跌回关键点下方、大盘不在防守档; 谨慎/观察档加一句「轻仓试」。②收盘再动 —— 方向对但还差一个确认: 盘中临时信号 / **回升途中盘中刚过关键点**(这正是用户问的"收盘后动手"那一档) / 距触发价 2% 以内 / 转多第 4~5 天 / 盘中回落 ≥3% 或跌回关键点下方; 利弗莫尔的规矩是收盘价说话。③不动手 —— 大盘防守 / 盘中跌破生命线 / 当日涨幅到板幅 70% / 已高出关键点 5%+ / 贴上轨 / 转多第 6 天起 / 回升还没突破(附「等站上 X 再看」)。**规则顺序**: 排除项 → 等收盘项 → 今天动手, 同时命中时排除项赢 —— 不动手比等待更保守(有用例钉这条)。缺数据跳过那条规则, 不制造结论也不因缺数据放行。每条结论都带一句理由与围绕的关键点价, 界面胶囊 + 理由一行, 三态三色(强调色/警示色/灰); HTML 导出同步加列。顺带把「距触发」改名「距关键点」, 值从「-2.55%」改成「已过 2.6% / 还差 7.0%」—— 负号要人翻译一次, 直接说人话。测试 +21(每条规则一个, 外加"阈值与评分曲线对齐"的守卫)。 | 后端新增 `services/action_timing.py`、`tests/test_action_timing.py`, `api/today.py`；前端 `pages/Today.tsx`(新增 `ActionCell`)、`lib/api.ts` | 低(纯注记, 新增字段与列) | 删掉 today.py 里那段 `action_timing` 调用与前端 `ActionCell` 列 |
| R157 | **个股分析页: 定位当前个股**(用户: 「加个定位当前个股的功能, 任何适合被选中的都要能当前页面显示, 我不想每次都找半天」)。决策台 150 行, 搜索框选中一只票之后它在哪一行只能靠肉眼扫。两条路径一个函数: ①**自动** —— `currentSymbol` 一变(搜索 / URL `?symbol=` / 上次记忆恢复 / 行内点击)就把那一行 `scrollIntoView` 进视野, 用 `nearest`: 已经看得见的不动, 行内点击时表格不会跳一下; ②**手动** —— 页头「当前 xxx」旁加一个定位按钮, 滚到正中 + 整行闪一下(1.8s), 眼睛一下落到它身上。**三种"不在"分清楚, 不静默**: 数据还没到 → 记成待定位, rows 一渲染出来就补做(所以打开页面就能定位到 URL/上次那只); 不在自选 → 提示「不在自选里, 决策台没有它这一行」(只在手动时提示, 自动时搜索一只非自选的票不该弹); 被「只看持有」挡住 → 自动切回「全部」再定位并提示。行加 `scroll-mt-10` 避开 sticky 表头。表格不是虚拟列表(纯 tbody map), 行 ref 直接可用。**R157b**(用户: 「比如我搜索了, 也要跟着跳转显示到这个个股的位置」): 搜索选中、URL 跳转、恢复上次那只这三条『到达』路径改走**手动那一档**(滚到正中 + 闪烁), 不再只是『进视野』; 『就近』只留给行内点击(点了不该让表跳)。另外搜索后先弹出关键价位弹窗, 闪的那 1.8 秒多半被盖住, 所以当前个股那一行改成**常驻高亮**(底色 10% + 左侧一道靛蓝边), 关掉弹窗一眼认得出它在哪。 | 前端 `pages/StockAnalysis.tsx`、`components/stock-analysis/WatchlistDecisionBoard.tsx` | 低(纯前端交互, 新增 prop 可选) | 去掉 `locateNonce` 与 `locate`/`rowRefs` 那段 |
| R156 | **今日总览提速**(用户: 「今日总览加载显示得好慢」)。前端 R153 的预取只能让 `/api/today`**早开始**, 救不了它**算得慢**; 本地没有数据集无法压测, 但代码上能证明的最大一块: **历史胜率逐只扫盘**。`_build_overview` 对每只候选调 `bullish_win_rate_for_symbol`, 每次一趟 320 日历日的 parquet 扫描(`get_daily` 快路径 = `_scan_daily_symbol`), 候选上限 80 只 = **80 趟扫盘**; 而同一函数里 `trends_for_symbols` 早就示范了正确做法(同窗口同列, 一次 `get_daily_batch` 再按 symbol 分组)。修三处: ①新增 `livermore_service.bullish_win_rates_for_symbols`: 股票一次批量读, ETF/指数逐只回退, 再按 (票, 窗口末日, 阈值, horizon) **记忆** —— 一只票的历史胜率只在新日线落地或阈值改动时才变, 同一天再打开总览连 compute 都省了; 结果与逐只**一字不差**(同窗口、同阈值、同纯函数, 有用例逐票比对)。②**ATR 走快照**: 仓位建议原来每只机会都单独 `get_daily_asset` 扫一趟 30 天盘, 十来只就是十来趟; enriched 最新快照与 Keltner 用的是同一份、已在内存、本来就带 `atr_14/close`, 先从它取, 快照里没有的(ETF/指数)才回退逐只, 口径不变。③**分段计时**: `_Stages` 记录 14 个阶段的毫秒数, 随响应带出(`perf` 字段), 总耗时 ≥1.5s 时再记一条 INFO 日志列出最慢四段 —— 「慢」这种反馈没有分段数字就只能猜, 下次直接开 `/api/today` 看 `perf`。**没动的**: 打分逻辑、候选选材、任何阈值/曲线一个字没碰; `trends_for_symbols`/Keltner/门槛原料本来就是批量, 不动。**诚实标注**: 提速幅度是推的不是量的 —— 80 趟扫盘变 1 趟是可证的结构性收益, 但总览还有别的阶段(中观主线、策略命中缓存、大盘模式的指数扫描)本地量不到; `perf` 就是为这个加的, 部署后哪段还慢一看便知。测试 +9(批量=逐只、只读一趟、记忆生效、ETF 回退、inspect 钉住不许长回逐只循环、perf 字段)。 | 后端 `api/today.py`、`services/livermore_service.py`、新增 `tests/test_today_perf_batch.py` | 低(批量函数是新增; 总览只换调用点, 结果不变) | `_build_overview` 里胜率块与 ATR 块改回逐只; 删 `_Stages` |
| R155 | **换色板: Radix Slate 骨架 + 低饱和 Indigo 强调 + Red/Green 只归涨跌 + Amber 做 warning**(用户指定 Radix Colors, 并要求"结合炒股看盘系统的性质来定")。**先说结合性质定下的两条**: ①「Green/Amber/Red 只做状态」在 A 股面板**不能照搬** —— 红涨绿跌是这个系统里出现最多、最不能歧义的语义, Red/Green 必须先归涨跌; 于是 danger 与红涨同色(两者语境不重叠: 破坏性按钮/错误提示 vs 价格格子, 之前也是这么共用的), warning 用 Amber, 「success」不给绿 —— 绿在这里是"跌"。②**明度阶梯与对比度预算原封不动沿用 R138/R141 用真实界面验证过的数**(亮: base→surface ΔL 0.044、border 0.853、muted 6.9:1; 暗: `bg-surface/40` 叠底 ΔL ≥ 0.05、`border-border/40` ≥ 0.058、muted 5.2:1、accent 4.7:1), 换的是色相与彩度 —— 用户两次抱怨看不清的教训不能因为换色板重来一遍。所以 Radix 的档位**不是顺着 1/2/3 抄的**, 是按预算挑的: 亮色 slate 3/白/4/7/12; 暗色 slateDark 1/5/7/8/12/11 —— surface 取 5 档(整块 ΔL 0.133, /40 后 0.053, 与 R141 一致), border 取 8 档(Radix 定义的"强边框", /40 后 0.124)。骨架色直接取 Radix 色值; 需要对比度保证的文字/强调色取该档色相彩度、明度按目标反解。③**Indigo 低饱和**: 色相取 Radix indigo9 的 267°, 彩度从 0.19 压到 0.13。④**强调色拆两档**: 暗色里"深底上的靛蓝文字"要亮(L 0.69 才 4.7:1)而"靛蓝实底配白字"(全站 64 处 `bg-accent text-white`)要暗(L ≤ 0.57 白字才 4.5:1), 一个 token 兼顾不了 —— Radix 自己也是 9 档实底 / 11 档文字。加 `--accent-text`, tailwind `extend.textColor.accent` 单独指向它, `bg-/border-/ring-accent` 仍走 `--accent`; **组件一个字不用改**。亮色白底上一个值两用(都 5.0:1)。⑤图表调色板(`theme.ts`, 画布不吃 CSS 变量)的灰阶从 Tailwind Zinc 同步换成 Slate 对应档, 画布与 DOM 才是同一套灰。⑥`--t-*`(Tailwind 语义色, 一千多处 text-amber-300 之类)与回测图表里写死的系列色(#ef4444/#22c55e/#3b82f6, 是数据系列色不是交互强调)**不动** —— 不在 Slate/Indigo/状态色的范围内。**关于 tweakcn**: 它输出的是 shadcn 那套变量名(--background/--primary…), 本项目不是 shadcn 结构、自有一套 token, 硬套等于再做一层映射; 用户要的效果(Zinc/Slate 骨架 + 低饱和靛蓝主色)直接用 Radix 色阶生成更准, 所以没引入 tweakcn。新增 `scripts/gen-radix-theme.cjs`(读 `@radix-ui/colors`、转 OKLCH、按目标对比度反解、打印核对表), 值全部由它生成, 注释里标了档位与实测对比度。 | 前端 `src/index.css`、`tailwind.config.ts`、`src/lib/theme.ts`、`package.json`(+`@radix-ui/colors`)、新增 `scripts/gen-radix-theme.cjs` | 低(只换 token 值 + 一个 textColor 覆盖; 上游 tailwind.config/index.css 再改时按作者结构重套) | 还原 `index.css` 两个 token 块、`tailwind.config.ts` 的 textColor、`theme.ts` 两个调色板 |
| R154 | **启动请求分档**(用户: 「你决定, 我只要最优解」)。R153 把"字节怎么到浏览器"做完之后, 剩下的瓶颈在 API 层: Layout 一挂载并发发 10 个 query, 与 `/api/settings`、`/api/today`(几秒钟的主内容)**抢同一批服务端 CPU 与连接**, 而其中大半只是侧栏角落的徽标和小字。新增 `useBootTier()`: 0 = 挂载即刻(settings / 偏好 / 能力矩阵 / 行情状态 / 四指数 —— 首屏必需); 1 = 首帧画完(`requestAnimationFrame`)之后: 数据源健康、AI 档位、纳指、分析菜单、自选分组 —— 下一帧就发, 只是别挤在首帧那一撮里, 不产生可见延迟; 2 = 再过 1.5s: 管道任务轮询、告警计数、自选增强(这条在服务端是全自选的指标批量, 与 `/api/today` 抢的正是同一批 CPU)。**实现只加 `enabled` 条件**: 每个 query 的 key / fn / staleTime / refetchInterval 一个字没动, 晚到的数据照常渲染 —— 界面上唯一的变化是"打开的头 1.5 秒, 数据菜单的转圈和监控徽标的计数还没出来"。react-query 对 `enabled:false` 的 query 不轮询, 所以档位放行前那两个轮询也安静。 | 前端 `components/Layout.tsx` | 低(纯前端, 只加 enabled 条件) | 删掉 `useBootTier` 与各 query 上的 `bootTier >=` 条件 |
| R153 | **浏览器打开提速**(用户: 「往性能方面优化, 让浏览器打开更快, 别搞崩我逻辑和所有功能」)。原则: **只改"字节怎么到浏览器、什么时候开始要"**, 不碰任何业务逻辑、任何 query 的 key/fn/staleTime、任何 API 响应内容。先量后动, 四处: ①**去掉两条渲染阻塞的境外字体外链**(`index.html` 里 rsms.me 的 Inter 与 fonts.googleapis.com 的 JetBrains Mono) —— 这两套字体本来就已经通过 `@fontsource` 打进 `index.css` 本地供给了, 外链是纯冗余; 而 `<link rel="stylesheet">` 是渲染阻塞的, 国内访问这两个域名慢或不通时**首屏要干等到超时才画**, 这是国内用户冷启动最大的一块。等宽字体的 600/700 两档原来靠 Google 那条链接供, 改由本地 `latin-600/700.css` 补齐, 视觉一字不差。②**`/assets` 长缓存**: 新增 `app/static_assets.py::HashedAssets` 替换 `StaticFiles`, 带 hash 的产物一律 `Cache-Control: public, max-age=31536000, immutable`。原来没有缓存头, 浏览器**每次打开**都要对每个文件发一次条件请求(304 也是一次往返), 首屏十几个文件就是十几次往返; 现在二次打开 JS/CSS 一次网络都不走。index.html 仍由 spa_fallback 以 no-store 直出, 发新版不受影响。③**构建期预压缩 + 直出**: `frontend/scripts/compress-dist.mjs`(只用 Node 自带 zlib, 零新依赖, 挂在 `pnpm build` 尾部, Docker 构建自动跑)给文本产物生成同名 `.br`/`.gz`; `HashedAssets` 按 Accept-Encoding 直接发对应文件(br 优先), 不在请求路径上压缩, 零 CPU。实测 51 个文件 **3.58 MB → 0.89 MB(brotli)**。**刻意不加全局 GZipMiddleware**: 一是 SSE 流会被裹进缓冲, 二是上游 bc9461d 刚给分时/日K 批量做了自己的可选 gzip, 全局中间件会和它叠在一起; API 路径因此一个字节没变。④**启动链并行化**: 原来是一条串行链 —— 入口 JS → (执行到 `import()` 才发现要)路由 chunk → 渲染守卫 → 这时才发 `/api/settings` → 通过后渲染 Layout → 这时才发现要 Today chunk → Today 挂载后才发 `/api/today`(这条要好几秒), 每一环都在等上一环。改法: (a) `router.tsx` 从"模块顶层立即建路由"改成 `createAppRouter()` 调用时建, 让 `main.tsx` 能**静态** import 它 —— Vite 因此把路由并进入口 chunk, 少一次串行往返(产物里 `router-*.js` 消失, 入口 603KB→brotli 157KB); 扩展注册表"先 load 再 finalize"的顺序约束原样保留, 只是从"动态 import 排在 await 之后"变成"await 之后才调用", 更直白。(b) `main.tsx` 一启动就 `prefetchQuery` `/api/settings` —— 守卫用的是**同一个 queryKey + queryFn**, 到时直接复用在途请求, 语义一字不差。(c) 落地在 `/` 或 `/today` 时同时预取 `/api/today` 与 Today 的 chunk, staleTime 取 Today.tsx 那条 useQuery 的 60s, 不会造成二次拉取; 登录页不预取。**没做的**: 没动 Layout 启动时那 10 个 query 的任何一个(哪个能砍是业务判断, 不在"别搞崩逻辑"的授权里); 没改全局 staleTime; 没引入 vite-plugin-compression 之类的新依赖。测试 +13(协商/回落/q=0/304/HEAD/字体原样/缓存头, 其中 brotli 一条在无 brotli 库的环境自动 skip)。全量 2443 passed, 2 skipped; tsc 干净, build 通过。 | 前端 `index.html`、`src/index.css`、`src/main.tsx`、`src/router.tsx`、`package.json`、新增 `scripts/compress-dist.mjs`；后端 `app/main.py`、新增 `app/static_assets.py`、`tests/test_static_assets.py` | 中(`index.html`/`main.py`/`main.tsx`/`router.tsx` 是上游文件; 改动都是包裹式, 上游再改这几处时以作者为准重套一遍即可) | `main.py` 那两行改回 `StaticFiles`; `router.tsx` 去掉 `createAppRouter` 包裹 + `main.tsx` 改回动态 import; `index.html` 加回字体外链 |
| R152 | **同步上游 `dab8a2c..576850d`(20 提交, 43 文件, +1657/-178)**。用户定案:「以作者为尊, 但我的增强功能都不能丢, 尤其是 AI 分析系统架构别瞎搞」。**先划边界再动手**: 清点下来 AI/评分那一整套(`api/today.py`、`opportunity_score.py`、`score_ledger.py`、`today_annotations.py`、`today_ai_store.py`、`today_ai_verify.py`、`today_mainline.py`、`factor_ai.py`、`ai_json.py`、`ai_pick_ledger.py`、`external_view.py`、`global_indices.py`、`pages/Today.tsx`)**全是上游根本没有的文件** —— 合并在结构上就碰不到它们, 合完逐个核对确认零差异。上游改动与 fork 改动的文件交集 16 个, 真冲突只有 3 处。**三处冲突的处置**: ①`test_job_stall_and_cancel.py` —— 两边各自往文件尾追加测试(fork R75 的 create 前置自愈 / 作者的手动取消端点契约), **两个都留**, 并把 fork 那段挪到最后, 以后作者再往尾部加就不会再撞。②`test_data_integrity.py` —— 同一个目的(让测试不随运行日期失效)的两种写法, fork 是 monkeypatch 钉死 `today=TODAY`, 作者是按真实日期推算 `snapshot_day`; **取作者的**(且冲突段之后的断言本来就引用 `snapshot_day`, 混用会直接坏)。③`lib/api.ts` —— fork 给 `pipelineJob` 加过 `quiet`(轮询期间不弹错误提示), 作者加的是取消任务的注释, 两者不矛盾, **都留**。**自动合并的 13 个文件也逐个复核过**: 把整个合并对 fork HEAD 的**删除行**全部列出来看, 确认每一行都是作者在改自己的代码(分钟数据集、gzip 压缩、回测两处修复、A 股拉取开关), **没有一行 fork 内容被抹掉**。**上游这批带来的实质变化**(值得知道, 因为会改变既有结果): `max_hold_days` 强制退出**此前是不生效的**(#198) —— 之前设过最长持有天数的回测结果都偏乐观; 因子回测默认区间 3 年→180 天(#202); A 股盘后拉取开关此前失效(#216, 修了两次); 全量分钟数据集开放给插件/自定义源, 健康时策略页与监控注入改读本地分时; 分时/日K 批量响应可选 gzip。验证: 全量 **2431 passed, 1 skipped**(fork 2391 + 上游新增 40), 前端 tsc 干净、build 通过, `git diff backend/app/strategy/` 为空。 | 43 文件(见 `git show` 本次 merge commit); fork 侧仅 3 处冲突解析 | — (合并本身) | `git revert -m 1 <merge>`；或单独回退某文件到合并前版本 |
| R151 | **卡面不再显示延时**(用户定案:「那就都不显示延时, 能实时就行了」)。A 股四张卡与全球卡的「延迟N分」徽标 + 黄点一并去掉, 回到两态: 绿点脉冲=在跳 / 灰点=静止。**后端一行没动** —— `quote_at` / `stale` / R150 的时区补正全部保留, 因为它们**不是为了显示**, 是 R148 那条"盘中按行情自带时刻挑最新的源"的依据; 拆掉它们等于把用户要的"能实时"一起拆了。行情时刻退到 `title`(悬停可见, 零视觉重量), 想重新亮出来只是把徽标那几行加回来, 数据侧不需要任何改动。**背景**: 那个标签本来是 R148 为了让"冻住的数看起来像刚更新的"这件事现形而加的 —— 而它上线后第一次报的 720 分是**假警报**(R150 查明是时区口径, 不是延迟)。选源既然已经从根上解决了不动的问题, 一个既容易误报、又要用户天天扫一眼的标签就不值得留在卡面上。**代价写在注释里**: 万一某天源真的又冻住, 卡面会看起来一切正常 —— 那时靠悬停的行情时刻, 或 `/api/global-indices/debug` 的 `age_s` 判断。 | 前端 `components/Layout.tsx` | 低(纯展示回退, 数据字段一个没删) | 把 `stale` 三态与「延迟N分」那几行加回 `SidebarIndexQuotes` |
| R150 | **修「恒定延迟720分」+ A 股卡也标延时**(用户报 + 「你决定」)。①**720 分是我自己的 bug, 不是真延迟**: 720 分 = 12 小时整, 北京与美东夏令时正好差 12 小时 —— R148 里我把各家行情时刻**一律当北京时间**解析(那是我写死的假设), 那家若发的是当地时间, 算出来就恒定偏 12 小时。**真卡住不长这样**: 它的年龄会随时间连续变大, 不会钉在整数小时上。修法是给 `_Preset` 加 `tz_shifts_h` 候选补正(纳指 `(0, 12, 13)`, 12=EDT 13=EST), `_shift_quote_at` 逐个试。②**这里最要紧的是那条守卫, 不是补正本身**: 补正只有在能把时刻拉回**确实新鲜**(年龄 < `STALE_IN_SESSION_S`)时才采纳 —— 补完还差几小时的说明它什么也没解释, 一律按原样报。没有这条守卫, 这个"修复"会把 R148 好不容易抓出来的**真故障一起抹掉**, 那比原来的 bug 更糟: 界面会重新变得看起来一切正常。极端巧合(真冻在 12 小时前)只能骗过很短一瞬 —— 时间一走原始年龄超过 12 小时, 补正不再落在新鲜区间, 卡片马上变回延迟(有专门用例钉这一幕)。③补正必须发生在**比新鲜度之前**, 否则发当地时间的源会被恒定误判成落后 12 小时、在候选里永远排最后, 等于白改。④`/debug` 补正前后都给(`quote_at_raw_text`/`raw_age_s`/`tz_shift_h`/`age_s`)——「卡片说不延迟了」必须是一句**能复核**的话。⑤**A 股四张卡也标延时**: 原来只有 `cnLive` 一个信号, 而它答的是「**我们**有没有在干活」(交易时段 + 轮询在跑 + 数据来自实时缓存), **不是**「这个数新不新」—— 上游卡住/限流/失败而缓存还留着旧值时, 绿点照样脉冲、数照样不动, 正是境外指数栽过的同一个跟头。改用行情自带的 `timestamp`(毫秒 epoch, 后端本来就有, 零改动), 三态与全球卡完全同构: 绿点脉冲=在跳 / 黄点=盘中卡住+「延迟N分」/ 灰点=静止。**门槛 3 分钟而非 10 分钟** —— 境外指数本就是延迟行情、8 秒轮询只是习惯, A 股这条链是 6 秒一轮的实时链, 拿 10 分钟当门槛等于白加。日线回退那条路没有 `timestamp`, 但那时 `cnLive` 本就是 false, 不会误标。**诚实标注**: 本机出网受限, **没能实测到底是哪一家发的当地时间**(sina/腾讯/TickFlow 都有可能) —— 所以补正做成"候选逐个试 + 只在解释得通时采纳", 而不是给某一家写死时区; 真凶开 `/api/global-indices/debug` 看 `tz_shift_h` 一眼便知。测试 +12。 | 后端 `services/global_indices.py`、新增 `tests/test_global_indices_tz_shift.py`；前端 `components/Layout.tsx` | 低(补正有守卫且默认 `(0.0,)`=不动; A 股侧纯前端展示) | 还原 `_shift_quote_at` 与 `tz_shifts_h`; A 股延时标注还原 `SidebarIndexQuotes` 里 items 那段 |
| R149 | **删掉韩国综合, 纳指首选改走 TickFlow**(用户: 「韩国指数删掉吧, 纳指就用 tickflow 看看能不能解决」)。①韩国整行删除 —— 顺带解掉一个长期死结: SDK 的 `Region` 枚举写死 `Literal["CN","US","HK"]`(generated_model.py), 压根没有韩国, 所以 R119 当初为了兼顾韩国**只能**全走第三方源。韩国一走, 这块只剩美股, 而 `region=US + type=index` 是有定义的, 于是**回到项目本来的「只用 TickFlow」原则**, 把它排成纳指第一候选。老配置里只剩 `kospi` 的用户由 R116 的回落逻辑接住(全被过滤掉 → 回默认), 不会落成一张卡都没有。②**代码不靠猜**: 与其赌 `IXIC.US` / `.IXIC.US` / `NDX.US` 哪个对(赌错只会得到一个静默的空值), `_tickflow_resolve` 去 `get_instruments("US", type="index")` 的清单里按**代码根**找, 找不到再退到**名称**匹配 —— 对方换个写法也能自己跟上; 负结果同样缓存(「这个档位没有美股指数」是稳定事实, 不该每 20 秒再问一遍), 清单 TTL 6 小时。③**三条自我约束**, 写在模块头也钉在测试里: **(a) 不许让现状变差** —— 档位不给/列合约被拒/能列不能取报价/报价回空, 四种失败都静静回落 sina+腾讯, 与 R148 之前逐字一致(四个 parametrize 用例专钉这条), 这是敢把它排第一位的前提; **(b) 不烧配额** —— 只在美股交易时段调用(休市那个数静止, 花配额拿它没意义), 且自带 20 秒最小间隔(前端 8 秒轮询不会变成三次 TickFlow 请求); **(c) 不进能力路由** —— 直接 SDK 只读一次, 不落盘、不走 QuoteService, 保住本模块「挂了也只坏这一张卡」的性质。④结构化厂商接入: `_parse` 分流 dict/文本两条路, TickFlow 只给 `last_price`/`prev_close`(涨跌自己减; `prev_close` 缺或为 0 时只报价、涨跌留空, **不编一个 0%**), `timestamp` 是**毫秒**, 直接喂给 R148 的新鲜度比较 —— 所以规则依旧是「挑最新」而**不是「偏袒 TickFlow」**: 它自己卡住时照样让位给新浪(有专门用例)。⑤`/tickflow-probe` 重写成直奔正题: `nasdaq_resolved`(对上了哪个 symbol)+ `nasdaq_quote` + `nasdaq_quote_age_s`, 一眼看出「这个档位到底给不给」。侧栏 title 补上源与代码, 换没换源看得见。**诚实标注**: 本机没有 TickFlow key 且出网受限, **没能实测用户档位到底给不给美股指数** —— 所以这次做的是「接上并保证接不上也不会更差」, 通不通请开 `/api/global-indices/tickflow-probe` 当场看。测试 +18 新增, 并把原 `test_global_indices.py` 里以 kospi 为样本的 8 处换成纳指(换的只是样本, 断言一条没少)。 | 后端 `services/global_indices.py`、`api/global_indices.py`、新增 `tests/test_global_indices_tickflow.py`、改写 `tests/test_global_indices.py`；前端 `components/Layout.tsx` | 低(独立模块, 不进能力路由/不碰 A 股主链; 新支路失败即回落) | 删掉 PRESETS 里那行 `_Src("tickflow","IXIC")` 即可停用整条支路 |
| R148 | **美股开盘了纳指却不动**(用户报)。**不是刷新的问题** —— 前端 8 秒轮询、服务端 TTL 5 秒, 每次都真的问了上游; 生产容器 `TZ=Asia/Shanghai`(Dockerfile:142), 交易时段判定也是对的(红点没标错)。真因在**挑候选的规则**: 原来是「候选表里第一个能解析出数的就用」, 而一家把上次收盘价一直挂着不动**也算"能解析出数"** —— 于是永远轮不到后面真在跳的那家。更糟的是这种故障**从界面上完全看不出来**: 我们记的 `updated_at` 是**自己抓取的时刻**而不是**行情的时刻**, 所以一个冻住的数永远显示"刚刚更新"。改法是把选择依据从"排名"换成"行情自带的时刻": ①新增 `_quote_ts()` 从行尾往前扫, 认三种写法(腾讯完整版 14 位紧凑戳 / 日期时间挤一格 / 新浪 int_ 的日期时间分两格) —— **不认列号**, 因为各家甚至同一家不同代码的列位都不统一; 一律按**北京时间**显式解析(`ZoneInfo`), 不跟服务器 TZ 走, 换个部署环境不会整体偏几小时。②`pick_candidate()`: **盘中**谁的行情时刻最新用谁, 读不出时刻的(如腾讯 `s_` 精简版不给时间)排在能自证的后面; **休市**时人人静止, 比新鲜度没有意义, 回到候选表排名。③口径/时区万一猜错, `_epoch()` 的 ±范围守卫会把离谱时刻当"读不出" —— 最坏退化成原来的排名规则, 不会挑出更差的。④行返回 `quote_at`/`quote_age_s`/`stale`, 侧栏据此三态显示: 在跳=绿点脉冲 / 休市=灰点 / **盘中卡住=黄点常亮 + 「延迟N分」**(不脉冲, 它没在跳); 该源不给时刻就老实说"无法判断新旧", **不拿抓取时刻冒充行情时刻**。⑤`/debug` 每个候选补 `quote_at_text`/`age_s` 与最终 `picked`, 下次再出这类问题一眼看得出是哪家卡住。⑥纳指候选把腾讯完整版 `usIXIC` 提到精简版 `s_usIXIC` 之前(前者带时刻, 能自证的排前面)。**诚实标注**: 本机出网被代理策略挡住(sina/tencent 均 403 CONNECT), **没能实测到底是哪一家在卡** —— 所以这个修法刻意不去写死"换用某家", 而是让时刻自己决定谁上; 真要确认是谁卡了, 开 `/api/global-indices/debug` 看各候选的 `age_s`。测试 +12(含本 bug 的直接回归: 新浪卡在 5 小时前、腾讯 1 分钟前 → 必须选腾讯; 以及"新浪自己最新时不许被无故换掉", 钉住规则是"挑最新"而不是"躲开新浪")。 | 后端 `services/global_indices.py`、新增 `tests/test_global_indices_freshness.py`；前端 `components/Layout.tsx`、`lib/api.ts` | 低(独立模块, 不进能力路由/不碰 A 股主链) | 还原 `pick_candidate` 与 `_quote_ts`, `get_quotes` 改回逐个候选取第一个 |
| R147 | **AI 导读·优选加「补充说明」弹窗**(用户: 「ai导读优选按钮点击后弹窗我能填点东西带着一起提问, 或者不填也能点击按钮分析」)。要害是**不填也要能一键走完** —— 弹窗打开时焦点直接落在「开始分析」上, 回车即走, 零输入路径一次点击没变长; 想说话就打字, Ctrl/⌘+Enter 也能提交, 另给四个例子(「今天只想看半导体」「重点看量能和回踩」「解释详细一点」「偏保守一些」)点一下就填。**边界钉死在后端**: 这句话只进 user payload, **绝不进 system prompt** —— 也就是它能改语气/详略/侧重, 改不了字段与格式, 更改不了候选池(候选池由规则层门槛给定, AI 只做解读, 这是 R134 定的分工)。system prompt 里新增一节 `[R147] 用户补充说明` 明写这条: 「字段与格式一字不改」「宁缺毋滥」「候选池由规则层给定」。上限 500 字(`TodayAiIn.max_length`), 落盘时连 `note` 一起存, 卡片上回显「问了:…」小标签, 免得看到一份口径不同的解读却想不起来当时问了什么。测试 +3, 专钉这条边界。 | 后端 `api/today.py`、`services/today_ai_store.py`、`tests/test_today_select_payload.py`；前端 `pages/Today.tsx`(新增 `AiAskDialog`)、`lib/api.ts` | 低(新增可选入参, 不传时行为与之前完全一致) | 还原 `AiAskDialog` 与 `TodayAiIn` 两处 |
| R146 | **外部页面每次点开都重抓一次**(用户: 「这部分不应该每次点开都需要抓取分析一次」)。原来只有"抓取结果"层的缓存, **AI 整理那步没有缓存** —— 于是每次进页面都要等一次完整的抓取+大模型往返, 既慢又白烧 token。新增 `external_view_latest.json`「上次成品」层: 按 `sha256(url + \0 + hint)` 归档**整份成品**(含 AI 整理后的正文), 30 分钟内进页面**直接给上次的结果, 一次上游都不打**, 想要新的就手动点刷新(`force=True` 照旧绕过)。成品里存 `fetched_at`, 返回带 `cache_kind`(`latest`/`source`/`fresh`)与 `age_seconds`, 页脚显示「AI 整理于 HH:MM(18 分钟前)」+ 复用徽标 —— **省下的那次请求必须是看得见的**, 否则用户分不清"是缓存"还是"抓挂了"。存档按 `generated_at` 封顶 20 条。前端 `staleTime` 5 分钟→30 分钟并关掉 `refetchOnWindowFocus`(切回标签页不该触发一次 AI 整理)。测试 +9: 往返、按 url+hint 分别归档、空白归一、坏文件容错、20 条封顶、**命中新鲜缓存时 fetch 与 AI 都不许被调用**(用 monkeypatch 让 fetch 一被调用就 assert 失败)、过期落空、`force` 绕过、未来时间戳拒收。 | 后端 `services/external_view.py`、新增 `tests/test_external_view_cache.py`；前端 `pages/ExternalPage.tsx`、`lib/api.ts` | 低(新增一层缓存, 未命中时走原路径) | 删掉 `load_latest`/`save_latest` 与 `build_view` 开头那段 |
| R145 | **连板梯队配「所属同花顺行业」后页面 500**(用户报)。查下来梯队这条路**自己写了一份 ext JOIN**, 而它漏掉了全站通用路径 `_load_ext_value_maps` 里至关重要的两步: ①时序扩展表只取**最新分区**; ②JOIN 前按 symbol **去重**。通用路径的注释里白纸黑字写着这么做的理由 ——「时序扩展表只取最新分区, 避免历史分区把同一 symbol JOIN 放大」—— 而「所属同花顺行业」正是一张按日分区的时序表: 直接查 `ext_*` 视图会拿到一只票 × N 个历史分区那么多行, 左连接之后梯队行数被成倍放大。修法不是给这份实现补两个 if, 而是**删掉它、改用通用路径**(值在 rows 生成后逐行贴, 不再动 DataFrame) —— 少一份实现就少一处会漏掉去重的地方; 顺带白拿通用路径已有的 parquet mtime 缓存与**逐列异常隔离**(扩展列从此不可能再把整页打成 500, 最坏也只是那一列不显示)。测试 +5(含两条 inspect 断言, 钉住"不许再长出第二份手写 ext JOIN")。**诚实标注**: 手上的 `data/backend.log` 混着本地跑测试的输出, 不是用户线上日志, 因此**没能确认这就是那次 500 的真因** —— 但这是该路径上一个可证的缺陷, 且修完之后扩展列已不可能导致 500。 | `api/screener.py`、新增 `tests/test_limit_ladder_ext.py` | 低(删掉重复实现, 改用既有通用函数) | 还原 `limit_ladder` 里那段 |
| R142 | **顶部三行重做版式**(用户: 「这三行重新设计布局, 而且很难看清」)。问题不在配色(那是 R138/R141 修的), 在**版式**: 三行都是跑马灯式的长句, 关键数字全埋在散文里 ——「沪深300 收盘 4611 已跌破年线(200日均线)4699,大环境转坏;自选:在涨势中的自选只剩 40%,今天转弱的(38 只)明显多于转强的(54 只)…」。要从这样一句话里读出"今天能不能出手", 得逐字扫一遍; 而真正要看的就五个数。改法: ①**把数字从散文里拎出来做成统计格**(标签在上、数值加大加粗在中、口径在下), 五格并排: 总仓位基调 / 出手结构 / 自选强弱 / 全市场 / 两市成交额 —— 一眼扫过去是五个数, 不用读句子; 「出手结构」还按过关比例上涨跌色。②**长句降级成可展开的「为什么」** —— 它是解释不是结论, 不该默认占两行。③**「市场天气」与「中观」两张卡合并** —— 大盘姿态与中观资金是同一件事的两个层面, 拆成两张反而要在两处找同一个判断; 主线单独一行(它答的是"钱往哪儿聚", 与那排数不同层)。④**AI 导读按正文排版**: 行宽卡到 `max-w-[80ch]`(整屏宽 1600px+ 的一行中文最难读, 眼睛回行会跑错行)、字号 12→13px、行高 1.8、正文用满对比度(原来 `foreground/90` 是把主角调暗), 生成来源与「数据已更新」提示退到标题行。响应式: 5 格在 lg 一行、sm 三列、窄屏两列。 | 前端 `pages/Today.tsx`(新增 `StatCell`/`MarketStatusCard`, 删掉原两块横幅) | 低(纯版式, 数据字段一个没换) | 还原 `pages/Today.tsx` |
| R141 | **修暗色模式**(用户: 「夜间模式的主题配色不好看, 也有点难看清楚东西」)。R138 修亮色时找出的三个毛病, 暗色**一个没落下** —— 只是深色背景更能藏。实测: ①**面板浮不起来** —— 组件普遍写 `bg-surface/40`, 叠上透明度后与页面底的感知明度差只有 ΔL 0.024, 卡片和背景一个色; 把底压到 0.125、面板抬到 0.255 后, /40 之下是 ΔL 0.052, 整块面板 0.130。②**分隔线几乎没有** —— `border-border/40` 只有 ΔL 0.046; 边框 0.320→0.400 之后是 0.058, 整条线 0.145(暗色界面的结构本来就该由边框扛, 不是靠填充)。③**`text-muted/50` 只有 2.01:1**, 占位的「—」和淡说明基本看不见; muted 0.610→0.665(单独用 5.17:1), 给透明度留余量。**方法论上纠正了一件事**: 面板分层**不能用 WCAG 比值衡量** —— Y = L³, 接近黑时分母几乎全是公式里那个 +0.05 常数, 再大的明度差算出来也就 1.1, 那个数说明不了任何事; 深色底的层次要看**感知明度差 ΔL**(经验上 >0.04 才明确看得出)。我第一版就是拿比值去解暗色, 解出来的层次全是假的。**审美层**: 灰阶加了极轻的冷色调(C 0.005~0.010, 色相 265)。R126 当时按 skill 取纯中性灰(C=0), 好处是让红绿涨跌更跳, 但整屏纯灰显得发死; 这点彩度远低于"看得出是蓝的"的阈值, 既不抢涨跌色, 又让暗色像是设计过的。面板抬高后 accent 与红涨掉到 4.35/4.41, 已按 4.7:1 反解提亮(色相彩度不动): accent 0.6261→0.6456, bull/danger 0.6488→0.6646; 绿跌 6.65、warning 6.39 本来就够, 不动。亮色一侧零改动。 | `src/index.css`(仅 `html.dark` 块) | 中(暗色整体重调; 亮色零变化, 已核对产物两套 token) | `git revert` 本提交, 或只还原 `html.dark` 块 |
| R140 | **板块筛选按钮"点了没反应"**(用户: 「筛选按钮没跟着点击切换还是保持全部是亮着, 下面的结果是没跟随筛选变」)。**不是后端的问题** —— PUT/GET/`_boards()` 归一/`filter_opportunities()` 逐层验过都对(HTTP 往返实测: 存 `['沪主板']` 读回 `['沪主板']`, 过滤也正确)。真因在前端的**状态来源**: 按钮亮不亮完全取决于 `d.prefs.boards`, 而那要等 `PUT /prefs` + `GET /api/today` 两次往返才更新; 偏偏 `/api/today` 很重(Keltner 三档批量、MA120 批量、最多 80 只的历史胜率、龙虎榜), 一次好几秒。那几秒里按钮纹丝不动 = "点了没反应", 于是用户再点一次 —— 而第二次读到的 `boardFilter` 还是旧的空数组, 于是又发了一遍**一模一样**的请求, 屏幕上叠出两个相同的 toast(用户截图里正是两个「只看:沪主板」)。修三处: ①加**乐观值** `boardDraft`, 点下去本地立刻切换, `await q.refetch()` 真的落地之后才撤掉(提前撤会让按钮闪回旧状态); ②失败时撤回乐观值 —— 存不上就该退回服务端真实值, 不留一个假的高亮; ③补**进行态**(「筛选中」+ 转圈), 那几秒不再是沉默。顺带把两个 onClick 合并成一个 `toggleBoard(b \| null)`, 全选与单选走同一条路径, 免得两处各写一份取反逻辑。 | 前端 `pages/Today.tsx` | 低(纯前端交互状态) | 还原 `boardDraft` 相关几处 |
| R139 | **AI 优选面板压成一只一行 + 同分时的次序改成有含义的**。①版式(用户: 「一行一行的显示个股会有点浪费空间」, 随后澄清「下半部分倒是可以一行一个这样好看」—— 即候选表保持原样, 要压的是上面那块): 原来每只优选占三行(名字+核对徽标 / 理由 / 存疑说明)外加一圈边框与内边距, 两只就吃掉 ~180px, 把下面真正要看的候选表挤下屏。现在名字、徽标、理由、存疑提示、对账入口横排成一行, 窄屏才回落换行; 驳回区同样收成一行一条。**信息一项没少, 少的是包装**。②排序: 同把握分时原来按代码字典序 —— 那是个**无意义**的顺序, 而用户会照着名次从上往下看。改成两级有含义的兜底: 先把**数据齐全的排在 partial 前面**(同样 78 分, 三个维度都算出来的那只更可信), 再比**趋势强度**(三维里权重最大, 也最接近"这波起来了没有"), 最后才用代码保证确定性(同一份数据每次刷新顺序不变, 名次不会自己跳)。测试 +2 | 前端 `pages/Today.tsx`;后端 `api/today.py`、`tests/test_opportunity_score.py` | 低 | 还原两处即可 |
| R138 | **修亮色模式**(用户: 「日间模式的主题配色不合理, 太白了好多东西都看不见」)。三层原因叠加, 逐层修: ①**卡片与页面同色** —— `--base` 0.985 与 `--surface` 1.0 只差 1.5% 明度, 面板边界整个消失; 页面底压到 0.962 才让白卡片浮得起来。②**分隔线等于不存在** —— `--border` 0.922 本已很淡, 而组件里普遍写 `border-border/40`, 叠上透明度后与白底只有 1.05:1; 压到 0.855 后 /40 仍有 1.19:1(与 Tailwind 标准浅色边框 `gray-200` 的 1.14 同档), 全站结构才回来。③**最要命的一层: 全站一千多处用的是为深色背景挑的浅色** —— `text-amber-300` 在白底上只有 **1.44:1**, `sky-300` 2.64, `emerald-400` 3.19。逐个文件改不现实(涉及一百多个组件), 改法是**把 Tailwind 的 300~600 四档收进 CSS 变量**, 亮/暗各一套: 组件一个字不用动, 换主题自动切, 而**暗色一侧原样填 Tailwind 值, 视觉零变化**。62 个色值不手调, 由 `scripts/gen-light-palette.cjs` **按目标对比度反解**: 保持色相不变(色相才是语义 —— 红涨绿跌、琥珀提醒), 只压明度; 低明度处高彩度掉出 sRGB 色域再逐步降彩度收回。档位目标递增(300→4.5:1 / 400→5.4 / 500→6.4 / 600→7.4), 否则三档会挤成一个颜色, 色阶就没了。只覆盖 300~600: 50/100/200 本就是该淡的浅底, 700+ 已够暗, 动它们只会把亮色弄糟(已核 `bg-sky-100`/`text-orange-700`/`text-emerald-900` 仍正常生成)。同时把核心 token 一并按同一套数学重解: `fg-muted` 0.556→0.470(比"刚好达标"再暗一档是有意的 —— 组件普遍写 `text-muted/50`, 叠透明度后那个值才是真要读的, 得留余量)、`accent` 5.09:1、红涨 4.94、绿跌 4.90(原绿只有 1.9:1, 报个跌幅都看不清)、warning 4.89。**踩过的坑记一笔**: 第一版生成脚本把 OKLab→线性 sRGB 的结果又套了一次 gamma 解码, 对比度被系统性高估, 解出的颜色全都不够暗 —— 62 个值作废重算。修正后 `lum()` 直接用线性值。 | `src/index.css`、`tailwind.config.ts`、新增 `scripts/gen-light-palette.cjs` | 中(亮色模式整体重调; 暗色模式零变化, 已逐变量核对) | `git revert` 本提交; 或只还原 index.css 的 `:root` 块即回到旧亮色 |
| R137 | **把握分冻在收盘口径, 盘中的变化单独摆一列**(用户: 「看我肯定是看收盘价, 但是盘中我会一直盯着」)。发现的问题: 盘中把握分会**自己动** —— 量比走实时叠加层(盘中覆盖盘后快照)进了评分, 而位置(Keltner)与三道门槛(MA20/MA120)都是昨收, 于是那个分数既不是收盘口径也不是实时口径, 盯着它反而被带偏。改法是把两份数据彻底分开: `vol_map/turn_map` 只取**盘后快照**喂评分(盘中一动不动, 是稳定的决策基准), 实时叠加层单独收成 `live_rows` → `live_view()` → 候选上的 `live` 字段, **一分不进评分**。盘中视图给四样: 现价与涨跌幅、盘中量比、现价落在**昨天那条**通道里的位置(与收盘位置的差 = 今天往哪走), 外加最要紧的 **`below_lifeline` 破生命线红标** —— v2 的生命线是硬门槛, 一只昨天入选的票今天盘中跌回 MA20 之下, 收盘定稿后就会被挡掉, 盯盘的人得**当场**知道, 而不是等收盘发现它凭空消失。口径近似的依据: MA20 与通道边界都是慢变量(一天挪不了多少), 所以「今天的价比昨天的位置」给的是**方向性预警**而非结论 —— 结论仍然等收盘, PRD §7.5 的分工不动。六态的盘中临时判定(R16/R18)保留不变: 那是盘中新冒出来的候选, 照旧带「盘中·待收盘确认」标; 冻的是**数据口径**不是**候选发现**, 两者不冲突。界面: 实时行情开着时表格多一列「盘中」(关着时整列不占版面), 顶部徽标改为「分数收盘口径 · 盘中列实时」, 展开行补一条口径说明。测试 +5 | 新增 `services/today_annotations.live_view`;改 `api/today.py`(盘后/盘中取数分离)、前端 `pages/Today.tsx`(LiveCell)、`lib/api.ts` | 中(改了量比的取数口径 —— 盘中分数从此不再跟着量比动) | 还原 `_build_overview` 里那段取数即可回到「实时覆盖盘后」 |
| R136 | **把握分台账补上第三条记账路径: 数据管道**。原来只有两条 —— 用户在收盘口径下打开今日总览, 或定时导读任务跑起来。两条都靠不住: 没开页面、又没配 AI 的那天就**永久少一天样本**, 而台账是事后统计, 漏掉的补不回来。`run_now` 结尾(数据刚落盘、as_of 必新、必然收盘定稿)走一次 `_build_overview` 落快照; `record_day` 自己按 as_of 去重, 与另外两条路径重复触发是安全的。整段 try/except 只记日志 —— 记账绝不能让一次成功的数据管道被判失败。测试 +2(用 inspect 钉住这个 hook 别再被删掉) | `jobs/daily_pipeline.py`、`tests/test_score_ledger.py` | 低 | 删那一段 try 即可 |
| R135 | **补上 R134 漏掉的两个注记数据源: 策略命中 与 龙虎榜**。R134 把注记层建起来了(主线/AI/历史胜率/通道结论), 但这两项当时**只写了展示分支没接数据源** —— 界面上因此永远不出现, 属于实现漏项不是设计取舍。补法遵守三条纪律: ①**只读不算** —— 策略命中读策略页 `run_all` 已经写好的 `user_data/strategy_cache.json`(`today_ever_matched`: 今日曾命中的 symbol 并集), 龙虎榜读 `dragon_tiger` 的按日缓存; 在总览接口里现算 27 个内置策略 × 全市场历史矩阵是几秒级开销, 为一个**不参与打分**的展示标付这个代价没道理, 取不到就不显示。②**陈旧不显示** —— 策略缓存的 as_of 与总览 as_of 对不上就整个不给(昨天的命中摆在今天的候选旁边是误导, 与 R37 主线的 stale 原则同源); 龙虎榜回退到上一期时在文案里标出是哪天的。③**只算内置策略**(用户: 「不用管我自建的策略, 参与的只用内置策略」) —— 自建/AI 生成的数量与口径都不受控, 混进来会让「命中 5 个策略」失去可比性; research_only 模板同样排除。两条文案上的判断: 策略命中的 tone **恒为中性**(内置策略里均线/突破/量价几类高度重叠, 一只放量突破的票天然同时命中三四个, 那反映的是策略集自身的冗余而不是这只票更强, 给它「命中越多越好」的暗示是错的); 龙虎榜的 tone **按净额正负定**(净卖出上榜不是利好, 只报「在榜」等于把方向抹掉了), 金额按亿/万自适应。测试 +18 | 新增 `services/today_annotations.py`、`tests/test_today_annotations.py`;改 `api/today.py`(`_build_overview` 增加可选 engine 参数, 注记打标) | 低(纯新增注记, 不碰打分) | 删 `today_annotations.py` 与 `_annotations` 里那两个分支 |
| R134 | **AI 优选评分系统 v2: 三道硬门槛 + 三维度加权(取代 R12~R47 累积的"底分+八项加减")**。v1 的三个结构性毛病用户在界面上直接看得见: ①**分数饱和** —— 理论上限 70+15+20+8+8+8+12+10=151 夹到 100, 榜首一片并列 100(用户截图里两只并排满分), 前几名之间零区分度; ②**因子全是单调的** —— 量比越大越加、相对强度越强越加, 可"量比 4.0"意味着这一波**已经发生**了, 与用户要的"高概率的有苗头的东西"方向相反; ③**没有硬门槛** —— 跌破生命线、长期下跌的票只要别的因子够猛照样上榜(通道结论最多扣 15), 而用户明确说这两种"不看"。**门槛**(全纯价格/无前视/可回测): 六态在多头侧(UT/NR/SR)、收盘 ≥ MA20 且**连续两日**(防 MA20 附近反复穿越把同一只票每天踢进踢出)、非长期下跌(`close < MA120 且 MA120 向下`, 位置+斜率两条同时成立才算, 与 market_mode 判指数同源 —— 只看位置会误杀底部拉起的强势股, 只看斜率会漏掉高位刚拐头的)。**数据缺失一律放行**: 门槛的职责是挡掉明确不该看的, 不是挡掉我们没读到的; 新股不足 120 根不该被判长期下跌, 那种消失用户永远查不出来。**三维度**: 趋势强度 45%(新鲜度主导/六态状态/相对强度)、量能确认 30%(量比/换手)、位置成本 25%(Keltner 短期通道位置)。**三条曲线全是区间最优的分段线性控制点**, 不是阈值 if/elif —— 阈值在边界上阶跃(量比 1.49 与 1.51 差 8 分而盘面上没区别), 控制点只有几个自由度, 台账回来后调的是"峰在哪、多宽", 过拟合空间小(R5)。量比峰 1.3~2.5、通道位置甜区 0.50~0.65、相对强度峰 +6~+14。缺因子时**维度内按剩余权重重归一化**而非记 0(记 0 等于因为"没读到换手率"惩罚这只票), 整档缺席则标 `partial` 让界面如实说。**注记层**(主线/AI 置信度/历史胜率/通道结论/龙虎榜)全部退出评分只作展示: 要么不可回测(主线口径随情绪周期漂移)、要么样本太小(单票历史突破常不足 10 次)、要么是模型对自己输出的自评。AI 看空不再扣 40 分把票藏起来 —— 改成**显眼的矛盾注记**, 藏起来用户就不知道有过这个冲突。**一条必须记住的等价关系**: Keltner 短期带以 MA20 为中轴, 故 `pct ≥ 0.5 ⟺ 收盘 ≥ MA20`(恒等), 因此生命线门槛把**所有贴下轨的低吸候选整个挡在门外** —— v2 只做站上生命线之后的趋势中继, **不做底部反转/低吸**, 这是用户那两句话的直接推论而非遗漏, 有测试钉住。门槛原料(MA20 含前一日 + MA120 及斜率)并进 keltner_service 本来就有的那一次 260 日批量读, **不新增 IO**。配套: `max_show` 10→15; 门槛漏斗计数回传前端(熊市里机会区空空如也不会被读成"系统没干活"); 台账加 `scoring_version`, 新旧口径**绝不混算**(两把不同刻度的尺子), 归因表从"加分组/扣分组"改为"维度高分组/低分组/缺席"。**界面**: 值得关注每行的把握分下面画三条维度分解条(同样 87 分, 三条形状完全不同, 一眼看出这分是谁给的), 新增位置列与注记列, 展开行给到因子级子分 + partial 告警 + 注记全文; 中观层补一句"出手结构 N/M 只候选过门槛"。**AI 提示词**: 送的不再是黑箱"规则分"而是三维度分解 + 原始输入 + 注记(黑箱分只能被复述"它规则分高", 分解才能被核对"它说量能 92, K 线上量比确实 1.7" —— 核对正是我们要 AI 做的事), 并写死"注记都不参与把握分"的边界。测试 +38 新增, 6 个 v1 打分测试文件按 v2 语义重写(全量 2304 通过) | 新增 `services/opportunity_score.py`、`tests/test_opportunity_score.py`;改 `api/today.py`(打分/门槛/注记/AI 提示词/中观)、`services/keltner_service.py`(`long_trend_map`)、`services/score_ledger.py`(版本隔离 + 维度归因);前端改 `pages/Today.tsx`、`components/ScoreLedgerDialog.tsx`、`lib/api.ts` | 高(整套打分口径替换; 但 `rank_opportunities` 对外签名与返回形状不变) | 打分回 v1 需还原 `today.py` 的 `score_opportunities` 与 `opportunity_score.py`;门槛与注记是新增层, 删掉即回到"全部候选都进榜、注记全部加减分" |
| R133 | **规则层把握分台账 + 一键导出(评分系统的"有没有用"归它回答)**: 起因是用户问"不然不知道这系统是否有效", 要的是**能直接交出去做调参的数据**。已有的 R121 台账回答不了 —— 它只记 AI 从规则层前 8 名里挑中的那 1-3 只, **60 分和 90 分的票从没被同台比过**(选择偏差), 所以它证明得了 AI, 证明不了把握分本身。新台账换一个记法: 每个收盘定稿日把**完整候选池**(含被 `min_score`/`max_show` 滤掉的)整个记下来, 连同**因子拆解**(这 10 分是新鲜度给的还是主线给的)与名次/当时显不显示, 之后回看 T+1/T+3/T+5。由此能答三个问题: ①**分层单调吗**(90 分档胜率是否真高于 70 分档 —— 这比"前十胜率 58%"重要得多, 不单调说明分数不承载信息); ②**门槛/条数该设几**(记了名次, 可直接算"只看前 5 / 前 15 会怎样", 不必改了参数再等三个月); ③**哪个因子在做功**(加分组不明显强于扣分组的因子就是白占权重)。为此把 `rank_opportunities` 拆成 `score_opportunities`(完整排序, 附 `factors`/`ctx`) + `filter_opportunities`(门槛截断), 老函数保留为薄封装 —— 六组既有测试按原签名写的, 拆分是为了让台账拿到完整列表, 不是改接口。**工程上刻意的四条**: 收益**算出即落盘**(到期的不会再变, 每次打开重算几百只日线会拖死接口), 未走够 N 根 K 的**留空不许拿最后一根冒充**, **盘中快照不记**(实时价当收益起点会算出假收益; 判"定稿"看数据不看时钟 —— 没有任何一只用了实时价即为定稿), 进程级去重使总览接口每次打开不会重写整本台账。统计带**同期沪深300 基准**(没有基准的胜率会骗人)与样本量。两个导出口: 服务端拼好的 Markdown 摘要(一键复制, 四张表齐全, 粘出去即可分析)+ 扁平明细 CSV(一行一候选, 因子增量各占一列, 可离线重算任意权重组合)。入口在机会区「门槛」按钮旁边 —— 调门槛前先看这个门槛值不值。测试 +22 | 新增 `services/score_ledger.py`、`tests/test_score_ledger.py`;改 `api/today.py`(拆函数/落快照/2 个端点);前端新增 `components/ScoreLedgerDialog.tsx`, 改 `pages/Today.tsx`、`lib/api.ts`、`lib/queryKeys.ts` | 中(动了 `rank_opportunities` 的内部结构, 但对外签名与行为逐字段不变, 有测试守着) | 删 `score_ledger.py` + 对话框, `today.py` 里 `rank_opportunities` 已是自足封装可直接留用 |
| R132 | **龙虎榜「0 只上榜」实为未发布, 界面把它当成了正常结果**(用户问"怎么打开什么都没"): 排查链路 —— 龙虎榜走 fuyao 专有接口(tickflow 无此能力), 按日缓存, 当日未发布时**靠 `FuyaoError` 触发回退上一期**。问题就在这: fuyao 榜单未发布时返回的是 **200 + 三榜空列表**, 不抛错 —— 于是 `state=ok`、`stock_count=0`, 界面照实显示「0 只上榜」+ 一张空表, 与"数据没到"完全分不出来。**判据**: A 股交易日的龙虎榜不可能是 0 只(每天几十只上榜), 所以"三榜全空"只可能是还没发布(通常收盘后才出)。修三处: ①`_is_empty()` 判空 → 走与拉取失败同一条回退路径(抛 FuyaoError 让 except 统一处理, 回退逻辑不写两遍); ②**空结果永不落缓存** —— 早期版本会把未发布的空结果按"历史日不可变"写进 `data/dragon_tiger/date=*.json`, 那份文件会把这一天**永久钉死在 0 只**; ③读缓存时空缓存视同未命中, 已被写坏的文件重拉一次自愈; 回退分支同样挡空, 两期都空则如实报 `no_data`。前端兜底文案改为「榜单尚未发布(通常收盘后才出)」。测试 +5 | `services/dragon_tiger.py`、`tests/test_dragon_tiger.py`、前端 `pages/Review.tsx` | 低(只收紧空结果的处理) | 还原两文件 |
| R131 | **AI 批量分析改增量 + 顺手挖出两个哑火的定时功能**: ①**批量不再全量重跑**(用户: "有些当天已经分析过了, 要有个时间差值"): 70 只自选点一次「AI 分析全部」就是 70 次调用, 而绝大多数输入数据一个字没变。判据刻意**以数据为主、时间为辅** —— 信号是拿日 K 算的, 没有新 K 线落盘就没必要重跑(喂给 AI 的还是同一份输入, 花钱买不到新信息); 具体用「信号生成时刻 vs as_of 当天 15:00 收盘」判它见没见过最新那根 K, 这个近似**保守**(收盘到落盘之间生成的会被多算一次), 方向刻意如此: 宁可多花一次调用, 也不能把没看过新数据的旧信号当最新的摆出来。TTL(24h)只当安全阀 —— 长假数据不更新时不至于永远不重算, 也给"换了 AI 档位想重跑"留出口。前后端**同口径两份实现**(前端 `lib/signalFreshness`、后端 `daily_pipeline.signal_needs_refresh`), 按钮上直接显示待分析数(`AI 分析全部·12`), 点击时 toast 如实报"跳过 N 只已最新"; 单只强制重跑走行内 ✨(不过滤)。②**顺带发现两个功能一直是哑的**: `api/settings.py` 从 `daily_pipeline` import `TODAY_AI_JOB_ID`/`SIGNAL_AI_JOB_ID`/两个 `_register_*_job`, 但**定义在某次上游合并中整段丢失** —— 用户在设置页一开「定时导读」或「定时信号」就 ImportError 500, R27 做的两个定时功能等于死了很久没人发现(import 在函数体内, 不开定时就不报错)。已按 R27 原版恢复并接上启动注册, 定时信号顺带用上增量判据(无人盯着的任务最该省)。测试 +16(含一条专门盯 import 别再丢) | 后端 `jobs/daily_pipeline.py`、新增 `tests/test_signal_freshness.py`;前端新增 `lib/signalFreshness.ts`、改 `components/stock-analysis/WatchlistDecisionBoard.tsx` | 中(恢复了两个调度 job; 增量过滤改变批量按钮行为) | 删增量过滤即回全量; 两个 job 若要退回"哑火"状态需同时删 settings.py 里的 import |
| R130 | **自选决策台「AI 分析」列改上下结构**: 原来报告胶囊 + ✨分析 + 🔔提醒三件横排, 挤在 7% 宽的列里 —— 胶囊里的「17天前」被压得几乎贴着图标。改成两行: 上行报告胶囊(吃满列宽, 不再被图标挤), 下行两个动作图标; 无报告时不再占位一个「—」, 直接只显示动作。顺带把图标按钮点击区从 `p-1`(约 20px)放大到 6×6(24px), 并补上 `aria-label`(原来只有 title) | `components/stock-analysis/WatchlistDecisionBoard.tsx` | 低(纯版式) | 还原该单元格 |
| R129 | **复盘/使用观察两页去居中 + 全系统按钮统一方角**: ①**两页挤在中间**(同 R115 的毛病): 复盘 `mx-auto max-w-[1100px]`、使用观察 `mx-auto max-w-5xl`, 宽屏下两侧各空一大片, 复盘右边的「历史复盘」栏还被推到屏幕中间。改为贴左 + 上限 1600px; 使用观察那面卡片墙顺带在 2xl 加到三列。**踩到一个自己写的坑**: 我在复盘的注释里写"行长由正文自己的 max-w 控", 结果一看正文是 `prose max-w-none` —— 1100px 那个上限**正是**唯一的行长控制, 页面一放宽正文就会横跨一米多。改成把行长控在正文自己身上(`max-w-[75ch]`, 中英混排的舒适上限), 注释才成立。②**按钮统一方角**(用户: "别油圆又方的都有"): 全项目 268 处 `rounded-full` 逐条分类后, 只把**带 padding 的胶囊按钮/标签** 73 处改成 `rounded-btn`(典型如复盘工具条那个「当日/连读昨日/近7日」分段控件 —— 它和旁边一排方角按钮并排, 正是最扎眼的那处); **保持圆的 195 处**: 状态圆点、加载动画、开关轨道与滑块、单选圆钮、进度条、头像/图标圆底、侧栏激活指示条、取色圆点 —— 这些圆是控件语义不是装饰, 方了反而不像话 | 前端 `pages/{Review,UsageNotes}.tsx` + 32 个含胶囊样式的 tsx | 低(纯版式) | 还原这些文件 |
| R128 | **全项目按钮按 skill §8 过一遍**(917 个按钮的实测审计, 不是凭感觉改): **先量后改** —— ①`type=` 缺失 653 处, 但全项目只有 4 个 `<form>`, 而那 4 个里的按钮**本来就都写了 type=** —— 表单外的 button 默认就不是 submit, 这 653 处不是 bug, **不动**(改 653 处只为让统计好看, 是假工作); ②绑了 useMutation 却没 disabled 的: **0 处** —— 加载/禁用态本来就做到位了; ③实心主按钮只有 10 个、6 种尺寸写法, 变体混乱没想象中严重。**真正的缺口是按下反馈**: 917 个按钮里只有 **4 处**写了 `active:scale`, 点下去毫无反应, 网络慢时尤其难受(不知道到底点上没有)。改法与 [R124] 焦点环同源 —— 与其在 917 处各写一遍, 不如一条全局规则兜住: `:active{scale(0.97)}` + `duration-press`, 禁用态不给反馈(按不动的东西不该假装被按下), 拖拽手柄加 `.no-press` 排除(按下要跟手移动, 缩放会让指针错位), `prefers-reduced-motion` 下直接取消位移而不是加速。**幅度取 0.97 不是 skill 举例的 0.95**: 这界面按钮普遍 28~32px 高, 0.95 在小尺寸上有明显塌陷感。**触控目标明确偏离 skill**: §8 的 44×44 是**移动端**指南, 桌面密集交易界面一屏几百个数字根本排不下, 取桌面工具常见的 28px 底线 —— 7 个 16~20px 的小开关/勾选用 `.tap-target` 伪元素把命中区撑到 28px, **视觉尺寸一点不动**(放大会毁版式)。另加 `lib/button.ts` 变体配方(primary/secondary/ghost/danger × 4 尺寸, 统一禁用态), 明确写清**只给新代码用, 不做 917 处大规模替换** —— 在每天做交易决策的界面上, 那种机械改动收益不抵回归风险 | `src/index.css`、`src/lib/button.ts`(新增)、7 个含拖拽手柄的 tsx、4 个含极小控件的 tsx | 低(全局 :active 与伪元素扩展区都是纯附加) | 删 index.css 里 `[R128]` 两段 + `lib/button.ts`; `.no-press`/`.tap-target` 类名留着无副作用 |
| R127 | **修侧栏「自动」重复显示 + 挤爆换行**(用户截图: 一个格子里两个「自动」): [R118] 当时同时加了两处 —— 齿轮旁的「自动」文字按钮(控件)与状态行的「· 自动」(状态), 各自看都对, 摆在一起就是同一个词出现两次; 更糟的是侧栏只有 230px 宽, 文字按钮 + 齿轮 + 开关三件挤一行, 把左边数据源名整个挤没了还换行, 于是屏幕上出现「自动 / 自动」上下两个。改法: ①按钮改成与齿轮同尺寸的图标按钮(CalendarClock, title/aria-label 说明用途, 开启时染 accent) —— **控件归控件, 状态归状态**, 一屏只出现一次「自动」; ②状态行允许换行(flex-wrap) —— 宁可占两行, 也别把「fuyao」这种关键信息截成空白 | `components/Layout.tsx` | 低(纯版式) | 还原本文件该段 |
| R126 | **按 skill 的 Modern Dark Mode (Vercel/Linear) 重定配色**(用户定夺: "直接使用 skill 的方案"): 与 R125 的区别 —— R125 只换坐标系(视觉不变), 这一版**真的换了配色**。①**灰阶去色**(C=0): 原来所有灰都带一点蓝(hue 286, C 0.003~0.016), 而 Linear/Vercel 风格的标志就是**纯中性灰 + 彩色只留给有含义的东西**; 去色后红绿涨跌与蓝色强调在同屏里更跳得出来。②**明度阶梯取 skill 原值**: 0.145/0.205/0.970/0.922/0.556 都是 skill 那段里的数, 暗色一侧按同间距补齐(0.145/0.205/0.245/0.320)。③**亮色强调色压暗到 L=0.55**: 原 L=0.626 在白底只有 3.48:1, 正文链接达不到 WCAG 4.5:1 —— **换配色不能换掉可读性**; 压暗后 4.99:1, 色相彩度不动, 看着还是同一个蓝(暗色一侧 5.45:1 本来就够, 不动)。④**圆角**按 skill 的 `--radius: 0.625rem` 走: card 8→10px, btn/input 6→7px, dialog 12→14px(控件比卡片小一档 —— 密集界面里控件圆角跟卡片一样大会显得肉)。**红涨绿跌一个字没动**: 那是 A 股语义不是审美。全部对比度实测过并写进注释(暗 fg/base 18.96:1、muted/surface 4.73:1; 亮 fg/surface 19.79:1、muted/surface 4.73:1、accent/surface 4.99:1, 无一项低于 4.5:1)。顺带扫过全项目硬编码灰: 只有 Review 页排名第二的"银牌色"用了 zinc, 与新的纯中性灰反而更配, 保留 | `src/index.css`、`tailwind.config.ts` | 中(全站配色底座) | 见上方「前端设计体系改造的回退指引」 |
| R125 | **按 skill 换 oklch 色彩空间 + 字体打包进产物**(用户定夺: "不走 CDN 就打包进去", "只要不破坏功能前端允许调整"): ①**oklch**: 全套色板从 HSL 改 oklch, 数值由原 HSL **精确换算**(sRGB→线性→OKLab→OKLCH, 脚本算的不是手调), **视觉与改版前一致** —— 换的是坐标系不是配色。收益: L 是感知亮度(HSL 那个 lightness 骗人, 同 L 的蓝与黄看着不一样亮), 项目大量用的 `/10 /15 /20` 半透明叠加在感知均匀空间里更可预期, 广色域屏不再被 sRGB 夹住。**同步改了 tailwind 包装器与 6 个 tsx 里 38 处内联 `hsl(var(--x))`** —— 漏掉任何一处都会把 oklch 三元组当 HSL 解析成乱色。②**字体打包**: 之前 tailwind 写着 JetBrains Mono 却没有任何地方提供它 —— **只有本机装过的人看得到**, 其余人静默回落 Consolas, 等宽数字对齐全看运气; 现在 JetBrains Mono 400/500 + Inter Variable 随产物自托管(96KB, 只取 latin 分片: 中文由系统字体栈接管, Inter 无 CJK 字形浏览器会自然回落, 这正是混排栈的工作方式)。**不走 Google Fonts 是必须而非偏好**: 国内访问经常直接不通, 等于把首屏交给一个会超时的第三方。**过程中踩了两次同一个坑**: CSS 的 `@import` 必须排在**所有**规则之前, 第一次放在 `@tailwind` 后、第二次放在自写 `@font-face` 后, 两次都被静默丢弃 —— 编译不报错、产物里一个字体文件都没有, 靠 `find dist -name '*.woff2'` 才发现 | `src/index.css`、`tailwind.config.ts`、`package.json`、`pnpm-lock.yaml`、6 个含内联色的 tsx | 中(改的是全站色彩底座与字体; 已用产物抽查确认 oklch 与 3 个 @font-face 均落地, tsc/build/eslint 全绿) | **回退**: `git revert <本提交>` 即可整体还原(色板/包装器/内联色/字体一起回到 HSL 与系统字体); 只想退字体就删 index.css 顶部三段与 tailwind sans 首项; 只想退 oklch 就把 index.css 色板与全项目 `oklch(var(` 改回 `hsl(var(` |
| R124 | **全项目前端按 frontend-design skill 升级(§4 动效 / §7 可访问性)** —— 不是逐页重画, 而是把 skill 里成体系的两条补在**设计系统层**, 161 个 tsx 一次受益: ①**动效时长统一**: 全项目 136 处手写 `duration-150/200/300/100/500`, 同类交互在不同页面快慢不一也无从统一调; 立四档语义令牌 `press 120 / hover 150 / expand 200 / enter 300` 并全量替换(46 文件 138 处)。取值刻意**沿用现状多数派**(150 出现 82 次、200 出现 43 次), 所以替换在视觉上是 no-op, 只是把散落数字换成语义名, 顺带收编 4 处 100ms、2 处 500ms 两个异类 —— 以后想整体调快慢改一处即可。②**键盘焦点环全局补齐**: 161 个 tsx 里**只有 2 个**文件写过 focus-visible, 键盘用户 Tab 到哪基本看不见; 在 index.css 加一条全局 `:focus-visible` 轮廓(只在键盘导航时出现, 不打扰鼠标用户)。**关键坑**: 代码里有 130 处 `focus:outline-none`(40 文件), Tailwind 该工具类权重 (0,2,0) 高于 `:where()` 的 (0,1,0), 不加 `!important` 这条规则会被静默盖掉 —— 焦点指示器是 WCAG 2.4.7 硬要求, 恰恰不该允许被随手覆盖, 故全项目仅此一处用 `!important`, 并留 `.focus-ring-custom` 退出类给自带 ring 的组件(AlertToast/Layout 已加, 免得环与轮廓叠两层)。③**尊重系统减少动效**: 补 `prefers-reduced-motion` —— 不是关掉动画了事, 保留 0.01ms 让状态切换仍可感知, 只去掉位移与缩放。④**图标按钮可读名**: 用脚本扫全项目, 初版正则把"文字来自 JSX 表达式"的按钮误报成 23 处, 收紧后真正无可读名的只有 1 处(数据页列显隐勾选), 已补 `aria-label`。**审计后判定无需改的**: 70 处 `shadow-xl/2xl` 全在弹窗/浮层/抽屉/Toast 上 —— 那正是重阴影该在的地方(标识浮于页面之上), skill 反对的是卡片用重阴影, 本项目卡片一律用 1px 边框分层, 已符合 | `src/index.css`、`tailwind.config.ts`、46 个含动效时长的 tsx、`components/{AlertToast,Layout}.tsx`、`components/data/PageSettingsModal.tsx` | 低(时长取值等价替换; 焦点环是新增视觉, 只在键盘导航出现) | 还原 index.css 尾部段与 tailwind 时长档位; 时长类名批量替换回数字 |
| R123 | **机会区补两个决定"动不动手"的数字**: 量比与距触发价此前只以散文形式埋在 `why` 里("现价距买入触发价仅 1.9%""放量突破(量比 1.7)"), 扫一眼表格看不到。后端在两路来源合并之后**统一算一次**结构化字段 `vol_ratio` / `gap_pct`(两路都存了 pivot, 不必在分支里各写一遍), 前端提成两列: 距触发按 已越过/≤1.5%/更远 三档染色, 量比按 ≥1.5 放量 / <0.8 缩量染色。选这两个而不是别的: 距触发回答"今天能不能动手", 量比回答"这个突破是不是真的" —— 后者也是 AI 优选的核心判据, 摆成列用户才能自己核 AI 那句"量比 1.25"是真是假(与 R121 同源)。取不到一律留空不填 0(0 会被读成"正好到触发价", 是有含义的数字)。测试 +5 | 改 `api/today.py`、前端 `pages/Today.tsx`、`lib/api.ts`;新增 `tests/test_today_opportunity_metrics.py` | 低(纯新增字段) | 删该段与两列 |
| R122 | **今日总览版式重做**(按用户提供的 frontend-design skill 走「先画版式 → 定主题 → 排动效 → 再写码」四步): ①**机会区卡片→表格**: 旧版 15 只票 = 15 张 4~5 行卡片, 其中"建仓路径:先试 0.5 成 → 站稳 X 3 日加至 1 成 → …"每张一模一样, 眼睛扫不动, 真正有区分度的字段反被埋在长句里。改成一行一只、可比字段对齐成列(把握分/名称/信号/主线/建议仓位), 模板文字收进展开行(`aria-expanded` + 箭头 180° 旋转)。②**把握分补分布条**: 顶上一串 100/100/97/95 光看数字没有区分度, 保留数字(用户可能靠它取舍, 不擅自删)另加一条按分值画的条形 + "今日第 N/M"提示。③**三条通栏横幅→两条**: 时段提示条只是一句"这页该怎么用"的说明, 不值得独占一条通栏, 并成天气条里的一个徽章(说明收进 title), 首屏省出一整条。④**动效时长进 tailwind 主题**: 此前 150/200/300ms 各处手写、同类交互快慢不一, 按"越轻越快"立四档 `press 120 / hover 180 / enter 320 / expand 220` + `rise-in` 关键帧, 新代码一律引用; AI 优选卡逐条错开 60ms 入场(结果是"长出来"的), 表格行不做入场(每次刷新都动会闹)。**未采纳 skill 里的**: CDN 版 Tailwind / Flowbite / Unsplash 占位图 —— 本项目是打包构建 + 已有完整设计令牌(JetBrains Mono、语义 HSL 变量、8px 圆角、Linear 缓动), 引 CDN 反而破坏一致性; oklch 也未改, 现有 HSL 变量与 Tailwind alpha 语法配套, 换色彩空间是全站级改动, 收益不抵风险 | 前端 `pages/Today.tsx`、`tailwind.config.ts` | 低(纯展示层; 数据字段与导出 HTML 未动) | 还原两文件 |
| R121 | **AI 优选可信度三件套**(用户: "AI 优选一定要靠谱"): 此前优选只有一行"名字 + 理由", 理由里那些很具体的数字(涨 7.2%、量比 1.25、守住 2372)**没有任何东西检查真假** —— 模型把量比 0.87 写成 1.25、把关键位编到该股从没到过的价位, 界面照样原样展示, 而用户会照着这个数挂单。补三层: ①**事实校验** `services/today_ai_verify`: 从理由里抽涨幅/量比/价位, 拿**送审时喂给 AI 的那份日K**(不另读行情 —— 要检验的正是"它有没有忠实使用我们给的数据")逐条对账, 容差涨幅 0.35pp / 量比 0.15 / 价位近 60 日区间±15%; 判四档 已核对·待查·存疑·驳回, 另查提示词明令禁止的空理由(复述规则分/AI 看多)与"盘中信号未注明等收盘确认"。**价位离谱直接驳回且不当推荐展示** —— 一条编造价位的建议比没有建议更糟。关键设计: `ok is None`(数据缺失)既不算对也不算错, 绝不并入"已核对"(那等于把"没查"说成"查过了", 恰是这套东西要杜绝的事)。②**命中率台账** `services/ai_pick_ledger`: 每天落一条(只记未被驳回的, 起点=当日收盘价), 用日线回看 T+1/T+3/T+5 收益, 出胜率与均值, 直接贴在优选卡上 —— 靠不靠谱最后只能用记录说话; 刻意**不回喂给提示词**(一旦拿历史胜率影响当期选择, 这个数就不再是干净的事后统计), 且必须带口径说明(收盘价对收盘价、未计滑点, 不说清就是误导); 被驳回的不进台账。③**提示词加一节**: 明确告知 AI 数字会被自动对账、编造必被抓出、记不准宁可写定性描述 —— 事前威慑比事后标注更省事。前端 `AiPickPanel` 重做: 校验徽章 + 可展开的逐项对账(AI 说 X / 实际 Y)+ 历史命中率条 + 驳回条目单独折叠展示(要让用户看见"它编了什么")。测试 +24 | 新增 `services/{today_ai_verify,ai_pick_ledger}.py`、`tests/test_{today_ai_verify,ai_pick_ledger}.py`;改 `api/today.py`(+`/ai/track-record` 端点)、前端 `pages/Today.tsx`、`lib/{api,queryKeys}.ts` | 低(校验是纯附加层, 失败不影响优选本身; 台账 try/except 包住) | 删两个服务+端点+`AiPickPanel`, picks 回落纯文本; 数据卷多 `user_data/ai_pick_ledger.json` |
| R120 | **同步上游 dab8a2c(4d27f31→dab8a2c, 18 提交)——「代码遵循作者、增强一个不删」**: 作者的新结构照单全收, fork 增强改嫁接在其上。**关键收益**: 作者给 fuyao 插件接入指数快照端点(`/api/a-share-index/prices/snapshot`, `.BJ` 先过滤防整批 1002 连坐), quote_service 自定义源分支鸭子类型调 `get_realtime_indices` 补拉指数 —— **修掉了"fuyao 路由下指数冻结在日K兜底"**, 用户侧栏 A 股指数卡这才真正实时(此前一直是收盘价, R119 的绿灰点判据正是如实标了灰)。**让位给作者的**: ①指数偏好五件套(`sidebar_index_symbols`/`indices_nav_pinned`/`realtime_pull_index`/`realtime_index_mode`/`realtime_index_symbols`)整套下线, 展示层收敛为 `services/index_const.py` 固定核心四只 —— fork 侧栏卡改读那份单一权威, R119 的自适应轮询(6s/30s/60s)与"在跳/静止"绿灰点全部保留; 设置页「左侧菜单指数」卡按作者删除, 但**全球指数开关单独成卡保住**(独立数据源, 与 A 股无关)。②`update_realtime_quotes` 采作者的首用门禁(空库 409); 作者那段里重复的 paused 门禁不采纳 —— fork 早已把它挪到完整性检查之后并注明原因(启动自检可能已启动修复, 前端应接续任务而非被 409 提前截断), 两处只留挪过去的一处。③Watchlist 分时改走作者的 `fetchMinuteBatchIncremental`(since 增量), fork 的 toast 引入并存。**给 fork 补的一刀**: 作者的首用门禁在 API 层, [R118] 自动开关走服务层 `enable()` 绕得过去 —— `QuoteService._has_local_data()` 按同口径在服务层再判一次, 空库不自动开(+1 测试)。内置策略 19→26(作者新增 7 个矩阵策略), fork 保 `factor_rank_research` 故计数 27/26。TickFlow SDK 0.1.25。后端全量 2179 passed / 1 skipped; 前端 tsc 干净、vite build 过、ESLint 0 error(7 条 warning 均为上游 Watchlist 既有) | 9 个冲突文件(`api/settings.py`、`services/preferences.py`、3 个测试、前端 `Layout.tsx`/`api.ts`/`Watchlist.tsx`/`settings/Monitoring.tsx`)+ 41 个自动合并 | 高(指数链/实时链/策略计数全动过, 已逐处核对 fork 增强完整) | 回退 merge commit(会同时退掉作者 18 个提交) |
| R119 | **全球指数改多源候选 + 侧栏 A 股指数卡自主轮询**: ①`_Preset.codes` 升级为 `sources`(厂商+代码候选串), 新增**腾讯** `qt.gtimg.cn` 解析器(`~` 分隔; `s_` 精简版取 3/4/5 位, 完整版取 3/31/32 位)与新浪并列, 按序取第一个能出价的, 返回里记 `source`/`source_code` 便于诊断; 取数按厂商分组各拉一次, **一家挂了不影响另一家**; `/debug` 改为逐候选回 raw 与解析结果。韩国综合仍是待确认代码(新浪 4 个 + 腾讯 2 个候选全列着), 确认哪个有数后挪到第一位删掉其余即可。②新增 `/api/global-indices/tickflow-probe`: 用用户自己的 key 调 `exchanges.list()` + `get_instruments("US", type=index)` + 一次 `quotes.get` —— 回答"境外指数能不能走 TickFlow"。**韩国已有定论: SDK 的 `Region` 枚举写死 `Literal["CN","US","HK"]`(tickflow/generated_model.py), 没有韩国, 这是数据源边界不是配置问题**; 美股 region=US + type=index 有定义, 能不能用取决于账号档位, 探针当场回答。③侧栏 A 股指数卡不再只靠 SSE 推: 加自适应轮询(交易中且轮询在跑 6s / 交易中未开实时 30s / 休市 60s; 该端点只读缓存与日线回退, 不触发 TickFlow 请求), 并按 `source` 给卡片加"在跳/静止"绿灰点 —— 与全球卡同一套语义, 不让用户把静止当故障。测试 +10 | 改 `services/global_indices.py`、`api/global_indices.py`、`tests/test_global_indices.py`(19 条)、前端 `components/Layout.tsx`、`lib/api.ts` | 低(独立模块内 + 前端轮询) | 还原这几个文件 |
| R118 | **实时行情按交易日自动开关**(侧栏那个开关旁加「自动」): 新增 `services/realtime_schedule`(纯函数时段判定: 交易日 09:15 开 / 15:05 关, **午休不关**——11:30 午休定版要线程活着; 周末与探针判定的节假日一律关; 探针 None 时沿用「维持周几近似」宁可多开也不误关) + `QuoteService` 独立守护线程(30s 一拍; 开关关着时轮询线程根本不存在, 所以这段不能塞进 `_poll_loop`)。**边沿触发**: 只在应开/应关翻转的那一刻动手, 中途用户手动改了就一直听手动的, 到下一个边界才回自动节奏; 盘中现开「自动」立刻生效(改偏好即清边沿记忆)。自动关调 `stop()` **而非 `disable()`** —— disable 会连自选实时叠加层一起清空(那是 [R16]「用户要回收盘口径」的语义), 自动关只是时段结束, 当天实时价要留给今日总览/决策台, 等盘后管道落官方收盘价再覆盖; 收盘定版没成功时延后关闭, 最晚 15:40 无条件关。none 档无实时权限时空转。测试 +22 | 新增 `services/realtime_schedule.py`、`tests/test_realtime_auto.py`;改 `services/{quote_service,preferences}.py`、`api/settings.py`、前端 `components/Layout.tsx`、`pages/settings/Monitoring.tsx`、`lib/api.ts` | 低(新增守护线程与一个偏好项, 默认关=行为不变) | 删 realtime_schedule.py + quote_service 里 `_auto_*` 三个字段与四个方法 + 端点; 偏好里多一个 `realtime_auto` 字段(向后兼容) |
| R117 | **外部网页改「抓取 + 面板 AI 整理 + 固定版式」**(用户: 不做嵌入了, 也不粘贴代码, 让内部 AI 格式化): 上游的 iframe 内嵌保留为 `mode=iframe`(默认值不变, 零回归), 新增 `mode=fetch` —— ①后端 `external_fetch` 代抓原文(绕 CORS), **SSRF 守卫**: 只放 http/https、禁 URL 内账号密码、主机名解析出的每个 IP 都必须是公网(环回/私网/链路本地/保留段全拒)、重定向逐跳重新校验(最多 3 跳)、2MB 与 12s 硬上限、10s TTL 合并轮询; ②`external_view` 清洗压缩原文(去 script/style/注释/全部属性, 只留标签骨架; JSON 原样保留; 超 12000 字截断)后走 `ai_provider` 档位链, 提示词把输出结构钉死(title/stats/sections{columns,rows}/notes, 数值必须是数字、涨跌列 tone=delta、禁止编造、没数据就只回 notes 说明原因), `extract_json_object` 容错解析 + 失败带死命令重试一次; ③`normalize_spec` 服务端收敛契约(别名/二维数组/缺列自动推断, 行列/表数/KPI 数都有上限) —— AI 输出**一律当不可信数据**; ④结果按「URL+原文哈希+提示词」缓存到 `user_data/external_page_view.json`(json_store 原子写), 原文没变不重复花钱, 「重新解析」走 force; ⑤前端固定版式渲染器(KPI 卡 + 若干张表 + 备注, 红涨绿跌, 一律纯文本渲染绝不 innerHTML), 设置页给模式二选一 + 「想看什么」大白话输入 + 「只抓原文看看」与「试运行(调一次 AI)」两个按钮。用户改的是提示词, 不是代码。测试 +42 | 新增 `services/{external_fetch,external_view}.py`、`api/external_page.py`、`tests/test_external_{fetch,view}.py`、前端 `lib/externalView.ts`、`components/ExternalViewRender.tsx`;改 `services/preferences.py`、`api/settings.py`、`main.py`、前端 `pages/ExternalPage.tsx`、`pages/settings/ExtPages.tsx`、`lib/{api,queryKeys}.ts` | 中(动了上游的 external_page 偏好与页面; 但 iframe 分支原样保留, 新字段全部可选) | 删两个服务+API+两个前端新文件; preferences 去掉 mode/ai_hint 两字段与形参; `ExternalPage.tsx` 只留 iframe 分支; 数据卷多 `user_data/external_page_view.json` |
| R116 | **全球指数只留韩国综合 + 纳斯达克**: 用户定案是"删除"而非"默认不选" —— 日经/恒生/道指/标普四行从 `PRESETS` 整行移除, 设置页选项里也不再出现(选项列表本就由 `/options` 驱动, 前端零改动); `DEFAULT_KEYS` 改为由表推导。老用户存的选择若只剩已删除的键(过滤后为空)回落默认, 免得一张卡片都不显示 —— 主动存空列表(全关)不触发回落。港股行格式解析分支(`kind="hk"`)保留备用, 测试改用现造档位守住 | `services/global_indices.py`、`api/global_indices.py`、`tests/test_global_indices.py`(9 条) | 低(独立模块内) | 还原三文件 |
| R115 | **设置页重排(不再整屏居中)**: 原来内容整块 `mx-auto max-w-[1100px]` 居中、tab 导航还额外 `justify-center min-h-[60vh]` 垂直居中 —— 宽屏下导航浮在屏幕正中, 左右各空一大片, 内容区反被挤窄。改为贴左贴顶: 容器去掉 `mx-auto` 与 1100px 上限, 导航 `sticky top-3` 从顶部起排, 内容区吃满剩余宽度(留 1500px 上限防超宽屏行长失控) | `pages/Settings.tsx` | 低(纯版式) | 还原本文件 |
| R114 | **侧栏 AI 徽标改为下拉快切档位**: 点徽标弹出档位列表(fixed 定位逃逸侧栏裁剪), 点任一档即把它提到第 1 位并整表保存 —— 顺手把该档设为启用(选了就是要用它); 当前首选打勾, 未启用档标灰提示, 底部留「管理档位」入口去设置页。与「设置 → AI 设置」共用同一 queryKey, 保存后两处 invalidate, 两边永远同一份顺序。整表回传 api_key 空串即可(后端按 id 对号沿用原 key) | `components/Layout.tsx` | 低(复用既有保存端点) | 还原本文件 |
| R113 | **韩国综合不显示: 行情代码改多候选自动轮试**: 用户实况 —— 日经/恒生/美股三大都出数, 唯独韩国综合空白, 即新浪对 `int_kospi` 没有数据。改 `_Preset.code` 为 `codes` 元组(多候选), 取数时把所有候选一并请求、逐个解析取第一个出价的, 并在返回里记 `source_code` 便于诊断; 韩国配 4 个已知写法(int_kospi/gb_ks11/znb_KS11/hf_KS11), 恒生配 2 个。`kind=hk` 的候选若返回的不是港股行格式(字段少于 9)自动按 int 解析。默认选择改为「韩国综合 + 纳斯达克」(用户定案; 已手动选过的用户不受影响, 需自行在设置里取消其余) | `services/global_indices.py`、`tests/test_global_indices.py`(8 条) | 低(独立模块内) | 还原两文件 |
| R112 | **全球指数做成真·实时(交易时段感知 + 自适应刷新)**: 用户要"做成实时", 但休市时上游值本来就静止, 光提速没用 —— 关键是让"在跳"与"休市"一眼可辨。后端 preset 表补各市场交易时段(北京时间: 韩 8:00-14:30 / 日 8:00-14:00 / 港 9:30-16:00 / 美股 21:30-次日 5:00 取夏冬令时并集, 周末一律休市), 每行返回 trading 标记; 前端据此: 有市场在交易 → 8s 轮询(与 A 股同频), 全部休市 → 退到 60s(不白打上游); 卡片名字前加状态点(交易中=绿点脉冲, 休市=灰点+整卡降透明), 悬停写明"交易中, 实时刷新"/"当前休市, 显示最后成交值"。服务端 TTL 10s→5s | `services/global_indices.py`、`Layout.tsx`、`api.ts` | 低(独立模块内) | 还原三文件 |
| R111 | **修 AI 首选档显示错位(用户实况: 置顶了 gpt-5.6-sol, 侧栏仍显示 MiniMax-M3)**: 根因是"是不是后端合成的兼容档"一直靠 `id === 'legacy'` 猜 —— 但早期合成的那条一旦被用户保存进档位表, id 就原样留在存储里, 它已是**真实档位**且调用链确实在用它; 前端(侧栏徽标 R109 / AI 设置页 R94)按 id 过滤, 正好把用户排第一的那档当占位滤掉, 首选顺延到第二条(徽标的 +1 也由此而来)。改为后端显式标记 `synthesized`: `_clean_profile`(来自存储)一律 False, `_legacy_profile`(现合成)为 True, 前端改判该字段。顺带: 表内第 1 档未勾启用时徽标给 ⚠ 提示(调用链会跳过它) | `secrets_store.py`、`Layout.tsx`、`settings/AI.tsx`、`api.ts` | 低(判据换成显式字段, 保存链不变) | 还原四文件 |
| R110 | **竞价一进二融入异动监控「竞价异动」标签**: 用户参考外部"开盘啦·竞价一进二", 要在本面板做同类功能 —— 原「全市场竞价扫描」是从未落地的占位卡, 现填成真功能: 昨日首板(enriched 的 signal_limit_up + consecutive_limit_ups==1, 与连板梯队同源; 仅主板, 剔 ST/双创) × 当下全市场竞价快照, 四维评分。**与外部那套的三处刻意差别(均在界面写明口径)**: (1) 竞价涨幅 35 分按**温和高开**(3~6%)给满分、>9% 判为追高陷阱区 —— 依据是本面板自己的 60 日回测"高开≥5%当日均值 -1.97%", 不照抄"越高越好"; (2) 封板质量 20 分为**替代口径**(真封单量需 depth5 五档, TickFlow Expert 专有, 本 fork 免费档+fuyao 都没有), 改用昨日换手/一字板近似, 返回 quality_proxy=true 且界面标注; (3) 外部的"板块地位"需概念映射(依赖用户配的扩展数据源), 改为必然可得的"市场情绪"=昨日首板宽度。竞价数据无历史接口, 故 refresh 现拉+按日落盘积累(data/user_data/auction_scan/), 默认只读当日存档不打数据源。快照取数走实时数据源抽象(不直连 SDK) | 新增 `services/auction_scan.py`、`tests/test_auction_scan.py`(7 条); 改 `api/abnormal.py`、`AbnormalMoves.tsx`、`api.ts`、`queryKeys.ts` | 低(新增独立扫描, 不碰既有异动链) | 删服务+端点+卡片 |
| R109 | **侧栏 AI 徽标跟随档位表首选**: 用户在「AI 档位」里把别的档拖到第 1 位后, 侧栏徽标仍显示旧单档配置的 ai_model(纹丝不动) —— 徽标改为优先读档位表首个**启用**档(label 优先, 否则 model), 并在名字后显示 `+N` 备用档数, 悬停给完整兜底链路(A → B → C); 没配过档位表(仅 legacy 合成档)时回落原字段, 老用户显示不变。档位保存本就 invalidate QK.aiProfiles, 故切换后侧栏立即跟随 | `components/Layout.tsx` | 低(纯展示取数改源) | 还原本文件 |
| R108b | **跷跷板并入「AI 打板复盘」弹窗(用户改定案)**: 不再做独立跷跷板弹窗/按钮 —— AI 打板复盘弹窗内加标签页「打板复盘 / 跷跷板」, 跷跷板标签显示出处副标题+概念/行业切换, 正文为 SeesawPanel(embedded, 含 AI 定性/配对明细/一键识别/30 天留档); 打板标签的存档选择/删除/生成/追问控件仅在自己标签下显示。复盘页头独立「板块跷跷板」按钮与 SeesawDialog 撤销(R108 的过渡形态) | `LadderAiReview.tsx`、`Review.tsx`、`regime/SeesawPanel.tsx` | 低 | 还原三文件 |
| R108 | **板块跷跷板收编到复盘弹窗(全站唯一入口)**: 市场环境页的内联跷跷板区块撤销, 复盘页头新增「板块跷跷板」按钮(与 RPS 轮动并排)。弹窗版式对齐「AI 打板复盘」: 图标+标题+出处副标题("来自市场环境页主线强度数据 · 识别资金在两个板块间来回切换"), 右上概念/行业切换与关闭; 正文按【AI 定性】/【配对明细】分节组织(配对行沿用 R107 单行紧凑版), AI 一键识别、30 天留档全保留。SeesawPanel 加 embedded 模式复用, 面板本体仍可独立使用 | `regime/SeesawPanel.tsx`(+SeesawDialog)、`Review.tsx`、`Regime.tsx` | 低(入口迁移+弹窗包装, 数据链不变) | 还原三文件 |
| R107 | **板块跷跷板去图重构(用户定案"不看图, 只留核心信息")**: 每对从"三行卡片+双线走势图"压成**一行**: 配对 ⇄、跷跷板分、来回轮数、AI 判定、节奏提示(截断, 悬停看全文+AI备注)、右侧"X占优·N天"。删 DualSpark 走势图组件; AI 一键识别、30 天留档、AI 总结条全部原样保留 | `regime/SeesawPanel.tsx` | 低(纯展示压缩, 数据链不变) | 还原本文件 |
| R106 | **「报告」列升级为「AI 分析」列 — 页头两按钮按标的整合进列**: 用户定案 —— 决策台"报告"列改名"AI 分析", 每行三件套: 报告胶囊(点开最近报告, 原样保留) + ✨对该标的生成/更新 AI 四维分析 + 🔔该标的点位提醒; 原页头「AI 个股分析」「点位提醒」两按钮撤销。分析流程通用化: handleAnalyze/doAnalysis/openPriceAlert 接受目标股参数, 行内点击静默选中(不弹关键价位窗)再走原有当日报告二次确认链, 二次确认记住目标股(重新分析不会打给切换后的另一只)。非自选股保底: 搜索行"当前 XX"标签旁保留同一对迷你 ✨/🔔(搜出来的股不在列表里, 没有这里就没入口) | `pages/StockAnalysis.tsx`、`WatchlistDecisionBoard.tsx` | 低(入口重排 + 参数化, 分析链路不变) | 还原两文件 |
| R105 | **涨幅RPS轮动分析收编到复盘(全站唯一入口)**: 用户定案"一个功能只放最有用的一处" —— 行业分析/概念分析两页右上角的「涨幅RPS轮动分析」按钮撤销, 复盘页头新增「板块RPS轮动」(琥珀色, tooltip 标明出处); RpsRotationDialog 增加 allowKindSwitch: 弹窗内直接切行业/概念维度(切换时重置层级与选中), 标题旁一行小字说明数据出处(行业/概念分析页的板块行情)与功能(按每日涨幅算 RPS 排名观察轮动)。原两页 kind 固定用法保留兼容 | `RpsRotationDialog.tsx`、`Review.tsx`、`IndustryAnalysis.tsx`、`ConceptAnalysis.tsx` | 低(入口迁移, 功能不变) | 还原四文件 |
| R104 | **个股分析页头重排(配合全站统一弹窗)**: 列表点标的已能弹整合版个股弹窗, 页内两个重复入口撤销 —— 页中"名称按钮"与右上"上次查看"胶囊删除(上次自动恢复逻辑保留); AI 个股分析/点位提醒 上移页头右侧(带作用对象 tooltip); 搜索行留搜索框 + 非交互"当前个股"标签。顺带修 R103 叠窗: 点标的名称不再同时弹 R28 关键价位弹窗(整合弹窗自带该视图), 搜索选中的 R28 行为保留。另: 全站弹窗关键价位视图默认开启(自选/监控/异动等与策略页同三视图) | `pages/StockAnalysis.tsx`、`StockPreviewDialog.tsx` | 低 | 还原两文件 |
| R102 | **全球指数与 A 股指数同格同开关(背后独立)**: 侧栏左上角只有一块指数网格 —— A 股四指数与全球指数(日/韩/纳等)混排同一 grid, 同受「固定显示」开关与实时开关控制; 全球卡不带跳转、悬停标"全球·独立源"与新鲜度, 超 10 分钟未更新降透明度。选择哪些全球指数并入「设置→实时监控→左侧菜单指数」同一张卡(A 股勾选下方独立一节), 侧栏齿轮与独立"全球指数"区块撤销。背后仍是两条独立数据链: A 股走行情主链, 全球走新浪独立服务, A 股实时不可用时全球卡照常显示 | `Layout.tsx`、`settings/Monitoring.tsx` | 低(纯前端重排) | 还原两文件 |
| R101 | **策略池一键找回(按盘后跑批记录)**: 用户的池在 R95 留底机制之前被清空, 浏览器侧无从恢复; 但 strategy_cache.json(盘后跑批缓存, 在服务端数据目录)的键正是"上次跑批时池里有谁"且保留顺序 —— 池空且缓存非空时策略页给横幅, 一键把缓存里的策略清单(含顺序)原样放回池。纯前端, 读现有 cached-summary 接口零后端改动 | `pages/Screener.tsx` | 无 | 删横幅 |
| R100 | **个股弹窗合并"最近查看"并支持弹窗内随意切换**: 个股分析页两处入口(页内名称按钮 / 右上角"上次查看"胶囊)合并为同一个弹窗 —— StockPreviewDialog(全站 11 个页面共用的那一个)顶栏下新增切换条: 全局"最近查看"(localStorage, 8 条, 打开/切换自动入列)点击即切换 + 内嵌搜索框任意换股, 不用关弹窗。实现取"内部覆盖 + 外部重置": 弹窗内部维护 override symbol, 外部换股/重开时回落到外部指定, 全部调用方零改动即获得该能力; 父级关闭时 override 立即失效防"扣住"。个股分析页: 右上胶囊点击=恢复该股+直接弹详情, 弹窗开放关键价位视图 | `StockPreviewDialog.tsx`、`StockAnalysis.tsx`、`storage.ts` | 低(调用方接口不变, 纯弹窗内增强) | 还原三文件 |
| R99 | **全球指数实时(韩综指等) — 自成一体的独立模块**: 用户要在侧栏看韩综指等境外指数实时, TickFlow/fuyao 均为纯 A 股源给不了, 按"独立模块单独维护"要求新建: 新浪财经公开行情接口(免 key, 带 Referer, GBK, int_/rt_hk 两种行格式按 preset 表钉死字段位), 服务端 20s TTL 合并轮询 + 失败回旧值(≤10 分钟), **不进能力路由、不碰 QuoteService、不落盘**, 挂了只影响这一块卡片。时差处理: 不做时段门控(休市时上游值天然静止), 卡片超 10 分钟未更新降透明度+悬停显示几分钟前。侧栏 A 股指数卡下方新增"全球指数"区: 齿轮弹出芯片选择(韩综/日经/恒生/道指/纳指/标普, 存 preferences), 前端 30s 轮询驱动、没人看零请求。默认只选韩综指。⚠ 引入了 TickFlow/fuyao 之外的第三方源(新浪), 系用户明确要求的境外数据, 与 A 股数据链完全隔离 | 新增 `services/global_indices.py`、`api/global_indices.py`、`tests/test_global_indices.py`(6 条); 改 `main.py`、`api.ts`、`queryKeys.ts`、`Layout.tsx` | 无(只读独立小服务) | 删两个新文件+侧栏区块 |
| R98 | **收盘正式覆写确认 — "确保落盘完整"的最后一道闸**: 常态交易日管道的日K窗口是[今天~今天], 若数据商(fuyao)当日正式数据在管道时刻未发布会写 0 行, 当日行停留在收盘快照版且当天无人回头。调度路径补确认循环: 窗口写入 <500 行且当天是交易日 → 标记待确认, 1 小时后自动重跑管道, 最多 3 次(计数在确认过关/跨日时清零; 休市日写 0 行不误报, 探针失灵按交易日处理宁可多确认)。全部落空也无害: 快照版数值≈正式版, 次日管道必然覆写; 每步有日志。多天补缺口窗口不判(行数无法归因到今天, 次日 integrity 兜底) | `jobs/daily_pipeline.py`、`tests/test_pipeline_official_confirm.py`(5 条) | 低(只在调度路径加确认, 管道本体不变) | 删确认三函数与调用点 |
| R97 | **fuyao 主备 key「失效才切换」(用户定案)**: FuyaoClient 支持逗号分隔多 key(与 TickFlow 同一填法), 语义按用户要求收紧为主备而非分担 —— 鉴权失败(401/403)=key 真失效**立即**切备并粘住; 限频(4001/429)视为瞬时, 单次只抛错交给软失败/下一轮自愈, 同一把 key **连续限频满 3 次**才判"额度耗尽"升级切备(成功清零计数); 网络/业务错误换 key 也没救, 不切。所有 API 调用走同一 _get, 快照/日K/除权/财务/龙虎榜/风向标全部受益; 设置页探测逐把单独实探, 任何一把失效拒绝保存并指明是哪把。轮询默认间隔维持 6s 不动(用户定案, 曾短暂改 3s 已回退); 弹窗单票小灶维持 TickFlow 池(与实时源无关, 降级后挑空闲 key, 核对无冲突) | `plugins/fuyao/{client,provider,plugin.yaml}`、`tests/test_fuyao_failover.py`(8 条) | 低(单 key 行为不变) | 还原两处 |
| R96 | **「我的使用观察」按真实用法重设计为观察验证账**: 上线后看到的用法是记"盘面观察→等市场验证"的猜想, 不是系统使用笔记。重排: 每条可标状态(随手记/待验证/已验证/不成立, 点芯片循环切换)、可置顶、可搜索、按状态过滤(带计数); 宽屏两列紧凑卡片, 时间与操作收进一行 footer; 新增框单行起步聚焦展开。语义细节: 标状态/置顶不刷新 updated_at(整理动作不打乱时间线); 置顶稳定排最前; 老数据读时补默认字段不回写。后端 update 改 PATCH 语义(只带要改的字段) | `services/usage_notes.py`、`api/usage_notes.py`、`tests/test_usage_notes.py`(7 条)、`pages/UsageNotes.tsx`、`api.ts` | 无(独立模块) | 回退本提交 |
| R95 | **修策略池被自动清理误伤(用户实况: "策略页的策略被清空")**: 策略池存浏览器 localStorage, Screener 挂载时 prune 会把"后端列表里没有的 ID"永久移出池。原守卫只挡了失败/加载中/空列表 —— **列表非空但缺一截**时照清: 数据目录未迁移(新部署实例没有用户自定义/挖掘策略文件)、个别策略加载失败, 都会让好好的策略暂时从列表消失, 一次打开策略页就被永久清掉, 全程无提示无留底。三处修: (1) prune 增加 load_errors 闸 —— 有任何策略加载失败就整轮不清; (2) 被清的 ID 先留底(strategy-pool-prune-backup, 合并去重); (3) 策略页给恢复横幅 —— 一键把留底 ID 放回池(策略暂不存在时不显示卡片, 待文件恢复自动重新出现), 或"不再提示"清掉留底。已被清的旧池无留底可恢复, 需迁移数据后手动重加 | `lib/storage.ts`、`lib/useStrategyPool.ts`、`pages/Screener.tsx` | 低(prune 只多一道闸 + 纯新增留底/恢复) | 还原三文件 |
| R94 | **AI 设置统一入口: 档位表接管全系统 AI**: 后端本来就是"配了档位后只走档位链(第 1 档优先, 服务不了自动顺位), 旧单档完全不参与", 但设置页里两张卡并存, 旧单档的接口/模型/Key 仍可编辑、连接状态还显示旧单档模型 —— 看起来像两套。改法: 配过档位表(非合成 legacy)时, 下面那张卡收起接口/模型/Key/清空, 变成「全局参数」卡(UA/输出上限/上下文窗口 —— 这几样确实对所有档位全局生效), 顶部说明一句"由档位表统一管理"; 连接状态显示"首选 X · 共 N 档自动兜底"; 测试按钮不再顺手回写旧单档配置。没配过档位的老用户界面与行为原样。后端小补: 记录最近真正服务成功的档位(_LAST_SERVED), 测试结果报真实服务档而非固定第一档 —— 兜底切过档时别把功劳记错 | `services/ai_provider.py`(+last_served_profile_name)、`api/strategy.py`(/ai/test 标签)、`pages/settings/AI.tsx` | 低(默认路径不变, 仅档位用户的界面收纳 + 测试标签) | 还原三处 |
| R93 | **新页面「我的使用观察」— 纯文本笔记本(增删改)**: 用户归纳系统用法/哪些功能有用的个人笔记。刻意极简: 无分类无标签无富文本, 一个输入框新增(Ctrl+Enter), 每条就地编辑/二次确认删除, 按最近编辑排序。后端 `usage_notes.json` 走 R72 的按文件锁+原子落盘, 解析失败不回写空(R68 教训), 上限 500 条/单条 2 万字; API `/api/usage-notes` CRUD。前端进菜单体系: 可在「设置→菜单」里排序/隐藏。测试 5 条(含并发不丢) | 新增 `services/usage_notes.py`、`api/usage_notes.py`、`tests/test_usage_notes.py`、`pages/UsageNotes.tsx`; 改 `main.py`、`api.ts`、`queryKeys.ts`、`router.tsx`、`Layout.tsx`、`MenuSettings.tsx`(各一小段) | 无(纯新增独立模块) | 删两个新后端文件+页面, 还原六处接线 |
| R92 | **同步上游 v0.2.2(afbf432→4d27f31, 56 提交), 一处明确背离作者**: 大头是 feat/capability-routing 分支入正史(R81/R83 已按分支状态先行整合, 本次接上历史), 真正的新内容照单全收 —— TickFlow 取数列式直转提速(as_dataframe=False)+除权因子 UTC 偏移修复、分钟增量两段式重构(intraday.universe 单请求稳态 + batch 全天修复, 间隔 [3,120] 默认 6s, 上游整文件接管)、单块失败重试的全市场脉冲、能力路由矩阵 UI(数据源页上游正式功能, fork 旧 CapBadge 路由显示让位——R70 先例)、复盘接入 fuyao 龙虎榜/盘前风向标上下文、弹窗分时档位按分钟源深度收窄、监控中心"实时不可用"横幅(适配 fork 版式)、首用引导改版、图表角标关闭。**背离**: 上游 v0.2.2 砍掉免费档自选实时("free=无实时, 用 fuyao 补"), 本 fork 明确保留 —— watchlist 档、多 key 轮转拉取链(_fetch_watchlist_quotes/_quotes_to_records/get_realtime_watchlist_symbols)、监控自选档、放量编辑器"非全市场模式不触发"提示全数保住, realtime_mode 注明背离原因(fork 主干功能, 用户即此形态运行)。放量差值保 fork 实现(金额缺失 fail-closed + 跨轮距守卫, 为上游超集), 状态声明并入上游 _holiday_active 探针; 规则指纹保 fork"改配置重置冷却"。Keys 页被上游收编进数据源页, fork 多 key 清单(R58 TickflowKeys)嵌入新 TickFlowKeySection 顶部。AI 设置保多档位 Profiles、Review 保钉钉、Screener 保摘要→明细顺序与 columnsReady 门闩、监控卡片保 R60/R89 版式。测试: 矩阵/ETF 计数保 fork(+factor_rank_research), ETF 采上游新形状; 放量测试 fork 超集; watchlist_batch 测试保留(上游随功能删除); 上游新测试 test_realtime_mode 按 fork 语义改写并注明背离。后端全量 2051 passed / 0 failed; 前端 tsc/vite build 过、ESLint 0 error(顺手修 Review 页 _DtStockTable 下划线命名触发的 rules-of-hooks) | 29 个冲突文件(quote_service/preferences/monitor*/minute_refresh/kline_sync/market_recap/settings/worker + 前端 13 文件) + 大量自动合并 | 高(实时链/分钟链/监控链全动过, 已逐处核对 fork 增强完整) | 回退 merge commit(会同时退掉 v0.2.2 全部新功能) |
| R91 | **外部网页(利弗莫尔趋势)移出「盘面参考」分组**: R88 把它归进了分组, 用户要求回到一级菜单且可拖动。分组成员资格集中于 `BROWSE_GROUP.paths`(R67 设计), 从数组移除 `/external-page` 即完成 —— Layout 与菜单设置共用同一 `isBrowsePath` 判定, 该项自动回到顶层并参与 `nav_order` 排序。已存过顺序的用户: 它出现在原分组块之后的位置(存档顺序里它就在那), 可在设置里拖到任意位置 | `frontend/src/lib/navGroups.ts` | 无(单数组一行) | 把路径加回 `paths` |
| R90 | **AUTH_DISABLED 免登录开关(用户明确选择, 风险已告知)**: 部署在 Cloudflare Access 等外层门后面时, 面板自己的登录页成了第二道多余的门。新增环境变量 `AUTH_DISABLED=1`: 认证中间件对 `/api/` 完全放行(公网也放), `/api/auth/status` 一律报告已登录(手动打开 `/login` 会被前端弹回首页——前端零改动, 复用其"已登录即跳转"逻辑)。安全边界: 默认(未设置)时行为与从前逐字节一致, 有专项测试钉死(未设密码公网 403 / 已设密码无会话 401 / status 字段原样); 开启时启动日志打大字裸奔警告(明文 key/清库/仓位对全网开放)。回退路径: 删掉环境变量重启即恢复, auth.json 里的密码原样保留 | `config.py`、`main.py`(中间件+启动警告)、`api/auth.py`(status)、`tests/test_auth_disabled.py` | 无(默认关闭, 不碰上游认证逻辑本体) | 删开关三处 + 测试文件 |
| R81 | **同步上游 1 提交(回测子进程收尾修复)**: 上游 afbf432 与本 fork 另一会话的 937411b 是**同一个 bug 的两份修法**(终态结果已送达但子进程退出收尾超 10s 时, 不再把成功结果当失败丢弃): 子进程侧 close+join_thread 冲刷两边一致; 父进程侧 fork 版是超集(多一层 join 后兜底排空, 盖住"先看到 Empty 再发现进程已死"的另一个竞态, 带回归测试)。冲突解法: 注释措辞取上游对齐(减少未来 diff), 功能与测试保留 fork 版 | `backtest/worker.py`、`tests/backtest/test_worker_process.py` | — | 回退 merge commit |
| R89 | **参考 FluxDown 重构全局字体、字距与控件密度**: 以 `zerx-lab/FluxDown@3e63139` 的 `web/src/{design,index}.css` 为排版依据，建立中文优先字体回退链（MiSans→HarmonyOS/PingFang/微软雅黑 UI→系统字体）、数字/代码等宽栈、13px 主字号/12px 辅助字号、语义字号行高与克制字距；常用 `h-9` 控件统一为 32px、输入圆角统一 6px，页面标题栏固定最小 52px 并处理长副标题省略，侧栏顶级菜单收至 32px 行高，设置页区块标签从 0.2em 过度字距收至 FluxDown 同档的 10.5px/0.045em。未复制 FluxDown 的字体二进制或颜色主题，不新增远程字体请求；显式像素字号、K 线/分时/图表画布和业务表格列宽保持不动，实时、策略、监控及后端零改动。后端全量可执行范围 `1872 passed`；前端 TypeScript + Vite 生产构建通过，定向 ESLint 0 error | `frontend/{tailwind.config.ts,src/index.css}`、`components/{Layout,PageHeader}.tsx`、三个设置页区块标题 | 低（全局排版令牌会影响语义字号/常用控件高度；金融图表和数据逻辑不变） | 回退本提交恢复 R88 的字体与间距 |
| R88 | **可配置外部单页嵌入“盘面参考”**: 复用「设置 → 扩展页面」增加外部网页名称、HTTP(S) 地址和启用开关，默认启用用户指定的利弗莫尔趋势页；启用后动态加入「盘面参考」分组，沿用现有折叠、隐藏和组内排序。新增独立 iframe 页面，提供刷新、设置和新窗口兜底；浏览器直接连接目标站，后端不代理、不读取第三方内容，也不转发 TickFlow 数据/API Key/牛来登录凭据。iframe 使用 sandbox、严格 referrer 策略，后端拒绝非 HTTP(S) 及 URL 内嵌账号密码；目标站若以后设置 X-Frame-Options/CSP 禁止嵌入，仍可新窗口打开。旧 preferences 缺字段自动取默认值，实时、策略、监控和数据链路均未修改。后端全量可执行范围 `1872 passed`，新增专项 `7 passed`；前端 TypeScript + Vite 生产构建通过，定向 ESLint 0 error | `services/preferences.py`、`api/settings.py`、`lib/{api,navGroups}.ts`、`components/Layout.tsx`、`pages/{ExternalPage,settings/ExtPages,settings/MenuSettings}.tsx`、`router.tsx`、专项测试 | 低（独立展示页与偏好配置；不接触金融数据/实时服务） | 删除 `/external-page` 路由和菜单接线、外部网页设置及对应偏好字段 |
| R87 | **策略监控卡片显示真实策略名 + 全系统更名“牛来”**: 监控规则卡片旧逻辑先按 `scope=symbols` 渲染，导致限定标的的策略监控显示第一只股票代码/名称而非策略名。现对 `type=strategy` 提升显示优先级，复用完整策略池按 `strategy_id` 解析日线、分钟、自定义及叠加策略的当前名称；保存策略规则时后端同时将规则名规范为 `策略监控 · {策略名}`，保证后续卡片与通知口径一致，既有规则无需迁移即可正确显示。用户可见品牌名、浏览器标题、登录/引导、导出页脚、桌面安装显示名、后端应用标题及相关开发文档统一由 Tick Stock Panel/TickFlow 股票面板改为“牛来”；内部 TickFlow 数据源名、API、镜像、可执行文件名和旧数据迁移目录保持不变，避免破坏部署与兼容性。后端全量可执行范围 `1865 passed`，监控专项 `26 passed`；前端 TypeScript + Vite 生产构建通过，定向 ESLint 0 error | `frontend/src/pages/Monitor.tsx`、`backend/app/api/monitor_rules.py`、品牌展示/打包/文档文件、新增策略名称回归测试 | 低（监控执行条件与实时链路不变；只修显示优先级、保存名称和品牌文案） | 回退本提交恢复原名称和卡片标题逻辑 |
| R86 | **策略结果个股弹窗增加“关键价位”视图切换**: 策略页筛选结果原先只能打开通用日 K/分时详情，查看六态趋势、盘中口径和十一类关键价位必须退出后再去个股分析页。现将个股分析弹窗的既有关键价位主体提取为共享组件，策略详情顶栏增加第三个“关键价位”页签，可在同一股票、同一弹窗内往返日 K/分时/关键价位；复用原 `analysisKline`、`stockLevels`、`stockTrend` 查询键和 R74/R76 单票实时刷新顺序，不新增接口或第二套缓存。入口采用显式 prop，仅策略页启用，今日总览/监控/自选/板块等其他共用弹窗保持原样；切股仍重置为日 K，原 footer 插槽只在其既有日 K/分时契约内渲染。前端 TypeScript + Vite 生产构建通过，定向 ESLint 0 error | `frontend/src/components/StockPreviewDialog.tsx`、新增 `frontend/src/components/stock-analysis/StockLevelsPanel.tsx`、`frontend/src/pages/{Screener,StockAnalysis}.tsx` | 低（纯前端复用与 opt-in 接线，不改后端、数据、实时服务或查询契约） | 删除 `enableLevelsView` 接线与共享组件，将个股分析页主体移回原文件 |
| R85 | **补实时修复的重启/并发兼容闭环**: 复审 R84 时发现两个历史系统高概率边界：(1) 启动 30 秒自检已创建完整性修复并用 `QuoteService.paused()` 暂停行情时，实时开关 API 原先先判 paused、直接返回普通 409，前端拿不到正在运行的修复任务 ID；现改为先检查完整性并接续已有任务，仅没有可接续任务的普通数据同步才返回 409。(2) 容器更新/后端重启会让仅存内存的活跃任务 ID 变成 404；实时开关轮询现在静默识别真实 HTTP 404，重新按磁盘状态创建/接续修复，避免再次掉进 `job not found`。数据门禁、写盘暂停和最多两轮保护均保持不变。新增启动自检并发复现测试；后端全量可执行范围 `1864 passed`，前端 TypeScript + Vite 生产构建通过 | `api/settings.py`、`tests/test_data_integrity.py`、`lib/{api,useSharedMutations}.ts` | 低（只调整门禁判定顺序与任务失联恢复，不改修复算法/行情源/数据格式） | 回退本提交恢复 R84 行为 |
| R84 | **修实时行情“点开即触发修复、修完仍打不开”**: 历史完整性门禁检测到停机前盘中快照时，旧流程只创建/复用修复任务并返回 409，用户必须自行等待、再点一次；任务慢时表现为开关始终回弹。改为结构化返回修复任务 ID，所有实时开关入口统一跟踪任务，成功后重新扫描并自动完成原本的开启动作；失败显示任务错误，两轮后仍有坏快照则停止，禁止无限建任务。修复期间仍不调用 QuoteService、实时写盘仍保持暂停，未放宽任何数据安全门禁；监控设置在等待期间禁用重复点击。现场触发样例：2026-08-27 盘中快照、任务 `957af037fa`。专项 55 passed，后端可执行全量 1863 passed，前端 TypeScript + Vite 生产构建通过 | `api/settings.py`、`tests/test_data_integrity.py`、`lib/{api,useSharedMutations}.ts`、`pages/settings/Monitoring.tsx` | 低（只补门禁后的异步续接，不改行情拉取与修复内容） | 回退本提交恢复“修完手动再点” |
| R83 | **整合作者分钟策略完整功能链，同时保留全部 fork 增强**: 同步 upstream/main `afbf432` 后，按作者 `feat-minute-strategy@8e84304` 的最终状态整合分钟策略执行上下文、日线/分钟统一策略池与周期路由、受 Expert 能力及开关门控的盘中分钟增量刷新、分钟回测/回放、分钟图表实时补取、卡片数据更新闪烁，以及作者后续的 worker 终态竞态、Fuyao/custom provider 百分比口径修正。冲突按“作者功能语义优先、fork 增强不丢”处理：策略页保留 R80 的摘要→明细顺序、旧完整结果占位和原子缓存失效，分钟结果不写入日线摘要/明细缓存；Review 保留钉钉配置识别，AI 设置保留多档位 Profiles；矩阵/ETF 测试保留 fork 额外策略计数。补三项并发/失败安全：分钟分区 read-merge-write 复用 repository 写锁，缺配置状态 fail-closed，worker 收到终态后不再多等一轮；固定两项随日期漂移的完整性测试时钟。TickFlow 单一数据源、多 Key、单票实时隔离、轮询放量监控、今日总览/操盘手/挖掘等 fork 功能均未删除。后端全量可执行范围 `1863 passed`，分钟/相关专项 `109 passed`，worker 终态竞态专项 `2 passed`；前端生产构建由推送后的 Docker workflow 验证 | 后端分钟策略/刷新/回测/worker/数据源适配，前端策略池/回测/图表/设置/监控，相关文档与测试（共约 60 文件） | 高（策略引擎、回测、Screener、分钟写盘均为核心链路，已逐处适配） | 回退本提交（会整体移除作者分钟功能链，R1~R82 保留） |
| R82 | **同步作者回测 worker 收尾修复**: 整合 upstream/main `afbf432`。worker 发送终态消息后显式冲刷队列并立即退出，避免大数据量任务因解释器 teardown 超过 10 秒；父进程在终态消息已送达但子进程仍未退出时强制结束进程并保留真实结果/错误，同时记录 `worker_exit_forcibly` 指标。新增对应回归测试 | `backtest/worker.py`、`tests/backtest/test_worker_process.py` | 低(独立 worker 生命周期修复) | 回退本提交 |
| R81 | **整合作者次要分支的“轮询放量监控”并补安全边界**: 仅移植 `feat-volume-delta-alert` 这一项，不合并整条旧分支。全市场实时行情每轮记录股票累计成交量/成交额，与上一轮完整快照求差；跨交易日、09:30/13:00 时段首轮、非连续竞价、累计量回退均不触发，缺成交额时金额口径 fail-closed，不用 0 伪造增量。监控规则新增手数/金额阈值、价格/市值/当日成交额/ST 基础过滤、5 分钟默认冷却与批量合并通知；仅全市场模式可运行，免费自选轮询不会进入差值状态，编辑器明确提示当前模式是否可用。规则阈值或过滤器修改会重置旧冷却态，避免新配置被旧状态压住。保持 TickFlow 单一数据源，不改策略加载/缓存/SSE 链路 | `api/monitor_rules.py`、`services/quote_service.py`、`strategy/{monitor,monitor_rules}.py`、前端规则编辑/监控展示/API 类型/梯队类型收窄、专项测试 | 中(接入实时行情→监控→告警链，但仅新增临时列与独立规则类型) | 删除 `volume_delta` 类型、快照状态/注入函数及前端表单，移除专项测试 |
| R80 | **修同步时"一直 404 job not found / 502"刷屏**: 链条 —— 运行中的任务只存内存, 后端重启(R77 让用户重启过)后正在跑的任务 ID 消失; 数据页还捏着旧 ID 每秒轮询 `/jobs/{id}` → 每秒 404; 重启窗口内代理层 502。页面其实**有**自愈守卫, 但它用 `/404/` 正则匹配错误**文本** —— 而 `request()` 取后端 detail("job not found")当消息时把状态码丢了, 文本里没有"404", 守卫永远匹配不上; 更糟的是拿到 activeJobId 后历史列表停止刷新, 死循环没有任何出口。修三处: `request()` 把 HTTP 状态码挂上错误对象(err.status, 调用方从此不再靠正则猜文本); 自愈守卫改判 `err.status === 404`(保留文本回退), 清 ID 同时失效历史列表让它接回真实活跃任务; job 轮询对 404 不重试(别拿死 ID 反复撞) | `lib/api.ts`、`pages/Data.tsx` | 低(错误对象加字段, 现有 catch 不受影响) | 还原两文件 |
| R79 | **测试全绿: "13 个缺 SDK 失败"逐个查清, 全部消灭(1785 passed / 0 failed)**: 装上 `tickflow` SDK(就在 PyPI 上)后真跑那批被挡住的测试, 发现"缺 SDK"这个标签下积压着四类完全不同的问题 —— (1) `test_ai_provider` 2 条: 上游测试桩掉底层、没配 key, 撞上 fork 多档位兜底的"未配置即报错"(生产行为正确); 测试补一个空档位(回落全局配置)走到钳制逻辑; (2) `test_watchlist_enriched_join` 8 条: `_FakeRepo` 假件缺 fork 后来加的 `overlay_watchlist_live`/`get_watchlist_live` —— **一直被误归类为缺 SDK, 其实早就该修**; (3) `test_watchlist_batch` 3 条: 测试还在打桩旧接口 `get_paid_realtime_client`(fork 已改多 key 池), 且断言"一次全拉完"的旧语义(R30 轮转窗口后 6 只超容量应分两轮) —— 按轮转语义重写; (4) `test_extensions` 1 条: 容器 fastapi 0.141(新版 include_router 懒包装 `_IncludedRouter`, 不再摊平进 app.routes)与项目锁定 0.136.1 不符, 装回锁定版即过 —— **用户机器上 uv --frozen 用锁定版, 从未受影响**; `test_data_integrity` 1 条: R69 铺的 130 天密度让 enriched 分区数变多, 断言按意图放宽(守"坏分区被删后重算写回", 不守总数)。产品代码零改动 —— 全是测试假件失修与环境错配 | 4 个测试文件 | 无(纯测试) | — |
| R78 | **同步上游 3 提交**: 挖掘两处修复 —— enriched 数据并发更新时**重读快照**而非整轮失败(挖掘跑一半赶上数据刷新不再报废)、市场环境数据缺失时挖掘**启动即失败并前置提示**(不再跑到一半才炸)。零冲突自动合并; 上游另开了 `feat/volume-delta-alert` 分支(开发中, 未并 main, 不动) | 7 文件(挖掘运行时/worker/API + MiningWorkbench) | — | 回退 merge commit |
| R77 | **修 R74/R76 造成的监控/策略污染(用户实况: 异动监控和策略出的票几乎没用)**: 单票按需刷新此前"复用"了自选实时叠加层和 kline_daily —— 但那两层有**隐含契约**: "只含自选、只含当天、只由轮询喂"。三处被污染: (1) **监控引擎**在自选实时档直接拿叠加层当股票评估快照、且把日期强制标成 cn_today(quote_service ~L1301) —— 弹窗随手点过的票混着可能过期的价进去后, 异动监控和策略规则评估的就是一份垃圾快照; (2) 自选页 overlay_watchlist_live 无日期守卫, 盘后快照会被弹窗票的旧行盲目顶掉; (3) `_build_daily` 把日期**无条件标 cn_today**, 非交易时段点弹窗会把"幽灵交易日"分区写上磁盘。另发现 R74 慢的真正大头: 实时关着时 live_agg 为空 → 走全量回退, **为一只票把全市场 90 天 enriched 重算一遍**。修法: 单票刷新**彻底退出共享层** —— 结果只进独立的 `_single_live` 缓存(≤200 条), 日期从行情自带 quote_ts 推**真实交易日**(推不出宁可放弃, 不造日期), 不写盘、不算 enriched(顺带把重算开销归零); 消费者只有弹窗那一族: 日K注入兜底读它(带日期守卫)、六态 live 映射只补叠加层没有的票(不覆盖, 轮询喂的永远更新鲜)。监控/自选页/今日总览回到 R74 之前的干净输入。已污染现场: 叠加层是内存, **重启后端即清**; 盘中写脏的当日分区被盘后管道 batch 覆盖自愈, 幽灵分区极端情况可手动数据修正。测试重写 +2(共享层写入口全装警报器: 单票刷新碰到即失败 / 无 ts 拒绝造日期) | `services/quote_service.py`(`_pull_single` 重写 + `_single_live`/`get_single_live`)、`api/kline.py`(注入兜底)、`services/live_quotes.py`(可选并入)、`api/stock_analysis.py`(传 quote_service)、测试 | 低(共享层回到只由轮询喂; 弹窗功能不变) | 还原这批文件 |
| R76 | **弹窗提速: 实时拉取移出响应路径 + 空闲 key 单独服务**: R74 把"现拉行情"放在了日K响应路径上, 弹窗要白等一次网络才出图(用户反馈"点开加载变慢了")。两处改: (1) `refresh_single_background` —— 认领冷却位后把拉取扔进后台线程, 响应**立刻**回现有数据, 带 `live_refresh` 状态(started=后台在拉/fresh=已是新的/off=没 key); 前端看到 started 在 1.5s/4s 各补取一次把新蜡烛接进来, 第二次必落在冷却期内回 fresh, 不会打转。(2) 单票拉取**挑空闲 key**(用户点的方向): 后台轮询每轮占用池里一部分 key(R35 错峰), 单票再去挤同一个 key 撞上的就是限流等待 —— 轮询侧现在记下本轮占用的下标(`_busy_key_idx`), 单票优先挑没被占的, 全忙/轮询关着时随机摊开。顺手修一个被全量跑测出来的**既有 flake**(改动前也挂): mining_manager 三个 finisher 和 cancel() 都是"先翻终态状态、后落终态事件", 轮询方看到终态去读事件流会撞上"还是 running"的窗口 —— 统一改成**事件先落、状态后翻**, 15 轮连跑无失败。测试 +3(started→fresh 节奏/无 key 说 off/空闲 key 优先) | `services/quote_service.py`(拆 claim/pull + background + `_pick_single_client`)、`api/kline.py`(非阻塞 + live_refresh)、`services/mining_manager.py`(写序)、`lib/api.ts`、`pages/StockAnalysis.tsx`(补取节奏) | 低(refresh_live 不传时行为不变; mining 只动写序不动语义) | api 里改回 refresh_single 同步调用即回 R74 |
| R75 | **修"每天自动同步会被僵尸任务静默挡掉"**: 用户实况 —— 26 号发现有些数据停在 24 号, 手动点同步才更新到 25 号。链条: 卡死任务的自愈(reap_stale)只挂在 `/run` 和 `/jobs/{id}` 两个 **HTTP 端点**上(上游注释"任意轮询都能自愈"), 但轮询只有浏览器开着才发生; 定时同步 15:30 到点时没人开页面, 一个卡死的 running 任务让 `_run_tracked` 的单飞判定天天返回"已有活跃任务"→ **静默跳过, 数据停更**; 手动点同步恰好走先 reap 的那条路, 所以一点就好 —— 而"没人看页面时也能跑"恰恰是定时任务存在的意义。修法: 把前置 `reap_stale()` 放进 `JobStore.create()` 本身(单飞检查前永远先回收), 让**每个**入口(定时管道/开机完整性自检/分钟K按钮/手动同步)统一拿到自愈, 不用逐个记得补; reap 失败不阻断, 按现状单飞。正常推进中的任务(心跳新鲜)照旧去重, 不受影响。测试 +2, 卡死挡 create 那条在旧代码上真实失败。另: "容器 15:30 没开机"那类漏跑, 上游 R69 并入的开机完整性自检(检测缺分区→自动建修复任务)已覆盖, 两个洞现在都有兜底 | `services/pipeline_jobs.py`(create 前置 reap)、`jobs/daily_pipeline.py`(注释)、`tests/test_job_stall_and_cancel.py` | 低(reap 本就设计为可随时调用; 多调一次幂等) | 还原 create() 里那一段 |
| R74 | **个股分析弹窗"点开就是最新"(单票按需实时)**: 弹窗的 K 线只到最后一个同步日 —— 六态条能亮「盘中口径」是因为趋势接口读自选实时叠加层, 但后台实时开关平时是**关**的, 叠加层没人喂, 日K注入(`_maybe_inject_live_candle`)自然没料可捡。修法不是再开一条口径, 而是给 QuoteService 加 `refresh_single(symbol)`: 现拉**这一只**的行情, 走与自选实时**完全相同**的落盘链(建日K行 → 增量 enriched → overlay 进叠加层), 于是日K注入/六态盘中口径/决策台读到同一份数据。约束: 只走 TickFlow(fork 铁律); 每票 **15s 冷却**且失败也占位(反复开关弹窗、坏 key 连点都不烧配额); 无 key/失败一律静默 False, 图表退回收盘口径不报错; 网络请求在锁外、写盘段拿 `_fetch_lock` 与后台轮询串行(§6.2)。接线: `/api/kline/daily?refresh_live=1`(服务端先喂叠加层再返回), 弹窗两处查询带上此参 + `refetchOnMount: always`; 日K回来后补一次 trend/levels 失效 —— 它们是并行发的, 可能赶在叠加层落地之前。测试 +4(无 key 静默/走 overlay 链/冷却短路/失败占位) | `services/quote_service.py`(+`refresh_single`)、`api/kline.py`(+参数)、`lib/api.ts`、`pages/StockAnalysis.tsx` | 低(默认不带参时行为与之前完全一致; 带参失败也只是退回原行为) | 前端两处查询去掉 refreshLive 参数即回 |
| R72 | **按 CONTRIBUTING 全文自查 + 修掉六处并发写隐患**: 上次(R70)查的是二开守则(扩展结构), 这次把 CONTRIBUTING.md 的金融数据契约(§3)、插件化(§4)、并发(§6.2)逐条对到 fork 代码上。**查过没问题的**: 涨停/炸板/连板全取 enriched 预计算列不自己判(§3.2); change_pct 全链小数制口径一致, _fmt_pct ×100 只在展示层(§3.1); 交易窗口按数据行数(交易日)算, timedelta 只用于超取 fetch 范围(§3.3); 信号年龄用同 tzinfo 差值, 无时区混用; fork 服务零处直连 TickFlow SDK, 全走 repo 抽象(§4)。**修掉的**: R68 在操盘手账本上修过的"裸 write_text + 读-改-写无锁", 在 fork 另外六个自建 JSON 存储里原样存在(§6.2 明确要求锁/原子替换) —— 最危险是 ai_signals.json: 今日总览定时、个股分析页、多个操盘手 refresh 并发写同一文件, 撕裂后 load_all 回空、下一次保存把全部信号清空, 和丢操作员同一形状。按守则"出现真实重复再提取"(第 7 处重复)抽 services/json_store.py(lock_for 按文件互斥 + atomic_write_json 临时文件+fsync+os.replace), 六个店(stock_signal/workflow/mining_autopilot_store/seesaw_store/today_ai_store/today_prefs)全部接上; 操盘手账本不迁 —— 它另有 .bak 回退与 StoreError fail-closed 语义且有测试守着。测试 +7, 其中两条并发测试在旧代码上真实失败 | 新增 services/json_store.py、tests/test_json_store.py; 改上述六个 store | 低(键与文件格式不变, 只加锁与原子化) | 还原这批文件 |
| R71 | **同步上游 9 提交(v0.2.1 之后)**: fuyao 同花顺官方实时行情插件(默认 hidden)+插件 Key 界面配置、**数据源能力卡片标签式路由**(每个数据集单独选提供方 + TickFlow 档位要求徽标)、自定义源百分制涨跌幅自动归一小数制、主线维度映射 Series 报错修复、Codex CLI 状态检测实跑 --version、PR #193 的 AI 调用放开 max_tokens、监控新建规则默认通知事件改选股结果、macOS dev.sh 修复。唯一冲突 `DataSources.tsx`: 我们 R58 期自己做过"按数据集逐项路由"下拉, 上游这次把同一件事做成了正式功能且更完整(能力卡片+档位徽标+服务中标识)——按二开守则"不平行实现第二套"**整块让位给上游**, fork 无独有功能损失(明文 key 列表在独立的 TickflowKeys 页, 不受影响)。ai_provider 自动合并干净, 多档位兜底链完好(测试守着) | 22 文件(上游侧); 冲突仅 `DataSources.tsx` | — | 回退 merge commit |
| R70 | **按上游二开守则(docs/secondary-development.md)自查并整改**: 逐条对照后两类结果 —— **整改**: (1) fork 页面的 React Query 键此前散在 14 个文件里内联写(守则 §3.4 明令查询键集中于 `queryKeys.ts`), 其中 `'ai-profiles'`/`'workflows'` 各自重复出现在两个文件 —— 正是改一处漏一处、缓存悄悄失联的形状; 全部收进 QK 的 `[fork 增强]` 段(22 个键), 上游自己的内联键不动(改动最小原则)。(2) 台账补上守则 §8 要求的**上游基线锚点**(v0.2.1=17ca245)、**高冲突热点复核清单**(§5.2 的 7 个文件里我们动过哪些、每次合并后复核什么)和 `scripts/upgrade_check.py` 预检约定。**查过没问题的**: 无绕过 api.ts 的裸 fetch/URL 拼接; 无复制核心页面后长期分叉; 后端无继承大型编排服务(操盘手/工作流/今日总览全是独立模块+组合), AI 调用全走 `ai_provider` 单一链路; 工作流驱动复用挖掘/回测引擎公开入口而非平行实现; `strategy/engine.py` 的 watchlist_only 注入正对应守则"按需扩展"的 CandidateFilter —— 守则明说单用例不建接口, 按 L3 管理并已进复核清单; ruff 报的 RUF100 类噪音上游自身同样触发(main.py 29 条), 属 ruff 版本/配置基线, 不追 | `lib/queryKeys.ts` + 14 个 fork 页面/组件、`FORK_NOTES.md` | 低(键值字符串未变, 只是引用集中; 行为零变化) | 还原这批文件 |
| R69 | **同步上游 v0.2.1→main(38 提交)**: 上游把 v0.2 正式并进 main 并追加一批。拿到的新东西: **异动监控全链路**(偏离值计算/独立监控页 `/abnormal`/系统告警接入 + ST 过滤, 页面已进侧栏与菜单设置)、**AI 生成自定义信号条件**(上游试验性合并的 PR #193 + 12 个测试修复)、**自选分组多组并存**(M:N, `group_ids`, 搜索下拉可把已加标的继续加进其他组)、**同步卡死判定改进度停滞 + 协作式取消 + 执行槽所有权**(慢带宽冷启动不再被误杀; 与我们 R55 撞车, 上游版本更完整 —— 基础设施整体取上游, `kline.py` 三处旧 `JobCancelled` 调用点对齐新 API)、**停机缺口检测与自动修复**(盘中停机次日开实时留下的中午快照不再被"只刷今天"分支永久留存)、僵死 enriched 发布标记修复、ETF/指数矩阵链路不再要求股本字段、监控中心新建标的展示重构、数据源插件化文案与引导页去品牌化。冲突 14 个文件, 原则**基础设施取上游、fork 功能保留**: `daily_pipeline` 取上游完整性自愈的同时**保留 fork 的历史稀疏检测**(上游只扫最近 7 天, 年尺度大洞仍要稀疏检测兜底, 两个互补); `ai_provider` 保留多档位兜底链、叠加上游的输出上限/输入预算检查(预算检查放在进档位循环**前** —— 预算超了换哪档都超, 不该触发兜底); `watchlist` 的缓存失效与上游 `_REVISION` 并存; `watchlist_ai_group`(R26) 适配 M:N。上游自身修了三处: 时区脆弱测试(`date.today()` vs `cn_today()`, UTC 16-24 点必挂)、integrity 测试铺足历史密度避免与稀疏检测打架、`WatchlistGroups` 新代码踩 fork 的 eslint 规则 | 全仓 347 文件(上游侧); 冲突解在 `pipeline_jobs`/`api/pipeline`/`daily_pipeline`/`ai_provider`/`watchlist`/`main`/`kline` + 前端 7 页 | —(这就是同步本身) | 回退整个 merge commit `5709f2c` |
| R68 | **修操作员凭空消失(账本会被写坏, 写坏之后被当成空账本)**: 建了三个操作员, 点一下"改定时", 剩一个。链条有三环: (1) 前端 `<input type="time">` 打一个时间会**分几次** onChange(时段一次、分段一次), 每次发一个 PUT —— 顺带一提, 中间那几次还会把定时短暂地设到 `01:30` 这种莫名其妙的时间上; (2) 同步路由跑在 FastAPI 线程池里、定时任务跑在调度器线程里, 几个请求**同时** `Path.write_text` 同一个 JSON —— 而 `write_text` 是**先清空再写**, 撞上就留下一个半截的文件; (3) **最要命的一环**: `_read()` 把解析失败 `except` 掉、返回 `{"traders": []}` —— 于是紧接着的一次 `save()` 写回去的是一份"只剩当前这个人"的文件。前两环只是**短暂**的损坏, 第三环把它变成了**永久**的删除, 而且全程没有任何报错。三环各修各的: `_write` 改成**原子落盘**(同目录临时文件 → `flush`+`fsync` → `os.replace`, 换之前留一份 `.bak`), 所有"读—改—写"进同一把 `RLock`(名额检查和写入也必须在同一把锁里, 否则同时开的两个会双双通过 `MAX_TRADERS` 检查), `_read` 读不出来时**先退 `.bak`、再抛 `StoreError`**(→ 503)而**绝不返回空**; 另加 `mutate(id, fn)` 让改设置的路由在锁里就地改, 不再"先 get 出来捧着、隔一会儿再 save"(一次 AI 决策要跑好几分钟, 这中间人完全来得及去改设置, 旧副本一回写就把它盖掉了); `save()` 发现人已被删就**不写**(否则一次跑了几分钟的决策会把刚删掉的操作员连账一起复活)。前端把两个输入框改成停手 0.6 秒才发 —— 改成 14:30 本来就该是一次保存, 不是四次。测试 +8, 全部在旧代码上失败(其中并发那条复现出的正是 `pt.get()` 中途返回 `None`, 也就是"人不见了") | 改 `services/paper_trader.py`(`_read`/`_write`/`save`/`delete`/`create`/`reset` + `_LOCK`/`mutate`/`StoreError`)、`api/paper_trading.py`(设置/本金/定时三个路由走 `mutate`)、`main.py`(`StoreError` → 503)、`pages/PaperTrading.tsx`(`useDebouncedField`) | 低(只动这一个功能的存储层; 老账本文件格式没变, 直接能读) | 还原这四个文件; `.bak` 会留在 `user_data/` 里不影响老代码 |
| R67 | **「盘面参考」变成菜单里真正的一行**: R57 建这个分组时, 表头是"挂"在**这一组第一个可见成员**上的 —— 分组本身在「设置 → 菜单」里没有对应的一行。于是三个毛病: (1) 想挪它没得挪, 列表里只有四个成员没有分组; (2) 去拖成员也不一定挪得动它 —— 表头挂在谁身上取决于谁碰巧排最前, 拖了个排在后面的等于白拖; (3) 成员一旦被拖散到顺序两端, 表头和成员会被中间的普通菜单**切开**, 看起来就是"分组卡在中间不动"。现在分组有自己的 id(`group:browse`), 和普通菜单项一样进 `nav_order` 参与排序: **拖它就是拖整块**, 四个成员从顶层抽出来只作它的子项(仍可各自单独隐藏, 组内也能单独排序); 分组行自己也能隐藏 —— 一下藏掉整块。存盘时成员紧跟在分组行后面写进同一条扁平 `nav_order`, 顺序本身就连成一块, 不可能再被切开。**升级不动位置**: 老数据里没有 `group:browse`, 走"新条目插回默认位置"那条路, 默认位置特意放在「个股分析」之后 —— 正是老逻辑算出来的那个位置, 所以已经存过顺序的人看到的位置和以前一样, 只是从此拖得动。顺带把侧栏那段重复的菜单项 JSX 抽成 `PlainNavLink`, 顶层和组内共用一份(原先组内那份是把整段抄了一遍, 加个徽标要改两处) | 新增 `lib/navGroups.ts`(分组定义 + `splitBrowseGroup`/`composeNavOrder`, 两边共用一份定义); 改 `components/Layout.tsx`(分组进 `nav` 数组, 成员从顶层抽出, +`PlainNavLink`)、`pages/settings/MenuSettings.tsx`(分组行 + 组内嵌套拖拽上下文) | 低(纯前端菜单渲染与排序; `nav_order`/`nav_hidden` 后端不校验 id, 无需改接口) | 还原这三个文件 |
| R66 | **操作员能触发新数据(信号旧了让它现场重出)**: R65 补齐了信息面, 但漏了一件事 —— 系统里大部分东西(六态趋势/通道三档/关键价位/日 K 走势)每次都是**现算**的, 本来就不会旧; 只有 **AI 个股信号是缓存的**, 会摆着一个三周前的判断不动。而缓存信号最危险的一点是: **它读起来和今天刚出的一模一样** —— 不标年龄的话, 模型没有任何办法知道自己在拿旧判断当今天的依据。所以: (1) 细看里的 AI 信号一律**带上生成时间与年龄**, 超过 3 天标「已过期, 建议 refresh」; (2) 看盘那一轮除了 `focus` 还能给 `refresh` —— 列进去的会**现场真跑一次 AI 个股分析**再进细看(重出成功的自动进细看名单: 它开口要新信号, 就是打算据此判断)。上限 3 只/次: 每次都是真实开销, 无上限的话模型一句"全都刷一遍"就能把一次决策拖成几十次调用; refresh 同样受"本轮候选 + 自己持仓"的范围约束 —— 那一步会花钱, 更不该被一个范围外的代码触发。**关键的一处**: 重出时把操作员的 AI 档位**放掉**, 走系统默认那条链 —— AI 信号是全局缓存的**系统产物**(个股分析页上人看到的就是它), 用操作员自己的模型生成的话, 这份"系统数据"就变成了那个模型的意见, 而且会被**别的操作员**当成系统数据读到, 等于绕开隔离互相影响; 跑完要还回来, 不还的话紧接着的下单轮会用系统默认模型, 这一天的成绩就记到错误的模型名下了(两条都有测试守着)。测试 +8 | 改 `services/paper_trader_run.py`(+`_signal_line`/`refresh_signals`, `parse_focus` 返回二元组, `run_once` 在细看前先重出)、`pages/PaperTrading.tsx`、`lib/api.ts` | 中(一次决策可能多出最多 3 次 AI 调用; 全部有上限与范围约束, 失败不影响下单) | `run_once` 里去掉 refresh 那一段即回到 R65 |
| R65 | **操盘手的信息面纠偏: 禁的是外部信息, 不是"少给它看"**: R59 把"不能联网"错做成了"只能看一小段摘要"——模型每天只拿到候选清单的一行行摘要, 那考的就不是"这套系统的信息够不够用", 而是"一段摘要够不够用"。界限本来是: **新闻/公告/研报/行情网站一律不许用, 但这套系统自己算出来的东西一样不该藏**。改成**两轮**: 第 1 轮「看盘」给候选与账户, 让它自己挑最多 6 只想细看的; 第 2 轮把系统里关于那几只的东西一次给全 —— 六态趋势(状态/天数/翻转触发价/关键点/系统建议)、Keltner 三档与结论、`summarize_levels` 的十一类关键价位(与个股分析图上画的同一批)、近 20 日走势(带涨停/跌停/炸板标注 + 收盘在均线哪一侧 + 区间), 以及这只票**已有的 AI 个股分析报告**。刻意分两轮而不是一次给全: 所有候选的完整明细会长到把账户和规则都挤出上下文, 而且大半是它根本不打算买的票。**"看图"就是这段走势摘要** —— 模型读不了图片, 但"哪天涨停、收在均线哪一侧、这段区间多宽、缺口在哪"正是人看图时真正在读的东西。细看范围仍受限于**本轮候选 + 自己的持仓**: 放它细看一只不在候选里的票, 等于让它出了这本账的选股范围, 而两本账能对照的前提就是各自只在自己那个池子里选(凭记忆报的代码会被丢掉)。看盘轮失败不让这一天报废 —— 退回只看摘要下单。提示词也改了措辞: 只写禁令会让模型保守到不敢用系统给的东西, 所以明写"系统里的东西你都可以用, 想细看哪几只就说"。测试 +8 | 改 `services/paper_trader_run.py`(+`symbol_detail`/`_kline_story`/`_recent_report`/`_allowed_symbols`/`parse_focus`/`LOOK_PROMPT`, `run_once` 走两轮)、`pages/PaperTrading.tsx`(文案纠正 + 显示细看了几只)、`lib/api.ts` | 中(每次决策多一次 AI 调用; 失败有退路, 撮合与账本未动) | `run_once` 去掉第 1 轮即回到 R59 |
| R64 | **看板收进「盘面参考」, 并从根路径挪走**: 和连板梯队/概念分析/行业分析同一类 —— 打开看两眼有概念, 但不产出任何可执行的东西。除了收进折叠分组, 还把它从 `/` 挪到 `/dashboard`, 根路径改为跳今日总览: 一个展示型的页面不该是每次打开应用第一眼看到的那一页, 决策汇聚层才该在那个位置。`/overview` 的旧跳转跟着指向 `/dashboard`。**顺带修一处会静默失效的地方**: 设置→菜单 里内置页清单还按 `/` 登记看板, 路径改了之后隐藏/排序对它就失效了 —— 那种失效不报错, 只表现为"我明明勾了不显示, 它还在"; 同时补上原来漏登记的「今日总览」和「AI 操盘手」(它们此前同样隐藏不掉) | 改 `router.tsx`、`components/Layout.tsx`、`pages/settings/MenuSettings.tsx` | 低(路由与导航; 页面本身未动) | 把 index 路由改回 Dashboard |
| R63 | **操盘手: 持仓只数上限 + 两本账各自的本金**: (1) **同时最多持有几只**可设(默认 10, 上限 50)。这个上限存在的理由不是"防止乱买", 而是**让成绩可比** —— 一个能同时拿 40 只的账户跑出来的曲线, 本质上是在拿分散度换波动, 和只拿 5 只的账户不是同一回事; 两本账要对照, 这个数就得对齐, 所以它是**操作员级**设置而不是按账设。只挡**新开**的仓, 已有持仓加仓不受限 —— 挡加仓等于逼它去开一只新的, 正好和"控制分散度"反着来。上限会写进给模型的上下文: 不写的话它会开一堆买单、大半被拒, 那一天的决策就废了一半。(2) **本金移到每本账自己身上**, 两本可以设成不同的数(比如全市场那本给多一点看分散效果), 收益率各按各的本金算所以照样可比。改本金会**连带把那本账重置** —— 中途换本金而不重来的话, 收益率的分母变了但历史成交还在, 那条曲线就再也读不懂了; 反过来, 单纯的「重置」**不动本金** —— 我改过的设置不该被一次重置抹回默认值。老账本没有本金字段的就地补上(取操作员那份, 行为不变)。测试 +15 | 改 `services/paper_trader.py`(本金进账本 + `max_positions` + `clamp_max_positions`, `apply_order` 加上限)、`services/paper_trader_run.py`(上限写进上下文并传下去)、`api/paper_trading.py`(+2 端点)、`pages/PaperTrading.tsx`、`lib/api.ts` | 低(有迁移路径与测试守着) | 本金移回操作员级, 去掉 `max_positions` |
| R62 | **去掉 AI 服务商预设, 一律 OpenAI 兼容**: 用户明确"不需要这些玩意选择, 默认兼容 openai 就行, 我自己填链接密钥模型"。删掉两处: AI 档位表里的「套用常用配置…」下拉, 以及设置页那张「快速预设」卡片(DeepSeek / Kimi / OpenAI / 炸鸡中转站 / Codex CLI 的按钮组)。理由不只是"用户不喜欢": 那种清单只在你正好用清单里那几家时省事, 否则**先得从里面挑一个再把三个字段全改掉, 比直接填还多两步**; 而且它会过期(模型名换代、中转站换域名), **过期的预设比没有预设更误事** —— 照着填一遍发现模型不存在, 还以为是自己填错。现在只剩三个输入框: 接口地址 / 模型 / API Key, 占位符改成说明性的("服务商给的模型名, 原样填")而不是某个具体厂商的值。配置卡片标题也不再随匹配到的预设变化 —— 预设都不给选了还显示「DeepSeek 配置」, 会让人以为自己在某个选项里。**注意**: Codex CLI 那一路原来只能从预设按钮进入, 预设去掉后界面上没有入口了(已配置的仍然能跑, 后端逻辑一行没动); 需要的话再单独给它一个入口 | 改 `pages/settings/AiProfiles.tsx`(删 PRESETS 与下拉)、`pages/settings/AI.tsx`(删「快速预设」卡片与 `handlePreset`, 标题固定) | 低(纯删 UI; 后端 provider 字段默认就是 openai_compat, 判定逻辑未动) | 把两处预设列表加回去 |
| R61 | **操盘手: 两本账 / 定时 / 生命线实时例外**: (1) **每人两本独立的账** —— 「全市场」只能从全市场候选里选, 「我的自选」只能从我圈的票里选。这不是两个功能, 是这套系统最想问的那个问题的**对照组**: 我这份自选到底有没有价值。同一个模型、同一天、同一套判定口径, 两条净值曲线的差就是我选股这件事的价值 —— 所以两本账严格分开(各自一份初始资金、现金持仓流水互不相干), 共用任何一样这个对照就废了。全市场候选**复用系统已有的「结论」层**(按成交额粗筛 → 三档通道 → 只留偏买那几档), 刻意不另造一套排序: 两边口径不同的话, 比出来的是"两套规则谁强"而不是"我的自选有没有用"。「需要行动」只进自选那本 —— 那讲的是我自己持仓的风险, 漏给全市场那本等于让对照组看到了我的持仓。老数据就地迁移进**自选**那本(之前那版的上下文本来就是自选口径, 记到全市场会把成绩安到错误的对照组上)。(2) **定时** —— 长期观察靠人记得点按钮是不成立的, 漏几天就是净值曲线上几个说不清的缺口。默认**关闭**且默认时间在收盘后(收盘价出来了才有得算); 一次定时**先查生命线再让 AI 决策**, 顺序不能反: 清仓会腾出现金, 反过来模型是拿着一笔本该已卖掉的持仓在做判断。关掉/删掉操作员时任务会真的从调度器上摘下来 —— 只管添加的话"我明明关了它还在跑"会持续烧 AI 额度。(3) **生命线是全流程唯一的实时例外** —— 操盘手其余全部走收盘口径(拿盘中价成交等于给模型一个它复盘时看不到的价格)。跌破 20 日线是硬纪律不是策略, 所以这一路**完全不问 AI**: 让模型有机会"再看看"就等于把纪律变成建议。触发可以看实时价, 但生命线本身永远是收盘均线 —— 拿实时价掺进均线, 这条线自己就会跟着盘中抖; 不足 20 根就没有生命线, 不拿 12 根算个假的去强平。强平在流水里单独标「纪律强平」, 不和模型自己的决定混在一起。测试 +20 | 改 `services/paper_trader.py`(两本账 + 迁移 + 定时字段, 撮合改吃账本)、`services/paper_trader_run.py`(scope 化 + `market_candidates` + `check_lifelines`)、`api/paper_trading.py`(按账本的端点 + 定时 + 生命线)、`main.py`(启动装定时)、`pages/PaperTrading.tsx`、`lib/api.ts`; 新增 `services/paper_trader_schedule.py` | 低(R59 刚落地, 没有历史数据要担心; 迁移路径仍有测试守着) | 回到 R59 的单账本形态 |
| R60 | **全站统一版式**: 先量了一遍才动手 —— 内容宽度散着 6 个值(1500 / 1440 / 1280 / 1100 / 6xl / 7xl)外加好几页完全不限宽; 页面留白 5 种组合(`px-8 py-6` / `px-4 py-5` / `px-6 py-5` / `p-4 md:p-6` / `p-1.5`); 今日总览与市场环境还各自手搓了页头。后果不是"某一页丑", 而是**切页面时内容边界每次都在跳** —— 同一块卡片在今日总览宽 1500、在复盘宽 1280、在自选顶到边, 眼睛每次都要重新找基准线。这种不齐单看任何一页都不明显, 只有连着翻才难受, 所以最容易一直没人改。新增 `PageShell` 把"一页长什么样"收在一处, 只定三件事: **内容宽度**三档(`full` 表格页不限宽 —— 限宽只会逼出横向滚动条; `wide` 1440 看板页; `read` 1100 文字页, 一行太长眼睛回不到行首), **页面留白**一档(`px-3 pb-4 pt-3 lg:px-4`), **纵向节奏** 12px。限宽层与留白层刻意分开 —— 合成一层的话宽屏上内容会贴着限宽边缘, 左右留白反而消失, 那正是原来几页"顶到边"的原因。今日总览/市场环境/指数三页的手搓页头改走 PageShell; 其余页保留 PageHeader 只统一留白与宽度。**Dashboard 的渐变头部条刻意保留** —— 它带着日期选择器、行情时延、实时状态, 是看板自己的仪表而不是页标题, 硬塞进 PageHeader 会把这些挤成一行小字; 只把它的 `p-1.5`(全站最窄)并入标准留白。改后: 宽度只剩 1440/1100 两个值, 留白 16 处全部统一, 弹窗那几处 `px-5 py-4` 属于对话框自己的一套, 不动 | 新增 `components/PageShell.tsx`; 改 `pages/` 下 14 个页面的外壳(Today/Regime/Indices 换 PageShell, Data/Financials/Review/Concept/Industry/Monitor/Analysis/Settings/Branding/Dashboard/Backtest/Mining/Screener/StockAnalysis/LimitUpLadder 统一留白与宽度) | 低(只动外层容器的 className 与页头, 页内内容与数据路径逐字未改; tsc/eslint/build 全绿, 告警数与改前一致) | 逐页改回原 className |
| R59 | **AI 操盘手(模拟交易)**: 让模型只用本系统给的信息自己炒股, 长期观察下来回答一个问题 —— 这套系统给的信息, 够不够一个模型据以赚钱? 跑不出来, 差在哪一块就是下一步该改的地方。所以每一笔都留着当时的理由, 那份记录才是真正的产出。**三条硬约束都是结构上的, 不靠提示词客气地要求**: (1) 只能用系统里的信息 —— 模型拿到的是一段服务端拼好的纯文本, 没有工具、没有联网出口, 想查也没有手; 上下文里每一块都注明来自哪个模块, 复盘时对得上。(2) 操作员之间互相看不见 —— `build_context` 只按传进来的那**一个** trader_id 读持仓与历史, 别人的持仓/成交/理由一个字都不进来(有测试拿另一个操作员的独门理由去搜, 搜到就算漏)。界面上也刻意不做"所有人持仓一览": 我自己看完再去调提示词, 同样是把隔离破坏掉。(3) 撮合按 A 股规矩 —— 100 股一手、T+1 当天买的不能卖(加仓会刷新这个时钟, 否则可以靠"昨天买过一手"绕开)、按收盘价成交、佣金印花税滑点两边都按不利方向算。放宽任何一条, 跑出来的收益都不能拿来判断系统好不好。**操作员的名字就是模型名** —— 这张表要回答的是哪个模型用同一份信息做得更好, 名字里带别的只会看走眼; 每个操作员绑定自己的 AI 档位且**不走 R56 的兜底链**: 悄悄换成另一家会把成绩记到错误的名下。拒单也留痕("想买但钱不够"和"没想买"是两件事, 只记成交的话复盘时看到的是一段安静的空窗); 模型输出解析不出来就记成"这天没交易"并保留原文, 不硬猜(猜错一笔比空一天糟得多)。详情弹窗有一个「它看到了什么」页 —— 要判断信息够不够, 先得看清到底给了什么, 那里也是核对隔离的地方。测试 +27 | 新增 `services/paper_trader.py`(账本+撮合+解析)、`services/paper_trader_run.py`(上下文+决策)、`api/paper_trading.py`、`pages/PaperTrading.tsx`、`tests/test_paper_trader.py`; 改 `main.py`(挂路由)、`router.tsx`、`Layout.tsx`、`lib/api.ts` | 无(纯新增的一页与一套存储, 不碰任何既有路径) | 删这几个文件与路由注册 |
| R56 | **多 AI 档位 + 按优先级自动兜底**: 用户反馈"有时候 token 没能自动切换到下一个"—— 查下来**这个功能根本不存在**: 原来只有单个 provider / 单个 key / 单个模型, 一家用完额度整个 AI 功能就停摆。改成一个**有序列表**(顺序即优先级, 不另设 priority 字段: 两处表达同一件事迟早打架), 排在前面的先用, 那一档服务不了就顺位往下试。实现用 `ContextVar` 记住"当前正在试哪一档", 几个 `current_*` 读它优先 —— 这样 `_run_openai_once` / `_stream_openai` / `_openai_client` 里那一堆读配置的地方一行不用改; 用 ContextVar 而不是全局变量是因为并发请求可能各自在试不同的档, 全局会串档(那种 bug 只在有并发时才现)。**两条分寸是这个功能的全部要害**: (1) 只有"这一档服务不了"才换档 —— 401/402/403/429/5xx/连接类/模型不存在换档, 400/422 这种请求本身的错误**不换**: 换谁都一样失败, 挨个试等于把每个 key 都白烧一次往返, 还会把真正的报错埋进一句"都失败了"; (2) 流式只在**吐出第一个字之前**能换档 —— 已经流给用户的收不回来, 中途换档会把两家的输出接在一起, 读起来通顺但那是拼的, 比直接报错更糟。全部档位失败时错误消息逐档写明各自的原因(用户要能判断是该充值、该换 key 还是对面在抽风); 只有一档时原样抛原错, 不包一层。**老配置零改动照跑** —— 没配过多档时从旧字段合成一条, 否则升级这版等于把所有人的 AI 关掉。设置页给一个可上下移动的表, key 脱敏下发、留空 = 沿用原来那条(前端手里没有明文, 每次保存都要求重填等于逼人把所有 key 再贴一遍)。测试 +28 | 改 `secrets_store.py`(+`list_ai_profiles`/`save_ai_profiles`)、`services/ai_provider.py`(+`_ACTIVE_PROFILE`/`_should_try_next`/两个兜底循环)、`api/settings.py`(+2 端点)、`lib/api.ts`; 新增 `pages/settings/AiProfiles.tsx`、`tests/test_ai_failover.py` | 低(单档时行为与原来一致, 有测试守着; 兜底只在多档时才起作用) | 清空 `ai_profiles` 即回到单档行为 |
| R55 | **挖掘/回测的历史记录可以删了**: 三处历史原来都只进不出 —— 工作流(两页共用)、自动挖掘会话、挖掘运行。攒久了列表全是没用的失败记录, 而每次挖掘运行还各自带一个产物目录, 是真占盘。三处都补上删除(store 层 + `DELETE` 端点 + 界面按钮)。**唯一要守的规则是"正在跑的不给删"**, 三处的原因是同一个 —— 删的是记录, 跑的那一路并不知道: 工作流会被后台节拍下一拍写回来(表现是"删不掉"); 会话删了正在跑的那一轮没人收尾; 运行的目录 worker 还在写, 删掉后它会把目录重建成半个残骸, 之后 `list_runs` 扫到一条读不出来的记录。所以三处都是"先停/先取消, 再删", 按钮在跑着时就禁用并写明原因, 不让人点了才知道不行。工作流开的会话也不给单删(R53: 它是那条工作流的一次「重开」, 工作流还要靠它接着推)—— 要清就删工作流。删运行是 `shutil.rmtree` 整个目录(只摘 manifest 等于假删), 越界的 run_id 走既有的 `_run_dir` 校验, 有测试专门守这条(删除是破坏性操作)。顺带补上**「以前的 N 个会话」列表** —— 自动挖掘面板原来只跟随最新会话, 旧记录既翻不出来也删不掉。三条确认框都写明"候选方案不受影响, 删的只是这条记录"。测试 +14 | 改 `services/workflow.py`、`services/mining_autopilot_store.py`、`services/mining_jobs.py`(各 +`delete`)、`api/workflows.py`、`api/mining.py`(+3 个 DELETE)、`components/backtest/WorkflowPanel.tsx`、`pages/backtest/MiningAutopilot.tsx`、`pages/backtest/MiningWorkbench.tsx`、`lib/api.ts`; 新增 `tests/test_research_history_delete.py` | 无(纯新增端点与按钮; 既有读写路径未动) | 删掉三个 DELETE 端点与对应按钮 |
| R54 | **回测/挖掘改成「AI 拿内置的那套自己跑」为主, 手动配置默认收起; 顺带修掉「结果解析失败」**: 用户要的能力其实已经有了 —— 回测工作流本来就让 AI 从 `engine.list_strategies()`(作者给的内置策略, 排除 research_only)里自己挑、自己配风控, 挖掘工作流同理从内置因子里挑; 只是这两页都把一整列手动配置摆在主视野, 工作流被挤成边角一条, 读起来像"得先自己配完才能跑"。所以改的是版面与默认值, 不是能力: 两页的手动配置栏都做成可收起并**默认收起**(共用 `researchManualOpen`), 收起后工作流占满宽度成为第一屏。收起不是隐藏事实 —— 回测那条收起栏把仍在生效、且会原样透传给工作流的口径(本金/持仓上限/总仓位/佣金印花税滑点/建仓口径)写在一行里, 并注明"AI 只动策略与风控那几个旋钮"; 挖掘那条写资产/因子数/区间。不写出来就成了看不见的默认值, 那比多占一列更糟。展开后所有控件逐字未动, 状态也一直都在(收起只是不显示)。**另修一个真 bug**: 单次回测的 SSE `done` 负载漏了 `_json_safe`(优化器与 walk-forward 两路都有)—— 零波动区间算出的 nan sharpe/sortino 让 `json.dumps` 写出裸 `NaN`, 那不是合法 JSON, 浏览器 `JSON.parse` 直接抛, 界面上只剩一句"结果解析失败", 跑了几分钟的结果全丢。这个 bug 能一直活着是因为 Python 自己的 `json.loads` **默认认** NaN/Infinity(非标准扩展), 后端怎么自测都发现不了 —— 测试里专门用 `parse_constant` 模拟浏览器的严格口径, 并钉住"三路 done 都必须洗"。测试 +9 | 改 `api/backtest.py`(单次回测 done 加 `_json_safe`)、`pages/backtest/StrategyBacktest.tsx`、`pages/backtest/MiningWorkbench.tsx`、`pages/Mining.tsx`(hint 写明用内置的那套)、`lib/storage.ts`; 新增 `tests/backtest/test_sse_json_safety.py` | 低(版面与默认值; 回测/挖掘的计算路径未动) | `researchManualOpen` 默认改回 true 即回到原版面; JSON 那处是纯修复, 不该回退 |
| R53 | **工作流与「AI 自动挖掘」不再打架, 挖掘页排出层级**: 用户问这两块冲不冲突 —— 设计上不冲突(工作流的一次「重开」**就是**开一个自动挖掘会话, 是包含关系不是并列), 但操作上真会打架, 而且正是之前那个「跑了很久是不是卡死了」的来源: (1) **两路一起挖** —— 挖掘是重活, heavy_job_limiter 容量 2 而一个挖掘 run 独占两格, 再开一路不会更快, 它只会静默排队; (2) **两边推同一个状态机** —— 工作流开的会话照样出现在自动挖掘面板里(面板默认跟随最新会话), 用户对它按「AI 再调一轮」就和后台节拍同时 step 同一个会话, 轮次会错乱(两边都以为自己开的是第 N 轮)。修法是两侧对称装闸: 会话落盘时记 `owner_workflow_id`(工作流开的会话由 driver 认领), 有挖掘工作流在跑时手动开会话 409、有主的会话手动 step/stop 409(单停会话没用 —— 工作流会当成这次重开结束了转头再开一个), 反过来手动会话还开着时建挖掘工作流也 409。三条错误文案都写清"为什么"(不会更快, 只会排队)和"怎么办"(先停哪个), 不是干巴巴一句不行。已收工的会话和有主的会话都不占坑, 否则跑过一次就再也起不了工作流、或者工作流重启后把自己锁死。前端: 面板据 `owner_workflow_id` 停用手动按钮并说明是谁在跑(自动模式也强制关掉, 否则定时器一直去撞 409), 工作流跑着时「开新会话」置灰带原因; 页面顶部加一段**层级说明**(工作流 ⊃ 自动挖掘会话 ⊃ 单轮挖掘, 缩进画出包含关系)—— 三块并排平铺最容易读成"选一个用", 那正是同时开两路的起因。测试 +11 | 改 `services/mining_autopilot_store.py`(+`owner_workflow_id`)、`api/mining.py`(两处 409)、`api/workflows.py`(建挖掘工作流时的对称检查)、`services/workflow_drivers.py`(认领会话)、`pages/Mining.tsx`(+`LevelGuide`)、`pages/backtest/MiningAutopilot.tsx`、`lib/api.ts`; 新增 `tests/test_mining_workflow_conflict.py` | 低(只加拦截与展示; 手动那条路在没有工作流时逐字未变) | 删两处 409 检查与 `owner_workflow_id` 即可回到 R39 的行为 |
| R52 | **趋势视图里把「结论」徽标列加回来**: R51 拆两个视图时把它从趋势表里去掉了, 理由是"重复" —— 但那个判断的前提是当时结论视图跟趋势表长得一模一样。现在结论视图已经是摊开的卡片流(每一档说了什么、之后走成什么样), 两边内容分开了, 趋势表里那个 4 字徽标就不再是抄一遍, 而是让状态和当天的通道位置能**横着对上一眼**: 「这个板是在什么位置上出的」「转折那天通道在哪」—— 那正是这张表按时间轴排开的意义。徽标带 R49 的悬停卡片, 想摊开看仍切到「通道结论」视图(表尾写明) | 改 `components/stock-analysis/StockReviewDialog.tsx`(`TrendView` +1 列) | 无(展示层) | 删掉那一列 |
| R51 | **两列点开看的不是同一张表**: R48 那版两边弹出同一份逐日表, 从「结论」列点进去只是在趋势表最右边多一个 4 字徽标 —— 把列里已经有的东西又抄了一遍。拆成两个视图各答各的问题: **趋势视图**(从「趋势」列进)只讲状态怎么走到今天 —— 日期/收盘/涨跌(带涨停标)/六态状态 + 转折日整行标出, 表里不再混进通道那套; **结论视图**(从「结论」列进)把每天的**悬停卡片**摊开 —— 徽标背后那三段(怎么做/为什么/依据 + 当天三档位置)才是这一列真正的内容。连续同一档的天**合并成一段**: 「强势深调」连着 4 天时铺 4 张一模一样的卡片才是真重复, 合成一段标出天数反而看得出这一档持续了多久; 中间隔了没有结论的日子不并段(那是两次独立出现)。每段另给「期间累计涨跌」「含 N 次涨停」与「之后 5 日」—— 后者取**段内最后一天**的前瞻收益: 一档结论连着出现几天时该问的是「它最后一次说完之后怎么样了」, 拿段首那天算等于把段内涨跌也算进兑现里。为此后端给每一行单独带上 `fwd`(末尾不足 5 天为 null, 不拿半截数据凑)。顶部可互相切换 —— 从哪一列进来只决定默认视图, 不把人锁死。测试 +2(逐日 `fwd` 与 outcomes 汇总必须对得上, 两处对不上就说明有一处走了另一套口径) | 改 `components/stock-analysis/StockReviewDialog.tsx`(拆 `TrendView`/`VerdictView`/`SegmentCard`/`toSegments`)、`services/review_service.py`(+`fwd`)、`lib/api.ts`、`WatchlistDecisionBoard.tsx`(两列各带 tab) | 无(展示层拆分 + 后端一个新字段) | 合回单一视图即可回到 R48 |
| R50 | **「AI 战法」改名「AI 打板复盘」并搬到复盘页**: 它做的事本来就是复盘 —— 盘后拿当日梯队回看情绪走到哪一档、谁是龙头、明天该盯什么, 放在连板梯队页等于让人先切页面再复盘。而复盘页本来就有一份「AI 大盘复盘」: 大盘那条线看指数与情绪, 这条线看梯队与打板, 两份并排才构成一次完整的盘后复盘。名字也跟着改 —— 在复盘页上「AI 战法」看不出和旁边那份报告是什么关系, 「打板复盘」与「大盘复盘」一对照就清楚。抽成自带梯队查询的独立组件(复盘页没有梯队数据, 让它走 props 等于把两个页面绑死), 且**只在点开后才拉梯队** —— 进复盘页先看的是大盘报告, 没点开就先拉一份梯队等于白花一次请求。梯队还没就绪时「生成清单」禁用: 拿空快照调 AI 只会得到一份煞有介事的空报告。ext 字段(题材/行业)的读取与取值从梯队页抽到 `lib/ladderExtFields.ts` 两边共读 —— 各写一份的话梯队页显示的题材和喂给 AI 的迟早对不上, 而那正是这份清单的判断依据。后端存档格式与路由未动(改路由会让已有的 30 份存档失联), 只把用户可见的那句错误文案改成「无法生成打板复盘」 | 新增 `components/LadderAiReview.tsx`、`lib/ladderExtFields.ts`; 改 `pages/LimitUpLadder.tsx`(移除按钮/payload/弹窗, 改用 lib)、`pages/Review.tsx`(+按钮)、`lib/api.ts`、`api/screener.py`(仅文案) | 低(纯搬迁; AI 调用、存档、追问路径逐字未动) | 把按钮与 `LadderAiDialog` 搬回 `LimitUpLadder.tsx` |
| R49 | **「结论」悬停改自绘卡片 + 「到位了」改名「该止盈了」**: 原生 `title=""` 在这一列有三个治不好的毛病 —— 要悬停一秒多才弹(扫一列票根本等不及)、系统渲染不能分层次("怎么做/为什么/依据"三段被 `\n\n` 挤成同样大小的一坨灰字)、决策台表格在 `overflow-auto` 里靠边那几行会被裁掉。改为自绘 `VerdictHover`: 立刻弹出、tone 色条 + 三段分级排版(「怎么做」放最上且给底框)、`position:fixed` + portal 挂到 `document.body` 不受任何祖先 overflow 影响、空间不够自动上下翻面并水平夹进视口、滚动/缩放时收起(fixed 不跟着滚会浮在原地指着别的行)。额外把 tone 的字面意思写出来(徽标只有 4-6 字, 光看「候选池」分不出该动手还是先盯着)。`note` 由调用方给 —— 同一个"到上沿", 持仓侧是止盈时机、买入候选侧是追高, 这个差别写死在 verdict 里对不上两边。决策台/今日总览(买入候选与持仓两侧)共用同一个组件。另: `top_confirmed` 的标题从「到位了」改为「该止盈了」——「到位了」看不出是该买还是该卖 | 新增 `components/stock-analysis/VerdictHover.tsx`; 改 `WatchlistDecisionBoard.tsx`、`pages/Today.tsx`、`indicators/keltner.py`(仅标题字串) | 无(纯展示层; 判定与配色仍全部由后端 verdict 决定) | 删 `VerdictHover.tsx`, 两处改回 `title=` |
| R48 | **「趋势」「结论」两列可点开逐日复盘**: 那两列只显示**今天**的读数, 要判断它们靠不靠谱得能翻回去看 —— 上次说「强势深调」是哪天、之后走了什么、这只票的涨停都出现在什么状态下。新增 `/api/stock-analysis/review`: 把六态状态、Keltner 三档位置与结论、涨停/跌停/炸板/连板数按**同一条时间轴**对齐成一张逐日表(默认 120 个交易日, 可切 60/120/250)。**全部走既有模块**: 状态机走 `livermore.compute` 的 steps 且用这只票**自己配的阈值**(复盘用默认值而列里用调过的值, 两边就对不上); 通道走 `indicators.keltner`, 只是把输入换成当天的均线/ATR; 涨停直接取 enriched 的 `signal_limit_up`/`consecutive_limit_ups` 预计算列(涨跌停幅度按板块与 ST 分档、还有 2026-07-06 那次 ST 规则变更, 在这里自己按 9.9% 判一遍必然对不上)。多取 130 根做暖机, 只参与计算不进结果; 不足 120 根时长期档留空。弹窗给涨停/最高连板/炸板/跌停四个数、涨停出现在哪些趋势状态下、以及**各档结论出现后 5 日表现**(样本小, 只报次数与均值, 不折算成百分比胜率去装得像统计结论); 「只看有事的日子」一键滤掉没有结论、没有涨跌停、也没有趋势翻转的那些天; 趋势翻转日整行标出来。口径提醒写在弹窗底部: 均线与 ATR 按**当前**复权因子回算, 之后除权的话同一天算出的通道会和当时屏幕上略有出入。测试 +24(含路由级 —— 接线错了表现是 422 而不是报错, 只有真发一次请求才看得出来) | 新增 `services/review_service.py`、`components/stock-analysis/StockReviewDialog.tsx`、`tests/test_review_service.py`; 改 `api/stock_analysis.py`(+`/review`)、`lib/api.ts`、`lib/queryKeys.ts`、`WatchlistDecisionBoard.tsx`(两列改按钮 + 弹窗) | 无(新端点 + 新弹窗; 两列原有的显示与悬停内容未变) | 删 `review_service.py` + 弹窗 + `/review` 路由, 两列改回 `span` |
| R47 | **「值得关注」的把握分结合 Keltner 三档结论**: R43 那版只看 `pressure` 的 side/level, 有两个洞 —— (1) `pressure` 要求**短期档到轨**才有读数, 「候选池」那一档(短期还在中部、中期已到下沿)整个够不到, 而那正是低吸候选最典型的样子; (2) 只分"上沿/下沿 × 强/弱"四格, 分不出「强势深调」和「下跌途中」, 两者短中期读数一模一样、长期档一翻含义完全相反(一个是这套里最好的买点, 一个是越抄越套), 给同一个分等于把最好的和最坏的混在一起。改为按 `verdict` 的 code 打分(+10 … −15), 幅度对齐既有因子(量比 ±12、胜率 ±12、相对强度 −15), 不让通道位置一项压过量价本身; 两个陷阱档(超跌反弹/下跌途中)与大顶同档扣分 —— 它们都是**出了买信号但位置不该买**, 信号越像越危险。打分放在**两个候选来源合并之后**(与 R37 主线加成同一位置): 只挂趋势那一路的话, 一只在大顶区域的 AI「逼近突破」候选照样满分排在最前, 而它的结论标就在旁边写着"不该开新仓", 标签与排序自相矛盾。`verdict` 与 `pressure` 在 `_build_overview` 里**分开算**且对**全部自选**打标(不只趋势候选)。前端机会区换上 `VerdictTag`(与决策台「结论」列、导出件同一份 tone), 导出 HTML 一并带上。测试 +22 | 改 `api/today.py`(+`_VERDICT_SCORE`, 打分移到合并之后)、`pages/Today.tsx`、`lib/api.ts`; 新增 `tests/test_today_r47_verdict_score.py`; 删 `test_keltner_stance.py` 里过时的机会区打分段 | 低(只改把握分的一项加减与展示标; `pressure()` 与持仓侧的挡加仓/止盈减仓路径未动) | 删 `_VERDICT_SCORE` 与合并后那段循环即可回到 R43 的打分 |
| R46 | **决策台导出 HTML(只导有结论的行)**: 工具栏加「导出 HTML」按钮(带条数角标), 生成自包含浅色页面 —— 无脚本无外链, 存档/打印/转发都不依赖本应用, 与今日总览导出同一套排版。**只导「结论」列有内容的行** —— 三档通道都在中部的票没有位置信息, 导出来只占篇幅; 导出件首行明写"导出 N 只(自选共 M 只)", 免得看到 148 只自选只导出 4 行时以为漏了。**按当前排序导出** —— 界面上怎么排, 导出件就怎么排(结论列升序 = 候选池聚在一起, 降序 = 该减的在前)。列: 标的/现价/涨跌/仓位/浮盈/趋势/短中长三档/结论/怎么办, 最后一列带完整的 action + detail。导出件配色单独给一套浅色(深色主题那套搬过去看不清), 并在页首写明通道口径(MA20±2ATR / MA60±2.5ATR / MA120±3ATR, 收盘判定)与那条纪律(结论说的是位置不是涨跌, 清仓看止盈线/生命线)。无可导出行时按钮禁用并给 toast, 不静默下载一个空表 | 改 `components/stock-analysis/WatchlistDecisionBoard.tsx`(+`buildBoardHtml`/`exportRows`/`exportHtml`/按钮) | 无(纯新增, 不碰任何既有渲染与数据路径) | 删 `buildBoardHtml` + 按钮 + `exportRows` |
| R45 | **补上「候选池」观察档 + 列宽按比例自适应**: 拿用户截图那 8 行真实读数跑 R44 的结论层, 发现 **7/8 显示「—」** —— 短期档在通道中部就不给结论(设计如此: 短期是操作级别), 但寒武纪/兆易创新/宏和科技三只**中期已破下轨**、只是短期收回了通道内, 这恰恰是低吸候选池该有的样子(大级别跌到位、就等短期给入场点), 却被整批滤掉。补 `_watch()` 观察档: 短期在中部 + **中期**到轨时给「候选池 / 高位回落」, 用独立的 `TONE_WATCH` 与四个动作档区分开。**只报中期, 长期单独触轨不报** —— 半年通道太钝、一年难得触几次, 报了也没法据以行动, 只会把这一列填满噪音(有测试守着)。观察档 rank 排在动作档中间(比该吸的靠后、比该减的靠前), 且短期到轨时永远优先于观察档。同时把排序权重从前端自编的 `VERDICT_RANK` 改为后端随结论下发的 `rank` 字段, 消除两边各一套顺序。**注意 `pressure()` 未动** —— 今日总览的挡加仓/止盈减仓仍只在短期到轨时触发, 观察档只出现在决策台的展示列, 不改变任何操作建议。顺带列宽改 `colgroup` 百分比分配(标的12/现价5/涨跌5/仓位4/成本8/浮盈5/止盈线7/趋势7/三档各4.5/结论5/置信4/报告5, AI 信号吃剩余 19.5%), 去掉拍脑袋的 `min-w-[1040px]` 与 AI 信号格的 `min-w-[300px]` —— 原来富余空间会被浏览器全塞给 max-content 最大的那一列, 别的列挤成一团。百分比随视口走才叫自适应; 配合上一版的 nowrap(内容宽度即底线), 既按比例分又不会压变形。真实数据下亮起的行从 1/8 增至 4/8。测试 +6 | 改 `indicators/keltner.py`(+`_watch`/`TONE_WATCH`/每条结论加 `rank`)、`lib/api.ts`、`components/stock-analysis/WatchlistDecisionBoard.tsx`(+colgroup, 排序改用后端 rank)、`tests/test_keltner_verdict.py` | 低(观察档只加展示; `pressure()` 与所有操作建议路径未动) | 删 `_watch()` 与 `verdict()` 里的回落分支即可回到 R44 |
| R44 | **三档通道组合 → 一句话结论**(用户: "上下轨结合短中长期一起做出结论"): 原来界面只给得出每一档的位置(贴上轨/通道内/…), 三档怎么合成一个结论要用户自己脑补。新增 `keltner.verdict()` 纯函数, 把八种组合翻成带标题+动作+理由的结论: 偏贵侧「短线冲高/到位了/大顶区域/超跌反弹」, 偏便宜侧「短线回调/调到位了/下跌途中/强势深调」。**判定顺序是核心**: 先判"短期与长期反向"的两种矛盾组合 —— 短期到上轨+长期在下沿 = 跌深了反弹(卖点不是买点), 短期到下轨+长期在上沿 = 强势股洗盘(最好的低吸位置); 同样一个"短期到轨"长期档一翻结论完全相反, 放后面判会被"三档同向"粗规则先吃掉, 前者被说成止盈时机(把反弹当上涨), 后者被说成别抄(错过最好的低吸位)。语气分四档(sell/buy/hold/avoid)且**只谈加减不谈清仓** —— 清仓是出场线/生命线的事, 优先级在通道之上, 有测试守着结论文案里不出现"清仓"; 只有短期触上轨时给 hold 而非 sell(强势票沿上轨走是常态, 说该减会让人反复卖飞)。结论由后端 `channels_for_symbols` 一并返回, 决策台「结论」列(可排序: 降序=偏卖在前, 升序=偏买在前)、今日总览标签、导出 HTML 三处读同一份, 不各自拼规则。测试 +19 | 改 `indicators/keltner.py`(+`verdict`/`_VERDICTS`/`TONE_*`)、`services/keltner_service.py`、`api/today.py`、`lib/api.ts`、`components/stock-analysis/WatchlistDecisionBoard.tsx`(+`VerdictCell`+第 15 列)、`pages/Today.tsx`; 新增 `tests/test_keltner_verdict.py` | 低(纯增量: verdict 为 null 时所有路径与 R43 一致) | 删 `verdict()` + 决策台该列 + 两处 `verdict?.title` 回退即可 |
| R43 | **高抛低吸全面改用 Keltner 三档**(用户: "之前做的高抛低吸我不要了, 直接用 Keltner 短中长来决定" + "Keltner 必须是系统自带的, 不允许创造"): ①**撤掉 R41** —— 基于 RSI + MA20 乖离的 `services/overheat.py` 与其测试整体删除。②**新增内置策略 `keltner_reversion`(通道高抛低吸)** —— 本仓**第一个出场由价格位置驱动**的策略: 此前所有内置策略的 exit 都是趋势破位型(`ma20_breakdown`/`ma_dead`), 哪怕入场是低吸类, 跑出来也是「低吸进 + 破位出」, 所以"高抛低吸行不行"在回测里根本无法表达。入场 = 触下轨后**收回通道内**(不在触轨当天买, 那是接飞刀), 出场 = 到上轨止盈(可选到中轨), 默认带 MA120 趋势闸拦住"下跌趋势里反复抄底"这个均值回归最亏钱的场景; 硬止损 -8% + 最长持有 20 日兜底。③**今日总览决策层换源** —— `holding_stance` 的 heat 输入从 RSI 过热换成 `keltner.pressure`(短期档定方向、中期档定强弱只放大不改向、长期档仅参考); 止盈减仓要求短中期共振(只有短期贴上轨太常见, 每次提示等于天天喊减仓), 防卖飞两道闸保留(浮盈≥10%、刚转强头 2 天不减); 机会区共振在上沿扣 10 分, 仅短期贴上轨只标注, **贴下轨不扣分**(那正是低吸要的位置)。**三档参数与我动手前的 `levels.py` 逐字一致**(ma20/2.0、ma60/2.5、ma120/3.0), 未自创; 新增的只有"贴近"阈值 `NEAR_ATR=0.5` 与三档合成 —— 系统原本回答不了"贴着哪条轨"。为让策略沙箱能 import 共用常量, 把 `app.indicators.keltner` 加入策略安全白名单(纯常量+纯数学零 IO, 能力远小于已在名单的 `app.backtest.matrix`; 白名单对 builtin 也生效是刻意的纵深防御, 不绕过)。内置策略数 19→20, 相应清单计数同步。测试 +52(净 +28) | 新增 `strategy/builtin/keltner_reversion.py`、`tests/test_keltner_reversion.py`、`tests/test_keltner_stance.py`; 删除 `services/overheat.py`、`tests/test_overheat.py`; 改 `indicators/keltner.py`(+`pressure`)、`api/today.py`、`strategy/ai_generator.py`(白名单)、`lib/api.ts`、`pages/Today.tsx`、`tests/test_screener_etf.py`、`tests/backtest/test_matrix_strategy.py` | 低(新策略为纯增量; today 的 heat 字段形状变了但 None 时行为不变) | 删策略文件 + `holding_stance` 的 heat 分支 + 前端 `HeatTag` |
| R42 | **决策台加 Keltner 三档位置列**(用户: "想知道每个个股当前处于短/中/长期哪种状态, 贴着下轨、在轨下还是在轨上"): 自选决策台新增「短通道/中通道/长通道」三列, 五档判定 破上轨 / 贴上轨 / 通道内 / 贴下轨 / 破下轨, 悬停给真实上下轨价、通道内位置百分比、距上下轨各几个 ATR。**公式抽到 `indicators/keltner.py` 与个股分析图表共用** —— 原来只写在 `levels._keltner_band` 里, 决策台再写一份的话, 图表说"贴上轨"决策台说"通道内"那天没法查; `levels.py` 三档参数改为取自 `keltner.BANDS`, 有测试断言两边同源且给出同一组上下轨。两个口径判断: **"贴近"用 ATR 而非百分比**(通道宽度本身就是 ATR 倍数, 用百分比会让高波动票永远不贴、低波动票永远贴着); **判定顺序先轨外再贴近**(破上轨的票必然也在上轨 0.5 ATR 内, 反过来判会把突破说成贴近, 那是两件事)。批量服务两次读盘: enriched 快照取 close/atr_14/ma20/ma60, 一次批量日 K 只为算 MA120(唯一没有预计算列的一档, 不足 120 根则该档留空而不是拿 60 根算个假均线)。**收盘口径** —— 通道要 ATR 与均线, 实时叠加层只有价格, 拿实时价比昨天的通道会得到半新半旧的判定。测试 +33 | 新增 `indicators/keltner.py`、`services/keltner_service.py`、`tests/test_keltner.py`; 改 `indicators/levels.py`(改为委托共用公式)、`api/stock_analysis.py`(+`GET /keltner`)、`lib/api.ts`、`lib/queryKeys.ts`、`components/stock-analysis/WatchlistDecisionBoard.tsx`(+3 列 + `KeltnerCell`) | 低(新增端点与列; 对 levels.py 的改动是等价重构, 图表数值不变) | 删三列 + `KeltnerCell` + 端点; `levels.py` 回退到内联公式即可 |
| R41 | **短期过热 —— 系统缺的那句"涨太急了"**(高抛低吸评估的第一步): 今日总览原来只说得出"该买了"(转多/回升)和"该跑了"(破线/趋势转坏), 持仓体检的三个减仓触发全是**风险驱动**, 没有一条是**过热驱动**。新增测量层 `overheat.assess`, 两个口径要点: ①**用 ATR 归一化而非固定百分比** —— 乖离 15% 对主板是极端、对 20cm 的创业/科创很常见, `(close-ma20)/atr14` 自动按标的自身波动缩放, 不需要按板块维护阈值表, 也不会把高波动票系统性全判成过热; ②**RSI 超买"且"远离均线两个条件同时满足**才算数 —— 一轮像样的主升浪 RSI 常年 70 以上, 只看 RSI 会大批误伤健康趋势。测量层与决策层刻意分开("新转强第 2 天 RSI 80"和"横盘半年突然拉到 RSI 80"是同一读数、完全不同两件事)。决策接入两处: **挡加仓**(比减仓更要紧 —— 原来只看趋势+AI 不看价格冲到哪, 那是在最贵的位置加最重的注)、**补一档止盈减仓**但排在所有风险驱动减仓**之后**, 且加两道防卖飞闸(浮盈 ≥10% 才谈、刚转强头 2 天不谈)。机会区: `hot` 扣 10 分并说明理由, `warm` 只标注不扣分(一并扣会把系统推成专挑弱势票)。数据全部来自 enriched 已有列(`rsi_14`/`ma20`/`atr_14`), 一次批量读取, 零新计算源; 取不到时行为与 R41 之前逐字一致。测试 +24 | 新增 `services/overheat.py`、`tests/test_overheat.py`; 改 `api/today.py`(`holding_stance` 加三个可选参数 + 批量读取 + 机会区打分)、`lib/api.ts`、`pages/Today.tsx`(`HeatTag`) | 低(新增可选参数与字段, `heat` 为 None 时全部路径行为不变) | 删 service + `holding_stance` 里的 heat 分支 + 前端 `HeatTag` |
| R40 | **机会区板块过滤**(用户: "有时候我只想看主板的或者某个板的"): 「值得关注」标题行加一排板块筛选片(全部/沪主板/深主板/创业板/科创板/北交所, 可多选), 每条机会带板块徽章(20cm 的创业/科创用暖色、30cm 的北交所用红色 —— 同一个把握分, 20cm 的票波动天然更大)。**过滤在后端于 `max_show` 截断之前生效** —— 前端筛的话会漏掉被截掉的同板块票, 用户看到的"主板机会"是残缺的且他不会知道; 有专门的回归测试守这条。板块归属统一到 `price_limits.board_of`(原来 `market_overview_builder._board` 有一份私有副本 —— 板块决定涨跌停幅度, 两处各判一套迟早对不上), 判定顺序先后缀 `.BJ` 再数字前缀最后交易所, 否则 300/688 会被 `.SZ/.SH` 吞成主板。选择落 `today_prefs`(换设备/刷新都保持), 全选归一为空列表(否则以后加新板块时旧的"当时全选"会变成排除新板块), 传了但一个都不认识时保持原样而不是静默变成全看。导出 HTML 同步带板块。测试 +28 | 改 `price_limits.py`(+`board_of`/`BOARDS`)、`services/market_overview_builder.py`(改为委托)、`services/today_prefs.py`、`api/today.py`、`lib/api.ts`、`pages/Today.tsx`; 新增 `tests/test_today_board_filter.py` | 低(新增字段与可选参数; `boards` 为空时行为与从前逐字节一致) | 摘掉筛选片 + `rank_opportunities` 的 `boards` 参数即可 |
| R39 | **研究工作流**(用户: "做成工作流，一整套工作起来，还不影响手动"): 原来的 AI 代跑跑在页面里 —— 关页面就断、用满 5 轮就停, 是一次性脚本不是工作流。新增服务端工作流层, 三件原来没有的事: ①**服务端自己跑**(会话落盘 + 后台节拍线程 20s 一拍, 关页面照跑, 重启接着跑); ②**会重开**(一次跑完不达标不算结束, 换一批配置再开一次 —— 用户说的"抽卡"; 预算按"最多重开几次 / 每次几轮 / 最多跑多久"给); ③**记账**(`overfit_note` 按累计轮数给出这个结果该怎么看, 跟着赢家带到界面上)。**一定会停**是核心不变量: 达标停、次数用完停、到点停(存绝对截止时刻, 不存时长 —— 存时长的话每次重启等于重新计时)、连错 3 次停。分层刻意做死: `workflow.py` 只有状态机与账本(纯函数, 墙钟可注入), 真跑挖掘/回测的动作由注入的 driver 完成, 单测不需要真跑一次回测。两个 driver 走的都是手动路径同一批入口(挖掘走 `step`, 回测走 `run_worker_task`), 没有第二套实现。**手动路径一行没动**。顺带: 回测给 AI 的旋钮从 5 个加到 7 个(+持仓天数/分钱方式, 编造的分钱方式回落等权); 账户事实(费率/印花税/滑点/初始资金/撮合口径)由页面原样透传, AI 碰不到。为共用路径把 `api/mining.py` 里的 `load_run_result` / `build_autopilot_session` / `advance_autopilot_session` 抽了出来, `autopilot_start`+`autopilot_step` 改为调用它们。测试 +42 | 新增 `services/workflow.py`、`services/workflow_drivers.py`、`services/workflow_runner.py`、`api/workflows.py`、`components/backtest/WorkflowPanel.tsx`、`tests/test_workflow.py`、`tests/test_workflow_api.py`; 改 `api/mining.py`(抽公共路径)、`services/backtest_autopilot.py`(+2 旋钮)、`main.py`(挂路由+节拍器)、`lib/api.ts`、`pages/Mining.tsx`、`pages/backtest/StrategyBacktest.tsx` | 低(新增为主; 对 mining.py 的改动是等价抽取, 端点行为不变) | 摘掉路由 + 节拍器 + 两处面板即可; `workflow*.py` 整体删除不影响任何既有路径 |
| R38 | **长任务的"看得见"与"停得下"**(用户实测: 自动挖掘跑了很久、日志无报错、分不清是不是卡了, 且没有任何办法停): ①**进度可见** —— 会话接口给正在跑那一轮补 `live` 块(run 状态/阶段/百分比/入队与起跑时刻), 界面显示"排队中(已等 X)"或"第 3/8 折 · 37% · 已跑 X", 有百分比才画进度条(给不出就不画假的匀速条), 有 run 在跑时会话轮询从 15s 收紧到 4s。**排队与运行必须分得开**: 挖掘在 `heavy_job_limiter` 里独占全部 2 个槽位且 `acquire` 无超时, 只要别的重任务占着 1 个槽, 挖掘就会停在 `queued` 无限等下去 —— manifest 里一切正常、日志一行不报, 旧界面只转圈, 这正是"看起来卡死"的主因。②**中止** —— 自动挖掘新增 `POST /autopilot/sessions/{id}/stop`: 取消正在跑的 run + 把该轮落成 cancelled(不落的话下次 step 还以为它在跑) + 会话收成**独立的 `stopped` 而非 `failed`**("我按了停"和"它自己崩了"在复盘时是两件事); 幂等, 已收工的会话重复点不会抹掉赢家。`step()` 同步: 末轮 run 为 cancelled 时走 STEP_DONE→stopped, 且消息里的状态改中文(原来直接插英文 "第 1 轮挖掘 failed")。③**两个 AI 代跑循环也能停** —— 循环跑在前端, 用 `useRef` 不用 state(闭包里读 state 拿到的是点开跑那一刻的旧值); 回测代跑能真停(调 `stopBacktest` 触发后端 cancel_event, 按钮写「中止代跑」), 因子代跑的单次筛选请求发出去断不掉, 按钮如实写「跑完这轮就停」而不假装能立刻断。测试 +12 | 改 `api/mining.py`(+`_attach_live_run`/`autopilot_stop`)、`services/mining_autopilot.py`(+`status_cn`, cancelled 分流)、前端 `lib/api.ts`、`pages/backtest/MiningAutopilot.tsx`、`StrategyBacktest.tsx`、`FactorDiscovery.tsx`; 新增 `tests/test_mining_autopilot_stop.py` | 低(纯增量: 新端点 + 只读的 live 块 + 三个按钮, 原有 step/run 路径未动) | 删端点 + `_attach_live_run` 调用 + 三处按钮; `stopped` 回落为 `failed` 仅影响展示文案 |
| R37 | **今日总览补上中观层**(三层推导 `大盘 → 主线 → 个股` 里缺的那一层): 系统原来只有第一层(市场天气/大盘红绿灯)和第三层(六态 + AI 信号), 于是"这只票凭什么值得买"永远只能答到"它自己形态好", 答不了"它站没站在今天最强的那条线上"。①**机会区加主线归属**: 候选票命中当日主线(涨停梯队聚合出的概念排行)时打标并加把握分, 一票多概念取名次最靠前的当主归属、其余进 also。②**中观快照条**: 两市成交额 + 近 250 交易日分位、全市场涨跌家数、当日主线前三(带涨停家数/最高连板/龙头), 放在天气横幅与机会区之间。零新计算源 —— 两份数据都是既有的 `market_mainline` 与 `regime_builder` 日频时序。三条刻意的设计约定(都有测试守着): **不在主线内不扣分** —— 主线由涨停梯队推出, 只覆盖市场最躁动的一小撮, 一只慢牛票所在概念可以整年零涨停, 扣它的分等于系统性偏向妖股; **数据陈旧就不打分** —— 跑批停更几天后的"当前主线"其实是上周的主线, 超过 7 个自然日只展示并标 stale; **加成做小(12/10/8/5)** —— 主线归属是佐证不是主因, 第一主线里的烂形态不该排到第三主线里的好形态前面。加成在两个来源(趋势转强/逼近突破)合并**之后**统一加, 同一只票命中两路也只加一次。成交额分位把当天排除在分母外(自己不可能小于自己), 样本不足 20 天只报绝对值不硬凑分位。AI 导读侧: `meso` 随总览整体进提示词(PRD §10.2 AI 与界面看同一组数), 候选带「主线归属」字段, 并加一条纪律 —— 主线是佐证不许单独当选中理由, 只有量价质量相当时才用它分高下。测试 +29 | 新增 `services/today_mainline.py`、`tests/test_today_mainline.py`; 改 `api/today.py`(`rank_opportunities` 加主线项 + `_build_overview` 返回 `meso` + AI 提示词与 payload)、前端 `lib/api.ts`、`pages/Today.tsx`(中观条 + 机会区标签 + 导出件同步) | 低(主线/regime 数据缺失时 `meso` 为 null, 机会区打分与从前完全一致) | 删 service + `_build_overview` 里的 meso 块 + `rank_opportunities` 中的主线循环 + 前端两处渲染 |
| R36 | **补上前端 ESLint(以及它当场抓到的一个必崩 bug)**: `package.json` 一直有 `"lint": "eslint ."`, 但既没把 eslint 列进 devDependencies 也没有 `eslint.config.js` —— ESLint 9 起不认旧格式, 这个脚本从来跑不起来(之前误以为能跑, 是全局装的 eslint 被捡到了)。补配置后第一次跑就抓到 `react-hooks/rules-of-hooks` 一条: `MiniIntraday` 的 `useId()` 写在空数据早退**之后**, 分时数据从无到有时本组件 hook 数 0→1, React 会抛 "Rendered more hooks than during the previous render" 崩掉整棵子树 —— 已把 `useId()` 提到早退之前。配置取舍是「基线 error = 0」, 因为永远是红的 lint 等于没有 lint: 只有 `rules-of-hooks` 这类必崩的留 error; `exhaustive-deps` 存量 60+ 处几乎全是 `const rows = q.data ?? []` 喂给 useMemo 的性能味道(不是错误), 先降 warn 保持可见, 清完再提回 error; 命名/格式/any/未使用变量交给 tsc, `no-useless-escape` 关掉(存量全是字符类末尾多余的 `\-`, 为它改 4 个上游文件只会白白制造合并冲突)。顺手把 fork 自己那 5 处 `?? {}` 包进 useMemo。**依赖装在 pnpm 上**: 仓库用 `pnpm install --frozen-lockfile`(Dockerfile 有 `\|\| pnpm install` 兜底, release.yml 没有), 只更 `package.json` 不更 `pnpm-lock.yaml` 会让 release CI 直接挂。现状 0 errors / 58 warnings, tsc 与 build 均通过 | 新增 `frontend/eslint.config.js`; 改 `frontend/package.json` + `pnpm-lock.yaml`(+eslint/typescript-eslint/eslint-plugin-react-hooks/globals)、`components/stock-table/MiniIntraday.tsx`(hook 位置)、`components/stock-analysis/WatchlistDecisionBoard.tsx`、`pages/backtest/FactorDiscovery.tsx` | 低(除 MiniIntraday 一处 hook 位置外不改运行时行为) | 删 `eslint.config.js` 与 4 个 devDependencies |
| R35 | **多 key 轮询错峰 + 抖动**: 原来同一轮里 n_keys 批背靠背打成一个突发, 打完干等一个窗口。改为摊开在窗口内并加随机抖动 —— 刷新变连续滚动而非整块跳变; 一次网络抖动只毁一批(5 只)而不是整轮 70 只; 不与其他客户端形成同步节拍。key↔批次映射每轮重新洗牌(不再固定 `i % n_keys`) —— 固定映射下某 key 出问题永远伤同几只标的, 打散后失败面随机分布。**额度约束不变(有测试守着)**: 抖动系数恒为 [1, 1+jitter] 只增不减, 同一 key 两次调用间隔始终 >= 60/rpm(14 key + rpm=10 实测一轮 6.13s vs 窗口 6.00s)。另加**每轮随机只启用一部分 key**(偏好 `realtime_keys_per_round`, 0=全部): 摊开后一轮正好铺满窗口、余量几乎为零, 某批响应慢或重试时同一 key 的下次调用可能落进窗口内; 少用几个就谁都不贴上限跑(base 从 6/14=0.43s 放宽到 6/10=0.60s), 且单 key 失效只影响它被抽中的轮次。代价是每轮容量 5×启用数, 147 只仍 3 轮覆盖完。GET/PUT `/api/settings/preferences/realtime-keys-per-round`(返回带 `total_keys` 供界面显示「10 / 14」)。测试 +24 | 改 `tickflow/rate_limits.py`(+`spread_delays`/`shuffled_key_order`/`select_keys`)、`services/quote_service.py`、`services/preferences.py`、`api/settings.py`; 新增 `tests/test_rate_limit_spread.py` | 低(只在多 key 池化路径生效, 单 key 退化为原行为) | 循环里去掉两处调用, 恢复 `pool[i % n_keys]` |
| R34 | **策略回测 AI 代跑**(量化研究页→策略): 与 R33 同形态, 对象换成策略回测。点「不会选? 让 AI 帮我跑」, 最多 5 轮 —— AI 选策略定参数 → 把配置真的灌进左侧表单(策略/环境过滤/持仓上限/总仓位/区间当场变) → 跑 → AI 看结果 → 不行换一个再跑 → 满意收手给结论。**只让 AI 碰五个旋钮**(策略、环境过滤档位、最大持仓数、最大总仓位、区间天数): 费率/撮合口径/初始资金属于「用户的账户事实」不该由 AI 替改, 策略自有参数是各策略特有语义、盲调等于乱试。配套给 `backtestTask` 加 `waitForBacktest`/`currentBacktestId` —— `startBacktest` 是 fire-and-forget(结果走 SSE 回填全局任务), 代跑要串行跑多轮必须能 await; 只认发起后新产生的任务(id 大于调用时的 id)免得把上一轮旧结果当本轮, 并带超时兜底。规则层达标判定与提示词同一套(交易数≥30 且 回撤≥-30% 且 收益>0 且 夏普≥0.5)。提示词纪律: 结论须说清是历史回测不代表未来并引导去「验证」tab 做参数敏感性与滚动样本外; 达标即收手不许刷数字; 交易数少/回撤大/收益靠个别几笔要明说。测试 +17 | 新增 `services/backtest_autopilot.py`、`tests/test_backtest_autopilot.py`; 改 `api/backtest.py`(+POST `/strategy/ai-plan`)、前端 `api.ts`、`lib/backtestTask.ts`、`pages/backtest/StrategyBacktest.tsx` | 低(新增端点 + 按钮 + 过程条, 原「运行回测」路径未动) | 删 service + 端点 + 按钮/PilotTrace + `backtestTask` 尾部两个导出 |
| R32 | **因子批量筛选 AI 解读**(量化研究页→因子→批量筛选): 61 个因子 × 5 指标的表没人看得动。规则层(零 AI 成本)先算可用度分 = 区分度 |IC| 50 + 稳定性 |IR| 30 + 方向确定性(胜率偏离 50%) 20, 全用绝对值以免反向因子被符号埋没; IC 胜率用**双边死区**(50%±5%)判随机而非单边下限(单边会误杀反向因子); 短名单按可用度降序且**同组最多留 2 个**(MA5/10/20 乖离高度相关, 全放是虚假多样性); 另给漏斗统计回答「61 个里为什么只剩这几个」。AI 层只看短名单, 输出能用的 2-5 个(理由须落到指标)、同类冗余、当前市场风格、下一步拿哪几个去挖掘; 明确禁止把多空收益当可执行收益推荐(docs §成交和成本: 理论价差, A 股不能真做空), IC 高但 IR 低/胜率贴 50% 必须明说不稳。面板按可信度排序(规则层漏斗→AI 结论→冗余), 底部可把 AI 选中因子或短名单一键灌回勾选框重跑。**②AI 代跑**(R33, 用户真正要的): 点「不会选? 让 AI 帮我跑」, 最多 5 轮循环 —— AI 挑因子定参数 → 把配置真的灌进左侧表单(勾选框/调仓/分组数当场变, 用户看得见它在操作) → 跑 → AI 看结果 → 不满意换一批再跑 → 满意收手给结论。跑复用同一个 run mutation(加可选 override, 因为 setState 异步、代跑必须拿刚给的配置立刻跑), 结果自动进原来那张表; 手动「自己跑」按钮保留且行为不变。提示词纪律: 批量筛选是描述性统计不是验证过的策略, 结论必须说清只是「值得进一步研究的方向」并引导去挖掘页做样本外验证; 达标即收手不许刷数字; 因子须跨组挑。测试 +25 | 新增 `services/factor_ai.py`、`tests/test_factor_ai.py`; 改 `api/backtest.py`(+POST `/factor/ai-reading`、`/factor/ai-plan`)、前端 `api.ts`、`pages/backtest/FactorDiscovery.tsx` | 低(新增端点 + 面板, 未改原筛选逻辑) | 删 service + 端点 + ReadingPanel 与按钮 |
| R31 | **AI 自动挖掘**(挖掘页顶部新面板): 用户要的闭环 —— 跑 → AI 看结果 → 改配置 → 再跑, 直到 AI 认为够好。**关键设计: 锁一段终检窗口**。反复读样本外指标再改配置重跑同一段, 等于用样本外挑了 N 次, 试够多必然撞上一个"过门槛"的(多重检验), 自动化只会坏得更快; 所以区间切成 搜索窗口(AI 循环只在这里试) + 终检窗口(循环全程看不到, 日期都不进 payload), 赢家须**人工确认发布**后才在终检窗口跑一次定生死。收工附 `confidence_note`, 迭代越多提示越明确地要求打折。编排是单一入口 `step()` 状态机(running/iterated/done/error), 手动点一次调一次、自动模式即前端轮询同一入口, 两种模式无第二套逻辑。严格对齐 `docs/mining.md`: 档位只允许 balanced/strict(探索档不能发布)、搜索窗口下限按档位算(balanced≈1185 自然日、strict≈1756)并再走 `require_mining_availability` 数真实交易日、赢家按 candidate signature 标识、上一轮未终态绝不起新 run(挖掘独占 2 槽位)、循环绝不调 publish、AI 只能输出因子 id 与受控数值参数(编造丢弃/越界夹紧)。测试 +38 | 新增 `services/mining_autopilot.py`、`services/mining_autopilot_store.py`、`tests/test_mining_autopilot.py`、前端 `pages/backtest/MiningAutopilot.tsx`; 改 `api/mining.py`(+4 端点)、前端 `api.ts`、`pages/Mining.tsx` | 低(全新面板 + 新端点, 未改动原挖掘工作台任何逻辑) | 删两个 service + 4 个端点 + 面板, `Mining.tsx` 去掉一行 |
| R30 | **按 PRD 收口六态价位口径**: ①**建仓锚点钉死**(§10.1 决策可复盘)——金字塔计划原用 `up_pivot` 当「站稳价/作废价」, 而它在上涨趋势里随新高天天上移(昨天跌回 13.00 作废、今天变 13.40), 事后对不上账; 改用 `entry_pivot`=进入当前状态那天的关键点, 进入后不再变(`compute` 新增 `entry_up_pivot/entry_dn_pivot`, today.py 建仓路径改用之, 取不到回退 up_pivot)。②**盘中价位强制收盘口径**(§7.5 价格口径统一用正式收盘价)——开实时叠加时盘中冲高会顶高 `leg_high`, 转弱线跟着虚高, 等于拿未成立的高点放松出场纪律; 新增 `_overlay_closing_prices`: 状态/持续天数/信号仍是盘中临时口径(用户要看当前阶段), 但所有价位与操作建议一律用已收盘日线重算, 另附 `price_basis=closing`/`closing_state`/`closing_as_of` 供对照; 单只与批量两条路径共用; 收盘序列不足 `_MIN_DAYS` 时原值不动(§9.4)。前端悬停改为「实时价只参与状态判定, 价位一律按已收盘日线算」。测试 +5 | 改 `indicators/livermore.py`、`services/livermore_service.py`、`api/today.py`、前端 `api.ts`、`TrendStateBar.tsx`、`WatchlistDecisionBoard.tsx`; 扩 `tests/test_livermore_flip.py` | 低(纯增字段 + 盘中分支) | today.py 改回 `up_pivot`; 删 `_overlay_closing_prices` 两处调用 |
| R29 | **六态关键点改用「翻转触发价」**: 用户发现上涨趋势里上关键点天天等于当日收盘 —— 属实, 趋势途中 `up_pivot = max(up_pivot, 收盘)` 即本轮最高收盘价, 创新高当天就是当日收盘, 拿它当「突破 X 加仓」的触发价等于没给价位。补出状态机本就隐含、但从未暴露的两条前瞻价: `flip_down`(收盘跌破即转弱: 上涨/回升态 = 本轮最高×(1-阈值), 回撤态 = 下关键点)、`flip_up`(收盘站上即转强: 回撤/下跌态 = 本轮最低×(1+阈值), 回升态 = 上关键点), 另带 `leg_high/leg_low` 本轮高低水位。操作建议文案随状态换用对应价位(上涨趋势不再说「突破今天的收盘价」); K 线六态价位线优先画翻转价(与关键点重合则不重复); 趋势条 / 决策台悬停 / 六态汇总表与 HTML 导出同步换列, 关键点降级为悬停参考。四个中间态的关键点本就是触发价, 逻辑不动。**PRD 校对补丁**: ①补回 ADD 语义(§6.6 五信号)——上涨趋势建议里加回「可按金字塔计划加仓」; ②个股信号 AI 提示词同步喂 flip_down/flip_up(§10.2 决策可解释性: AI 看的数必须是界面主打的数); ③决策台悬停明示出场优先级「组合回撤风控 > 生命线 > 止盈线(ATR) > 六态转弱」(§7.2/§12.5 风控最高权限), 并提示盘中转弱线会随盘中高点虚高。测试 +12 | 改 `indicators/livermore.py`(+`_flip_prices`, `action_text` 增两参且向后兼容)、`services/livermore_service.py`、`api/stock_analysis.py`、前端 `api.ts`、`TrendStateBar.tsx`、`WatchlistDecisionBoard.tsx`、`TrendSummaryDialog.tsx`;新增 `tests/test_livermore_flip.py` | 低(纯增字段, 老调用方不传新参行为不变) | `action_text` 去掉后两参 + 前端改回读 up_pivot/dn_pivot |
| R28 | **个股分析改版 + 板块跷跷板**: ①自选决策台铺满整页(`fullPage` 模式, 表格高度吃满剩余视口), 点标的改为**弹出**关键价位分析(日K+压力支撑+六态趋势条, Esc/点遮罩关), 列表不再被 K 线图挤走、可连续扫多只; ②新增**板块跷跷板**(市场环境页→情绪周期 tab, 主线排行上方): 规则层零 AI 成本(用本地主线日度强度序列算反向相关 + 领先方换手轮数 + 双方活跃度, 反向相关 ≤-0.15 硬门槛挡掉『一边不动一边窜』的假跷跷板), 开页即有; 「AI 一键识别跷跷板」把候选对的真实日度序列送审, AI 只做甄别与解读(哪几对成立/存疑、当下轮到谁、对手盘什么条件接力); 结果**滚动留档 30 天**(同日同 kind 覆盖, 序列不入档), 面板内可展开逐日回看。③关键价位开关取消「更多价位」折叠, 14 个价位组一次全排开(用户要全展示)。测试 +22 | 新增 `services/seesaw.py`、`services/seesaw_store.py`、`tests/test_seesaw.py`、前端 `components/regime/SeesawPanel.tsx`;改 `api/regime.py`(+GET `/api/regime/seesaw`、POST `/api/regime/seesaw/detect`)、前端 `api.ts`、`pages/Regime.tsx`、`pages/StockAnalysis.tsx`、`components/stock-analysis/{WatchlistDecisionBoard,AnalysisKChart}.tsx` | 低(新增为主, 只有个股分析页布局是改写) | 删 seesaw 两文件+两端点+面板;个股分析页恢复内联 `StockAnalysisBoard` |
| R27 | **AI 结果常驻 + 定时自动运行 + 自动刷新**: ①导读·优选落盘(`services/today_ai_store` → `user_data/today_ai.json`, 只留最新一份), GET `/api/today` 带出 `ai` 字段, 前端进页面常驻显示并标注'定时生成/上次生成'与'数据已更新建议重新生成'; ②定时任务(照复盘动态注册模式, 默认关闭, 工作日): 导读·优选默认 18:30、个股 AI 信号批量默认 19:00(scope 只跑持有/全部自选, **逐只串行+可配间隔默认20秒/只**), GET/PUT `/api/settings/preferences/{today-ai,signal-ai}-schedule` 改配置即时重注册 job、开启校验 AI Key; ③今日总览 AI 逻辑抽 `generate_today_ai` 供手动与定时共用; ④门槛面板加定时配置区; ⑤今日总览每小时自动刷新+聚焦刷新, 决策台 AI 信号每小时拉一次。测试 +8 | 新增 `services/today_ai_store.py`、`tests/test_today_ai_store.py`;改 `api/{today,settings}.py`、`services/preferences.py`、`jobs/daily_pipeline.py`、前端 `api.ts`、`Today.tsx`、`WatchlistDecisionBoard.tsx` | 低(定时默认关闭) | 删 store+两个 job 注册+端点+面板段 |
| R26 | **AI 一键分组**(自选页工具栏 ✨): 新增 `services/watchlist_ai_group` —— 原料用本地概念/行业标签(复用 `_load_concept_map_df`), AI 只做归纳命名与合并(降幻觉、可解释), max_tokens=None 对齐上游; `parse_groups` 纯函数严格校验(编造代码丢弃/同票只进第一组/组名截断6字/单只组退回未分组/未覆盖如实报数); **两段式**: POST `/api/watchlist/ai-group` 只出方案不动数据 → 前端弹窗展示分组成员理由与未归类清单 → POST `/ai-group/apply` 才写入, 支持「应用方案」(只移动涉及的票)与「清空后重分」, 同名分组复用不重建。测试 +11 | 新增 `services/watchlist_ai_group.py`、`tests/test_watchlist_ai_group.py`;改 `api/watchlist.py`、前端 `api.ts`、`Watchlist.tsx` | 低(纯新增, 不改既有分组行为) | 删服务+两个端点+按钮弹窗 |
| R25 | **同步上游(自选分组完善)**: 作者 3 笔——`a8a573a` 分组卡片视图(组内按涨跌降序/前N条可展开/卡片设置)+ 分组统计条(中轴分叉条形图)+ 分组拖拽排序(后端端点+9测试, 顺序贯通标签栏/统计条/卡片/侧栏)+ **监控规则编辑器支持从自选/自选分组批量导入标的**(去重合并)+ 修自选添加菜单滚动误关 + 前端扩展插槽; 另 2 笔 README 二维码。**零冲突自动合并**(分组功能与 fork 的 weight/决策台增强不在同一区域)。验证: fork 118 项测试通过、总览冒烟通过、tsc+vite build 通过 | 全仓合并 | —(基线推进) | 回退到存档 v4(`fdad334`) |
| R24 | **情绪周期当前阶段**: ①空数据误判修正——当日未落盘时末行(0板0家)不再被判'退潮第1天', 阶段卡改用最近定稿日+'定稿口径'角标; ②盘中实时阶段 GET `/api/regime/phase/live`, 按 Cap.QUOTE_POOL 门控(Starter+ 全市场实时): 实时快照聚合当日涨停生态+昨日定稿分母算晋级率, 拼接定稿序列跑同一 classify_phase_series 得盘中临时阶段, 前端每分钟轮询、可用时琥珀'盘中'角标(免费档 available=False 零痕迹零额度)。会员位预留: 升级后自动点亮 | 改 `api/regime.py`、前端 `api.ts`、`Regime.tsx` | 低(免费档零行为变化) | 删端点+卡片改造段 |
| R23 | **合并上游 v0.2**(12 提交/147 文件/+2.1万行): 市场阶段与主线识别+因子挖掘全链路、情绪周期完善×5、自选分组等权涨跌幅、五档盘口依赖实时开关修复、通知配色、AI max_tokens 放开(与 R22 同坑, 以上游为标准)。冲突 4 处调和: daily_pipeline 上游 mining 包装叠加 fork 当晚重试; main.py 保 today 路由; market_recap 保复盘三模式+上游空正文守卫; Screener 保只看自选+上游 memo。fork AI 调用(信号/导读·优选/调参)max_tokens 改 None 对齐上游, <think> 剥离/容错解析/自动重试保留。上游 main 的 2 笔(热路径信号缓存+日K快路径性能/uv chore)随后一并合入(dev.sh 冲突取我方超集)——v0.2 与 main 全部同步。**合并后修上游 2 bug**: ①regime 重算 500 → 分段兜错带真实原因返回(核心/阶段重标/主线回填分开, regime.py 补 logger); ②`rps_rotation._load_concept_map_df` 缓存只存 df 未存元组, 命中时解包成 Series → 主线回填/概念轮动 600s 内第二次起必败, 已修+回归测试 | 全仓合并 | —(基线推进) | 回退到存档 v3(`8e7a329`) |
| R22 | **AI 输出容错解析统一化**(用户常换厂家模型, 格式纪律参差): ①新增 `services/ai_json.extract_json_object`(原文直解→最大花括号块→截断补括号修复, 纯函数), AI 信号/六态调参顾问/导读·优选三处统一改用; ②AI 信号 max_tokens 300→800、信号词兼容中文(买入/卖出/持有/观望), 解析失败报错带原文摘录; ③导读·优选 max_tokens 800→1500, `parse_ai_brief_response` 原文兜底当导读仅全空报错, 前端失败红横幅+重试按钮(不再只靠一闪而过的 toast)。④解析失败自动带'只输出JSON'指令重试一次(零星失败自愈); ⑤思考型模型兼容: 解析前剥 `<think>…</think>` 推理段(未闭合=截断返回 None), token 上限给思考段留余量(信号 2000/导读 2500/调参 1500)——上限非目标, 普通模型零影响。测试 +19 | 改 `api/today.py`、`services/{stock_signal,livermore_service}.py`、前端 `Today.tsx`;新增 `services/ai_json.py`、`tests/test_{ai_brief_parse,ai_json_tolerant}.py` | 低 | 还原三处解析+删 ai_json.py |
| R21 | **当日数据未出齐自动重试**: 盘后管道(默认15:30)早于数据源发布(17:30~20:00)时 enriched 缺当日分区、梯队/概念/行业停昨日且要等次日调度——现在调度管道检测到 today_daily_incomplete 每 90 分钟自动重跑(当晚≤3次, 补齐即停, replace_existing 不堆积); 完整性检测下限 15:30→15:00(修"15:10 调度恰好躲过检测"漏洞) | 改 `jobs/daily_pipeline.py` | 低(仅调度包装+阈值) | 还原 _pipeline_with_retry 包装与 15:30 下限 |
| R20 | **把握分量价+胜率因子**(对齐"选最值得关注胜率最好"初衷): ①量能质量——候选量比(盘后快照批取+实时叠加层覆盖)≥1.5 放量+8 / <0.8 缩量-12; ②个股历史胜率——livermore `_bullish_event_win_rate` 纯函数(历史转入多头侧后 5 日为正比例, 未完整事件不计, 样本<3 不给), ≥60% +8 / ≤40% -12, 中间只展示; 仅带新信号候选计算(≤40 只), 缺数据静默跳过。规则层粗筛=新鲜度+AI+RS+量能+个股胜率, 形态识别归 AI 优选。测试 +6 | 改 `api/today.py`、`services/livermore_service.py`;新增 `tests/test_today_r20_quality.py` | 低 | 删 extras 段+两个胜率函数 |
| R19 | **确定性刷新**: `QuoteService.refresh_full`(连续拉取至轮转偏移回绕=全自选覆盖一遍, 每轮保留按 key 限速, max_rounds=12 防阻塞, 没转完如实报 partial)+ POST `/api/intraday/refresh-full`(带覆盖轮数与叠加层覆盖数); 今日总览「刷新」= 先全量同步实时('同步中…')再刷页面, toast 报'全量同步 N 只'/'未全覆盖'; 实时不可用静默退回收盘刷新。测试 +3 | 改 `services/quote_service.py`、`api/intraday.py`、前端 `api.ts`、`Today.tsx`;新增 `tests/test_quote_refresh_full.py` | 低 | 删 refresh_full/端点/按钮逻辑 |
| R18 | **盘中使用框架进页面**("盘中是执行层, 收盘是决策层"固化进 UI): 今日总览时段提示条(北京时间: 盘前=计划/盘中=执行·新信号等收盘/盘后=落盘定稿·纪律以定稿为准/周末=复盘); livermore 在实时价确实参与判定时给 trend 标 `intraday`(收盘落盘后自然消失)——今日总览机会标"盘中·待收盘确认"、决策台趋势徽章带 * 角标、个股趋势条琥珀"盘中口径"徽章; "持有票转跌"行动项盘中降级 mid 预警, 收盘定稿才是 high 纪律 | 改 `api/today.py`、`services/livermore_service.py`、前端 `api.ts`、`Today.tsx`、`WatchlistDecisionBoard.tsx`、`TrendStateBar.tsx` | 低 | 删 intraday 标注段+提示条组件 |
| R17 | **推送渠道批量设置 + 一键创建点位就地选渠道**: ①`monitor_rules.batch_set_channels`(存储层纯函数)+ POST `/api/monitor-rules/batch-channels`——一次改全部/指定规则的 feishu/wecom/dingtalk, mode add/set/remove, 未变化不重写, 改后同步引擎; 监控中心规则栏铃铛按钮开批量弹窗(未配置渠道置灰, set+空选清空有提示); ②点位提醒「推荐点位配置」区内嵌渠道开关(与下方通知渠道同状态), 一键创建全部带什么渠道所见即所得。测试 +5 | 改 `api/monitor_rules.py`、`strategy/monitor_rules.py`、前端 `api.ts`、`Monitor.tsx`、`PriceAlertDialog.tsx`;新增 `tests/test_monitor_batch_channels.py` | 低 | 删 batch_set_channels/端点/弹窗/渠道开关段 |
| R16 | **今日总览实时化**: 自选实时叠加层接入决策汇聚层——livermore `append_live_bar`(当天实时价作临时收盘追加进六态窗口, 日期新于最后一根才追加, 收盘落盘天然去重), trends 接口加可选 live 参数; today `_watchlist_live_map` 读叠加层(股票+ETF), 实时价参与六态/机会距离/出场线现价距离/持仓浮盈/组合净值; **纪律判定(triggered/fatal)仍收盘口径**。头部绿点"实时中(N只)"/关闭时琥珀提示。开关关闭零回归。**补全**: 实时映射抽为 `services/live_quotes` 共享, 决策台趋势列(`/trends`)、个股趋势条(`/trend`)、止盈线距离(`/exit-lines`)同接实时价——决策台全列实时(现价/浮盈本就实时); 线位与 triggered 仍收盘纪律口径, 监控规则同步不受实时价影响; K线弹窗当日蜡烛注入改为叠加层优先(免费自选档此前永远注入不了, 只查了付费档的全市场缓存), 头部现价随蜡烛实时。测试 +12 | 改 `api/{today,stock_analysis,watchlist}.py`、`services/livermore_service.py`、新增 `services/live_quotes.py`、前端 `api.ts`、`Today.tsx`;新增 `tests/test_today_live.py` | 低(live 参数可选, 默认行为不变) | 删 append_live_bar/_watchlist_live_map 及调用点 |
| R15 | **金字塔建仓路径**(利弗莫尔式"趋势确认后快速上满"的约束版): `build_pyramid_plan` 纯函数把目标仓位拆三步——试仓(默认35%)→ 站稳关键点 N 日(默认2)加至确认档(默认70%)→ 回踩不破上满; 收盘跌回关键点清试仓、计划作废。关键点取六态上关键点/AI 触发价; 目标<1成不拆步, 较小自动两步, 确认档强制>试仓档。参数(试仓%/确认加至%/站稳天数)进门槛面板(today_prefs)。机会条目理由下显示"建仓路径:…", 导出 HTML 与 AI 送审摘要同步。测试 +6 | 改 `api/today.py`、`services/today_prefs.py`、前端 `api.ts`、`Today.tsx`;新增 `tests/test_pyramid_plan.py` | 低 | 删 build_pyramid_plan 及调用点+prefs 三字段 |
| R14 | **PRD 缺口补全五件套**(逐条校对 PRD 后补齐):①**年线斜率**——decide_mode 纯函数化; 破50日线+年线拐头向下 → 软防守(走 2 日确认), 硬防守(破年线/年动量负)仍立即生效;②**全市场宽度**——复用 regime 历史涨跌家数, 普跌日(跌>涨×2)把进攻压到谨慎, 理由带"全市场 X涨/Y跌", regime 无数据静默跳过;③**仓位比例**——positions 加可选 weight(0-100%), 决策台成本旁"仓%"输入, 不填不影响既有功能;④**组合回撤纪律**——新增 `portfolio_history`(净值=1+Σ仓位×浮盈, 同日覆盖, 记高点), 回撤超纪律线(默认10%, 门槛面板可配3-30%)→ 行动区置顶降仓提醒; 总仓位超姿态基调 → 建议减差额; 体检组合行显示总仓位/基调/距高点;⑤**模式切换推送**——大盘模式变化落 alert_store(进行动区与监控中心, 防守 critical)+ 桌面通知。测试 +9(软防守确认/decide_mode 分支/weight 边界/回撤计算) | 改 `api/{today,watchlist}.py`、`services/{market_mode,positions,today_prefs}.py`;新增 `services/portfolio_history.py`、`tests/test_portfolio_gaps.py`;前端 `api.ts`、`Today.tsx`、`WatchlistDecisionBoard.tsx` | 低 | 按文件还原;数据卷多 `user_data/portfolio_history.json`, positions.json 多 weight 字段(向后兼容) |
| R11-R13 | **AI-TIS PRD 移植三件套**(以用户提供的 AI 半导体趋势投资系统 PRD 为指南, A 股适配, 全部纯规则零 AI 成本):**R11 大盘红绿灯**——新增 `services/market_mode`: 基准沪深300(缺数回退上证指数, 走独立 kline_index parquet 不碰选股链路), 收盘<年线 或 年动量<0 → 防守(一票否决立即生效); >年线且>50日线 → 进攻; 破50日线守年线 → 谨慎; 非防守切换需连续 2 交易日确认(同日刷新不重复计数, 翻回清零), 状态存 `user_data/market_mode.json`; 历史<200根明说退化观察。今日总览最终姿态 = 大盘与自选广度取更保守者, 横幅加大盘灯 chip。**R12 仓位建议**——姿态定总仓位基调(进攻≤8成/谨慎≤5成/防守≤2成/观察≤3成); 单票建议 = 单票上限 × 把握分系数(≥80满额/60-79七折/40-59三成/20-39一成) × 波动率压缩 min(1, 目标日波动/ATR日波幅)只压不加, 向下取整到半成, 不足半成给"仅观察仓"; 单票上限与目标日波动进门槛面板(today_prefs 扩展)。**R13**——①相对强度: 趋势结果附 ret_20d, 跑赢大盘5点+8/跑输5点-15; ②持仓操作档位 holding_stance: 离场/减仓/加仓/持有(加仓要求趋势刚走强+AI看多+离出场线5%以上缺一不可), 体检表新增列; ③组合汇总(平均浮盈/已触发/逼近/空头数)。测试 33 项新增(市场模式16/仓位8/R13 9) | 新增 `backend/app/services/market_mode.py`、`backend/tests/test_{market_mode,position_advice,today_r13}.py`;改 `api/today.py`、`services/{today_prefs,livermore_service}.py`、前端 `api.ts`、`Today.tsx` | 低(livermore 仅加一个输出字段, 其余自包含) | 删 market_mode.py+还原 today.py 相应段;数据卷仅多 `user_data/market_mode.json` |
| R1 | **构建/CI 必需项**:GHCR PAT 凭据 + 本分支构建触发;Dockerfile/pyproject README 路径修复(hatchling 构建失败) | `.github/workflows/docker.yml`、`Dockerfile`、`backend/pyproject.toml` | 低 | 不可回退(镜像构建依赖) |
| R3 | **数据可靠性三件套**(用户新卷首拉即复发上游盲区后重加):①**无进展判死**——`progress()` 写 `progress_at` 心跳,`reap_stale` 按「最后进度距今」而非总时长判卡死(首次全量拉取 >20 分钟不再被误杀,真卡死照常回收);②**历史稀疏自愈**——同步前查近一年 `count(DISTINCT date)`,<120(正常约240)判定历史大洞,强制走"从一年前重拉"分支(被中断的首拉/实时快照拉高 max(date) 导致补缺口分支永不回补的上游盲区);③**盘后完整性提示**——交易日 15:30 后同步完成时检查今日全市场日线行数,不足(阈值 max(100, 池一半))则完成消息+result 标记+前端红 toast「今日日线尚未出齐(仅N只),数据源一般 17:30~20:00 发布」 | `backend/app/services/pipeline_jobs.py`、`backend/app/jobs/daily_pipeline.py`、`frontend/src/pages/Data.tsx`、`frontend/src/lib/api.ts` | 中(改上游任务/管道判定) | ①还原 started_at 计时;②删 history_sparse 段与两处条件;③删完整性检测段与 toast |
| R10 | **今日总览打磨(存档 v2 收尾)**:①**机会区把握分筛选**——`rank_opportunities` 纯函数打分(0-100):趋势刚转强底分最高,按信号第几天加减(第 1-2 天最佳,第 4 天起判"已过入场窗口"扣分),AI 信号同向按置信度加分/反向重扣(矛盾机会直接滤),逼近买入触发价按距离加分;低于门槛或超出条数上限的不显示只报数量,同票多来源合并取高分。**卖出/风险提醒永不过滤**(行动区不经过打分)。②**门槛可配置**——`services/today_prefs`(today_prefs.json,min_score 0-100/max_show 1-50,损坏按字段回落默认),GET/PUT `/api/today/prefs`,页面「门槛」面板滑块+输入框即调即存。③**AI 导读·优选合一**——POST `/api/today/ai` 一次调用:候选(≤8 只)附真实日 K 20 根(量比/换手/均线/MACD/RSI/ATR)+关键价位送审,提示词要求先量价横向对比选 1-3 只(禁止拿"规则分高/AI 看多"当理由,规则分只是粗筛门票,宁缺毋滥),再基于优选写导读,两者结论必须一致;取不到行情的候选标注 kline_error。④**排版/文案**——页宽 1500px+列表大屏双栏;姿态原因/横幅统计/AI 提示词全面大白话(禁"胶着"类行话);中文名改走 `repo.get_name_map()`(自选表只存代码)。测试 21 项:打分排序/陈旧信号滤除/AI 加减分/上限/门槛读写边界/送审必须含量价/提示词关键约束 | `backend/app/api/today.py`、新增 `backend/app/services/today_prefs.py`、新增 `backend/tests/test_today_{opportunity_rank,prefs,select_payload}.py`、前端 `api.ts`、`Today.tsx` | 低(今日总览自包含) | 还原 today.py+删 today_prefs.py 与三个测试;数据卷仅多 `user_data/today_prefs.json` |
| R9 | **生命线(=20 日均线,自动)+ 今日总览导出 HTML**:生命线恒为 **MA20(收盘口径),自动计算无需手填**——标了「持有」即自动生效(不填成本也盯),**优先级高于 ATR 三阶段线**:收盘跌破 → stage=fatal「无条件清仓离场」(AI 信号纪律第 8 条:必须 sell、禁止"再观察"类表述);未破但 20 日线高于阶段线 → 生效线抬到生命线。监控双保险:生效线规则(生命线生效时 critical)+ 20 日线低于生效线时另立 `exitlife_*` critical 规则(注:MA20 每日变动,规则值随决策台加载/日线管道刷新);推送文案「跌破生命线(20日线) X!按纪律无条件清仓离场,此票不再看」。今日总览行动区置顶破位、K 线图独立画线、决策台止盈线列「生命线破位!」。**今日总览页新增「导出 HTML」**:自包含单文件(内联样式无脚本无外链,浅色排版),含姿态/行动区/机会区/持仓体检表,已生成的 AI 导读一并带上,可存档/分享 | `backend/app/services/{positions,position_exit,stock_signal}.py`、`api/{watchlist,stock_analysis,today}.py`、前端 `api.ts`、`WatchlistDecisionBoard.tsx`、`Today.tsx` | 低 | 还原各文件生命线分支;删 Today.tsx 导出段 |
| R8 | **今日总览(决策汇聚层)**:侧边栏首位新页面,把各模块既有产出按"需要行动的紧迫度"聚合成一屏——①**需要行动**(出场线已触发/距离<3%、持有票转入下跌趋势、近24h监控触发;空时明示"今日无需操作,管住手");②**值得关注**(六态刚转多/回升、AI 看多且现价距突破预案价<2%);③**市场天气**(自选口径多空比+新转多/转空计数→规则版仓位姿态:进攻/谨慎/防守/观察,判定可复现);④**持仓体检表**(浮盈/出场线距离/阶段/趋势/AI信号,已触发排前)。全部复用既有服务零新计算;条目点击直达个股分析页(个股页补 ?symbol= URL 参数支持);**AI 导读**按钮可选(3-4句盘前导读,一次调用,未配 AI 不影响页面) | 新增 `backend/app/api/today.py`、`frontend/src/pages/Today.tsx`;改 `main.py`(+注册路由)、`router.tsx`、`Layout.tsx`(+导航项)、`api.ts`、`StockAnalysis.tsx`(URL参数) | 低(纯新增页面+聚合接口) | 删两个新文件+还原注册/导航/URL参数三处 |
| R7 | **持仓出场线(ATR 三阶段:止损/保本/移动止盈)**:对「持有+已填成本」的自选每日确定性计算出场线——①浮盈<1×ATR:止损线=成本−k×ATR(六态下关键点更近时取其为准,两道防线先触者算);②1~2×ATR:保本线=成本;③≥2×ATR:吊灯止盈线=持仓以来最高收盘−k×ATR(棘轮只上移,以逐日运行最大值实现,无状态可复现);进入③后线不低于成本。锚点是持仓最高价而非"成本×比例"(市场不知道成本;成本只定阶段)。k 默认 3.0、20cm 板 3.5;ATR14=TR 简单均值;"持仓以来"以标记成本日(positions.updated_at)为起点。**展示**:决策台「止盈线」列(线位+距离,逼近琥珀/跌破红+已触发)、K 线「持仓止盈」价位组、AI 信号纪律第 7 条(watch_points 必须含该线,AI 不得改数值,action 按阶段:跌破止损/保本离场/止盈了结)+持仓上下文注入。**推送**:出场线自动同步为价格监控规则(id exit_*,cooldown 1 天,收盘价口径),复用现有引擎盘中评估与通知;决策台加载与日线管道完成时刷新,平仓自动清理规则 | 新增 `backend/app/services/position_exit.py`;改 `api/watchlist.py`(+/exit-lines)、`api/stock_analysis.py`(levels 注入 exit 组)、`indicators/levels.py`、`services/stock_signal.py`、`jobs/daily_pipeline.py`(+刷新钩子)、前端 `api.ts`、`AnalysisKChart.tsx`、`WatchlistDecisionBoard.tsx` | 低(纯新增;管道仅加 try/except 尾钩子) | 删 position_exit.py + 按文件还原;监控规则里删 exit_* 前缀规则 |
| R6 | **AI 信号行动导向 + 六态汇总/导出**:①决策台 AI 信号列显示 watch_points 到价预案(↑涨至X→操作 / ↓跌至Y→操作;数据早已缓存,现有信号即刻可见);②信号提示词按原作者四维分析的骨架重写(资深人设+信号纪律六条+数据说话),明确 watch 仅限多空证据矛盾/不成熟时用、reason 必须点明下一步触发条件、关键价位存在时预案必须一上一下、逆六态趋势信号需更强证据;③生成信号时注入六态趋势上下文(状态/持续/关键点/参考动作);④决策台「六态汇总」按钮:统计卡(总数/多头/空头)+六状态页签+明细表(零 AI 成本,复用批量趋势),可**导出自包含 HTML**(内联样式无外链无脚本,浅色排版可存档分享) | `backend/app/services/stock_signal.py`、新增 `frontend/src/components/stock-analysis/TrendSummaryDialog.tsx`、`WatchlistDecisionBoard.tsx` | 低(提示词+纯新增 UI) | 还原 stock_signal.py 提示词段;删 TrendSummaryDialog + 按钮 |
| R5 | **六态趋势分析(利弗莫尔 Market Key)**:引入用户提供代码包的六态状态机(逐日状态+关键点序列与 JS 原版对拍完全一致),文案沿用原版表达(六状态名/操作建议/转多·转空·回升·回撤/上·下关键点/多头·空头);操作建议动态代入具体关键点价位(如「观望,下破 1028.00 确认转空」),拒绝"上一关键点"式模糊指代。只用日线收盘价(复权口径,与关键价位同源),窗口近 180 交易日。**显示**:①个股分析页趋势条(状态徽章+持续天数+关键点+操作建议);②K 线背景多空分段着色(复用图表预留 ranges 通道)+「六态关键点」新价位组(markLine+点位提醒弹窗自动可用);③决策台「趋势」列(批量接口一次拉取,可排序,悬停看详情)。**回测调参**:3%~15% 阈值网格回测(纯函数毫秒级,指标:翻转/假信号率/段长/跟随收益 vs 买入持有/前后半段一致性),规则建议保底(邻域平滑防尖峰过拟合+样本不足明示),可选 AI 调参顾问(一次调用,只准选网格内阈值,提示词内置防过拟合纪律;走 ai_provider 带重试);阈值每票可覆盖(livermore.json,default+overrides),弹窗一键应用/恢复默认。趋势判定零 AI 成本 | 新增 `backend/app/indicators/livermore.py`、`backend/app/services/livermore_service.py`、`frontend/src/components/stock-analysis/TrendStateBar.tsx`;改 `api/stock_analysis.py`(+4 端点+levels 注入)、`indicators/levels.py`(+livermore 类型)、`AnalysisKChart.tsx`、`StockAnalysis.tsx`、`WatchlistDecisionBoard.tsx`、`api.ts`、`queryKeys.ts` | 低(纯新增,仅 levels 响应多一组) | 删除两个新文件+按文件还原;数据卷仅多 `user_data/livermore.json` |
| R4 | **增强功能整批移植**(用户指示"还有什么能做的一起做了",从 `claude/rebuild-tickflow-enhancements` 一次性搬入全部剩余增强,均带 `[fork 增强]` 注释标记):**自选决策台**——自选页每票"AI 信号"卡片(WatchlistDecisionBoard,规则先行+可选 AI,含 stock_signal/positions 服务);**关键价位智能选择**(规则版一键选指标);**复盘三模式**(快速/标准/深度)+归档标注生成模式+历史报告整合(JsonReportStore);**连板梯队 AI 战法**+部分数据日防线(当日行数不足不生成误导梯队);**策略体检**(StrategyHealthDialog,胜率/期望/样本量);**参数扫描增强**;**钉钉 webhook 适配**;**监控规则编辑器增强**+仅自选开关;**AI 调用韧性**——429/5xx 重试(2s/5s 退避)+失败显式报错到前端(不再静默 200+error);**任务协作取消**(JobCancelled + is_cancelled 心跳检查);**数据持久化自检**(容器内 data_dir 非挂载点则启动告警+数据页红横幅,防拉新镜像丢数据);**select 崩溃修复**、**repository 叠加层/写盘守卫**、**stocksdk bridge 单位修正**(手/股,该插件默认不打包仅修源码)。⚠️ 移植时已剔除旧版 #47 事故代码:日K 拉取严禁多 key 池(free-api 无视 key,免费 key 打付费端点被拒致全量拉取失败),`sync_daily_batch` 保持上游单 client;每夜除权同步(旧#46)未搬入,除权保持上游管道阶段 | 后端 api/services/strategy/jobs 共 29 文件、前端 20 文件(见本次 commit)、`backend/app/main.py`(手工植入,保留作者同日新增的文件日志) | 中高(覆盖面大,但均为增量功能,数据管道核心仅 R3 已述改动) | 按功能对应文件 `git checkout upstream/v0.2 -- <文件>`;main.py 删 `data_dir_persistent` 段 |
| R2 | **免费多 key 池化(修正版)**:同一 key 字段填多个免费 key(逗号/换行/空格分隔),每 key 各 5 只自选实时额度凑成 5×N,自选超容量时轮转窗口全覆盖。`secrets_store` 加 `get_tickflow_keys()`(`get_tickflow_key` 改为取第一个,单 key 行为不变);`client` 加 `get_realtime_client_pool()`(**仅实时**;付费端点。历史日K的 free-api 路径严禁用池——免费 key 打付费端点会被拒,此为旧版 #47 事故教训);`preferences.get_realtime_watchlist_symbols` 解除 5 只截断;`quote_service._fetch_watchlist_quotes` 池轮询+轮转窗口+按 key 独立限速;Keys 页输入提示。⚠️ 多免费 key 池化可能违反 TickFlow ToS,用户知情自担 | `backend/app/secrets_store.py`、`backend/app/tickflow/client.py`、`backend/app/services/preferences.py`、`backend/app/services/quote_service.py`、`frontend/src/pages/settings/Keys.tsx` | 中(改上游 key/行情层) | `git checkout upstream/v0.2 -- <上述文件>` |
