# R428 — AI 四维分析的入口从个股分析页头挪进个股弹窗

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R428 | 用户截图(个股分析页头「当前 大普微 301666.SZ」后面那颗 ✨): 「ai 四维分析想要放到弹窗里面去, 找个合适的位置, 外面就不要了」。页头那个按钮删掉; 个股弹窗(`StockPreviewDialog`)顶栏操作区加一个 ✨, 排在「自选」「加监控」之后、「刷新 / 放大 / 关闭」之前 —— 前三个是**对这只票做的事**, 后三个是**对这个弹窗做的事**。作用对象是弹窗当前这只(含弹窗里切过的), 不用再回页头看「当前」是谁。流程(当日已分析过 → 确认查看今日报告 / 重新分析 → 发起分析)仍是这一页原来那一套, 弹窗只是入口: 新增入参 `onAiAnalyze` / `aiBusy`, 传了才出按钮 —— 目前只有个股分析页传, 别的页面打开的个股弹窗不变。**顺带修掉一个会随之出现的 bug**: 「今日已分析过」确认框原来渲染在个股弹窗**之前**, 两者都是 fixed z-50, 同层级后渲染的在上 —— 入口进了弹窗后, 确认框会被弹窗盖住、点了像没反应; 挪到弹窗之后。行内(决策台「AI 信号」那一列)的 AI 按钮用户没提, 未动。R399 棘轮跟着下调: 删掉的页头按钮带走了一处裸圆角与一处硬编码色(945 → 944、1047 → 1046)。 | 改 `frontend/src/components/StockPreviewDialog.tsx`、`frontend/src/pages/StockAnalysis.tsx`;新增 `backend/tests/test_ai_entry_in_dialog.py`(3 条: 页头没有 / 弹窗有且作用在当前这只、位置在对票操作那一组 / 确认框在弹窗之后);改 `backend/tests/test_design_spec.py`(棘轮下调);**后端业务代码、打分系统、`strategy/`、六态 diff 全空**;变异 4 个全杀;后端 4518 passed、vitest 155、tsc / eslint / build 0;出图核对按钮位置与悬停 | 低 | 可(页头按钮加回, 弹窗不传 `onAiAnalyze` 即不显示) |
