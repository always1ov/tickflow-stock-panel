# R421 — 全站禁止粉色 / 洋红 / 玫红

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R421 | 用户: 「斐波那契二型换种颜色, 整个系统禁止少女系风格, 比如粉色, 投资是一件很严肃的事情」。按 OKLCH 色相扫了整个前端与后端发出的颜色, 粉区(315°~12°)里的全部换掉: **价位组三个** —— 斐波那契二型 洋红 → 靛青(亮 `#322097` / 暗 `#7687BA`, 暗色第一版 `#807EBA` 出图偏薰衣草, 往钢蓝挪)、ATR 波动通道 玫红 → 赭橙(`#9A3412` / `#E07A12`, 警示色不借红, 红是涨)、六态关键点 品紫 → 宝蓝(`#2859B1` / `#2288E8`)—— 三组新色都过 R409 那组守卫(两两 ΔE ≥ 8、离 K 线涨跌 ≥ 10、对比度、一二型隔 60°), 后端 `dinapoli.C_RETR` 同步改; **换色前缓存里的响应仍带旧键 `#E01DB5`**, 前端 `fib2RoleColor` 加了退役键映射, 否则会原样画成洋红。**其余十处**: 行情 K 线的布林线 `#E879F9` → 青 `#14B8A6`、情绪周期「高潮」品红 → 深红、换手率最高一档 → 紫、板块轮动走势线里三个粉 → 蓝灰 / 褐 / 浅灰、因子分组 Q6 → 蓝、「动能」分量条 rose → red、涨停梯队名字 rose → red / 近白、持仓提醒日 rose → 琥珀、监控「量能增量」标签 rose → 靛、转折模拟盘「主线」品红 → 琥珀;自选**分组颜色**「品红」「玫红」两个选项下架 —— 后端仍认这两个 id(接口不动), 已存成它们的分组显示成紫 / 橙;`index.css` 与 `tailwind.config.ts` 里 fuchsia / rose 两族令牌删掉。新守卫 `test_no_pink.py`: Tailwind 三族类名与族名、hex / rgb / hsl / OKLCH 令牌落进粉区一律红, 注释与测试样本不算, 确需保留的值进 `ALLOWED` 写明理由(现有一条: 退役查表键)。**红不受影响**(22°~29.5°), 紫没被点名不扩大。`AGENTS.md` 硬约束加第 15 条。 | 改 `frontend/src/lib/theme.ts`、`backend/app/indicators/dinapoli.py`、`lib/watchlist-group-colors.ts`、`lib/api.ts`、`index.css`、`tailwind.config.ts`、`components/EChartsCandlestick.tsx`、`SectorRotationCard.tsx`、`stock-analysis/AnalysisKChart.tsx`(注释)、`pages/FlipPaper.tsx`、`Watchlist.tsx`、`ConceptAnalysis.tsx`、`IndustryAnalysis.tsx`、`LimitUpLadder.tsx`、`Lots.tsx`、`Monitor.tsx`、`backtest/charts/FactorGroupNavChart.tsx`、`backend/tests/test_flip_fusion.py`(断言跟色)、`AGENTS.md`;新增 `backend/tests/test_no_pink.py`(6 条)、`frontend/src/lib/watchlist-group-colors.test.ts`;**打分系统 / `strategy/` / 六态判定 diff 全空**(六态关键点只换了显示色);变异 10 个全按预期(含"注释里写旧值不该拦");R399 硬编码色棘轮未升 | 中(碰的文件多, 但每处只是换一个颜色值) | 可(逐文件换回旧值; 守卫会跟着红, 需一起撤) |
