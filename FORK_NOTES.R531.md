# R531 — 文字收成两档、去蓝: 正文近黑, 次要并进正文, 辅助只去色不调浅

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|---|---|---|---|
| R531 | 用户拿 Aipha AI(shadcn/ui 默认主题)的页面问「黑白配色是怎么搭的, 尤其是字体背景色」, 看完分析后说「改」。分析给了三条建议, **这一轮只做第一条**: ① 文字收成两档 —— 亮色正文 #222738 → #0A0A0A(近黑中性, 19.8:1), 次要文字 `--fg-secondary` 并进正文(同一个值), 辅助 #5A6377 → #636363(**只去蓝, 明度仍是 R408 压深后的 0.50**, 6.0:1); 暗色次要 #c4c4cb 并进正文 #fafafa, 辅助不动。层级改靠字号与字重拉开(Aipha 的做法), 不靠第三种灰。**没做的两条, 因为会推翻用户自己定过的事, 先问**: ② 悬停灰调浅、边框调浅 —— 正是 R408 用户说「所有颜色显示都淡了好多, 看起来好辛苦」之后压深的那几档(test_R501 钉着「次级面明度没被调浅」); ③ 深色模式卡片并进页面底 —— 暗色的 base/surface/elevated/border 是 R163「还是用回以前作者的黑」定过案的(test_R382_作者的黑一个字没动 钉着)。底色、边框、悬停、强调色、红涨绿跌一个没动。index.css 里「文字三级必须分得开」那条设计说明标注了改口 | frontend/src/index.css; backend/tests/test_theme_palette.py(CHROME 三个文字值) | 低: 只改三个令牌值 | 可以: git revert 本提交 |
