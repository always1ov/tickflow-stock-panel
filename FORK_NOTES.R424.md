# R424 — 关键价位配色整张重排: 不浅、不挨着

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R424 | 用户: 「关键价位指标的颜色不能用浅色的, 要用深色, 而且不能很接近」。量出来两处: ① **暗色那一份是一整套粉彩**(浅桃 #FCB177、浅天蓝 #86C0FC、浅靛 #7F96FA、近白 #DBE6F2), 用户用的正是暗色(截图底是黑的); ② **R421 为躲粉色把六态关键点 / 二型 / 布林 / 推算位四组全挤进蓝靛一带**, 最近一对只差 ΔE 8。两套整张重排: **暗色一律饱和色**(彩色色度 ≥ 0.10、明度 ≤ 0.87), **亮色一律深色**(明度 ≤ 0.65); 两两 ΔE **暗色 ≥ 12、亮色 ≥ 11**(原来 8), 量化通道三档仍刻意同族(三档之间 ≥ 9.5, 色相散开 < 25°)。排法: 15 种颜色(13 组 + 二型推算位 / 作废线)在「避开涨红跌绿 + 禁粉 + 一二型隔 60°」之后, 冷暖两侧都不够分, 所以**三个中性组按明度占三档** —— 六态关键点 = 最强(暗白 `#FBF9F1` / 亮黑 `#07080A`)、前高前低 = 次强、整数关口 = 中灰 —— 冷暖两侧各让出一个位置; 二型从靛青挪到青绿(暗 `#17DEA9` / 亮 `#004631`, 离一型的金隔 77°~82°、ΔE ≥ 20), 枢轴点电紫、布林靛、推算位天蓝, 三者靠明度与色相一起拉开。用退火 + 手工挑(退火单跑会挑出薰衣草、橙粉这类不严肃的色, 加了"暗色紫区不许偏亮、暖区不许偏淡"的约束再手调)。**亮色做不到 12** 是白底的物理限制: 黄 / 金一压深就成了橄榄土色, 暖色那侧挤不下, 这一点写在 `theme.ts` ⑥。后端 `dinapoli` 三个角色键(= 亮色值)跟着换, 前端 `FIB2_ROLE_RETIRED` 收进 R405~R423 的全部旧键, 缓存里的旧响应仍按角色上新色。守卫: 两两 ΔE 按主题分档、新增「没有浅色」「中性三档强弱次序」两条。 | 改 `frontend/src/lib/theme.ts`、`backend/app/indicators/dinapoli.py`、`backend/tests/test_level_palette.py`(门槛 + 2 条新守卫)、`frontend/src/lib/futureZone.test.ts`(退役键全覆盖)、`components/stock-analysis/AnalysisKChart.tsx`(注释);**打分系统 / `strategy/` / 六态判定 diff 全空**(只换显示色);变异 7 个全杀(暗色回粉彩、浅靛、亮色变浅、枢轴撞布林、六态撞前高、中性次序乱、漏映射旧键);13 组全开在亮 / 暗两主题出图核对 | 中(整张配色换了, 看惯了的颜色都会变) | 可(`LEVEL_PALETTE` / `FIB2_ROLE` / 后端三常量换回 R421 的值, 守卫门槛改回 8) |
