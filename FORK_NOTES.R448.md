# R448 — 现状块删掉「三档位置」小标题

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R448 | 用户指着现状块最右那一块: 「删掉三档位置这四个字」。R445 给这一块补的小标题撤掉, 格子、上轨 / 中轨 / 下轨、短期 / 中期 / 长期与各自位置照旧。守卫 `test_R445_现状块删掉打码的部分_没有分隔竖线_四块等分` 改为钉「三档位置」不许出现在渲染文本里; 变异 1/1 杀死(小标题加回来) | `frontend/src/components/stock-preview/StatusSection.tsx`、`backend/tests/test_status_section.py` | 低(只在 fork 自己的新块里) | 回退本提交即恢复小标题 |
