# R490 — 两张副图共用单票日 K 历史缓存(加载提速)

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|---|---|---|---|
| R490 | 用户:「优化一下性能计算什么的, 现在加载出来要等很久, 想个好方案」。先测后改: 用与真实库同布局(按交易日分区, 每天一个全市场文件)的模拟库, 1000 个交易日 × 5000 只, 走真实 KlineRepository 与真实端点计时。每个副图请求约 450 ms, 拆开: 扫 1000 个日文件挑出这一只 ~300 ms(随历史年数线性变长)、没指定列而顺带重算全套指标 ~90 ms、指标本身计算 ~45 ms; 且无任何缓存(第二次照样 450 ms), 两张副图各扫一遍, 前端盘中每个实时行情都让两张副图重取。新增 services/ohlcv_history.py: 只读 6 列(走仓库的列下推快路径, 不重算指标)、同一只票只扫一次放进内存(库里最新交易日变了或过 10 分钟才重扫; 最多 64 只, 超出淘汰最久没用的)、同一时刻同一只票只让一个请求去扫(single-flight)、返回拷贝。/quant-macd 与 /trend-quant 都改从它取(量化MACD 再切出最近 1500 天, 行与原来按区间取的一模一样); 盘中实时那一根照旧每次请求自己叠, 不进缓存。结果: 两个端点在模拟库上的 12 份完整输出(3 只票 × 2 端点 × 2 种根数)与改前逐字一致; 量化MACD 第二次起 16 ms(原 430)、趋势量化共用同一次扫描 62 ms(原 440)。进程启动后首个请求的约 4 s 是作者仓库预热全市场缓存, 所有接口都有, 未动。新增 tests/conftest.py(每个用例前清缓存, 防假仓库 id 复用串数据)与 test_ohlcv_history.py 10 条; 变异: 缓存失效 → 3 条红, 不走列下推 → 1 条红 | backend/app/services/ohlcv_history.py; backend/app/api/stock_analysis.py; backend/tests/test_ohlcv_history.py; backend/tests/conftest.py | 低(新文件为主; stock_analysis.py 只换了两处取数) | 可以: git revert 本提交 |
