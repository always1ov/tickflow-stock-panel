# R386 — 触发价撑破格子,压到右边一列的字上

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R386 | **用户: 「显示有问题」**, 截图里「手上这些」那一档的触发价**压在右边一列的股票名上**(「现 46.23深科技」「现 284东山精密」)。**是我 R385 算错了列数**: 那一版按「标的 176 + 动作 72 + 六态 112~256 + 间距」≈**560px** 定的三列, **漏掉了触发价** —— 它是 `auto` + `whitespace-nowrap`, 有值时要 ~175px, 一行实际要 **~740px**, 塞进 539px 的格子就溢出。**而这是同一个错犯第二次**: R385 的教训写的是「`shape.trend` 为假并不意味着别的也没有」, 我当时只把「名次与走势同进同出」这一条吃进去了, 没意识到**触发价是第三个独立的东西** —— 「手上这些」那一档每行都有它(还没触发的离场线), 「要动手」那一档反而没有(转折已成, 不再有待触发的线)。所以列数改成**按这一组有没有触发价**分两支: 没触发价 ≈560px → 1180 起两列、1560 起三列(原样); 有触发价 ≈740px → **1560 起两列, 三列直接放弃**(三列要 ≥2560 的视口, 不现实)。**按组算而不是按整屏算** —— 按整屏的话, 「手上这些」有触发价就把「要动手」那一档也一起拖成两列。**这个 bug 断言 class 串是抓不到的**: 类名一个不少, 错的是算术。真正抓到它的是**拿真浏览器逐行量 `scrollWidth > clientWidth`** —— 复现做法: 拦 `/api/flip-paper` 喂一份「5 行要动手(无触发价) + 10 行持仓(每行都有触发价)」的数据, 在 1990/1700/1560/1400/1180/900/390 七个宽度上逐行量。**撤回修复复验过**: 1990 下 2 行撑破(548>539), 1560 下 10 行全撑破(534>396); 修完七个宽度**零撑破、零叠字**。**守卫钉的是算术的依据**(列数必须看触发价、必须按组算、算出来必须真用上), 外加一条钉住 `whitespace-nowrap` 不许去掉 —— 它是这一格宽度需求的来源, 去掉会让它在窄格里折行, 而**行高就跟着这一格变**, 那是 R356 定死行高要挡的事; 该让列数去适应它, 不是反过来。**顺带删掉我自己刚写的一行废断言**(`assert ... or True`, 恒真), 并补上它本该有的反向那一半(没触发价那一支的三列不许被顺手砍掉)。**5 处变异逐一确认会红**, 其中 M1 就是这个 bug 本身。 | 改 `pages/FlipPaper.tsx`(`plainCols`/`plainCell` 按 `plainHasPrice` 分两支);`tests/test_flip_fusion.py` +3 条;**后端 `app/` diff 全空**;**判定、六态、打分、口径、指标色、内容没动**;后端 pytest **4238 passed / 10 skipped 退出码 0**, 前端 tsc 0 / eslint 0 error / vitest 47 条全过 / build 通过;七个宽度实机逐行量过无溢出 | 低(只改没进候选池那一组的列数断点) | 可 |
