# R493 — 个股日K副本复查修补(空副本、补历史、发布中、淘汰、清空数据)

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|---|---|---|---|
| R493 | 用户:「再检查梳理一次」。按 code-review 技能分两路复查 R491/R492(对照 CONTRIBUTING/AGENTS 规范一路, 对照用户原话与动手前承诺一路), 逐条核实后修。会读到旧数的两处: 一是截止日前没有这只票时留了空副本, 空副本抽查直接放行, 之后管道把它的历史补进已有日文件(分区数不变)就连着 7 天只给最近十来根(复查实测原路 30 行、副本 10 行) —— 改成不留空副本, 且磁盘上万一有空副本也不认; 二是往已有日文件里补这只票更早的历史、或补停牌后那段, 首尾两根与分区数都不变看不出来 —— 抽查加两头外侧: 第一根的前一个交易日、以及截止日(副本最后一根早于它时), 分区里这只票必须没有行。承诺过的「这只票除权就重建」原来只靠抽查间接兑现, 现在正面接上: 盘后管道按除权重算完就删那几只的副本(精确到票), 全量重建整类作废, ETF 除权同理; 管道发布标记未就绪时不建也不用副本(建的途中开始发布则这份不存), 免得把半新半旧存下来。其余: 抽查那天分区里这只票有重复行时原来次次重建(结果不错但白费), 改为对照那天的全部行; 重复列名原来在异常兜底之外抛出仓库, 挪进兜底; 淘汰从按建的时间改为按最近用过(读到就刷新修改时间), 自定义策略逐只扫全市场也挤不掉天天看的; 建副本时顺手清崩溃留下的超过 1 小时的临时文件与孤儿说明; 「清空本地数据」一并删副本、占用统计算上副本; 说明文件字段改成 format/partitions, 局部变量不再叫 copy; 删掉被抽查完全覆盖的列检查。记下但不改: 分区目录名与行里 date 不一致时会与原路差几行 —— 仓库唯一的写法按 date 列落盘, 写进模块说明作为前提; 单票锁里建副本是有意的 single-flight(只挡同一只票, CONTRIBUTING §6.2 防的是全局锁); 管道的最近 5 天收盘价修复全在现读的 10 天之内, 不会留下旧副本(已核对 _prune_stale_price_partitions 的 max_dates=5)。验证: 294 项对照在读旧副本、全新建、读新副本四轮都与改前逐字一致; 耗时与 R492 持平; 新增 14 条用例, 在 R492 的代码上 10 条红; 变异 37 个 36 红, 活下来的仍是格式版本号(列与内容一变抽查就拦下, 版本号留给以后含义变了的升级) | backend/app/services/symbol_daily_store.py; backend/app/jobs/daily_pipeline.py; backend/app/api/data.py; backend/tests/test_symbol_daily_store.py | 低~中(daily_pipeline.py 在 enriched 重算后与 ETF 日K写完后各加几行作废; api/data.py 清空名单与占用统计各加一项; 上游改这两处时按测试 test_R493_管道与清空数据都接上了副本 补回) | 可以: git revert 本提交(回到 R492 的副本行为) |
