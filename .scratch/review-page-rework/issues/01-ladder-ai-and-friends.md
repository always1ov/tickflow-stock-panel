Status: ready-for-human
Type: grilling

# 整改复盘页时先提醒用户: R504 撤掉的四样要不要回来

用户原话(2026-09-24):「切断 ai 打板复盘, 后续我做复盘部分再提醒我, 保持干净和上游一致准备整改这个页面」。
**下一次动复盘页(`frontend/src/pages/Review.tsx`)之前, 先把下面这张表念给用户听, 逐项问要不要。**
不要默认接回来, 也不要默认永久删掉。

## R504 撤掉了什么(都是 fork 加的, 上游没有)

| 撤掉的 | 原来在哪 | 做什么用 | 存档 |
|---|---|---|---|
| AI 打板复盘 | 复盘页页头按钮 + 弹窗(`components/LadderAiReview.tsx`) · 后端 `POST /api/screener/ladder-ai` 与历史报告两个接口 | 把当天连板梯队快照交给 AI, 按龙头 / 二进三 / 反包分组出候选清单, 可追问; **喂给 AI 的参考里带着市场环境最新一天、看板情绪分、市场宽度、概念/行业领涨** | `data/user_data/ladder_ai_reports.json`(最近 30 份)**没删** |
| 板块跷跷板 | 打板复盘弹窗里的第二个标签(`components/regime/SeesawPanel.tsx`) · 后端 `/api/regime/seesaw` 两个接口 + `services/seesaw.py`、`seesaw_store.py` | 规则找「一边熄火另一边点火」的板块对, AI 甄别哪几对成立 | `data/user_data/seesaw_history.json` **没删** |
| 复盘三模式 | 复盘页页头分段按钮「当日 / 连读昨日 / 近7日」 · 后端 `market_recap` 的 `mode` 参数 | 连读: 先核对上一份复盘的观察要点兑现没有; 近7日: 以 7 个交易日为主轴看情绪演变 | 旧复盘存档里的 `mode` 字段还在, 只是不再显示 |
| RPS 轮动的复盘页入口 | 复盘页页头「板块RPS轮动」(R105 收编的统一入口, 弹窗里能切行业/概念) | —— **功能没撤**, 入口放回了作者原来的行业分析 / 概念分析两页 | 无 |

## 想接回来怎么办

- 整套接回: `git revert` R504 那个提交(见 `FORK_NOTES.R504.md`)。存档文件都在, 接回来历史报告原样能看。
- 只要其中一样: 从 R504 之前的提交里取对应文件(`git show <R504^>:path`), 按上表的接线点接回。

## 整改时顺带要想清楚的

- 用户的打法是「大道至简, 趋势为王, 少动」—— 打板本身是高频短线, 跟这条纪律方向相反。
  接不接回来, 先问它会让人多动还是少动(AGENTS.md 第 10 条)。
- 复盘三模式里「连读昨日」是唯一一个让复盘**自己对自己负责**的机制(核对昨天说的兑现没有), 值得单独问。

## Comments
