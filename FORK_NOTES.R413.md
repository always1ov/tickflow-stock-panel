# R413 — 主图补三样:密集带标价、现价贴签、「未来」区(只在二型开着时才存在)

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R413 | 用户拿了一份「主图必标」的清单来问对不对。**逐行核对之后是三行对、两行有实缺口、两行与用户口径冲突**(冲突那两行是把「失效位」当止损、把扩展位当"决定盈亏比必须同屏" —— 前者不是卖出线、后者与「趋势没坏就不给卖出理由」相反, 已在对话里逐条说明, **代码一个字没按它改**)。这一轮做的是那两个实缺口加一个欠了三轮的账: ① **回踩密集带标上上下沿价格** —— 它是这一组里唯一真会拿来挂单的东西, 而在此之前那块色带上**一个数字都没有**, 读不出价就挂不了单; ② **现价贴签** —— 在此之前只有 hover 时的十字线, 手一移开就没了, 而「离那条线还差多少」是要反复看的; ③ **「未来」区** —— 位移均线平移之后露到最后一根之外的那 3 个值, R405 就把数据发过来了、一直没画, 因为画它要往右扩空槽而 **x 轴是所有价位组共用的**。**第三样的关键不是怎么画, 是什么时候不画**: 用户原话「避开影响就独立显示, 只为了看看而已, 别影响六态那些」—— 落实成 `futureVals = activeTypes.has('fib2') ? ... : []`, 二型开关没开就**一个空槽都不加, 整张图与加这段之前逐字节相同**; 六态的分段底色、关键点线、别的十一组全都碰不到。**没有画成 3 根灰蜡烛**(用户说的「用灰色K线」): 未来的开/高/低/收根本不存在, 画出来就是编的, 而**一根编出来的蜡烛在图上和真的长得一模一样** —— 所以只给 3 个空槽加一块中性灰底(标「未来(均线已知)」), 均线自然探进去。**现价贴签出图两次才放对**: 第一版放右边, 结果是「15.90 / 浅回踩 15.72 / 现价 15.50」叠成一团谁也读不出; 第二版挪到左边, 左边距只有 56px(刚够 y 轴刻度), 「现价」的「现」被切掉; 第三版放进绘图区内侧, **被贴得近的价位线直接穿过去** —— 价位线和它在同一片画布上, 调 `z`/`zlevel` 都压不住(试过, 无效)。最后的做法是**结构上不重叠**: 右边预留带 144px, 价位标签从 +6 起最宽约 66px, 现价让到 **+78** 单独占一列。**这三样全是出图才发现的**, 靠读代码一个都看不出来。**守卫 +3 条, 4 个变异逐条确认会红**, 其中最要紧的一条钉的就是「二型没开就不许多出空槽」—— 那条退化得极安静: 图上只是右边多一小条灰底, 没人觉得不对, 但那时候每一只票、每一次打开都在动共用坐标轴。 | 改 `frontend/src/components/stock-analysis/AnalysisKChart.tsx`(未来槽位与灰底 / 现价 markLine / 密集带标签);改 `backend/tests/test_fib2_readability.py`(+3 条);**后端逻辑一个字没动**;**打分系统 / `strategy/` / `keltner*` / 六态 / `position_exit` / `today*` diff 全空**;后端 pytest **4478 passed / 4 failed / 10 skipped**(那 4 个是 R411 记过的时间依赖, 与本轮无关), 前端 tsc 0 / eslint 0 error / build 退出码 0 / vitest 128 passed | 中(动到 x 轴的槽位数 —— 那是所有价位组共用的; 但**只在二型开着时才动**, 关着时逐字节相同, 并由守卫钉住) | 可(删未来区那个 useMemo 与灰底、删 `nowLine`、密集带标签去掉两个 `toFixed`) |

**那份清单里我认为是错的两行, 记下来免得下次又被同一份文档说服**:

| 清单说 | 为什么在这个仓库不成立 |
|---|---|
| 「止损 —— 进场前就要看到最坏结果」 | 它指的是**失效位**, 而那条线**不是止损**: 它的意思是跌破之后这一整组位置不再成立。R410 改名成「这组作废」的理由原话就是「失效太抽象, 而且容易被读成一条卖出线, 它不是」。本仓库的止损口径是**生命线**(收盘跌破无条件清仓)与**止盈线** —— 让二型也来答这个问题, 就是两个体系抢同一个答案, 而且给的数不一样。 |
| 「目标一二 —— 决定盈亏比, 必须和进场区同屏」 | 「盈亏比」是**目标价驱动**的交易框架; 用户的纪律是「趋势没坏就不给卖出理由, 卖点是止盈线、生命线、六态转弱」。R410 已把这三条默认关掉并改名「上攻推算位」。清单说"必须同屏", 在这儿**正好反过来: 默认不画才对**。 |

**根子**: 那份清单是从「完整交易系统」的视角写的 —— 它默认你用这套系统下单、止损、止盈。
本仓库只取位置层。所以那张表该这么读: **形式全部可参考, 理由只信一半。**

**还没解决的**: **右边预留带里价位标签之间仍然会叠**(实测「15.90」与「浅回踩 15.72」
两条贴得近时叠在一起)。这是 R407 就记着的老问题, 与这一轮无关 —— 现价贴签已经
让开到自己那一列, 不在其中。真要治得做一遍标签避让(按价格排序后逐个下推),
那是单独一轮。
