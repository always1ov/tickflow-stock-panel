# R385 — 分界线是「每一行进没进候选池」,不是「整屏有没有」

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R385 | **用户第三次指同一张卡**: 「你看展开的时候还是很占用空间, 页面利用效率不高, 这第二个卡片的内容显示明显不合理」—— 这次给的是**实机截图**, 而它跟我一直在用的假数据完全不是一回事。**R383/R384 的判据("整屏一行都没有才收")被实机打脸**: 他那一屏 32 行里 **3 行有名次与走势、29 行两样都没有**, 于是 3 行把 29 行全拖住 —— 29 行陪着撑 56px 的行高、陪着空出 `1fr` 那一整列走势, 卡片又长回 ~1500px。**混着才是常态, "整屏"那个判据几乎永远不成立** —— 我连着两轮都在优化一个现实里不会出现的情形。**而这两类行本来就该分开, 且分界线是现成的**: `conv` 只收 `rank != null` 的条目, 走势也只在有 `c` 时渲染 —— **名次与走势永远同进同出**。所以一行要么两样都有(进了候选池), 要么两样都没有。按这条线把每一档内部拆成两组: **进了候选池** → 六列全开、单列铺满、行高撑住(它们有东西要说); **没进候选池** → 只剩 标的/动作/六态, 1180 起两列、1560 起三列。**组内仍然行行对齐**(R350 那条立论要的就是这个), 变的是"两组各自多宽"; **次序也没动** —— `byRank` 本来就把没名次的排在本档末尾, 拆开正好是原来的顺序。**行里撤掉的那句「没进候选池」改在标题上报个数**(「32 笔要动手 · 按把握分排序 · 29 只没进候选池」), 判据同样从「整屏都没有」换成「有几行没有」。**三段共用同一个 `renderRows`** —— 各写一遍的话「要动手」分了组而「只是盯着」没分, 同一张卡里两种版面; 守卫顺带钉住 `SignalRow` 全文件只在那两处渲染。**按他的比例复现后量出来**: 卡片高 **~1500 → 646px**, 3 行全宽 1688×56、29 行 539×24 排成三列十行。**手机端逐项量过**: 390px 无横向溢出, 29 行仍是单列(分列断点 1180 起), R366 那套卡片版面没动。**两条既有守卫的锚跟着走**(三段改调 `renderRows`), **三条 R383 守卫整段重写**(它们钉的"整屏"立论已经被推翻, 留着就是绿着说假话), 一条 R384 守卫改名(「整屏没名次」→「没进候选池那一组」, 模板本身一个字没动)。**8 处变异逐一确认会红**, 其中第一条钉的是**分组的前提**: `conv` 一旦收了没名次的条目, 「名次与走势同进同出」就不成立, 整个分法的地基就没了。 | 改 `pages/FlipPaper.tsx`(`renderRows` 分组 + `FULL`/`PLAIN` 两套形状 + 三列 + 标题报数, 删掉 R383/R384 那套"整屏"判据);`tests/test_flip_fusion.py` 重写 3 条 / 新增 2 条 / 改名 1 条, `tests/test_flip_today.py` 更新 2 条;**后端 `app/` diff 全空**;**判定、六态、打分、口径、指标色没动; 撤掉的那句话在标题上补回来了**;后端 pytest **4235 passed / 10 skipped 退出码 0**, 前端 tsc 0 / eslint 0 error / vitest 47 条全过 / build 通过 | 中(信号行按"进没进候选池"分成两组各排各的 —— 但每一格的内容、次序、判定没动, 窄屏那套版面量过没变) | 可 |
