# R530 — 个股分析只放今天要看的: 三段固定名单, 四个筛选撤掉, 判定缺失不拿全量冒充

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|---|---|---|---|
| R530 | 用户:「我觉得自选页面和个股分析页面有点重复了。希望个股分析页面只显示那些我需要看的, 不然一大堆。自选页面里面才是一大堆, 当做个收藏夹」→「替我选择, 我的核心需求就是个股分析页面只看那些我需要看的」, 看过方案图后「确认」。**自选是仓库, 个股分析是工作台。** 原来两页的行集合完全相同(全部自选), 个股分析靠「只看要动的」把 202 只压下去, 筛选一关就回到一大堆。现在: ① 决策台改成三段固定名单 —— 今天要动的(急迫度前四档: 已触发 / 逼近 / 刚转折 / 到轨, 持有与否都算)/ 持有·无事 / 计划中·贴轨(焦点名单里过推送门的两档, 含钉住; 用 `effective` 档), 一只票只出现一次, 按①②③归段; 观察档、无事的票在这一页根本不出现, 要看随便哪只页头搜索开弹窗。段头是一行小字, 与监控中心的日期分组条同一个形状。② 「只看要动的 / 只看转折 / 只看持有 / 只看哪个分组」四个筛选一起撤(R178 / R330 / R276 / R521), 两条本地偏好键删掉, 登记进 docs/hidden-features.md「二、撤下」。③ **判定缺失不拿全量冒充**: 原来「只看要动的」在急迫度没回来时一律放行, 接口一超时 202 只全体变成"要动的"(用户手机截图: 202 只全列、走势格全空); 现在①在急迫度没回来时是空的, 并明说「急迫度还在算… / 没算出来 —— 刷新再试」; 真没事时印「今天没有要动的」。④ `/api/stock-analysis/trends` 上限 200 → 300, 与 keltner / urgency 一致 —— 用户 202 只自选, 多的两只六态被静默截掉。⑤ 表头「N 只 · 持有 M · 自选共 T」, T 一直写着。定位到不在这一页的票时说一声「用搜索打开它」, 不再撤筛选。四列 / 表头排序 / 弹窗 / 持有切换 / 刷新 / 全量回测 / 导出 照旧; 急迫度 / 六态 / 通道 / 焦点名单的判定一个没动; 自选页一个字没动。守卫: 新增 test_board_sections_r530; test_board_group_filter(R276/R330)与 test_board_default_actionable_r521 整个退役(git show c0bda83b 可取回); test_board_columns 的比较器锚改成 sortRows; test_skeletons 空表锚改成 totalRows; test_terminology 前端侧的哨兵词从「只看要动的」换成「今天没有要动的」 | frontend/src/components/stock-analysis/WatchlistDecisionBoard.tsx; frontend/src/lib/storage.ts; backend/app/api/stock_analysis.py; docs/hidden-features.md; backend/tests/test_board_sections_r530.py(新); backend/tests/test_board_group_filter.py(删); backend/tests/test_board_default_actionable_r521.py(删); backend/tests/test_board_columns.py; backend/tests/test_skeletons.py; backend/tests/test_terminology.py | 低: 决策台是 fork 自己的; stock_analysis.py 只改了一个数字 | 可以: git revert 本提交 |
