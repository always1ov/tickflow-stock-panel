# R431 — 个股弹窗重做第三块:「复盘」(逐日复盘 + 两套买卖对比 + 六个状态的历史表现)

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R431 | 用户发来第三块排版图(复盘)。新区块排在「图表与价位」之后, 天数跟头部 60 / 120 / 250 日走, 与旧复盘页同一个查询(`useStockReview`); 旧顶栏里的旧复盘页原样留着。三样: (1) 逐日复盘 —— 原来「趋势状态」「通道档位」两个页签各一张逐日表, 并成一张, 六态状态与通道档位并排; 动作 / 成交→了结 / 结果按用户选的**两套都显示**, 同一天两套都动了手就上下两行、前面标「六态」「通道」, 每行固定高度保证三格里第 n 行是同一笔; 「只看有事的日子」「只看换档的日子」两个筛选取并集; (2) 两套买卖对比 —— 跟着做 / 一直拿着 / 多赚 / 买卖次数 / 规则, 一套一行, 「别当真」的提醒与数字同进同出, 算不出来时说原因; 下面挂着原来的「依据 · N 项读数」; (3) 六个状态在这只票上的历史表现 —— 按用户选的**只摆数、不下结论**: 图里那列「一进这档就该走, 不抢反弹」是建议, 而自然回撤在上升趋势里并不算坏, 写「该走」就是多给一次卖出理由(硬约束第 10 条), 所以「这只票的表现」只写客观情况(没出现过 / 还没走完 / 样本太少), 「怎么看」只指出当前那一行和它的数。数据: 后端新增 `review_service._state_history`, **复用** R177 的 `_episodes` 分段(同一个口径, 只给每段多带一个起点下标), 在其上算共多少天 / 段里涨跌(进段前一天收盘到段末) / 走完后 5 天(段末起算, 与 R177 段首起算的 avg_fwd 不是一个数) / 收涨段数; 还在走的那段只计次数与天数; 六个状态都占一行; 不给百分比胜率。载荷多一个 `state_history` 字段(只增不改, 原字段与 side_edge / now 判定不动)。共用件收拢: 两套口径那句话收成 `FLIP_BASIS` / `VERDICT_BASIS`, 四个数的说明收成 `TRADE_STAT_TIPS`, 每一笔的画法拆成 `LegAct` / `LegFill` / `LegResult`(旧表与新表都用它们), 区块标题行抽成 `SectionTitle`。**顺带删掉** `FlipTradeLine` —— 它唯一的调用方在 R295 就撤了, 一直是孤儿, 守卫却还在钉「两处说同一套话」; R293 守卫改成「每一笔只有一份写法」, R288 / R302 两条改钉到常量上(路标仍在)。棘轮随之下调: 裸圆角 944 → 942、任意字号 2280 → 2275、硬编码色 1046 → 1044。 | 改 `backend/app/services/review_service.py`(`_state_history` + 载荷字段 + `_episodes` 带下标); 新增 `frontend/src/components/stock-preview/ReviewSection.tsx`、`SectionTitle.tsx`; 改 `frontend/src/components/StockPreviewDialog.tsx`、`stock-preview/ChartLevelsSection.tsx`(用 SectionTitle)、`stock-analysis/StockReviewDialog.tsx`(导出共用件与查询 hook)、`stock-analysis/FlipTradesPanel.tsx`(拆件、删孤儿)、`frontend/src/lib/api.ts`(类型); 新增 `backend/tests/test_review_state_history.py`(7 条)、`test_review_section.py`(8 条); 改 `backend/tests/test_flip_trades.py`(3 条改指)、`test_design_spec.py`(棘轮下调); **打分系统、`strategy/`、六态状态机 diff 全空**; 变异 15 + 3 个全杀; 后端 4547 passed、vitest 155、tsc / eslint / build 0; 亮暗两套出图核对版面、同一天两笔对齐、两个筛选、当前状态行高亮 | 中(复盘载荷加字段; `FlipTradesPanel` 拆件) | 可(删掉弹窗里 `<ReviewSection>` 那一段即回到旧复盘页; 后端字段不读就无影响) |
