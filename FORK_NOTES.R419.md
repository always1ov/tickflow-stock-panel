# R419 — 主图恢复换成交量之前的高度, 量化MACD 副图另加高, 弹窗往下滚着看

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R419 | 用户看到副图里金叉 / 死叉箭头「嵌进去柱子」, 确认那是通达信本来的画法(图标从 DEA、DEA×1.1 往下挂, DEA 的 10% 换成像素要看副图多高 —— 通达信截图里死叉箭头也嵌进柱子 10~15px)后, 要求「不能让主图变矮, 副图调高」「可以搞整个弹窗内容滚动往下看」。新增 `lib/levelsChartLayout.ts` 作纵向版面的唯一产地: **主图高度 = 调用方给的 `height` − 156**, 与 R415 之前(成交量 90px 那一版)**逐像素相同**(R415~R418 为了塞副图把主图挤矮了 20~90px, 这次还回去); **副图 = 主图的 55%, 限 180~300**(个股预览 364→200, 最大化 564→300, 窄屏 164→180; R418 是 100~180); **画布高 = 主图 + 副图 + 边距**, 比 `height` 高, 多出来的由两个弹窗本来就有的 `overflow-auto` 内容区滚动承接(个股预览 520 → 画布 650)。`height` 的意思随之变为"主图按它算"。顺带在重设 option 前调一次 `resize()`: 画布高度跟着最大化 / 还原变, ECharts 不会自己量新尺寸。R418 的 `subPaneHeight` 从 `quantMacdSeries.ts` 挪进新模块(一个量一个产地)。 | 新增 `frontend/src/lib/levelsChartLayout.ts` + `.test.ts`(3 条, 含「主图与 R415 之前的算式逐项相等」);改 `components/stock-analysis/AnalysisKChart.tsx`、`lib/quantMacdSeries.ts`(删 `subPaneHeight`)、`.test.ts`;**后端、打分系统、`strategy/`、六态 diff 全空**;变异 4 个全杀;tsc 0、eslint 0 error、vitest 146 passed、vite build 0;在 600px 高的滚动框里出图: 画布 650 = 16 + 364 + 14 + 200 + 26 + 22 + 8, 可滚到副图底部 | 低 | 可(回到 R418: 副图从主图里抠) |
