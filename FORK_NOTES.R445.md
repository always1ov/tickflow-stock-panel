# R445 — 现状块删掉打码的部分与分隔竖线, 四块重新排版

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R445 | 用户在现状块截图上打码: 通道那一格的档位徽标(或「三档都在中部」)、三字组合码、「该盯什么」那一句, 连同格与格之间的分隔竖线 ——「删除掉我打码的部分和间隔符合, 然后排版好显示」。全部删掉; 那一格只剩阶段(上升中 / 下跌中 …), 标题跟着从「通道档位」改叫「通道阶段」(代码里一直叫这个名字; 档位是「短线回调」「候选池」那一类, 标题不改就是一个名字两个意思)。排版: 四块(趋势状态 · 六态 / 跌破转弱或站上转强 / 通道阶段 / 三档位置)同一个结构 —— 顶上一行小标题、下面读数、顶端对齐, 等分一行, 窄屏两块一行; 转强 / 转弱价去掉外框改成与其余几块同一种读数写法, 三档位置补上小标题。数据源不变(六态接口 / 复盘接口, 距离仍用后端给的带符号的数)。守卫 `test_R445_现状块删掉打码的部分_没有分隔竖线_四块等分`; R433 里钉「档位徽标」「该盯什么」的两条断言撤掉, 改钉通道阶段取自复盘接口。变异 5/5 杀死 | `frontend/src/components/stock-preview/StatusSection.tsx`、`backend/tests/test_status_section.py` | 低(只在 fork 自己的新块里) | 回退本提交即恢复打码的那几样与竖线 |
