# R392 — 更正 R391:病因诊断错了,真凶是体检接口挂在 30 秒闸上

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R392 | **R391 修对了一半, 另一半是我诊断错了, 这一条把它更正过来**(`/code-review` 两轴复审查出)。**错在哪**: R391 认定"开着实时行情看盘 → 台账天天不记"是病根, 于是给记账那段 if 链不成立的几支都接上了 `_h.skip`。**漏看了 [R136]** —— 日线管道落盘之后会自己跑一次 `_build_overview`, **那才是"每个数据日都被走到"的保底路径**, 页面上这一次跳过根本不会少一天样本。所以那几条上报是**把一件本来就对的常态塞进故障通道**: 自检条会在每个盘中日常驻一行「另有 1 处只影响细节」, 而 R274 给它定的性质恰恰是"一切正常时一个像素都不占", 常驻两天就成了背景板, 真出事那天照样被忽略。现已撤回, **只留"打算记却没记成"那一支**(`record_day` 自己吞异常, 失败只体现在返回值上, 不看的话写盘挂了和写成功在界面上长得一模一样)。**真凶另有其人**: `todayScoreLedger` 挂在 `request()` 的**默认 30 秒闸**上, 而这是个**同步计算型接口** —— `evaluate()` 每次都要把还缺收益的记录补算一遍(逐只读日 K, 攒几周就是几百只), 弹窗自己的转圈文案写的就是「正在补算历史收益…」。**台账越厚越容易直接超时**, 用户看到的就是「读取台账失败」。这是 **[R231] 那次漏掉的同一处**: 当时按"会调大模型的接口"逐个开豁免, 隔壁的 `todayScoreLedgerDigest` 拿到了 AI 档, **这个纯计算的反而没人管** —— 判据选成了"调不调模型", 而闸要防的是"等多久", 两者不是一回事。现改走 `COMPUTE_REQUEST_TIMEOUT_MS`。**空态那段话改过两轮, 两轮都在说假话**: 原文指向 R340/R351 已拆掉的「今日总览」; R391 我改成的「这一页每打开一次就记一天」同样不对(记账发生在 `GET /api/today` 而不是这个弹窗, 且真正保底的是日线管道), 还顺带许诺「顶上那条自检会写明这次记没记」—— 而 `TodayHealthBar` 一切正常时**整条不渲染**, 它只说得出"没记"。现在照实写: 快照由日线管道在数据落盘后自己记, 不需要谁去点开哪一页; 空着通常是管道还没跑完一次收盘更新。**顺带扫掉同一个假路标的其余三处**: `FocusBar.tsx` 仍在让人"打开一次今日总览, 名单就会生成"(踩硬约束 12)。**守卫全部重打**: R391 那两条扫措辞的(查有没有「盘中」「收盘」二字)退役 —— 它们恰恰抓不到我这次新写的同类假话; 换成按**数据流**钉(记账返回值绑给了谁、哪条 if 拿它当判据)、按**性质**钉(`_h.skip` 只许长在"没记成"的路径上, 不许长在"这次不记"的路径上; 已拆页面的名字不许出现在渲染文本里; 空态不许许诺自检会报好消息)。**9 处变异逐一确认会红。** | 改 `backend/app/api/today.py`(撤回 R391 的两条常态上报, 留返回值检查)、`frontend/src/lib/api.ts`(体检接口改走计算档超时)、`frontend/src/components/ScoreLedgerDialog.tsx`(空态照实写)、`frontend/src/components/monitor/FocusBar.tsx`(三处旧页面名);重写 `backend/tests/test_score_ledger_silent_skip.py`(7 条);**`backend/app/strategy/` diff 全空**;冻结的 `opportunity_score.py` / `score_ledger.py` 未动;**判定、六态、口径、指标色一个没动**;后端 pytest 4292 passed / 10 skipped 退出码 0, 前端 tsc 0 / eslint 0 error / build 退出码 0 | 低(超时档位调整 + 撤回两条上报 + 纯文案; 接口出入参形状未变) | 可 |

**仍未做、要用户定夺的两件**:

1. **「今日总览」这个已拆页面的名字还剩三处渲染文本没清**: `api/today.py` 的 AI 提示词(硬约束 7 划的红线, 属 AI 分析链)、`api/today.py` 与 `services/data_doctor.py` 里的「今日总览偏好」(存储注册表标签, 改名要同步改注册表)。按硬约束 12 本该同时加一行 `TERMS` 把旧名封掉, **但那会当场把这三处判成违规**, 而其中一处在不许动的文件里。所以这轮**没加 `TERMS` 行**, 把选择权交回去。

2. **`FORK_NOTES.md` 的 R 行从 R371 起就不在那份文件里了**, 一路是独立的 `FORK_NOTES.R<N>.md`(本条是第 22 个)。复审按硬约束 4 的字面把这记成违规 —— 它字面上确实破了。这是既有惯例不是本轮引入的, 要不要收回主文件由用户定。
