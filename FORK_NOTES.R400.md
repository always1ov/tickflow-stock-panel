# R400 — 阶段三 3.2:三件基础件落地;顺带修掉 R399 埋下的、和一条从没生效过的聚焦边框

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R400 | 承 R399(那一轮只立了规范变量、把 Tailwind 接上线)。这一轮做 3.2:**把按钮/卡片/输入这三样收进一处取值表**, 并拿模拟盘那条链当第一个真调用方验证它。**动手前先数**: 995 个 `<button>`, 能静态解析出 class 串的 232 个里**有 193 种互不相同的写法** —— 平均 1.2 个按钮一种样式; 「风格不统一」不是感觉, 是这个数。**取值不是我发明的, 是从现状里数出来的多数派**再折到 R399 的档位上(`bg-accent…text-base` 39 处 → `primary`; `border-border bg-base text-muted` 最常见 → `outline`; 另有 secondary/ghost/danger)。**`selected` 做成与 variant 正交的开关而不是第六档**: 现状里"选中"有 `sky-400/15`、`accent/10`、`violet-500/20` 三四种颜色, 同一件事换页面换颜色。**先修一个 R399 埋下的、还没咬到人的坑**: R399 把五档字号/两套间距/三档容器宽接进了 `tailwind.config.ts`, **但没人告诉 `cn()` 它们是什么**。实测未修时 `cn('text-micro','text-muted')` → `'text-muted'`(**字号整个被丢掉**, 因为 `text-micro` 不在 twMerge 已知的字号表里, 落进了文字颜色那一组); `cn('px-s1','px-s2')` → 两个都留(**调用方写的覆盖根本不叫覆盖**, 最终哪个生效由 CSS 先后决定)。两条都不报错。它现在还没咬到人**只因为那批类名一处都还没用上** —— 而 3.2 的基础件正要开始用。顺带把早于 R399 就在用的 `rounded-btn`(782 处)/`rounded-card`(246 处)/`duration-*`/`ease-*` 一并登记, 它们一直覆盖不掉。**拆掉一个歧义**: `text-base` **同时是颜色和字号**(Tailwind 出厂 16px 一档), 裸 class 串里靠 CSS 先后侥幸各管各的(查过产物: `.text-base{font-size}` 排在 `.text-xs` 前面, 所以现状没坏), 一旦进 `cn()` 就与字号撞组、**颜色被静默丢掉**。新增语义色 `on-accent`(值与 `base` 完全相同, 只是给「强调实底上的文字」这个角色一个不含歧义的名字)。**量出来一条从没生效过的代码**: 那几个框写的 `focus:border-sky-400/50` / `focus:border-violet-400/50` **一天都没生效过** —— `index.css` 给输入框定边框的那条 `input:not([type='checkbox']):not([type='radio'])` 是 (0,2,1), 而 Tailwind 的 `.focus\:border-*:focus` 只有 (0,2,0), **低一档永远抢不过**(单起一页复现过, 两条并存时聚焦读到的始终是前者)。所以基础件**故意不抄这句话** —— 抄进来就是又一行"看起来在做事、其实什么也没做"的代码, 而且会被后面几十个调用点跟着抄; 聚焦指示本来就有且是好的那个(R124 全局 `:focus-visible` 2px 强调色轮廓, 带 `!important` 抢得过)。**这一条只写进基础件, 没去动 `index.css`** —— 改那条规则的适用范围是全站每个输入框都受影响的改动, 见下面「留给用户定的」。**棘轮本身也修了**: R399 那把连注释一起数, 于是**写清楚"这儿原来是 `sky-400/50`、为什么不再这么写"会让数字涨**(这次当场撞上), 反过来删掉一段解释能让数字降 —— 与 AGENTS.md 第 12 条「注释里复述旧说法是允许的」相反。改成剥注释再数。**四条既有守卫是钉在字面类名上的, 这次逐条改钉性质**(R379 栽过同一族): 走势格占位原来锚 `text-[11px]`(改档位就红, 而"占位"没变)→ 改钉 `col-span-3 min-w-0`; 「定时个股信号」原来锚一整段 `<span>` 标记 → 改钉"这四个字必须挂在一个 checkbox 上"(且要逐个看每一处, 第一处恰好是 toast 文案); 插槽原来锚 `{extra && <div` → 改钉渲染本身; 卡壳原来锚一整条 class 串 → 改钉 `<Card>` + 顺序, 分隔线到 `CardSection` 里查。**守卫 15 条, 变异逐条确认会红**: `cn` 那组 10 条 vitest(完全退回原版杀 9 条, 另 7 个定点变异各杀 1~2 条); 基础件那组 6 条 pytest(9 个变异全红); 四条改钉的(8 个变异全红); 棘轮**双向**验过(新写一处硬编码色 → 红; 注释里复述旧写法 → 不红)。**六种组合真浏览器量过**(亮/暗 × 375/768/1280): 横向溢出全为 0; 迁移后的三个控件尺寸完全一致(25px 高 / 11px 字 / 4px 圆角 / 4·8 内边距), 而同屏未迁移的两个仍是 30px·fs14·r6 与 35px·fs15·r4 —— 那正是 3.3 要收的。**功能与文案一个字没改**: `Field` 不接管 `value`/`onChange`/`onBlur`(现状里受控与非受控混用、提交时机各不相同, 收进来就必然要统一时机, 那是功能改动)。 | 新增 `frontend/src/components/ui/{Button,Card,Field,index}.tsx`;改 `frontend/src/lib/cn.ts`(登记本项目刻度)、`frontend/tailwind.config.ts`(+`on-accent`)、`frontend/src/components/today/TodayControls.tsx`(4 个按钮 + 9 个输入/下拉 + 卡壳 + 插槽)、`frontend/src/pages/FlipPaper.tsx`(1 个按钮 + 29 处任意字号 + 2 处 hover 色统一);新增 `frontend/src/lib/cn.test.ts`(10 条);改 `backend/tests/test_design_spec.py`(+6 条 + 棘轮剥注释并重新定基线)、`backend/tests/test_flip_fusion.py`(4 条改钉性质);**`backend/app/` diff 全空**;**`backend/app/strategy/` diff 全空**;冻结的 `opportunity_score.py` 未动;**判定、六态、口径、涨跌色一个没动**;后端 pytest **4369 passed / 10 skipped 退出码 0**, 前端 tsc 0 / eslint 0 error / build 退出码 0 / vitest **99 passed / 18 个文件**(R399 时是 89) | 中(`cn.ts` 是全站每一处 `className` 合并都要过的那一层 —— 它只**多**认得一批类名, 不改变对 Tailwind 原生类的处理, 但作用面是全站; 另两处改动面很窄) | 可(三个基础件是新增文件; `cn.ts` 与 `tailwind.config.ts` 两处各自独立; 两个迁移文件可单独 revert) |

**棘轮这一轮的数字由两件事一起造成, 分开记清楚**(`test_design_spec.py` 里也写了):

| | 裸圆角 | 任意字号 | 硬编码色 | 任意容器宽 |
|---|---:|---:|---:|---:|
| R399 基线 | 2305 | 2336 | 1071 | 44 |
| ① 3.2 真迁移掉的 | 2295 | 2291 | 1053 | 44 |
| ② 改成剥注释再数 | **2285** | **2282** | **1047** | **42** |

② 那一截是**一直躺在注释里的旧写法, 不是迁移成果**, 只此一次, 以后不会再降。

**知情保留, 逐条说清**:

- **模拟盘只有 1 个按钮进了 `Button`**, 另外 6 个没进 —— 它们不是工具条按钮: 三个是整行宽的折叠条(手上这些 / 只是盯着 / 净值走势), 三个是包着标的名的行内文字链。**硬塞进按钮取值表会比现在更糟**, 它们是另一个物种, 该在 3.3 里单独立件(`DisclosureRow` / `LinkButton`)。
- **FlipPaper 还剩 2 处 `text-[9px]`**(「没进/候选池」那个两行标签、月度收益那一排 12 个月份)。规范里没有 9px 这一档, 但升到 11px 是 +22%, 两处都在很紧的格子里 —— **要量过才敢动**, 留给 3.3 连同那一页的网格一起看。
- **`Button` 的 md 档目前没有调用方**。它是照"主按钮该更显眼"留的档位, 3.4 排 IA 时那个"全页唯一的主色实心主按钮"会用到; 现在留着是空的, 记在这儿免得下一个人以为它在用。

**留给用户定的一件事(属于 AGENTS.md 第 14 条的第五类, 不自己动)**:

`index.css` 那条给输入框加深边框的规则(R368)带着一句注释:「只在没被更具体的类覆盖时生效: 组件若显式写了 `border-accent` 之类, 那是它自己的意图(聚焦态、错误态), 这条不去抢。」——**这句话是假的**, 它恰恰抢得过每一个 `focus:border-*`(权重 (0,2,1) vs (0,2,0), 上面复现过)。修法只有一行, 把非聚焦态排除出去:

```css
input:not([type='checkbox']):not([type='radio']):not(:focus), select:not(:focus), textarea:not(:focus)
```

**没有替你做, 因为影响面是全站每一个输入框**: 改完之后, 那些**没有**写 `focus:border-*` 的框在聚焦时会掉回浅一档的 `--border`(看起来像"聚焦时边框变淡了"), 得逐页看过才知道能不能接受。要么改, 要么把那句注释改成实话 —— 但不该维持现状。
