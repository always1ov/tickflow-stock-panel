# R397 — AI 助手抽屉在手机上比屏幕还宽,左边整块被切出视口

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R397 | 用户截图: 手机上点开 AI 助手, 标题只剩半个「AI 助手」、每条快捷建议的首字都被切掉(「今天市场怎么样?」显示成「天市场怎么样?」)。**这不是 R394/R395 漏了它** —— 那两轮修的是页头与表格, 这个抽屉是另一条线上的另一个缺陷。**根因是算术, 不是类名**: `clampWidth()` 里外两个 `Math.max` 都在保证"无论如何至少 `MIN_WIDTH`(480px)", 于是 390px 的手机上 `Math.max(480, 390-64)` 算出 480 —— **抽屉比屏幕还宽 90px**, 而它是 `right: 0` 贴右缘的, 多出来的 90px 全挂在屏幕左边外面。**实测坐实**: 退回原版在 390px 视口下量到 `x=-90, width=480, 越界=true`; 修后 `x=0, width=390`。**更该记一笔的是那条假注释**: 调 `clampWidth` 的那个 resize 监听写着「保证面板不越出视口」, 而它调的这个函数**做不到它承诺的事** —— 这是本仓库反复出现的那一类(注释说一套、代码做一套, 且不报错)。**修法只加最后一个 `vw`**: 不变式就一条"永不超过视口", 别的都在它之内; 窄屏铺满整屏是这条不变式的**结果**, 不需要单写一支。**第一版我确实单写了一支** `if (vw <= MIN_WIDTH) return vw` 的早返回, **变异测试当场证明它与末尾那个 `vw` 完全重复**(拿掉任意一个, 四条断言照样全绿)—— 两道看起来像两层防线、其实只有一层, 那比一层更坏, 已收敛成一条。**顺带**: 整屏铺满时左缘那个 `touch-none` 的调宽把手不再渲染 —— 它在整页抽屉的左边缘会把那一条竖带上的触摸滚动也吃掉, 而"调宽"在铺满时本来就没有意义。**守卫钉算术不钉类名**: 这类 bug 扫源码抓不到(类名一个不少, 错的是算术; R386 栽过同一族, 那次是靠真浏览器逐行量才抓到)。所以 `clampWidth` 导出成可测, 新增 4 条 vitest 单测, 喂 16 个真机常见视口宽度 × 5 个请求宽度, 断言一条性质: **返回值永不超过视口**。**3 处变异逐一确认会红**: 拿掉兜底的 `vw`(= 原版行为)→ 2 红; 把下限抬到视口之上 → 1 红; 收口收成 `maxWidth` 而不是 `vw` → 2 红。 | 改 `frontend/src/custom/assistant/ui/AssistantDrawer.tsx`(`clampWidth` 收成一条不变式 + 导出供测 + 窄屏不渲染调宽把手 + 视口宽度进 state);新增 `frontend/src/custom/assistant/ui/AssistantDrawer.clampWidth.test.ts`(4 条);**后端一个字没动**;**`backend/app/strategy/` 未动**;前端 tsc 0 / eslint 0 error / build 退出码 0 / vitest **89 passed / 17 个文件**;三档视口真浏览器复验: 手机 390 铺满不越界、平板 834 与桌面 1440 与改动前逐像素相同 | 低(只收窄了一个宽度的取值范围; 宽屏上 `MIN_WIDTH`、留 64px 上下文、拖拽调宽全部照旧) | 可 |

**这一条与上游合并的关系**: R396 合进来的上游改动**没有碰这个文件**(`AssistantDrawer.tsx` 停在 9-20, 作者这一轮动的是同目录下的 `DailyChartCard.tsx` 与 `messages.tsx`)。所以这个缺陷在 fork 与上游两侧都存在 —— 修在 fork 这边, 同步上游时它会成为一处 fork 增强。
