# R410 — 斐波那契二型:说人话 + 只画该看的那几条(仍然不出任何结论)

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R410 | 用户: 「目标1目标2失效位这些表达没能让用户抓得住重点看得懂, 而且好多根线, 好难抓住之前说的做不做在哪里做, 走不走这些」。**先问清楚再动手**: 「做不做 / 走不走」如果由图来答, 那就是新增一档判定 —— 与 R405 定的「二型只有位置、没有动作」和用户自己那句「是否共振我自己人工判断, 不打算代码判断」相反, 也踩中硬约束第 14 条的「核心逻辑重构」。**用户选了「只摆位置, 不出结论」**, 所以这一轮**一条判定都没加**, 全部是名字与主次。**改名四处, 每处都是撞名或误导, 不是换个好听的说法**: ① **浅/深回撤 → 浅/深回踩** —— 「回撤」在本仓库**已经有两个别的意思, 而且都动不了**: 账户口径的回撤(最大回撤、蒙卡回撤、组合回撤纪律线、回撤止盈)和六态里**作者命名**的「自然回撤 / 次级回撤」; 同一个词第三个意思, 让最新来的这组让路。顺带**去掉 `R{k}` 摆点编号** —— 那个数对看盘的人没有任何意义, 只是让右边一排标签看着像乱码。② **强支撑区 → 回踩密集带** —— 「支撑」已经被「压力支撑」那一组占着。③ **失效位 → 这组作废** —— 「失效」太抽象, 而且**容易被读成一条卖出线, 它不是**: 它只说明跌破之后这一整组位置不再成立。④ **目标一二三 → 第一站/第二站/第三站** —— 「目标」在本仓库还指目标仓位与目标日波动; 而且「目标」听着像"要去达成的", 这三条只是推算出来的、涨上去会路过的位置。**四处全部进名词表(7 行)**, 「目标一」那条写成正则, 因为 `目标一致` 这个正常词组在后端注释里有三处, 纯子串会把它判成违规。**减线四条, 由我定(用户答「不懂怎么处理」)**: ① 默认档 中 → **粗**(实测 12 条 → 7 条); ② **上方推算位收进一个开关, 默认不画** —— 它们回答"涨上去会路过哪", 与当下的「在哪里做 / 走不走」无关; ③ **只画离现价最近的上下各 3 条**, 但**密集带里的和作废线不论多远一律保留** —— 密集带离现价远不代表它不重要, 恰恰是提前知道它在哪才有用, 而作废线是「走不走」的下界; ④ **强弱改成按"在不在密集带里"给**(原来全是 `medium`): 帝纳波利这套东西的全部意思就在"几条挤在一起", 单独一条本来就弱, 让粗细浓淡直接说出来, 不要用户自己去数。**减线最坏的结果不是少看几条, 是用户以为这一档就这么多线** —— 所以开关上如实写「画了 6 条 / 这一档共 12 条」, 并说明藏的是哪一类。**三处旧断言当场翻修, 其中一处是绿着漏的**: 原来几个守卫拿 `label.startswith("目标")` 之类分类, 一改名断言就落空, 而**落空的方式是"所有线被归进同一类"** —— 于是"每类只有一种颜色"永远成立, **测试变绿而洞还在**; 改成按**角色键**(R409 那三个常量)或直接问 `Fib2` 结果对象分类。同一条断言还暴露出它用的那份数据**根本出不了作废线**(没有密集带就没有作废线), 一直只覆盖了三类里的两类 —— 换了一份搜出来的、三类齐全的数据。**顺带修掉一个会差一条线的边界**: 出去的价位 `round(v, 2)`(A 股报价到分)而密集带上下沿没有 round, 12.9844 那条会画在 12.98、也就是画在带的下沿**之下一丝** —— "带里那几条"和"标成 strong 那几条"会差一条, 屏幕上看不出来, 但那正是这个读数唯一要表达的东西; 加了半分容差。**守卫 +15 条(后端 7 + 前端 vitest 8), 17 个变异逐条确认会红。** | 改 `backend/app/indicators/dinapoli.py`(四处改名 + 强弱按密集带给 + 分容差)、`frontend/src/components/stock-analysis/AnalysisKChart.tsx`(`thinFib2` 减线 + 默认档 + 推算位开关 + 开关如实报数 + 色带改名)、`backend/tests/test_terminology.py`(+7 行)、`backend/tests/test_dinapoli_pivots.py` 与 `test_fib2_endpoint.py`(断言改按角色键 + 换数据)、`test_level_palette.py`(角色键提成具名常量后跟着改解析)、`frontend/src/lib/theme.ts`(三个角色键具名导出)、两处注释(`api/stock_analysis.py`、`lib/api.ts`);新增 `backend/tests/test_fib2_readability.py`、`frontend/src/components/stock-analysis/thinFib2.test.ts`;**打分系统 / `strategy/` / `keltner*` / 六态 / `position_exit` / `today*` diff 全空**;后端 pytest **4466 passed / 10 skipped 退出码 0**, 前端 tsc 0 / eslint 0 error / build 退出码 0 / vitest **115 passed** | 中(改名动到前后端两侧的渲染文本与三处旧断言; 但**一条判定、一个数值都没动** —— 线的位置与算法逐字节未变) | 可(名字改回去要同时撤 `TERMS` 那 7 行; 减线撤 `thinFib2` 与两个默认值即可, 与改名互不牵连) |

**为什么先问了一句再动手**:

用户说的是「好难抓住…做不做在哪里做, 走不走这些」。字面上这可以读成两件完全
不同的事: (a) **把位置摆清楚**, 答案仍然自己下; (b) **图直接给结论**。
(b) 会让二型从"位置"变成"判定层" —— 而 R405 立这一组的时候, 用户的原话是
「是否共振我自己人工判断, 不打算代码判断」; 硬约束第 10 条也写着「新增任何一档
判定之前先问: 它会让人多动一次还是少动一次」。两个读法做出来的东西差得很远,
**做错了不是返工的问题, 是把已经定过的口径推翻了**。所以这一处停下来问了。

用户选 (a)。减线那一问用户答「不懂怎么处理」, 那部分按第 14 条自行定夺, 四条
全做了, 理由逐条写在 `thinFib2` 的注释里。

**留给用户的那一步**: 这个开发环境里没有真实行情。请打开一只熟悉的票, 点开
「斐波那契二型」看一眼 —— 现在默认应该是**几条线, 一个带, 一条作废线**。
看着还是多就告诉我, `NEAR_EACH_SIDE`(现在是上下各 3 条)是一个数的事。

**这一轮没动的**: 「未来」区(要扩 x 轴)、右侧标签在线多时会叠(减线之后应该
好很多, 但没根治)、`SegmentedControl` 收口(R402 留的)、阶段三 3.3 逐页迁移。
