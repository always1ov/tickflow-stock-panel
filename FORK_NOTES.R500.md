# R500 — 转折页持仓改小卡片、成交流水改小长方条

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|---|---|---|---|
| R500 | 用户:「用小卡片显示」(追问后指现在拿着与成交流水)「或者小长方条, 你决定哪个好」。定为持仓用小卡片、流水用小长方条: 持仓只有几只到十来只, 每只最要紧的是浮盈那一个数, 卡片把它放大到右上角, 持有天数/股数/成本→现价/市值收在下面两行, 电脑与手机同一套(sm 两列、2xl 三列), 点卡片仍打开这只票; 流水是按时间读的几十笔, 卡片网格会打乱先后, 所以一笔一条, 宽屏用 columns 排两栏(列优先, 左栏读完接右栏), 标的定宽 12.5rem 让「因为」那列对齐(11rem 时代码被截), 手机上同一份 DOM 靠 order 折成两行(动作·标的…成交价 / 日期·因为…金额)。默认条数不变(手机 10、宽屏 30), 只渲染一份, 第 11 笔起在窄屏收起, 同一元素只给一个 display 类。R498 的手机/宽屏两套 DOM(卡片 + 表格)随之撤掉。字段一个没少: 延迟成交的「·延」与其顺延说明、原因里的六态名都在。卡片圆角走 rounded-btn 规范 token。守卫: test_flip_layout_r498 里两条手机卡片的换成 R500 两条; 全量验证通过 | frontend/src/pages/FlipPaper.tsx; backend/tests/test_flip_layout_r498.py | 低(fork 自有页面) | 可以: git revert 本提交(回到 R499 的表格 + 手机卡片) |
