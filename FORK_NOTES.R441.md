# R441 — 复盘对比表删掉「按档位买卖 · 通道」那一行

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R441 | 用户指着复盘块对比表里「按档位买卖 · 通道 +1.2% +11.8% −10.6% 1 次 换档次日开盘进出 ...」那一行(连同下面两条黄字提醒): 「删除」。从 `SummaryCard` 的 `systems` 里去掉这一项, `VERDICT_BASIS` 的引用随之撤掉; 「按转折买卖 · 六态」那一行照旧。只删这一行: 逐日表里通道那一笔笔动作是旧「通道档位」表的原有内容(R440 定了与旧表一模一样), 不动; 旧复盘页「通道档位」页签的按档位买卖也不动(旧部分等统一删除)。守卫 `test_R441_对比表没有按档位买卖那一行`, 原来钉这一行存在的那条断言撤掉; 变异 3/3 杀死(通道那一行回来 / 六态那一行也删了 / 逐日表通道动作跟着删) | `frontend/src/components/stock-preview/ReviewSection.tsx`、`backend/tests/test_review_section.py` | 低(只在 fork 自己的新块里) | 回退本提交即恢复这一行 |
