# R437 — 新弹窗对照旧代码逐块审计: 补回分时信息栏, 刷新认新旧两个视图

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R437 | 用户: 「检查新的弹窗是否都是利用系统的东西填充, 每个模块是否代码都几乎和以前一模一样, 尤其是图形部分, 改样式不算」。逐块对照 R429 之前(c2bcedea)的代码, 查出两处实质偏差并修掉: ① 新「图表与价位」的分时视图少了旧分时页图上方那条信息栏(`StockPanel infoBarOnly`, 代码 / 现价 / 涨跌 / 可配字段), R430 搬过来时漏了 —— 照旧分时页原样补回, 只有区间用 heroRange(与新日 K 同一份缓存, 不多发请求); ② 旧顶栏「刷新」只按旧 `view` 刷, 新块自己的 `chartView` 在分时、旧的停在关键价位时, 点刷新刷的是关键价位那份 —— 改成新旧两个视图都刷(同一个只刷一次)。其余各块核对结论(未改): 日 K / 关键价位 / 多日分时用的是同一个组件, 区间与可见根数按用户选的「图跟着头部走」; `AnalysisKChart` 除 R430 按下标缩放、R434~R436 副图横格外都是把状态挪进 `useLevelControls`、文案挪进 `levelControls.ts` 的原样搬移; `LevelToolbar` 与旧那排开关同一套开关、同一个 `levelGroupStat`, 多一个「全部清除」; 复盘块的逐日表 / 两套对比全用旧页导出的同一批组件与函数; 「六个状态在这只票上的历史表现」是 R431 为排版图新加的后端汇总(`_state_history`, 分段复用 `_episodes`)。守卫 `test_R437_新分时与旧分时同一套_信息栏不能漏`(新旧两处分时图与信息栏逐属性比对, 只许 heroRange 这一处不同) + `test_R437_刷新同时认新旧两个视图`; 变异 4/4 杀死 | `frontend/src/components/StockPreviewDialog.tsx`、`backend/tests/test_chart_levels_section.py` | 低(只在 fork 自己的新块里) | 回退本提交即回到新分时无信息栏、刷新只认旧视图 |
