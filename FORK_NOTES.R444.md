# R444 — 逐日复盘只留旧「趋势状态」那张表

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R444 | 用户: 「逐日复盘还是没完全一样啊, 我只看转折所以我有趋势状态就行了, 通道已经在结果列有显示, 所以才叫你和以前一模一样的显示这部分」。R442 拆回的两张表里撤掉「通道档位」那一页(表、「只看换档的日子」开关、脚注)和「趋势状态 / 通道档位」页签; 逐日复盘就是旧复盘页「趋势状态」那一张表: 页头「起 ~ 止 · N 个交易日 · 六态阈值 N%」、「只看有事的日子」、八列(最后一列是通道档位, 与旧表一样只有档位徽标或一道横线)、空表那句、脚注, 全部照抄旧页, 只换样式。ReviewSection 不再收 `tab`, 换票仍靠 `key={symbol}` 复位。守卫 `test_R444_逐日复盘就是旧趋势状态那张表_去掉样式后逐字一样` 替掉 R442 的两页比对, 并钉通道档位那一页与页签不许回来; R441 里「逐日表通道动作留着」那条断言撤掉, `test_R293_每一笔只有一份写法` 改为新表只有一处 `FlipTradeCells`。变异 6/6 杀死 | `frontend/src/components/stock-preview/ReviewSection.tsx`、`frontend/src/components/StockPreviewDialog.tsx`、`backend/tests/test_review_section.py`、`backend/tests/test_flip_trades.py` | 低(只在 fork 自己的新块里) | 回退本提交即回到 R442 的两页切换 |
