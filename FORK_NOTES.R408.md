# R408 — 亮色主题压深四档:R399 撤掉阴影时欠下的账

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R408 | 用户: 「好像你前面改过网页后的所有颜色显示都淡了好多, 看起来好辛苦」「总之觉得颜色都浅了」。**先去量, 不是先去改**: 用真浏览器把 `oklch()` 解析成像素再算 WCAG, 并把 R399 前后的 `/watchlist` 逐像素比了一遍 —— **R399 一个颜色变量都没改**(每个 CSS 变量的 diff 全是新增行), A/B 两张图 0.05% 的像素有差, 平均差 0.00~0.01/255, 饱和度与对比度到小数点后四位一致。**所以"颜色被改淡了"这个说法不成立, 但"亮色主题确实太淡"是成立的** —— 量出来: `--fg-muted` 4.38:1(**低于正文线 4.5**)、`--border` 1.12:1、`--elevated` 1.10:1, 而暗色那边同样三个是 6.09 / 1.86 / 1.28。**根子在 R399 那一轮把静态面的阴影全关了**(`shadow-sm` 48 处 / `shadow-card` → none), 理由写的是「层级靠 base/surface/elevated 三档明度差 + 1px 边框, 不靠阴影」—— 这话**在暗色成立, 在亮色不成立**: 边框 1.12、抬升 1.10, 阴影一撤就真的什么都不剩了。所以这一轮补的是那一轮欠下的账, 不是重新配色。**压深四档**(色相逐个照抄, 只动明度, 彩度补了两三个千分位免得发灰): `--border` #E7EAF2→#D1D4DD(1.12→1.38)、`--border-input` #DCE0EF→#C2C7D6(1.23→1.58, 仍比普通边框深一档)、`--fg-secondary` #596376→#4B5569(5.65→7.00)、`--fg-muted` #6B7488→#5A6377(4.38→**5.63, 过了正文线**), 外加 `--elevated`(1.10→1.22)。**`--bull`/`--bear`/`--warning` 与强调色一个没碰** —— 那三个是 R368 冻结的。**守卫当场拦了我两次, 而且拦得对**: ① `test_R382_作者的黑一个字没动` —— 我顺手把暗色的 `--border`/`--elevated` 也提了一档, 而那是 R163「还是用回以前作者的黑吧」定过案的, **已退回原值, 暗色这一轮一个字没动**; ② `test_R368_每个色值都真的是用户给的那个hex` —— 亮色这四个 hex 是用户当初逐值给的。这一条不是"绕过守卫", 是**用户改口了**: 那张表的注释本来就写明铬色是皮肤「以后还可能再换」, 而指标色(`MARKET`)才是冻结的。所以更新的是 `CHROME` 表, 并把旧→新、为什么是这四个、暗色为什么不动全写进了那张表的注释里。**顺带修掉一处会说假话的手抄件**: `lib/theme.ts` 的 `LIGHT` 图表调色板是 index.css 的手抄件(canvas 不吃 CSS 变量), 它自己的注释写着「改 token 就得回来改这里, 否则轴上的灰和界面上的灰是两种灰, **而两边都不报错**」—— 这一轮正是那种情况, 已同步 `text`/`border`/`crosshairLabelBg` 三个。 | 改 `frontend/src/index.css`(亮色五档 + 注释), `frontend/src/lib/theme.ts`(`LIGHT` 手抄件三处), `backend/tests/test_theme_palette.py`(`CHROME` 四个 hex + 改口的来龙去脉);**暗色一个字没动**;**打分系统 / `strategy/` / 六态 / `keltner*` / `position_exit` / `today*` diff 全空**;后端 pytest **4452 passed / 10 skipped 退出码 0**, 前端 tsc 0 / eslint 0 error / build 退出码 0 / vitest 107 passed | 中(动的是全站共用的铬色令牌 —— 影响每一页, 但只改明度不改色相, 且逐值量过) | 可(把 index.css 那五个值与 `CHROME` 表一起改回旧 hex; 两处必须同时改, 否则守卫会红) |

**这一轮学到的, 值得记住**:

- **「感觉变淡了」和「确实太淡」是两回事, 但结论可以一样。** 我先花时间证明了 R399
  没碰颜色(逐像素 A/B), 差点就拿这个去回用户「不是我改的」—— 那是**答非所问**:
  用户说的是现在看着辛苦, 不是在追责。量完发现亮色本来就不及格(`--fg-muted` 4.38
  低于 WCAG 正文线), 该改还是得改。
- **守卫拦住我两次, 两次都是对的。** 暗色那两处是我自作主张加的, 退回去了;
  亮色那四处是用户改口, 改的是需求本身, 于是连同理由一起写进守卫的注释。
  **区别在于"谁改的口"** —— 这条区别没有机器能判, 只能写清楚留给下一个人看。

**这一轮没动的**: 暗色主题(足够, 且是定过案的作者黑)、`--bull`/`--bear`/`--warning`、
R399 撤掉的那些阴影(补对比度就够了, 不需要把阴影加回来)。
