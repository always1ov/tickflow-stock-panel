# R535 — 设置八个子页整改: 一种卡头、一个宽度, 逐栏整理版面

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|---|---|---|---|
| R535 | 用户「设置里面的每个子页面也要整改」, 看过方案图后「确认」。**只改表达**, 每个开关/输入框/按钮的行为与文字、数据、接口、`?highlight=` 深链都没动。全局: ① 卡头收成一个产地 `SettingsCard`(图标 + 标题 + 一行说明 + 右侧动作 + 高亮定位) —— 原来 AI / 实时监控各抄一份 `Card`、系统五张卡各自手搓、菜单 / 扩展页面用 24px 大号开篇块 `SectionIntro`; 后者只剩这两处, 随之删除(R379 的「开篇块只有一个产地」守卫改为钉 SettingsCard); ② 宽度只在外壳定一处: 八栏统一 1024px(原来 AI 672、网络/系统/扩展页面铺到 1500); ③ 卡片间距统一 20px。逐栏: AI —— 连接状态并进配置卡第一行, 页底通栏「保存配置」挪到卡头(与「AI 档位」的保存同位置), 橙色 Key 提示降成卡底灰字; 系统 —— 五张单行卡收成「常规」「维护」两张; 网络 —— 「超时设置」与「数据传输压缩」拆成两张卡(原来一张卡的保存只管超时), 两种尺寸的开关并成一种, 滑块由过渡 `left` 改为 `transform`(动效硬规则第 1 条); 菜单 —— 16 行同样的「内置」徽标不再逐行印, 只标分组/扩展, **修手机 bug**: 7 列固定宽度 304px 把菜单名整列挤没, 手机上藏掉类型列与路径; 扩展页面 —— 两块大号开篇换统一卡头, 外部网页说明与表单合成一张卡, 列表与空状态收进卡里; 数据源 —— 置顶黄色「插件化」提示条降成一行灰字(黄色只留给要处理的状态); 实时监控 / 通知 —— 只换卡头与间距 | frontend/src/pages/settings/SettingsCard.tsx(新); frontend/src/pages/Settings.tsx; frontend/src/pages/settings/{AI,AiProfiles,DataSources,ExtPages,JobTimeoutCard,MenuSettings,Monitoring,Notifications,System,Timeout}.tsx; frontend/src/components/SectionIntro.tsx(删); backend/tests/test_settings_r535.py(新); backend/tests/{test_theme_palette,test_ui_hierarchy,test_settings_r533,test_design_spec}.py | 中: 设置区各面板上游仍会改, 卡头这一层是 fork 的共用件, 合并时上游新卡按 SettingsCard 改写即可 | 可以: git revert 本提交(SectionIntro 随之恢复) |
