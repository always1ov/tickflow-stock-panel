# R485 — 庄现移植到量化MACD 副图(狗头标记, 冻结)

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|---|---|---|---|
| R485 | 用户:「移植到量化macd里面, 当作辅助指标不在动了, 就算我以后微调量化macd也不动这些了」「庄现用狗头」。把通达信「主力捉妖」里的庄现逐行复刻成独立模块 indicators/zhuang_xian.py(9 日 KDJ 的 J 上穿 -J, 且 10 天前收盘在 60 日线上方); 需要的 SMA / MA / HHV / LLV / REF / CROSS 在模块里各抄一份, 不 import quant_macd。/quant-macd 响应多一个可选字段 zhuang(出现那根为 1, 其余 null), 另取 high / low 列, 由该模块单独算、不经过 quant_macd.compute。前端新文件 lib/zhuangXianSeries.ts 画一个柴犬头(自带 SVG, 黄褐 / 米白 / 深褐, 全站禁粉), 贴着量化MACD 副图底边、水平居中于那一天, 位置不读 DIFF / DEA; 在 AnalysisKChart 里放在 series 最后, 不动悬停联动的下标。不可点、无悬停说明、无动画。拐点 / 确认按用户选择不移植(要 WINNER 与未来函数 DYNAINFO, 系统里对不上)。守卫 test_zhuang_xian.py 14 条: 与独立 pandas 逐行翻译逐根一致、冻结基线(固定输入下出现的 12 根下标)、后端不 import quant_macd(ast)、前端不读量化MACD 数值、把 quant_macd.compute 换成胡乱结果庄现一根不变、缺高低价不抛异常; 变异: 接回 quant_macd / 去掉 60 日线条件 / 改 SMA 权重各被抓到。前端 zhuangXianSeries.test.ts 3 条。CONTEXT.md 加「庄现」词条 | backend/app/indicators/zhuang_xian.py; backend/app/api/stock_analysis.py; backend/tests/test_zhuang_xian.py; frontend/src/lib/zhuangXianSeries.ts; frontend/src/lib/zhuangXianSeries.test.ts; frontend/src/lib/api.ts; frontend/src/components/stock-analysis/AnalysisKChart.tsx; CONTEXT.md | 低(除 stock_analysis.py 的量化MACD 端点外都是 fork 自有新文件; AnalysisKChart 只加 4 行) | 可以: git revert 本提交; 前端对没有 zhuang 字段的响应本来就兼容 |
