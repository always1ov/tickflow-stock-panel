# R393 — 「今日总览」这个已拆页面的名字,按用户划的范围清到底

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R393 | **承 R392 留给用户的那个问题**, 用户定了范围: 「渲染前端可以动的, 核心逻辑不允许」。那一页 R340/R351 拆了并重定向到模拟盘, 名字却还散在六处。**按这个范围逐处定性**: ① **前端三处 + 弹窗一处 —— 清掉**(R392 已做); ② **`data_doctor.py` 存储体检那张表的标签「今日总览偏好」—— 清掉**, 改成「总览门槛偏好」。它是 `Store.cn`(docstring 原话「给人看的名字」), **`rel` 才是键** —— 只动展示名, 动键会让体检对不上这份存储; ③ **`api/today.py` 剩下两处 —— 不动**: 一处是**送给模型的提示词**, 一处是 payload 的**键名**, 而那份 payload 按 [R121] 同时是"校验 AI 有没有忠实使用我们喂的数据"的账本 —— 改键等于动核心逻辑, 正是用户划掉的那一半。**所以这一轮没有给它加 `TERMS` 行**: 加了会当场把这两处判成违规, 而唯一的消解办法是去动不许动的东西, 或者把 `api/today.py` 整个塞进 `SKIP_FILES` —— 那会连它里面真正的界面文案(自检条那几个登记名)一起不扫, **比不加更坏**。理由写进 `NOT_A_CONFLICT` 存档, 免得下一个人再从头论证一遍。**守卫的扫描面这次一次性钉满**: R392 那条只扫改过的两个文件, 正是 R258 栽过的「扫描面比纪律小」—— 那两个文件清干净之后, 下一处新写的旧名照样能溜进去而守卫全绿。现在**整个 `frontend/src` 一起扫**(前端渲染文本一处都不许有, 剥注释后再扫), 后端那一侧则**只钉存储注册表的 `cn` 标签**(纯展示面, 不碰提示词)—— 两条合起来正好覆盖用户划的那个范围, 不多不少。**2 处变异逐一确认会红**: 别的前端文件里冒出旧页面名 → 1 红(证明扫描面真的铺开了, 不是只盯着改过的文件); 体检标签退回旧名 → 1 红。 | 改 `backend/app/services/data_doctor.py`(**只改 `cn` 展示名, `rel` 键未动**)、`backend/tests/test_terminology.py`(补一条 `NOT_A_CONFLICT`, 纯文档不参与断言)、`backend/tests/test_score_ledger_silent_skip.py`(扫描面从两个文件铺到整个 `frontend/src`, 加一条钉存储注册表标签);**`api/today.py` 的提示词与 payload 键一个字没动**;**`backend/app/strategy/` diff 全空**;冻结的 `opportunity_score.py` / `score_ledger.py` 未动;**判定、六态、口径、指标色一个没动**;后端 pytest 4293 passed / 10 skipped 退出码 0, 前端 tsc 0 / eslint 0 error / build 退出码 0 | 低(纯展示名与守卫扫描面; 键、提示词、接口形状均未动) | 可 |

**知情保留**: 「今日总览」在 `api/today.py` 里仍有两处, 是**有意留下的**(见上)。它们不在界面上, 但确实是同一个已拆页面的名字 —— 哪天那条 AI 链要动了, 顺手一起改掉, 那时再加 `TERMS` 行就没有代价了。
