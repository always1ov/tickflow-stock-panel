# R519 — 自选「一张表」: 名称格右侧按钮组每行对齐(分组按钮定宽)

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|---|---|---|---|
| R519 | 用户:「一张表, 显示全部个股或者未分组的个股, 我滚动个股, 会发现不对齐, 可能是有些分组了, 有些没分组导致了视觉差别」。问题原因: 名称格右边那组按钮(分组 / 移除 / 置顶)靠右对齐, 而分组按钮的宽度跟着内容走 —— 未分组是 14px 文件夹图标, 一个组是 8px 圆点, 叠点更宽, 超过三个组还挂「+N」, 于是分组按钮的左边缘每行不同; 用 Playwright 量 42 行, 落在 617 / 637 / 641 三个位置, 最多差 24px。修复: 分组按钮定宽 32px(w-8 shrink-0)、内容居中、去掉撑宽用的左右内边距; 32px 装得下最宽的一种(三个叠点 + 「+N」)。验证: 同一份数据(含同时属于 2 个、4 个组的票)再量, 42 行只剩一个位置。守卫 test_watchlist_table_align_r519(定宽类名 + 叠点上限不许放宽) | frontend/src/components/WatchlistGroups.tsx; backend/tests/test_watchlist_table_align_r519.py | 低: WatchlistGroupPicker 是 fork 加的组件 | 可以: git revert 本提交 |
