# R412 — 粗细档交给回测:评「线画得准不准」,不是「跟着做赚不赚」;顺带把那三条再改一次名

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R412 | 用户: 「粗中细我看不懂, 这个调优能不能交给 ai 就像我六态设置了一个回测按钮, 参考这种模式」。**先修正我自己说过的一句话**: R406 我写过「这个指标不出买卖信号、不进把握分, **没有可打分的目标函数, 也就没有"校准"这回事**」—— **那句话说快了**。"赚不赚钱"确实评不了(要算收益就得先编一条买卖规则, 而这一组从 R405 起的口径就是「只有位置, 没有动作」, 编一条去回测等于把判定层从后门接回来, 卖出那一半还会和止盈线/生命线抢答案); **但另有一样东西完全可以回测, 而且它正好就是"粗细档"在问的问题**: 历史上每一次上攻之后, **实际回踩的最低点有没有落在当时画出来的某条线上 / 那条密集带里**。这个量不需要任何买卖规则, 不碰判定层, 不进把握分。**必须除以线数, 这是整件事的关键**: 细档画更多线, 蒙中的概率天然更高 —— 不除这一下, 任何"命中率"排序都会**必然**推荐最细那一档, 那不是调参, 是过拟合的标准形态; 所以主指标是「每条线的贡献 = 命中率 ÷ 平均线数」, 表上单独一列并加粗。**建议规则故意只有两句、写死、读得懂**: ① 样本 < 3 次上攻就不给建议(三次里蒙对两次说明不了任何事, 六态那个回测也有同一条); ② 命中率差在 10 个百分点以内算"差不多", 这时选线更少的那一档(这一条同时表达「大道至简」和挡住"线多蒙中"那条捷径)。**故意不做加权评分** —— `0.4×命中率 + 0.3×带命中 + 0.3×线数` 看着科学, 但那几个权重是我编的, 没人能复核; 不如把两句话写在代码里, 一眼看得出它凭什么这么选。**不用未来数据, 三处都钉了守卫**: 容差用**那一段结束那根**的 ATR(用最后一根的等于把今天的波动率泄露回过去); 回踩窗口在"价格站回聚焦点"或"下一段上攻开始"处结束; **窗口不足 3 根就不算数** —— 最后一段的回撤往往才走一两根, 拿"到目前为止的最低点"当答案会系统性偏高。**AI 顾问按六态那个形状接**(规则建议保底 + 可选一次调用), 提示词里写死四条纪律, 第一条就是「线多必然更容易蒙中, 只看命中率排序是过拟合」, 第三条是「不许给任何买卖/仓位/止盈止损建议 —— 你选的是画多细, 不是做不做」; 返回的档名不在三档之内就忽略。**顺带把那三条推算位再改一次名**: 用户提的是「阶段止盈点一/二」, 但**本仓库已经有「止盈线」**(`position_exit.py` 的 ATR 三阶段吊灯止盈 —— 决策台有一列、会触发盘中推送、优先级排在生命线下面); 同一屏上两个不同体系的「止盈」数字正是名词表在防的事, 用户听完选了不撞名的 **「上攻推算位一/二/三」**(把来源写进了名字里)。**写的过程里被自己的守卫拦了三次, 三次都对**: ① 浮点边界 —— `0.8 - 0.10` 在浮点里是 0.7000000000000001, 于是**正好差 10 个百分点的那一档被挤出了"差不多"的范围**, 而它恰恰是这条规则最该照顾的边界(测试当场抓到); ② 新弹窗写了两处 `rounded-lg`, 设计棘轮当场红; ③ AI 提示词里写了「斐波那契回撤」, 名词表那条「光秃秃的斐波那契不许上界面」当场红(改成「斐波那契二型的回踩位」)。**守卫 +16 条, 7 个变异逐条确认会红**, 其中一条专钉「结果里一个收益字段都不许有」—— 只要冒出一个, 就说明有人从后门把判定层接回来了, 而那在屏幕上只表现为"多了一列数"。 | 新增 `backend/app/indicators/dinapoli_fit.py`(纯函数, 无 IO)、`backend/app/services/fib2_grain_service.py`、`backend/tests/test_dinapoli_fit.py`(16 条)、`frontend/src/components/stock-analysis/Fib2GrainDialog.tsx`;改 `backend/app/api/stock_analysis.py`(+1 端点)、`backend/app/indicators/dinapoli.py`(三条改名)、`backend/tests/test_terminology.py`(三行改名 + 旧名进别名)、`test_fib2_readability.py`、`frontend/src/lib/api.ts`(客户端 + 类型)、`AnalysisKChart.tsx`(按钮 + 弹窗 + `symbol` 入参)、`StockLevelsPanel.tsx`(传 symbol)、`thinFib2.test.ts` / `fib2Status.*`(跟着改名);**打分系统 / `strategy/` / `keltner*` / 六态 / `position_exit` / `today*` diff 全空**;后端 pytest **4478 passed / 4 failed / 10 skipped** —— 那 4 个是 R411 记过的时间依赖, 与本轮无关; 前端 tsc 0 / eslint 0 error / build 退出码 0 / vitest 128 passed | 中(新增一个端点与一个回测模块; 但**回测只出建议, 改不改档位仍是点一下的事**, 不改任何线、任何判定) | 可(删 `dinapoli_fit.py` + `fib2_grain_service.py` + 端点 + 弹窗与按钮; 改名要同时撤 `TERMS` 那三行的别名) |

**这个回测和六态那个的区别, 值得单独记一笔**:

| | 六态「回测调参」 | 二型「回测这三档」 |
|---|---|---|
| 评什么 | 跟着做**赚不赚** | 线画得**准不准** |
| 凭什么能评 | 它出买卖信号, 有收益可算 | 它不出买卖信号 —— 但"实际回踩低点落没落在线上"是个可数的事实 |
| 主指标 | 跟随收益 − 买入持有 | 命中率 ÷ 平均线数 |
| 最大的坑 | 过拟合(前后半段一致性检查) | **线多蒙中**(必须除线数) |
| 存哪 | 每票一档(`livermore.json` overrides) | **全局一档**(用户选的) —— 「N 条挤在一起」在所有票上含义一样才能横向比 |

**还没做的**: 批量回测(六态有 `/trend/backtest-batch`, 这里只有单只)。一只票一次
AI 调用, 一百多只自选就是一百多次 —— 六态那边批量是**纯计算不问 AI** 的, 这里要做
也该照那个口径。等用户先用单只看看值不值得。

**留给用户的那一步**: 这个开发环境里没有真实行情, 这一轮的回测是喂手工构造的
两段上攻验的。请在自己环境里挑几只熟悉的票点「回测这三档」——
**先看「样本」那一列**: 如果大多数票都只有一两次可评估的上攻, 那说明窗口(现在是
750 天)得放长, 或者推进段的门槛(≥8 根、≥3×ATR)对 A 股偏严。这是回测本身
能不能用的前提, 比它推荐哪一档更要紧。
