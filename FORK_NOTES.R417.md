# R417 — 量化MACD 副图对着用户的通达信截图逐像素校正

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R417 | 用户发来通达信里这张副图的截图, 要求「显示必须得和通达信显示的一样」。逐像素量出 R415 按文档推的四处不对, 全部照实测改: ① **柱宽**: 截图 K 线间距 15.1px、实心柱 10px → 66%(R415 照手册「宽度 10 为标准间距」推成 20%, 与实际显示不符); ② **空心柱会盖住底下的 DIFF**: 通达信画空心柱是先用底色把整块填掉再描边, 所以 DIFF 实心柱只露出**超出 DEA 的那一截**, DIFF 没 DEA 长时整根被盖住、只剩 DEA 的框(截图里「上面一截实心、下面一个空框」)—— 新增 `diffExposed()` 只画露出来的那一截(不去猜页面底色, 结果与"先画再盖"相同), DIFF 改用自画矩形, 颜色按 DIFF 本身正负; ③ **空心框是点线**(1 点 1 空): 截图里空心框两条竖边的亮度按 3 像素一个周期起伏且两边同相, 实线怎么缩放都出不来这种纵向起伏, 正是 1 点 1 空点线被截图缩到 2/3 的样子; ④ **图标**: 带箭杆的箭头, 约 13×18px, **图标顶端对准数值**、水平居中, 1 号红朝上、2 号绿朝下; 2 号图标的绿是通达信图标自带的 #00DC00(截图三个死叉箭头一致, 而绿柱是纯 #00FF00)。截图同时证实了: 红 FF0000、绿 00FF00、黄 FFFF00、`COLOR0000CC` 是深红不是蓝、DIFF 实心 / DEA 空心 / 黄柱实心、黄柱画在最上面。 | 改 `frontend/src/lib/quantMacdSeries.ts`(+`diffExposed`/`renderDiffRect`/`HOLLOW_DASH`/`ICON_UP`/`ICON_DOWN`)、`.test.ts`(14 条)、`lib/theme.ts`(`icon2`);**后端、打分系统、`strategy/`、六态 diff 全空**;变异 13 个全杀(首轮存活 1 个: 按那一截起点判颜色 —— DEA 在正侧、DIFF 在负侧时会把绿判成红, 补了用例后杀掉);tsc 0、eslint 0 error、vitest 142 passed、vite build 0;与用户截图并排出图核对 | 低(只动副图画法) | 可(回到 R416 的画法) |

**量法**(下次再对屏时照这个来): 截图是 2000px 宽的 webp。柱宽取黄柱(实心、边界清楚)的
亮度跳变位置; 间距取相邻黄柱左沿之差; 空心柱的"盖住"取 y=150 一行 —— 那一行在
DEA 以下、DIFF 以上, 框里是纯黑而不是红, 说明 DIFF 在框里被抹掉了; 点线取框竖边
逐行亮度; 图标取三个死叉 / 三个金叉箭头的最亮像素。
