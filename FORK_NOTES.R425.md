# R425 — 个股分析决策台: 「走势」「位置」两列并成一列一个按钮

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R425 | 用户: 「个股分析页面的走势列和通道列合并成一个按钮入口」(通道列 = 「位置」列, 它的悬停写着「点开就是复盘的『通道档位』页」)。两格原本**通向同一个复盘弹窗**, 只是落在不同页签(走势 → 趋势、位置 → 通道档位)—— 两个挨着的按钮、一个去处, 正是 R228 在走势列内部治过的那个毛病, 这回在两列之间。并成一列「走势/位置」(列名照 R212「现价/涨跌」的写法), **整格一个按钮**, 点开落在「趋势」页签, 通道档位在弹窗顶上切。版面: 左半 = 六态徽标 / 转折后第 N 天, 右半 = 短期通道位置名 / 离轨距离, 外壳是 2 行 × 2 列的网格(按列排), 左右同一行共用行高 —— 第一版左右各自竖排, 出图看到徽标带边框比位置名高, 第一行一高一低, 换网格后两条基线都齐。悬停仍各半一份(讲的是两件事), 去处那句两半共用一个常量。组件: `ChannelStateCell` / `PositionCell` 退成只交内容的 `TrendSegment` / `PositionSegment`(已经不是格子了, 名字跟着改), 新增外壳 `TrendPositionCell`; 列宽原数相加 20% + 14% = 34%, 总和仍 100、仍大于「AI 信号」的 26%;表头两份说明并成 `HEAD_TIPS.trendPos` —— 顺手去掉原 trend 说明里「右边的『档位』列」那句过期指路(R308 就改名「位置」了)。排序不变(仍只按六态, 位置那半 R307 起就不挂排序)。骨架行跟着少一格。 | 改 `frontend/src/components/stock-analysis/WatchlistDecisionBoard.tsx`、`decision-board/cells.tsx`、`decision-board/BoardSkeletonRows.tsx`;守卫跟着换锚点、规矩一条不丢: `backend/tests/test_board_columns.py`(列序 / 宽度不变式 / 表头 / 调用点 + 新增「整格恰好一个按钮、两半不许自带按钮或格子、不按两半分落页签」)、`test_flip_trades.py`、`test_state_run.py`、`test_hint_layer.py`(带「?」的表头 5 → 4);**后端业务代码、打分系统、`strategy/`、六态 diff 全空**;变异 2 个全杀;后端 4512 passed、vitest 155、tsc / eslint / build 0;亮 / 暗样例出图核对 | 低(只动决策台一列的呈现) | 可(恢复两列与两个组件, 守卫锚点换回) |
