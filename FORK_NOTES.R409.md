# R409 — 关键价位重新配色:一个指标一个颜色, 两套主题各一份, 而且不许浅

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R409 | 用户: 「关键价位的所有指标的各个指标的颜色都要不一样, 因为重叠的时候容易混淆是视觉, 而且不能搞浅色, 也要符合自身含义」。**先把现状量出来**(ΔE = OKLab 距离×100; 对比度 = WCAG, 底色取两套主题各自的页底): **压力支撑与布林带是同一个 `#F97316`, ΔE 0.0** —— 两组线叠在一起完全分不出谁是谁; 另有 32 对 ΔE < 20, 其中 5 对 < 8.5; **亮色主题下四个组几乎看不见**(通道长期 1.35、通道中期 1.69、前高前低 1.79、一型 2.01 —— 1px 虚线在这种对比度下等于没画); **ATR 波动通道 `#EF4444` 离 K 线涨红 `#C74040` 只有 ΔE 5~6**, 一条指标线长得像一根阳线, 这是最要命的一个。**改法四条**: ① **每个主题一份值**。一个 hex 要同时在近白底和近黑底上站住, 就只能挤在很窄的一段明度里 —— **那正是"浅"的来源**; 分成两份后亮色整体压深(对白底 3.01~9.70)、暗色整体提亮(对黑底 5.13~15.66), 两头都不浅。② **色相按语义排开**, 红(涨)绿(跌)两段整个让开, 任何指标色离 K 线涨跌色 ΔE ≥ 10。③ **量化通道三档留在同一个青族靠明度走档** —— 它们本来就是一个指标的三个周期, 换三个色相反而读不出"同一家"; 但不再靠"一档比一档浅"来分(原来长期是 `#67E8F9`)。④ **斐波那契二型整组从金挪到洋红**: 一型(作者的)保住金 —— 斐波那契配金是约定, 二型是 R405 新加的由它让路; 两个名字只差一个字, 再同色就是全图最容易混的一对。**「不能搞浅色」不只是色值的事**: 画线时还叠了两层透明度 —— 常态 `opacity: 0.7` 再乘 `strengthColor` 的 0.55(弱档), 实际落到 **0.39**, 色值再深也白搭; 现在收窄到 0.9 × 0.75 = 0.68。**顺带收掉三个野生 hex**: `CURVE_DEFS` 里布林中轨 `#FB923C` / ATR上轨 `#F87171` / 二型均线 `#8A8578` 是组色之外另写的 —— 开关上的小圆点一种色、图上的线另一种, 而且这三个与别的组撞不撞谁也没管(`#F87171` 离涨红只有 ΔE 6)。上下轨靠位置、中轨靠实线就分得开, 不需要再换色。两个"档位/粗细"选择器的选中态也从写死 hex 改成跟组色走。**配色只有一个产地**(`lib/theme.ts` 的 `LEVEL_PALETTE`), `LEVEL_GROUPS` 与 `CURVE_DEFS` 的 `color` 列一起删掉了。**后端那三个常量退回去只当角色标识**: 颜色要按主题分两套而后端不知道当前是哪套, 所以 `dinapoli.py` 的 `C_RETR`/`C_TARGET`/`C_INVALID` 现在是键, 前端 `FIB2_ROLE` 以它们为键补上另一套 —— **这是一层隐式耦合**, 值取的是亮色那一份(认不出来最多退化成"暗色下用了亮色的值", 不会没颜色), 并专门钉了一条前后端逐字对得上。**守卫 12 条, 14 个变异逐条确认会红**; 其中两条**当场抓到我自己挑错的色**: 暗色 ATR 我只比了 K 线画布那两个 hex, 忘了界面文字用的 `--bull` 是另一对(ΔE 8.9, 已往洋红推一档); 「一型二型不同色族」第一版写成 `ΔE ≥ 25` 而实测 24.3 —— **ΔE 在这儿不是好判据**(它把明度差也算进去, "同一个金一深一浅"和"金 vs 粉"能得到相近的数), 改成量色相隔了多远。**最终**: 两套主题下最接近的一对都是 ΔE 9.1, 且都是有意同族的量化通道三档(改之前是 0.0)。**用户点名的红线再查一遍全未动**: 打分系统、作者内置(`strategy/` + `keltner*` + `pipeline`)、六态。 | 改 `frontend/src/lib/theme.ts`(`LEVEL_PALETTE` / `FIB2_ROLE` / `levelColors` / `fib2RoleColor` / `useLevelColors`)、`frontend/src/components/stock-analysis/AnalysisKChart.tsx`(两处定义去掉 color 列、曲线与价位线取色、二型色带与标记、开关与两个选择器、透明度两档)、`backend/app/indicators/dinapoli.py`(三个常量改为角色标识);新增 `backend/tests/test_level_palette.py`(12 条);**打分系统 / `strategy/` / `keltner*` / `pipeline` / 六态 / `position_exit` / `today*` diff 全空**;后端 pytest **4452 passed / 10 skipped 退出码 0**, 前端 tsc 0 / eslint 0 error / build 退出码 0 / vitest 107 passed | 中(动到 13 个价位组共用的取色路径, 且后端三个常量换了值; 但形状没变, 不传 `color` 的那 12 组行为与以前一致) | 可(`LEVEL_PALETTE` 的值改回旧 hex 并把 `color` 列加回两处定义; 后端三个常量与 `FIB2_ROLE` 的键要同时改, 否则守卫会红) |

**新配色与语义对应**(亮 / 暗):

| 指标 | 颜色 | 为什么是它 |
|---|---|---|
| 压力支撑 | 橙 `#C46D16` / `#FCB177` | 成交密集 = 热度; 它本来就是橙, 只压深 |
| 前高前低 | 中性最强 `#38414A` / `#DBE6F2` | **硬事实**, 不加色彩判断; 也是全图对比最高的一档 |
| 斐波那契一型 | 金 `#7D650E` / `#EDC327` | 斐波那契配金是约定, 作者这组保住金 |
| 缺口位 | 橄榄 `#8E9618` / `#AAB520` | 跳空断口; 原来的粉让给了别处 |
| 整数关口 | 中性中档 `#758290` / `#93A0AE` | 心理位 |
| 枢轴点 | 紫 `#9362F9` / `#9968FA` | 算出来的中枢 |
| 六态关键点 | 品紫 `#9414B3` / `#D75AFA` | 原来是淡紫 `#A78BFA`(亮底 2.54), 压深 |
| 斐波那契二型·回撤 | 洋红 `#E01DB5` / `#FCA2DF` | 整组让出金色 |
| 二型·目标一二三 | 蓝 `#1889E6` / `#86C0FC` | 规格 §12「目标蓝」 |
| 二型·失效位 | 暖褐 `#6B4D41` / `#937E6D` | 作废 = 退色 |
| 布林带 | 靛蓝 `#3D39F7` / `#7F96FA` | 统计带; **让出橙**(原来和压力支撑一模一样) |
| 量化通道 短/中/长 | 青三档 `#1C9DAE`·`#147F86`·`#0D626B` / `#2EE2F9`·`#26C4CF`·`#1EA3B1` | 一个指标的三个周期, 同族靠明度走档 |
| ATR波动通道 | 玫红 `#B31364` / `#F845AF` | 警示, 但**避开 K 线的正红** |

**留给用户的那一步**: 这个开发环境里没有真实行情, 上面这套是**单独出图逐色量过**
(亮暗两张都拍了)的, 但**叠在真实蜡烛上是什么感觉还没看过**。请打开一只熟悉的票,
把几组常用的一起勾上看看 —— 觉得哪一个不顺, 告诉我是"太跳"还是"看不清",
改一个值就行(守卫会保证改完仍然两两分得开)。

**这一轮没动的**: 「未来」区(要扩 x 轴, 会动到所有价位组共用的坐标轴)、
右侧标签在线多时会叠、`SegmentedControl` 收口(R402 留的)、阶段三 3.3 逐页迁移。
