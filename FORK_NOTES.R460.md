# R460 — 全站迁移 · 连板梯队

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R460 | 全站迁移第十二步(规范见 `docs/ui-hierarchy.md`)。连板梯队: 页内 44 处写死字号按角色落档; 筛选小胶囊 10px → 13px(可点的控件按正文级); 封板口径、梯队筛选、显示设置(显示模式 / 排列方向)、隐藏位置这几处的选中态从强调色换成全站反相(共 9 处); 一个强调色底的按钮换成全站按钮。**这一页在本地测试数据下渲染不出来**(没有交易日数据, 只有加载圈), 没有截图核对, 改动只限字号落档与选中态这种不改排版的替换, 由类型检查与全量测试兜底。棘轮 任意字号下调到当前数 | `frontend/src/pages/LimitUpLadder.tsx`、`docs/ui-hierarchy.md`、`backend/tests/test_ui_hierarchy.py`、`backend/tests/test_design_spec.py` | 中(与上游同源, 上游改这一页时字号类会冲突, 以本 fork 的档位为准) | 回退本提交即回到原样式 |
