# R402 — 自选分组栏够不到下面;异动监控等六处「字是断的」;看板页补上粘住的页头

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R402 | 承 R401。用户两条: 「那两页也一起改了」(指 R401 末尾自陈没覆盖到的 `/dashboard` 与 `/limit-ladder`)、「自选股看不到下面, 滚动下去也不行」, 中途又发来异动监控的截图「这个页面还是又显示不完整的文字」。**① 自选分组栏没有上限也不滚。** 那一栏是 `flex-wrap`, 分组一多就往下堆; 它是 `flex flex-col h-full` 里的一个 flex 项, `min-height: auto` 让它不肯缩到内容高度以下, **于是有多高占多高**。后果分两档, 用户遇到的是第二档: 分组中等多 → 表格被挤成一条缝(实测 375×780、32 个分组时表格只剩 **184px**, 而里面有 6527px 内容); 分组再多 → 整栏超出视口, 而外层是 `h-full` 没有页面级滚动, **下面的东西根本够不到, 也没有任何东西能滚**。给它 `max-h-[34vh] + overflow-y-auto`: 手机上约六行药丸, 导航占掉三分之一、表格留三分之二(实测表格 **184px → 375px**), 分组滚得到最后一个。**右侧那个操作按钮是兄弟节点, 不在滚动层里**, 仍钉在首行, 与原有约定一致。**顺带修掉一处死代码**: 拖拽排序的 `autoScroll` 滚的是 `scrollLeft`, 且开头 `scrollWidth <= clientWidth` 就返回 —— 而这一栏自从改成 `flex-wrap` 之后**永远不横向溢出**, 条件恒真, 整个函数每次都在第一行返回; 注释还写着「分组很多时标签栏横向滚动」。改成纵向, 那才是它真正会溢出的方向。**② 「字是断的」这一族扫了一遍, 不再一张截图修一处。** 写了个真浏览器探针, 22 条路由 × 360/375/412 三档宽度全扫。**探针本身迭代了两轮**: 第一版用 `scrollHeight > clientHeight` 判断, 扫出 11 处, **一半是假阳性** —— `leading-none` 下 CJK 字形比行高高出一两像素, 根本没换行; 第二版改成用 `Range.getClientRects().length` **精确数文字排了几行**, 假阳性归零(「编辑」那一处就是这么洗掉的: 20px 宽、两个 10px 的字, `rects=1`, 一行)。真问题六处, 根因全是同一条 —— **定高盒子 + 允许被压扁 = 字换行之后画到盒子外面**: 异动监控的板块分段控件(「全板块」成了「全/板/块」, 实测 `scrollHeight 53 > clientHeight 26`, 三行字塞在一行高的盒子里)、「刷新」、「过滤ST」、「开启实时计算」; 决策台那三个「只看…」开关; TickFlow key 那一排工具按钮的「加一个」; 涨停梯队徽章的「降级」(10px 字被压进 15px 宽)。还有一处不同根因: **指数页的指数名被压成 4px 宽 —— 一个字都看不见**, 因为 `truncate` 含 `overflow:hidden`, 而 flex 项一旦 overflow 不是 visible, 自动最小尺寸就变成 0, 于是它被旁边三段短数字挤到没有。**③ `/dashboard` 补上粘住的页头。** 它是全站唯一不用 `PageHeader` 的页(渐变条是看板自己的仪表, 不是页标题, 原注释说明过), 所以 R401 给页头加的粘住惠及不到它, 那个 `fixed` 的悬浮汉堡一路压在内容上。不是把它改成 `PageHeader`, 而是给那条渐变条补同一件事: `sticky top-0` + **不透明外层**(`-mx-3 px-3` 把页面左右留白也盖住, 否则内容会从渐变条两侧的缝里露出来; `-mt-3 pt-3` 把顶部留白收进粘住层, 否则上面留一条会透内容的缝) + `pl-11` 给汉堡让位。**还把这块挪到了页面最前** —— 原来它排在「无数据引导卡」之后, 首次使用时汉堡照样压在引导卡上。**`/limit-ladder` 查了源码, 它是用 `PageHeader` 的**(三处渲染分支都是), R401 已经覆盖到; 之前扫描报它"没有页头"是因为当时页面还在转圈。加了一条守卫钉住"别哪天把它改成手搓的"。**守卫 7 条, 12 个变异逐条确认会红** —— 其中 **M5 第一次跑是绿的**: 「分段控件整组不许被挤扁」那条查的是"这一段里有没有 `shrink-0`", 而按钮自己也带 `shrink-0`, 把外壳那个拿掉照样过; 改成外壳与按钮分开查之后才真的拦得住。守卫钉的是**规律不是字面量**(「定高盒子必须配不换行」按正则逐个扫), 换文案不会漏也不会误报。**评分 / 六态 / 作者内置指标一个字节没动** —— `backend/app/` 整个 diff 为空。 | 改 `frontend/src/components/WatchlistGroups.tsx`(高度上限 + 自己滚 + 自动滚动改纵向)、`pages/AbnormalMoves.tsx`(分段控件 + 两处工具条 + 三个按钮)、`components/stock-analysis/WatchlistDecisionBoard.tsx`(三个开关)、`components/SealedBadge.tsx`、`pages/settings/TickflowKeys.tsx`(工具栏四个按钮)、`pages/backtest/StrategyBacktest.tsx`、`pages/Indices.tsx`(指数名不再被挤没)、`pages/Dashboard.tsx`(粘住的头部条 + 前移);新增 `backend/tests/test_mobile_text_and_scroll.py`(7 条);**`backend/app/` diff 全空**;后端 pytest **4381 passed / 10 skipped 退出码 0**, 前端 tsc 0 / eslint 0 error / build 退出码 0 / vitest **107 passed** | 中(自选分组栏加了高度上限 —— 分组多的时候那一栏从"全展开"变成"滚动", 是可见的交互改变; 看板页头部条粘住并前移, 也是那一页的版面改变。其余六处只是不让字断, 不改版面) | 可(八个文件各自独立; 想只退分组栏那一条, revert `WatchlistGroups.tsx` 即可) |

**扫描的完整结果**(22 条路由 × 360/375/412, 用 `Range` 精确数行数):

| 轮次 | 360px | 375px | 412px |
|---|---:|---:|---:|
| 修之前(去掉假阳性后) | 13 | 11 | 4 |
| 修之后 | **5** | 0 | 0 |

**剩下这 5 处只在 360px 出现, 而且都不是"断字", 是版面本身该重排**, 所以没有夹在这一轮里改:

- **`/dashboard` 的四个统计小格**(「涨跌 ≥3%」「封板率 0%」「最高连板」「成交额」): 六格一行, 360px 下每格只有 33px —— 四个字必然换行。这不是加个 `whitespace-nowrap` 能解决的(那只会让字溢出格子), **要么窄屏减列, 要么缩字号**, 属于版面决定。
- **`/data` 的「扩展数据」**: 360px 下排了 **4 行**(49px 宽)。同上, 是那一块的列宽没给够。

两处都要改网格列数, 影响那两页的整体版面 —— 该单独提一轮, 不该混进"修断字"里。

**这一条与 R401 的关系**: R401 修的是市场环境一页, 修法对但**范围只到用户那张截图**。这一轮把同一族缺陷扫了全站, 并且把探针留成了可重跑的脚本思路(写在这份说明里)。真要长期不复发, 下一步该做的是把那三组定高药丸收成一个 `SegmentedControl` 基础件(R400 的 `Button` 不适用 —— 分段控件是一组共用外框的药丸, 是另一个物种), 放进 3.3 逐页迁移时一起做。
