# R372 — 模拟盘持有天数

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R372 | 「现在拿着」在标的与股数之间新增持有天数；本轮首次实际买入日算第 1 个交易日，清仓后重置。 | `frontend/src/pages/FlipPaper.tsx`、`frontend/src/lib/flipHoldingDays.ts`、`frontend/src/lib/flipHoldingDays.test.ts`、本记录 | 低 | 回退本次独立提交 |

## 实现边界

- 只读既有模拟盘响应中的完整 `orders`、`nav` 与 `as_of`；不增加接口、依赖、后端计算或存储字段。
- 交易日沿用这次模拟盘净值序列的日期；截止于模拟盘的最后一天，与持仓和净值是同一份快照，不按设备时钟补天数。
- 起点取 `order.date`（实际成交），不取 `signal_date`（信号出现）；封板延期未成交期间不计入。
- 本模拟盘的 `sell` 是整笔清仓；重新买入重新计数。同一轮的再次买入不重置起点。
- 缺少买入记录或对应日历时显示 `—`，不伪装为 0 天或 1 天。
- 保留既有明暗主题、横向滚动与股票点击行为；不动策略、评分、买卖规则、收益、持仓排序或动效。

## 提交前实际验证

先用空结果实现跑首个用例，确认失败；再实现计数并确认通过。10 个用例覆盖买入当天、周末、日历长间隔、同轮再次买入、清仓再买、多标的、延期成交、快照截止、缺数和只读乱序输入。

本地没有 Vitest 包，因此用 Node 22.16.0 的 `node --experimental-strip-types --test` 执行同一份测试：仅替换测试框架导入与相对模块路径，断言和测试数据原样保留。实际结果：10 passed / 0 failed。已核对测试过的生产 helper 和测试源文件的 Git blob SHA，与提交内容一致。没有把它写成已运行 `pnpm test` 或后端全套测试。

完整 TypeScript/Vite 构建与镜像发布由本提交对应的现有 Docker Actions 验证，结果以工作流为准。部署触发成功不等于线上页面已完成验收。

本记录沿用 R371 的独立补充方式；约 0.8 MB 的 `FORK_NOTES.md` 总台账保持原样，没有用不完整文本覆盖历史。
