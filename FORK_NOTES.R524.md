# R524 — 监控中心整改: 三栏分页, 记录一条一行按日分组, 规则一条一行, 焦点名单成栏

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|---|---|---|---|
| R524 | 用户:「监控中心页面也要整改」, 看过方案图后「确认」。这一页要看的是「今天哪条规则响了、响在谁身上」。原来三层叠一屏: 顶上一条可折叠的「推送焦点」+ 左栏触发记录(卡片, 一条三四行, 900 高只看到 7 条, 9 个类型筛选占两行)+ 右栏 400px 监控规则(卡片一条两行), 各自内部滚动; **手机上触发记录整段消失** —— 那一栏是 flex-1 min-h-0, 被 13 条规则挤成 0 高(真 bug, 现状截图里根本没有记录)。现在: ① 三栏分页 触发记录 / 监控规则 / 焦点名单, 分栏条用 R512 的 PageTabs(与 Minds、模拟盘同一份), 当前栏写在 `?tab=`; 整页一个滚动区, 手机 bug 随之消失; 深链 `?new=abnormal` 改成切到规则栏再弹编辑器。② 触发记录一条一行按日分组(今天 / 昨天 / 周几 + 日期, 行里只印时分): 时间 · 谁 · 价 涨跌 · 规则徽标 · 命中了什么 · 行业/概念; 同一屏 14 条全在。「规则 …」那行灰字收进徽标悬停; 「现价」不再重复印; 行业/概念从彩块改灰字; 严重级别只留行首一个点(info 灰是常态, warn / critical 才上色); 单票规则的徽标不再印那只票的代码(与左边重复), 退回类型名; 卡片的阴影、悬停上浮、逐行 stagger 撤掉, 只有进页后新到的那条有 200ms 淡入上移(完整 transform 字符串), 旧的 1.2 秒五帧闪烁撤掉; 删除键手机上常显、宽屏悬停出现。③ 类型筛选只列有记录的类型并带计数(「全部」那一份一直拉着, 计数从它来; 选了类型再多拉一份该类型的, 与原来一样走服务端筛选)。④ 监控规则一条一行: 类型 · 对象 · 盯什么 · 启停/编辑/删除, 13 条一屏放完; 「新建规则」从 6px 图标升成带字的主按钮; 批量渠道 / 清空留图标(sr-only 有名字)。⑤ 焦点名单成一栏, 四档四列摊开(观察档仍默认折着), 说明、只认焦点名单总开关、过期提示、钉住 / 静音全在; `FocusBar.tsx` → `FocusPanel.tsx`。⑥ 「推送焦点」改叫「焦点名单」—— CONTEXT.md 与后端一直是这个名字, 名词表加一行(别名「推送焦点」), 决策台那句提示同步改。⑦ 页头副标题「实时信号与规则管理」撤掉。规则的类型 / 作用域 / 条件 / 渠道、三个弹窗(编辑器 / 批量渠道 / 标签配置)、只看焦点开关与焦点外灰显、进页清徽标、点代码看日K、点板块看成分、点行业/概念看成员、两步删除 —— 逻辑一个没动。守卫: 新增 test_monitor_r524; test_ui_hierarchy 的 MIGRATED 路径换成 FocusPanel; test_terminology TERMS 加「焦点名单 ← 推送焦点」 | frontend/src/pages/Monitor.tsx; frontend/src/components/monitor/FocusPanel.tsx(新, 原 FocusBar.tsx); frontend/src/components/stock-analysis/WatchlistDecisionBoard.tsx; backend/tests/test_monitor_r524.py(新); backend/tests/test_ui_hierarchy.py; backend/tests/test_terminology.py | 中: Monitor.tsx 有上游底子(触发记录 / 规则列表的字段渲染逻辑保留原文), 同步时版式会冲突, 以「三栏分页、一条一行」为准重放; FocusBar 是 fork 自己的(R159) | 可以: git revert 本提交 |
