# R528 — 图表上的布林线改成 26 日: 关键价位图 + 日K图, 名字带周期; enriched 的 20 日那组不动

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|---|---|---|---|
| R528 | 用户:「帮我改成 26 日布林线, 名称也是对应的表达」(问的是「图表指标里的布林轨」)。**不改 enriched 表的 `boll_upper` / `boll_lower`**: 那两列是 20 日的, 作者内置策略「布林突破」「Keltner 回归」读的就是它们(硬约束 3), 因子库 `boll_position` / `boll_width`、回测矩阵、盘中增量、监控命中消息也全挂在上面, 改了等于隔着一层改作者策略的输入, 还得把回测矩阵与增量路径一起挪才不会两套口径打架。只改图表: ① 新建 `indicators/bollinger.py` 作唯一产地(`BOLL_CHART_WINDOW = 26`, `BOLL_CHART_K = 2.0`, 三条线的名字), 给两条路: `bollinger_series`(polars 序列, 关键价位图用)与 `attach_bollinger`(纯 Python 逐行, 日K接口用), 都是样本标准差 ddof=1, 守卫钉两条路同数。② 关键价位图: `api/stock_analysis.py` 的曲线与 `indicators/levels.py` 的价位列表改为现场算 26 日, 中轨不再是 MA20(与生命线分开了); 开关叫「布林(26日)」, 端点标签「布林上轨/中轨/下轨(26日)」。③ 日K图(个股 `kline.get_daily` 与指数 `indices.get_index_daily`): 多读 60 个自然日垫窗口, 注入今日实时蜡烛**之后**再附 `boll26_upper / mid / lower`, 裁回请求窗口再返回; 前端 `StockDailyKChart` / `Indices` 改读 `boll26_*`, `EChartsCandlestick` 的叠加叫「BOLL(26)」, 多画一条中轨(以前中轨与 MA20 重合不用画), 悬停里的中轨从 `ma20` 改成 `boll_mid`。④ 20 日那组凡是印出来的名字都带上周期 —— 自选 / 策略结果表的列「布林上轨(20日)」「布林下轨(20日)」, 条件字段名(signals.ts / 回测页 / 策略设置弹窗)同样; 信号「突破布林上轨」是事件名, 不改, 理由进 test_terminology 的 NOT_A_CONFLICT。守卫: 新增 test_boll26_chart(参数、两条路同数、enriched 20 日四处未动、三个消费点都从产地取、前端名字) | backend/app/indicators/bollinger.py(新); backend/app/api/stock_analysis.py; backend/app/indicators/levels.py; backend/app/api/kline.py; backend/app/api/indices.py; frontend/src/lib/api.ts; frontend/src/components/StockDailyKChart.tsx; frontend/src/pages/Indices.tsx; frontend/src/components/EChartsCandlestick.tsx; frontend/src/components/stock-analysis/AnalysisKChart.tsx; frontend/src/lib/watchlist-columns.ts; frontend/src/lib/screener-columns.ts; frontend/src/lib/signals.ts; frontend/src/pages/backtest/StrategyBacktest.tsx; frontend/src/components/screener/StrategySettingsDialog.tsx; frontend/src/lib/theme.ts; backend/tests/test_boll26_chart.py(新); backend/tests/test_terminology.py; backend/tests/test_index_daily_live.py(替身钉 indices.cn_today) | 中: kline.py / indices.py / levels.py / stock_analysis.py 都是作者的文件, 同步时那几段(多读 60 天、附 boll26、_boll_channel 现场算)可能冲突, 以「图表 26 日、enriched 20 日不动」重放 | 可以: git revert 本提交 |
