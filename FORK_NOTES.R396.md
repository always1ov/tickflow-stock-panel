# R396 — 同步上游 42 个提交(v0.3.0 之后);作者又一次强推重写了历史

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R396 | 用户: 「作者有没有更新」→ 选了「全部合进来」。**第一件事是先把"有多少更新"算对 —— 我一开始算错了**: 直接 `git merge-base HEAD upstream/main` 返回**空**, 于是我照着"两条线没有共同祖先"去找, 落到了 2026-09-14 那个点, 报出「100 条提交 / 12044 行」。**真实增量是 42 条 / 100 文件 / +3769−378** —— fork 在 2026-09-20 已经由 `ad1f40dd`+`e0dcc28e` 同步过一次 v0.3.0, 我没先去查这件事。**教训写进基线行**: 这个上游的 `merge-base` 从此恒为空, **空 ≠ 没更新**, 要看 `git diff <上次锚点> upstream/main`。**作者强推重写了历史**(904 条提交哈希全换; 同一条「NDJSON 空闲心跳保活」在 fork 是 `48ddc68b`、在新线上是 `163d1bca`, 两者树只差一个 README)。沿用上次的办法: 造一个合成提交把上游当前的树接在 `ad1f40dd` 之后, 三方合并的基线就落回 `ad1f40dd`, 差异正好是 `0cd46f95..28ba5be2`。**预检脚本先修了一个真 bug 才跑得动**: `upgrade_check.py` 用严格 utf-8 解码 `git merge-tree` 的输出, 而作者往 README 里加了截图, 第一个字节就是 PNG 的 `0x89`, 当场 `UnicodeDecodeError` —— 同步上游前唯一的预检工具整个跑不完(已单独提交)。**7 处冲突, 逐条取舍**: ① 三个页面的 `toNavItems/NavItem` import 路径 —— 作者把切股导航抽成了 `lib/listNav` + `lib/useListNav` + `components/NavPager`, **取作者的**(fork 这侧没有增强); `Monitor.tsx` 额外保住 fork 的 `FocusBar`/`toast`。② `Screener.tsx` 图标清单取作者的超集; `publishSummary`/`invalidateScreenerDetails` 在上游与基线里**都不存在**(= fork 自己的代码)所以留, 下面那行注释取作者的新版(描述他新加的「分钟策略自动异步算命中数」)。③ `StockPreviewDialog.tsx` 最费事 —— 作者把导航整个抽走(−185/+81), fork 这侧有 +104 行增强(R103 关键价位视图、R100 最近查看、R164 与作者切股并存)。**自动合并其实做得很好**: 作者的 `useListNav`/`NavPager` 进来了, fork 的内联导航被干净移除(不重复), fork 的增强全在; 6 处冲突逐个解, 其中**一处是合并一度造出了两个 `role="tablist"`** —— 去掉 fork 那个重复的, 把「关键价位」这一档按作者的新样式补进他的 tablist。弹窗外壳留 fork 的设计 token: 作者那行写的是 `transition-all`, 而 AGENTS.md 前端动效硬规则第 1 条明写不许用它。④ `quote_service.py` **两边各修各的, 都保**: 作者把「持久化关闭」从 `stop()` 挪到 `disable()`(修容器重启后实时行情被自动关掉), fork 在 `stop()` 里有 R76 的空闲 key 重置、在 `disable()` 里有 R16 的叠加层清零 —— 两件事互不相干, 合成一个既收修复又留增强的版本。⑤ `kline.py` 四处: import 取作者超集; 留 fork 的 R74/R76 非阻塞单票实时块并收下作者的 `cn_today()` 时区修复(原来用 `date.today()`, 美西主机整个 A 股交易时段比北京早一天, 当日实时 K 被排除在窗口外); `_latest_live_candle` **保 fork 的三级取值**(R16 叠加层 → 批量缓存 → R77 单票兜底, 作者那版只有第二级, 是它的子集), 但把作者新加的 `index` 资产类型接进来。**`tsc` 当场抓到我解错的一处**: 我取作者的 import 块时把 `RpsRotationDialog` 带了回来, 而 fork 在 **R105** 有意把 RPS 轮动收编到复盘统一入口、从这两页删掉了 —— 作者那侧这一行根本没动(基线与上游都是 2 次), 所以该以 fork 的删除为准。**这正是「合完把删掉的行过一遍」那条纪律要抓的东西**, 只不过这次是反过来: 不是 fork 的东西被抹掉, 是被抹掉的东西被我带回来了。 | 合并上游 42 个提交(100 文件, +3769/−378: 新增 `lib/listNav.ts`/`lib/useListNav.ts`/`components/NavPager.tsx`/`services/minute_adjust.py` 等);fork 侧手工解 7 个文件;`FORK_NOTES.md` 基线锚点前移并写明「merge-base 恒为空」;**`backend/app/strategy/builtin/` diff 全空 —— 因子、阈值、META 一个没碰**;`api/today.py` / `opportunity_score.py` / `score_ledger.py` / `today_annotations.py` / `action_timing.py` / `focus_list.py` **上游一个都没动**;后端 pytest **4351 passed / 10 skipped 退出码 0**(合并前 4303, +48 是作者的新测试), 前端 tsc 0 / eslint 0 error / build 退出码 0 / vitest **85 passed**(合并前 47) | 中(上游改动面广, 且动到了实时行情开关的持久化语义与 K 线窗口的时区口径 —— 两者都是作者修的真 bug, 但都会改变线上行为) | 否(合并提交不宜单独回退; 要回退请回到 `484eed44`, 已打 tag `sync-safety-before-R396`) |

**作者这一轮的主要内容**(按他自己的提交分类: 43 个 fix + 10 个 feat):

- **AI 对话助手**扩展模块继续做(个股问答自动附分时+日K 双图、回答里的涨跌语义色与涨停徽章、系统提示词结构化)
- 自选页新增「加入日期」「加入以来」两列, 日K 上标注加入自选那天
- 分钟K 原始口径落盘 + 共享复权因子表, 读取时即时前复权投影(`services/minute_adjust.py`)
- 切股导航抽成共享件, 异动监控三个 tab 与回测 K 线弹窗都接上
- 筛选器: 新建策略「默认基础参数」配置与一键应用、分钟策略进页面自动异步算命中数
- 两个前视偏差修复: 分钟穿越成交参考线剔除当根收盘、分钟 VWAP 缺 ×100 量纲

**知情保留**: 作者在自选页新增的那批列(今开/最高/最低/昨收/成交量/涨停价/跌停价)与 fork `BUILTIN_COLUMNS` 里早就有的同名列**是同一件事做了两遍**, 这轮没有去合并两份定义 —— 合并后功能正常(那批列在 fork 这侧本来就默认不显示), 但它是一处已知重复, 哪天动那张列注册表时该一并收口。
