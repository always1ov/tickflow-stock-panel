# R510 — 宏观分析「现在」卡: 四维拆解条子定宽 + 轨道可见; 「口径」按钮改叫「过滤」

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|---|---|---|---|
| R510 | 用户发来 2000px 宽的截图:「四维拆解不正常了, 口径改成过滤」。截图里赚钱 0 / 投机 55 / 抗跌 0 / 趋势 30, 综合 20 分 —— 数是对的(0×0.35 + 55×0.25 + 0×0.20 + 30×0.20 = 19.75, 大跌日赚钱与抗跌两维本来就会贴地), 坏的是画法, 两个根因: ① 四条在 lg 起四等分 + 条子 flex-1, 卡片那一格是 1fr, 屏一宽条子就被拉到三百多像素, 数字离标签半屏远, 读不成一行 → 改成条子定宽 96px, 四条按内容排, 放不下再换行; ② 轨道底色 --base, R501 之后页底是纯白, 轨道在白卡上看不见, 0 分那一维就什么都没画, 像没渲染 → 轨道改用 --elevated。顺带每条加 title「赚钱 0 / 100」。「口径」按钮按用户改回「过滤」(R506 我给它起了新名, 与原来主线排行上的「过滤」不一致, 本就该同名), 面板文案里说明性的「口径」一词不动。守卫: test_macro_regime_prune_r505 加一条(条子定宽 + 轨道 --elevated + 不再四等分 + 按钮叫过滤) | frontend/src/pages/Regime.tsx; backend/tests/test_macro_regime_prune_r505.py | 无 | 可以: git revert 本提交 |
