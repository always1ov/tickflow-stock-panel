# R506 — 宏观分析: 情绪周期整组撤下页面, 当前主线从转折页搬进「现在」卡, 阶段切换推送关掉

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|---|---|---|---|
| R506 | 用户问「情绪周期呢, 我完全不知道什么对我有用, 经过一段时间测试也没观察出什么对我有用」, 听完分析后说「你找最优解, 转折页面的那个显示主线我想搬回这里」。分析结论: 情绪周期(作者的 market_phase)全部由涨停梯队算出(高度 / 首板与 N 板宽度 / 晋级率 / 封板率 / 梯队完整度), 衡量的是打板情绪, 阶段提示(试仓主线快进快出、不接力)是写给短线接力的; 与综合分 25% 的「投机」维是同一批数; 阶段按天判定、一段几天到两三周, 比趋势持仓短, 照它做只会多动。最优解三件事: ① 宏观分析页撤掉情绪周期整组(阶段概览六卡、阶段规律、情绪周期时间轴与点击回看、阶段 × 主线、主线排行), 连同只为它服务的查询与计算(phases / phaseLive / 主线区间查询、阶段段统计、时间轴 option 与点击选日、PHASE_LEGEND 与 MARKET_PHASE_GUIDE); 后端照算不删(regime 重算里 refresh_phase_labels 不动, 对话里 AI 助手的 get_regime 工具还读阶段); ② 「当前主线」从转折页页头副标题搬进宏观分析「现在」卡(最新状态 · 当前势头 · 当前主线 · 四维拆解), 琥珀色与停更变灰改标题「当前主线(停更)」两条规矩原样带过去, 另加「其后」两条; 数据走新增只读接口 GET /api/today/mainline, 与今日总览 meso.mainline 是同一个函数 today_mainline.latest_mainlines 出的(停更判定只在那一处, 前端不另算), 单开口子是为了不让宏观分析页为一行主线拉整份今日总览; 主线口径面板(宽基屏蔽 / 剔除 ST)原挂在主线排行上, 排行撤了改由「当前主线」旁的「口径」按钮打开, 接口一并返回当前口径; ST 开关文案从「主线 + 情绪周期」改为「主线 + 市场环境」(剔除 ST 同时作用于 regime 统计); 转折页页头不再显示主线; ③ 关掉盘后管道的「情绪周期阶段切换」推送(作者的 _push_phase_change_alert, 切入退潮 / 冰点按 warn 推): 「主升→退潮」读起来像卖出理由, 正是 AGENTS 第 10 条「趋势没坏就不给卖出理由」的反面。守卫: 新增 test_macro_mainline_r506.py 5 条(情绪周期整组不再显示、当前主线进了现在卡且色与停更规矩与口径入口都在、转折页页头不再有主线、接口行为三种情形——正常 / 停更 / 取不到返回 null 不报 500、推送关了而阶段照算); 随之改: test_flip_fusion 的 R346 主线三条断言改为「页头不再有主线」, test_macro_page_r503 与 test_macro_regime_prune_r505 去掉情绪周期那一组的断言, test_regime_mobile_text 的药丸计数按剩下的一组下调, echartsLegend.test.ts 撤掉情绪周期图例那一条(前端测试 161 → 160); 删掉的那一大段带走了不少裸圆角与写死色, 设计规范棘轮按守卫要求收紧(裸圆角 728→715, 硬编码色 729→726); 全量验证通过 | frontend/src/pages/Regime.tsx; frontend/src/pages/FlipPaper.tsx; frontend/src/lib/api.ts; frontend/src/lib/queryKeys.ts; frontend/src/lib/echartsLegend.test.ts; backend/app/api/today.py; backend/app/jobs/daily_pipeline.py; backend/tests/test_macro_mainline_r506.py(新增); backend/tests/test_flip_fusion.py; backend/tests/test_macro_page_r503.py; backend/tests/test_macro_regime_prune_r505.py; backend/tests/test_regime_mobile_text.py; backend/tests/test_design_spec.py | 中(Regime.tsx 情绪周期那一大段是作者的, 上游再改那一段会冲突, 保留本侧删除即可; daily_pipeline 删的是作者的推送函数与调用点) | 可以: git revert 本提交(阶段数据一直在算, 接回后历史完整) |
