# R406 — 斐波那契二型加「粗 / 中 / 细」档位;顺手修掉棘轮自己数错的那条

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R406 | 用户拿 R405 里那四个「原书没规定、由规格作者补的」参数去问了出规格的 AI, 对方确认: 原书只给原则和图例, 没给机器能执行的定义, `PIVOT_K` 调大只认大级别回调、调小小回调也算, 「回测时最需要拿数据去校准」。**那句话对整套系统成立, 对这个用法不成立** —— 这个指标不出买卖信号、不进把握分, **没有可打分的目标函数, 也就没有"校准"这回事**。它是个**粗细旋钮**, 哪一档合适得用眼睛定 —— 而那正好就是用户要的那一环。用户选了「一次做完, 当场能比」。**做法**: 后端一次把三档全算出来(纯几何, 250 根 K 线跑三遍毫秒级), 图上切换**零延迟不重新请求**; 界面照抄旁边枢轴点那个「档位」选择器的形状, 每个档上带当前线数(粗 7 / 中 12 / 细 14 这样), 用户不用理解参数, 只要回答「线太多」还是「线太少」。**全局一档不按票存** —— 每票一个值的话「3 条回撤重合」在不同票上含义就不一样, 没法横向比。**上攻段与首次回踩不按档重复发**: 它们只看收盘在不在均线上方, 与摆点无关, 三档共用一份。**验的过程里栽了三次, 每次都是用例数据的问题, 记下来**: ① 第一版冒烟三档结果完全一样 —— 不是接线断了, 是那份台阶数据里的浅回调**全都被后来更低的点盖住了**, 于是粗细不影响结果。这件事本身是对的, 单独钉了一条 `test_R406_平滑趋势里三档一样是对的_不是旋钮坏了`, 免得下一个人当成 bug 去"修"。② `compute()` 那条用例只给了 25 根, 而它要求 ≥30, 三档一起返回空。③ 「默认档是中档」那条**第一版是绿的却测不出东西** —— 变异测试证明把默认改成粗档或细档照样过, 因为那份数据上三档算出来一样; 换成一份搜出来的、**三档两两都不同**的序列(粗/中/细分别认出 2/4/5 个回调)才真的拦得住。**最后撞上棘轮, 而棘轮这次抓的是它自己**: 我照着旁边那个选择器写了 `rounded-md` 和 `text-[10px]`, 「任意字号」当场红 —— **这是棘轮该干的事, 所以改代码不抬基线**, 顺手把旁边那个枢轴点选择器一起迁到规范档位(两个选择器紧挨着, 档位不一致看得出来)。但「裸圆角」那条怎么改都降不下来, 查下来是**正则数错了**: `\brounded\b` 把 `rounded-btn`(780 处)、`rounded-card`(243)、`rounded-input`(52)这些**规范想要的**语义 token 一起算成了越界, 2285 里有 1075 是误报。**真正要命的不是数字虚高, 而是把 `rounded-md` 迁成 `rounded-btn` 时这个数一动不动** —— 棘轮看不见它存在的意义所在的那种迁移, 「迁移让数字往下走」这个前提根本不成立。修正正则并重定基线 2285 → **945**(`rounded-full` 也不算越界: 药丸和圆点本来就该用它)。**守卫 +9 条, 12 个变异逐条确认会红**(含「假旋钮」那一条: 三档切来切去图上不变比没有这个按钮更坏, 用户会以为自己已经试过粗细了)。**用户点名的三条红线再查一遍, 全部未动**: 打分系统、作者内置(`strategy/` + `keltner*` + `pipeline`)、六态; 出场线模块也一并确认未动。 | 改 `backend/app/indicators/dinapoli.py`(`GRAINS` 三档 + `compute(pivot_k=)`)、`backend/app/api/stock_analysis.py`(一次算三档, 上攻段/回踩不按档重复)、`frontend/src/lib/api.ts`(`Fib2Grain` + 叠加层改成按档)、`frontend/src/components/stock-analysis/AnalysisKChart.tsx`(档位 state/选择器/选中档覆盖线与色带/开关计数跟档 + 两个选择器迁到规范档位);改 `backend/tests/test_design_spec.py`(裸圆角正则修正 + 重定基线)、`test_dinapoli_pivots.py`(+5 条)、`test_fib2_endpoint.py`(+4 条 + 换成三档两两不同的数据);**打分系统 / `strategy/` / `keltner*` / `pipeline` / 六态 / `position_exit` diff 全空**;后端 pytest **4440 passed / 10 skipped 退出码 0**, 前端 tsc 0 / eslint 0 error / build 退出码 0 / vitest 107 passed | 低(档位默认「中」= R405 的行为, 不切就一模一样; 棘轮基线的修正只影响守卫自己) | 可(撤 `GRAINS` + 前端选择器即可回到 R405 的单档) |

**三档在什么时候才分得出差别**(这条值得记住, 免得以后误判):

旋钮咬不咬得住, 取决于有没有**浅、且后来没被更低的点盖过**的回调。一段干净的台阶式上涨里, 浅回调要么不存在、要么被后面的更低点覆盖 —— **那时候三档给出同一批线是对的**, 不是旋钮坏了。

**留给用户的那一步没变**: 这个开发环境里没有真实行情, 图是把组件单独挂起来喂真算出来的 payload 验的。请在自己环境里打开几只熟悉的票, 切一下粗/中/细, 挑一个看着最顺的。觉得哪一档都不对, 告诉我往哪个方向调 `GRAINS` 里的数。

**下一轮可选**: 「未来」区(要扩 x 轴)、`SegmentedControl` 收口(R402 留的)、阶段三 3.3 逐页迁移。
