# R390 — 「AI 识别文本」的截断修复其实是坏的:写死的后缀表少了最常用的那一种

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R390 | **既有缺陷, 不是这轮安全审查改出来的**(`ai_json.py` 这轮此前一个字没动), 是查「粘长文没认出个股」时顺着用户「会不会是格式问题」的猜测挖出来的。`extract_json_object` 里那段「抢救被 max_tokens 掐断的输出」是**一张写死的后缀候选表** `("", "}", "]}", '"}', '"}]}', "}}")`, 而 docstring 声称「逐级补右括号/引号」—— **那句话没兑现**: 表里缺了 `}]}`, 也就是「内层对象没闭 + 数组没闭 + 外层没闭」, 正是 `{"mentions":[{…}]}` 这种**对象套数组套对象**被掐断时最常见的形状, 而「AI 识别文本」走的就是这个形状。拿一条两只票的完整输出逐个截断点实测: **140 个截断点里 121 个救不回来**, 救回来的全是尾巴上那几个。既有的 `test_ai_json_tolerant.py` 只钉了 `{"signal":…,"reason":"放量突破` 和 `{"picks":[{"symbol":…}` 两种**浅层**形状 —— 它们恰好都落在那张表里, 所以深一层的那种一直没人发现。改成**按结构补**: 扫一遍拿括号栈(`_scan`), 砍掉末尾那个残缺成员, 按栈反序补 `}` / `]`; 补不成就往回再砍一个重试。三处取舍是有意的: **①掐在字符串中间只丢不补** —— 补上引号能多救一条, 但救回来的是**残缺标识符**, 「中国船舶」掐成「中国船」照样是非空字符串, 而本模块有简称匹配, 半截名字很可能解析成**另一只票**(宁可少一条, 不可错一只); **②往回砍只砍最内层那一层**(`_innermost_cut`) —— 拿「整段最后一个逗号」会砍错层, 且 `{"a": [{"b": "x` 这种一个逗号都没有的只能整段认输, 现在改成「这层只剩这一个残缺成员就把这层清空」, 外层结构留住; **③括号配对本身就错(多一个右括号)一律返回 None** —— 那不是被掐断而是坏数据, 硬补会拼出一个**能解析但语义错误**的对象, 调用方分不出模型到底说没说过, 比认输更糟。修后同一组实测: **140 个截断点只剩最前面 19 个救不回来**(第一只票的名字本身都还没传完, 本来就无从救起)。**新增 13 条守卫**, 逐一变异确认会红: 退回写死的后缀表 → 6 红; 掐在字符串里改成补引号 → 36 红; 配对检查改成跳过 → 1 红; 只砍最内层改成砍整段最后一个逗号 → 1 红; 最内层没逗号时认输(不清空该层) → 2 红。**顺带删掉两处死代码**(末尾悬空 `,` / `:` 的单独处理、悬空反斜杠的截断): 变异后测试全绿, 查下来都被重试路径完全覆盖, 留着只会让人以为那里还有一道防线。**注意这不等于用户报的「没反应」已经定位** —— 那更像是前端 5 分钟超时里干等, 这条是同一条路上**确凿存在**的另一个缺陷。 | 改 `backend/app/services/ai_json.py`(`_scan` / `_innermost_cut` / `repair_truncated_json` 三个函数, `extract_json_object` 改为调用前者);新增 `backend/tests/test_ai_json_truncation.py`(13 条);**`backend/app/strategy/` diff 全空 —— 因子、阈值、META 一个没碰**;冻结的 `opportunity_score.py` / `score_ledger.py` 未动;**判定、六态、口径、指标色一个没动**;后端 pytest 4285 passed / 10 skipped 退出码 0 | 低(纯容错路径: 完整 JSON 与原先一样直接解析, 只有「原本解析失败返回 None」的输入才会有新结果) | 可 |

**边界说明**: 修复只在**成员粒度**上抢救, 不在字段粒度上造值。被掐断的那个成员可能只剩 `{"group":"军工"}` 这样既没 name 也没 code 的残条 —— `parse_mentions` 会跳过它, 不会变成一只票。这是有意的: **补全比丢弃危险**, 丢一条用户重试一次就有, 错一只是真金白银。
