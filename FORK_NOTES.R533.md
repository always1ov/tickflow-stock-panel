# R533 — 设置页: 竖向菜单换成页头分栏, 栏名去掉重复的「设置」

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|---|---|---|---|
| R533 | 用户:「设置页面也要整改」, 看过方案图后「确认」。① 页面里那列 144px 竖向菜单(带「收起菜单」, 手机上缩成一列图标, R374)换成页头右侧的分栏条, 与 Minds / 模拟盘 / 监控中心同一份 `PageTabs`; 内容区多出 164px, 手机上分栏横向滑动。`tf-settings-nav-collapsed` 那条本地偏好不再读。② 栏名去掉重复的「设置」: AI 设置 → AI、网络设置 → 网络、菜单设置 → 菜单、系统设置 → 系统; 名词表加三行(别名只收这三个)。界面上点名旧栏名的地方同步改: 任务卡「设置 → 网络」、侧栏 AI 档位空态「去「设置 → AI」里添加」、后端两处上下文窗口超限报错「在「设置 → AI」里调大」、菜单栏开篇块的小标签。③ 「菜单」挪到「扩展页面」旁边, 新顺序 数据源 · 实时监控 · AI · 菜单 · 扩展页面 · 网络 · 系统。④ 「系统」栏里重复的「系统设置 · 全局行为开关」大标题撤掉; 页头副标题「管理账户、数据刷新策略和高级功能配置」撤掉(这里没有账户)。⑤ 切栏瞬时, 撤掉每次切栏的淡入上移(高频操作)。七个面板的每张卡片 / 开关 / 输入框一个没动; `?tab=` 键名一个没改, 老书签与各处深链照旧。通知相关的设置仍分在「实时监控」(推送通知)与「系统」(弹窗 / 语音)两栏 —— 合并要拆两个大面板, 不在这一轮。守卫: 新增 test_settings_r533 | frontend/src/pages/Settings.tsx; frontend/src/pages/settings/System.tsx; frontend/src/pages/settings/MenuSettings.tsx; frontend/src/pages/settings/JobTimeoutCard.tsx(注释); frontend/src/components/data/ActiveJobCard.tsx; frontend/src/components/Layout.tsx; backend/app/services/ai_provider.py(报错文案); backend/app/custom/assistant/chat_service.py(报错文案); backend/tests/test_settings_r533.py(新); backend/tests/test_terminology.py | 低: Settings.tsx 外壳有上游底子, 同步时以「页头分栏」重放; 面板未动 | 可以: git revert 本提交 |
