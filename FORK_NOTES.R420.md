# R420 — 斐波那契二型「未来」区看得清: 格子铺满、交界线、均线加粗

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R420 | 用户: 「斐波那契二型的未来区域是灰色, k线也是灰色看不清了, 想办法解决」。出图查到两处: ① **只盖住了 3 格里的 2 格** —— R413 用 markArea 从「未来1」画到「未来3」, 类目轴上这两个值落在格子中间, 第一格左半、第三格右半都没盖; ② **一片灰** —— 暗色底色只有 5% 白、标签灰字、穿过它的现价虚线也是灰, 暗色二型均线又是浅粉(R421 换色前), 整块混在一起。改为自画系列 `lib/futureZone.ts`: 每格一个矩形(宽 = 一个 K 线间距)首尾相接铺满; 底色加深(暗 10% 白 / 亮 8%), **仍是中性色** —— 「还没发生」不该带涨跌或任何价位组的颜色含义; 「今天 / 未来」交界画一条竖虚线; 标签用主文字色、只写「未来」(默认缩放下 3 格约 23px, 原「未来(均线已知)」出图被切掉); 均线在未来那段另画一条 2.2px 的组色实线接在历史段后面。新系列放在 series 数组最后, 不打乱悬停联动的下标。 | 新增 `frontend/src/lib/futureZone.ts` + `.test.ts`;改 `components/stock-analysis/AnalysisKChart.tsx`(删 markArea 那段, 加两个系列);**后端、打分系统、`strategy/`、六态 diff 全空**;变异 3 个全杀;亮 / 暗两主题出图核对 | 低(只在二型开关打开且有未来值时出现) | 可(恢复 R413 的 markArea 那段、删两个新系列) |
