# R536 — 设置八个子页铺满内容区

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|---|---|---|---|
| R536 | 用户「设置里面的每个子页面都要铺满内容区域, 现在我看到的是, 会有右边一部分, 有的页面是空白的」。R535 把八栏统一成 1024px(max-w-5xl), 宽屏上右边空一大块。撤掉设置外壳的宽度上限, 八栏一起铺满内容区; 宽度仍只在外壳这一处定, 面板里不自带上限(test_settings_r535 的守卫同步改为钉「外壳不设上限」) | frontend/src/pages/Settings.tsx; backend/tests/test_settings_r535.py | 低: 只改一个容器类名 | 可以: git revert 本提交 |
