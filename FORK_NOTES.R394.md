# R394 — 手机上所有页面的页头都是塌的:19 条路由里 12 条标题压在汉堡底下

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R394 | 用户: 「我用手机看网页显示排版非常不正常, **所有页面都是**」, 附两张截图(策略页的工具栏中文竖着排成一列; 自选页的图标条压在汉堡上)。**先复现再改**: 拿 Chromium 起真实产物, 390×844 + `isMobile` 逐条路由量, 确认不是错觉 —— **19 条里 12 条**的页标题几何上压在悬浮汉堡上, 策略页的按钮量到 **x=391 而屏幕只有 390 宽**, 整条被根节点的 `overflow-hidden` 吃掉。**三件事叠在一起, 但根因只有一条**: **① `flex-wrap` 当初只写在 `PageShell` 传给 `PageHeader` 的 className 里** —— 而有 **10 个页面**(策略、自选、复盘、数据、财务、概念、行业、连板、系统工具、扩展维度)是**直接用 `PageHeader`** 的, 压根拿不到那份 className, 于是页头是一行不换的 flex: 工具栏要么被压成一个字宽, 要么整条溢出视口。修法是把 `flex-wrap` / `lg:flex-nowrap` 写进 `PageHeader` **自己的基础类**里 —— 换行是这个组件的性质, 不该由调用方记得传。**② 没有人给那个悬浮汉堡让位**: `Layout` 在 <768px 把菜单按钮做成 `fixed left-3 top-3`(34×34, 右缘到 x=46), 而每一页的页头都从 x=12/16 起排, 标题正好落在按钮底下 —— 24 个页面同一个症状。给标题块加 `pl-11 md:pl-0`。**让位写在标题块这一层而不是 `header` 上**: `className` 是调用方传的, `PageShell` 那份里写着 `px-3 lg:px-5`, 经 `twMerge` 合并后会把基础类里的左内边距整个吃掉 —— **加了不生效, 而且不报错**; **也不用占位元素**, 占位只顶开第一行, 副标题换行之后那几行照样贴回左边缘钻到按钮底下。**③ 工具栏一换行, 标题就飘到半空**(实测那一页页头 197px 高, 标题停在正中像块浮标): 窄屏 `items-start`、宽屏仍 `items-center`。**外加 10 个页面各自那一行**: 它们往 `right` 里只塞**一个** `<div className="flex items-center gap-2">`, 而 `PageHeader` 的 wrap 只管直接子节点 —— 那一个 div 就是唯一的直接子节点, 里面十几个按钮全被压扁。逐个补 `flex-wrap`。**改前/改后同一份扫描: 12/19 → 0/19**, 横向溢出全程为 0; 桌面 1440 的页头仍是 60px 一行、标题仍在 x=240, **一个像素没动**。**新增 4 条守卫 + 修好一条旧的**: 旧的那条(`test_R379_页头有呼吸感而且只有一个产地`)钉的是整串 `"flex min-w-0 items-center gap-2.5"` 字面量, 而它 docstring 里在乎的只有 `items-center`(药丸徽标 baseline 会坐歪)——**这次对齐方式一个字没动、只多加了几个响应式类, 它就红了**: 又一次「钉名字不钉性质」。改成只问标题行是不是 `items-center`, 并明确禁掉它真正怕的 `items-baseline`。新的四条: 页头基础类里必须有换行(且宽屏还原)、**让出的位置要算得过汉堡的右缘**(两个数分别写在两个文件里, 各改各的必然对不上, 所以把 `left-*`/`p-*`/图标尺寸/`px-*`/`pl-*` 真算一遍)、两边用同一个断点(768)、`right` 里的工具栏一律要能换行。**9 处变异逐一确认会红。** | 改 `frontend/src/components/PageHeader.tsx`(基础类加换行 + 窄屏顶对齐 + 标题块让位, 并把攒了三轮的注释收成一段);10 个页面的页头工具栏各加 `flex-wrap`(`ExtDimensionAnalysis` / `ConceptAnalysis` / `Data` / `Dev` / `Financials` / `IndustryAnalysis` / `LimitUpLadder` / `Review` / `Screener` / `Watchlist`);改 `backend/tests/test_theme_palette.py`(旧守卫改钉性质);新增 `backend/tests/test_mobile_header_layout.py`(4 条);**后端 `app/` 一个字没动**;**`backend/app/strategy/` diff 全空**;**判定、六态、口径、指标色一个没动**;后端 pytest 4297 passed / 10 skipped 退出码 0, 前端 tsc 0 / eslint 0 error / build 退出码 0 / vitest 47 passed | 低(纯响应式版面: 桌面逐项量过没有变化; 窄屏上页头会比原来高几行 —— 那是把原本压扁/溢出的东西摊开, 不是新增内容) | 可 |

**没做、也不该顺手做的**: 用户说的是「排版不正常」, 这一轮只解决了**页头**那一层 —— 它是唯一一处 24 个页面共用、且能一处修完的。各页**正文内部**在窄屏下还有没有别的挤压(比如宽表格只能横向滚动), 没有逐页量过, 也不该在同一次改动里顺手动 —— 那是一页一页的版面决定, 不是一个共用组件的缺陷。要继续的话按页提。

**验证方式记一笔**: 这一条是**真跑出来的**, 不是读代码推的 —— Chromium 起产物、390×844、逐条路由量 `getBoundingClientRect()` 与 `scrollWidth`, 改前改后各扫一遍。页面几何这种事读源码很容易读岔(我这一轮就先后猜错过两次: 以为是 `overflow` 裁切、以为 `PageShell` 那份 className 人人都有), 量一次就定了。
