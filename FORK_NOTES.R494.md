# R494 — 个股日K副本兼容盘中实时(实时落盘撞上发布标记时先等几毫秒)

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|---|---|---|---|
| R494 | 用户:「考虑如何兼容盘中实时情况」。先把盘中链路逐段核对: 付费档每轮行情(默认 6 秒, 最快 1 秒)经 flush_live_enriched_asset 覆写「今天」分区, 带发布标记, 但标记只在换文件与提交那几毫秒处于发布中; 盘后管道 run_pipeline 重算历史是整轮一次发布(commit 在全部分区写完之后), 一挂几分钟。今天那根的数有三层: 磁盘最近一轮落盘、仓库 get_daily 快路径用最新行情缓存覆盖(原代码未动)、两张副图与 K 线接口再叠 _maybe_inject_live_candle; 两张副图 10 分钟内存缓存里的今天那根可能旧, 但叠实时蜡烛时按日期覆盖, 且磁盘有今天行时同一行情源的内存必有更新的值, 不会露出旧值; 前端弹窗只在打开、刷新按钮与 refresh_live 的 1.5 秒/4 秒补取时重取, 不随每轮行情重取。发现的问题: R493 的发布守卫一见标记未就绪就退回扫全部日文件, 盘中实时每轮都会短暂触发。模拟(作者真实落盘路径每约 60 毫秒覆写今天分区, 6 个线程并发读 6 秒)实测 476 次读里 52 次退回原路(按真实 6 秒节奏约千分之一, 每次白花一整次全扫)。改法: 见到发布中先等最多 0.2 秒, 就绪了照常用副本, 等满还未就绪才当作管道在跑退回原路; 删掉只对股票和 ETF 查标记的集合(指数没有标记文件, 读到即视为就绪, 变异下无用例能区分)。改后同一模拟 511 次读全部走副本、0 次退回, 历史部分与今天那根全部正确(今天那根总是某一次写入的值), 40 只票的副本都建下来; 294 项对照与改前逐字一致。新增 2 条用例(实时撞标记照常用副本、管道整轮发布退回原路且不久等), 变异: 不等待、死等两者均红 | backend/app/services/symbol_daily_store.py; backend/tests/test_symbol_daily_store.py | 低(只动 fork 自有模块) | 可以: git revert 本提交(回到见标记即退回原路) |
