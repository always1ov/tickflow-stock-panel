# R427 — 复盘弹窗并进个股弹窗: 点名字、点「走势/位置」进的是同一个弹窗

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R427 | 用户发来两张截图(个股弹窗: 日K / 分时 / 关键价位; 复盘弹窗: 趋势状态 / 通道档位): 「两个图片是两个弹窗, 融合成一个, 以后点个股名称还是走势位置按钮都跳转融合后的弹窗」。做法: **复盘成了个股弹窗的第四页「复盘」**(日K / 分时 / 关键价位 / 复盘), 页内保留自己的「趋势状态 / 通道档位」与「60 / 120 / 250 日」; 原 `StockReviewDialog` 去掉外框、遮罩、票名、关闭, 退成只画内容的 `StockReviewPanel`(票名与切股、自选、关闭都归个股弹窗, 不再各写一遍)。个股弹窗加 `initialView` / `reviewTab` 两个入参: 决策台点名字仍落在「关键价位」(R185 的默认), 点「走势/位置」落在「复盘 · 趋势状态」; 页面上只挂一个个股弹窗, 决策台自己那个复盘弹窗删掉。模拟盘点动作原来开的也是复盘弹窗, 一并换成个股弹窗的复盘页(它另有的独立「关键价位」弹窗用户没提, 未动; R364 那条「两个入口开两样东西、各存各的 state」照旧成立)。**出图抓到一个自己引入的 bug**: 第一版另加了「symbolProp 一变就重置页面」的 effect, 与原有「首次打开重置到关键价位」的那条打架, 复盘页被盖回关键价位; 而且它会在父级方向键切股时把当前页也重置, 破坏 R185 那条注释要防的事 —— 删掉它, 只把原有那一处的重置目标改成入口指定的页。 | 改 `frontend/src/components/StockPreviewDialog.tsx`、`components/stock-analysis/StockReviewDialog.tsx`(`StockReviewDialog` → `StockReviewPanel`, 文件名不变: 两张表与一批守卫认这个文件)、`WatchlistDecisionBoard.tsx`、`pages/StockAnalysis.tsx`、`pages/FlipPaper.tsx`;守卫: `backend/tests/test_board_columns.py` +1 条(复盘不再有自己的弹窗外壳 / 个股弹窗有复盘页且能指定落页 / 决策台两个入口同一个弹窗 / 全站没有地方再开旧复盘弹窗)、`test_flip_fusion.py`(R364 断言换组件名);**后端业务代码、打分系统、`strategy/`、六态 diff 全空**;变异 3 个全杀;后端 4515 passed、vitest 155、tsc / eslint / build 0;拦截接口喂测试夹具算出的复盘数据, 出图核对了「复盘 · 趋势状态」直达、「通道档位」切换 | 中(动了全站共用的个股弹窗: 所有入口都多了「复盘」一页) | 可(恢复 `StockReviewDialog` 外壳与两处调用, 去掉 `review` 视图) |
