# R380 — 换皮肤第二层:版式(页头放开 + 开篇块收成一个产地)

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R380 | **R379 换的是色, 这一轮换的是版式**(用户: 「按照最优解处理」, 回的是我给的 A/B/C 三档)。**最优解不是把 WavMint 照搬过来** —— 那套是给「一屏几张卡」的工具站做的(48px 控件、96px 区块间距), 照搬会把看盘要的信息密度毁掉, 而密度正是这个工具的用处。所以取的是 **A+B**: 外围页给足留白, 看盘页**只动页头**, 表格一个像素不碰。**关键是找对缝, 不是逐页改。** 先量了一遍才动手: `PageShell`(R60 立的统一骨架)实际只有 2 个页面在用, 走它铺不开; `rounded-card` 全仓 244 处、内边距 p-3/p-4/p-5/p-6/p-8 什么都有, 没有单一的缝 —— **逐页改既碰不全, 又必然随时间漂成 24 种样子**。真正的缝只有三个, 这一轮就动这三个: **① `PageHeader`(一处改, 24 页受益)**: 52px 高、上下 8px、一条实边框 —— 那是工具栏不是页头, 也是「像后台系统」最大的那个口音。改成 60px / 上下 12px / 标题 18→20px / 底边框降到 `/60`(页底本来就比页头浅一档, 分界不需要实线来画)。一屏只付出 8px。**中途自己改回一处**: 标题行顺手写成了 `items-baseline`, 但 `titleExtra` 是药丸徽标, baseline 会让它坐歪 —— 留回 `items-center`, 并把这条写进守卫。**② 新增 `SectionIntro`**(小号大写标签 + 大标题 + 一段说明): 这个版式本来就在仓库里, 只是**手搓了三份**, 字号、间距、标签颜色各写各的(一处 `text-accent/80`、另一处 `text-cyan-400/80` —— 后者还是照深色底调的, 白卡片上偏淡)。**我自己只记得两处, 第三处是守卫一开就抓出来的** —— 这正是「同一个位置两种写法, 谁也不会记得同时改两处, 而且不报错」。**③ 设置区容器**(一处改, 七个面板受益): 留白给到 WavMint 那一档(`px-3 pb-4 pt-3` → `px-4 pb-8 pt-5 lg:px-6`)。**没做的两件, 说明白**: 244 处卡片内边距**没统一** —— 那需要逐个判断哪些是密集表格哪些是说明卡, 没有安全的批量改法, 留着; 其余五个设置面板**没有硬加开篇块** —— 那要凭空写五段文案, 是在编内容不是在做版式。**守卫钉的最要紧一条不是「改了什么」而是「没改什么」**: 新加的 `test_R379_看盘页的密度一个像素没动` 钉住自选表格的紧凑字号与模拟盘信号行的行高/栅格 —— 下一轮谁再想「顺手放开一点」, 这条会拦住。**实机验过**: 起 Chromium 看了设置/扩展页面/模拟盘三页, 并在 375px 下确认两页都**没有横向溢出**(`scrollWidth === 375`)。**7 处变异逐一确认会红。** | 新增 `components/SectionIntro.tsx`;改 `components/PageHeader.tsx`、`pages/Settings.tsx`(容器留白)、`pages/settings/ExtPages.tsx`(两处手搓收编)、`pages/settings/MenuSettings.tsx`(一处手搓收编);`tests/test_theme_palette.py` +3 条;**`backend/app` diff 全空**;**指标色、表格密度、暗色一套全没碰**;后端 pytest **4221 passed / 10 skipped 退出码 0**, 前端 tsc 0 / eslint 0 error / vitest 47 条全过 / build 通过 | 低(只动两个共用件与一个容器; 判定、打分、六态、模拟盘、指标色、表格密度一个字节没动) | 可(`git reset --hard origin/backup/pre-visual-refresh` 连 R379 一起回, 或单独 revert 本次提交只回版式) |
