# R492 — 仓库的单票日 K 读取接上个股副本(关键价位、复盘、六态等一起提速)

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|---|---|---|---|
| R492 | 用户确认:「做」(此前明确只关心「作者的内置指标、我的评分系统、六态有没有被改变」)。实测个股弹窗 8 个请求, R491 后最慢的是关键价位 ~420 ms、复盘 ~500 ms、六态 ~165 ms, 且再开也不变快; 拆开看 80~90% 都花在仓库的单票读取把全部日文件逐个打开挑出这一只(复盘还读两遍: 先试列下推、缺指标列再读全量)。改法: 作者 repository.py 的 _scan_daily_symbol / _scan_index_daily_symbol / _scan_etf_daily_symbol 各加 4 行, 先问 symbol_daily_store.scan_symbol, 返回 None 才照原来扫分区, 原代码一行未改。副本从 R491 的 6 列扩成存储 schema 的全部 15 列, 入口与这三个函数一一对应(同样的代码、起、止、列进, 同样的行、列、列序、类型出); 只要最近 10 天以内的请求不碰副本; 首尾抽查从 6 个数改成整行; 旧 6 列副本按格式版本与列检查自动重建; 分区目录里出现非 date= 子目录或散落 parquet 就不用副本(原来的 **/*.parquet 会把它们也读进来); 每种资产最多留 800 份、超出删最早建的(模拟库每份约 50 KB, 自定义策略逐只扫全市场也撑不爆硬盘)。R491 在 ohlcv_history 里照抄的那段最新一根覆盖逻辑删掉, 改回直接用仓库(同一个读数只留一个产地)。指标计算、把握分、六态的代码都没动, 它们拿到的表逐位不变: 模拟库(1000 交易日 × 5000 只股票 + 20 指数 + 200 ETF)上 294 项对照(7 只票 × 6 种区间 × 6 种列组合的仓库读取 + 弹窗 6 个端点完整输出)在旧副本升级、新建、读副本四轮都与改前逐字一致。耗时(同一台机器, 串行): 已开过的票 关键价位 ~420→~80 ms、复盘 ~500→~110 ms、六态 ~165→~28 ms、弹窗全部请求合计 ~1200→~360 ms; 从没开过的票第一个请求要建副本(~450~580 ms, 与原来扫一遍相当), 同时来的其余请求等它建完直接读。测试 test_symbol_daily_store.py 重写为 67 条(主线是 6 种区间 × 6 种列组合 × 4 只票逐位对照原路, 另有仓库 get_daily 快路径含最新一根覆盖、全量指标重算、指数与 ETF、三种资产目录同代码不串、R490 历史缓存); 变异 26 个 25 红, 活下来的是格式版本号检查: 眼下旧副本都会被列检查先拦下, 版本号留给以后列相同而含义变了的升级 | backend/app/tickflow/repository.py; backend/app/services/symbol_daily_store.py; backend/app/services/ohlcv_history.py; backend/tests/test_symbol_daily_store.py | 中(动了作者 repository.py 的三个单票读取函数, 各在开头加 4 行, 上游改这三个函数时会冲突, 保留原函数体、把 4 行留在开头即可) | 可以: git revert 本提交(副本退回 R491 的只供两张副图); 或只删 repository.py 那三处 4 行, 仓库即回到原读法 |
