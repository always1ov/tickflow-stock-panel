# R498 — 转折页重新排版(今天优先, 不动数据只改表达)

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|---|---|---|---|
| R498 | 用户:「先这样保守的改, 每个页面里面的东西重新排版。不动数据, 只考虑怎么改表达。我们一页一页的改。先分析这一页有什么东西, 然后生成一张图片, 确定了再动手改」→ 从第一页(转折模拟盘)开始, 按 prototype 技能在原路由上用 ?variant= 做了三种结构不同的排法(今天优先 / 驾驶舱 / 标签页, 一次性代码留在本地 prototype/flip-paper-layout 分支, 未推送), 截图给用户看, 用户:「按照页面来设计一个页面来搞定」选单页的今天优先。改法(数据、取数、每块里的内容与判据一个没动): 首屏让给信号 —— 成绩与参数从筛选卡插槽上搬下来成独立一张「这套打法的成绩」卡, 筛选条后面紧跟今天该挂什么单; 其下持仓与成绩并排(1.25:1), 再下流水与有信号但没做成并排(没有没做成时流水独占整行), 这套规则默认收起并记住展开状态、收起时不挂载; 成绩卡半幅时六格改 2 列(1800 起 3 列), 全幅时仍一行六格(实测半幅一行六格日期被截成两行); 逐月收益放不下时一挂上就滚到最右, 先露出最近的月份(半幅时被藏起来的恰好是最近几个月); 手机上持仓与流水改成两行卡片(原表格向右截断, 浮盈、成交价、金额整列看不到), 流水手机默认 10 笔、宽屏仍 30 笔; 区块标题不再被说明挤成两行; 手上这些那一行在手机上收掉后半句说明, 右边两个数不再挤成竖排; 板块按钮加换行(手机上北交所被挤出屏幕); 流水的买入/清仓徽标抽成一处(表格与卡片共用)。R358/R359 当年用户点名的成绩并进筛选卡被这次用户的选择推翻, 其背后的性质全部保留并改了钉法: 参数条紧贴成绩上方、成绩卡无条件渲染(打分那层挂了或跑不动时参数条仍在)、净值图收起时不挂载; R329 被 R358 缩掉的那一半(今天要动手的排在回测结论前面)恢复并重新钉住。改了 test_flip_fusion(R343/R358/R359/R381 的钉法)、test_skeletons、test_flip_today 各一处, 新增 test_flip_layout_r498.py 12 条; 变异 14 个全红; 全量验证通过, 设计规范裸圆角计数 750 → 749 | frontend/src/pages/FlipPaper.tsx; frontend/src/components/today/TodayControls.tsx; frontend/src/lib/storage.ts; backend/tests/test_flip_layout_r498.py; backend/tests/test_flip_fusion.py; backend/tests/test_skeletons.py; backend/tests/test_flip_today.py | 低(FlipPaper.tsx 是 fork 自有页面; TodayControls 只加一个 flex-wrap) | 可以: git revert 本提交(回到成绩在顶部筛选卡里的版面) |
