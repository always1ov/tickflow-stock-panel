# R496 — 日K衍生缓存登记表(改写日K后统一通知一处)

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|---|---|---|---|
| R496 | 用户:「做第2条」。从带指标日K算出来、存下来的有四份: 作者的带指标日K内存缓存、作者的回测矩阵缓存、fork 的个股日K副本、fork 的两张副图单票历史内存缓存, 作废规则原来散在四处。新增 services/derived_caches.py: REGISTRY 逐份写明放在哪、什么时候作废; 改写日K的地方只调 enriched_rewritten(data_dir, 资产类型, 点名的票或 None=整类), 由登记表挨个通知, 一份失败只记日志不连累别的也不抛给调用方。作者的两份经核对都自带判据(内存缓存在盘后管道收尾 finally 里 refresh_cache 且历史缓存每次读按发布代次校验; 回测矩阵缓存键带发布代次, 无代次时带分区指纹), 登记在表里只为一处看全, 不接通知、不改作者逻辑。「原地改写日K历史」的调用点全部接上: 盘后管道股票除权重算与全量重建、ETF 除权(原 R493 两处直接作废改走登记表), 以及原来没接的往前扩展历史(extend_history)与手动全量重算(/api/kline/rebuild_enriched)。顺带补上登记时查出的一处缺口: 两张副图的内存缓存原来只靠「最新交易日变了 / 10 分钟」, 除权重算而当天没有新交易日时会用旧复权价最多 10 分钟 —— 新增 ohlcv_history.invalidate 点名作废。每天新增交易日、盘中覆写今天、完整性修复先删后补不算原地改写(说明写在模块里)。守卫: 调了 run_pipeline 的文件必须也调 enriched_rewritten(按 AST 调用点扫, 新增调用点会被点名)、ETF 除权处接了通知、除登记表外无人直接调两个作废函数。新增 test_derived_caches.py 10 条; 变异 11 个全红(删掉的一处空名单提前返回与下游行为重复, 变异下无用例能区分); 294 项对照与改前逐字一致 | backend/app/services/derived_caches.py; backend/app/services/ohlcv_history.py; backend/app/jobs/daily_pipeline.py; backend/app/services/extend_history.py; backend/app/api/kline.py; backend/tests/test_derived_caches.py; backend/tests/test_symbol_daily_store.py | 低~中(作者的 daily_pipeline.py 两处由直接作废改为调登记表、extend_history.py 与 kline.py 各加三行; 上游改这几处时按 test_R496_调了run_pipeline的文件都接了通知 补回) | 可以: git revert 本提交(回到 R495: 副本仍由管道直接作废, 两张副图内存缓存回到只靠 10 分钟兜底) |
