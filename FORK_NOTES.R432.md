# R432 — 图表与价位挪到「结论」正下方; 关键价位开关回到图上方(方形按钮)

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R432 | 用户: 「图表与价位这部分提取到结论后面, 而且关键价位还是按照以前那样显示吧, 在右侧很不方便, 可以保留方形按钮。后面再梳理一遍统一删除」。(1) 位置: 弹窗头部(含「结论」那一行)以下整块一起滚, 顺序改成 头部 → 图表与价位 → 复盘 → 旧顶栏 / 切换条 / 信息条 → 旧内容; 原来新块夹在钉住的旧顶栏下面。复盘块跟着一起提上来, 新的几块保持连在一起。代价: 旧顶栏不再钉在顶上, 它的关闭按钮要滚下去才看得到(Esc / 点弹窗外照常能关), 统一删旧的时候一并处理。(2) 关键价位: R430 右边那张列表撤掉(`LevelSideList` 删除), 开关回到以前那排小开关的位置(图的正上方), 样子保留方形按钮 —— 新组件 `LevelToolbar`: 色点 + 名称 + 条数、点开的反相、没有价位的灰掉、末尾「全部清除」; 枢轴点「档位」、二型「粗细 / 上攻推算位 / 回测这三档」照以前, 那一类开着时接在这排后面。图于是占满整张卡片的宽度。状态仍由弹窗持有, 切到日 K 再切回来开着的那几类还在(出图时实测)。计数与小开关文案仍只有 `levelControls.ts` 一个产地; 右侧列表专用的「只开不关」(`turnOn`)随列表一起删掉。 | 新增 `frontend/src/components/stock-analysis/LevelToolbar.tsx`; 删除 `frontend/src/components/stock-preview/LevelSideList.tsx`; 改 `frontend/src/components/StockPreviewDialog.tsx`(滚动区与顺序)、`stock-analysis/AnalysisKChart.tsx`(受控时画方形按钮那排)、`stock-analysis/levelControls.ts`(去掉 turnOn)、`stock-preview/ChartLevelsSection.tsx`(去掉右栏); 重写 `backend/tests/test_chart_levels_section.py`(8 条), 改 `test_fib2_readability.py`(改指 LevelToolbar); **后端业务代码、打分系统、`strategy/`、六态 diff 全空**; 变异 12 个全杀(另有 1 个变异写错了只改到注释, 换成真的挪位置后也被拦下); 后端 4546 passed、vitest 155、tsc / eslint / build 0; 亮暗两套出图核对顺序、方形按钮在图上方、小开关展开、切视图后开关保留 | 低 | 可(撤回这一提交即回到 R431: 右侧列表 + 新块在旧顶栏之下) |
