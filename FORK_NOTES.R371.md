# R371 — 夜间模式主题变量修复

本条是 `FORK_NOTES.md` 的独立补充记录，尚未并入总台账。当前连接只能整文件替换，原总台账约 0.8 MB；为避免用不完整内容覆盖历史，本次保留原文件不变。

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R371 | 修复夜间模式继承浅色背景：补齐 `--sidebar`、`--border-input`、`--accent-hover`、`--accent-soft` 的深色定义，复用既有深色色阶；增加变量完整性、深色表面和侧栏文字对比度三项回归检查。 | `frontend/src/index.css`；`backend/tests/test_dark_theme_tokens.py`；本记录 | 低 | 回退本次独立提交 |

## 原因与修复

R368 只在 `:root` 定义了四个新增主题变量。切换为 `html.dark` 时，侧栏等背景继续使用亮色值，而文字已经变为深色主题的浅色文字，导致截图中的白底白字。

在现有 `html.dark` 中补齐：

```css
--sidebar: var(--surface);
--border-input: var(--border);
--accent-hover: var(--t-blue-400);
--accent-soft: var(--elevated);
```

不修改亮色配色、其他既有深色色值、字体、布局、主题切换逻辑、动效、接口、依赖、策略、评分或业务数据。

## 提交前实际验证

- 用此前从基线 `7cdca08ca420861cb8501276311d958c83f57380` 提取的 CSS 色板复现：同一组回归测试在修复前三项失败、修复后三项通过。
- Chromium 144，1100×640 和 390×844：亮色前后结果不变，深色→亮色→深色往返一致。
- CSS 最小复现中的侧栏文字对比度：主要文字 1.002→16.974，次要文字 1.658→10.213，辅助文字 3.107→5.450。
- 上述浏览器验证是 CSS 最小复现，不是完整 React 应用或线上截图；没有将它表述为线上验收。

完整前端构建交由现有 Docker Actions 执行；发布结果以该提交对应的工作流日志为准。工作流已配置在镜像发布后通知 Coolify 部署，通知成功不等于线上健康检查通过。
