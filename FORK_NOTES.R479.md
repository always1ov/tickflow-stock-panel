# R479 — 个股弹窗: 删掉最底下的旧内容

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|---|---|---|---|
| R479 | 用户框出弹窗最底下那一整块:「我是想删除掉框出来的这部分」(之前要求「记得先检查确认一次」, 已先列清单、R478 先把加监控搬到新头部)。删掉: 旧顶栏(代码 / 日K·分时·关键价位·复盘四个页签 / 区间选择 / 自选 / 加监控 / AI / 刷新 / 放大 / 关闭 / 上一只下一只)、切换条(最近查看 + 搜索)、监控触发信息条、异动信息条、旧的日K·分时·关键价位图与旧复盘页, 以及只给它们用的状态与辅助(view / maximized / 内部换股 override / 最近查看记录 / 异动查询 / PRESETS / boardTag / AB_STATUS_META 等)。StockPreviewDialog 906 → 456 行。保留: 方向键切股与首尾循环提示(useListNav / NavWrapToast)、加监控的规则编辑器弹层、点位提醒弹窗、二开公开插槽 stock-preview.footer(显隐改跟新「图表与价位」的 chartView, 插槽名与 context 形状不变)。刷新只认 chartView。Props 去掉 triggerInfo / reviewTab(不再有显示), 五个调用方(Dashboard / Monitor / Review / StockAnalysis / FlipPaper)同步去掉。initialView="review"(决策台「走势 / 位置」、模拟盘复盘入口)原来落在旧复盘页, 改成打开即定位到新「复盘」块: 上面内容异步长高时持续对准, 用户一动即放手、最多 3 秒, 不做滚动动画。StockReviewDialog.tsx 不删(新复盘块在用它的常量与函数)。守卫: 九条原先钉「新旧并存」的改成钉删完后的实情(分时信息栏属性写死、AI 入口在新头部、复盘入口定位), 新增 test_R479_旧顶栏与旧内容删干净了(十样旧东西回来即红、四样该留的不在即红); 棘轮 裸圆角 764→750、任意字号 64→42、硬编码色 769→756, StockPreviewDialog 进 MIGRATED | frontend/src/components/StockPreviewDialog.tsx; frontend/src/pages/{Dashboard,Monitor,Review,StockAnalysis,FlipPaper}.tsx; backend/tests/{test_ai_entry_in_dialog,test_board_columns,test_chart_levels_section,test_flip_fusion,test_preview_hero,test_review_section,test_design_spec,test_ui_hierarchy}.py | 中: 上游改 StockPreviewDialog 旧顶栏部分会冲突 —— 那部分本 fork 已删, 取本处 | 可以: git revert 本提交(旧内容整段回来) |
