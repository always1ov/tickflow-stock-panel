# R383 — 模拟盘重新排版(第二次):没内容的列不该占宽

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R383 | **用户第二次说同一件事**: 「不是叫你重新排版模拟盘页面吗, 怎么一点都变化」。他是对的 —— **R381 改的三处在他那一屏上一处都看不见**: 六态列加宽只对「盘中越线」那档有效而他满屏是「已转折」, 「现在拿着/成交流水」并排与规则改多列**全在折叠线以下**。**根因是我拿自己造的假数据改的, 而那份假数据恰好把真问题遮住了** —— 我给每行都填了 `flip_price` 和打分数据, 于是右边两格看起来都有内容。他的实机是 **32 行全「已转折」、全「没进候选池」、`flip_price` 为 null**。照着他的数据形状重新量了一遍才看见: **行宽 1688, 走势那一格独占 1068 而且是空的(63%)**, 触发价那格也空, 而左边内容 620px 就排完了; 行高还被 `min-h-[3.5rem]` 撑着 56px, 32 行就是 1792px 的滚动。**改法是让栅格按「这一屏到底有没有那些内容」选**, 不是写死一套。R356 定下定宽网格的立论是「行与行天然对齐」—— 那条立论要的是**行与行之间**对齐, **不是某一列必须占住某个绝对宽度**; 所以整屏都没有的那一列, 宽度给 0。形状(`rank`/`trend`)**从全部行算一次, 三段共用** —— 分段各算各的话,「要动手」那段和「只是盯着」那段会挑到不同的栅格, 上下两段列对不齐**而且不报错**。**行高同理改成整屏统一决定**: 撑行高的理由只在真有名次时存在(名次那一格三行高, 要把没名次的行垫到同样高度), 整屏一个名次都没有时每行只有两行字, 再垫就是每行白送 26px。**注意这不是放开密度而是相反** —— 该紧的时候更紧了。**行窄下来之后排两列**: 一行 620px 排完而宽屏有 1600+, 32 行排两列滚动直接砍一半; 走势有内容时保持单列(那时行本来就要吃满宽度), 断点与 R381 那处并排同一个。两列时分隔线得画在格子上 —— `divide-y` 在两列栅格里只会横着画, 竖缝没人画。**量出来的结果**: 行宽 1688 → **826**, 行高 56 → **40**, 信号区可视高度 1792 → **约 640(-64%)**。**两条路都真跑过**: 有打分数据时回到单列(8 行 8 个不同的行顶、行宽 1688、行高 56 —— 与 R356 那版逐项一致), 没打分数据时两列。**动了四条既有守卫, 每条都写清为什么**: `_grid_class` 的切片锚(栅格串搬进了 `ROW_GRID`, 且**行容器自己也是 `cn(...)`**, 只认 `<div className={cn(` 会切到外层那个 —— 改成认「cn 的第一个实参就是 `'grid …'`」); `test_R350`/`test_R360` 的栅格计数(1 套窄屏 + 2 套宽屏 = 3); **`test_R356_行高定死`** —— 它在我改完之后**仍然是绿的, 但描述的行为已经不成立**(行高变成条件的了), 这正是最危险的那种绿, 所以把判据从「写死 3.5rem」换成「那一档还在, 且由 `shape` 统一决定, 不是每行各自算」; R380 那条密度守卫同理。另有两条 `test_flip_today` 的锚随 map 形状(多包了一层格子 div)跟上。**7 处变异逐一确认会红。** | 改 `pages/FlipPaper.tsx`(`ROW_GRID` 两套 + `RowShape` + 两列列表 + 三段共用);`tests/test_flip_fusion.py` +4 条并更新 4 条, `tests/test_theme_palette.py` 更新 1 条, `tests/test_flip_today.py` 更新 2 条;**后端 `app/` diff 全空**;**判定、六态、打分、口径、指标色、内容一个字节没动**;后端 pytest **4232 passed / 10 skipped 退出码 0**, 前端 tsc 0 / eslint 0 error / vitest 47 条全过 / build 通过 | 中(信号行的栅格从写死变成按内容挑, 且宽屏下可能排两列 —— 但每一格的内容、次序、判定没动, 窄屏那套栅格也没碰) | 可 |
