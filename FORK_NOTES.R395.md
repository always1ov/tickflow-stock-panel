# R395 — 手机端第二轮:宽表格钉住第一列,并把量出来的挤压逐个修掉

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R395 | 承 R394(那一轮修的是**页头**)。用户: 「全部页面都排版好手机端页面, 做好三端自适应」。**这一轮先把测量环境建起来**: 真起 FastAPI + vite 代理, 跑通首次向导, 灌 28 只自选与 7 个分组, 再用 Chromium 按**三档宽度**(手机 390 / 平板 834 / 桌面 1440)逐条路由量 —— 量四件事: 横向溢出、元素伸出视口、中文被压成竖排、可点目标过小。**没有数据的空页面量不出东西**(第一轮扫出来 0 个问题, 正是因为全部路由都被重定向到了首次向导), 这一步值得。**表格怎么排是用户定的**: 我给了四个方案(首列钉住 / 窄屏只显示优先列 / 换成卡片 / 前两项都要), 用户选「**首列钉住 + 横向滑**」—— 表格形式不变、**存的列配置一列不动**、桌面零影响。范围也是用户定的: 先做常用的几页。**① 首列钉住**: 自选与策略两页共用 `StockDataTable` 骨架, 加一个 `pinFirstColumn` 开关(默认**关**, 共用骨架不替没表态的调用方做决定); 决策台是手写 th/td, 单独钉。实测自选页 390px 宽 `scrollWidth 990 / clientWidth 350` —— 只露得出两列半, 往右滑之后满屏数字对不上是哪一只票; 钉住之后「代码/名称」停在 x=20 不动, 底色不透明。**只在窄屏钉**(`lg:static`), 桌面实测 `position: static` + 背景透明, 与改动前逐像素相同。**不透明底色写进骨架而不是交给调用方** —— 它是"钉住"能成立的前提(要盖住从底下滑过去的内容), 漏了就是两层字叠在一起**而且不报错**; 调用方只能往上叠色, 叠不掉这层兜底。**两处知情取舍**: 策略表的选中行 `bg-accent/10` 与决策台的 `bg-accent/25`/`/[0.10]` 都是半透明的, 同一个元素上给两个底色会被 `twMerge` 合掉一个, **叠不起来**, 所以窄屏上那一格只有页面底色 —— 决策台那边当前行左边那道靛蓝边还在, 它本来就是比淡色底更强的信号。**② 量出来的挤压逐个修**: 回测页「环境过滤」那一行(图标 + 标题 + 一句长说明 + 右侧输入框, 不换行 → 「最低分」被挤成 19×48 的一竖列)、决策台最后一格「未分析 + 两个图标按钮」(12×52)。**③ 可点目标**: 决策台三个筛选开关实高 21px, 用仓库**已有的** `.tap-target`(`::before` 把可点范围撑到 28px)—— 实测视觉仍是 25px、可点 28px, **密度一个像素没动**: 这一页的密度是刻意的, 不能为了好点就把控件撑大。**过程中踩了一次**: 给三元表达式的分支加注释时用了 `{/* */}`, 那里不是 JSX 子节点位置, 等于两个相邻表达式, 当场把整个文件编译坏了 —— `tsc` 逮住了, 注释里记了一笔。**新增 6 条守卫, 7 处变异逐一确认会红**, 钉的都是「破了会静默变难看」的那种: 钉住的格子必须不透明、只在窄屏钉宽屏要还原、表头表体要么都钉要么都不钉(只钉一头会错位)、钉住是开关不是写死、开关真的有人打开、扩大可点范围不许改变视觉尺寸。 | 改 `frontend/src/components/stock-table/StockDataTable.tsx`(+`pinFirstColumn`/`pinnedCellClass`)、`components/screener/ScreenerTable.tsx`、`components/stock-analysis/WatchlistDecisionBoard.tsx`(首列 th/td + 三个开关 + 未分析那一格)、`pages/Watchlist.tsx`、`pages/backtest/StrategyBacktest.tsx`;新增 `backend/tests/test_mobile_table_layout.py`(6 条);**`backend/app/` diff 全空**;**`backend/app/strategy/` diff 全空**;冻结的 `opportunity_score.py` 未动;**判定、六态、口径、指标色一个没动**;后端 pytest 4303 passed / 10 skipped 退出码 0, 前端 tsc 0 / eslint 0 error / build 退出码 0 / vitest 47 passed | 低(窄屏加钉住 + 三处换行 + 可点范围; 桌面逐项量过无变化, 数据与判定零改动) | 可 |

**量出来但这一轮没动的, 逐条说清**:

- **可点目标偏小还剩一批**(各页合计约 25 个不同控件: 板块筛选片、页签药丸、「3个月/6个月/1年」这类区间片)。它们是**刻意的密度**, 不是疏忽 —— 仓库里 `.tap-target` 只用在作者判断值得的 5 处。要不要整体铺开是一次密度决定, 该单独提, 不该混在"顺手修一下"里。
- **`/data` 平板下「个股维表」被压成 21×32**。它不在这一轮商定的范围(常用页)里, 且只是两行折行, 记在这儿备查。
- **别的页面的表格没量到** —— 策略结果、模拟盘成交、连板梯队、监控记录这些表在测试环境里**没有数据就不渲染**, 我量不到也就不敢改。自选与决策台两张是唯一真跑出数据的, 都已钉住。**下一轮要接着做的话, 得先有一份能让这些表长出来的样本数据**, 否则改了也验不了。
