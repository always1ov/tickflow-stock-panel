# R414 — 那 4 条「看时钟的测试」:改对之外,把它变成任何时候都会红

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R414 | R411 跑全量时撞见 4 条红的, 当时确认过**不是那一轮弄的**(干净 HEAD 上一样红), 记在 `FORK_NOTES.R411.md` 里留到这一轮。**病因**: `app/api/kline.py` 与 `app/services/abnormal_moves.py` 判"是不是今天"用的是**北京日期** `cn_today()`(前者的注释还把两种时区各自的症状写得清清楚楚), 而测试拿 `date.today()`(进程本地时区, 容器是 UTC)去造数据 —— 两边只在 **UTC 00:00~16:00** 对得上, 过了 UTC 16:00 北京已跨日而本地没有, 日期差一天, 注入被跳过, 断言全线落空。**所以这几条测试每天到点自己变红、白天又自己变绿。这比测错更坏**: 红色变成噪音, 人会习惯性忽略它, 而下一次真测出问题时也被一起忽略。**代码是对的, 错的是测试** —— 四处 `date.today()` 换成 `cn_today()`。**但光改对不够, 因为这个坑这个仓库犯过两次**: `test_abnormal_moves.py` 里 `test_benchmark_momentum_today_excludes_today_rows` 早就修过一次(注释还在, 写着「CI 跑在 UTC 上正好每天踩中这一段」), **当时没扫一遍别处**, 于是同一个文件里另外两处、加上 `test_kline_live_candle.py` 两处, 一共四处带着同样的写法活了下来。下一个人写新测试照样会写 `date.today()`, 而他在白天跑, 全绿。**所以这一轮的重点是守卫**: **不去扫"有没有写 `date.today()`"**(那是钉措辞, 而且 `date.today()` 在别处有大量正当用法 —— 全仓测试里 73 处、app 里 58 处, 绝大多数是查询区间上界这类无害用法), 而是**把时差直接造出来**: 把四个模块里的 `cn_today` 引用都换成「本地日期 + 1 天」, 再把那几条测试原样跑一遍。用 `cn_today()` 写的 → 造数据和被测代码用同一个(被挪过的)日期 → 绿; 用 `date.today()` 写的 → 差一天 → **当场红**。**这条守卫把一个只在特定时段出现的 bug 变成了任何时候都出现的 bug。** 并且单独钉了一条「守卫自己没哑掉」—— `_shift` 漏掉任何一个模块的引用, 时差就没造出来, 上面两条会退化成"普通重跑一遍"永远绿(这个仓库栽过好几次同形状的哑法, 最近一次是 R404 的正则别名)。**顺带把这一类的现状摸清楚了**: 全量套件在 **UTC / Asia/Shanghai / Pacific/Honolulu 三个时区各跑一遍**(覆盖"本地落后北京"与"本地等于北京"两个方向, 外加 UTC+14 的"本地早于北京"也试过), **4488 全过** —— 也就是说除了这四处, 眼下没有别的同类定时炸弹。 | 改 `backend/tests/test_kline_live_candle.py`(3 处 + 表头说明)、`backend/tests/test_abnormal_moves.py`(2 处 + 模块级 import);新增 `backend/tests/test_beijing_today_not_local_today.py`(3 条);**被测代码一个字没动**(它本来就是对的);**打分系统 / `strategy/` / `keltner*` / 六态 / `position_exit` / `today*` diff 全空**;后端 pytest **4488 passed / 10 skipped 退出码 0**, 三个时区各一遍全绿 | 低(只动测试; 被测代码未改) | 可(把那 5 处换回 `date.today()` + 删新增那个文件 —— 但那等于把定时炸弹装回去) |

**变异测试里最值得记的一行**:

把那四处改回 `date.today()`, 然后**在 `TZ=Asia/Shanghai` 下跑**(本地 == 北京,
正是白天开发时的情形):

```
[M1 kline 那两条改回 date.today()]        变异后老测试本身: 绿(所以光靠它发现不了)
RED    M1 kline 那两条改回 date.today()    ->  新守卫
[M2 abnormal_moves 那两条改回 date.today()] 变异后老测试本身: 绿(所以光靠它发现不了)
RED    M2 abnormal_moves 那两条改回 date.today()  ->  新守卫
RED    M3 _shift 漏掉一个模块 = 守卫自己哑掉      ->  新守卫
```

**「变异后老测试本身: 绿」** 就是这整件事的要害 —— 写错了的版本在开发者的机器上
是绿的, 所以靠"跑一下测试"永远发现不了; 只有 CI 在 UTC 上、而且恰好在北京午夜
之后跑, 它才会红。新守卫把这个条件去掉了。

**没做但考虑过的**: 在 `conftest.py` 里给整个套件钉一个**不是北京**的时区
(那样任何"把本地日期当北京日期"的错都会当场失败, 而不只是这四处)。
实测过可行 —— 三个时区全绿, 说明钉哪个都不会误伤。**但那是一个影响全部 4488
条测试的全局改动**, 超出了这次「把那 4 个修了」的范围, 按硬约束第 14 条该先问。
要做的话是单独一轮, 证据都在这儿了。

**这一类的现状(摸过底, 记下来省得下次重查)**:

| | 裸 `date.today()` 处数 | 危险吗 |
|---|---|---|
| `backend/tests/` | 73 | 绝大多数无害; 危险的是**喂给按 `cn_today()` 判定的代码**那几处, 已全部修掉 |
| `backend/app/` | 58 | 绝大多数是 `end = date.today()` 这类**查询区间上界**, 差一天不改变结论; 真正按日期做**相等判定**的地方本来就用的 `cn_today()` |
