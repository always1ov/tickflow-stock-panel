# R382 — 换皮肤漏了暗色,而用户用的正是暗色

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R382 | **把靛蓝补到暗色**(用户截图问: 「怎么前端还是老样子」)。**根因是我漏了一整套主题**: R379 换皮肤时只动 `:root`(亮色), `html.dark` 有意一个字没碰 —— 而用户用的就是暗色, 于是「换了皮肤」在他屏幕上基本没有发生。版式那两轮(R380 页头 / R381 模拟盘排版)是**主题无关**的, 暗色下照常生效; 漏的只有颜色这一层。**教训不是「暗色忘了改」, 是「主题色」这东西本来就不该分主题** —— 同一个产品在两套主题下得是同一个颜色, 否则它就是两个颜色。第一条守卫钉的就是这个: 亮暗两边 `--accent` 的**色相必须一致**(明度可以各调各的, 暗底上要更亮, 那是可读性不是身份)。**改了三个值, 全是强调色**: `--accent` 色相 259.81(作者的蓝)→ **273.06**(与亮色同一个靛蓝), 明度顺手 0.63 → 0.65 —— 靛蓝比蓝暗, 不提的话对比度会掉(5.53 → 5.41), 提完是 **5.90 对页底 / 5.29 对卡面**, 比原来的蓝还好一档; `--accent-hover` 从 `var(--t-blue-400)` 改成自己的一档 0.71 —— 借的那个是**蓝族色阶**, 不跟着强调色换色相, 于是 hover 一下颜色就跳回蓝, 而且暗色里 hover 要**更亮**(与亮色相反); `--accent-soft` 从 `var(--elevated)` 改成带靛蓝的 0.265/0.045 —— 借的那个是**中性灰**, R379 把侧栏选中态改成「同色系药丸」之后, 暗色下那颗药丸是**灰的**, 「选中」只剩字色在说、底色一句话没说。**这一条最值得记的是它的代价为零**: 与卡面的 ΔL 0.052 → 0.055, 靛蓝字压在上面 4.61 → 4.61 —— **抬升档位与对比度一个数都没变, 只换了色相**, 所以它不是「调深浅」而是「把灰换成品牌色」。**作者的黑一个字没动**: `--base`/`--surface`/`--elevated`/`--border` 全部原值, R163 记着用户原话「还是用回以前作者的黑吧」, 那是定过案的 —— 专门加了一条守卫按明度逐个钉死。**暗色的指标色同样没动**: 涨红/跌绿/警示三个色相逐个钉住, `--danger` 仍等于 `--bull`, 主题色离涨跌 ≥40° 的隔离带这次也钉了暗色那一份(亮色那条 R379 已有)。**顺带修掉两处说假话的 docstring**: 模块头与 `_light_block` 都写着「暗色一套这次一个字没动」, 而这一轮动了; `_token` 的断言消息写的是「亮色里找不到」, 但它现在两边都在用。**7 处变异逐一确认会红。** | 改 `src/index.css`(仅 `html.dark` 的三个强调色令牌);`tests/test_theme_palette.py` +4 条并新增 `_dark_block()`;**亮色一侧、作者的黑、两套的指标色全没动**;后端 pytest **4228 passed / 10 skipped 退出码 0**, 前端 tsc 0 / eslint 0 error / vitest 47 条全过 / build 通过;暗色实机渲过 | 低(只动暗色三个强调色令牌; 底色、指标色、版面、判定一个字节没动) | 可 |
