# R403 — 关键价位撤掉「持仓止盈」那一组线;出场线系统与六态一个字没动

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R403 | 用户: 「持仓止盈可以删除掉了, 但是六态的所有一点都不能改」。**动手前先把依赖查清楚**, 因为「持仓止盈」在这个仓库里是两样东西: 图上那一组线, 和底下那套出场线判定。查下来它撑着六个面, 其中一个已经是死的: 关键价位图上那一组线、决策台「止盈线」列、盘中 `exit_*`/`exitlife_*` 监控推送、AI 信号的持仓上下文(纪律第 7 条)、信号/回测里的 exit 因子, 以及**今日总览行动区 —— 前端没有任何地方渲染 `TodayOverview.actions`, 今日总览页 R351 就删了**。更要紧的是**生命线长在同一个模块里**(`position_exit.py` 的 `fatal`/`lifeline` 两档), 而用户自己的纪律写着「卖点是止盈线、生命线、六态转弱」—— 整个删掉就等于砍掉其中两条卖出理由, 那是判定层的改变。所以**没有自行决定范围, 给了三档让用户选**(只删图上那组线 / 删展示与推送但留生命线 / 整套删干净), 用户选了**第一档**。**顺带查清并当场回答了一个误会**: 我在选项里提过「决策台那一列可以改成显示六态下关键点」, 用户回「什么意思, 六态是不能动的」—— 读六态**已经算好的输出**不等于改六态, 判定逻辑一行不碰; 何况选了第一档之后决策台那一列根本不动, 这个选项自动作废。**另有一个可以放心的结论**: 模拟盘完全不依赖出场线 —— `flip_paper.py` 里一次 `position_exit` 都没有, 它的清仓判据只有六态转折(「转折后在空头侧 → 清仓」); 界面上那句「跌破离场线才清仓」的「离场线」指的是六态触发价, 不是这条 ATR 线。**实际改动只有三处**: `LEVEL_TYPES` 去掉 `"exit"`; `api/stock_analysis.get_levels` 里那段 19 行的注入整块删掉; 前端 `LevelType` 联合类型与 `LEVEL_GROUPS` 各去掉一项。**`position_exit.py` 一个字节没动**, 六态那一组线原样留着。**顺手修掉两处说假话的注释**(AGENTS.md 第 14 条: 顺手撞见的错当场修): 前端写着「14 个开关」, 而**改之前实际是 13 个**(撤完是 12); 后端 `levels.py` 头部写着「输出 4 类结构化价位点」, 而 `compute_levels` 早就算 11 类。两处都不再写死数字 —— 数字写在注释里必然过期, 改成「以 LEVEL_TYPES / LEVEL_GROUPS 为准」。**守卫 4 条, 12 个变异逐条确认会红**, 钉的是两件事而**第二件才是重点**: 一、图上真的没有它了(前后端两侧 + 联合类型); 二、**出场线系统整套还在** —— 模块在、三个函数在、生命线那两档在、三个活的展示面一个没少。**其中一条第一次跑是绿的**: 「生命线那两档在不在」原来是扫 `"fatal"`/`"lifeline"` 这两个词, 而它们在注释里也出现, 把档位表整个改名照样过; 改成 `from ... import STAGE_CN, STAGE_ACTION` **钉档位表本身**, 并顺带钉死「跌破生命线 = 无条件清仓离场」这句话, 才真的拦得住。 | 改 `backend/app/indicators/levels.py`(`LEVEL_TYPES` 去掉 exit + 头部注释)、`backend/app/api/stock_analysis.py`(删掉 19 行注入)、`frontend/src/components/stock-analysis/AnalysisKChart.tsx`(联合类型 + `LEVEL_GROUPS` + 计数注释)、`frontend/src/lib/api.ts`(联合类型);新增 `backend/tests/test_exit_level_removed_only_from_chart.py`(4 条);**`backend/app/services/position_exit.py` diff 全空**;**六态相关文件(`livermore_service.py` / `indicators/livermore.py`)diff 全空**;**`backend/app/strategy/` diff 全空**;冻结的 `opportunity_score.py` 未动;后端 pytest **4385 passed / 10 skipped 退出码 0**, 前端 tsc 0 / eslint 0 error / build 退出码 0 / vitest 107 passed | 低(只是少画一组线。任何判定、推送、AI 上下文都没碰; 关键价位默认一组都不打开, 所以多数人看不出差别) | 可(恢复办法写在两处注释里: `LEVEL_TYPES` 加回一行 + API 层那段注入贴回来, 前端两处联合类型与一行 `LEVEL_GROUPS`) |

**撤之后关键价位还剩 12 组**:

| 来源 | 组 |
|---|---|
| 作者内置(`compute_levels`, 纯日 K 算) | 压力支撑、枢轴点、前高前低、布林带、量化通道短/中/长、ATR波动通道、缺口位、斐波那契、整数关口 |
| fork 增强(API 层注入) | 六态关键点 |

**没做、也不该顺手做的**: 决策台那一列没有补替代。用户在第二问上表达的是对「动六态」的担心, 不是要求换一列; 而第一档范围本来就不动决策台。要不要换成别的读数是一次独立的判定层决定, 该单独提。

**下一步仍然挂着**: 用户原本要「在关键价位加一个指标」, 还没说是哪一个。那件事按之前列的三档(横线 / 通道曲线 / 需外部输入)走, 并且要先回答「只画线还是进判定」—— 几何量自 R229 起整层不进把握分, 界面上「不进把握分」那句话得一直是真的。
