# R381 — 模拟盘重新排版:把宽屏上空着的那半边用起来

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R381 | **模拟盘重新排版**(用户: 「模拟盘页面的内容需要重新排版, 合理利用显示空间」)。**只改版面, 不改内容** —— 六块一块不少、每条口径一个字没动、判定与六态零触碰(R341 的铁律)。**这一轮是拿真数据看出来的, 不是拿空页面猜的。** 本地这套没有行情 Key, 直接打开是空页面, 而**空页面上"哪儿浪费"全看不见** —— 信号行、持仓、流水、没做成全都不渲染。所以用 Playwright 把 `/api/flip-paper` 拦下来喂一份像样的假数据(9 条信号 / 6 只持仓 / 46 笔流水 / 23 次没做成 / 12 个月), 在 **1366 / 1600 / 1920 / 390** 四个宽度上各看一遍。看出来三处: **① 六态那一列在截字, 右边那格却空着。** `minmax(7rem,9rem)` = 144px 装不下「按现价会转折 —— 收盘还站在这边才算数」, 盘中越线那几行一直显示成「…收盘还...」—— 而截掉的正是这一档唯一要说的话(收盘站不住就不算数)。更别扭的是它右边那格(走势)在没进候选池的行上是**空的**, 1600px 上白白空着六百来像素, 左边却在截字。上限提到 16rem(256px, 刚好装下那 20 个字)。**仍是定宽列不是 `1fr`**, 行与行照旧对齐(R350 的前提); `truncate` 留着兜窄屏。**窄屏那条栅格一个字没动** —— R366 的卡片式布局靠它。**② 「现在拿着」与「成交流水」各占一整行。** 两张都是 `min-w-[640px]` 的窄表, 1600px 上右边一半是空的, 而它们本来就该放在一屏里对照着看(拿着的这几只, 是哪天买进来的)。断点 **1560 是算出来的**: 内容区 ≈ 视口 − 侧栏 224 − 留白 32, 两列 640 + 12 的间隙要 1292 → 视口 ≥ 1548。**窄了也不会坏**: 两张表自带 `overflow-x-auto`, 最坏是卡片内部出现横向滚动条, 不是版面塌掉。**③ 「这套规则」七条竖着排、「有信号但没做成」一条一行。** 都是「标签 + 一行值」的窄行, 铺在 1600px 上每行右边空掉三分之二, 还把下面的东西挤出首屏。规则改成 `md` 两列 / `2xl` 三列(「成本」那条值最长, 多列时独占一整行, 免得一个人把行高撑成两倍), 七条一条不少、次序也没动 —— 这是查证用的清单, **顺序本身就是信息**。**中途推翻过一次自己的做法**: 第一版并排之后截图一看, 左边空了七百多像素(拿着 6 只对流水 46 笔), 于是把「没做成」也收进左列。接着试的是「不动 DOM, 用 `col-start`/`row-start` 摆位」—— 想让单列时的阅读顺序一个字不变, 结果右列 `row-span-2` 把第一行撑高、左列两块中间裂开一道四百像素的缝(跨行元素比它跨的两行都高时, 多余高度怎么分摊不听我的)。**摆位摆不出来就别硬摆**: 改成左列一个容器装两块、右列装流水, 高度各自算。代价是**单列时顺序变成 拿着 → 没做成 → 流水**(原来流水在中间)—— 这个代价划算: 「没做成」是一句话的小结、「流水」是几十行的长表, 短的放前面本来就更好读; 而真正有讲究的那一条(「规则排在最后」, 查证用的不该天天占首屏)**没动**, 并在守卫里单独钉住。**还有一处也是截图抓的**: 「没做成」那几条第一版写到 `2xl:grid-cols-4`, 但它从这一轮起活在**半幅左列**里, 而 `sm:`/`xl:` 量的是**视口**不是它自己的宽度 —— 1600px 上实际只有 650px 可用, 四列挤得「6 次」两个字都拆行。收到两列, 并把次数改成右对齐(原因那几句长短差很多, 标签自己换行、次数永远在同一条竖线上)。**两条既有守卫跟着更新并写清为什么**: `test_R356_把右边那片空地用上`(立论一个字没变, 只是发现还有一处没用上)与 `test_R343_模拟盘原有六块的顺序没被动过`(那一处对调是有意的, 理由写进注释, 并新增一条断言钉死「规则排在最后」)。**8 处变异逐一确认会红。** | 改 `pages/FlipPaper.tsx`(信号行列宽 / 底部两列 / 规则与没做成改多列);`tests/test_flip_fusion.py` +3 条并更新 2 条;**后端 `app/` diff 全空**;**判定、六态、打分、口径、指标色零触碰**;后端 pytest **4224 passed / 10 skipped 退出码 0**, 前端 tsc 0 / eslint 0 error / vitest 47 条全过 / build 通过;四个宽度实机验过, **均无横向溢出**, 1600px 上整页滚动从 2.31 屏降到 **1.98 屏**(1920px) | 中(版面动得不少 —— 但每一块的内容、口径、判定一个字节没动, 窄屏那套栅格也没碰) | 可 |
