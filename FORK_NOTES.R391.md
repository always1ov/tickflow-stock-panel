# R391 — 体检打开只说一句「台账还是空的」,而它说不出为什么空

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R391 | 用户报「体检按钮的功能失效了」, 追问后确认是**弹窗能开, 但报错或说台账是空的**。按钮、接线、路由、接口全查过没问题(最小 app 真跑了一遍 `GET /api/today/score-ledger` 回 200, 结构完整), **坏的是这条线上「它不肯说自己为什么空」**, 三处独立缺陷: **① 后端一条分支从头到尾不吭声** —— `_build_overview` 落台账那段原本是 `if as_of and not any(intraday): record_day(...)`, **条件不成立时什么都不做也什么都不说**。不记本身是对的(盘中的现价不是收盘价, 拿它当收益起点会算出假收益), 错的是**不说**: 开着实时行情看盘, 台账就天天不记, 而界面上没有任何一处提过; 攒几周之后点开体检只看到「台账还是空的」, 那句话解释不了也给不出出路。**这正是 `_SelfCheck` 那个类立起来要消灭的静默跳过, 而它漏了自己这一处** —— 因为 `test_R274_每一处跳过都要登记` 扫的是 `except`, 而这一处**根本不抛异常**, 是「条件为假」这另一种形状。现在 if 链三支全部有动作: 没 `as_of` → 上报; 盘中口径 → 上报「N 只在用实时价, 这一次不记, 收盘后或关掉实时行情再打开一次才会落账」; 正常记账 → **接住返回值**(`record_day` 自己吞异常只返回 `ok: False`, 不看就又是一次静默失败, 只是换了个位置)。**判定口径一个字没动** —— 该不该记的判据原样保留, 改的只是「不记的时候要说」。**② 唯一能解释空的那句话恰好在最需要它的时候不可达** —— `legacy_days`(「另有 N 天是换打分口径之前记的」)只渲染在 `recorded_days > 0` 分支里, 而「攒的全是老口径」**正好会让 `recorded_days` 是 0**。**③ 空态指向一个已经删掉的页面** —— 写着「打开**今日总览**会自动记一天」, 而那一页在 R340/R343 已经拆了, 照着做是做不到的(踩硬约束 12)。改成指「这一页」, 并补上真正有用的那句: **只在盘中看盘的话台账会一直是空的**。顺带把错误态的「读取台账失败, 请稍后重试」换成带上真实错误消息 —— 吞掉原因是同一个毛病的第四种写法。**新增 7 条守卫, 8 处变异逐一确认会红**(盘中那支退回什么都不做 → 2 红; 整块退回原来那个无 else 的 if → 2 红; 写失败不上报 → 1 红; 返回值根本不接 → 1 红; SITES 删掉登记 → 2 红; 空态又去指今日总览 → 1 红; 空态不读 `legacy_days` → 1 红; 空态删掉盘中那句 → 1 红)。**守卫钉的是性质不是措辞**: 盘中那一支按**数据流**定位(先找哪个名字是从 `intraday` 算出来的, 再找哪条分支拿它当判据), 改文案不会误伤, 而把那条分支改成什么都不做会当场红 —— 这仓库栽过好几次的「锚太宽 / 钉名字不钉性质」不再重演。**扫前端那三条走 `frontend_source.code_of` 剥注释**: 解释「以前指的是今日总览」那段注释必然要写出旧名字, 不剥的话断言会被自己的注释喂饱(本仓库第八次)。 | 改 `backend/app/api/today.py`(记账那段 if 链, **判定口径未动**)、`frontend/src/components/ScoreLedgerDialog.tsx`(空态 / 错误态文案);新增 `backend/tests/test_score_ledger_silent_skip.py`(7 条);**`backend/app/strategy/` diff 全空**;冻结的 `opportunity_score.py` / `score_ledger.py` 未动;**判定、六态、口径、指标色一个没动**;后端 pytest 4292 passed / 10 skipped 退出码 0, 前端 tsc 0 / eslint 0 error / build 退出码 0 | 低(后端只增上报不改判据, 前端纯文案; 接口出入参形状未变) | 可 |

**没做、且不该由我做的**: 这几条修的是「它说不出为什么空」, **不保证台账马上就有数据**。真正让它开始记的动作在用户那边 —— 收盘后(或关掉实时行情)打开一次模拟盘页面。**特别没有去动的是那道 `any(intraday)` 闸**: 它是「只要有一只用了实时价, 整页快照都不算定稿」, 看着过严, 但那正是 R133 立它的理由(收益起点必须是真收盘), 属于核心判定口径, 不在「发现问题直接修」的范围里。
