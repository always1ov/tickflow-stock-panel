# R430 — 个股弹窗重做第二块:「图表与价位」(左图右列表)

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R430 | 用户发来第二块排版图(「接着后面的部分」): 小标题「图表与价位」下左右两张卡片 —— 左边图(日 K / 分时 / 关键价位三个视图), 右边关键价位一类一行的列表(色点 + 名称 + 条数, 点开的那一行反相, 没有价位的灰掉, 顶上「全部清除」)。四条答复全按推荐: (1) 新块加上、旧的先留着(新块排在滚动区最前, 旧的日K/分时/关键价位/复盘原样在下面); (2) 枢轴点「档位」、二型「粗细 / 上攻推算位 / 回测这三档」放右侧列表, 点开那一类时就在它下面展开; (3) 图下面的价位清单先留着; (4) 图跟着头部 60 / 120 / 250 日走 —— 关键价位图默认露最近 N 根, 日 K 区间从关键价位那份日 K 倒数第 N 根起、并把 StockPanel 的可见根数也设成 N(带分时小图时它默认只露 40 根)。结构: 开关状态从 `AnalysisKChart` 提成 hook `useLevelControls`(新文件 `levelControls.ts`), 图不传就自己持有(个股分析页等老入口一字不变), 传了就听外面的且不画自己那一排; 每类旁边那个数(`levelGroupStat`)、图上实际画的那份价位(`useShownLevels`)、两组小开关的悬停说明都只有一个产地, 图与列表共用; 关键价位日 K / 价位两个查询抽成 `useAnalysisKline` / `useStockLevels`(头部与列表同键同选项); 头部那套按钮样式挪到 `pill.ts` 共用。看日 K / 分时时点列表某一类 = 切到关键价位并打开它(只开不关)。选中行反相, 色点换用另一套主题的调色板, 否则暗色主题下近白的六态关键点画在白底上就没了。**顺带修掉**: (a) 缩放起点原来按百分比取整, 250 根里 1% 是 2.5 根, 「60 日」实际露 62 根、与头部那行对不上 —— 改成按第几根(startValue); (b) 二型「中」档悬停写着「默认档」, 而 R410 早把默认改成了粗档, 这句一直在说假话。窄屏(< lg)两卡上下叠, 「右侧 / 左边」这类方位词因此改成不带方位的说法。R410 的三条守卫随代码挪到新产地并改指(断言不变, 变异 5 个全杀)。 | 新增 `frontend/src/components/stock-analysis/levelControls.ts`、`frontend/src/components/stock-preview/ChartLevelsSection.tsx`、`LevelSideList.tsx`、`pill.ts`; 改 `frontend/src/components/StockPreviewDialog.tsx`、`stock-analysis/AnalysisKChart.tsx`(可受控 / visibleBars / 按根缩放 / 计数与文案走共用)、`stock-analysis/StockLevelsPanel.tsx`(两个查询 hook + 透传)、`stock-preview/PreviewHero.tsx`(用共用 hook 与样式); 新增 `backend/tests/test_chart_levels_section.py`(9 条); 改 `backend/tests/test_preview_hero.py`、`test_fib2_readability.py`(改指新产地); **后端业务代码、打分系统、`strategy/`、六态 diff 全空**; 变异 15 + 5 个全杀; 后端 4532 passed、vitest 155、tsc / eslint / build 0; 亮暗两套 + 窄屏出图核对版面、点开枢轴点与二型时小开关展开、点 60 日后关键价位图与日 K 都露 60 根、日 K 页点某一类切到关键价位、全部清除 | 中(`AnalysisKChart` 开关状态换了持有方式) | 可(删掉弹窗里 `<ChartLevelsSection>` 那一段即回到只有旧内容; 老入口不传 `controls` 本来就走原路) |
