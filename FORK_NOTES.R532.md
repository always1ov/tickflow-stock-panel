# R532 — 照 Aipha AI 的灰阶: 悬停 / 边框调浅, 深色页面与卡片同一个底, 表头去掉灰底条

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|---|---|---|---|
| R532 | 接 R531。R531 只做了「文字两档」, 另两条因为会推翻用户以前定的事先问; 用户回「改」, 又补一句「尽可能参考那个网页」。于是整套灰阶照 Aipha AI(shadcn/ui 默认 Gray)原值: **亮色** 正文 #030712 / 辅助 #6B7280(4.83:1) / 悬停·次级面 #F3F4F6 / 边框 #E5E7EB(1.24:1) / 输入框边框 = 普通边框(参考页 --input = --border) / 选中底 #F3F3F3(强调色三档须同色相, 保持中性); 页底 #FFFFFF 与侧栏 #FAFAFA 本来就一致。**深色** 页面与卡片同一个底 #030712(卡片不再比页面亮一层, 分区全靠边框), 边框 = 悬停 = #1F2937, 正文 #F9FAFB, 辅助 #9CA3AF; 侧栏跟卡片底走。**表头**(components/ui/table.ts 的 THEAD)去掉浅灰实底条, 坐在卡片底上, 仍不透明(R252)。**推翻的两条旧决定, 都是用户这次明确改口**: R408「所有颜色显示都淡了好多, 看起来好辛苦」压深的 #E2E2E2 / #D1D4DD / #C2C7D6; R163「还是用回以前作者的黑」的 #0a0a0b / #18181b / #242429 / #3e3e42。**没抄的**: 参考页的紫色主色(全站禁紫, 强调色保持黑白反相)、红涨绿跌与警示色(R368 冻结)。守卫: test_theme_palette 的 CHROME 换成新值; test_R501 改成只钉页底纯白与侧栏略灰, 次级面 / 边框照参考页; test_R382_作者的黑一个字没动 换成 test_R532_暗色照参考页_页面与卡片同一个底 | frontend/src/index.css; frontend/src/components/ui/table.ts; backend/tests/test_theme_palette.py | 低: 只改令牌值与一个表头类名 | 可以: git revert 本提交(R531 的文字两档不受影响) |
