# R407 — 斐波那契二型的两块底色加深并补边框:规格那个透明度是在白底原型上定的

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R407 | 用户: 「新指标的背景色看不清太淡了」。两块底色都太淡, 原因不一样但同源 —— **规格 §9 给的数是在原型图的白底上定的, 没考虑这个仓库有两套主题**。① **单边上攻那一段**规格写的是 6% 透明度, 在暗色主题上基本等于没有(暗底本来就深, 6% 的红压上去看不出边界在哪)。改成按主题分开给: **暗色 20% / 亮色 13%**, 并补一圈**虚线边框** —— 有了边框, 这一段从哪根开始到哪根结束才看得出来, 而那正是这块底色存在的意义。② **强支撑区**原来只有半透明填充, 起点 20%; 而这个区常常只有两三条回撤线的厚度(实测那份数据里只有 0.2 元宽), **在蜡烛底下几乎看不出边界**。填充起点抬到 30%(重合越多越浓, 上限 55%), 并补一圈**实线边框**。边框是关键那一半: 薄区靠填充永远显不出来, 靠边框才立得住。③ 两处标签都加粗, 强支撑区的标签色也分了主题(亮色下原来那个金色压在浅金底上糊成一片)。**为什么不是简单调大一个数**: 一开始我就是照规格抄的 6%, 而规格那张原型图是纯白底 —— 同一个 alpha 在 `--base` 近黑的暗色主题上完全是另一回事。这类"照着设计稿的数抄, 没折算到实际底色"的毛病, 这个仓库在 R317 的阴影上栽过同一族(Tailwind 默认投影是为浅色底调的黑色半透明, 放在近黑主题上等于没有)。**亮暗两套都重新出图确认过**。**没有加守卫**: 这一轮改的是四个透明度数值和两圈边框, 属于观感取舍, 没有可断言的性质 —— 硬编一条「alpha 必须 ≥ 某值」只会变成走过场, 下次调它还得先改断言。真要退化了, 是用眼睛看出来的。 | 改 `frontend/src/components/stock-analysis/AnalysisKChart.tsx`(上攻段与强支撑区的 `itemStyle` + 标签);**后端一个字没动**;**打分系统 / `strategy/` / 六态 / `keltner*` diff 全空**;后端 pytest **4440 passed / 10 skipped 退出码 0**, 前端 tsc 0 / eslint 0 error / build 退出码 0 / vitest 107 passed | 低(纯观感, 只动两个 markArea 的样式; 位置、数值、判定一个没碰) | 可(把两处 `itemStyle` 改回原样即可) |

**这一轮没动的、但下次可能还要碰的**:

- **右侧标签在线多的时候会叠**(细档 14 条线时「深回撤 R2」和「深回撤 R4」挤在一起)。那是 ECharts `endLabel` 的老问题, 要么错位排布要么按距现价取前 N 条 —— 都是另一件事, 没夹进来。
- **「未来」区仍未画**(数据早就发过去了), 要扩 x 轴, 会动到所有价位组共用的坐标轴。
