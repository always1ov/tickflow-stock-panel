# R384 — 「今天该挂什么单」那张卡:1022px → 453px

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R384 | **用户: 「今天该挂什么单这个卡片也要重新设计很占用空间」。** R383 之后按他的数据形状(32 条全「已转折」、全没名次、`flip_price` 为 null)量出来: **整张卡 1022px**, 拆开是「表头 44 + 16 个可视行 × 60px」—— 而每行 60px 里**栅格只有 40px**, 上下各 10px 的留白占掉三分之一; 最左边那 56px 的格子, 32 行写了 **32 遍同一句「没进候选池」**。**三处一起改**: **① 名次那一列整屏没名次时收成 0。** 同一句话说 32 遍不是信息是噪音, 它该在区块标题上说**一次** —— 所以标题的 note 里补了「都没进候选池」, 行里那句撤掉。**收成 0 而不是不渲染那一格**: 窄屏那套卡片版面(R366)靠 `row-span-2`/`col-span-3` 把六个格子折成一张卡, 抽掉一格整套跨行跨列全要重算; 给 0 宽度则一个 `col-span` 都不用动, 代价只有 `gap-x-3` 那 12px。**② 行留白跟着名次走**(`py-2.5` → `py-1.5`): 名次那一格是三行高, 留白撑着才不挤; 没名次时行只有两行字, 10px 的上下留白在 40px 的栅格上就是三分之一。与行高那一条同一个判据。**③ 行再窄下来就排三列。** 列数按**这一行到底要多宽**定: 有走势 → 单列; 没走势 ≈620px → 两列(1560 起); 再收掉名次 ≈560px → 三列(1900 起)。断点都是算出来的(内容区 ≈ 视口 − 侧栏 224 − 留白 32, 再除以列数)。**量出来: 1022 → 453px(−56%)**, 行外层 60 → 36, 名次列 56 → 0; 「现在拿着」与「成交流水」因此回到了同一屏里。**守卫替我抓到一个真 bug**: 窄屏那套栅格原本写死在 div 上, 而模板又给一个 —— **同一个元素上两个 `grid-cols-`, 而 CSS 里谁赢取决于样式表里谁排后面, 不是 class 串里谁排后面**。那种冲突不报错, 只表现为「某些情况下列宽莫名其妙」。数 `grid-cols` 条数时当场发现, 窄屏那套也一并挪进 `ROW_GRID`, 并加了一条「div 上不许再写死列宽」。**还抓到我自己写的一个守卫洞**: 第一版 `for key in ("'trend':", "':")` 里两次断言的是**同一个字符串**, 与 key 无关 —— 于是只把其中一套改回去照样绿(变异 M1 当场抓到)。改成把四条表项逐条切出来、两种情况各查各的, 并补上「有名次那两套必须占住 3.5rem」的反向断言。**「锚太宽 = 没有锚」, 这一轮又栽一次。** **手机端逐格量过**: 名次格 0×48 仍 `row-span-2`, 标的+动作同一行, 六态跨列第二行, 390px 无横向溢出 —— R366 那套卡片版面一个 `col-span` 没动。**8 处变异逐一确认会红。** | 改 `pages/FlipPaper.tsx`(`ROW_GRID` 四套 + `gridOf` + 留白跟 shape + 三列 + 标题 note);`tests/test_flip_fusion.py` +5 条并更新 4 条;**后端 `app/` diff 全空**;**判定、六态、打分、口径、指标色没动;撤掉的那句话在标题上补回来了**;后端 pytest **4237 passed / 10 skipped 退出码 0**, 前端 tsc 0 / eslint 0 error / vitest 47 条全过 / build 通过 | 中(信号行的列数与留白都跟着内容变了 —— 但每一格的内容、次序、判定没动, 窄屏那套版面逐格量过没变) | 可 |
