# R429 — 个股弹窗重做第一块: 新头部(按用户排版图)

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R429 | 用户: 「我想重做整个弹窗, 我发你一点就修改一点」→ 头部排版图(「你只需要看排版, 我里面的文字大都是占位符」)。新组件 `PreviewHero` 两行: 第一行左边 名称 / 代码 / 现价 / 涨跌幅 / 「起 ~ 止 · N 个交易日」/ 方框星星(自选: 不在 = 空心、点了选分组加入; 已在 = 实心金、点了移出), 右边 60 / 120 / 250 日、AI 四维分析(图里漏了, 用户要补)、导出复盘、使用说明; 第二行「结论 (待定)」+ 一道细线拉满。按用户答复: **加在最前面, 旧顶栏原样留着**(「后面等我叫你删除旧的」); 60 / 120 / 250 与复盘页**是同一个值**, 由弹窗持有, 复盘页 `StockReviewPanel` 改成可受控(不传仍自己管); 「结论」与两个新按钮先占位, 点了弹提示。起止日期与交易日数从弹窗已在取的那份日 K 数最后 N 根(同一个查询键, 不另发请求)。**出图时抓到两处并当场修掉**: (1) 星星方框第一版写成 `PILL w-8 px-0`, 而 PILL 里有 `px-3`, 同一元素两个 px 谁生效看样式表先后, 结果 `px-3` 赢, 32px 方框左右各吃 12px, 星星被挤成 6px 宽 —— 公共形状串拿掉横向内边距与字色, 方框单列 `SQUARE`, 实测 16x16; (2) 天数提到弹窗里后, 复盘页 `key={symbol}` 重建已带不走它, 切股会把上一只的「60 日」带到下一只, 违背原注释「是上一只票的上下文, 不该带到下一只」—— 在弹窗切股那个 effect 里复位, 默认值收成 `HERO_DAYS_DEFAULT` 一个产地。 | 新增 `frontend/src/components/stock-preview/PreviewHero.tsx`; 改 `frontend/src/components/StockPreviewDialog.tsx`(渲染新头部、持有天数、切股复位)、`frontend/src/components/stock-analysis/StockReviewDialog.tsx`(天数可受控); 新增 `backend/tests/test_preview_hero.py`(5 条: 新头部在旧顶栏之前 / 天数头部与复盘页共用 / 切股复位 / AI 入口防连点与两个占位按钮 / 方框不叠冲突内边距); **后端业务代码、打分系统、`strategy/`、六态 diff 全空**; 变异 10 个全杀; 后端 4523 passed、vitest 155、tsc / eslint / build 0; 亮暗两套出图核对排版、空心与实心星、点 60 日后头部与复盘页同步 | 低 | 可(删掉弹窗里 `<PreviewHero>` 那一段即回到旧顶栏) |
