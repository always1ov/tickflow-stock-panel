# R422 — 量化MACD 副图降到主图的 40%

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R422 | 用户: 「副图调得太高了, 你要合理调啊」。R419 给的是主图的 55%、限 180~300, 最大化时副图 300px 快赶上主图一半, 看盘重心被拉走。改为主图的 40%、限 130~200(个股预览 364 → 146, 最大化 564 → 200, 窄屏 → 130); **主图高度不变**(仍与 R415 之前逐像素相同), 整张图随之变短, 弹窗滚动距离跟着缩。 | 改 `frontend/src/lib/levelsChartLayout.ts`、`.test.ts`(加「副图不超过主图 40%」);**后端、打分系统、`strategy/`、六态 diff 全空**;全量前端 154 passed | 低 | 可(改回 0.55 / 180~300) |
