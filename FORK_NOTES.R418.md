# R418 — 量化MACD 副图: 按「放大时看得清」定尺寸

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R418 | 用户: 「显示部分你可以自己决定多大合适, 只要我放大的时候看得清楚就行, 因为我显示的 K 线多, 全景的时候像素是肯定不够的」。按这个标准定了四件事: ① **副图高度**从写死 90px 改成整张图的 1/4、最少 100 最多 180(`subPaneHeight`; 个股预览 520 → 130, 最大化 720 → 180)—— 通达信那张副图约为原来的 3 倍高, 空心框、黄柱、箭头挤在 90px 里, 放大后横向够了纵向仍扁; 上限免得主图被挤得太矮; ② **柱宽仍是间距的 66%**, 随缩放变宽, 全景时最少 1px(DIFF 那一截与 DEA / 黄柱同一个下限), 不会整根消失; ③ **图标固定 13×18px**(与通达信一样不随缩放变), 任何放大程度都看得清; ④ **亮色主题下这张副图铺通达信的黑底**(`QUANT_MACD_COLORS.paneBg`)—— 颜色一个不改(R416 定的), 纯黄在白底上对比度只有 1.0, 放多大都看不见, 改的是它脚下的底; 副图名字在黑底上改用浅灰。**不加背景网格线**: 通达信副图里那几条暗红点线是它窗口自带的网格, 不是公式画的。 | 改 `frontend/src/lib/quantMacdSeries.ts`(`subPaneHeight`、最小宽度)、`.test.ts`(16 条)、`lib/theme.ts`(`paneBg`)、`components/stock-analysis/AnalysisKChart.tsx`(副图高度、亮色黑底、名字颜色);**后端、打分系统、`strategy/`、六态 diff 全空**;变异 5 个全杀;tsc 0、eslint 0 error、vitest 144 passed、vite build 0;亮 / 暗主题各在默认视图与滚轮放大后出图核对 | 低 | 可(回到 R417: 副图 90px、亮色白底) |
