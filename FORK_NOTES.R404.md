# R404 — 「斐波那契」组改名为「斐波那契一型」;名词表学会了带上下文的别名

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R404 | 为加「斐波那契二型」(帝纳波利点位, 下一轮做)先腾名字。用户: 「新按键叫做斐波那契二型, 原本的斐波那契改成斐波那契一型」。**两套都是回撤位, 算法完全不同**: 一型是近 120 日里**一个**波段(窗口内最高/最低)的 0.236~0.786; 二型是推进段最高点配最多 5 个反应点、**每一对**都算 38.2%/61.8%, 价值在于哪几条挤在一起。留一个光秃秃的「斐波那契」在界面上, 读的人不知道指哪一套。渲染文本一共只有两处(后端 `LEVEL_TYPES` 的组名 + 前端开关的 label), 且**全仓库没有一处按字面量比对这个名字**(查过), 所以改名不会牵动任何逻辑。**按规则 12, 改名必须同时加一行名词表 —— 而这一行加不进去**: 那张表是**纯子串匹配**(`if w in v`), 而正名「斐波那契一型」**自己就含着**旧名「斐波那契」, 写成字符串别名会把正名判成违规, 这条改名根本登记不上。所以给 `find_hits` 加了第二条路: 别名可以是**编译好的正则**, 于是能写 `斐波那契(?![一二]型)` —— 只抓光秃秃的那个。**这一路是被逼出来的, 不是图花哨**, 而且以后还会再用到: 凡是"新名字里含着旧名字"的改名都是这个形状。**变异测试当场抓到这条新路有个安静的坏法**: 把 `pat.search(v)` 写回纯子串之后, 拿去比的就成了**正则的源码文本**(`斐波那契(?![一二]型)`), 那串字哪儿都不会出现 —— 一条命中都没有, 整组**全绿**。守卫不是报错, 是**不吭声了**。补了 `test_R404_正则别名这一路真的在查`: 喂一个已知一定命中的正则进去, 它必须真抓到; 再反过来确认正名不会被自己的别名规则抓到。**7 个变异逐条确认会红**(组名退回光秃秃的 / 前端开关退回 / 别处新写一个 / 正名整个消失 / 别名退回字符串 / 正则那一路断掉)。**中途撤销了一条改名**: 用户先说「已经有的止损线更新为保命止损线」, 我按字面改了 `position_exit` 的 `risk` 档; 随后他补一句「生命线就是保命止损线」—— 而那两条在系统里是**不同的两档线**(`risk` = 成本−k×ATR; `fatal` = MA20 跌破无条件清仓), 所以我把改动退回并把两种理解连同一个硬障碍一起摆给他: **「生命线」在渲染文本里有 23 处, 其中 3 处在逐字节冻结的 `opportunity_score.py` 里** —— 一旦加名词表禁掉旧名, 那三处当场判违规, 而唯一的消解办法是去改不许动的文件(与 R393「今日总览」被记进排除表是同一个形状)。用户看完回「生命线是一定有的, 作者的那些命名也是不变的, 这个新指标遇到了冲突你可以新命名」, 再补「保命止损线要是不行就不搞」。**最终: 生命线、止损线、作者的命名一个不动**; 二型那条止损改叫**「失效位」**(跌破它这套回撤不成立, 比叫止损更准, 且不与任何现有名字相撞)。 | 改 `backend/app/indicators/levels.py`(`LEVEL_TYPES` 组名)、`frontend/src/components/stock-analysis/AnalysisKChart.tsx`(开关 label)、`backend/tests/test_terminology.py`(`find_hits` 支持正则别名 + 新增一行 TERMS + 新增一条自校验);**`opportunity_score.py` diff 全空**(打分冻结未动);**`position_exit.py` diff 全空**(生命线/止损线一个字没改);**`backend/app/strategy/` diff 全空**;后端 pytest **4387 passed / 10 skipped 退出码 0**, 前端 tsc 0 / eslint 0 error / build 退出码 0 / vitest 107 passed | 低(一个组名, 两处渲染文本; 无逻辑依赖, 无字面量比对) | 可(两处 label 改回去 + 撤掉 TERMS 那一行即可; `find_hits` 的正则支持向后兼容, 留着不影响任何现有别名) |

**为什么先做这一步而不是直接写二型**: 二型一上图就要在开关条上写字, 那时候旁边杵着一个叫「斐波那契」的旧组, 两个组名立刻就分不清。名字得先定下来。

**下一轮(斐波那契二型)已经商定的范围**, 记在这儿免得走样:

- **一个开关**, 按下去整幅画面出来, 再按消失, 能和别的指标叠着看 —— 和现有 12 个开关同样的用法。
- 画: 回撤线(浅/深)、目标一二三、失效位、短期均线(DMA3×3)、「单边上攻 N 天」底色、强支撑区金色横带、「首次回踩」▼、未来区。
- **不画**: 决策卡、进度条、交易计划表、条件清单、信号列表页。
- **共振由用户人工判断** —— 组内几条回撤线重合(强支撑区怎么定)要算, 那是帝纳波利自己的定义; 跟六态/量化通道等**别的指标**撞不撞, 代码一句结论都不出。
- **不进把握分、不产生提醒、不碰六态、不碰模拟盘。** 几何量自 R229 起整层不进把握分, 界面上那句话得一直是真的。
- 参数先用规格默认值(`PIVOT_K=3` / `THRUST_MIN=8` / 推进≥3×ATR / `MAX_REACTIONS=5`), 用户看图说话再调; **不做回测校准**。
- 摆点识别要单独做成可验证的纯函数配手工用例 —— 规格作者自己写明「原书对『有意义的摆点』没有硬公式」, 这一层认错一个 FOCUS, 上面所有线全错。
