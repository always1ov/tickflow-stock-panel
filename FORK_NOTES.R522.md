# R522 — 策略页整改: 策略卡统一成一颗芯片, 工具条收成一行, 实现细节不再展示

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|---|---|---|---|
| R522 | 用户:「策略页面也要整改」, 看过方案图后「确认」。这一页要看的是「哪个策略今天选出了谁」, 原来策略卡(标准档)14 个占三行 150px、工具条两行 11 个控件, 结果表从 335px 才开始。① 策略卡四档(隐藏 / 紧凑 / 标准 / 详细)统一成一颗芯片: 名字 · 命中数 · 今日失效数(灰), 描述进悬停, 「内置」不再每颗印一遍(只给 AI / 自定义 / 叠加标), 命中数原来的琥珀→橙渐变字改成前景色(全站黑白主题后它是芯片上唯一的彩字), 监控天线与设置齿轮仍在芯片上; 手机两列网格(R398 铺满一格、右缘不锯齿的立论照旧)。四档卡片登记进 docs/hidden-features.md「二、撤下」, 偏好 screenerCardSize 删掉。② 工具条 11 → 8: 周期筛选(全部 / 日线 / 分钟)与卡片尺寸两组开关撤掉(周期值固定「全部」, 仍是 run_all 分日线 / 分钟两路的钥匙, 两路都跑); 重载 / 叠加策略 / 创建策略·AI / 默认参数只留图标(文字进 sr-only, 悬停有名字); 股票/ETF、日期、全部命中、策略池照旧。③ 三处实现细节撤下: 页头副标题「基于本地 enriched 表 · 毫秒级 SQL」、结果头「· N 策略 · 共 M 只」(每颗芯片已写着自己的命中数)与「29.7 ms」。④ 手机: 工具条 5 行 → 2 行; 结果头 flex-wrap, 标题不再被按钮挤成竖排。效果: 1440 结果表 335px → 225px 开始, 一屏 14 行 → 17 行。数据、结果表的列 / 排序 / 筛选 / 加自选 / 列配置、各弹窗、盘后跑批与分钟单跑逻辑一个没动。守卫: 新增 test_screener_r522; test_mobile_card_columns 的锚从 cs.card 改成芯片根元素; StrategyCard.test 去掉 cardSize 与 mini 那条; 设计规范棘轮 裸圆角 700→687 / 硬编码色 675→666 / 任意容器宽 41→38 | frontend/src/pages/Screener.tsx; frontend/src/components/screener/StrategyCard.tsx; frontend/src/components/screener/StrategyCard.test.tsx; frontend/src/lib/storage.ts; docs/hidden-features.md; backend/tests/test_screener_r522.py(新); backend/tests/test_mobile_card_columns.py; backend/tests/test_design_spec.py | 中: Screener.tsx / StrategyCard.tsx 有上游底子, 同步时工具条与卡片可能冲突, 以「一颗芯片、一行工具条」为准重放 | 可以: git revert 本提交 |
