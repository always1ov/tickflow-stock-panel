# R435 — AI 信号整套停用(待以后用斐波那契二型重做)

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R435 | 用户先问「ai 信号给出的买卖点都是 ai 生成的吗, 这列删除对系统有没有影响」, 看过影响清单后: 「清除了ai信号这部分, 后续我打算用斐波那契二型重做这部分, 但现在不做」; 三条答复全按推荐 —— 整套停掉 / 持仓档位的「加仓」不再出现 / 那一列头一行三个入口一起去掉。**用户明确授权动 AI 分析系统里的 `api/today.py`**(硬约束第 7 条那一组)。撤掉的: 后端 `services/stock_signal.py` 整个模块、`GET /api/stock-analysis/signals` 与 `POST /signal/{symbol}`、个股 AI 信号批量定时(job、`signal_needs_refresh` 增量判据、设置接口 `/preferences/signal-ai-schedule`、偏好读写函数); 今日总览里 AI 信号**从源头断成空**(`signals = {}`), 于是候选路 B「逼近买入触发价」没有候选、「AI 看多 / 看空」注记撤掉、持仓档位去掉「AI 转看空 → 减仓」与整个「加仓」分支(加仓原要求 AI 看多, 用户选不拿剩下两条凑一个更易触发的加仓); 「怎么办」收敛层去掉 AI 输入与三条 AI 打架规则(① 趋势 vs AI、③ 阶段 vs AI 两条)。前端: 决策台「AI 信号」列(含报告胶囊 / 行内 ✨ / 🔔 三个入口、到价预案、`signal` 排序键)、「AI 分析持有 / 全部」两个批量按钮、`lib/signalFreshness.ts`、今日总览设置里的「定时个股信号」、导出的「置信 / AI 信号」两列与空了的「AI」组、价位提醒里优先读 AI 预案那一段(改为只剩规则版: 上方最近压力 + 下方最近支撑)、api / queryKeys 里的对应方法与键。列宽按「判断那一列最宽」重分: 18/10/34/12/26 → 22/12/48/18。**留着的**: 候选路 B 的代码按「信号槽位」原样留, 注明眼下恒空、留给二型重做时接; `user_data/signals.json` 与偏好文件里的旧键不删(回退时数据还在), 体检注册表照旧认得它(标「已停用」); 弹窗里的 AI 四维分析入口、页头的点位提醒不受影响; 打分系统、出手时机、焦点名单、台账一个字没动。顺带修掉三处说假话的注释 / 文档: `levels.py` 与 R309 守卫里列着的「AI 信号里的持仓上下文」「`paper_trader_run` 还在读信号」, CONTEXT.md 与 `docs/rule-layers.md` 的注记清单(并补一句: 停用前 AI 其实在持仓档位里起作用, 不只是注记)。 | 删 `backend/app/services/stock_signal.py`、`frontend/src/lib/signalFreshness.ts`; 改 `backend/app/api/today.py`、`api/stock_analysis.py`、`api/settings.py`、`jobs/daily_pipeline.py`、`services/stock_playbook.py`、`services/preferences.py`、`services/data_doctor.py`、`indicators/levels.py`(注释); 改 `frontend/src/components/stock-analysis/WatchlistDecisionBoard.tsx`、`PriceAlertDialog.tsx`、`decision-board/BoardSkeletonRows.tsx`、`decision-board/ExportColumnsDialog.tsx`、`components/today/TodayControls.tsx`、`lib/api.ts`、`lib/queryKeys.ts`、`lib/decisionBoardExportColumns.ts`、`pages/StockAnalysis.tsx`; 改 `CONTEXT.md`、`docs/rule-layers.md`; 新增 `backend/tests/test_ai_signal_removed.py`(4 条); `test_signal_freshness.py` 改名 `test_scheduled_ai_jobs.py` 并改写(今日总览 AI 定时照守 + 信号一支撤干净的反面); 改写 16 个钉着被删功能的守卫文件(每处留退役注记, 能换主语的换到还在的东西上, 如「表头说的和实际做的一致」换到走势列、「打架盖过逼近」换成趋势 vs 通道档位); 棘轮下调 裸圆角 942 → 941、任意字号 2275 → 2266、硬编码色 1044 → 1022; 变异 10 个全杀; 后端 4531 passed、vitest 166、tsc / eslint / build 0; 亮暗两套出图核对决策台四列 | 中(删模块 + 改今日总览与持仓档位) | 可(撤回这一提交; 旧的 signals.json 与定时偏好都还在盘上) |
