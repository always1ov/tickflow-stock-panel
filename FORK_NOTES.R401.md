# R401 — 市场环境页在手机上「字是断的」:药丸腰斩、标题腰斩、图例压住轴名、汉堡压住控件

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R401 | 用户发来手机截图 + 一句「文字有问题」, 并划了红线:**评分、六态、作者内置指标代码不许动, 其余前端样式随便改**。量出来是四处, 根因只有三个, 全在渲染层。**① 定高药丸 + 允许被压扁 = 字画到药丸外面。** 视图页签(市场环境/情绪周期)、时间范围(1年/2年/全部/自定义)、主线口径(概念/行业)三组药丸都是 `h-6`/`h-7` **定高**, 而它们所在的 flex 行**不许换行**、药丸组又是默认的 `shrink:1` —— 旁边那句说明一长, 药丸被挤窄, 里面的字换行, 盒子却是定高的, 于是**第二个字直接画到药丸外面**。实测 375px 下「市场环境」这个按钮 `scrollHeight 34 > clientHeight 28`, 屏幕上就是截图里那个「市场环」/「境」。修法是**三层一起**(行可换行 / 药丸组 `shrink-0` / 药丸内 `whitespace-nowrap`), 少任何一层都不成立。**第三组药丸是守卫扫出来的, 我照着截图改的时候漏了它** —— 那一组旁边的说明比前两处还长。**② 区块标题被右边的说明腰斩。** 「环境综合分趋势」断成「环境综合」/「分趋势」: `SectionTitle` 里 `hint` 是同一行的兄弟, 这一张图的说明有三小句, 把标题压到只剩 59px(四个字宽)。同一个修法: 标题不许断, 说明可以断, 行可以换行 —— 说明放不下就整块挪到下一行, 不去挤标题。**③ 图例折了行, 而 `grid.top` 没跟着变。** 截图里「涨停」(左轴名)压着「综合分」(图例)、「投机」压着右边的「综合分」(右轴名)。六个图例项在手机宽度上排不下, ECharts 自动折成两行, 而 `grid.top` 写死 36 —— 只够一行, 第二行就画进了绘图区。**桌面上排得下一行, 所以这个毛病只在窄屏出现, 开发机上一眼看不到。** 真浏览器里逐档量过(327px = 375 手机上这张图的可用宽): 默认 `itemWidth=25/itemGap=10` 时「趋势」被挤到第二行 y=29, 而 grid.top=36 —— 三处重叠; 收到紧凑的 `10/6` 之后六项一行(跨度 250/327), **再把 `grid.top` 量到 44 才零重叠** —— 因为要让开的不止图例还有轴名(`yAxis.name` 画在 `grid.top − nameGap` 处, grid.top 太小它就被顶进图例那一行)。36/44/48/52/56 五档都量过, 44 是刚好够的那一档。**④ 页头跟着内容滚, 而汉堡钉死在视口上。** 用户截图里汉堡压着「1年」。R394 给标题块留了 `pl-11` 专门让开那个 `fixed left-3 top-3` 的悬浮汉堡 —— **但页头会滚走, 汉堡不会**, 往下滚一点汉堡就落到页面控件上(实测滚 120px 之后压住「市场环境」页签; 滚 400px 压住「环境综合分趋势」)。**让位让了个寂寞。** 页头改成 `sticky top-0`, 汉堡底下**永远**是那块专门空出来的内边距。**背景必须同时改成不透明**: 内容从粘住的页头底下滑过去, 原来那 5% 透明会让两层字淡淡叠在一起; 而且 `PageShell` 传进来的 `bg-base/95` 经 `cn()` 会盖掉 `PageHeader` 自己写的 `bg-base`, **只改一处不生效** —— 守卫两处都查。**踩了一次 R395 记过的坑**: 在 `right={( … )}` 里写 `{/* */}`, 那是**表达式位置**不是 JSX 子节点位置, 当场把文件编译坏了, `tsc` 逮住; 改成 `//` 并在注释里写明。**守卫 13 条, 变异逐条确认会红**: 源码侧 5 条 pytest(9 个变异全红, 其中「药丸组允许被压扁」那条第一次跑就抓出我漏改的第三组); 图例侧 8 条 vitest, 钉的是**会随内容重新破掉的不变式** —— 今天六项刚好排得下, 明天有人加第七条曲线就又会折行, **而那件事在桌面上看不出来**, 所以必须由测试说。边界也是量出来的: 真浏览器里 7 项仍一行(跨度 292/327)、8 项折行, 与断言一致。**粘住页头影响全部页面, 所以 22 条路由在 375px 上逐条扫过**: 横向溢出全 0、页头全部 `sticky@0`、汉堡底下无一处露出页面内容。**评分 / 六态 / 作者内置指标一个字节没动** —— `backend/app/` 整个 diff 为空。 | 改 `frontend/src/pages/Regime.tsx`(三组药丸 + `SectionTitle` + 两张图的图例与 `grid.top`)、`frontend/src/components/PageHeader.tsx`(粘住 + 不透明)、`frontend/src/components/PageShell.tsx`(`bg-base/95` → `bg-base`);新增 `frontend/src/lib/echartsLegend.ts`(图例排布的纯函数 + 紧凑取值 + 量出来的 `LEGEND_GRID_TOP`)、`frontend/src/lib/echartsLegend.test.ts`(8 条)、`backend/tests/test_regime_mobile_text.py`(5 条);**`backend/app/` diff 全空** —— 打分、六态、`opportunity_score.py`、`strategy/builtin/` 一律未动;后端 pytest **4374 passed / 10 skipped 退出码 0**, 前端 tsc 0 / eslint 0 error / build 退出码 0 / vitest **107 passed / 19 个文件**(R400 时 99) | 中(页头粘住是**全部页面**的行为改变 —— 22 条路由逐条量过, 但它确实改变了所有页面的滚动观感; 图例与药丸两处只影响市场环境一页) | 可(三个文件各自独立; 想只退页头粘住, revert `PageHeader.tsx` + `PageShell.tsx` 两处即可, 市场环境的文字修复不受影响) |

**两件量出来但这一轮没动的**:

- **`/dashboard` 与 `/limit-ladder` 没用 `PageHeader`**(它们各自手搓了页头), 所以这次的粘住对它们不生效。实测这两页滚动后汉堡底下只压到一个空 `div`(没有文字、没有控件), 也就是说**眼下没有可见症状**; 但它们缺的是同一条保障, 哪天那个位置放上控件就会复发。该做的是把这两页也收进 `PageShell` —— 那是版面重排, 不该夹在一次修文字里做。
- **图例还剩一项的余量。** 现在六项、最多七项能排一行, 第八项就会折行并再次画进绘图区。`echartsLegend.test.ts` 把这个边界钉住了, 加曲线的人会当场看到红。

**这一条与 R400 的关系**: R400 立了 `Button`/`Card`/`Field` 三件基础件, 而这一页的药丸**没有迁过去** —— 它们是 `rounded-[5px]` 的分段控件(一组药丸共用一个外框), 与 `Button` 的独立按钮不是一个物种, 硬塞进去会比现在更糟。这一轮只修断字, 该不该立一个 `SegmentedControl` 件留给 3.3 逐页迁移时一起看。
