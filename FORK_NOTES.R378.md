# R378 — 任何一行都能拖进 / 拖出「闲置功能」

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R378 | **「闲置功能」的成员从写死改成用户随时可调**(用户: 「想要所有菜单都可以随时调整, 比如都能拉进拉出闲置里面」)。**两个毛病叠在一起**: ① 成员名单是 `navGroups.ts` 里一个四条路径的**编译期常量**, 谁也搬不动; ② 设置页把顶层与组内**开了两个 `DndContext`** —— 两个上下文互相看不见对方, 从一个里拖出来的东西另一个接不住。所以界面上那几行看着能拖, 实际只能在组内换先后, **拖不出去也拖不进来, 而且不报错**。**归属存哪儿: 不新开键, 让顺序本身说明归属。** `nav_order` 里给分组行配一个收尾哨兵 `group:browse:end`, 「谁是成员」= 夹在 `group:browse` 与它之间的那一段。这么选有三个理由: **不动后端**(`nav_order` 后端只当一串字符串存, 多一个非路径 id 不需要新接口新字段新迁移 —— 而 `group:browse` 本来就已经是非路径 id, 哨兵同一个体例); **顺序与归属不可能打架**(分两处存迟早出现「名单说它在组里、顺序把它排在组外」这种谁也不报错的矛盾, 挤在一条串里这种状态根本表示不出来); **老配置照旧**(存过的顺序里没有哨兵就回落到默认名单, 升级上来一模一样, 头一次拖动才写进哨兵)。渲染侧不用认识哨兵 —— 两处合并菜单走的都是「按 id 查条目, 查不到就跳过」。**空组是单独一条规矩**: 带哨兵但中间为空 = 用户真把它搬空了, **不许**回落到默认名单, 否则他永远搬不空这个组; 而空组时 `SortableContext` 里一个可投放点都不剩, 所以另摆一个 `useDroppable` 投放区 —— 没有它就是「拖得出去再也拖不回来」。**两串仍然分开**(两个 `SortableContext`, 一个 `DndContext`): 并成一串扁的, 拖分组行就不是整块挪了。跨容器那一步落在 `onDragOver` 而不是 `onDragEnd` —— 拖到一半就能看见它已经缩进去了, 松手只是确认; 等松手再落, 整个拖拽过程那一行都待在原处, 看不出会掉进哪一边。**另给一条不用拖的路**: 每行一个「闲置」按钮一键移入/移出。这不是给拖不动的人留的备份, 是**键盘那条路** —— dnd-kit 的键盘拖拽在单个列表里好用, 跨到另一个容器得靠空格加方向键摸落点, 谁也摸不准。分组行不给(它不能钻进自己肚子里)。**顺手修掉一句假话**: 扩展页面那一页的标题写着「把一个外部网页变成『闲置功能』里的一页」—— 外部网页是**一级菜单**, 从来就不在那个分组里。**不只跑了断言, 起 Chromium 真拖了一遍**: 四条路径逐一在真浏览器里验过(顶层拖进组 / 成员拖回顶层 / 按钮搬空后刷新不复活 / 拖进空投放区), 每一步都回读后端 `nav_order` 对账。中间有两次"失败"是**我的测试脚本错了不是代码错了**: 目标行落在视口底边之外, `mouse.down()` 根本没在元素上, 拖拽压根没开始; 另一次是投放区在拖进去的一瞬间**正确地**卸载了(组不空了), 而我的定位器还在等它。**11 处变异逐一确认会红。** | 改 `lib/navGroups.ts`(`browseMembersOf` + 哨兵, `splitBrowseGroup` 改收名单)、`components/Layout.tsx`(归属按保存的顺序算)、`pages/settings/MenuSettings.tsx`(并成一个 `DndContext` + `onDragOver` 跨容器 + 空组投放区 + 「闲置」列)、`pages/settings/ExtPages.tsx`(一句假标题);新增 `lib/navGroups.test.ts` 12 条;`tests/test_menu_settings_consistency.py` +5 条;**后端 `app/` diff 全空**;后端 pytest **4215 passed / 10 skipped 退出码 0**, 前端 tsc 0 / eslint 0 error / vitest **47 条全过** / build 通过 | 中(菜单归属的存法变了 —— 但对已存过顺序的人是无感的: 没有哨兵就走默认名单, 和以前逐字一样; 判定、打分、六态、模拟盘一个字节没动) | 可(回退后哨兵会被当成不认识的 id 跳过, 归属回到默认名单) |
