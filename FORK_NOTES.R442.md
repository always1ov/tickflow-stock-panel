# R442 — 逐日复盘回到旧的两张表(页签切换), 去掉样式后与旧页逐字一样

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R442 | 用户: 「逐日复盘并没有和之前一模一样」(上一句: 「可以保留样式, 但是内容必须和以前一模一样, 不多也不少」)。R440 只把改写过的几句换回原话, 但 R431 的并表本身就改了内容: 趋势表的「通道档位」列多出「← 换档」和「三档都在中部」(旧表是一道横线)、这一列从最后挪到了动作前面、两套动作挤进同一格并多出「六态 / 通道」小字、行高亮与两个筛选按两套取并集。改成旧复盘页的样子: 「趋势状态」「通道档位」两个页签各一张逐日表, 列、每格内容、筛选开关(趋势页的开关切页签不丢、通道页的切走复位, 与旧页一样)、空表那句、脚注、页头「起 ~ 止 · N 个交易日 · 六态阈值 N%」(阈值只在趋势页)全部照抄旧页; 动作三格直接用旧表同一个 `FlipTradeCells`; 先看哪一页跟着入口(`reviewTab`), 换票复位(`key={symbol}`), 都与旧页一样。只换了样式(字号、圆角、间距、表头底色、开关用新块的 PILL)。守卫 `test_R442_逐日复盘是旧两张表_去掉样式后逐字一样`: 新旧两张表连同各自的开关与脚注, 去掉所有 className 后逐字比对; 并表时代的三条守卫与 R440 那条撤掉, `test_R293_每一笔只有一份写法` 改钉新表用 `FlipTradeCells`。变异 10/10 杀死 | `frontend/src/components/stock-preview/ReviewSection.tsx`、`frontend/src/components/StockPreviewDialog.tsx`、`backend/tests/test_review_section.py`、`backend/tests/test_flip_trades.py` | 低(只在 fork 自己的新块里) | 回退本提交即回到 R440 的一张并表 |
