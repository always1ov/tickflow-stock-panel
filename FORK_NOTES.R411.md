# R411 — 斐波那契二型:把图上那四样串成一句话(只报事实, 一个动词都不出)

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R411 | 用户: 「要串起来, 你内部可以表示 f3f5cop 这些, 但是前端要显示通俗易懂一点」。**起因是聊出来的一件事**: 图上那四个部件(上攻段底色 / ▼首次回踩 / 回踩位与密集带 / 这组作废)**其实是一个有先后顺序的形态**, 不是四样零件 —— 推进发生过 → 回撤开始了 → 可能停在哪 → 停不住就别看了。但图上是按"位置"摆的, 顺序要用户自己在脑子里拼, 而那一步本不该存在。**边界先问后做**: 「做不做/走不走」由图来答就是新增一档判定, 与 R405 定的「只有位置没有动作」和用户那句「我自己人工判断」相反, 也踩中硬约束第 14 条; 用户明确选了**「只报形态走到哪一步」**, 于是这一行**一个动词都没有**(守卫逐条扫「买/卖/可以/应该/建议/进场/离场/止盈/止损」)。**而且它根本出不了"卖出"那一侧** —— 二型的卖出逻辑是那三条推算位, 那半套与「趋势没坏就不给卖出理由」冲突已经定了不用; 本仓库的卖出口径是 生命线 / 止盈线 / 六态转弱, 与二型毫无关系, 所以这行字里若出现「卖出」, 要么是假的, 要么就得把砍掉的半套接回来。**黑话全留在代码里**: F3/F5/COP/OP/XOP/FOCUS 一个都不进返回值, 对照表写在模块头(免得下一个人去翻原书)。**冒烟当场抓到一个真问题**: 第一版在上攻段**还在走**的时候, 一边说「这组位置每天都在变」、一边接着报「下方最近 浅回踩 13.98」—— 推进段没结束时聚焦点每创一次新高就抬一次, 下面所有价位跟着挪, **那个精确到分的数明天就不是它了, 而读的人会把它记住**。既然这一段的结论是"现在还不能用", 就不该同时递出一个看起来能用的价格; 改成说完就停, 并单独钉了一条。**顺带修掉我自己刚犯的撞名**: 正名是「回踩密集带」「这组作废」(R410 刚进的名词表), 我在这行字里写成了「密集带」「作废线」—— 同一个东西两个名字, 正是上一轮才立的规矩; 现在逐字一致, 并钉了一条专防简写。**措辞只有一个产地**(`lib/fib2Status.ts` 返回成品短语, 组件只负责用「·」摆开)—— 措辞散在两处必然漂移, R277/R278 栽过两次。**读的是整档而不是画出来的那几条**: R410 的减线藏掉的线不该让这句话跟着变, 它说的是形态, 不是"图上现在画了几条"。**守卫 13 条, 11 个变异逐条确认会红。** | 新增 `frontend/src/lib/fib2Status.ts`(纯函数)、`fib2Status.test.ts`(13 条);改 `frontend/src/components/stock-analysis/AnalysisKChart.tsx`(算这一行 + 开关开启时渲染一条字);**后端一个字没动**;**打分系统 / `strategy/` / `keltner*` / 六态 / `position_exit` / `today*` diff 全空**;前端 tsc 0 / eslint 0 error / build 退出码 0 / vitest **128 passed**;后端 pytest **4462 passed / 4 failed / 10 skipped** —— **那 4 个与本轮无关**, 在干净的 HEAD 上一模一样红(见下) | 低(纯新增一行只读文字, 不改任何线、任何数、任何判定) | 可(删 `fib2Status.*` + 撤组件里那一段 JSX 与一个 useMemo) |

**后端那 4 个失败不是这一轮弄的, 但它是个真问题**:

`test_abnormal_moves`(2 条)与 `test_kline_live_candle`(2 条)**在 `git stash` 之后、干净
HEAD 上一样红**。根因是**时间依赖**: 测试用 `date.today()`(容器是 UTC), 而被测代码走
北京日期 —— 跑在 UTC 16:00 之后(北京次日 00:00 之后)这两边就差一天。

**也就是说这套测试每天到了那个时段会自己变红**, 而白天跑全绿。这类"看时钟的测试"
比测错更坏: 它让人习惯性忽略红色。修法是让测试与被测代码共用同一个"今天"的口径,
但那在异动监控/K 线模块, 与本轮无关, **单独一轮修**, 记在这儿免得下一个人以为是自己
弄坏的。

**这一行现在长什么样**(拿后端真算出来的数据跑出来的):

```
上攻 10 天 · 还在走          ·  这组位置每天都在变
上攻 12 天已结束             ·  回踩第 6 天   ·  下方最近 浅回踩 13.98（-11.5%）
上攻 12 天已结束             ·  回踩第 10 天  ·  现价在回踩密集带内（2 条挤在一起）
上攻 12 天已结束             ·  回踩第 14 天  ·  已跌穿回踩密集带 · 这组作废 11.57（-4.4%）
上攻 12 天已结束             ·  回踩第 24 天  ·  已跌破这组作废 11.57
```

**没有推进段时整条不显示** —— 那时候图上本来也一条线都没有, 硬挤一句「暂无形态」
只是多一行要读的字。**「没有重合」是要说的**: 那一波上涨底下没有结构支撑, 真回踩了
不知道停哪 —— 这句话让人**少动一次**, 所以值得占一格。
