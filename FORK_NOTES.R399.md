# R399 — 设计规范层:方向 A「仪器盘」的变量底座 + 只许收紧的棘轮

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R399 | 用户: 「风格不统一、排版乱、层次不清、三端适配也不好, 整体不伦不类」, 要求按**诊断 → 定方向 → 统一重做**三阶段整理。第一阶段量出现状(圆角 10 种以上、字号 16 种、硬编码色 1071 处、**995 个 `<button>` 零个共用组件**、阴影 8 种、容器宽 12 种以上、5 个断点混用); 第二阶段给了三个方向, 用户选 **A 仪器盘(TradingView)**, 并追加「亮色也要逐项打磨」。这一条是第三阶段的**第一批: 规范层**。**颜色一层一个值都没动** —— 那一层本来就是对的(对比度逐个标注过, `--bull`/`--bear` 是用户逐值指定并冻结的)。缺的是另外五样, 此前**完全没有变量层**, 所以才长出那些变体。**新增**: 容器宽三档(`--w-full/wide/read`)、**两套间距刻度**、字号五档(每档差 ≥2px、用途唯一, 最密一档落在 11px)、圆角三档(4/6/10)、`--shadow-flat`。**两套间距刻度是我对用户规格的一处有意偏离, 理由已当面说明**: 用户要求只用 8/16/24/32/48/64, 那套用在页面骨架上完全正确, 但套进数据表行内会把 100+ 行的自选拉长约 40%、一屏少看十几行 —— 而「看盘的人要的是稳定可扫读」是这个项目的既定纪律。所以骨架走 `--space-*`(8 基), 行内密度另有 `--gap-*`(2/4/6/8), **都进变量、都不许硬编码**, 守卫分开管。**Tailwind 的接法分两种**: 圆角**直接改指向**(246 处 `rounded-card`、782 处 `rounded-btn` 立刻跟着变 —— 圆角是方向 A 最便宜也最见效的一步, 改回去就能还原, 不像字号/间距那样牵动行高与密度); 字号与间距是**新增语义档, 旧档原样保留** —— 一次性改掉 1012 处 `text-xs` 既没法验证也没法回退, 留给 3.3 逐页迁移。阴影把静态面拍平(`sm`/`DEFAULT`/`card` → `--shadow-flat`), 浮层那三档原样不动。**守卫用棘轮写法**: 把当前越界数记为上限(裸圆角 2305 / 任意字号 2336 / 硬编码色 1071 / 任意容器宽 44), **只许降不许升** —— 迁移让数字往下走, 新写的越界会让它往上走当场红; 降下去之后还会**主动失败并要求把基线调低**, 否则这一批迁移的成果不会被锁住。**为什么不是"一处越界都不许有"**: 那样这条从第一天起就是红的, 只能被注释掉, 等于没有。**顺带扩了一条既有守卫**: `test_every_theme_token_has_an_explicit_dark_definition` 要求每个 token 都有暗色定义, 而新加的**度量类** token 必须两套模式共用 —— 用户既然要求亮色也逐项打磨, **两套主题就只该差在颜色上**; 允许间距/字号各写一份, 亮暗的密度会慢慢漂而没有任何东西会报错。按**前缀**豁免度量类(列名字的话每加一档就要回来补一行, 迟早漏), `--shadow-flat` 按**确切名字**豁免(其余阴影是亮暗各一份的, 前缀豁免会把它们一起放过)。**6 + 2 处变异逐一确认会红。** | 改 `frontend/src/index.css`(新增规范变量块, **颜色区一个值未动**)、`frontend/tailwind.config.ts`(圆角改指向 + 新增字号五档/两套间距/容器宽三档 + 静态面拍平)、`backend/tests/test_dark_theme_tokens.py`(豁免规则按前缀扩展);新增 `backend/tests/test_design_spec.py`(9 条);**后端 `app/` 一个字没动**;后端 pytest 4363 passed / 10 skipped 退出码 0, 前端 tsc 0 / eslint 0 error / build 退出码 0 / vitest 89 passed;亮暗 × 375/768/1280 六种组合实测横向溢出全为 0 | 中(圆角是全站立刻生效的; 字号与间距是新增档位, 现有页面零影响) | 可(圆角改回 `card:'14px' btn:'10px' input:'8px' dialog:'18px'` 即还原) |

**这一批之后还剩什么**(第三阶段的其余部分):

| 批次 | 内容 | 体量 |
|---|---|---|
| 3.2 | `Button` / `Card` / `Field` 三个基础组件, 把 995 处手写按钮收口 | 大 |
| 3.3 | 逐页迁移到规范档位(字号、间距、裸圆角、硬编码色), 按常用度排序 | 很大, 分页进行 |
| 3.4 | 导航与 IA 重排(主功能置顶、同类合并、辅助进页脚)+ 全页唯一主按钮 | 中 |

**棘轮的四个数字就是 3.3 的进度条** —— 每迁完一页就该往下调一次。
