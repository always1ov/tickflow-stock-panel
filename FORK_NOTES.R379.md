# R379 — 换皮肤:WavMint 暖白＋靛蓝(第一层:令牌)

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R379 | **把界面铬色换成用户给的那份 WavMint 视觉设计包**(用户: 「我这里有一份视觉设计前端文件, 你看看怎么使用上, 我觉得现在的前端太像一个后台系统」)。**先立回退点再动手**(用户要求): 分支 `backup/pre-visual-refresh` 钉在 `30f8a3c5`, 已推到远端, `git reset --hard origin/backup/pre-visual-refresh` 一条命令回到改之前。**这一轮只动令牌层, 不动任何组件的版面**: R317 立下的规矩是「`index.css` 是设计语言的唯一产地」, 所以换的是那里的值 + `tailwind.config.ts` 的圆角刻度 + `lib/theme.ts` 那份图表手抄件, 组件里一千多处 `text-bull`/`bg-elevated` 一个字没动。**换了什么**: 页底 #F6F8FC→**#F6F7FB**(工作台冷灰); 侧栏 #F8FAFC→**#FFFFFF**(白侧栏 —— WavMint 工作台的分界不是「侧栏更灰」而是「页底更灰」, 分界由页底那条缝给出, 侧栏因此显得轻); 边框 #E4E7EC→**#E7EAF2**, 输入框边框 #D0D5DD→**#DCE0EF**(都带蓝灰倾向); 文字三级 #101828/#475467/#667085→**#222738/#596376/#6B7488**(手册 §1.2「主要文字深墨蓝黑, 不用通篇纯黑」); 主题色 #2563EB→**#4F5DE8 靛蓝**, 悬停 #1D4ED8→**#3E49CE**, 选中底 #EFF6FF→**#EFF1FD**。**圆角整体上一档**(card 12→14, btn 8→10, dialog 16→18; input 留 8 —— 密集表单里再大会显得肉), 照手册 §8.4 的分层, 级差仍是 1.3~1.4。**亮色阴影改成更长更淡**(手册 §8.5: 边框优先, 真要投影就大模糊低不透明, 它给的弹窗值是 100px 模糊 20% 不透明)—— 短而实的投影正是后台系统的观感。**侧栏选中态从「灰底 + 左侧竖条」换成同色系药丸**(浅靛蓝底 + 靛蓝字 + 加粗, 手册 §6.3), 三处选中态一起换、两条竖条一起删 —— 药丸自己画出了范围, 再叠一条竖线是两套指示。**用户中途补的那句话决定了这次的边界**: 「我这个是炒股系统, 所以一些指标显示颜色要对要注意」。所以**指标色一个都没动**: `--bull` #D92D20 / `--bear` #15803D / `--warning` #B54708 / `--danger`, 以及 R317 锚在它们身上的红族与翠族色阶、K 线那两个共用常量 #C74040/#2D9B65 —— 全部原样。守卫里那张 `PALETTE` 表因此**拆成两张**: `CHROME`(跟着皮肤走)与 `MARKET`(冻结), 并写清楚两者来自两次不同的要求、改动权限也不同。**新加三条守卫钉「这次不许动的东西」**: ① 三个语义 token 逐字对上冻结值且 `--danger` 仍等于 `--bull`; ② K 线那两个常量没被顺手改、多日分时与它没脱钩(canvas 不吃 CSS 变量, 这类常量换皮肤时既不会跟着变也没人拦着); ③ **主题色色相必须离涨跌 40° 以上** —— 钉的是以后: 下次换皮肤要是挑了个偏红或偏绿的主题色, 界面上就会出现「不表示涨跌、但看着像涨跌」的色块, 那是颜色在说假话, 没有任何东西会报错。**数都是算出来的不是估的**: 三级文字白底对比 14.84 / 6.05 / 4.69(都过 4.5), 相邻 ΔL 0.222 / 0.060; 主题色对白 5.17。**一条 R317 不变量照旧没达标, 数字照实写在注释里**: `--elevated` 与页底 ΔL 0.0326 < 0.046 —— 撞的是和 R368 同一堵墙(浅端 L 带太窄, 要凑够就得深过边框, hover 行会看着像画了个框)。**顺带**: 档位标签里那一档蓝与引导页品牌色跟着换成靛蓝(它们代表「强调」不是涨跌, 留在旧蓝会两种蓝并排); 侧栏那条守卫的 docstring 改写 —— 它钉的从来不是「两个颜色不一样」而是「侧栏走自己那个令牌」。**起 Chromium 真看了三页**(模拟盘 / 自选 / 菜单设置), 不是只跑断言。**7 处变异逐一确认会红。** | 改 `src/index.css`(亮色令牌 + 阴影)、`tailwind.config.ts`(圆角)、`lib/theme.ts`(图表铬色手抄件)、`components/Layout.tsx`(选中态药丸 + 删两条竖条)、`lib/capability-labels.tsx`、`pages/Onboarding.tsx`;`tests/test_theme_palette.py` 表拆两张 + 3 条新守卫;**`backend/app` diff 全空**;**`html.dark` 那一段一个字没动**;后端 pytest **4218 passed / 10 skipped 退出码 0**, 前端 tsc 0 / eslint 0 error / vitest 47 条全过 / build 通过 | 中(全站观感变了 —— 但判定、打分、六态、模拟盘、指标色一个字节没动, 版面与密度也没动) | 可(`git reset --hard origin/backup/pre-visual-refresh`, 或单独 revert 本次提交) |
