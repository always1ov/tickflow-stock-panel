# R377 — 补上 R368 两条守卫漏掉的那一半

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R377 | **两条 R368 守卫各漏掉一半, 补齐判据并清掉它们本该拦住的东西。** 起因是 R376 合上游 v0.3.0 时, 新带进来的 AI 助手悬浮球带着一圈写死成颜色字面量的辉光阴影, 而「全站没有发光」那条守卫是**绿的**。查下去发现两条守卫都在犯同一个毛病 —— **扫的是「我当初想到的那一种写法」, 不是立论**。**① 发光**: 判据写的是 `shadow-\[0_0_…\]`, 只认**正中无偏移**的光晕; 带偏移的辉光(`0_8px_24px_rgba(…)`、`0_6px_24px_-10px_rgba(…)`)整个漏网, 仓里蹲着 6 处。立论其实是**阴影里不许出现写死的颜色** —— 辉光之所以是辉光在于它带色, 而写死的色值同时还绕开了 `--accent`/`--border` 那套令牌, 换主题不跟着变。判据改成「任意值阴影里含颜色字面量」, 位移模糊随便写, **中性黑 `rgba(0,0,0,…)` 的抬升阴影照旧放行**(那是层次不是发光), 走令牌的 `oklch(var(--accent)/…)` 也放行。**② 蓝紫渐变**: 判据要求 `bg-gradient` 与 `from-…/to-…` **写在同一行**, 而那两个 AI 气泡把方向类放在 `className` 上、把 `from-purple-500/25 to-fuchsia-500/20` 放在另一行的 `accent` 变量里 —— 一整条紫→品红渐变从守卫底下走过去了。色阶类本来就常被抽成变量, 判据改成**单看色阶类**, 不管方向类在不在同一行。**清掉的**: AI 助手悬浮球的蓝色辉光;两个 AI 气泡(财务分析 / 个股分析)的跨色相渐变 + 辉光 + 照深色底调的 `-300` 档字色;回测「开始回测」按钮那两圈蓝辉光;以及 R368 换平色时**留下的 5 处死类**(元素上早就没有 `bg-gradient-to-*` 了, `hover:from-purple-500/30` 这些不产生任何渲染, 删掉零视觉变化)。**两个气泡的三档配色重排**: 生成中=主题蓝, 失败=**警示琥珀**(不能用红 —— 这仓库里红是「涨」), 完成=**中性一档**(不能用绿 —— 绿是「跌」, 禁令里还有「不用大面积绿色」); 三档本来就各带图标(转圈/警示/对勾)与文案, 不只靠颜色认。**查到但没动的一件事, 摆在这儿**: 全仓还有 **25 个文件约 70 处** `purple`/`violet`/`indigo` —— AI 分析弹窗那一族(`AiAnalysisDialog` 13 处、`StockFinancialDetail` 5 处)是**照深色底调的紫**, 白卡片上偏淡; 而 `TrendStateBar`(10 处)、`LimitUpLadder`(8 处)、`FactorGroupNavChart` 这些多半是**分类色/图表色**, 不是界面铬色, 动它们要先定口径。**这不是守卫的漏, 是另一件事** —— 那两条守卫钉的是「渐变」与「发光」, 不是「每一个紫像素」; 要扫要按口径来, 所以留给用户定夺, 没有擅自铺开。**6 处变异逐一确认: 4 红 2 绿** —— 两条「应该放行」的(中性黑抬升阴影、同色相透明度渐变 `from-accent to-accent/30`)确认**没被误伤**, 这类判据放宽之后最容易顺手禁得比该禁的多。 | 改 `tests/test_theme_palette.py`(两条判据各补一半);`components/financials/AiReportBubble.tsx`、`components/stock-analysis/StockAnalysisBubble.tsx`(三档配色换语义令牌 + 去渐变去辉光)、`custom/assistant/ui/AssistantFloatingButton.tsx`(去辉光)、`pages/backtest/StrategyBacktest.tsx`(去辉光)、`components/financials/AiAnalysisDialog.tsx`、`components/financials/StockFinancialDetail.tsx`、`components/stock-analysis/StockAnalysisDialog.tsx`(清死类);**`backend/app` diff 全空**(纯前端 + 测试);后端 pytest **4210 passed / 10 skipped 退出码 0**, 前端 tsc 0 / eslint 0 error / vitest 35 条全过 / build 通过 | 低(只改颜色与阴影; 布局、字段、交互、业务逻辑一个字节没动, 其中 5 处删的还是根本不渲染的死类) | 可 |
