# R440 — 逐日复盘的内容换回旧两张表的原话(样式不动)

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R440 | 用户: 「逐日复盘可以保留样式, 但是内容必须和以前一模一样, 不多也不少」。对照旧复盘页「趋势状态」「通道档位」两张逐日表, R431 并表时改写过的几处全部换回原话: ① 两个开关的悬停(「或六态转折」→「或趋势翻转」;「只留下通道档位换过」→「只留下档位换过」); ② 「动作」表头悬停由合成的一句换成旧两页各自那句叠起来; ③ 筛完一行不剩时, 由「这段时间里没有这类日子」换回旧两页各自的原话(两个开关都开就两句都说); ④ 脚注由合成的「两列同源 ...」换回旧两页脚注原话 —— 旧趋势页脚注后半句「要摊开每一档 ... 切到上方的「通道档位」那一页」不要了, 那一页的逐日内容就在这张表里; ⑤ 「六态阈值 N%」从脚注回到表头那一行, 写法与旧页页头一致。列、行、每格的读数本来就与旧表同源同组件, 没动。并成一张表唯一多出来的是动作三格前的「六态」「通道」两个小字(旧表靠页签区分)。守卫 `test_R440_逐日复盘内容与旧两张表一模一样`: 旧两张表里读得到的每一句新表逐字都要有、R431 改写的那几句不许再出现、表头恰好 8 列; 变异 6/6 杀死 | `frontend/src/components/stock-preview/ReviewSection.tsx`、`backend/tests/test_review_section.py` | 低(只在 fork 自己的新块里) | 回退本提交即回到 R431 的合成文案 |
