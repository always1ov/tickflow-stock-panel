# R517 — 自选页精简: 「按小分队」只留看的, 管理用的收进「一张表」; 分组药丸等长

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|---|---|---|---|
| R517 | 用户:「自选页面也从调整」, 看过方案图后又问「分组药丸条长度能做到一直保持一致吗」, 最后把决定交给我(「等我的事件, 你帮我选择最优解拍板」), 全部照做。原则: 「按小分队」是看的, 「一张表」是管的。按小分队里不再展示: ① 分组药丸条(32 组占三行, 与每块块头是同一份信息); 网格里点块头进某一组后顶上给一条「← 全部 N · 组名」返回; ② 筛选 / 导入 / 自定义列 / 清空四个按钮只在一张表出现(筛选条件两个视图照样生效, 有筛选时重置按钮两边都在); ③ 「小分队顺序」那一行与后面那句操作说明, 顺序开关挪进页头工具条, 说明进提示; ④ 每行名字后面的代码(一行只留名称 + 现价 + 今日涨跌 + 加入以来; 代码在悬停与弹窗里); ⑤ 计数「120/120 只」没筛掉时只写「120 只」。药丸(一张表里): 容器从按内容撑开的 flex-wrap 换成等宽网格 repeat(auto-fill, minmax(9rem, 1fr)), 每颗一样长、列与列对齐, 名字截断悬停看全名, 涨跌靠右。效果: 1440 首屏 6 排小分队 → 8 排; 手机第一只票从 340px 以下提到 180px 左右。守卫: test_watchlist_grid_r508 三处改钉法(代码不印行里但悬停有 / 药丸等宽网格 + 截断 / 手机独占整行的容器类) | frontend/src/pages/Watchlist.tsx; frontend/src/components/WatchlistGroupGrid.tsx; frontend/src/components/WatchlistGroups.tsx; backend/tests/test_watchlist_grid_r508.py | 中: Watchlist.tsx 有上游底子, 同步时工具条那段可能冲突, 以「管理按钮只在一张表」为准重放 | 可以: git revert 本提交 |
