# R508 — 自选页整改: 默认视图换成「按小分队」网格, 三种重复画法撤掉, 默认 5 列

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|---|---|---|---|
| R508 | 用户:「开始整改自选页」。按老规矩先分析后出图: 这一页有 4 种看法(表格 / 卡片 / 分组卡片 / 分组统计条)看的是同一份涨跌幅, 页头 10 个图标按钮一半是视图开关, 默认 9 列里换手 / 量比 / RSI14 / 60D 动量 / 连板 / 信号 是作者的短线筛选指标(用户本机没存过列配置, 看到的正是作者默认); 判断(六态 / 转折 / 档位)在个股分析的决策台上且那里能按分组看, 自选页的职责是管池子 + 扫涨跌 + 点开弹窗。第一版拟改用户看了说「我感觉你改过后还是每个个股占用很多空间」(一屏 14 只), 重出两版密排: A 按小分队网格一屏 69 只 / B 一张密排表一屏 28 只, 用户:「a默认保留切换按钮」。做的: ① 新组件 WatchlistGroupGrid —— 每个分组一小块(组名 · 只数 · 组内等权涨跌), 下面每只一行(名称 + 代码 / 现价 / 涨跌幅 / 加入以来), 1440 宽三列、2xl 四列、手机一列; 块内按今日涨跌降序, 块的顺序可切「分组顺序 / 今日涨跌」(未分组永远垫底), 点一行开个股弹窗, 点组名只看那一组; 吃的是与表格同一份筛选 + 排序后的行, 零额外请求, 且网格下不发日K批量 / 分钟批量两份表格专属请求; ② 页头三个视图开关(卡片视图 / 分组卡片 / 分组统计条)换成一组分段「按小分队 / 一张表」, 视图与块顺序两个偏好记在本地(老值 card 按 grid 算); ③ 撤掉卡片视图 StockCard 及其虚拟滚动、分组卡片 WatchlistGroupCards、分组统计条 WatchlistGroupStatsBar 与它们的设置面板 GroupStatsSettings, lib/watchlistGroupStats 里只为它们服务的指标 / 排序 / 卡片配置一并删, storage 的 watchlistGroupStats 键换成 watchlistGridSort; ④ 分组条: 药丸压矮到 28px 并一律带边框(32 组从 5 行收到 3 行), 折起时限高三行, 溢出才出「全部 N 组」展开按钮, 手机上药丸区独占整行(原来右侧两个按钮把它挤到 230px 宽); ⑤ 默认列 9 → 5: 代码/名称 · 现价 · 涨跌幅 · 加入以来 · 加入日期, 撤下的六列仍注册着可在「列」里勾回, 用户存过列配置的 mergeColumns 保留他自己的显隐; ⑥ watchlist.toolbar 插槽的 viewMode 联合类型只加 grid 不去 card(不破坏已有扩展类型), 二开文档同步。实测(示例数据 120 只 32 组): 网格一屏 54 只可见(1440×900) vs 原表格 14 只; 手机一屏 13 只 vs 原来两列半。表格视图本身(行高、操作列)是作者的, 没动。守卫: 新增 test_watchlist_grid_r508.py 8 条(三种画法撤干净、两个视图与偏好、网格吃同一份行且零请求、每只一行 / 块内排序 / 未分组垫底 / 无装饰动效、算法层判据、分组条折起与药丸、默认 5 列、插槽契约只加不改); vitest watchlist-columns.test 的「加入信息两列默认隐藏」按用户决定改为默认可见并加「默认就是这 5 列」一组; test_ui_hierarchy 文件清单去掉三个已删组件; test_mobile_text_and_scroll 的 R402「分组栏有上限且能滚」改为两档一起钉(折起限高 + 有展开的路, 展开 34vh 自己滚); 删掉的组件带走了不少裸圆角, 设计规范棘轮按守卫要求收紧(裸圆角 715→700); 撤掉的三种视图登记进 docs/hidden-features.md「二、撤下」 | frontend/src/pages/Watchlist.tsx; frontend/src/components/WatchlistGroupGrid.tsx(新增); frontend/src/components/WatchlistGroups.tsx; frontend/src/components/WatchlistGroupCards.tsx(删除); frontend/src/components/WatchlistGroupStatsBar.tsx(删除); frontend/src/components/GroupStatsSettings.tsx(删除); frontend/src/lib/watchlistGroupStats.ts; frontend/src/lib/watchlist-columns.ts; frontend/src/lib/watchlist-columns.test.ts; frontend/src/lib/storage.ts; frontend/src/extensions/types.ts; docs/secondary-development.md; docs/hidden-features.md; backend/tests/test_watchlist_grid_r508.py(新增); backend/tests/test_ui_hierarchy.py; backend/tests/test_mobile_text_and_scroll.py; backend/tests/test_design_spec.py | 中(Watchlist.tsx 是作者的页面: 撤的 StockCard / 虚拟滚动那段是作者的, 上游再改会冲突, 保留本侧; 分组条与分组统计是 fork 的; 默认列改的是作者的注册表, 上游加列不冲突) | 可以: git revert 本提交(视图偏好老值兼容, 回退后原样生效) |
