# R387 — 标识符拼文件名的四处缺口:把「挡穿越」从路由正则收回到各自的 store

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R387 | **用户:「用 cloudflare/security-audit-skill 审查一次我的系统」→「最优解修复」**。审查(run-1, 41 个覆盖单元)挖出**同一个根因的四处落点**: `factors/store.py::_path`、`services/pipeline_jobs.py::JobStore._read_file/_write_file`、`api/analysis.py::_path`、`data_providers/custom/loader.py::plugin_manifest/plugin_dir_of` —— 全都把请求里的标识符直接拼成文件名, 把「挡住穿越」**隐式外包给了路由正则**。而那个正则是 Starlette 的 `[^/]+`, uvicorn 又在匹配前 percent-decode: `%2F` 解码成真斜杠后单段路由不匹配(**POSIX 上安全, 但挡住它的不是这几段代码**), 而 **`%5C` 解码出的反斜杠 `[^/]+` 照收** —— 在 POSIX 上只是普通文件名字符, 在 **Windows 上是路径分隔符**, 而 `release.yml` 的 `platforms` 默认值就是 `windows`, 冻结版 `data_dir` 是 exe 同级的 `data/`, `job_store/`、`custom_factors/` 与 `user_data/`(`secrets.json`、`auth.json`)彼此平级。**同仓本来就有正确写法**: `MiningRunStore._validate_run_id`、`ExtConfigStore._VALID_ID`(注释明写「URL 路径参数的 id 必须过白名单, 防 `../` 穿越删除」)、`loader.save_config/delete_config` 都是「白名单 → resolve → 断言父目录」三件套; 这四处是仅有的例外。`factors/store.py` 更是**自己就声明了 `CUSTOM_ID_PATTERN`/`COMPOSITE_ID_PATTERN`, 却只接在 `to_spec`(新建/更新)上**, `_path`/`delete_one` 拿到的是原始路径参数。**顺带修掉三处同源问题**: ① `api/factors.py` 原本拿 `store.delete_one` **当存在性探测** —— 一个本意是「查有没有」的分支, 副作用是删文件(注释里已经绕过一次这个坑, 现在换成无副作用的 `exists_one`); ② `api/analysis.py` 写入侧用的 `menu_id.replace("_","").isalnum()` 是 **Unicode 感知**的, 全角数字/CJK 能过, 再被更严的 `AnalysisMenu.id` ASCII pattern 在函数体里抛成 **500 而不是 400**, 现在两侧共用同一条 ASCII 规则; ③ `plugin_manifest`/`plugin_dir_of` 的下游是 `yaml.safe_load` + `importlib.import_module(entry.split(":")[0])`, 所以这两个函数本身必须 fail-closed(本条已含); **路由层那道 `is_builtin` 白名单补在 R388** —— `POST /api/settings/plugin-key` 是三个同名兄弟路由里唯一没有它的一个, 而那个文件(`api/settings.py`)这一轮同时还有 R388 的掩码改动, 分不开两次提交, 就放在一起走。**校验点一律收在路径构造函数里, 不散在各调用方** —— 散着放就是这次四处不一致的由来。**新增 9 条守卫**, 每条**同时跑 POSIX 与 Windows 两种 join 语义** —— 只断言「POSIX 上没越界」正是这批 bug 能活下来的原因; 末尾一条用标准库把那个平台事实本身钉住, 免得将来有人看着 POSIX 的行为把白名单拆掉。**变异逐一确认会红**: 拆 `factors` 白名单 → 2 红; 拆 `job_id` 白名单 → 2 红; loader 那两道是**独立两层**, 单拆任一层仍被另一层挡住(拆完两层才红, 已确认非空测)。 | 改 `app/factors/store.py`(`validate_id`/`_path` 收口 + 新增 `exists_one`)、`app/api/factors.py`(先校验; 探测换 `exists_one`)、`app/services/pipeline_jobs.py`(`is_valid_job_id`/`_job_path`, 读写两侧 fail-closed)、`app/api/analysis.py`(`MENU_ID_RE`/`_validate_menu_id`, 读写共用)、`app/data_providers/custom/loader.py`(`_plugin_dir_checked`);新增 `tests/test_path_identifier_guards.py`(9 条);**`app/strategy/builtin/` diff 全空, 冻结的 `opportunity_score.py`/`score_ledger.py`/`strategy/scoring.py` 未动**;**判定、六态、打分、口径、指标色一个没动** | 低(只收紧输入, 合法 id 行为不变) | 可 |

**审查产物**:`~/security-audit-skill/tickflow-stock-panel/run-1/`(`coverage-ledger.json` 41 个单元、`architecture.md`、`candidates.json`)。本轮审查为**纯源码**模式 —— 环境没有技能要求的 OS 级沙箱, 因此不执行目标代码, 所有需要动态验证的结论一律记为 `needs_validation` 并附可安全执行的验证计划, 不伪装成已证实。
