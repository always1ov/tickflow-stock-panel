# R491 — 按个股另存日 K 副本(每只票第一次打开也快)

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|---|---|---|---|
| R491 | 用户:「按个股另存一份日K副本, 做吧」。R490 的内存缓存只管同一只票第二次打开; 每只票第一次打开仍要把一千来个日文件(每个是全市场)逐个打开挑出这一只。新增 services/symbol_daily_store.py: 第一次打开时把这只票的 6 列日 K 写成 data_dir/.symbol_daily_cache/<stock/index/etf>/<代码>.parquet(+ 一个 json 说明), 以后读它一个文件 + 截止日之后的日文件。截止日 = 建副本时倒数第 11 个分区, 建好不挪(否则每进一天就作废), 所以最近 10 天(盘中实时落盘、盘后补收盘价、修复过期分区改的都是这几天)永远现读。失效按内容判断而不是修改时间(盘后管道只要有一只票除权就会重写全部日文件, 按修改时间副本天天作废): 截止日之前的分区数变了(补历史、补缺口、删了某天) → 重建; 抽查副本首尾两根各自现读那天的分区对照 6 个数, 对不上(除权: 前复权改前面、后复权改后面) → 重建; 最多用 7 天到期重建, 兜住首尾都抽不到的中间某天被改(用例里明写了这个盲区)。副本读不了、写不了、坏了、代码不像代码(首字符须字母数字, 整串匹配, 接了仓库现成的穿越样例表) → 退回原来扫分区, 它只是加速不是数据源, 整个目录删了下次自动重建。接进 ohlcv_history: 内存没有时先读副本, 盘中最新一根的覆盖照抄 get_daily 列下推快路径那几行(指数、ETF 快路径本就不覆盖); 空结果交回仓库原路(ETF 还有旧版存在指数目录的兜底)。结果: 模拟库(1000 交易日 × 5000 只)上两个端点 12 份完整输出在建副本、读副本两轮都与改前逐字一致; 每只票取数 原路 ~295 ms → 建副本那次 ~315 ms → 以后 ~20 ms, 与原路逐位一致。新增 test_symbol_daily_store.py 20 条; 变异 17 个全红(含首尾抽查各拆一头、分区数、到期、截止日、现读范围、锁、异常兜底、代码校验、覆盖)。自查时删掉两条冗余判据(截止日落进现读区、最早分区日期): 前者不影响结果, 后者被分区数覆盖, 变异下均无用例能区分 | backend/app/services/symbol_daily_store.py; backend/app/services/ohlcv_history.py; backend/tests/test_symbol_daily_store.py | 低(新文件为主; ohlcv_history.py 只在取数处先试副本) | 可以: git revert 本提交; 已写下的 .symbol_daily_cache 目录可直接删 |
