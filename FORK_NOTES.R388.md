# R388 — 凭据的三件事:落盘权限、响应回显、以及用完不擦的明文口令

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R388 | **承 R387 同一轮安全审查(run-1)的第二批修复**, 三条线都属于「凭据」这一类。**① 落盘权限的机制是假的。** `fs_utils.atomic_write_text` 的注释写着「mode 在替换之前打到临时文件上……先改临时文件就没有这个窗口」——**那句话不成立**: 临时文件是先 `write_text` 写满内容、下一条语句才 chmod, 窗口没消失, 只是从正式路径**挪到了 `.tmp`**, 而 `.tmp` 里躺的是同一份明文(`secrets.json` 是全部三方凭据, `auth.json` 是 PBKDF2 哈希**加上明文的活 session token**)。改成**先以目标权限创建空文件、再写内容**(`os.open` + `O_CREAT｜O_EXCL` 带 mode), 文件从不以比 mode 更宽的权限装着明文; umask 只会削窄不会放宽, 之后补一次 chmod 只是把被削掉的位补回来。**chmod 失败不再静默** —— 一个「要求了 0600 却没拿到」的凭据文件必须留痕。**② 哪些文件算凭据类, 之前分错了。** 全仓只有两处传了 mode(`secrets_store.save/clear`、`auth._save`), 其余一律落到 umask。逐个核对内容后: 策略覆盖、持仓、自定义信号、监控规则、因子定义、分析菜单**确实不含凭据, 不传 mode 是对的**; 错的是 `preferences.json`(存着飞书 HMAC 签名密钥、企微机器人密钥, 以及 `key=`/`access_token=` **本身就是 bearer** 的企微/钉钉 webhook URL, 而模块注释白纸黑字写着「非敏感数据」)和 `ext_data/*/config.json`(`pull.headers` 是自由填写的映射, 而需要 Cookie 或多 header 鉴权的接口**只能**填在那里)。两处补 0600, 并把那句错的注释改掉。`save_ai_profiles` 也从「裸 `write_text` 再 chmod」换回 `atomic_write_text` —— 它是**直接打在正式路径上**的那一版, 且非原子: 中途被 kill 会留半截文件, `load()` 解析失败返 `{}`, 下一次 `save()` 就把其余凭据静默覆盖。`set_realtime_keys_per_round` 那次绕过锁的裸写也收进 `save()`。**③ 明文回显与明文残留。** `GET /api/settings/preferences` 把飞书签名密钥、企微机器人密钥、以及带 token 的企微/钉钉 URL **明文返给浏览器**, 而同一个响应里的兄弟字段(`custom_webhook_secret_set`/`email_smtp_password_set`)只返布尔 —— 统一成「掩码串 + `*_set` 布尔」, 掩码串非空所以只判真假的四个调用方(PriceAlertDialog / RuleEditor / Review / Monitor)行为不变。写回时**掩码回显 = 没改**, 与 AI Key「留空即保持」同一条语义。前端只有 `settings/Monitoring.tsx` 要动, 照**本页既有的写法**(`custom_webhook_secret` / SMTP 密码已经是这个模式)改成只写不读: 草稿从空开始、placeholder 复用同一句「已保存；留空保持不变」、**清除拆成独立按钮** —— 不这么做会有个更糟的坑: 把掩码串塞回输入框, 用户改中间几个字符提交上去**仍带掩码点, 后端判成「没改」而静默忽略**。`ext_pull` 的 `query` 型鉴权把 Key 拼进 URL, 而 httpx 异常消息带完整 URL, 那串东西同时进**响应体 / `config.json` 的 `last_message` / `backend.log` 与容器 stdout** —— 在 `_request_json` 里**单点收口**按值擦除(保留异常类型与 response, 上游按 status_code 做的 429 退避不受影响), 三个出口一次吃掉。`AUTH_PASSWORD` 在 bootstrap 用完后**从不从 `os.environ` 抹掉**, 而 `config.py` 与 `.env.example` 两处都写着「设置后不再读取」——**行为上确实不再读, 但值一直在**, 整个容器生命周期都能从 `/proc/self/environ` 拿到, 回测 worker 用 spawn 继承的也是这份; 现在 bootstrap 结束即 `pop`。**顺带补上 R387 拆不开的那一半**: `POST /api/settings/plugin-key` 是三个同名兄弟路由里**唯一没有 `is_builtin` 白名单**的一个(DELETE `/plugin-key/{name}`、POST/DELETE `/plugins/{name}/install` 都有), 补上; `clear_plugin_key` 的 gate 也从「先读清单再判」挪到「先判再读」。**`GET /api/settings/tickflow-keys` 的明文列出刻意保留不动** —— R58 记着用户原话「明文显示是用户明确要的……遮起来反而让人没法核对是哪一个失效」, 那是已定案的取舍, 改它的可达性(CORS / 免凭据状态)而不是改它的显示。**新增 8 条守卫**, 其中一条钉的是**机制而非结果**(创建时就带 mode, 且 `O_CREAT｜O_EXCL`)—— 只断言「最终 0600」在旧写法下同样成立, 抓不到这个 bug。变异确认: 还原成 write-then-chmod → 2 红; `save_ai_profiles` 还原 → 首次创建那一例红(mode 420)。**仓库原有的三条 torn-write 测试一度变红**, 原因是它们在 `Path.write_text` 上注入故障, 而我第一版把写入换成了 `os.fdopen` ——**那是我把测试的接缝挪了**, 不是行为回归; 改成「空文件带权限创建 + 仍用 `write_text` 写内容」后, 安全性质与故障注入点同时保住, 三条测试自然复绿。 | 改 `app/services/fs_utils.py`、`app/secrets_store.py`、`app/services/preferences.py`、`app/services/ext_data.py`、`app/services/ext_pull.py`、`app/services/auth.py`、`app/api/settings.py`;前端 `src/pages/settings/Monitoring.tsx`、`src/lib/api.ts`;新增 `tests/test_credential_file_modes.py`(8 条);**`app/strategy/builtin/` diff 全空, 冻结的打分文件未动**;**判定、六态、打分、口径、指标色一个没动**;后端 pytest 全过退出码 0, 前端 tsc 0 / eslint 0 error | 中(改了 `/api/settings/preferences` 的回显契约, 前端同批改完; 只判真假的调用方不受影响) | 可 |
