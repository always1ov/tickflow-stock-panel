# R398 — 窄屏上「一行一张」的卡片要铺满那一列,别留锯齿边

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R398 | 用户截图(策略页, 手机)+ 「每个页面里面的细节也要处理…所有页面都要处理」。**症状**: 每张策略卡宽度都不一样, 右边缘是锯齿状 ——「跌破生命线」窄、「相对活力指数转强」几乎顶到边。**原因**: 这些卡是 `inline-flex`, **按各自内容撑宽**。宽屏上一行塞得下好几张、平铺开来看不出问题; 到手机上一行只放得下一张, 每张的宽度差就全暴露了。顺带一个跟着来的毛病: 说明文字写死 `max-w-[120px]`/`[140px]`, 那是给平铺态定的, 卡片铺满整列之后它会把说明**提前截断**, 右边空着一大片却显示省略号。**改**: 卡片 `w-full sm:w-auto`, 说明限宽 `max-w-none sm:max-w-[...]` —— 只在 `sm`(640px)以下变, 宽屏的平铺一个像素不动。**实测**: 手机 390 下 8 张卡**宽度只剩一种**(366 = 390 − 两侧留白), 平板 834 与桌面 1440 仍是 3 种内容宽度(188/182/191), 与改动前相同。**这一轮真正的产出是把这类问题变成可自动发现的**: 写了一个「锯齿列」探测器 —— 同一容器里每个孩子各占一行、宽度却参差超过容器的 12%、且最宽的还没占满, 就是这个毛病的特征。**第一版判据太松**, 26 条路由报出 5 处, 逐个看全是空态说明块(图标 + 标题 + 一段说明, 宽度本来就该不一样); 收紧成「只看**卡片状**的孩子(有可见边框或底色)」之后降到 2 处, 内容仍是空态块与数据源小药丸, **也是误报**。**结论**: 26 条路由在 390px 下, **结构性问题已经全清** —— 无横向溢出、无元素伸出视口、无中文竖排、无锯齿卡片列。剩下的是"好不好看"这类判断, 探测器抓不到。**守卫写坏过一次**: 第三条断言查的是「文件里有没有 `sm:max-w-[120px]`」, 而文件里别处本来就有一个, 于是把某一处退回写死照样绿 —— **全文件查 vs 逐处查**, 改成逐处之后两个退回变异都红了。这是本仓库「扫描面比纪律小」的又一个变体。 | 改 `frontend/src/components/screener/StrategyCard.tsx`(卡片根 className + 两处说明限宽);新增 `backend/tests/test_mobile_card_columns.py`(3 条, 4 处变异逐一确认会红);**后端 `app/` 一个字没动**;**`backend/app/strategy/` 未动**;后端 pytest 4354 passed / 10 skipped 退出码 0, 前端 tsc 0 / eslint 0 error / build 退出码 0 | 低(只在 `sm` 以下改宽度; 宽屏逐项量过无变化) | 可 |

**测量环境的一个坑, 记下来免得下次再踩**: 这一轮两次量到"卡片数 0"、一次量到 `ERR_CONNECTION_REFUSED`, 都是因为后台起的 uvicorn / vite **被回收了**。用 `setsid nohup ... < /dev/null &` 起才稳。量之前先 `curl` 一下两个端口, 比对着空页面改半天强。
