# R433 — 个股弹窗「现状」块(结论后面); 下半块买卖判定写成草案待审

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R433 | 用户排版图「结论后面加」: 上半「现状 · 走到哪一步、在什么位置」, 下半一整套新的买卖判定(观望 / 暂不开仓、条件清单 3/6、进场区、止损与目标一二、盈亏比、进度条、到位提醒我)。下半块系统里没有, 属于判定层新增, 且「到目标就走」与硬约束第 10 条冲突, 先问了用户, 答复「先做现状, 规则写成草案给你审」。**现状**紧跟头部, 只用现成读数、各取原产地: 这一格历来(`comboHistory`, 与旧复盘页同一个函数, 天数跟头部走) / 趋势状态(六态接口, 与图上方六态条同一个查询; 「已 N 天」按 R249 的说法; 盘中口径会标出来) / 跌破转弱与站上转强(价位与后端给的带符号距离) / 通道档位(今天那一档 + 三字组合码 + 阶段, 下面是「该盯什么」—— 旧复盘页删掉前它得有个去处, R269 说它不能收) / 三档位置(三格上中下, 色用时间轴那份 `POS_FILL`, 名字用后端的 `pos_cn`)。**下半块**: 规则草案写在 `.scratch/decision-block/spec.md`(Status: needs-info), 核心建议是「做不做」直接用现成的出手时机(同一个问题只许一个答案)、「何时走」只放现成出场口径(出场线 / 生命线 / 六态转弱)、六态多头侧当硬门槛不参与计数、60 分钟那条先去掉; 列了 6 个待答问题。守卫钉住现状块里不许出现止损 / 目标 / 进场 / 盈亏比这类词, 审完之前不会被偷偷做进来。 | 新增 `frontend/src/components/stock-preview/StatusSection.tsx`、`.scratch/decision-block/spec.md`; 改 `frontend/src/components/StockPreviewDialog.tsx`; 新增 `backend/tests/test_status_section.py`(与 R434 共用, 5 条); **后端业务代码、打分系统、`strategy/`、六态 diff 全空**; 变异见 R434 那行(合计 10 个全杀); 后端 4551 passed、vitest 166、tsc / eslint / build 0; 亮暗两套出图核对 | 低 | 可(删掉弹窗里 `<StatusSection>` 那一行) |
