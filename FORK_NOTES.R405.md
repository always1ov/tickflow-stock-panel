# R405 — 关键价位新增「斐波那契二型」(帝纳波利点位);打分/六态/作者指标逐条自证未动

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|------|---------|:---:|---------|
| R405 | 用户给了一份《帝纳波利点位交易法 · 判定与显示规格》与一张详情页原型图, 并把需求收敛到一句:「我更想只当一个指标多重判断共振…放在关键价位里面当个指标」「一个按键就可以了吧」。**规格全文是一套完整交易系统**(高周期方向过滤、进场方式、状态机、分批止盈、铁轨/尖刺提前离场、信号列表页), **这一轮一条判定都没做**, 只做画在图上的位置层: 推进段 → 聚焦点 → 反应点 → 每对算 38.2%/61.8% 两条回撤 → 挤在一起的几条 = 强支撑区 → 区下方最近一条 = 失效位 → 由 A/B/C 三点推出目标一二三。**共振由用户人工判断** —— 用户原话「是否共振我自己人工判断, 不打算代码判断」; 组内几条回撤线重不重合要算(那是帝纳波利**定义**强支撑区的方式, 也是图上「N 条回撤重合」那句话的来源), 跟六态/量化通道撞不撞则一句结论都不出。**地基单独做成纯函数并先验后用**: 规格第 1 节自陈「原书对『有意义的摆点』没有硬公式…是全套系统中最需要校准的部分」, FOCUS 认错一根上面全错, 所以位移均线/分形摆点/推进段/聚焦点/反应点/聚类/失效位/首次回踩逐个拆开, 配**手算期望值**的用例(不拿真实行情当期望值 —— 那等于把当时的 bug 一起钉住)。**冒烟当场抓到一个真 bug**: 一段横盘里每根都是分形低点且都满足"其后没有更低", 五个反应点挤在同一个价位上, 量出五条**完全一样**的回撤线, 聚类报出 `strength=5` 的强支撑区 —— **那是同一条线数了五遍, 不是五段行情指到同一处**, 而后者才是这个读数的全部意义; A 股的平台、一字板天天触发它。修法是反应点按价位去重(容差与聚类同一把尺子)。**两个用例错在我自己身上, 也记下来**: 一是断言"一路走低能取到 3 个反应点"—— 单调下跌里根本不存在分形低点, 是用例错不是代码错(顺带说明了一件对的事: **下跌途中出不来做多的回撤位**, 与「逆势的机会一律不给」同向); 二是出线格式的断言没跟上后来加的 `color` 字段。**配色按规格 §12 分三种**: 回撤金 / 目标蓝 / 失效位灰 ——「每种颜色全站只表达一种含义」, 一组里三种意思不同的线全涂一个色等于把三件事说成一件; 为此给价位线加了可选的自带颜色(别的组不传, 行为一字不差)。K 线红涨绿跌, 所以价位线一律不碰红绿。**名词表梳理出一处撞名**: 我原本把 DMA3×3 那条曲线叫「短期均线」, 而 `lib/signals.ts` 里「短期均线」已经指 **MA5** —— 同名两物, 改叫「二型均线」。其余九个新名词(浅回撤/深回撤/目标一二三/失效位/强支撑区/单边上攻/首次回踩)扫过两侧渲染文本, **全是新词, 零撞名**。**用户点名要查的三样逐条自证**: `opportunity_score.py`+`score_ledger.py`、`strategy/` 全目录+作者的 `keltner*.py`/`pipeline.py`、`livermore.py`+`livermore_service.py` —— **git diff 全空**, 并且加了守卫扫这些模块里不许出现 `fib2`/`dinapoli` 字样。`levels.py` 只加了 4 行(`LEVEL_TYPES` 一个条目), **纯新增, 零删除**, 作者那 11 个计算函数一个没碰。二型也**不进 `compute_levels`** —— 走 API 层注入(与六态同一个套路), 于是 `stock_signal`/`stock_analyzer` 的 AI 提示词拿不到它, 用户要的是"图上一个指标"不是"给 AI 多一个判据"。**守卫 51 条, 24 个变异逐条确认会红。** | 新增 `backend/app/indicators/dinapoli.py`(纯函数, 无 IO)、`backend/tests/test_dinapoli_pivots.py`(34 条)、`backend/tests/test_fib2_endpoint.py`(11 条);改 `backend/app/api/stock_analysis.py`(注入那一组线 + 叠加层 + 补空数据早返回的形状)、`backend/app/indicators/levels.py`(+4 行)、`frontend/src/lib/api.ts`(类型)、`frontend/src/components/stock-analysis/AnalysisKChart.tsx`(开关/曲线/色带/标记/单线配色)、`StockLevelsPanel.tsx`(传叠加层);**`opportunity_score.py`、`score_ledger.py`、`strategy/`、`keltner*.py`、`pipeline.py`、`livermore*.py` diff 全空**;后端 pytest **4431 passed / 10 skipped 退出码 0**, 前端 tsc 0 / eslint 0 error / build 退出码 0 / vitest 107 passed | 中(新增一组价位并给价位线加了可选配色字段 —— 后者动到所有价位组共用的渲染路径, 但不传该字段时行为与以前逐字节相同) | 可(删 `dinapoli.py` + 撤 `LEVEL_TYPES` 一行 + 撤 API 注入那一段 + 前端四处; 与其余 12 组无耦合) |

**一个按键出来的是什么**(对着用户那张原型图逐项):

| 图上的 | 做了没有 |
|---|---|
| 回撤线(浅/深, 带 R1/R2/R3) | ✅ 金色 |
| 目标一 / 二 / 三 | ✅ 蓝色虚线 |
| 失效位 | ✅ 灰色 |
| 短期均线(DMA3×3) | ✅ 灰线, 界面名「二型均线」 |
| 「单边上攻 N 天」淡红底 | ✅ |
| 强支撑区金色带 + 「N 条回撤重合」 | ✅ 带最小可见高度 |
| 「首次回踩」▼ | ✅ |
| 决策卡 / 进度条 / 交易计划表 / 条件清单 / 列表页 | ❌ **有意不做** —— 那些是页面级的东西, 不属于"图上一个指标" |
| 「未来」浅底区 + 均线往右探 3 根 | ⚠️ **数据已经算好并发给了前端**(`dma3_future`), 但**图上还没画** —— 要把 x 轴往后扩几根空槽, 那会动到所有价位组共用的坐标轴。单独一轮做, 不夹在这里 |

**没有在真实行情上看过**: 这个开发环境里一根日 K 都没有(自选灌的是代码, 没有行情)。图是**把图表组件单独挂起来、喂后端真算出来的 payload** 渲染验证的(暗/亮两套都拍了, 探针文件用完即删)。**所以请你在自己的环境里打开一只票点一下那个开关**, 看看段划得对不对 —— 那四个"原书没规定、由规格作者补的"参数(`PIVOT_K=3` / `THRUST_MIN=8` / 推进≥3×ATR / `MAX_REACTIONS=5`)就是照着图调的, 觉得哪一段划得不对告诉我调哪个数。

**下一轮可选**: 「未来」区(要扩 x 轴)、把三组定高药丸收成 `SegmentedControl`(R402 留的)、阶段三 3.3 逐页迁移。
