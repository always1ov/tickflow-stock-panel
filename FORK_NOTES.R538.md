# R538 — 回测页整改: 左侧配置栏加宽(日期不折行、下拉不截断), 分栏条走共用件

| # | 改动 | 涉及文件 | 冲突风险 | 单独回退 |
|---|---|---|---|---|
| R538 | 用户「剩下的所有页面都需要整改」, 回测页看过方案图后「确认」。只改表达, 策略/参数/回测逻辑一个没动; 「验证」栏版面本来就齐、手机上本来就是上下叠, 不动。① 策略栏左侧配置栏 18rem → 21rem(288 → 336px): 回测区间两个日期原来各只剩 ~118px, 「2026-06-26」折成两行, 建仓口径「次日开盘（推荐）」被截断, 加宽后都不折不截, 右侧结果区照样吃满剩余宽度; ② 页头分栏条原是手抄的一份(与 PageTabs 逐字相同), 换成共用 PageTabs, 长相不变, 老链接(tab=factor / mining)兼容照旧 | frontend/src/pages/Backtest.tsx; frontend/src/pages/backtest/StrategyBacktest.tsx; backend/tests/test_backtest_r538.py(新) | 低: 只改一个网格列宽与分栏条 | 可以: git revert 本提交 |
