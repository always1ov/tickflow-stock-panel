# CONTEXT

本仓库的**共用语言**。agent 探索代码前先读这里，让说出口的话和代码里的词是同一套。

这是 fork 增强部分的词汇；上游本身的架构、数据契约、测试矩阵在
[`CONTRIBUTING.md`](CONTRIBUTING.md) 与 [`docs/secondary-development.md`](docs/secondary-development.md)。

---

## 这是什么

A 股看盘 / 决策面板。用户是**一个人**，自己盯盘、自己下单。系统不替他下单，
只回答三件事：**今天能不能出手、出手买谁、手上的该不该走**。

---

## 时间口径 —— 全仓库最要紧的一条区分

| 词       | 含义                                     |
| -------- | ---------------------------------------- |
| **收盘口径** | 用已收盘的日线算。**所有结论、纪律线、评分都走这个** |
| **盘中口径** | 用实时价算。只做**方向性预警**，永远不产出结论   |

用户的原话是「看我肯定是看收盘价，但是盘中我会一直盯着」。所以把握分**冻在收盘口径**
（盘中一动不动，是稳定的决策基准），盘中的变化单独摆一份（`live` 字段），一分不进评分。
两者混在一个数字里会得到"既不是收盘也不是实时"的半新半旧判定 —— 这个坑踩过（R137）。

---

## 决策链的词

### 六态趋势（利弗莫尔）

日线收盘价判定的六个状态。**多头侧** `BULLISH = {UT, NR, SR}`（上涨趋势 / 自然回升 /
次级回升），空头侧 `{DT, NREA, SREA}`。「**转多**」「**回升**」是当天刚发生的**信号**，
不是状态本身。

### 生命线

**MA20**。跌破即无条件清仓，这是纪律不是建议。它同时是把握分的**硬门槛**之一。

### Keltner 三档通道

| 档       | 公式             | 用途             |
| -------- | ---------------- | ---------------- |
| 短期 `s` | MA20 ± 2.0×ATR14 | 操作级别，高抛低吸 |
| 中期 `m` | MA60 ± 2.5×ATR14 | 一个季度的位置     |
| 长期 `l` | MA120 ± 3.0×ATR14 | 牛熊边界          |

`pct_in_channel`：0 = 贴下轨，1 = 贴上轨。**一个恒等式要记住**：
`收盘 ≥ MA20 ⟺ pct_in_channel ≥ 0.5`（短期通道以 MA20 为中轴）。

位置分五档，措辞在 `indicators/keltner.py` 的 `POS_CN`：
**破上轨 / 贴上轨 / 通道内 / 贴下轨 / 破下轨**。别自己另造说法，也别另立百分比阈值 ——
决策台「短通道」列写「贴上轨」的那天，别处也必须是贴上轨。

### 把握分（v2）

`services/opportunity_score.py`。**三道硬门槛 + 三维度加权**：

- 门槛（纯价格判据，可回测）：`trend_side` 六态多头侧 / `lifeline` 生命线 / `long_down` 长期下跌趋势
- 维度：**趋势 45% + 量能 30% + 位置 25%**
- 各维度内部因子曲线是**区间最优**的分段线性（量比峰在 1.3~2.5，位置甜区 50~65%）。
  不是"越大越好" —— 量比 5 说明这波已经走完了

**缺维度不给 0**：整档数据缺失时在剩下的维度上重归一化，并把 `partial` 标出来。
"数据越少分越高"是必须堵死的口子。

### 注记·不计分

挂在候选上给人看、**一分不加一分不减**的东西：主线归属、历史胜率、
通道档位、策略命中、龙虎榜。（AI 信号原来也在这一栏，R435 起整套停用，
以后打算用斐波那契二型重做那一块。）刻意不给权重字段 —— 一旦有权重，下一步就会有人把它
加回分数里，那就退回 v1 那个八项加减的黑箱了。

### 出手时机

`services/action_timing.py`。每只机会一句结论：**今天动手 / 收盘再动 / 不动手**。
把握分答的是"这票值不值得进候选"，出手时机答的是"今天这一天能不能下手"。
它是注记，不改名次。阈值全部对齐把握分的曲线常数，不另立口径。

### 姿态

`进攻 / 谨慎 / 防守 / 观察`，各自对应总仓位基调上限（`POSTURE_CAPS`）。
由大盘模式与自选广度**取更保守者**合成。

### 焦点名单

`services/focus_list.py`。回答"自选 150 只，谁值得推送通知"。四档：
**持有 / 计划中 / 贴轨 / 观察**。前三档过推送门，观察档只写触发记录不打扰人。
用户可**钉住**（永远推）或**静音**（永远不推）。

判定盖章在告警事件上（`focus_muted`），落盘 / SSE / 系统通知 / Webhook **四条出口
认同一个章** —— 不能出现"弹窗拦了、企微却推了"。任何异常一律**放行**：
宁可多推，不能因为一个辅助名单把出场线告警吞掉。

### 出场线

`services/position_exit.py`。ATR 三阶段：**止损 / 保本 / 移动止盈**。跌破自动推送。
`fatal` = 跌破生命线，无条件清仓。

### 庄现

用户通达信副图「主力捉妖」里的一个信号，逐行复刻在 `indicators/zhuang_xian.py`：上升趋势里的
深回调刚拐头（9 日 KDJ 的 J 由负转正，且 10 天前收盘在 60 日线上方）。**画在量化MACD 副图底边，
一个狗头**。它是独立冻结的指标，只是借量化MACD 那张图显示 —— 量化MACD 以后怎么调，庄现都不跟着变。
性质同「注记·不计分」：不进把握分、不推送、不碰六态。出现后仍要自己看生命线。
同一副图里的「拐点」「确认」没有移植（要用通达信的获利盘比例与未来函数，系统里对不上）。

### 趋势量化

用户给的通达信副图公式，逐行复刻在 `indicators/trend_quant.py`，画在关键价位图的**主图与量化MACD
之间**。一条 0~4 之间摆动的平均线（带红绿短柱）加八种字：极底、升、顶、下、建仓、逃、见底、绝底，
另有「吸筹」白柱。它是独立指标（不引用量化MACD / 庄现），只出位置与标记，不进把握分、不推送、
不碰六态。几样原文没写颜色的东西（平均线、吸筹线、升 / 顶 / 下三个字）暂取白 / 浅灰，待对照通达信截图校准。

### 台账

`services/score_ledger.py`。每天落一份候选池快照，事后回填各持有期收益 ——
这套评分有没有区分度，只能靠事后记录回答，不能靠感觉。用户每周把导出的 CSV 给
assistant 调参。**样本不足 10 个 / 前后半段不一致时不动权重。**

---

## 今日总览的四个区

`api/today.py::_build_overview` 产出，是决策汇聚层，也是根路径 `/` 的落地页。

1. **行动区** —— 卖出与风险，**永不受任何门槛过滤**
2. **机会区（值得关注）** —— 买入候选，用户可调门槛
3. **市场天气** —— 姿态 + 主线 + 中观资金
4. **持仓体检** —— 逐票 + 组合层（总仓位 vs 姿态上限、净值回撤纪律线）

---

## 数据源

**只用 TickFlow** 是项目原则。唯一例外是**境外指数**（`services/global_indices.py`），
它自成一体：不进能力路由、不碰 QuoteService、不落盘，挂了也只坏侧栏那一张卡。

境外指数挑数据源**按行情自带的时刻挑最新的**，不按候选表排名 —— 一家把上次收盘挂着
不动也算"能出数"，按排名挑就永远轮不到真在跳的那家（R148 踩过）。

---

## fork 自己的词

- **R 系列 / 台账**：`FORK_NOTES.md` 里每个改动一行，记 改动 / 涉及文件 / 冲突风险 / 单独回退。
  这是本 fork 的权威变更记录
- **上游基线**：`FORK_NOTES.md` 顶部记着当前对齐到上游哪个 commit。同步上游后要前移
- **脱敏分支**：`deploy` 分支不含任何真实域名/IP/路径，真值只在 Dokploy 面板的 Environment 里
